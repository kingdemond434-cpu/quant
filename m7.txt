research: live-clock revalidation + graveyard resurrection engine + mechanism graph

Principal Tier-1 items #2/#3/#4 delivered. No new alpha claimed; this is validation
infrastructure and research organisation.

#3 LIVE CLOCK RE-VALIDATION (scripts/revalidate_clocks.py)
  Every live axis re-run through the hardened harness + the shift-sensitivity test that
  killed bithumb.
  - KIMCHI: shift test CLEAN (-1d +0.023 | 0d +0.243 | +1d +0.822). The leak signature is
    shift0 ~= shift+1 (bithumb: 0.722 vs 0.694); kimchi shows 0.243 vs 0.822 = healthy, the
    future data is far more informative than the signal. implausible_leak=false.
    BUT the brain's NEW rail ic_exceeds_contemporaneous fires (fwd IC 0.243 > same-period
    0.119) and n_eff=121 / min_detectable_ic=0.178 / powered=false. Flagged for brain review:
    that rail may be too strict for genuinely LEADING signals (a capital-flow signal is
    supposed to have low contemporaneous and higher forward correlation).
  - STABLECOIN SUPPLY: shift test clean, no lookahead pattern.
  - CNY PREMIUM: 5 rows, 0 with usable z20 -- forward evidence has NOT started (20d warmup).
    "ACCRUING 1/40" overstates its true progress.
  - Self-corrected a FALSE-POSITIVE in my own diagnostic: the leak condition was written
    backwards (flagged s[+1] > s[0], which is normal healthy behaviour).

#2 GRAVEYARD RESURRECTION ENGINE (scripts/graveyard_resurrect.py)
  Different deaths need different treatment. Classifies every graveyard entry by CAUSE and
  ranks resurrection priority 0-5 with a prescribed treatment.
  DEAD (0): construction/mechanism failures -- lookahead, timing-artifact, unstable, no_economics.
  PRIME (5): killed at DAILY horizon only, or underpowered -- the exact remedies that now exist.
  Finding: the horizon-search shortlist is 2 entries, BOTH killed this session at a single
  daily horizon -- multilingual_wikipedia_attention and defi_health. Attention diffusion and
  TVL rotation are slow processes tested on a fast clock = likely FALSE NEGATIVES.

#4 MECHANISM GRAPH (docs/research/MECHANISM_GRAPH.md)
  Research-organisation layer, consumes no alpha-testing budget. Binding rule: every hypothesis
  entering the EV gate must name its mechanism node; one that cannot is a curve-fit.
  M1 liquidity expansion: 7/7 nodes observed = SATURATED (test links, not new sensors).
  M2 regional capital control: real but needs strict controls -- only KR/CN qualify.
  M3 participant behaviour: CLOSED, refuted at power.
  M4 information diffusion: OPEN and MIS-TESTED -- inherently slow, always tested at 1 day.
  M5 reflexivity/feedback: OPEN and UNBUILT -- all inputs already on disk, loop never modelled.
