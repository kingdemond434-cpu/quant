UNTESTED: llm_code_auditor + llm_blind_researcher (written for the brain to verify)

*** BOTH SCRIPTS ARE UNTESTED. OpenRouter is at 402, so neither has ever executed. The brain must
run each once and CHECK THE OUTPUT before anything relies on them. Prior evidence says assume they
are buggy: the two validators I DID test today (data_sanity, screen_auditor) both shipped with
high false-positive rates and needed fixing before they were readable. ***

1) scripts/llm_code_auditor.py -- adversarial review of the desk's own recent diffs.
   JUSTIFIED BY MEASUREMENT, not theory: on 2026-07-27 I shipped and self-corrected NINE defects,
   several in safety-critical machinery -- an activation gate that FAILED OPEN and authorised
   itself, a budget guard reading keys that do not exist, a leak test written BACKWARDS, a
   mechanical z>=2.0 cutoff that mislabelled a genuine replication, a 27x cost error that caused a
   402 mid-run, double z-scoring that collapsed every signal to zeros, and a futures filter read
   via the spot key that understated a capacity floor.
   I caught them by re-reading. The point of an external auditor is not speed -- it is that I
   CANNOT AUDIT MY OWN BLIND SPOTS.
   The prompt encodes THIS taxonomy (fail-open guards, wrong key names, inverted conditions,
   mechanical thresholds, double transformation, unit errors, missing gates, lookahead) rather
   than generic "find bugs", because these are the defects this codebase actually produces.
   Instructed to output NO-FINDINGS rather than invent: a false finding costs more than a missed
   one. Cross-seat agreement is the signal; a single seat is a hypothesis. Every finding must be
   REPRODUCED before acting.

2) scripts/llm_blind_researcher.py -- measures what the desk's own framing cannot see.
   FIXES A FLAW IN MY OWN DESIGN: every LLM role built today CONSUMES THE DESK'S FRAMING -- the
   breadth expander gets our class map and the six lenses I wrote, the collector author gets our
   template, the panel gets a dossier we compile. All three can only expand WITHIN our conceptual
   boundaries, inheriting our blind spots by construction. That is exactly the failure L6 exists
   to prevent, reintroduced one level up.
   This role receives NOTHING -- no map, no lenses, no graveyard. Just "you are a quant entering
   crypto today with public data; what would you study?" The answer is then DIFFED against the
   desk's vocabulary (class map + mechanism graph + graveyard + script names).
   THE DIFF IS THE PRODUCT, not the suggestions. An anchored model gives better sources; only an
   UNANCHORED one can reveal what our framing excludes. A topic named by MULTIPLE independent labs
   yet absent from our map is the strongest blind-spot signal this desk can generate.
   MUST always run cold -- feeding it desk context destroys the only property that makes it useful.

Still not built (need credits AND yield data that does not exist yet): prompt-evolution engine,
source-usability triage, model blindness matrix, disagreement/anti-consensus engine.
