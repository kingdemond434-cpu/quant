# Demo / paper environment configuration (no live capital).
# Used by scripts/run_demo.py. The full stack runs on a replay feed + PaperBroker.
demo:
  capital: 100000.0
  default_price: 100.0
  drawdown_alert: 0.10
  max_ticks: 1000
  symbols:
    - EURUSD
    - XAUUSD
    - US500
  db_path: data/sor_demo.sqlite
