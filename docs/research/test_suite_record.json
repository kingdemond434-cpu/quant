{
 "max_collected": 523,
 "at": "2026-08-06T07:01:00.103739+00:00",
 "note": "high-water mark of COLLECTABLE test modules; ratchets UP only. A suite may never quietly shrink -- deleting a test is a decision, not a side effect.",
 "_merge_note": "2026-08-09 merge: claude 362 vs codex 523 -> 523. This record ratchets UP only, so a merge takes the MAX; picking a side would let the suite quietly shrink, which is the exact thing it exists to prevent. The next full run raises it again, because the union of both branches collects more than either."
}
