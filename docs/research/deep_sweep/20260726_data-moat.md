# DEEP COLD AUDIT — DATA-MOAT SUBSYSTEM — 2026-07-26

_Auditor: weekly deep cold sweep (data-moat seat). Scope: NOT "what data do we have" but
"what information can only exist because of how WE combine data" — proprietary
transformations, composed/hierarchical features, graph representations, embeddings, event
reconstruction, market-state fingerprints, research memory, experiment lineage, feature
ancestry, alpha genealogy. Method: OUTCOME-NOT-CONFIG — every claim carries its proving
command + output. Read-only sweep._

## SCORES

- **current_capability_pct: 45%.** The CAPTURE half of the moat is strong (live proprietary
  tape, audited screen harness, preserved negative record, live memory/conversion ledgers);
  the COMBINATION half — the seat's actual charter — is thin: no interaction engine, no event
  studies, no tape-derived features, genealogy stack dead, novelty gate unwired, regime labels
  destroyed daily.
- **practical_ceiling_estimate: 85%** of what a solo-operator, free-first desk can run
  sustainably. **ceiling_gap: 40 pts.**
- **opportunity_cost_1y: HIGH** — quantified in §3: plausibly 1-2 validated axes forgone
  (own-tape, interactions, events all unscreened), a year of unrecoverable regime labels, and
  an unmeasured redundancy tax on shared DSR budget.
- **confidence: 0.8** on findings (all command-verified today), **0.6** on the impact ranking.
- **unknown_unknown_score: 0.40** — the store CONTENTS are now mapped; store USAGE (who reads
  what at generation time) is not, and prompt-organ behavior is invisible to code audit.
- **info_gain_if_investigated: HIGH** for E1/E2/E4 — each produces a permanent number
  (tape verdict, gate recall, effective-trial audit) whichever way it comes out.
- **expected_alpha_contribution: MED-HIGH direct** (three untested mechanism-backed families
  from already-owned data) — the cheapest discovery surface the desk has.
- **expected_compounding_contribution: HIGH** — O2/O4/O6 each multiply every future campaign
  (protected multiplicity budget, regime-conditioned everything, auditable trial accounting).
- **CEILING EXPANSION:** the 85% ceiling is ORGANIZATIONAL (one operator's triage bandwidth),
  not technological — local embeddings + retrieval over the desk's own memory would raise it
  by making accumulated knowledge self-serving rather than operator-served. Lifting condition:
  when research_memory crosses ~1k rows or a second concurrent operator/organ regularly needs
  recall, build retrieval; below that, the flat files are honestly sufficient.

## 1. WHAT WE KNOW — validated strengths (each with proving command)

**S1 — A live proprietary L2 order-book tape exists and is current (the raw half of a moat).**
`du -sh data/moat/*` → bybit 2.8G, fut 509M, spot 235M, execution_tape 128K. `find data/moat
-type f -newermt 2026-07-01 | wc -l` → **9,581 files this month**, newest `20260726_10.jsonl.gz`
(current hour). Sample row (`zcat .../spot/BTCUSDT/<latest> | head`) = top-20 bid/ask depth
snapshots at ~8s cadence with exchange update-ids. 30 spot + 30 fut symbols (`ls data/moat/spot |
wc -l` → 30), ~20 bybit. Recorders are self-healing (`crontab -l`: `ensure_recorder.py` +
pgrep-respawn every 10 min). Nobody sells this desk's exact venue/symbol/epoch tape back to it
later; every day recorded is unrepeatable. Consumers exist: `grep -rln 'data/moat' libs scripts`
→ `execution_tape.py`, `run_trade_forensics.py`, `run_cost_model.py`, recorders, max_audit.

**S2 — The Stage-A axis-screen harness is genuine methodological moat, not commodity code.**
`libs/research/axis_screen.py` (145 lines, read in full): z-score → IC → momentum/reversal
Sharpe with **horizon-correct annualisation** (fixed 2026-07-26 after "pure noise on 20d returns
scored 0.55 against a 0.5 floor"), the **angle-20 decontamination gate baked in** (same-period
corr >0.20 or residual-IC collapse ⇒ TIMING-ARTIFACT — the class that killed Coinbase- and
Turkey-premium), a **lookahead rail** (ic_ceiling/sharpe_ceiling + ic_exceeds_contemporaneous
diagnostic — caught the bithumb IC-0.72/Sharpe-10 fake), **power analysis** that separates
SCREEN-WEAK (graveyard-grade) from SCREEN-UNDERPOWERED ("could not tell" — never recorded as
refuted), and **panel-width correction** (139-symbol panel: t=3.5 apparent is t=0.35 real).
This encodes hard-won epistemic discipline no vendor ships; it is the desk's most defensible
transformation asset.

**S3 — Research memory was a null pipe and is now a live organ (0 rows ever → 122 rows).**
`scripts/research_memory.py` docstring admits "research_memory had 0 rows EVER … null pipe";
fixed 2026-07-24. Outcome check: `sqlite3 data/sor_research.sqlite` → research_memory=**122**,
latest row 2026-07-26T01:03Z (last night), breakdown construction/failure=93,
hypothesis/failure=17, method/success=7, dataset/success=3, dataset/failure=1. Negative results
dominate — exactly what the free-multiplicity-budget doctrine wants. Rows carry
failure_cause/failure_stage/lessons/metrics_json/predecessor_id (lineage-capable schema).

**S4 — Stage-B forward clocks are live, append-only, and honest about promotion.**
`stat data/axis_shadow_state.json` → updated 2026-07-26 08:32 (today). Content: per-axis
verdicts (cny_premium "ACCRUING, need 40 forward days") with the note "P&L starts at the clock's
first row, never the screen sample. ELIGIBLE … is NOT an automatic deployment." The
screen→clock→promotion boundary (two-stage law) is real in the artifacts, not just the doctrine.

**S5 — The 420/0 negative campaign is fully preserved with per-candidate forensics.**
`sqlite3 data/sor_crypto.sqlite` → research_candidates=**420**, every row carrying family,
subtype, symbol, params_json, content_hash, mechanism, annual_sharpe, dsr, pbo, reality_p,
oos_sharpe, capacity_usd, fragility, rejection_reason (e.g. seq 420 TRUMPUSDT cross_asset:
"failed: expected_value, cpcv, walk_forward, dsr, pbo, reality_check, capacity, fragility").
Plus `data/information_value.jsonl` (159KB): per-reject information-bits accounting
(info_bits≈0.2345/reject, family-tagged). The desk's most cited fact ("420 price-family
hypotheses, 0 survivors") is backed by queryable rows, not folklore. 57 + 49 + 49 more
candidates in sor_autodiscovery / lake / lake_v2. This is permanent negative knowledge a
competitor would have to re-burn compute to acquire.

**S6 — A daily market-state fingerprint is produced (simple, but live and self-checking).**
`stat data/crypto_regime.json` → written 2026-07-26 00:04 (today):
`{"regime":"trend:bear / vol:low_vol","btc_60d_mom":-0.1354,"rv14_ann":0.314,
"hmm_regime":"bull/low_vol","regime_confidence":1.0,"hmm_gmm_agree":false,…}` — rule-based +
HMM + GMM with an explicit agreement flag. `libs/regime/` holds hmm/gmm/bayesian engines.

**S7 — Conversion meta-accounting (data→alpha ratio machinery) runs daily and steers generation.**
`stat data/mine_conversion_log.jsonl` → updated 2026-07-26 11:34 (this hour); dispositions
(wired/deferred, tiers) per mined find. `libs/research/mine_conversion.py` computes class-level
conversion priors fed back into what gets mined (`class_priors`, `priors_payload`, starve-list
under 25%). The §33/§34 ratchet files exist and are current (`docs/research/conversion_record.json`
in today's git status). The moat META-layer — knowing which acquisition classes convert — is
itself proprietary accumulated knowledge.

**S8 — The one validated new edge came from a COMPOSITION, proving the moat thesis empirically.**
Kimchi premium = Upbit KRW price / Binance USDT price / USDKRW FX — three sources composed into
one series no single vendor provides at this alignment. Doctrine records IC +0.148, momentum
timing Sharpe 1.3, beating every single-source price sleeve (420/0). Artifacts:
`data/kimchi_premium.jsonl` exists with a live collector (`scripts/collect_kimchi_premium.py`),
and the axis sits in the Stage-B clock state (S4). The desk's own evidence says: edge lives in
cross-source composition — i.e., in exactly the layer this audit covers.

## 2. WHAT WE DON'T KNOW — ignorance ledger

**U1 — THE NULL-PIPE CLUSTER (adjacency sweep on the research_memory failure shape).** The
desk's hardest-won recent lesson was research_memory: "table existed, 0 rows EVER." Running
that exact failure SHAPE (designed store, zero output) across every moat store found FOUR more
instances:
  a. **trials_ledger: 0 rows in every database.** `sqlite3 <db> "select count(*) from
     trials_ledger"` → 0 in sor_research, alpha_registry, sor_crypto, sor_autodiscovery, both
     lakes. Yet `libs/store/trials.py` opens with "Nothing is validated without a ledger entry
     first" and is hash-chained/tamper-evident, and `libs/validation/gauntlet.py:89` falls back
     to **n_trials=2** when no ledger and no override is supplied (a near-toothless DSR bar for
     any caller that forgets both). The 420-run DSR values came from per-run accounting inside
     `libs/discovery`, not this ledger.
  b. **Alpha genealogy fields: 100% None.** All 8 alpha_cards have
     predecessor_id=successor_id=None; dsr/pbo/cpcv/walk_forward/holdout columns also all None
     (validation stats live only in extra_json fragments).
  c. **findings_ledger.json: 1 row ever** (F0001, 2026-07-21) — the panel-finding lifecycle
     store is barely used.
  d. **v1 campaign queue: 340 rows "queued" frozen since 2026-06-22** (`select status,count(*)
     from campaigns` → done=7, leased=1, queued=340) — phantom backlog any reader could mistake
     for live work.
  What we don't know: which of these are conscious deferrals vs silent breaks. GAP_ANALYSIS.md
  documents a REASONED deferral of the heavy alpha_factory layer ("do not resurrect until >5-10
  validated survivors") — that covers (b) honestly. It does NOT cover (a): trial multiplicity is
  being spent NOW (526 candidate rows + digger screens), so the empty trials ledger is a live
  accounting gap, not a deferred luxury.

**U2 — Whether any organ actually runs the mandated novelty gate.** `grep -rn 'PriorIdea('
libs scripts app` → constructors exist ONLY in the lib and `scripts/run_generation_roi_test.py`
(a test). No novelty verdict artifact exists anywhere in data/ or docs/. The universal duty
says "screen EVERY new hypothesis against the graveyard via hypothesis_novelty" — prompt-driven
organs may be doing this in-context by reading docs/graveyard.md, but there is no artifact
proving it, and the deterministic gate demonstrably has no production caller.

**U3 — The desk cannot currently audit effective-vs-raw trial counts (gate-optimality duty).**
The per-gate accept/reject histogram is reconstructable from research_candidates rows
(rejection_reason strings), but nobody computes it, and per-run n_trials inputs are not
persisted anywhere queryable. Whether the DSR bar is fed the honest cumulative count is
unverifiable today.

**U4 — Whether the tape's granularity (top-20 depth @ ~8s) carries alpha at tradeable
horizons.** Proven useful for execution/cost (consumers exist); NEVER screened for alpha —
`grep -rn 'depth_imbalance_signal|book_imbalance' libs scripts` → **zero callers** outside the
defining module. The assumption "our most proprietary dataset contains research value" is
untested in either direction; so is its negation.

**U5 — Standing alignment health of composed series.** Every composition (kimchi = Upbit×
Binance×USDKRW) carries cross-source timestamp risk; Stage-A checks it ONCE at screen time
(angle-20, lookahead rail), but nothing monitors alignment drift on the LIVE collectors.
Today's kimchi row reads z20=+2.868 (`tail -1 data/kimchi_premium.jsonl`) — a near-3σ reading
whose realness depends exactly on FX-vs-crypto close alignment staying sound. No monitor would
catch a silent FX-staleness regression.

**U6 — Which regime the market is actually in, per our own fingerprint.** crypto_regime.json
(today): rule engine says trend:bear, HMM says bull/low_vol, hmm_gmm_agree=false,
regime_confidence=1.0 despite the disagreement. The file is a 293-byte snapshot overwritten
daily (`scripts/classify_regime.py:20` → single _OUT path): **the desk keeps NO history of its
own regime labels**, so disagreement rate, label stability, and regime-conditioned research on
own fingerprints are all impossible without recomputation.

**U7 — The usage side of the moat is unmapped.** We now know what every store CONTAINS; we
don't know who READS what (which organs consult negative_knowledge.md or graveyard.md at
generation time; whether information_value.jsonl is ever read back — its `lesson` fields are
all empty strings). Suspected unknown-unknown: knowledge that exists on disk but is never
retrieved is functionally identical to knowledge that doesn't exist.

## 3. WHAT COULD MATTER MOST — ranked opportunities

Ranking = expected impact × confidence / (cost × maintenance). Compounding multipliers flagged ◆.

**O1 — Screen the desk's own tape: depth-imbalance Stage-A screen from data/moat.** [rank 1;
direct alpha + kills a standing defect]
- What: a loader that aggregates data/moat depth snapshots into an hourly/8h book-imbalance
  panel (the code half-exists: `libs/research/microstructure.py` was written FOR this and has
  zero callers), then `stage_a_screen()` it against H8/D1 returns with a stated mechanism prior
  (resting-depth pressure → short-horizon continuation), full trial logging.
- Why: the desk's MOST proprietary dataset (universe map lists pre-recorder L2 as
  "destroyed at source; recorder solves forward") has produced **zero research artifacts** in
  ~1 month of recording. This is the DATA-UTILIZATION law's named failure (data paralysis) on
  the desk's crown-jewel asset. The one validated edge (kimchi) came from exactly this move:
  screen an untouched axis.
- Evidence: S1 (tape live, 9,581 files), U4 (zero callers), sibling data-intelligence sweep
  independently recommends a "moat-extraction pilot" — two blind auditors converged.
- Complexity: LOW (1 script; gzip→pandas→resample). Dependencies: none. Validation: the
  audited harness itself. Failure modes: 8s/top-20 too coarse → SCREEN-WEAK/UNDERPOWERED is
  still a first-class negative deliverable (powered-flag decides which). ROI: unknowable
  ex-ante; option value high, cost ~hours. Confidence it's worth running: 0.9.
- Maintenance: near-zero (screen is one-shot; collector already runs). Monitoring: clock file
  if SCREEN-INTERESTING. Retirement: after verdict, graveyard or clock — either way done.
- Horizons: 1w build+screen; 1m clock accrual if interesting; 3m+ the tape length itself
  becomes the moat (longer history = more powered screens nobody else can run).

**O2 ◆ — Wire the novelty gate into live generation + build ONE canonical machine graveyard.**
[rank 2; compounding multiplier]
- What: (i) a builder that compiles PriorIdea rows from docs/graveyard.md (11+ class kills with
  lessons) + research_memory failures (110 rows) + rejected candidates (526 rows,
  family/subtype/params as features) into `data/graveyard_priors.json`; (ii) call
  `hypothesis_novelty()` in the factory candidate loop and expose the same check as a one-line
  CLI for prompt-organs (diggers/miners) with a logged verdict artifact.
- Why: every re-test of dead ground burns DSR budget for ALL concurrent candidates (multiplicity
  is shared). The gate exists (108 lines, audited, deterministic), the doctrine mandates it, and
  nothing calls it (U2). The graveyard currently lives in 4 disjoint substrates (doc table,
  partial web/discovery.json, research_memory, 4 sqlite DBs) — none in the shape the gate eats.
- Complexity: LOW-MED. Dependencies: none new. Validation: replay the 420 campaign through the
  gate — measure how many of the later rejects the gate would have flagged as redundant vs the
  earlier ones (a real recall number, not a vibe). Failure modes: token-Jaccard false-positives
  blocking genuinely-new work → gate stays ADVISORY (by design, returns lesson text).
- ROI: protects every future screening dollar; also makes the §33 novelty duty auditable.
  Confidence: 0.85. Maintenance: builder re-runs in the daily cycle (~seconds). Retirement: if
  replay-recall proves <10% (gate catches nothing), record that and drop to doc-only practice.

**O3 — Systematic mechanism-priored cross-axis interaction screen.** [rank 3; direct alpha]
- What: the extraction law explicitly demands "cross-feature interactions"; `grep -rn
  interaction libs/research libs/features` → **no implementation exists** (only audit-prompt
  text). Build a bounded interaction pass: for each PAIR of live/screened axes with a stated
  joint mechanism (e.g. kimchi × funding regime; stablecoin-supply × breadth; OI × regime
  state), screen product/conditional constructions through stage_a_screen with every cell
  logged as a trial.
- Why: composition is the desk's only proven edge class (S8); pairwise composition of ALREADY-
  OWNED axes is the cheapest untested orthogonal family; regime-conditioning is free once U6's
  history exists.
- Complexity: MED (a for-loop over aligned panels + the existing harness; the discipline is the
  work — mechanism priors first, full cell logging, no best-cell cherry-picking).
- Validation: harness verdicts + trial ledger rows. Failure modes: combinatorial p-hacking if
  quota-driven → hard rule: no pair without a WRITTEN mechanism prior; every cell logged.
  ROI: med-high. Confidence: 0.7. Retirement: after the priored pair list is exhausted; new
  pairs only as new axes land.

**O4 ◆ — Append-only history for the regime fingerprint (and any daily snapshot organ).**
[rank 4; trivial cost, compounding]
- What: classify_regime.py additionally appends its daily JSON line to
  `data/crypto_regime_history.jsonl` (one line/day). Backfill is already possible from bars
  (the engine is deterministic) — one script, one run.
- Why: U6 — the desk currently DESTROYS its own daily market-state fingerprint by overwrite;
  regime-conditioned research (which regime kills which sleeve; disagreement-rate as a signal)
  needs the labels the desk already computes and throws away. Also fixes "hmm_gmm_agree=false
  with confidence=1.0" being invisible: disagreement-rate becomes measurable.
- Complexity: TRIVIAL (3 lines + optional backfill script). ROI/effort ratio: highest on this
  list. Confidence: 0.9. Failure modes: none material. Retirement: never (it's an archive).

**O5 — Event-study harness over owned event data.** [rank 5; new orthogonal family]
- What: `grep -rn event_study libs scripts` → nothing. The desk already owns event streams:
  unlock_events.json, listings.jsonl, liquidations.parquet, plus its own tape around each
  event. Build one aligned-window event-study function (pre/post abnormal return vs matched
  control) and run the three obvious mechanism-backed studies (unlock supply shocks, listing
  attention, liquidation-cascade reversal).
- Why: event reconstruction is a moat class this audit is chartered to check and it's empty;
  the data is already collected and idle (data-paralysis shape again).
- Complexity: MED. Validation: stage_a-style verdicts on event-conditioned signals; every
  study a logged trial. Failure modes: tiny event counts → power discipline already exists
  (SCREEN-UNDERPOWERED). ROI: med. Confidence: 0.6.

**O6 — Decide the trials-ledger question honestly: populate it or retire it.** [rank 6;
integrity/audit multiplier ◆]
- What: EITHER factory/screen paths write trials_ledger rows (the hash-chain is built and
  tested), OR formally record (decision ledger) that research_candidates rows + per-run
  overrides ARE the desk's trial accounting, delete the "nothing is validated without a ledger
  entry first" claim from libs/store/trials.py's docstring, and raise gauntlet's no-ledger
  fallback from n_trials=2 to a fail-loud error. Plus: a small computed view (script) that
  reports effective-vs-raw trial counts per campaign from candidate rows — the gate-optimality
  duty's named audit, currently impossible (U3).
- Why: a tamper-evident ledger that is empty while its docstring claims it is load-bearing is
  exactly the config-vs-outcome lie the doctrine hunts. Either outcome is fine; the current
  limbo is not.
- Complexity: LOW. Confidence: 0.85. Failure modes: double-writing trials in two substrates →
  pick ONE.

**O7 — Registry hygiene: mark the v1 campaign queue terminal; sync the graveyard machine copy.**
[rank 7; cheap]
- What: set the 340 frozen "queued" campaign rows to a terminal status (or archive the table)
  with a one-line audit_log note; regenerate web/discovery.json to actually mirror the full
  graveyard table (today it carries sleeve results only — `python -c "json.load(open(
  'web/discovery.json'))"` → 6 keys, no per-hypothesis kill rows).
- Why: phantom backlog misleads any organ (or model) that reads the DB; a "machine copy" that
  isn't one silently breaks O2's builder assumptions. Complexity: TRIVIAL. Confidence: 0.8.

**Opportunity cost of NOT acting for 1y (top 3):** O1/O3/O5 unstarted means the three cheapest
untested orthogonal families (own-tape microstructure, interactions, events) stay untested — at
the desk's measured base rates (1 validated axis per ~4-6 weeks of new-axis screening; each
validated axis ≈ IC 0.1+ / timing Sharpe ~1 class), leaving ~3 candidate families unscreened
for a year plausibly forgoes 1-2 validated axes — the exact commodity objective #2 maximizes.
O2 unbuilt means every future campaign keeps paying redundancy tax on shared DSR budget
(unquantified today — measuring it IS O2's validation step). O4 unbuilt costs a year of regime
labels that can never be recollected (same "destroyed at source" logic the desk already accepts
for the tape — this one is self-inflicted).

## 4. WHAT WE TEST NEXT — concrete experiments

**E1 — Tape-alpha pilot (executes O1).** Build `scripts/build_depth_panel.py` (moat gz →
hourly imbalance panel, 30 fut symbols), screen via `stage_a_screen(..., horizon_days=1/3,
panel_width=30)` with mechanism prior written FIRST in AXIS_PREREGISTRATIONS.md.
Success criterion: a VERDICT (any of the five) with powered=True; SCREEN-INTERESTING starts a
clock file. Validation: the audited harness; both H8 and D1 cells logged as trials.
Retirement: verdict delivered = experiment done (negative is a deliverable; graveyard with
mechanism if weak).

**E2 — Novelty-gate recall replay (validates O2 before wiring it).** Compile priors from
graveyard doc + research_memory + the FIRST 210 candidates of the 420 run; replay the LAST 210
through `hypothesis_novelty()`; measure what fraction of actual rejects would have been flagged
redundant pre-compute (and what fraction of the 3 later shadow-eligible sleeves would have been
wrongly blocked — false-positive check, must be 0 at threshold 0.7).
Success: recall ≥25% with 0 false blocks → wire it into the factory loop. Recall <10% →
record the negative, DON'T wire it (gate stays doc-practice), and log the lifting condition
(richer feature tagging on priors).

**E3 — Regime-label archaeology (executes O4's backfill).** Recompute daily regime labels
2024→today from bars with the existing deterministic engines; write
crypto_regime_history.jsonl; report label-flip rate and hmm-vs-rule disagreement rate by
epoch. Success: history file + one table in the cycle report; immediate consumer = O3's
regime-conditioned interaction cells. Retirement: never (archive).

**E4 — Effective-vs-raw trial audit view (executes half of O6).** One read-only script:
per-campaign candidate counts, per-gate kill histogram from rejection_reason strings,
implied n_trials vs the DSR inputs recorded in candidate rows. Success: the gate-optimality
duty's named audit runs in CI/max_audit; any campaign whose DSR n_trials < its own candidate
count fires a defect. This also settles U3 with numbers.

**E5 — Alignment sentinel for composed series (closes U5 cheaply).** Daily assert on each live
composed collector: source-timestamp skew < declared bound (kimchi: FX quote age vs crypto
close age), fire a defect line into the daily cycle report on breach. Success: sentinel line
appears daily; a deliberately-staled FX fixture trips it in test.

## PERSPECTIVE COVERAGE LOG (mandatory six + negative space + battery)

- **INTERNAL** (measured, not configured): S1-S8 strengths all command-verified; null-pipe
  cluster U1a-d; tape has 0 research consumers (U4); regime history destroyed daily (U6).
- **EXTERNAL** (how another world-class team improves this): a pod-shop would (i) run the
  interaction pass O3 as standing practice, (ii) keep ONE canonical machine graveyard feeding a
  pre-compute novelty check (O2), (iii) never let a proprietary tape go unscreened for a month
  (O1). They would NOT build a heavyweight feature-store/lineage platform at 2 deployed sleeves
  — GAP_ANALYSIS.md's deferral of the heavy modules is the correct external call too.
- **FUTURE** (2-3y compute/AI): semantic retrieval over research memory becomes trivial (local
  embedding models over 526 candidate rows + 122 memory rows + graveyard lessons); novelty
  gating upgrades from token-Jaccard to embedding similarity with zero API cost; agentic
  replay of the (by then multi-year) tape into features on demand. The moat compounds ONLY if
  the raw substrate (tape, labels, lessons-with-mechanism) is being captured NOW — which is
  exactly what O4/E3 and the tape already do. Nothing to buy today; the capture discipline is
  the future-proofing.
- **CONTRARIAN** (core assumptions wrong?): tested three. (i) "The tape is valuable for alpha"
  — genuinely unknown (U4); the pilot E1 is how we find out, and a powered negative would be
  real knowledge, not failure. (ii) "Negative knowledge always helps" — over-blocking risk is
  real, but the desk already engineered the mitigations: kill-basis rule (economic vs infra),
  NK reopen-conditions, SCREEN-UNDERPOWERED never graveyarded, novelty gate advisory-only.
  Verified in the artifacts, not assumed. (iii) "Composition is edge" — the evidence is n=1
  (kimchi) plus two caught artifacts (coinbase/turkey); the honest posture is that composition
  is the best-EVIDENCED class, not a proven law; O3 is sized accordingly (mechanism-priored
  pairs only, no combinatorial sweep).
- **GREENFIELD** (rebuild with only validated knowledge): keep — axis_screen harness, the
  candidate-row schema, PIT feature layer, forward-clock state files, conversion ledgers,
  graveyard-with-mechanism. Would NOT rebuild: the 6-module in-memory genealogy library
  (rebuild trigger stays ">5-10 survivors"), the duplicated 22-table schema stamped into 8
  sqlite files (one research DB + per-run candidate tables would do), the v1 campaign queue.
  Historical-baggage score: moderate — flat-JSON pragmatism won correctly; the baggage is dead
  schema, not wrong architecture.
- **FRONTIER** (recently publicly possible): RD-Agent trace-conditioning is already absorbed
  into hypothesis_novelty's design (docstring cites it) — the gap is wiring, not awareness.
  DuckDB over the jsonl/parquet estate would make cross-store audits like this one one-liners
  (cheap, optional). Sentence-transformer-class local embeddings (frontier for this desk, not
  the field) unlock E2's upgrade path. No new paper/venue/API found this sweep that beats
  finishing the wiring of what exists; recorded as an honest "nothing found" beyond these.
- **NEGATIVE-SPACE SWEEP** (never asked/built): no interaction engine (O3), no event-study
  harness (O5), no regime-label history (O4), no alignment sentinel (E5), no retrieval
  interface over research memory (future), no learned embeddings (future), foreign-frontier
  profiling exists for exactly ONE region (`ls data/frontier_profiles` → CN.json only — vs the
  doctrine's 8-language list; noted for the digger charters, outside this seat's remit),
  information_value.jsonl `lesson` fields 100% empty (capture exists, meaning-capture doesn't).
- **PROACTIVE BATTERY moves run:** (2) ADJACENCY → the null-pipe cluster U1 (one known
  instance became five). (3) CONFIG-VS-OUTCOME → every store audited by row count, every
  "live" claim by mtime; produced S1-S8 and U1. (8) NEGATIVE SPACE → list above. (9) SCOPE THE
  NEGATIVE RESULT → try/coinbase collectors going dark on 07-23 is NOT a delivery break — both
  axes were graveyarded as timing artifacts, stopping collection is rational (the cron comment
  still naming them as live clocks is stale, minor). (10) RATCHET CHECK → conversion + coverage
  records ratchet correctly; regime snapshot has NO floor (fixed by O4). (1) CONTINGENCY →
  tape backup is the sibling data-intelligence sweep's top finding; cross-referenced, not
  duplicated. Moves 4,5,6,7 ran and produced nothing beyond the above; reported as such.
