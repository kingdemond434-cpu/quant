# WEEKLY DEEP COLD AUDIT — SYNTHESIS (2026-07-29)

_Synthesis lead. Inputs: the eight 20260729 auditor reports in this directory. Status: IN PROGRESS — filled incrementally; if this file ends mid-section, the sections above the cut are final._

**This is the first synthesis ever produced** (`ls docs/research/deep_sweep/ | grep -i synth` → empty before this file; meta M1: the synthesis seat ran 0/3 weeks). Every prior week's audit knowledge compounded into nothing. That fact frames everything below.

## (A) OVERALL VERDICT + CEILING TABLE

**Verdict: the desk's sensing layer is unusually strong and got measurably stronger this week; its actuation layer is the defect, and it is one defect appearing at every altitude.** Findings are raised at ~8/day and disposed at ~0/day (`recommendations.py report` → 39 rows, 31 open past a 24h grace). The campaign validation exit has been welded shut the entire time it ran — by three *independent* mechanisms at once (campaign-constant PBO/RC gates + a DSR bar that at T=310/N=420 requires TRUE ann SR≈5; val F8) — so "420/0" measured the instrument, not the market. The strategic narrative steering every organ ("price space dead, axes rich") rests on one retracted screen (kimchi, ~73% timestamp artifact, rm-20260729T111104) and that zero-information campaign, and doctrine still recites both as live evidence while the retracted axis still burns a Holm slot (verified this run: kimchi ACCRUING 7/40, m_concurrent=4). The weekly integrator (this document) had never run. The entire forward-evidence estate sits in one copy on one disk with a ~55–60-day fuse and a never-exercised restore path. None of this is a discovery-capability problem; it is a **conversion** problem, concentrated in roughly six fixes of a day or less each. The week's honest wins are real and command-verified: Stage-A screening broadened to near-doctrine-compliance with honest UNDERPOWERED verdicts, the utilisation loop closed (scheduler→executor→append-only verdicts), the fee-attribution join production-falsified the flagship carry config and the ruin rail did its job, and the desk found most of its own gate defects before this audit did.

| subsystem | current % | ceiling % | gap | opportunity_cost_1y (auditor's own words, compressed) |
|---|---|---|---|---|
| alpha-discovery | ~55 (est. — auditor left SCORES=TBD, itself a defect; see F) | ~90 | 35 | governor trio (positive control, EV audit, slot cohort) unfixed 3 sweeps → funnel false-negative rate unmeasurable; unlock axis + entire scheduled-event class idle; generation drifted 3:1 against the standing cross-sectional order |
| data-intelligence | 60 | 90 | 30 | unbounded left tail (single-copy moat, disk fuse ~55–60d) + prioritizer poisoned by ~7 false-DEAD verdicts + one Holm slot burning at 0/40 (cny) + two sweep-days of audit output evaporated un-rowed |
| data-moat | 62 | 85 | 23 | 84 candidates "refuted" without ever being validly tested; trial accounting cannot survive a file overwrite; regime labels destroyed daily (3rd consecutive sweep) |
| infrastructure | 62 | 92 | 30 | DR tail: P(disk/VM loss)≈2–5%/yr × loss of the entire forward-evidence record; quota tax = 100% first-attempt organ death; pager saturable exactly on bad mornings |
| execution-growth | INCOMPLETE (quota-killed after 3 findings; no scores) | — | — | three live-capital correctness defects stand open: CI writes phantom incident lines into the prod error log; closes double-log after a crash into the Gate-0 tape; the KILL branch is unprotected, polls 10× harder while halted, and crash-looped into a venue IP ban (HTTP 418) |
| validation-stats | 40 | 90 | 50 | every campaign until redesign is a guaranteed-zero pass; a year of empty funnel = the entire discovery objective |
| research-engine | INCOMPLETE (4 strengths + finding I-0 only; 3/3 scheduled runs died at auth) | — | — | the subsystem doctrine calls "highest-return" has never been fully audited |
| meta-and-blindspots | 45 | 85 | 40 | falsified doctrine claim steering all organs 6+ days; synthesis never ran → zero week-grain compounding of audit knowledge |

Ceiling notes: every auditor's ceiling is **organizational** (solo triage bandwidth, one ruling queue, free-only + single-box policy), none technological. The stated lifting conditions are themselves in this portfolio: the disposition organ (P0-1), the second brain path (already half-built, `_BRAIN_MODEL_CHAIN`), and ~€4–8/mo of principal-gated spend.

## (B) CAPABILITY MAP — highest-ROI missing capability; biggest systemic risk

**The missing capability that unlocks the most: finding-disposition capacity as an ORGAN.** Concretely: (i) every sweep/audit/panel output auto-rows into the §41 ledger at write time (`source=deep_sweep-<date>`); (ii) the FIRST slot of every daily cycle disposes ≥5 rows or logs the measured reason (meta T4); (iii) alarm-to-action telemetry so any alarm class whose P(action|alarm) falls below a floor gets redesigned, not tolerated (meta M8d). Evidence this is the binding lever: three independent audits re-discovered overlapping defect sets faster than one brain disposed *any* (val F10); the 07-28 sweep produced **zero** ledger rows and zero actions (data-int #1 — the handoff sentence had no owner); 27→31 open rows in 24h with 0 dispositions (alpha M6). Every other item below is downstream of this one actually executing. **Runner-up multiplier: gate metrology** — a reference-standard battery of known-truth synthetics through every gate (meta M8b/T2). No gate has ever been calibrated (the one probe is mis-wired: SR_true +0.5 realizes −2.32, R0017); repairing it unlocks trust in every past and future screen verdict, the 420 re-adjudication (X2/T5), EV recalibration, and the narrative re-derivation. These two are the pair that matters: **one restores actuation, the other restores sensing you can trust.**

**Biggest systemic risks in EXISTING capabilities:**
1. **The evidence estate is single-copy** (infra O1): ~7GB of moat/lake/SoRs/clocks on one disk, zero backups, restore never performed, `BackupManager` aimed at an empty 0-table decoy DB. A disk event resets the desk's own named binding constraint — calendar-time evidence accumulation — to zero. The graveyard survives (git); the *evidence engine* does not.
2. **The alert channel fails exactly when things fail** (infra O3): one ntfy topic, 7 independent senders, one global 1h mute after any 429 — observed rate-limited DURING this sweep; a ruin-rail page is mutable by organ chatter on precisely the mornings organs are dying. Recurs (07-20, 07-29).
3. **The single-vendor quota pool** is the availability floor: measured 100% first-attempt organ death (S10), killed 3 of 8 auditor seats this sweep, starved the synthesis three weeks running, and delayed the weekly sweep 3 days (M1, I-0, infra O2).

## (C) TOP OPPORTUNITIES — PRIORITIZED PORTFOLIO

Ranked by expected total long-term contribution (direct + cascade + optionality + compounding) / (effort × maintenance × opportunity-cost). ◆ = compounding multiplier. Implementation capacity is the proven scarce resource (~0 dispositions/day), so this is a **sequenced queue, not a list** — P0 before P1 before P2, and the displaced items are named at the end.

**P0 — minutes-to-hours each; land before the next cycle closes:**
1. ◆ **Close the audit→action loop** (data-int #1 + meta M2/T4 + val F10): auto-row sweep outputs at write time; bind disposal to the first daily-cycle slot (≥5 rows/day or the measured reason); the §41 report's DEFECT lines get a consumer (nightly page). Re-arms two sweep-days of already-paid findings and every future audit. *This synthesis has itself rowed its portfolio (see §41 note at end) — the auto-rowing must make that structural.*
2. **Free the kimchi Holm slot + mark the doctrine retraction** (meta M3/M4): retire the retracted axis's clock (honest m_concurrent for the 3 live clocks), annotate `ops/principal_doctrine.txt` line ~89 with the retraction (rm-20260729T111104, commit 02f2917), and seed `docs/DOCTRINE_CLAIMS.json` with kimchi + the §33 ratchet-path drift as rows 1–2 (T3). Stops a falsified claim being injected into every organ, 6 days running.
3. ◆ **Sweep-runner starvation fix** (meta M1/T1 + research-engine I-0): rotate the start seat weekly (week# mod 8), run SYNTHESIS FIRST on resume when ≥6 reports exist, add a per-seat failure-streak counter (streak ≥2 → its own alarm). ~20 lines; unlocks all future week-grain compounding.
4. **Regime-history 3-liner** (R0006, now 4th consecutive audit): append-only `crypto_regime_history.jsonl` incl. the rule-vs-HMM disagreement bit (live disagreement today). Every day unwritten destroys irreplaceable self-generated labels.

**P1 — ≤1 day each; this week, in order:**
5. **Executor correctness triad, BEFORE the book restarts** (exec F1/F2/F3): (a) tests must stop writing production logs — monkeypatch/redirect `_ERR/_KILL/_HB/_STATE/_TRADES` (phantom "naked leg TODAY" lines are cry-wolf poison for real incidents); (b) persist state atomically PER CLOSE, not at rebalance end (kills double-logged closes corrupting the Gate-0 tape and the crash→corrupt-JSON→permanent-crash-loop latent); (c) wrap the KILL branch in try/except and back idle polling off 60s→600s (the observed 418 IP-ban crash loop — one shared IP feeds executor, recorders, and collectors).
6. ◆ **Gate reference battery → shadow re-run of the 420** (meta T2 + val X2): first repair R0017's normalization trap (exact-sample-SR winner + raw-noise cohort — the val auditor's own near-miss documents the right construction), publish the false-pass/false-block matrix per gate, then re-run the 420 through the per-candidate path (0 capital, ~2h) and attach the survivor count to the pending R0033 ruling so the principal decides on a measured counterfactual, not a description.
7. **DR: nightly restic + weekly scripted restore drill** (infra O1/T1) with sha256 + row-count assertions; retarget BackupManager off the empty `sor.sqlite` decoy. Blocked only on the €3.2/mo approval — paged today (one line, per mandate).
8. ◆ **DQS triple-fix + reset-failed** (data-int #2 + infra O2b): full-pass max-timestamp (MAX_ROWS head-sampling has a second victim and every append-only feed is on countdown), distinct-stamp cadence (batch-write median-gap-zero), event-log kind exemption; `systemctl reset-failed` after catch-up. Un-poisons `conversion_engine`'s +0.5 cost bias against ≥7 healthy sources and restores two alarm surfaces to meaning.
9. ◆ **Pager lanes** (infra O3/T3): URGENT = separate topic/provider, never coalesced, monthly synthetic-page drill under a simulated routine-lane 429; routine = digest. Protects every future alert including the ruin rail's.
10. **Rotate the GitHub PAT → SSH deploy key** (infra O5): the write credential to the tree a live-capital box executes sits in `.git/config` mode 664; flagged 07-26 and then *dropped by the ledger* — the §41 failure mode on the highest-stakes item. 15 minutes.

**P2 — next two weeks:**
11. **Unlock event study as the event-harness's first consumer** (alpha R2/T1; vehicle = open row R0007): 24,201 owned licence-cleared events, 174 perp-matched symbols, cross-sectional by construction, event-time n_eff dodges the R0030 panel power hole; placebo-controlled, every cell rowed. The one genuinely new alpha surface this week, and the cheapest route back into compliance with the standing cross-sectional targeting order.
12. ◆ **Global trial accounting** (R0008 + data-moat M2 + val #5): generalize the append-only `stage_a_verdicts.jsonl` shape to every screen path; populate-or-retire `trials_ledger` (0 rows × 10 DBs while gauntlet falls back to n_trials=2); one query answers "what N feeds the next DSR."
13. **Pre-campaign power gate + DSR-as-ranking analysis** (val F8/X3/X5): refuse any campaign whose min-passable TRUE SR > ~3; evaluate Stage-A DSR as top-K prioritization into forward clocks rather than binary 95% proof at T=310. Redirects validation effort to T-extension (reconstruction/backfill) — quantified this week as the single highest-ROI validation lever the desk owns.
14. **Reject forward-scoring** (val F3/X1): score the 57 stored rejects on post-rejection data — the only empirical false-negative instrument; unblocks three starved organs (reject_shadow, reject_rescore, EV audit ledger).
15. **EV-gate recalibration net of venue-truth fees** (R0023/R0034 + alpha R1 amendment): the gate's calibration anchor (carry EV 0.1171) is production-falsified; recalibrate from the fee-attribution join, add the in-code append site for `ev_gate_audit.json` (3 entries in 18 days vs its own n≥50 condition).
16. **Generation-mix telemetry** (alpha R3/U5): classify every hypothesis row {cross-sectional|timing|other}×family×horizon, report vs the 07-19 standing order each cycle. 62%-BTC-timing drift accreted because nothing measures it; telemetry only, zero new bars.

**Explicitly displaced (L1.14 — what this portfolio outranks):** embeddings/retrieval over research memory (lifting condition unmet: 140/1k rows); DuckDB migration (stdlib pain threshold not hit); big-bang meta-store consolidation (measure leakage first — meta M7/T6); new-acquisition digging beyond owned-idle conversion (§33 backlog discipline; the frontier list is aging unexecuted, which is a conversion problem); any Stage-B bar change (constant for life); a 4th alpha-discovery sweep before ≥2 governor closes land (alpha U8: the finding-generation:closure ratio is running ≈∞).

## (D) HARD WALLS (do not confuse with headroom)

1. **Sample length.** At T=310 daily obs, DSR-0.95 requires TRUE ann SR ≈5 at any realistic N (val F8 table) — no plausible net crypto edge clears it. This is estimator physics, not a threshold choice. It moves ONLY by manufacturing history: reconstruction, backfill, cross-venue splicing (T=1500 → passable SR ≈2.4). Every "reconstruction is optional" instinct dies against this table.
2. **Calendar-time forward evidence.** 40-day Stage-B clocks and Gate-0's ≥4 weeks of live fills cannot be compressed — only parallelized (law allows 12 slots; 4 occupied, 1 of those burning on a retracted axis, 1 on a killed feed at 0/40, capacity memo says 5 — reconcile and FILL) and protected (DR).
3. **Single-vendor quota pool** — availability floor until a second brain path; billing verdict with principal; partial free lift via chain-tail routing + credit-window-aligned scheduling (infra T5).
4. **Box physics**: 2 vCPU / 3.7GB / no swap; observed 199MB free-RAM floor during organ bursts; ~900MB reclaimable today (tmpfs eviction); €8/mo lifts the tier.
5. **Data walls** (documented failed hunts, not defaults): Hyperliquid userFills 2000-cap; pre-2026 L2 depth does not exist free anywhere; prediction-market entry prices absent.
6. **Solo triage bandwidth** — every auditor's 85–92% ceiling cites it; the P0 disposition organ is its stated lifting condition.

**NOT walls (claims that would masquerade as walls):** "price space is dead" (unmeasured either direction — M3/U2); "Bybit basis family gated on maturity" (no threshold defined anywhere = an undated deferral in spirit; define ~30 venue-days and auto-arm); "the DSR bar is properly strict" (it is a design artifact of T=310); "we are at max audit coverage" (two seats have never completed a run).

## (E) AUDITOR DISAGREEMENTS — ADJUDICATED

1. **"Price-space dead / axes rich" — three positions.** Alpha-discovery (A4.3) steelman-rejects BTC-timing overweight and treats the cross-sectional order as standing; data-moat (contrarian i) says the 420/0 conclusion "probably survives" because the 84's OOS median is 0.02; validation-stats (U2) and meta (M3) say the narrative has **zero valid evidence either direction**. **Ruling: all three are compatible once *evidence* and *prior* are separated.** The 84's OOS profile is weak evidence against that specific family; the campaign gates carried zero information about the other 336-as-a-class claim; kimchi's retraction removes the "axes rich" proof entirely. Doctrine must downgrade the claim from finding to prior TODAY (P0-2); the allocation itself may stand meanwhile on literature priors + capacity arguments — stated as such. T5 (top-decile re-screen under calibrated gates) + the unlock study generate the first valid evidence either way.
2. **Kimchi.** Alpha-discovery (01:15, pre-retraction) treated the kimchi clock as an honest strength; meta (21:22, post-retraction) demands the slot freed. **Ruling: the retraction (11:11, rm-20260729T111104, commit 02f2917) is decisive and the timeline explains the conflict — no auditor was wrong at their timestamp.** The clock is accruing forward evidence for a retracted construction; its −4.6 ann-Sharpe first week is consistent with (mildly corroborates) the artifact verdict. Free the slot; a de-artifacted reconstruction re-enters only as a NEW pre-registration.
3. **Ledger counts 32 vs 36 vs 39.** Not a disagreement — snapshots at different hours. The shared fact is the growth rate: ~8 rows/day raised, ~0 disposed, which IS finding P0-1.
4. **Carry book: "the rail did its job" (alpha F0/K5) vs "the rail fired on an undercount" (PRINCIPAL_ACTION: $4,399.91 of verified inventory marked $0).** **Ruling: both true at different layers.** The deployed config was genuinely fee-toxic (venue-truth join: commission −35.01% of sleeve vs thesis −2.19%) AND the rail's equity measure undercounts real inventory. The principal's pending A/B/C decision already incorporates both; EV recalibration (P2-15) must use venue-truth fees regardless of the ruling.
5. **Validation-stats F4 ("R0033 ruling displaced off the principal page") vs current state.** **Ruling: correct when written, since remedied** — verified this run: `data/PRINCIPAL_ACTION.md` now leads with both Tier-3 decisions (carry book A/B/C; gate flip YES/NO). The page-queue defect it exposed (single-slot file, overwrite semantics) remains real and is covered by P0-1's ledger discipline + the doctrine-claims manifest.
6. **Sweep cadence: alpha U8 ("no 4th sweep before closures — marginal token belongs to closure") vs the constitutional weekly cadence.** **Ruling: fix the loop, don't slow the clock.** The sweeps' marginal value collapsed because outputs evaporated (severed handoff), not because auditing over-samples. With auto-rowing + the disposal slot, weekly cadence stands. Alpha's narrower injunction is adopted verbatim: no 4th alpha-discovery deep sweep until ≥2 governor closes land.
7. **Auditor-order fix: research-engine I-0 ("run highest-ERV auditor FIRST") vs meta M1 ("rotate weekly + synthesis-first-on-resume + streak counter").** **Ruling: adopt M1's design.** A fixed priority order just relocates the permanently-starved tail; rotation guarantees every seat drains-first on a cycle, and the streak counter catches a systematically-dying seat regardless of position. I-0's core insight (order = starvation) is the same finding.
8. **DQS trust: data-int ("vitals ~90% false-DEAD — until fixed, trust collector_health + data_health alone") vs data-moat E5 (cites vitals as the partial alignment sentinel).** **Ruling: no conflict — both say the organ exists and misfires.** Adopt data-int's interim trust ordering explicitly until P1-8 lands, so readers aren't trained to skip pages.

## (F) RECURSIVE META — improving the audit itself

**Most value this week:** (1) **validation-stats** — the F8 DSR surface is the week's single most consequential number (it redefines campaign design permanently), plus F1's measured histogram and F11's 4.1× annualization bug; (2) **meta-and-blindspots** (first completed run ever) — found the starvation mechanism that explains why audits never compounded, the narrative-evidence rot, and imported a usable outside frame (control theory: anti-windup, sensor calibration, alarm-fatigue-as-detection-theory); (3) **data-intelligence** — the severed audit→action mechanism with its exact evidence, and the three DQS false-verdict mechanisms. **Least value per token:** alpha-discovery — high quality but its 3rd sweep in 4 days re-verified known-unmoved governors (by its own U8 admission); its real new content (unlock axis, generation-mix measurement) was ~20% of its length. execution-growth and research-engine produced the least in absolute terms, but the shortfall is **availability, not seat design** — both died at brain-auth mid-run; exec's three findings are individually among the week's most critical. Verdict: fix the runner (P0-3), don't delete seats.

**New audit section for next week:** an **incident-series & alarm-telemetry seat** (from meta M6/M8d): recurrence analysis across deadman fires (#1–#7 in ~10 days), organ auth-deaths, pager 429s — rate, trend, owner, P(action|alarm) per class. The desk autopsies losses (L2.6) but not recurrences; no existing seat owns "the same thing keeps happening."

**No-longer-discriminative questions:** (a) "Is X configured / does X have a monitor?" — everything is monitored now; the discriminative question is **"what did the monitor's last firing CAUSE?"** (b) FRONTIER-as-external-novelty — three seats independently answered "the frontier is internal adoption/conversion, not acquisition"; until the owned-idle backlog clears, FRONTIER should explicitly audit the owned-and-idle inventory (unlock events, spread column, CFTC COT, farside archive, OKX portal) rather than hunt new sources.

**Audit-process defects found by the audit:** (1) SCORES left TBD in a completed report (alpha-discovery) and two seats died mid-contract — the runner's completion check should verify scores-filled, not just byte count; (2) the read-only seats' "next live cycle must row these" handoff sentence has failed twice — replaced by P0-1's write-time auto-rowing; (3) two producers share `deep_sweep/` (R0015, still open) — the LIT_/T1a_ files interleave with sweep reports; (4) reports quote ledger counts at different hours without timestamps — cite `report` output WITH its run time.

## (G) RESEARCH CAPABILITY CAGR

First-ever measurement (no prior synthesis exists) — this is the **baseline**, scored 0–100 per component from this week's command-verified evidence:

| component | score | evidence + direction |
|---|---|---|
| experiment throughput | 70 ↑ | stage_a_verdicts 6→38 rows in 2 days; screens broadened to panels/horizons/power-fields; research_memory 133→144 |
| hypothesis quality | 75 ↑ | mechanism priors enforced; novelty gate live in production screens (n_priors=46); 11/11 preregistrations honestly EV-rejected |
| validation quality | 40 → | primitives world-class (S1) but 5 of 9 campaign gates degenerate; stepwise built+tested awaiting ruling; the DEFECTS are now measured, which is the improvement |
| automation | 55 ↑ | catch-up layer provably recovers organ deaths; but first-attempt success ≈0% and 3 seats + synthesis starved |
| knowledge reuse | 65 ↑ | resurrection→horizon loop ran with correct Bonferroni; graveyard 43 rows w/ mechanisms; NEW leak: powered screen-kills bypass the permanent stores (moat U7) |
| implementation velocity | **15 ↓** | 0 dispositions in 24h vs 8 raised; 31/39 open past grace; governor repairs 0-for-3-sweeps |
| data coverage | 70 ↑ | unlock 24,201 events, fred deep, bybit 4.5G, HL spread column all owned; idle-at-discovery persists (the coverage is ahead of the conversion) |

**Composite ≈ 56/100.** Directional verdict: **the engine's SENSING is compounding week-over-week; its ACTUATION is not** — the desk gets better every week at knowing exactly what to do and is flat-to-declining at doing it. Cycle-grain compounding is real (recursion rule accretes, forecast calibration Brier 0.072, generation priors reweight); week-grain compounding **starts with this document**. Next week's synthesis re-scores this table; the honest success criterion for the week is a single number: **open-past-grace ≤5** (from 31) with P0 landed. If that number doesn't move, nothing else in this report will have mattered — same as the last two sweeps.

---

## §41/§33 NOTE (this synthesis's own compliance)

This synthesis wrote: this report; portfolio entries #72–#78 appended to `docs/research/improvement_inbox.md`; ONE principal line (DR spend) in `data/PRINCIPAL_ACTION.md`; and ledger rows for every NEW portfolio item via `scripts/recommendations.py add` (source `deep_sweep`), deduplicated against open rows per §41 no-duplication (cited instead of re-rowed: R0006 regime history, R0007 event harness, R0008 trials ledger, R0016/R0017 gate probe, R0023/R0034 EV gate, R0033 stepwise ruling — already re-paged). Artifact-different-because-of-what-was-mined: this file, the inbox entries, the ledger rows, the principal page line.
