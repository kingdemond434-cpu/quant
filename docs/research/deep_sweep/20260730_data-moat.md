# WEEKLY DEEP COLD AUDIT — subsystem: **data-moat** — 2026-07-30

**Scope:** not "what data do we have" but *what information can only exist because of how WE
combine it*: proprietary transformations, hierarchical/composed features, graph representations,
embeddings, event reconstruction, market-state fingerprints, research memory, experiment lineage,
feature ancestry, alpha genealogy.

**Mode:** READ-ONLY. Every claim carries its proving command + output. Prior complete report:
2026-07-29 (this run audits the delta and opens new seams).

**Headline of this sweep:** the moat's *combination* layer is in better shape than its *reality
anchoring*. Three independent findings all have the same shape — **a capability was built,
verified, and then never connected to anything**: (a) a full alpha-genealogy + embedding layer
that passes 91 tests and has zero callers, (b) an own-fill execution tape accruing at 0.8% TCA
field coverage while "per-fill TCA" has been the #1 ROI item for six consecutive cycles, and
(c) two uncorrected copies of the *retracted* leaky Upbit keying, one of which wrote a
"STRENGTHENING" verdict on the retracted kimchi edge **today at 02:41**. The organ that is
supposed to catch exactly this — `module_justification` — globs `scripts/*.py` only and is
therefore blind to 349 of the 613 Python modules, including the entire genealogy layer.

**Status:** COMPLETE.

---

## SCORES

| metric | value | basis |
|---|---|---|
| current_capability_pct | **58%** | down from 62% on 07-29 — not a regression in build, a correction in measurement: the libs/-blindness (§1 D6) means prior scores credited capability the desk cannot see or run, and the leaky-copy survival (D1) plus 0.8% TCA coverage (D5) are load-bearing misses on *reality anchoring*, which the constitution ranks above simulation |
| practical_ceiling_estimate | **85%** | unchanged; ceiling is organizational triage bandwidth, not technology |
| ceiling_gap | **27 pts** | of which ~12 pts is pure wiring of things that already exist and already pass tests (D5, D7, D1) — the cheapest ceiling gap this seat has ever measured |
| opportunity_cost_1y | **HIGH, and partly irreversible** | D5 is the only *permanently destructive* item: every day at 0.8% coverage is a day of own-fill execution reality that can never be reconstructed (26 days already lost). D1 taxes every decay/fusion conclusion. D7 is pure deferred value, not loss |
| confidence | **0.9** on findings (all command-verified today), **0.65** on ranking |
| unknown_unknown_score | **0.35** (up from 0.30) | *raised deliberately*: this sweep found a 349-module blind region that three prior sweeps reasoned around without noticing. The lesson is that the desk's self-measurement organs define the audit's field of view, and one of them was scoped to 43% of the code. Where else is a measurement organ's glob narrower than its claim? Unmeasured. |
| info_gain_if_investigated | **HIGH** for D5 (each day's delay is permanent loss), **MED-HIGH** for D1 (contaminates two live organs) |
| expected_alpha_contribution | **MED direct** (D5 → real execution-cost model → capacity/EV honesty on the one live sleeve), **MED-HIGH indirect** (D1 fix removes phantom signals from the decay + fusion record) |
| expected_compounding_contribution | **HIGH** — D6 (measure libs/ too) and D7 (join the two halves of genealogy) both multiply the value of every future experiment; D5 creates the dataset the Execution Reality Model is built from |
| **CEILING EXPANSION** | the 85% is organizational. But this sweep found the assumption that makes it *feel* technological: the desk believes it knows what it owns. It measures 43% of its modules and its lineage layer was invisible to three consecutive audits. **The lifting condition is a whole-repo capability inventory**, after which the ceiling estimate itself should be re-derived — it may be that the desk is further from its ceiling than 85% implies, because it has been scoring a subset. |

---

## 1. WHAT WE KNOW — validated strengths and verified defects (each with its proving command)

### Strengths

**K1. The proprietary tape keeps compounding and is still ranked the desk's #1 information
advantage by its own scoring.**
```
$ du -sh data/moat/*
4.8G data/moat/bybit   136K data/moat/execution_tape   818M data/moat/fut   431M data/moat/spot
$ du -sh data/moat  →  6.0G      $ find data/moat -type f | wc -l  →  16541
$ find data/moat -type f -newermt 2026-07-30 | wc -l  →  240
```
6.0G (4.3G on 07-29 for bybit alone → 4.8G), 240 files already written today. Collection is
alive and the growth rate is real.

**K2. The own-fill execution tape module is the best-reasoned piece of moat engineering in the
repo.** `libs/execution/execution_tape.py` (119 lines) documents the exact defect it fixes —
`cashcarry_trades.json` was a `log[-500:]` rolling buffer destroying ~27 fills/day, which made
Gate 0's ">=4 weeks of live fills" *structurally unreachable* — and it is additive
(`_TAPE = data/moat/execution_tape/cashcarry_trades.jsonl`), exception-swallowing by design so
the observer can never kill the executor, and disk-guarded at 80%. It is wired into the live
executor (`scripts/run_cashcarry_executor.py:125  execution_tape.append(rec)`) and has a
backfill path (`scripts/run_trade_forensics.py:145  execution_tape.backfill(trades)`). The
docstring names the moat law correctly: *"own-fill history is the one dataset no vendor sells
and no free source replaces."*

**K3. A complete alpha-genealogy + embedding layer EXISTS and WORKS.** This is a genuine
capability that three consecutive data-moat audits (07-26, 07-28, 07-29) recorded as *absent*
("alpha ancestry exists only as prose in the KE genome").
```
$ ls libs/alpha_factory/
alpha_dna.py  alpha_embedding_engine.py  alpha_family_tree.py  research_graph.py
strategy_similarity_engine.py  concept_evolution_engine.py  feature_drift_engine.py
capacity_intelligence.py  crowding_intelligence.py  alpha_factory_controller.py  ...
$ .venv/bin/python -m pytest tests/alpha_factory tests/signal_engine -q -p no:cacheprovider
........................................................................ [ 79%]
...................                                                      [100%]
(91 passed)
```
`AlphaFamilyTree` implements `add(alpha_id, parent_id, mutation_type, performance)`,
`lineage()` (root-to-node ancestry), `children()`, and `best_lineage()` ("the ancestry of the
highest-performing alpha — the winning research direction"). `ResearchGraph` is a typed
directed graph over `idea → feature → signal → factor → alpha → performance`. `alpha_dna.py`
builds a structural fingerprint (signal type, horizon, factor/regime sensitivities, capacity,
turnover, risk) with `dna_distance()`. `signal_embedding_engine.py` embeds candidates and
clusters near-duplicates at cosine ≥ 0.98 — explicitly to catch *"tight clusters [that] reveal
redundant signals that would otherwise masquerade as diversification"*, i.e. the L1.18
alpha-diversity law, implemented, tested, unused. **The desk owns its lineage layer. It has
never plugged it in** (see D7).

**K4. The novelty gate spread from 2 call sites to 4 and now includes a libs-level consumer.**
```
$ grep -rn "hypothesis_novelty" --include=*.py scripts libs | grep -v "^libs/alpha_factory/hypothesis_novelty.py"
scripts/screen_idle_axes.py:50 / :96
scripts/screen_fred_macro_axis.py:90 / :189
scripts/screen_exchange_netflow.py:42 / :93
libs/autodiscovery/generation_roi.py:23 / :76
```
The universal-duty novelty screen is no longer a per-script bolt-on: `generation_roi` gates
generation itself. And it is honest about its method — `hypothesis_novelty.py:14`:
*"No AI-oracle: deterministic set/token similarity, no model deciding anything."*

**K5. The Upbit alignment leak produced a genuinely excellent single-source module.**
`libs/research/upbit_data.py` carries the full derivation in its docstring: KST day opens
15:00 UTC the previous calendar day → keying by `candle_date_time_utc` (the OPEN) labels every
close ~15h early → the "forward" screen was ~62% contemporaneous overlap; shift test +1d cell
0.823 vs 0d 0.225, honest no-overlap cell +0.018. It also correctly scopes the damage: *"LIVE
forward-clock rows were never affected: at collection time only completed candles exist."*
This is exactly the reality-anchoring reflex L1.4 demands. (Its enforcement failed — D1.)

**K6. The forward clocks remain honest to the point of self-harm.**
`data/axis_shadow_state.json` (07-29 08:46): kimchi_premium ACCRUING day 7/40, cum_return
−2.407%, ann_sharpe −4.6, nw_t −0.59 under Holm bar 2.24, m_concurrent 4; stablecoin_supply
_momentum day 6/40 +0.773%, ann_sharpe 1.86, nw_t 0.22. The artifact itself carries
*"ELIGIBLE means the evidence bar is met and a promotion decision may be taken — it is NOT an
automatic deployment."* No massaging.

**K7. The fusion engine's multiplicity discipline is textbook — better than its result.**
`scripts/fusion_engine.py` docstring pre-commits to: pre-registered combinations only with a
stated economic reason (no subset sweep, because *"k signals give 2^k subsets… a guaranteed
false-positive factory"*), Bonferroni across the number tested, **equal-weight z-composites
only** (*"fitting weights on the same sample is how a fusion engine becomes an overfitting
engine"*), and the honest comparison *"does the composite beat its BEST component? A composite
that merely tracks its strongest member is not fusion, it is relabelling."* Five pre-registered
combos, α = 0.05/5 = 0.01. The design is right; the plumbing and the conclusion are not (D3/D4).

**K8. The decay tracker's *instrument-first* reasoning is correct and rare.**
`scripts/signal_halflife.py`: *"a decay curve needs a TIME SERIES THAT STARTS NOW… you cannot
retroactively record what you never instrumented. Recording is cheap and irreversible if
skipped; MODEL-FITTING is what must wait for evidence."* It refuses to fit a half-life below
`MIN_POINTS_TO_FIT = 8`. It runs on the daily cycle (`scripts/daily_research_cycle.py:57`) and
appended a fresh observation at 02:41 today. The discipline is right; the input is contaminated
(D1/D2).

**K9. The black-swan scenario library is real content, not a stub.**
`data/black_swan_library.json` (5972 bytes): 7+ named scenarios each with shock parameters and
an explicit *kill mechanism* — `2020_covid_crash`: −50% in 2 days, vol ×5, *"liquidity
evaporates; spreads 10x; kills levered shorts via liquidation before spot offsets; maker fills
vanish"*; `2022_luna_depeg`: *"kills carry collateral + quote-currency assumption; spot leg
quoted in depegging stable"*; `2022_ftx_collapse`: asset_recovery 10%, permanent. Sourced from a
5-model external adversarial review. It is knowledge. It is also inert (D8).

---

### Verified defects

**D1 — THE RETRACTED LEAK IS STILL LIVE IN TWO SCRIPTS, AND ONE OF THEM WROTE A VERDICT TODAY.**
This is the sweep's most serious finding because it defeats a same-day L1.4 retraction.

`libs/research/upbit_data.py` docstring, written 2026-07-29 11:02: *"Both scripts now import
this function; **a third copy anywhere is a defect**."* The retraction commit (02f2917) states:
*"libs/research/upbit_data.py is now the single close-date-keyed source, both scripts import
it."* There were never two copies. There were **four**:
```
$ grep -rn "candle_date_time_utc" --include=*.py .
scripts/fusion_engine.py:66:    kb = {str(r["candle_date_time_utc"])[:10]: float(r["trade_price"])
libs/research/upbit_data.py:10:  (docstring)
libs/research/upbit_data.py:43:  d = ...fromisoformat(...[:10]) + _dt.timedelta(days=1)   ← the FIX
scripts/signal_halflife.py:55:  kb = {str(r["candle_date_time_utc"])[:10]: float(r["trade_price"])
$ grep -rn "upbit_data" --include=*.py . | grep -v "^./libs/research/upbit_data.py"
scripts/collect_kimchi_premium.py:25   scripts/revalidate_clocks.py:22        ← only 2 importers
```
`scripts/fusion_engine.py:66` and `scripts/signal_halflife.py:55` both still compute
`kb[d]/fx[d]/gb[d] - 1.0` on **open-date-keyed** Upbit candles — the exact ~15h look-ahead the
retraction removed. The §42 five-copies failure class was *declared closed while half the copies
were still leaking*, in the same commit that named the failure class.

**Live consequence, measured today.** The daily cycle ran `signal_halflife` at 02:41 during this
audit and appended:
```
$ tail -2 data/signal_halflife.jsonl
{"date": "2026-07-30", "signal": "stablecoin_supply", ... "status": "AGEING"}
{"date": "2026-07-30", "signal": "kimchi_premium", "n_windows": 4, "ic_early": 0.2071,
 "ic_recent": 0.4013, "trend": 0.1942, "half_life_windows": null, "status": "STRENGTHENING"}
```
The desk's **only** signal-decay organ recorded the retracted axis as **STRENGTHENING with
ic_recent +0.4013**, one day after the same axis was retracted to an honest forward IC of
+0.0604 (SCREEN-UNDERPOWERED), and while the live forward clock for that axis reads
**ann_sharpe −4.6, cum −2.4%** (K6). Three artifacts, one axis, three irreconcilable verdicts,
and nothing in the desk reconciles them.

**D2 — the decay organ's kimchi record carries ZERO information and its history is
double-counted.** Beyond the leak, the series itself is inert:
```
$ cat data/signal_halflife.jsonl   (12 rows, 8 signal-days)
07-27 kimchi  ic_early 0.1978  ic_recent 0.418   trend 0.2203  n_windows 4
07-28 kimchi  ic_early 0.2071  ic_recent 0.4013  trend 0.1942  n_windows 4   ← duplicated row
07-28 kimchi  ic_early 0.2071  ic_recent 0.4013  trend 0.1942  n_windows 4   ← duplicated row
07-29 kimchi  ic_early 0.2071  ic_recent 0.4013  trend 0.1942  n_windows 4   ← duplicated row
07-29 kimchi  ic_early 0.2071  ic_recent 0.4013  trend 0.1942  n_windows 4   ← duplicated row
07-30 kimchi  ic_early 0.2071  ic_recent 0.4013  trend 0.1942  n_windows 4
```
Two separate defects visible in six lines. (i) **Byte-identical values on 07-28, 07-29 and
07-30** — three consecutive "observations" of a decay curve that did not move at all. The
mechanism is in the code: `rolling_ic(..., win=60, step=20)` emits windows at
`range(20, len(s)-61, 20)`, so `n_windows` is constant for any series length in [142,161] — a
20-day plateau — and the windows are anchored to the series *start*, so the newest data lands
outside the last window entirely. A decay tracker whose most recent window ignores the most
recent data cannot detect decay. (ii) **Duplicate appends**: 07-28 and 07-29 each appear twice
with identical payloads (the organ ran twice on those days and `SERIES.open("a")` has no
idempotency key), so any future fit over this series double-weights two days. 12 rows for 8
signal-days = 33% synthetic history in the desk's canonical decay archive.

**D3 — the desk's only multi-source fusion result is a look-ahead-suspect composite that the
script itself labelled "FUSION ADDS VALUE", and that label became the commit message.**
```
$ python3 -c "…" data/fusion_engine.json   (updated 2026-07-27T13:12, never re-run)
flow_plus_regional (orthogonal fuse): members [stablecoin_supply, kimchi]
  ic 0.2687 | best_single 0.2102 | lift +0.0585
  verdict "SUSPECT-LOOKAHEAD"        ← axis_screen's own gate
  fusion_verdict "FUSION ADDS VALUE" ← the script's headline
$ git log --oneline -- scripts/fusion_engine.py | tail -1
7b85703 research: information fusion engine -- orthogonal fusion adds value, same-mechanism fusion subtracts
```
The bug is in `scripts/fusion_engine.py` at the verdict line:
`verdict = ("FUSION ADDS VALUE" if lift > 0.02 else "no lift over best component…")` — it reads
only `lift` and **never consults `r.get("verdict")`**, the gate output it prints one line above.
So the artifact gate fired, was printed, and was overruled by a formatting decision. The
resulting sentence *"orthogonal fusion adds value"* was committed as a research finding. And its
`kimchi` component came from the leaky keying (D1) — the composite's IC 0.2687 is built on a
signal whose honest forward IC is +0.0604. **The one positive fusion result the desk has is
unsupported twice over, and the artifact has not been re-run since 07-27 (kimchi was fixed
07-29 11:10).**

**D4 — "fusion of 3+ orthogonal sources" (L1.11, named in the constitution as the moat's
manufacturing target) has been tested exactly once, failed, and the failure is not in the
graveyard.** From the same artifact — every combo with ≥3 members lost to its best single
component:
```
liquidity_plus_activity  [stablecoin_supply, dex_volume, protocol_fees]        lift −0.0998
full_stack               [stablecoin_supply, dex_volume, protocol_fees, kimchi] lift −0.0562
liquidity_expansion      [stablecoin_supply, defi_tvl]                          lift −0.1067
onchain_economic_activity[dex_volume, protocol_fees]                            lift −0.0107
```
4 of 5 combos: *"no lift over best component (relabelling, not fusion)"*, and 3 of the 5 came
back `SCREEN-UNDERPOWERED`. That is a real, useful negative result about the desk's flagship
moat strategy — and it is recorded in exactly one overwritable JSON file:
```
$ grep -ci "fusion" docs/graveyard.md          → 0
$ python3 -c "'fusion' in open('data/negative_knowledge.json').read().lower()" → False
$ crontab -l | grep -c fusion                  → 0
$ grep -rn "fusion_engine" scripts/ ops/ libs/ | grep -v "^scripts/fusion_engine.py"
scripts/screen_auditor.py:75:    "fusion_engine.py": "forecast",
```
The engine is **unscheduled** (not in cron, not in `daily_research_cycle.py`'s 40-step list) —
a one-shot from 07-27. So: the moat's headline manufacturing method has one measurement, that
measurement is contaminated, it is not permanent, it is not repeated, and the novelty gate
(which compiles priors from `docs/graveyard.md`) cannot see any of it. The next organ to propose
multi-source fusion will burn fresh multiplicity budget re-deriving this.

**D5 — THE EXECUTION REALITY MODEL: the one dataset no vendor sells is being accrued at 0.8%
TCA field coverage, and the loss is permanent.** This is the highest-ROI finding in the sweep.

L1.11 names *"an Execution Reality Model from our own fills"* as a moat component to manufacture.
The desk has the tape (K2) and a full post-trade TCA implementation. It does not have the fields.
```
$ python3 -c "…" data/moat/execution_tape/cashcarry_trades.jsonl
rows 517   (2026-07-02T05:18 → 2026-07-28T15:20, 26 days)
FIELD FREQUENCY:
  event 517  symbol 517  qty 517  notional 517  funding_rate 517  opened 517  _taped 517
  closed 262  held_hours 262  price_pnl 262  est_funding 262  net 262
  spot_mode 17   fut_mode 17
  spot_fill 4  fut_fill 4  spot_mid 4  fut_mid 4  wait_s 4  spot_slip_bps 4  fut_slip_bps 4
TCA REQUIRED FIELDS PRESENT?
  decision_price 0   arrival_price 0   avg_fill_price 0   fill_price 0
  time_to_fill 0     maker 0   taker 0   order_type 0     slippage_bps 0
```
The executor **already computes these fields** — `scripts/run_cashcarry_executor.py:982 def _tca(fill, spot_fill, fut_fill, …)` builds `spot_fill/fut_fill/spot_mid/fut_mid/wait_s` and
derives `spot_slip_bps = (fill − mid)/mid × 1e4` at line 998, with `wait_s` stamped at line
1017. They reach the tape in **4 of 517 records (0.77%)**; maker/taker mode reaches 17 (3.3%).
Consequences, all verified:
```
$ ls web/tca.json → No such file or directory
$ grep -c "execution_tca_fill_log" data/cro_ai_logs/daily_research_cycle.log → 6
   (top-ROI item in CRO cycles 07-23, 07-25, 07-26, 07-27, 07-28, 07-29 — every logged cycle)
$ tail -1 data/cro_ai_logs/daily_research_cycle.log
CRO cycle 2026-07-29: next-ROI={'id': 'execution_tca_fill_log', 'roi': 0.128} | constraint=…
$ grep -rn "from libs.execution.tca import\|execution.tca" --include=*.py libs scripts api app
libs/execution/__init__.py:35: from libs.execution.tca import PostTradeTCA, SlippageAttribution, TcaResult
   (re-export only — libs/execution/tca.py, 87 lines, has ZERO functional consumers)
```
So: an 87-line `PostTradeTCA` with slippage attribution into spread/impact/timing sits unused
because its inputs are 99.2% missing from the only dataset that could feed it; the desk's own ROI
ranker has named this the #1 next build in **six consecutive cycles** at effort 3.0h; and
`web/tca.json` still does not exist. **This is the only irreversible item in this report.** Mid
price and wait time at the moment of a fill cannot be reconstructed after the fact — 26 days ×
~20 events/day of execution reality are already permanently un-analysable, and the count grows
daily. L1.5 (execution physics: no alpha is valid until it survives realistic slippage) is being
enforced against a cost model that has no fill-level evidence to be built from.

**D6 — the organ that detects dead code is blind to 57% of the code, and the blind region is
exactly where the moat's genealogy layer lives.**
```
$ grep -n "glob" scripts/module_justification.py
68:    scripts = sorted((ROOT / "scripts").glob("*.py"))
$ ls scripts/*.py | wc -l                                          → 264
$ find libs -name '*.py' -not -path '*__pycache__*' | wc -l        → 349
$ python3 -c "…" data/module_justification.json
total 264 modules scored;  any path-like name (containing '/'): 0
tally {'CRITICAL': 71, 'KEEP': 39, 'SCAR_TISSUE': 46, 'INERT': 89, 'DUPLICATE': 13, 'PREMATURE': 6}
  alpha_dna                  in justification set: False
  alpha_family_tree          in justification set: False
  research_graph             in justification set: False
  signal_embedding_engine    in justification set: False
  alpha_embedding_engine     in justification set: False
  strategy_similarity_engine in justification set: False
```
264 of 613 modules (43%) are measured. The 89 INERT + 46 SCAR_TISSUE verdicts the desk quotes as
proof it knows its own dead weight are computed over `scripts/` alone. Every module under
`libs/` — the entire alpha_factory, signal_engine, features, validation, execution, portfolio,
regime, discovery layer — is outside the frame. This is the mechanism behind K3/D7: three
consecutive audits (including this seat's own 07-26, 07-28 and 07-29 reports) concluded alpha
genealogy did not exist, because the artifact-and-scripts view they reasoned from could not see
it. **A measurement organ's glob silently defined the desk's field of view, and the desk scored
itself inside that frame.**

**D7 — the two halves of alpha genealogy have never been joined; both halves already work.**
```
$ grep -rln "alpha_factory_controller\|AlphaFactoryController" --include=*.py scripts api app
   (empty — zero invocations outside libs/ and tests/)
$ grep -rln "<module>" --include=*.py libs scripts api app tests   # per module
alpha_dna                → libs/alpha_factory/__init__.py + tests only
alpha_family_tree        → __init__.py, alpha_factory_controller.py, tests only
research_graph           → __init__.py, alpha_factory_controller.py, tests only
signal_embedding_engine  → libs/signal_engine/__init__.py + tests only
```
Half one (persistent, empty): `research_memory` has a `predecessor_id` column,
`scripts/research_memory.py:50` writes it, and the prior audit measured **0 of 140 rows
non-null**. Half two (functional, ephemeral): `AlphaFamilyTree.__init__` is
`self._nodes: dict[str, FamilyNode] = {}` and `ResearchGraph.__init__` is three in-memory dicts
— **no persistence layer at all**, so even if wired the lineage would evaporate at process exit.
The gap between "we have no alpha genealogy" and "we have alpha genealogy" is: one caller, and a
load/save on a dict. The capability was bought (Jun 18) and never installed; every file in
`libs/alpha_factory/` except `hypothesis_novelty.py` is untouched since 2026-06-18.

**D8 — the black-swan library is a self-greening gate: 7 scenarios, 0 consumers, and the
completion test is `.exists()`.**
```
$ grep -rn "black_swan_library" --include=*.py libs/ scripts/
scripts/research_cycle.py:75:  "black_swan_library": (_ROOT / "data/black_swan_library.json").exists(),
scripts/research_cycle.py:163: {"id": "black_swan_library", "title": "Black swan scenario library …
$ ls -la data/black_swan_library.json → 5972 bytes, Jul 11 17:25 (19 days untouched)
```
Its own stated policy is *"named crisis scenarios every PROMOTED alpha must be replayed against
before production."* Nothing reads the file. There is no replay harness
(`grep -rl "stress_lab\|synthetic_stress" libs scripts` → empty; `black_swan` appears only in
`scripts/research_cycle.py`). The item is marked complete forever because a JSON file exists.
Mitigating: zero alphas have been promoted, so nothing has been *skipped* yet — but the gate
will read "done" on the day the first promotion happens, which is precisely when it matters.

**D9 — all 32 completion gates in the ROI backlog are presence checks. Not one measures
behaviour.**
```
$ python3 (ast/regex over the gates dict in scripts/research_cycle.py)
total gate keys: 32
.exists() occurrences: 26
"substring in file text" checks: 6      e.g. "trade_logging": "_log_trade" in exec_txt
hardcoded True: 2                       e.g. "state_files_infra": True,
                                             "watchdog_enabled": True,  # comment: "enabled last cycle"
```
32/32. `"execution_tca_fill_log": (_ROOT / "web/tca.json").exists()` means the desk's #1 ROI
item will be marked DONE the instant any file appears at that path, regardless of whether it
contains a single fill. `"trade_logging": "_log_trade" in exec_txt` passes because a *function
name* appears in a source file — which is why D5 (the log exists but is 99.2% empty of TCA
fields) has been invisible to the backlog for 26 days. This is the doctrine's "config-not-
outcome" failure at the level of the desk's own priority engine: the thing that decides what to
build next cannot tell built from present.

**D10 — the constitution's moat vocabulary (REC, DIVR, data half-life at dataset level) has no
implementation.** L1.11 requires manufacturing proprietary states "at the physical ceiling" and
purging "low-REC high-decay low-DIVR data".
```
$ grep -rl "DIVR" libs scripts data/*.json                 → (empty)
$ grep -rl "reconstruction_cost" libs scripts data/*.json  → (empty)
$ grep -rl "monopolis\|monopoliz" libs scripts             → (empty)
$ grep -rl "data_genome" libs scripts                      → (empty)
$ python3 -c "…" data/data_registry.json
top-level keys: ['policy', 'last_verified', 'next_quarterly_review', 'sources']
field 'half_life' present: False   'decay': False   'revisit': False
field 'licence'/'license' present: False   'irreplaceable': False
```
(`REC` greps hit only unrelated tokens — `libs/research/finding_registry.py`,
`libs/stage14/models.py` — never as a reconstruction-cost score. `diversification_ratio` exists
in `libs/portfolio/` but is a *portfolio* metric, not a data-DIVR score.) The desk therefore
cannot answer, for any dataset it holds: what would it cost someone else to reconstruct this,
how fast is its information decaying, and how orthogonal is it to what we already have. Those
three numbers are the definition of a moat. Signal-level half-life exists (K8, contaminated by
D1); dataset-level does not exist at all. `data_registry.json` has no licence field either,
which is the charter-13 legitimacy gate having no machine-readable home.

**D11 — regime-label history: FOURTH consecutive sweep, still destroyed nightly.**
```
$ ls -la data/crypto_regime*.json*
-rw-r--r-- 1 quant quant 293 Jul 30 00:03 data/crypto_regime.json     ← snapshot, overwritten
   (data/crypto_regime_history.jsonl still does not exist)
```
Flagged 07-26 (O4), 07-28, 07-29 (M5, "rank 1 by shame"), register row R0006. Seven live
consumers act on this label. The rule-vs-HMM disagreement stream remains an unrecorded dataset.
A 3-line append has now outlived four audits — at ~90 labels per audit interval, ~360 labels of
regime history are gone.

**D12 — interaction screen and event-study harness: still zero, third and third sweep.**
```
$ grep -rln "interaction" libs/research libs/features libs/alpha_factory   → (empty)
$ grep -rln "event_study\|event_window\|abnormal_return" libs scripts      → (empty)
```
The cheapest untested composition families (pairwise feature interactions; unlock/listing/
liquidation event windows) remain untouched. `data/feature_library.json`'s 447-cell grammar is
`OBSERVABLE × TRANSFORM × WINDOW × NORM` — a univariate transform grid, not a composition
grammar, so it cannot enumerate an interaction cell today.

---

## 2. WHAT WE DON'T KNOW — the ignorance ledger

**U1. Where else is a measurement organ's scope narrower than its claim?** D6 is the finding of
the sweep in the sense that matters: not a bug, a *frame error*. `module_justification` globs
`scripts/*.py` and the desk read its 89-INERT tally as knowledge of its own dead weight. The
same question applies to every self-measuring organ — `unobserved_observables` (70 fields
"owned but never read": over which files?), `coverage_audit`, `dependency_graph`,
`data_vitals`, the §35 findings-coverage ratchet, `screen_auditor`. Each has a glob. None of
those globs has been audited. Suspected, unmeasured: several are scripts-only for the same
historical reason. **This is the highest-value unknown in this report** because it determines
whether any other score on this page is computed over the right denominator.

**U2. Whether the 0.8% TCA coverage (D5) is a code path that fires rarely or a code path that
usually errors.** The `_tca()` builder exists and 4 records carry its output — so it runs. What
is unknown is *why* 513 records lack it: are `spot_mid/fut_mid` only populated on the maker path
(17 records have `spot_mode`, suggesting a mode-specific branch), does a `None` mid silently drop
the fields, or is `_tca()` called on one of three open paths (lines 755/806/855 all build
records) and not the others? Determining this is a 20-minute read and it decides whether the fix
is 3 lines or 30. **Not investigated here because it requires reading ~1000 lines of live
executor and this seat is read-only on a running system.**

**U3. Whether the fusion result survives correction.** Re-running `fusion_engine.py` against
`upbit_daily_close_keyed` is a one-line import change and a re-run. Prior: the honest kimchi
forward IC is +0.0604 vs the leaky +0.2249, so the composite IC 0.2687 should fall sharply and
`flow_plus_regional`'s +0.0585 lift probably inverts. But "probably inverts" and "measured to
invert" are different assets, and the desk's record currently asserts the wrong one. Note the
second-order unknown: `stablecoin_supply`'s standalone |IC| 0.2102 is *itself* implausibly high
for a slow macro aggregate and the same artifact family (contemporaneous overlap via
publication-lag keying) is the obvious suspect — **it has not been shift-tested**, and it is on a
live forward clock.

**U4. Whether `AlphaFamilyTree`/`ResearchGraph` would ever be QUERIED if wired.** Unchanged from
07-29's U3 and still the right question — D7 lowers the cost of finding out to near zero (the
code and tests exist), which strengthens the case for the one-family pilot over a platform.

**U5. Whether any of the tape's 16541 files is genuinely irreplaceable.** The #1
information-advantage score (1.026) rests on the claim *"snapshots at OUR timestamps; nobody
else has these."* For orderbook state that is structurally true; for trades/klines/funding it is
false (Bybit and Binance publish historical archives). The per-stream classification, actual
coverage gaps, and whether a 6.0G irreplaceable asset is backed up anywhere were delegated to a
parallel probe this run and **are not resolved in this report** — flagged honestly rather than
asserted. If any *irreplaceable* stream has no backup, that is a one-disk-failure moat.

**U6. The regime-disagreement stream's information content** — unknowable until D11 lands
(fourth sweep).

**U7. Cross-store kill permanence, quantified.** 07-29's U7 named the leak (powered SCREEN-WEAK
verdicts appearing in no permanent store). This sweep adds a second instance — the fusion
negatives (D4) — which suggests it is systemic rather than a one-off at the newest seam. The full
store-by-store reconciliation was delegated in parallel and is not resolved here.

**U8. Whether `information_value.jsonl` is now dead rather than merely meaningless.**
`$ stat -c '%y' data/information_value.jsonl → 2026-07-22 05:20` — 8 days stale. 07-29 reported
it growing with 810/810 empty `lesson` fields; it has now stopped growing. Whether its producer
was retired deliberately or silently broke is unknown, and nothing alarmed.

**U9. Whether the daily cycle's two hard-failing steps matter.**
`$ grep "'ok': False" data/cro_ai_logs/daily_research_cycle.log | tail`:
`[desk_brief]` and `[contributor_score]` both fail with *"can't open file
'…/scripts/research_exchange.py brief': No such file or directory"* — a shell-quoting bug (args
passed as part of the path) that has failed every cycle silently; `[axis_shadows]` and
`[shadow_8h]` both died on `HTTP Error 418: I'm a teapot` (Binance geo/rate block). The last is
material: `axis_shadows` is the organ that advances the Stage-B forward clocks, which is why
`axis_shadow_state.json` is stamped 07-29 08:46 and kimchi still reads day 7/40 — **forward
evidence, the desk's only promotion currency, did not accrue today.** Whether it retried
successfully later in the run was not verifiable at audit time (the cycle was mid-flight).

---

## 3. WHAT COULD MATTER MOST — ranked opportunities

Ranking = expected impact × confidence / (cost × maintenance). ◆ = compounding multiplier.
Every item carries the L2.7 decision template.

### M1 — Fix the fill-record schema so the Execution Reality Model can exist. [rank 1]
- **Exactly what:** make `_tca()`'s output unconditional on every open/close/topup record in
  `run_cashcarry_executor.py` (lines 755/806/855 + the close path), so `spot_mid/fut_mid/
  spot_fill/fut_fill/wait_s/spot_slip_bps/fut_slip_bps/spot_mode/fut_mode` land on 100% of tape
  rows; then the 3h `web/tca.json` build the ROI ranker has wanted for six cycles.
- **Why:** D5. 0.77% coverage on the one dataset no vendor sells, and the loss is permanent.
  Every day of delay is a day of execution reality that cannot be reconstructed.
- **Evidence:** field-frequency table in D5; `_tca` at `run_cashcarry_executor.py:982-998`;
  `web/tca.json` absent; 6 consecutive `next-ROI={'id': 'execution_tca_fill_log'}` lines.
- **Expected benefit:** L1.5 becomes enforceable on measured fills instead of a forecast cost
  model; capacity and EV estimates on the one live sleeve get real inputs; Gate 0's ">=4 weeks
  of live fills" becomes measurable in the sense intended.
- **Complexity:** LOW-MED (U2 must be resolved first: 20-minute read of the three open paths).
  **Dependencies:** none. **Validation:** field-frequency re-run → all fields at 100% of new
  rows; `web/tca.json` non-empty with ≥1 week of attributed slippage.
- **Failure modes:** (i) touching a live executor — mitigate by making the enrichment additive
  and exception-safe exactly as `execution_tape.append` already is (K2's pattern); (ii) a `None`
  mid on the taker path writing nulls — record the null explicitly rather than dropping the key,
  so coverage is auditable. **Alternatives considered:** reconstruct mids from the bybit tape at
  fill timestamps — rejected as a *primary* (our own quote at our own moment is not the same
  object as a recorder snapshot) but **worth doing as a one-off backfill** for the 26 lost days,
  since we do hold `data/moat/bybit` at 1-minute-ish resolution; that recovers an approximation
  where the exact value is gone.
- **ROI:** highest in this report — it is the only item whose cost of delay is irreversible.
  **Confidence:** 0.9. **Maintenance:** near-zero. **Monitoring:** add TCA field coverage % to
  the daily cycle line. **Retirement:** never (it is the archive).
- **Horizons:** 1w = fields at 100%; 1m = first honest slippage attribution; 3m = cost model
  populated from live measurement, not forecast; 1y = an Execution Reality Model with 12 months
  of own fills, which is the moat component L1.11 names and no competitor can buy.
- **Opportunity cost:** displaces M2. M2 is a *correctness* fix on two organs; M1 is the only
  item where waiting destroys an asset. Ranked above it for that reason alone.

### M2 ◆ — Kill the two surviving leaky copies and re-derive both contaminated records. [rank 2]
- **Exactly what:** `scripts/fusion_engine.py:66` and `scripts/signal_halflife.py:55` import
  `upbit_daily_close_keyed`; then (a) re-run `fusion_engine.py` and overwrite the artifact,
  (b) mark the pre-fix `signal_halflife.jsonl` kimchi rows as superseded (append a correction
  row — never delete, it is an archive), (c) add the shift test to `stablecoin_supply` too (U3).
- **Why:** D1/D2/D3. A retraction that leaves half its copies live is not a retraction, and the
  desk currently holds three contradictory verdicts on one axis (STRENGTHENING +0.4013 /
  SCREEN-UNDERPOWERED +0.0604 / forward ann_sharpe −4.6).
- **Evidence:** the four-copy grep in D1; today's 02:41 append; the SUSPECT-LOOKAHEAD /
  "FUSION ADDS VALUE" contradiction in D3.
- **Complexity:** TRIVIAL (2 imports + 1 re-run). **Validation:** `grep -rn
  "candle_date_time_utc" --include=*.py .` returns only `libs/research/upbit_data.py`. That grep
  should become a fence — a mechanical check is the only thing that makes "a third copy anywhere
  is a defect" true.
- **Failure modes:** the corrected fusion result may invert and remove the desk's only positive
  fusion finding — that is the *point*, and it must land in the graveyard either way (M4).
- **ROI:** very high per hour. **Confidence:** 0.95. **Retirement:** when the fence exists.
- **Also fix while there:** `signal_halflife`'s `rolling_ic` window anchoring (D2) — a decay
  tracker whose last window excludes the newest 20+ days cannot do its job; and add an
  idempotency key so a double run does not double-append.

### M3 ◆ — Point `module_justification` at the whole repo. [rank 3]
- **Exactly what:** `scripts/module_justification.py:68` also globs `libs/**/*.py` (and ideally
  `api/`, `app/`); then audit every other self-measuring organ's glob for the same frame error
  (U1).
- **Why:** D6. 43% coverage produced three consecutive audits that concluded a working, tested
  capability did not exist. Every "we know what we own" claim inherits this denominator.
- **Evidence:** `:68` glob; 264 of 613; the six-module `in justification set: False` check.
- **Expected benefit:** immediate — the first run will produce verdicts on 349 unmeasured
  modules and D7's layer will surface as INERT-with-passing-tests, which is a *different and
  more actionable* verdict than "absent". **Compounding:** every future audit and every future
  ERV-deletion decision reasons over the true codebase.
- **Complexity:** TRIVIAL for the glob; LOW for the sweep of sibling organs.
  **Validation:** module count ≈ 613; the six named modules appear with verdicts.
- **Failure modes:** a flood of INERT verdicts on libs/ swamping triage → sort by LOC × age and
  report the top 20, don't dump 349. **Maintenance:** none. **Monitoring:** module count in the
  daily line, so a future scope shrink is visible (the §35 anti-denominator-shrink pattern).
- **Retirement:** never.

### M4 ◆ — Make screen and engine negatives permanent, automatically. [rank 4]
- **Exactly what:** 07-29's M4 (powered SCREEN-WEAK/artifact verdict → graveyard row + NK
  record) **extended to engine-level negatives**: the fusion engine's 4 no-lift combos are
  exactly as valuable as a killed axis and are currently in one overwritable file.
- **Why:** D4 + U7. Two instances in two sweeps means systemic, not a seam. The novelty gate
  compiles priors from `docs/graveyard.md`, so an unrecorded negative is an invitation to burn
  multiplicity budget re-deriving it — the universal-duty failure arriving by amnesia.
- **Evidence:** `grep -ci fusion docs/graveyard.md → 0`; fusion not in `negative_knowledge.json`.
- **Complexity:** LOW. **Validation:** `_graveyard_priors()` count increments without
  hand-coding; a re-proposed multi-source fusion is flagged redundant.
- **Failure modes:** graveyard noise from underpowered cells → ingest POWERED negatives only,
  and record the fusion combos with their `SCREEN-UNDERPOWERED` status attached rather than as
  refutations. **Confidence:** 0.85.

### M5 ◆ — Join the two halves of alpha genealogy (one caller + one dict persist). [rank 5]
- **Exactly what:** persist `AlphaFamilyTree`/`ResearchGraph` to JSONL (they are plain dicts),
  and populate `research_memory.predecessor_id` on the first iterated construction family — the
  270-cell tape sweep is a variation family by construction, so the parent is known for free.
- **Why:** D7 + U4. The desk owns a tested lineage layer and a persistent lineage column and has
  joined neither; `best_lineage()` ("which research direction compounds into edge") is precisely
  an L1.15 second-order-compounding instrument.
- **Evidence:** 91 tests pass; zero callers; `self._nodes: dict = {}`; predecessor_id 0/140.
- **Complexity:** LOW. **Validation:** one queryable chain of ≥3 generations; then judge whether
  anyone queries it before building more (the honest E-criterion from 07-29's M6).
- **Failure modes:** building a platform nobody queries → cap at one family, one pilot.
- **Note:** M3 should land first, so the decision to wire vs delete is made on a verdict.

### M6 — Replace the ROI backlog's 32 presence gates with outcome gates. [rank 6]
- **Exactly what:** each gate asserts a *property of the artifact*, not its existence:
  `execution_tca_fill_log` → `web/tca.json` exists AND covers ≥N fills AND is <48h old;
  `black_swan_library` → a replay harness ran ≥1 scenario against ≥1 candidate;
  `trade_logging` → tape row count grew this week.
- **Why:** D8/D9. 32/32 presence checks is the doctrine's prime quarry (self-greening gates) at
  the level of the desk's own priority engine. It is *why* D5 stayed invisible for 26 days.
- **Complexity:** MED (32 gates, each needs a real predicate). **Do it incrementally**: convert
  the gate the moment its item reaches the top of the ROI list, so the cost is amortized and the
  next build is validated by construction. **Confidence:** 0.8. **Maintenance:** slightly
  higher than `.exists()` — that is the price of the gate meaning something.
- **Failure modes:** an over-strict predicate marking a real completion incomplete (the
  gate-optimality duty cuts both ways) → each predicate must be satisfiable by a stated,
  reachable state, and logged when it flips.

### M7 — Dataset-level REC / decay / DIVR / licence fields in `data_registry.json`. [rank 7]
- **Why:** D10. The constitution's purge rule ("low-REC high-decay low-DIVR data is purged") has
  no operands. Four fields per source, hand-scored once, then re-scored on the quarterly review
  that `data_registry.json` already schedules (`next_quarterly_review` exists).
- **Complexity:** LOW (it is a metadata pass, not an engine). **Guard against theater:** a
  hand-scored REC number that nobody acts on is padding — the acceptance criterion is that the
  first scoring pass *names at least one dataset to purge or one to prioritize*, or it is not
  worth the row. **Confidence:** 0.6 (lowest here — this is the item most at risk of being
  ceremony). Ranked last for that reason, honestly.

### M8/M9 — carried, unchanged, still right: regime-label history (D11 — **fourth** sweep, and
this now belongs in the carry-over brief as a [SKIPPED]-class item that a live cycle must either
do or explain in writing) and the interaction/event-study families (D12).

**Opportunity cost of NOT acting, 1y.** M1 unfixed: 365 days × ~20 fills of permanently
un-analysable execution reality, and L1.5 stays a forecast rather than a measurement — the single
largest irreversible loss on this page. M2 unfixed: the decay organ and the fusion record keep
publishing phantom numbers on a retracted axis, and the next retraction inherits the same
enforcement failure. M3 unfixed: every capability audit keeps reasoning over 43% of the code and
the desk keeps rediscovering things it already built (three sweeps, one layer). M4 unfixed: the
novelty gate decays as unrecorded negatives accumulate. M5 unfixed: pure deferral, no loss. M6
unfixed: the priority engine keeps marking presence as completion, which is the mechanism that
hid M1 for 26 days.

---

## 4. WHAT WE TEST NEXT — concrete experiments

**E1 (executes M1) — TCA field-coverage repair + backfill probe.**
Success: (a) 100% of new tape rows carry all nine execution-quality fields, verified by re-running
the field-frequency script; (b) `web/tca.json` exists with ≥1 week of slippage attributed into
spread/impact/timing via the existing `PostTradeTCA`; (c) a coverage % line appears in the daily
cycle output so a regression is visible next day. Separate probe: for the 26 lost days, attempt
mid-reconstruction from `data/moat/bybit` at each fill's `opened`/`closed` timestamp and report
what fraction of the 513 records can be *approximately* repaired and at what timestamp distance
— an honest approximation with its error stated, never presented as a measured fill.
Validation: compare the 4 genuine `spot_slip_bps` records against their reconstructed values;
that is the fixture that calibrates the approximation. Retirement: never (archive).

**E2 (executes M2) — leak eradication + re-derivation.** Success: `grep -rn
"candle_date_time_utc" --include=*.py .` returns exactly one file; `fusion_engine.json` re-run
under corrected keying with the result recorded **whichever way it lands**; a correction row
appended to `signal_halflife.jsonl`; and `fusion_verdict` amended to require
`r["verdict"] not in {"SUSPECT-LOOKAHEAD","TIMING-ARTIFACT"}` before it may say "ADDS VALUE".
Pre-registered expectation (so the result cannot be reinterpreted after the fact): the
`flow_plus_regional` lift falls below +0.02 and the combo becomes "relabelling, not fusion".
Also shift-test `stablecoin_supply` (U3) — it is on a live clock with an implausible |IC| 0.2102.
Retirement: when the grep fence is in CI.

**E3 (executes M3) — whole-repo module census + glob audit.** Success: `module_justification`
reports ≈613 modules; `libs/alpha_factory/*` appear with verdicts; and a one-page table lists
every self-measuring organ (`unobserved_observables`, `coverage_audit`, `dependency_graph`,
`data_vitals`, `screen_auditor`, the §35 ratchet) with the glob it actually uses vs the scope it
claims. Success criterion for the second half: **at least one more frame error found, or an
explicit evidence-backed "all other organs are whole-repo"** — U1 says a null result here is
itself worth having. Retirement: when a fence asserts the module count never shrinks.

**E4 (executes M5) — one lineage chain, end to end.** Success: three generations of one tape
construction family recorded with `predecessor_id` populated and `AlphaFamilyTree.lineage()`
returning the chain from persisted state after a fresh process start. Judgement gate: if nothing
queries it within 30 days, record that and stop — do not build the platform.

**E5 (executes M4) — negative-permanence round trip, engine edition.** Success: the four
no-lift fusion combos appear as graveyard rows with their mechanism ("equal-weight composite
tracked its strongest member; same-mechanism members add no orthogonal information") and
`SCREEN-UNDERPOWERED` status; `_graveyard_priors()` count increments; a re-proposed multi-source
fusion is flagged redundant without hand-coding.

---

## SIX PERSPECTIVES

**1. INTERNAL (measured, not configured).** 07-29's M1 landed (commit 8da8acd: *"pbo 0/420 →
209/420, measured"* — the welded gate is unwelded, which was correctly ranked #1). M5 (regime
history), M7 (interactions), M8 (events) did not, for the fourth/third/third time. New this
sweep: three capabilities exist and are not connected (D5, D7, and the black-swan library), one
retraction was declared complete while half its copies still leak (D1), and the organ that
measures deadness measures 43% of the code (D6). The pattern across all five is identical and
worth naming: **this desk's bottleneck is not building, and it is not even conversion in the
§33 sense — it is *connection*.** Artifacts get built to a high standard and left one import
short of doing work, and the presence-gate backlog (D9) cannot tell the difference.

**2. EXTERNAL (how a world-class team improves this).** Any serious execution desk treats the
fill log as sacred and would have per-fill decision/arrival/fill/mid/latency from day one — D5
would be a firing offence at a prop shop, not a backlog item, because the data is gone. Second:
a pod shop's "what do we own" inventory is generated from the repo, not maintained per-directory
(D6). Third: nobody would let the only positive fusion result stand on a component retracted two
days later without re-running the notebook (D3) — the standard practice that prevents this is
*deriving artifacts from a pipeline* rather than from hand-run one-shots (D4: `fusion_engine.py`
is unscheduled). The desk's *statistical* discipline is genuinely at or above pod-shop standard
(pre-registered combos, Bonferroni, equal-weight-only, beats-best-component, powered/underpowered
distinction, Holm-corrected concurrent slots). Its *data-engineering* discipline is a tier below,
and the gap between the two is where every finding in this report lives.

**3. FUTURE (2-3y).** The substrate being captured now is the asset: multi-year own-timestamp
tape, own fills, kills with mechanisms, append-only verdict streams, causal edges with epistemic
states. Two things change the calculus by 2028. (i) **Local models make retrieval over research
memory free**, at which point 140 rows of memory + a populated lineage graph + the graveyard
become a queryable research corpus — which is an argument for M5 now (the *schema* must be right
before the volume arrives; retrofitting parents onto 5000 rows is much harder than writing them
on row 141). (ii) **Execution-microstructure models trained on own fills** are the one thing a
2028 open-source release cannot hand us, because the training data is our own order flow — which
makes D5 not just a data-quality bug but the destruction of the input to the most defensible
model the desk will ever own. Retrieval/embeddings over memory stay correctly deferred (140 rows,
lifting condition ~1k or a second recall consumer) — but note D7: the embedding *code* already
exists, so the deferral is now about data volume only, not build cost.

**4. CONTRARIAN (assumptions actively tested).**
- *"The tape is the desk's #1 information advantage (score 1.026)."* **Partly unsupported.**
  Orderbook state at our timestamps is genuinely irreplaceable; trades/klines/funding are
  published by both venues and re-downloadable. The score treats 6.0G as one undifferentiated
  asset. The per-stream classification and — more urgently — whether the irreplaceable part is
  **backed up anywhere** was not resolved this run (U5). A 6.0G moat on one disk is a moat with a
  single point of failure, and the desk has not established which it has.
- *"Composition is the edge."* **Weaker than last week, not stronger.** The n=1 composed
  survivor (kimchi) was retracted as ~73% artifact; the desk's only 3+ source fusion test came
  back negative on 4 of 5 combos (D4); the one positive is look-ahead-suspect on a leaky
  component (D3). Honest posture: *composition remains the best-motivated class and has zero
  clean validated instances.* Anyone citing "orthogonal fusion adds value" is citing a bug.
- *"More enforcement organs = more integrity."* **Tested and found wanting, with a number.**
  ~40 steps run in the daily cycle; 32/32 ROI gates are presence checks; two steps have failed
  every cycle on a shell-quoting bug nobody noticed (U9). Enforcement volume is not enforcement.
  The single highest-integrity artifact in this report — `libs/research/upbit_data.py`'s
  derivation docstring — was written by hand and enforced by nothing, which is exactly why two
  copies survived it. **One mechanical grep-fence would have beaten the entire prose.**
- *"The desk knows what it owns."* **False, measured.** D6: 43%. And the consequence was not
  hypothetical — three audits called a tested capability absent.

**5. GREENFIELD (rebuild with only validated knowledge).** *Keep:* the axis_screen harness with
its artifact gate and powered/underpowered distinction; append-only verdict streams
(`stage_a_verdicts.jsonl` is the right shape and should be the template for everything); the
execution tape's additive/exception-safe observer pattern (K2); the graveyard-with-mechanism;
Holm-corrected concurrent forward slots; `upbit_data.py`-style derivation docstrings. *Would not
rebuild:* one-shot hand-run research scripts that write authoritative artifacts (D4 — every
artifact should have a scheduled producer or be marked TERMINAL, which §36 already says); a
priority engine whose gates are `.exists()` (D9); dual doc+JSON negative stores needing manual
sync; in-memory-only graph classes with no persistence (D7 — the greenfield version writes JSONL
from line one); and `libs/alpha_factory`'s 23-module June build, of which ~9 modules have never
been called — greenfield would build `hypothesis_novelty` (the one that got wired) and defer the
rest until a caller existed. **Baggage score: moderate, and the shape is specific — not
complexity, but *disconnection*.** The repo's problem is not too many layers; it is layers that
never got their one import.

**6. FRONTIER (recently publicly possible).** Nothing purchased or adopted this sweep, and the
honest reason is that **the frontier is not this subsystem's binding constraint** — a desk with
0.8% fill-coverage and a leaked signal in its decay archive does not need a new method. Checked
and explicitly rejected for now: vector-DB/embedding retrieval over research memory (the code
already exists per D7 and the data does not justify it at 140 rows); graph-DB adoption for
`ResearchGraph` (plain dicts + JSONL is correct at this scale and networkx is not even a
dependency — `grep -rl networkx libs → empty`); LLM-based feature synthesis (would multiply
multiplicity burn against a gate the desk only just unwelded). The one frontier item genuinely
worth flagging: microstructure/execution models trained on proprietary fill tapes are becoming
standard practice at small shops precisely because the *data*, not the method, is the moat —
which is the FUTURE argument for M1, not a new capability to buy.

---

## NEGATIVE-SPACE SWEEP — what has never been looked at at all

1. **Own-fill microstructure.** 0.8% coverage (D5). Never asked: what is our realized queue
   position, our fill ratio by mode, our adverse selection by time-of-day, our slippage as a
   function of size? All answerable from a properly-fielded tape and none of it recorded.
2. **Dataset-level economics.** No REC, no DIVR, no decay, no licence field anywhere (D10).
   Never asked, for any of ~40 sources: what would it cost a competitor to rebuild this?
3. **Whether the moat is backed up.** Never asked in any audit of this subsystem, including
   this seat's three prior reports. 6.0G, one machine, unresolved (U5).
4. **Feature interactions and event windows** (D12) — the two cheapest composition families.
5. **Regime-label history and the rule-vs-HMM disagreement stream** (D11) — an unrecorded
   dataset generated daily and destroyed daily, four sweeps running.
6. **Market-state fingerprints / analog retrieval.** `grep -rli "nearest_neighbour\|nearest_neighbor\|knn\|analog" libs scripts` → nothing in libs; only incidental prose in three
   scripts. The desk has never asked "what are the 20 most similar historical market states to
   today, and what happened next?" — a classic, cheap, composition-native moat feature that
   needs only the bars it already holds and the embedding code it already owns (D7).
7. **Cross-organ verdict reconciliation.** Three artifacts hold three contradictory verdicts on
   kimchi (D1) and nothing compares them. There is no organ whose job is "do our stores agree?"
8. **The failure modes of our own collectors under adversarial conditions.** `HTTP 418` killed
   two steps today (U9); no synthetic outage has ever been simulated against the moat recorders
   (`grep -rl "stress_lab\|synthetic_stress" libs scripts` → empty).
9. **Provenance of derived series.** No composed axis records which source revisions produced it,
   so a vendor restatement is undetectable after the fact.
10. **Whether any of the 447 feature-library grammar cells is a composition.** The grammar is
    `OBSERVABLE × TRANSFORM × WINDOW × NORM` — single-observable by construction (D12). Nobody
    has asked what a 2-observable grammar would cost.

---

## PROACTIVE BATTERY — moves run this sweep

- **(2) ADJACENCY** — *ran, and it produced the sweep's top finding.* Given the 07-29 kimchi
  keying fix, asked "where else does this exact shape exist?" → found two uncorrected copies
  (D1), one of which wrote a verdict during the audit.
- **(3) CONFIG VS OUTCOME** — *ran.* Demanded the artifact for every claimed capability: the
  black-swan library has no consumer (D8), TCA has no inputs (D5), the genealogy layer has no
  caller (D7), 32/32 ROI gates are presence checks (D9).
- **(6) GENERALISE THE RULE** — *ran.* "A third copy anywhere is a defect" was written as prose
  in one module's docstring and enforced nowhere; M2 converts it to a grep fence.
- **(8) NEGATIVE SPACE** — *ran*, 10 items above; #3 (is the moat backed up?) had never been
  asked by any prior sweep of this subsystem.
- **(10) RATCHET CHECK** — *ran, and it fired.* `module_justification`'s 264-module count has no
  floor and no alarm; a glob that silently covers 43% is the same denominator failure §35 forbids
  for findings coverage. M3 adds the floor.
- **(1) CONTINGENCY BEFORE FAILURE** — *ran, produced an unresolved question, reported as such:*
  the replacement for `data/moat` is "re-download from venue archives" for the replaceable
  streams and **nothing** for the irreplaceable ones. Backup status unresolved (U5).
- **(4) REGRESSION SWEEP** — *ran on this report's own recommendations:* M1 touches a live
  executor (mitigated by the additive/exception-safe pattern); M3 will flood triage with libs/
  INERT verdicts (mitigated by top-20 reporting); M6 risks over-strict gates rejecting real
  completions (mitigated by the gate-optimality duty's satisfiability requirement).
- **(5) COST INVERSION** — *ran, nothing found:* no paid path is proposed or implied anywhere in
  this subsystem this sweep. Every item is free-first by construction.
- **(7) AUTONOMY CHECK** — *ran, and it fired:* `fusion_engine.py` has never been *seen* to run
  on a schedule because it is on none (D4); `signal_halflife` is scheduled and *has* been seen to
  run (02:41 today) — which is how D1's live consequence was caught.
- **(9) SCOPE THE NEGATIVE RESULT** — *ran on D4:* the 3+ source fusion negative is scoped to
  *equal-weight z-composites of same-mechanism liquidity proxies*, not to fusion as a capability.
  The route failed, not the capability — and that distinction must go in the graveyard row (E5)
  or the desk will over-generalize a single negative into "fusion doesn't work".

---

_Audit complete. Findings D1–D12 are command-verified above; U1–U9 are named ignorance, not
hedging. Two delegated probes (tape quality/irreplaceability/backup — U5; cross-store
reconciliation — U7) were launched in parallel this run and had not returned at write time;
their questions are recorded in the ignorance ledger rather than answered, which is the honest
disposition. The single sentence that matters: **the desk's data-moat problem is not
acquisition, and this week it is not even conversion — it is that finished capabilities sit one
import short of working, and the gate that should notice checks whether a file exists.**_
