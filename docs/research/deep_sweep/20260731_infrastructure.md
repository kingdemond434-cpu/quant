# WEEKLY DEEP COLD AUDIT — INFRASTRUCTURE — 2026-07-31

STATUS: COMPLETE

Auditor: infrastructure subsystem, weekly deep cold sweep (doctrine v2).
Method: outcome-not-config — every claim carries its proving command + output. READ-ONLY run.

## SCORES

- **current_capability_pct: 62%** — the money-path supervision, recorders, alert *delivery*, and rollback-safe deploy are outcome-verified and genuinely good (S1-S8). But the subsystem is carrying two live security holes (F2 push-capable PAT feeding a 10-min auto-deploy; F8 unauth `0.0.0.0` HTTP + public tunnel), a live data-corruption (F1 doubled collectors), an active deploy outage (F3), a ~30-day disk fuse with zero offsite copy of the irreplaceable moat (F5), a crying-wolf critical pager (F6), and three fences that certify their own defect (F4 dead referee, F9/F10 0.0 floors). Working ≠ safe.
- **practical_ceiling_estimate: 93%** — single supervision plane + deterministic gate + disk/rotation/drift fences + rebind+Access + offsite sync are all achievable with in-repo tools; the residual ~7% is off-box DR robustness that needs a small recurring spend.
- **ceiling_gap: 31 pts**, dominated by cheap-high fixes: F1 (hours), F3 (trivial), F5 disk fence (trivial), F6/F9/F10 (low).
- **opportunity_cost_1y: VERY HIGH.** Catastrophic tails: F5 = permanent moat loss on one disk/box failure; F2 = supply-chain path to a (post-Gate-0) live-key box. Unbounded: F1 = a doubled-density artifact can manufacture a phantom Stage-A survivor (negative discovery). Chronic: F3 deploy latency on money-path fixes; F4 quota mis-spend suppressing completed evidence.
- **confidence: 0.9** — every finding carries a reproduced command + output; soft spots are U1/U2 (firewall + Cloudflare Access unreadable without root) and U5 (CI flake mechanism not bisected).
- **unknown_unknown_score: 0.4** — moderate-high. The largest un-probed seams (host firewall U1, Cloudflare Access U2, provider backups U4, ntfy topic guessability U3) each could be materially worse than assumed, and all need one operator action to resolve.
- **info_gain_if_investigated: HIGH for U1/U2/U3/U4** — each is a yes/no that swings the risk picture; MED for U5/U6.
- **expected_alpha_contribution:** indirect but real — F1 removes a live data artifact that could fake a survivor (negative discovery is a scored defect); F4's referee protects the completed-evidence rate that discovery depends on.
- **expected_compounding_contribution: HIGH** — F5 offsite + F4 per-organ token ledger + F7/F9/F10 honest fences are the ⚙ multipliers: each removes a recurring silent-failure or mis-spend class, raising the reliability floor every future organ compounds on.
- **ceiling_expansion:** the ceiling here is bounded *organizationally*, not technologically — split-brain supervision (cron ⊕ systemd, watchdog.py:64-76) and laptop-era `0.0.0.0` binds are historical baggage. Collapsing to one plane + hardening the network surface lifts the ceiling above 93% by deleting the F1/F3/F7/F8 classes at the root. The single hard external wall is off-box DR, which needs storage spend / an operator decision — everything else is in-repo and self-serviceable.

## 1. WHAT WE KNOW (validated strengths, each with proving command)

S1. **Recorders are alive and writing real data, not just heartbeats.** `ls data/moat/bybit/BTCUSDT/ | tail -3` → `20260731_01.jsonl.gz` (937KB, mtime 01:50, current hour); same for `moat/spot/BTCUSDT` (500KB) and `moat/fut/*` (fresh hourly gzips for 30+ symbols). Heartbeats (`data/recorder_*_heartbeat`) are seconds-fresh AND backed by data — this is NOT the silent-zero class. Three recorder daemons up since Jul 21-22 (`ps`: PIDs 426218, 483236, 483446).

S2. **The money-path supervision stack is layered and live.** systemd runs `quant-cashcarry.service`, `quant-deadman.service`, `quant-liquidations.service`, `quant-dashboard.service`, `quant-tunnel.service` (all `active running`, `systemctl list-units 'quant-*'`). The watchdog's `_systemd_owns()` (scripts/watchdog.py:57-100) correctly defers to live systemd units for exactly these processes — the 2026-07-26 dual-supervision orphan lesson is encoded. Dead-man switch process alive (PID 1463355), `data/deadman_state.json` written minutes ago.

S3. **pull_deploy (merge-is-deploy) has genuinely safe failure semantics.** `deploy/pull_deploy.sh`: fast-forward only, refuses dirty tree, reverts to prior commit on CI-red, never restarts the dead-man, computes restart blast-radius from import closure (`libs/ops/deploy_plan.py`) instead of a hand list. The revert path was PROVEN working this very night: `data/pull_deploy_state.json` = `{"ts":"2026-07-31T01:40:01Z","status":"ci-red","from":"7e7f3db","to":"a1d6509","note":"reverted -- run_ci.py failed on the new commit"}` — repeated every 10 min. The gate bites (see F3 for the cost side).

S4. **Alert delivery is real and measured.** `tail data/alert_delivery.jsonl` → ntfy `http 200` deliveries seconds apart at 00:36; hourly `run_alert_canary` writes an explicit armed/sent/silent verdict (`data/cro_ai_logs/alert_canary.log`). Pages ARE reaching the phone path (`data/.last_alerts.json` `_paged: [auth_broken, cadence_floor_violation, clock_slots_idle, growth_defect, principal_action_needed, trade_class_bleeding]` at 01:16Z). The canary honestly reports the known ntfy SPOF (gap #38) every hour instead of greening it.

S5. **organ_catchup + brain mutex work as designed.** `tail data/cro_ai_logs/organ_catchup.log` → every 5 min: "field busy (deep_sweep running) -- holding retries" — one-brain-at-a-time is enforced (`ops/brain_env.sh` `brain_mutex`), and the catchup re-fired the prospector at 22:00 after its 18:00 systemd unit failed; that run COMPLETED and pushed (`data/cro_ai_logs/prospector_20260730T2200.log`: "Run complete, committed and pushed (051fe70, verified on origin)"). Organ recovery has been SEEN to work (battery #7), not just configured.

S6. **No OOM kills in 7 days** (`journalctl -k --since "7 days ago" | grep -i oom` → empty; `dmesg` same) despite 3.7GB RAM / no swap / load ~1.6 on 2 cores. Current headroom is thin but real (see F5 for the 08:21 threat to this).

S7. **Secrets hygiene at the filesystem level is correct.** `ls -la data/secrets/` → dir `700`, every live secret `600`, `.example` files world-readable by design; `data/*` gitignored (`.gitignore:11`), pack size 5.01 MiB (`git count-objects -vH`) — no data lake in history. (The one giant exception is F2: the GitHub PAT in the remote URL.)

S8. **fail2ban is running** (PID 1146655, since Jul 29) and sshd is the only system service exposed besides the desk's two web ports (`ss -tlnp`).

## 2. WHAT WE DON'T KNOW (ignorance ledger)

U1. **Host firewall state — unverifiable from inside.** `sudo -n ufw status` → denied; `iptables -L` → "you must be root". Both desk web ports bind `0.0.0.0` (`ss -tlnp`: 8080 serve_dashboard, 8090 ops_server). Whether a Hetzner Cloud Firewall filters them is invisible from this box. Until the operator confirms, assume the NAV/positions dashboard is internet-reachable at `<vps-ip>:8080` with zero auth (`curl 127.0.0.1:8080` → HTTP 200, 54KB full HTML, no auth gate).

U2. **Cloudflare Access on dash.quanttt.xyz.** `~/.cloudflared/config.yml` → ingress `dash.quanttt.xyz → http://localhost:8080`, no auth in config. Whether an Access policy exists on the Cloudflare side cannot be read from here. If not, the live book dashboard is public at a guessable hostname.

U3. **ntfy topic guessability.** `data/secrets/ntfy.json` holds a single 12-char mixed-case alpha topic (no digits) — shape is consistent with BOTH a random string and a CamelCase dictionary phrase. ntfy.sh topics are world-subscribable AND world-publishable: a guessable topic means anyone can read every page (incl. principal-action content) and forge pages to the principal's phone. Needs a human eyeball (is it random?) — not printed here by design.

U4. **Provider backups / disk snapshots.** No backup agent, no rclone/restic/borg in crontab or processes. Whether Hetzner's paid backup option is enabled is invisible from inside the box. Given F5 (no offsite copy of 8.1G moat+lake), this unknown is load-bearing.

U5. **Root cause of the two failing tests blocking deploys** (`tests/ops/test_organ_catchup.py::test_pick_prioritizes_brain_and_skips_running`, `tests/test_desk_integrity_checks.py::TestOrphanScripts::test_page_digest_is_reported_not_silently_wired`, from `.pytest_cache/v/cache/lastfailed`). The blocked commit 7e7f3db only ADDS `deploy/finish_setup.sh` (`git show --stat`), so these failures almost certainly pre-exist on the deployed HEAD — but I did not run them individually (read-only, and full pytest is ~8 min on this loaded box) to confirm which commit introduced them.

U6. **Whether the 08:21 thundering herd fits in RAM.** 13 cron jobs share the literal minute `21 8 * * *` (managed block), including `certify_gauntlet.py` (>25 min, per desk memory). The box: 2 cores, 3.7GB RAM, 0 swap, 1.1GB available now, live executor + 3 recorders + 2 web servers resident, and the one observed pytest process took 390MB RSS. 13 CPython+pandas processes at once plausibly exceeds available memory → OOM-killer roulette that could hit the executor. It has NEVER fired (block installed 2026-07-30T23:29Z; first firing is TODAY 08:21). Prediction, not observation — see T2.

U7. **Execution tape staleness: no-trades or broken pipe?** `data/moat/execution_tape/cashcarry_trades.jsonl` mtime Jul 28 15:20 while the executor holds 13 positions and writes `data/cashcarry_positions.json` minutes ago (01:56). The hold-top-3000 hysteresis ("holds while funding stays positive; sub-8h churn is pure cost", watchdog.py spawn comment) makes zero closes since Jul 28 plausible — but tape-write breakage would look identical. Distinguish via venue trade history vs tape (T6). NOTE: `data/nav_attestation.jsonl` last row says `mode: "PAPER (testnet) -- pre-Gate-0"`, equity_marked 13,154.86 — the book is paper, which caps the money-at-risk of every finding here, but NOT their research-continuity cost.

U8. **quota_verdict early-exit reason.** The referee organ that decides "does the full cadence fit the quota" last wrote its state 8 days ago (`data/quota_watch.json` mtime Jul 23 06:20) and both its cron logs are EMPTY (`tail data/cro_ai_logs/quota_watch.log`, `quota_verdict_cron.log` → nothing) despite two cron lines (`20 */3` legacy + `0 */3` managed). Whether it exits on a missing baseline marker or crashes silently was not determined — either way the organ is dead by artifact (F4).

U9. **Dependency staleness surface.** Python 3.12.13 (current). `pip list --outdated` not run (network + load). Unattended-upgrades daemon is running (ps), so OS patching is live; the venv's package drift is unmeasured — no fence exists for it.

U10. **Time sync quality.** chrony is installed (logrotate.d entry) but NTP offset was not measured. For a venue-timestamp-sensitive desk this should be a fenced metric, not an assumption.

## 3. WHAT COULD MATTER MOST (ranked findings + opportunities)

Ranked by expected impact × confidence / (cost × maintenance). Perspective tags: [INT]ernal, [EXT]ernal, [FUT]ure, [CON]trarian, [GRE]enfield, [FRO]ntier.

### F1. CRITICAL/IMMINENT — Dual scheduler planes: the managed cron block duplicated the legacy crontab; data corruption already live, double-brain and 13-job herd armed for today. [INT][GRE]

**Evidence.** `crontab -l`: the marker-fenced block "installed 2026-07-30T23:29Z by deploy/reconstitute_cron.sh (gap #58)" re-declares nearly every job that already exists in the legacy lines ABOVE the fence, which `reconstitute_cron.sh` preserves byte-for-byte by design ("everything outside the fence ... is preserved"). Concretely:
- `collect_defi_lending.py`: legacy `17 * * * *` (flock /tmp/defi_lend.lock) + managed `17 * * * *` (NO flock) → both fire the same minute. **Both ran at 01:17 today** (both log files written: `defi_lending.log`, `defi_lending_cron.log`) and both append to the SAME `data/defi_lending.jsonl`. **Row rate doubled exactly at the install boundary: 278 rows/hr (19:00–22:00 Jul 30) → 556 rows/hr (23:00 Jul 30 onward)** — every observation now recorded twice with different batch timestamps (not exact-line dupes: 0 exact-duplicate lines, so naive dedup won't catch it). Any Stage-A screen on this axis now double-counts N.
- `collect_oi_ls_live.py` (`32 * * * *` twice, same shape, same target `data/oi_ls_live.jsonl`), `collect_coinmetrics_flows.py` (47 3 twice).
- `ingest_axes.py`: legacy `40 6` **with `--tranche 400`** + managed `40 6` **without** — same minute, DIFFERENT lock files (`/tmp/ingest_axes.lock` vs `data/.cron_ingest_axes.lock`), so flock protects neither against the other: two concurrent ingests with different arguments racing on the same files at 06:40 today.
- `ops/run_cro_ai.sh`: legacy `45 2,14,20` (lock /tmp/cro_ai.cron.lock) + managed `45 2,14,20` (lock data/.cron_cro_ai.lock) — the brain scheduled TWICE at the same minute 3×/day. `brain_mutex` inside `ops/brain_env.sh` serializes actual reasoning (S5), so the second becomes a defer/no-op or a queued re-fire — quota-safe but scheduler-dirty.
- `watchdog.py` `*/3` twice (legacy flocked, managed unflocked, different logs — both proven ticking: both log tails advance) → every 3 min TWO watchdog passes each run `run_leverage_opt` + `run_live_combined` + `data_health` + `run_alerts` subprocesses (watchdog.py:207-222) on a box already at load ~1.6/2 cores.
- `max_audit.py` (2×/day), `quota_verdict.py` (2 interleaved 3h grids), `kimi_hunter` (legacy daily + managed 6×/day + weekly deep), `crypto_factory` (2×/day), `dl_oi_ls_universe` + `daily_research_cycle` + `organ_catchup` + `ensure_recorder` + both recorder respawn lines — all doubled.
- **The 08:21 herd is NEW**: 13 managed entries share `21 8 * * *` (compute_performance, hold_optimizer, certify_gauntlet, run_autodiscovery, research_alpha_optimizer, run_carry_crowding, carry_viability, stageb_capacity, flatten_cookie, graveyard_resurrect, run_research_tick, build_panel_rulings) — the manifest author assigned one identical "inferred" slot to every daily job. First firing TODAY (U6).

**Mechanism.** The manifest is honest about being "RECONSTRUCTED ... NOT yet verified against the live box" and the installer is idempotent *within its fence* — but installing an unverified reconstruction ONTO the box that already runs the real crontab duplicates every job by construction. The installer's own comment names the failure class it reproduced one level up: "the append failure mode is how boxes end up running a job twice."

**Exactly-what to do.** (1) TODAY before 02:45: delete the legacy (pre-fence) job lines, keeping the managed block as the single source — after diffing the two for the entries where they genuinely differ (`ingest_axes --tranche 400` vs bare; deep-sweep weekly vs daily; kimi cadences) and porting the CORRECT variant into the manifest. (2) Stagger the 08:21 herd across distinct minutes/hours with flocks (certify_gauntlet alone in its slot). (3) Dedupe `data/defi_lending.jsonl` rows since 2026-07-30T23:00 (collapse per collection-batch timestamp per key) and check `oi_ls_live.jsonl`/`coinmetrics_flows.jsonl` for the same. (4) Add the missing fence: a `max_audit` check diffing live `crontab -l` against the manifest — duplicate-job detection is mechanical (same script scheduled ≥2×). Complexity: low (hours). Dependencies: none. Validation: `crontab -l | awk '{print $6}' | sort | uniq -d` empty; defi rows/hr back to ~278. Failure mode of the fix: deleting a legacy line whose managed twin has a WRONG reconstructed cadence — hence the diff-first step. ROI: removes live data corruption + halves scheduled brain contention + defuses U6. Confidence: 0.95. Retirement: never (the drift fence is permanent). Time-horizon: 1w = corruption stopped; 1y = scheduler-drift class extinct.

### F2. CRITICAL/SECURITY — Plaintext push-capable GitHub PAT in the remote URL feeds an auto-deploy loop: a working supply-chain kill chain to the box that will hold live keys. [EXT][CON]

**Evidence.** `git remote -v` → `https://ghp_…kasoI@github.com/kingdemond434-cpu/quant.git` (token redacted here; it is world-readable to any process running as `quant` in `.git/config`, and appears in every organ transcript that ever ran `git remote -v`). `deploy/pull_deploy.sh` fast-forwards from this remote **every 10 minutes** and restarts affected services. Chain: token leak (one transcript/log/screen-share) → attacker pushes to master → box deploys it within 10 min → attacker code runs as the user owning `data/secrets/binance_live.json`. The CI gate does not help (attacker commits green code). Gate-0 being un-passed (paper mode, U7) caps today's damage at IP + testnet; the chain goes live the day live keys land.

**Exactly-what.** (1) Rotate the PAT NOW (it has also transited session logs, including this audit's). (2) Replace with a fine-grained token, `contents:read` ONLY, stored via git credential store outside the repo dir; push via a separate credentialed path used only by `git_snapshot.py`. (3) Add commit allow-listing to pull_deploy (verify committer email/GPG or require branch protection + signed commits) so a pushed compromise doesn't auto-ship. Complexity: low. Validation: `git remote -v` shows no token; push with old token fails (401). Failure mode: forgetting the push path breaks `git_snapshot` — test both directions. ROI: removes the single highest-blast-radius security hole. Confidence: 0.9. 1y opportunity cost of NOT fixing: tail-risk = entire IP + (post-Gate-0) live keys.

### F3. HIGH/ACTIVE OUTAGE — CI red on the deployed tree freezes "merge is deploy"; the launch-day finisher has bounced every 10 minutes for 2+ hours. [INT]

**Evidence.** `data/pull_deploy_state.json`: `{"ts":"2026-07-31T01:40:01Z","status":"ci-red","from":"7e7f3db","to":"a1d6509","note":"reverted -- run_ci.py failed"}`; `data/cro_ai_logs/pull_deploy.log` shows the identical revert at 00:30/00:40/00:50/01:00/01:10/01:20/01:30/01:40. `data/.ci_last_run.json`: `{"ok": false, failed: ["lint (ruff)", "tests (pytest)"]}`. Ruff's entire complaint is ONE auto-fixable error ("Remove unused noqa directive" — `.venv/bin/ruff check .` → "Found 1 error. [*] 1 fixable"). Pytest: the 2 tests in U5. Blocked payload: 7e7f3db = "deploy: one-command finisher for launch day" — a launch-blocking artifact stuck behind a one-character lint error and 2 likely-pre-existing test failures, re-running the FULL ~8-min CI every 10 minutes (constant CPU tax on a saturated box: the pytest I caught at 01:40 ate 90% of a core).

**UPDATE (verified 02:02Z, completing auditor):** the gate SELF-CLEARED at 02:00 — `pull_deploy_state.json` → `{"ts":"2026-07-31T02:00:01Z","status":"deployed","from":"7e7f3db","to":"a1d6509","note":"12 paths; ... 1 sched"}`, HEAD now == origin == a1d6509. So this was NOT a stable regression: the identical commit went CI-red at 01:40 & 01:50 then green at 02:00 (`pull_deploy.log`), and `.pytest_cache/lastfailed` names two tests that no longer exist (`pytest ...::test_pick_prioritizes_brain_and_skips_running` → "not found: no match") — i.e. the red was a FLAKY/load-sensitive failure, not the trivial noqa alone, and it reverted good code for ~90 min under contention. The finding is therefore **non-determinism of the deploy gate** (runs the whole ~1099-test tree + stress with no per-step timeout on a 2-core/3.7GB box under concurrent organ load), not a currently-stuck commit. The one auto-fixable ruff error (`.venv/bin/ruff check .` → "Found 1 error. [*] 1 fixable") is still real and should be cleared regardless.

**Exactly-what.** Fix the noqa; make the gate deterministic — add per-step `timeout=`, run pytest with a fixed seed / `-p no:randomly` + `--maxfail`, and **retry-once on red before reverting** so a fluke does not revert good money-path code; page only on a STABLE (2×) red. Then two cheap hardenings: (a) pull_deploy should SKIP re-attempting an already-CI-red commit until the remote head CHANGES (state file already records `from` — one comparison), saving 8 min × 6/hr of compute; (b) page on the FIRST ci-red revert (it currently only lands in the state file for max_audit to notice). Complexity: trivial-to-low. Validation: `pull_deploy_state.json.status == "deployed"`, `to == 7e7f3db+`. Confidence: 0.95.

### F4. HIGH — The organ economy runs unrefereed: cadence inflated by inference (deep sweep now DAILY by codified accident) while the quota referee has been dead 8 days. [CON][INT]

**Evidence.** (a) Manifest entry for the deep sweep, verbatim: "header says weekly (ops/run_deep_sweep.sh:2) yet dated artifacts landed 07-26/07-28/07-29 ... Daily slot chosen. CONFIDENCE: inferred" — i.e., organ_catchup's quota-death RE-FIRES of a weekly organ were read as evidence of daily cadence and codified as `0 1 * * *`. The legacy weekly line (`0 4 * * 0`) also survives → both. (b) 7 deep-sweep launcher logs in 24h (`ls data/cro_ai_logs/deep_sweep_2026073*.log` → 0200/0740/1200/1700/2245/2345/0035). (c) The cost is visible in output: yesterday's validation-stats report died as a 1,889-byte skeleton (`head 20260730_validation-stats.md` → "_Status: IN PROGRESS (skeleton...)"), and positions run in fixed dict order (run_deep_sweep.py:167 `for key in SUBSYSTEMS`), so tail positions systematically get starved quota (desk memory: position-8 starvation; the runner's per-day resume mitigates but each new day restarts the race). (d) The organ built to adjudicate exactly this — quota_verdict, "page a clear YES/NO" — is dead by artifact: `data/quota_watch.json` mtime Jul 23, both logs empty (U8). So cadence ratchets up (doctrine pressure + inference), completion is partial, and NOTHING measures whether the schedule fits the quota. This is the contrarian finding: past a quota-bound point, MORE cadence yields LESS completed evidence — currently invisible because the meter is off.

**Exactly-what.** (1) Revive quota_verdict (find its early-exit; give it a heartbeat fence). (2) Decide deep-sweep cadence EXPLICITLY (weekly full 8 + daily 2-subsystem rotation is the natural max-coverage-per-quota shape) and encode it in the manifest with CONFIDENCE: decided. (3) Add a per-organ token-spend ledger (the negative-space metric nobody records: quota spent per organ per day) so cadence decisions become arithmetic. Complexity: medium. ROI: every organ's completion rate rises; the 8-positions×daily×max-effort spend stops cannibalizing positions 5–8 and the overnight brain. Confidence: 0.8. Retirement: when quota >> schedule (bigger plan), the referee stays as a cheap floor.

### F5. HIGH/~30-DAY FUSE — Disk runway ≈ one month; no disk fence exists; the irreplaceable moat has zero offsite copies. [INT][FUT]

**Evidence.** `df -h /` → 23G available (36% used). Moat growth: `find data/moat -name '20260729*' | du -cb` → 719,480,309 bytes; Jul 30 → 693,990,455 — **~0.7GB/day**, so ~32 days to disk-full (before counting lake growth, logs, venv). At disk-full: recorders die mid-hour, executor state writes fail, git operations fail — the whole desk seizes. No fence: `grep -rn disk scripts/max_audit.py scripts/data_health.py scripts/check_ratchets.py | grep -iE 'free|usage|space|full'` → ZERO hits. Nothing pages before the wall. And the data is single-copy: `.gitignore:11` excludes `data/*` (correct), no backup tooling anywhere (U4) — `data/moat` 6.6G + `data/lake/bronze` 1.5G of historically-monopolised recordings (constitution L1.11: "historical monopolisation" is a named moat pillar) live on ONE disk of ONE VPS. The gap-#58 DR work restored the SCHEDULER; a dead disk still destroys the moat forever. The 2026-07-30 infrastructure sweep (122KB) predates none of this — the growth rate makes it a fuse, not a chronic.

**Exactly-what.** (1) TODAY: disk fence in max_audit/check_ratchets — page at <15% free, hard-page at <8% (trivial). (2) THIS WEEK: offsite sync of moat+lake to zero/low-cost object storage (Hetzner Storage Box ~€4/mo for 1TB, or Cloudflare R2 free-egress) via hourly rclone of closed hourly files (they're immutable once the hour rolls — perfect incremental shape); fence it OUTCOME-style (list remote, compare counts/bytes, alert on lag >24h). (3) Lifecycle: after N days verified-offsite, recompress locally (gz→zstd -19 --long ≈ 25-40% smaller on tick jsonl) or evict cold months to remote-only. Complexity: low-medium. Dependencies: €4/mo approval (names its free alternative per doctrine: R2 free tier ~10GB won't hold 8G growing — Storage Box is the honest primary; the paid-data trigger does not apply to STORAGE of our own data, but the €-decision goes to the principal). Validation: restore-drill ONE symbol-day from remote and diff bytes (T5). ROI: converts a certain ~30-day outage + permanent-moat-loss tail into a €50/yr line item. Confidence: 0.9.

### F6. HIGH/ALERT-QUALITY — The critical pager cries wolf by design: auth_broken contradicts the desk's own auth scheme; six classes paged at once. [INT][CON]

**Evidence.** `scripts/run_alerts.py:124` — `_auth_broken()` returns `not Path("/home/quant/.claude/.credentials.json").exists()`. `ops/brain_env.sh:8-10` documents the CURRENT auth: "setup-token does NOT update ~/.claude/.credentials.json — the token only" lives in `CLAUDE_CODE_OAUTH_TOKEN` from `data/secrets/claude_oauth_token`. The file does not exist (`ls` → ENOENT) while organs demonstrably reason (the prospector completed and pushed at 22:00; this audit is running). So auth_broken has paged every 12h (its dedup window) since the Jul 19 token switch — ~24 false CRITICAL pages — and it fired again in tonight's batch: `data/.last_alerts.json` `_paged: [auth_broken, cadence_floor_violation, clock_slots_idle, growth_defect, principal_action_needed, trade_class_bleeding]`. Six simultaneous critical classes, at least one provably false, trains the principal to skim pages — the one behavior a dead-man-adjacent pager cannot afford. (pager_delivered_24h's ratchet floor being 0.0 — F10 — compounds this: the fence tolerates silence, the pager emits noise.)

**Exactly-what.** (1) Fix `_auth_broken` to test the CAPABILITY: token file exists AND (cheap, no-quota) `claude auth status`-equivalent or last-organ-succeeded-within-24h from logs — outcome, not file. (2) Audit the other 5 paged classes for the same config-vs-outcome inversion (battery #3) — `trade_class_bleeding` and `growth_defect` on a PAPER book deserve a mode-aware severity. (3) Alert-quality metric: false-page rate, fenced and ratcheted (today unmeasured = 0-utilisation by L1.28a). Complexity: low. Validation: 48h with zero auth_broken pages while organs run. Confidence: 0.9.

### F7. MEDIUM — Watchdog spawned a doomed tunnel ~3,307 times: quant-tunnel is missing from its systemd-defer map and nothing writes the tunnel heartbeat. [INT]

**Evidence.** `grep -c "re)started public-tunnel"` → 3,247 (legacy log) + 60 (managed log). The REAL tunnel is fine: `quant-tunnel.service` active, cloudflared PID 36532 up since Jul 12 serving dash.quanttt.xyz. But watchdog gates on `data/tunnel_heartbeat` freshness <120s (watchdog.py:200) which only its own `scripts/run_tunnel.py` writes — the systemd cloudflared never does — and `_UNITS` (watchdog.py:57) maps only cashcarry/deadman/liquidations/dashboard, NOT quant-tunnel. Every 3 minutes (×2 watchdogs, F1) it spawns `run_tunnel.py`, which dies against the existing named tunnel, forever. Cost: ~1,600 futile process spawns/week, two log streams of noise burying real respawn events (the exact crying-wolf channel where a REAL executor respawn should stand out), and a per-tick "acted" that self-greens the watchdog's apparent usefulness.

**Exactly-what.** Add `"scripts/run_tunnel.py": "quant-tunnel.service"` to `_UNITS` (the defer already handles live-unit detection), or make the tunnel check `_port_open(20241)`/unit-liveness instead of a heartbeat file nobody writes. One line + one test. Validation: zero "(re)started public-tunnel" lines over 24h with the unit up; kill the unit in a drill → watchdog spawn fires (keep the backstop honest, battery #7). Confidence: 0.95.

### F8. MEDIUM/SECURITY — Both desk HTTP surfaces bind 0.0.0.0 with zero auth; the public tunnel adds a third, globally-routable, unauthenticated path to live book state. [EXT]

**Evidence.** `ss -tlnp`: `0.0.0.0:8080` (serve_dashboard — `--host default 0.0.0.0  # bind LAN so the phone can reach it`, a laptop-era comment now running on an internet VPS) and `0.0.0.0:8090` (ops_server, `HTTPServer(("0.0.0.0", port))`, GET-only — read-only by code inspection, no auth). `curl` both → 200, full desk HTML. Tunnel ingress → 8080 (U2). Exposure: equity curve, positions, strategy names, venue — strategy IP and personal-financial opsec, plus BaseHTTPRequestHandler's non-hardened, single-threaded, unauthenticated GET surface as an exploit/DoS target on a box that (post-Gate-0) holds live exchange keys. The `0.0.0.0` bind is gratuitous: cloudflared reaches the dashboard via `localhost:8080` (`~/.cloudflared/config.yml` ingress), and the phone reaches it via `dash.quanttt.xyz` — nothing needs the LAN bind the laptop-era comment describes.

**Exactly-what.** (1) Rebind both servers to `127.0.0.1` (serve_dashboard `--host 127.0.0.1`; ops_server `HTTPServer(("127.0.0.1", port))`) — the tunnel keeps the dashboard reachable, ops_server was never meant to be public. (2) Put Cloudflare Access (email/OTP policy) in front of `dash.quanttt.xyz` so the tunnel path itself is authenticated (U2). (3) Confirm the host/cloud firewall drops inbound 8080/8090 (U1, needs root). Complexity: trivial (2) + low (1,3). Validation: external `curl <vps-ip>:8080` / `:8090` refused; `dash.quanttt.xyz` prompts for Access. Confidence: 0.9. 1y opportunity cost of NOT fixing: strategy-IP leak (permanent once scraped) + a live-key box with an unauthenticated network surface.

### F9. MEDIUM/RATCHET-DEFECT — The `miner_seats_productive` floor is pinned at 0.0: the ratchet that is supposed to force miner productivity UP currently certifies zero as acceptable. [INT][CON]

**Evidence.** `data/ratchet_floors.json` → `miner_seats_productive = {"value": 0.0, "recorded": "2026-07-30T07:19:13Z", "proving_command": "python scripts/check_miner_runway.py --json --report-only"}`. Constitution L1.0 makes today's value the permanent FLOOR with a 100% target; a floor of 0.0 is a ratchet that has never engaged — it tolerates the exact state (no productive miner seat) it exists to prevent, and cannot fall further to fire an alarm. This is the fence-tolerates-the-defect pattern: the metric is measured and floored (so it looks governed) but the floor carries zero information. Directly compounds F4 (the organ economy runs unrefereed): miner productivity is neither measured-upward nor alarmed-at-zero.

**Exactly-what.** Establish a real floor (measure current productive-seat rate; if genuinely 0, that is a P0 research-engine defect to fix, not a floor to bless) and make check_ratchets FAIL — not record — when a productivity ratchet sits at 0.0 past a grace window. Complexity: low. Validation: floor > 0 with its proving command, or a blind_spot row explaining why 0 is structural. Confidence: 0.85.

### F10. MEDIUM/RATCHET-DEFECT — `pager_delivered_24h` floor is 0.0: the alert-delivery ratchet tolerates 24h of silence, so the pager is unmonitored for BOTH failure modes at once. [INT]

**Evidence.** `data/ratchet_floors.json` → `pager_delivered_24h = {"value": 0.0, "recorded": "2026-07-30T07:19:13Z", "proving_command": "python scripts/run_alert_canary.py"}`. A 0.0 floor means "zero pages delivered in 24h" never trips the fence. Combine with F6 (the pager emits provably-false CRITICALs): the alert channel is simultaneously NOISY (F6) and UNGUARDED-AGAINST-SILENCE (F10). The desk's most survival-critical signal path — the only wire that carries a dead-man/ruin page to a human — is the one whose health metric is floored at "any amount of silence is fine." Note the canary IS delivering today (`alert_delivery.jsonl` http 200s, S4), so the live state is OK; the DEFECT is that the fence would not catch it going silent.

**Exactly-what.** Floor `pager_delivered_24h` at 1 (canary sends ≥1 test page/day; the fence fires on a 24h gap), and separate "canary delivered" from "real page delivered" so a quiet real-alert day doesn't look like a dead pager. Complexity: low. Validation: kill ntfy in a drill → fence fires within 24h. Confidence: 0.9.

## 4. WHAT WE TEST NEXT (concrete experiments, success criteria, retirement conditions)

Ordered by (impact × confidence) / cost. Items 1-3 are same-day; the rest this week.

**T1 — De-duplicate the crontab and prove it (F1).** Diff legacy vs managed lines, port the correct variant into the manifest, delete legacy. **Success:** `crontab -l | grep -vE '^#|^$|=' | awk '{print $6}' | sort | uniq -d` returns empty (no script scheduled twice); `data/defi_lending.jsonl` row-rate returns to ~278/hr within one hour. Also backfill-dedup the doubled rows since 2026-07-30T23:00 in defi/oi_ls/coinmetrics. **Retire:** never — add the `crontab -l` vs manifest drift check to max_audit so this is a standing fence, not a one-off.

**T2 — Prove the 08:21 herd fits in RAM before it OOM-roulettes the executor (U6/F1).** Since the 13-job `21 8 * * *` block fires for the first time today, instrument it: `while sleep 5; do free -m; done` across 08:19-08:50, plus `dmesg | grep -i oom` after. **Success:** peak used < available, zero OOM kills, executor PID survives. **If it fails:** stagger the herd (T1 fix) is mandatory, not optional. **Retire:** once the herd is staggered and a fenced peak-RSS check exists.

**T3 — Make the deploy gate deterministic and unstick it (F3).** Fix the one `noqa` + the 2 failing tests (or quarantine-with-row), push, confirm deploy. Add "skip re-testing an already-red commit until remote head changes." **Success:** `data/pull_deploy_state.json.status == "deployed"`; over 24h, zero revert-flaps on unchanged HEAD; CI stops running every 10 min on the same red commit. **Retire:** when the flap-rate fence reads 0 for 7 days.

**T4 — Disk fence + offsite sync (F5).** Add `shutil.disk_usage('/')` to `data_health.py`/`check_ratchets` (page <15% free, hard <8%); stand up hourly `rclone` of closed hourly moat/lake files to a Hetzner Storage Box / R2. **Success:** `data/health.json` carries `disk_free_gb`; a synthetic low value pages via the canary; remote object count tracks local within 24h. **Retire:** never (both are permanent fences).

**T5 — Offsite restore-drill (F5).** Pull ONE symbol-day gzip from the remote and byte-diff against local. **Success:** `cmp` clean, gzip valid. **Retire:** re-run monthly as a standing DR drill (fence its last-run date).

**T6 — Distinguish "no trades" from "broken tape" (U7).** Reconcile venue trade history (exchange API) against `data/moat/execution_tape/cashcarry_trades.jsonl` (stale since Jul 28 15:20) while the executor holds 13 positions. **Success:** either venue shows zero closes since Jul 28 (tape is correct; the hold-hysteresis explains it) OR a write-path break is found and fixed. **Retire:** add a tape-freshness-vs-position-churn check so this self-answers.

**T7 — Tunnel defer + backstop honesty (F7).** Add the tunnel to watchdog `_UNITS`; drill by killing `quant-tunnel.service`. **Success:** zero "(re)started public-tunnel" lines over 24h while the unit is up (battery #10 ratchet: today's 3,307 spawns is the floor to drive to 0), AND the spawn still fires when the unit is genuinely down. **Retire:** never (the backstop-honesty drill is standing).

**T8 — Auth-broken outcome test (F6).** Rewrite `_auth_broken` to test the capability (token file + last-organ-success-within-24h), not `.credentials.json` existence. **Success:** 48h with zero false `auth_broken` pages while organs demonstrably run. **Retire:** fold into a general config-vs-outcome audit of all 6 paged classes (battery #3).
