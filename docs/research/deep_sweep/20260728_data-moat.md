# WEEKLY DEEP COLD AUDIT — DATA-MOAT SUBSYSTEM — 2026-07-28

**Auditor:** deep-sweep cold audit (v2 doctrine), READ-ONLY
**Subsystem definition:** NOT "what data do we have" but "what information can only exist because of how WE combine data" — proprietary transformations, hierarchical/composed features, graph representations, embeddings, event reconstruction, market-state fingerprints, research memory, experiment lineage, feature ancestry, alpha genealogy.

**Status: IN PROGRESS — findings appended incrementally as verified. If this line is still present, the run was cut off and this partial report is the deliverable.**

## SCORES (placeholders until final pass)

- current_capability_pct: TBD
- practical_ceiling_estimate: TBD
- ceiling_gap: TBD
- opportunity_cost_1y: TBD
- confidence: TBD
- unknown_unknown_score: TBD
- info_gain_if_investigated: TBD
- expected_alpha_contribution: TBD
- expected_compounding_contribution: TBD
- CEILING EXPANSION note: TBD

---

## 1. WHAT WE KNOW (validated strengths, each with proving command)

**K1. The graveyard is a real negative-knowledge moat, not a list.** 43 table rows (`grep -c "^| " docs/graveyard.md` → 43), each carrying verdict + tag + mechanism-of-death lesson; a kill-basis rule (economic vs data/infra) distinguishes permanent kills from revivable ones; tags feed EV-gate priors in `libs/research/alpha_economics.py`. Lessons are generalized rules ("positive IC ≠ tradeable P&L", "fat Sharpe is a red flag"), i.e., compounding knowledge. This is information that exists only because of how this desk combined its own failures.

**K2. Negative knowledge is machine-actionable with revival triggers.** `data/negative_knowledge.json` (fresh 2026-07-28T08:35): 42 records, permanence taxonomy {PERMANENT 5, NEAR-PERMANENT 13, REVERSIBLE 11, CONDITIONAL 9, UNKNOWN 4}, revival triggers actively EVALUATED (`"more_data_available": {"met": true, "why": "16273 rows across 28 clocks accruing"}`). Kills can come back when their killing condition lifts — that is genealogy with a pulse, and few desks have it.

**K3. A living causal graph + alpha genome exists and runs daily.** `data/knowledge_engine.json` (updated 2026-07-28T10:21): corpus_size 212, 9 causal edges with epistemic state + evidence (`{"cause": "leveraged crowding", "effect": "funding rate persists", "state": "MECHANISM_TESTED", "evidence": "IC +0.432 t +29.7..."}`), an alpha genome (id/name/mechanism/lifecycle/evidence/blocker per alpha), revival watchlist, and memory_queries answered from the experiment corpus. Wired: `scripts/daily_research_cycle.py:71` runs it with a 90s budget. This — not the alpha_factory stubs (see D1) — is the desk's real genealogy organ.

**K4. Research memory rows exist and are current after the 2026-07-24 null-pipe fix.** `sqlite3 data/sor_research.sqlite "SELECT COUNT(*) FROM research_memory"` → 140 rows, latest 2026-07-28T20:29 (today), spread across hypothesis/dataset/method/mission/construction categories. The CLI (`scripts/research_memory.py`) removed the friction that had kept this table at 0 rows for the desk's entire prior life (the file's own docstring records this).

**K5. The feature library turns "keep exploring" into a coverage percentage.** `scripts/feature_library.py` registers features as managed assets seeded ONLY from code that actually ran, enumerates a construction grammar (OBSERVABLE × TRANSFORM × WINDOW × NORMALISATION), reports tested-cell coverage, carries the measurement-gate verdict of each feature's input dataset (features on unverified inputs are BLOCKED), and hands ranked cells to Stage-A only (zero promotion authority). Artifact fresh: `data/feature_library.json` 2026-07-28 08:35.

**K6. Experiment lineage is git-anchored and consumed.** `data/experiment_registry.jsonl`: 385 rows spanning 2026-07-16→07-28, each keyed to a commit hash with decision (INFRA 169 / REFUTED 145 / FIX 21 / SURVIVED 15 / INCONCLUSIVE 10 / UNCLASSIFIED 25), code paths and artifact paths. Nine scripts consume it (research_cio, daily_research_cycle, knowledge_engine, data_vitals, measurement_gate, module_justification, research_exchange, wiring_audit, experiment_registry itself).

**K7. Generation is graveyard-conditioned at the prompt level.** `scripts/hypothesis_generator.py:108-137` feeds the graveyard both as prose and as refuted-token sets into external-LLM generation ("graveyard supplied: N refuted tokens (prevents re-proposing dead)") — negative knowledge shapes the proposal distribution, not just post-hoc filtering.

**K8. A deterministic pre-compute novelty gate exists.** `libs/alpha_factory/hypothesis_novelty.py`: token/Jaccard similarity of candidate statement+features vs prior failed ideas, returns nearest prior + its lesson, advisory by design, no AI oracle. Callers: `scripts/screen_fred_macro_axis.py`, `scripts/screen_idle_axes.py`, `libs/autodiscovery/generation_roi.py`.

## 2. WHAT WE DON'T KNOW (ignorance ledger: untested assumptions, evidence gaps)

(filling incrementally)

## 3. WHAT COULD MATTER MOST (opportunities ranked by impact × confidence / (cost × maintenance))

(filling incrementally)

## 4. WHAT WE TEST NEXT (concrete experiments, success criteria, retirement conditions)

(filling incrementally)

---

## PERSPECTIVE COVERAGE LOG (six mandatory: INTERNAL / EXTERNAL / FUTURE / CONTRARIAN / GREENFIELD / FRONTIER)

(filling incrementally)
