# WEEKLY DEEP COLD AUDIT — subsystem: **data-moat** — 2026-07-31

STATUS: COMPLETE — 6 strengths (K1–K6), 12 new defects (DM-1..DM-12, incl. one live catch at
01:34 during the audit), full D1–D12 delta table (8/12 untouched), 8 ignorance-ledger rows
(3 resolved in-sweep), 12 ranked opportunities, 6 tests, six perspectives, 10/10 battery moves,
ledger commands staged for the synthesis seat. All claims command-cited. A 02:03Z watcher on the
duplicate-scheduler collision was still pending at completion; its result lands as a dated
addendum at the foot of this file without altering the findings above.

**Scope:** not "what data do we have" but *what information can only exist because of how WE
combine it*: proprietary transformations, hierarchical/composed features, graph representations,
embeddings, event reconstruction, market-state fingerprints, research memory, experiment lineage,
feature ancestry, alpha genealogy.

**Mode:** READ-ONLY sweep; only this report is written. Every claim carries its proving command +
output. Prior complete reports: 2026-07-26, 2026-07-29, 2026-07-30. This run (a) re-verifies every
open defect from 07-30 (D1–D12) — the ratchet requires knowing whether the floor held — and
(b) opens new seams the prior three sweeps never touched.

**Headline:** The moat's *memory* is stronger than any prior sweep credited (a real causal
knowledge graph, permanence-graded negatives, revival triggers) and its *fusion* premise just
died honestly — today's re-run inverted the desk's only positive multi-source result to −0.035
lift, 5/5 combos negative. What dominates now is structural: **(1) the moat has NO backup and a
~29-day disk runway** before the desk's own disk-guard starts sacrificing irreplaceable L2
collection; **(2) the experiment record is split across three same-schema sqlite stores plus a
phantom fourth path that the promotion queue reads** — it will report an empty queue
"successfully" while 63 candidates sit in the real stores; **(3) four lineage-capable schemas
hold zero lineage edges**, so tonight's first-ever alpha-factory run will genealogize a flat
forest. Measured one layer deeper than 07-30's "capabilities one import short of working": **(4) the
memory layer is write-only end to end** — causal graph, permanence-graded negatives, lifecycle
events, information_value: every one producer-only, zero downstream readers (DM-11). And a live
catch during the audit: **(5) at 01:34 a spot fill occurred whose hedge was REJECTED, and every
record on the desk missed it** — tape, trades, positions, orphan detector all silent (testnet
book, no real capital; the mechanism ports to live unchanged) (DM-12). Delta discipline: 8 of
12 prior defects untouched, including both items ranked #1 and #2 on 07-30; the leaky keying
flagged there as defeating a same-day retraction ran five more times since.

---

## SCORES

| metric | value | basis |
|---|---|---|
| current_capability_pct | **55%** | 58% on 07-30. Down again as a *measurement correction*, not a build regression: fragmentation (DM-2), zero-backup (DM-1), empty trials ledger (DM-4) and the idle campaign queue (U7) all existed before and were uncounted; against that, K1/K2 (knowledge+negative stores) are materially stronger than previously credited and the fusion record is now honest (K3). |
| practical_ceiling_estimate | **85%** | unchanged — the binding constraint is wiring/triage, not technology (perspective 2: all four gaps to world-class are hygiene, not invention). |
| ceiling_gap | **30 pts** | of which ~15 pts is one-line-to-one-day wiring (M2, M3, M4a, M6, M9 are collectively <2 days). |
| opportunity_cost_1y | **HIGH, with one countdown** | DM-1(b) is not a risk, it is a schedule: ~29 days to the 80% guard absent action. D5's fill-field loss is paused only because fills are (U2). The rest is deferred value: unqueried genealogy, unscreened micro features, unconverted archaeology. |
| confidence | **0.9** findings (every claim command-cited today), **0.65** rankings | |
| unknown_unknown_score | **0.35** | held at 07-30's raised level: the glob audit (U1) is still undone and the two NEW instances found this sweep confirm the class is systemic; suspected instance #4 (the lake, U5) already queued. |
| info_gain_if_investigated | **VERY HIGH for T4** (restore drill — resolves the largest tail risk), **HIGH for U2/U7** (both are "is a core loop dead?" questions answerable in minutes from the right seat) | |
| expected_alpha_contribution | **MED, mostly indirect** — honest fusion negatives prevent phantom edges (already did, twice); M4/M7 make execution-cost and multiplicity honesty possible; M6's micro features are the only direct-alpha candidates here | |
| expected_compounding_contribution | **HIGH** — M1 protects everything; M5 (lineage at creation) and M10 (scope audit) multiply the value of every future experiment and every future self-measurement respectively | |
| **CEILING EXPANSION** | The 85% assumed organizational triage bandwidth is binding. Evidence against that assumption arrived yesterday: commit 6453ee3 wired **17 orphaned engines in one commit** once a list existed. Wiring throughput is not the constraint — *enumeration* is (module_justification still can't see libs/, D6). Lifting condition unchanged and now sharper: the whole-repo capability inventory (M10); if 3-for-3 audited organs had scope defects, the desk is plausibly further below its true ceiling than 85% implies, in both directions. |

---

## 1. WHAT WE KNOW — validated strengths and verified defects (each with its proving command)

### Delta on 07-30 defects (D1–D12): what got fixed in 24h

Audited at 01:24Z. A large wiring commit landed yesterday 17:11 (`6453ee3` "alpha factory: 17
orphaned engines wired, 1 retired; real CPCV and a baseline gate") — several 07-30 defects are
*materially advanced but unproven*, because the wired steps' first production run is TODAY's 02:00
cycle. This audit stays open past 02:00 to observe that first run (outcome, not config).

| 07-30 defect | status 07-31 01:24Z | proving command |
|---|---|---|
| **D1 leaky Upbit keying ×2** | **OPEN, day 2.** `grep -rn candle_date_time_utc --include=*.py .` → `scripts/fusion_engine.py:66` and `scripts/signal_halflife.py:55` both still open-date-keyed (`[:10]` on the OPEN timestamp). signal_halflife wrote 2 more `kimchi STRENGTHENING ic_recent 0.4013` rows on 07-30 — and the fusion artifact was regenerated TODAY 00:51 by the still-leaky engine (`run_intelligence_cycle.py:247 _subprocess_cap("fusion_engine", …)`, cron `50 */6`). The top finding of the 07-30 sweep survived its first day untouched. | above |
| **D2 decay organ inert + double appends** | **OPEN.** `wc -l data/signal_halflife.jsonl` → 14 (was 12); the two 07-30 rows are *each duplicated again* — the DI-1 duplicate-scheduler defect (two 02:00 daily cycles: crontab line 25 pgrep-guarded + line 81 flock-guarded, non-excluding) is double-appending into moat-derived stores. kimchi row still byte-identical (`n_windows 4, ic_recent 0.4013`) for the 4th consecutive day. New info: stablecoin_supply now shows `n_windows 41, half_life_windows 54.98` — the organ *can* produce a curve; the kimchi plateau is series-length-specific, as diagnosed. | above |
| **D3 "FUSION ADDS VALUE" overrule bug** | **CONSEQUENCE MOOTED, and U3 resolved: the flagship fusion result INVERTED.** Today's artifact (`data/fusion_engine.json`, updated 2026-07-31T00:51): `flow_plus_regional` ic 0.1310, best_single 0.1661, **lift −0.0351** (was +0.0585), verdict SCREEN-UNDERPOWERED, fusion_verdict "no lift over best component". All 5 combos now negative. kimchi standalone component fell 0.2249 → 0.0849. **The only positive multi-source fusion result the desk ever had is now measured negative — even while still computed on the leak-favoring keying.** The 7b85703 commit message "orthogonal fusion adds value" is now contradicted by its own artifact. | `python3 -m json.tool data/fusion_engine.json` |
| **D4 fusion negatives impermanent + unscheduled** | **HALF-FIXED.** Scheduling: fixed twice over — intelligence cycle runs fusion_engine 4×/day, and a NEW `scripts/run_fusion_search.py` + `libs/research/fusion_search.py` (a registry-gated combination *search*, distinct from the fixed-combo engine) is in cron daily 05:08. Permanence: **unfixed** — `grep -ci fusion docs/graveyard.md` → 0; `'fusion' in negative_knowledge.json` → False. Five negative combos, two engines, zero graveyard rows. | above |
| **D5 TCA 0.8% coverage** | **OPEN + a new wrinkle: the tape is FROZEN — and NOT because fills stopped.** Coverage still 4/517 rows with `spot_slip_bps`; `web/tca.json` still absent; CRO ROI ranker named `execution_tca_fill_log` #1 for the 7th consecutive cycle (07-30 log line). Tape mtime 2026-07-28 15:20 while the executor is alive (PID 1146567, heartbeat 01:22). This audit initially read that as a fill drought; **at 01:34 a real spot fill occurred (MOVEUSDT, 47,306 units) and the tape stayed silent** — the tape records completed pairs only. See DM-12: the freeze is partly a *coverage hole*, and no fence exists on tape staleness either way. | `stat data/moat/execution_tape/cashcarry_trades.jsonl`; `cat data/cashcarry_error.log` |
| **D6 module_justification blind to libs/** | **OPEN.** `grep -n glob scripts/module_justification.py` → line 68 `(ROOT/"scripts").glob("*.py")` unchanged. | above |
| **D7 genealogy layer zero callers** | **MATERIALLY ADVANCED, first proof pending at 02:00.** `scripts/run_alpha_factory.py` (12,008 bytes, 17:11 yesterday) is a real production caller: reads `research_agenda.json` + `alpha_pipeline.json`, calls `AlphaFactoryController`, records lineage (`:209 ctl.record_lineage(...)`), reports `best_lineage` (`:236`), writes `web/alpha_factory.json`; wired into `daily_research_cycle.py:50` (180s cap). **But** (a) `ls web/alpha_factory.json` → does not exist (first run is 02:00 today — verified live below), and (b) **persistence is still zero**: `grep -n "save\|load\|persist" libs/alpha_factory/alpha_family_tree.py libs/alpha_factory/research_graph.py` → empty. The tree is rebuilt each run from `alpha_pipeline.json` — i.e. ancestry of *current survivors only*; killed/retired alphas leave no lineage trace, so the genealogy is survivorship-biased by construction. | above |
| **D8 black-swan library: 0 consumers** | **OPEN.** `grep -rln black_swan --include=*.py libs scripts` → only `scripts/research_cycle.py` (the `.exists()` gate). 7 scenarios, 20 days untouched, no replay harness. | above |
| **D9 32/32 presence gates** | **OPEN.** `grep -c "exists()" scripts/research_cycle.py` → 27 `.exists()` checks still. | above |
| **D10 no dataset-level genome** | **OPEN.** `data_registry.json`: 20 sources, per-source fields `[info, status, tier, used_by]`; `half_life` False, licence False; `grep -rl "data_genome\|DIVR\|reconstruction_cost" libs scripts` → empty. | above |
| **D11 regime-label history destroyed nightly** | **OPEN — FIFTH consecutive sweep.** `data/crypto_regime.json` overwritten 00:13 today (292 bytes); `crypto_regime_history.jsonl` still does not exist. ~450 labels now destroyed since first flagged (07-26, R0006). | `ls -la data/crypto_regime*` |
| **D12 event windows + interactions absent** | **HALF-FIXED (event half), first proof pending.** `libs/validation/event_study.py` + `libs/research/listing_events.py` + `scripts/run_event_study.py` + tests all exist (17:11 yesterday); `VARIANTS_TRIED = 2` pre-registered honestly (`listing_events.py:50`); wired into daily cycle + max_audit; feed data live (`data/listings.jsonl` 8 events since 07-24). Output `data/event_study_listings.json` absent — first run 02:00 today (verified below). **Interaction screen: still absent** (`grep -rln interaction libs/research libs/features libs/alpha_factory` → empty), fourth sweep. | above |

**Delta verdict:** 2 of 12 half-fixed with first-run proof pending (D7, D12), 2 consequence-level
improvements (D3 inverted honestly, D4 scheduled), **8 of 12 untouched** — including both items
the 07-30 sweep ranked #1 (D5/M1 fill schema) and #2 (D1/M2 leaky copies). The leak flagged as
"defeats a same-day L1.4 retraction" ran again at 00:51 today and 02:00 yesterday, four more
contaminated verdicts written since flagged.

### Strengths

**K1. The knowledge engine is the desk's best composed-information asset, and it is real.**
`data/knowledge_engine.json` (regenerated daily, 08:41 07-30): corpus_size **247** (experiments +
graveyard fused into one retrieval space), **9 causal edges each carrying a test-state** —
`MECHANISM_TESTED` (funding persistence, IC +0.432 t +29.7), `REFUTED_AS_LEAD` (liquidity
withdrawal → vol: raw ρ +0.303 → residual +0.015 after orthogonalising to same-period RV),
`UNTESTED` (inventory risk → withdrawal: "1 of 270 constructions tested") — plus revival triggers
with concrete re-entry checks and `blind_validation_consistent: 3`. This is exactly the L1.11
"proprietary transformation" claim made real: *the combination of our own experiment history into
a queryable causal map exists nowhere else and cannot be bought.* Caveat: retrieval similarity is
weak — the top hit for "order book depth withdrawal predicts volatility" is the exactly-on-point
refuted experiment at **sim 0.159** (near-noise), consistent with the research-engine sweep's
novelty-gate 0%-recall finding. The asset is real; the index into it is blunt.

**K2. Negative knowledge now has permanence grades and machine-readable revival conditions.**
`data/negative_knowledge.json`: n=44, `by_permanence {NEAR-PERMANENT 14, REVERSIBLE 11,
CONDITIONAL 9, PERMANENT 5, UNKNOWN 5}`, **revivable_now: 10**, updated daily. L1.16a (kill
records carry re-entry conditions) is implemented at the artifact level, and the graveyard
entries themselves are exemplary — the exchange_netflow kill records the mechanism, the
sign-flip under de-contamination, the transferable lesson, and the exact re-test conditions in
one row. **Caveat found late in the sweep:** the artifact's only code reader is its own producer,
and the resurrection organ scheduled to act on `revivable_now` has never left a log — see DM-11;
the *store* is a strength, its *consumption* is not yet real.

**K3. The flagship fusion claim died honestly under its own re-run.** Resolves 07-30 U3: today's
00:51 artifact shows `flow_plus_regional` lift **−0.0351** (was +0.0585, committed as "orthogonal
fusion adds value"). All 5 pre-registered combos now negative, kimchi component 0.2249 → 0.0849.
The desk's honest answer to "does 2–4-source z-composite fusion add value on these axes" is
currently **no** — a real negative about the moat's L1.11 manufacturing target, measured twice.
And a genuinely new capability shipped beside it: `libs/research/fusion_search.py` +
`scripts/run_fusion_search.py` (cron 05:08 daily) — a registry-gated combination *search* that
reads screen verdicts from `reports/` rather than assuming eligibility.

**K4. Raw-moat collection is alive, deep, and includes the genuinely irreplaceable class.**
`du -sh data/moat/*` → bybit **5.3G** (+0.5G/day; oldest file 2026-07-21 09:xx = 10 days deep),
fut 889M, spot 484M, execution_tape 136K; 2080 files touched since 07-30. Stream content is
**L2 depth snapshots** (`zcat …BTCUSDT/20260721_09.jsonl.gz | head -1` → `"k":"depth"` with 30+
levels) — historical L2 at our own timestamps is the one stream no vendor republishes free. And
the execution tape's close path now emits the FULL TCA field set (last row: `spot_slip_bps
13.806, fut_slip_bps −4.585, wait_s 3.951`) — the schema fix exists on one path.

**K5. A hypothesis-identity layer exists and is coarse on purpose.**
`libs/research/mechanism_fingerprint.py` (+ collapse_detector, variation_blocker): fingerprint =
feature family + transform + log-spaced horizon bucket, built to catch "400 reparameterisations
of one mechanism" — the documented 420/0 failure — with entropy-over-fingerprints as the collapse
alarm. Correct design, correctly reasoned, and consistent with the trials-multiplier ×7 in the
gauntlet.

**K6. The research-memory CLI is adopted and axis-tagged.** `data/sor_research.sqlite
research_memory`: **160 rows** (was 140 on 07-29), 142 axis-tagged, latest 22:41 last night;
94 construction-failures preserved. Real readers exist for reporting (max_audit KPIs at :113,
:1004, :1110). The write-friction problem named on 07-24 is solved; what remains unused is the
lineage column (DM-3).

### New verified defects (DM-numbered)

**DM-1 — THE MOAT HAS ZERO BACKUP, AND ITS DISK RUNWAY IS ~4 WEEKS.** (Resolves 07-30 U5, badly.)
```
$ crontab -l | grep -in "backup|rsync|rclone|tar|b2|s3"   → (nothing)
$ ls scripts/ | grep -i "backup|archive"                  → make_archive.py (repo ZIP; excludes data/)
$ zcat data/moat/bybit/BTCUSDT/20260721_09.jsonl.gz | head -1  → {"t":…,"k":"depth","b":[[…30+ levels
$ df -h … → 38G disk, 13G used;  du -sh data/moat → 6.6G, growing ~0.5–0.6G/day
```
6.6G of which the L2-depth class (5.3G, *real* Bybit market data at our own timestamps) is
**irreplaceable by construction**, and the execution tape (136K, paper-book fills — still the
desk's only execution-reality genealogy and Gate-0 evidence) is unreproducible — one machine,
one disk, no copy anywhere, no restore ever attempted. Two distinct
failure modes: (a) disk dies → the desk's #1-scored information advantage is gone permanently;
(b) disk **fills** — at 0.5–0.6G/day the 80%-guard in the tape writer trips in ~29 days
(30.4G threshold − 13G used ÷ 0.6), at which point the desk's own disk-protection code starts
silently sacrificing the moat it protects. The tape alone is 136KB — one `cp` to anywhere is a
backup. Nothing in four sweeps of this subsystem ever verified backup existence until today.

**DM-2 — the experiment record is fragmented across THREE same-schema sqlite stores plus a
phantom fourth path, and the promotion queue reads the phantom.**
```
$ python3 … row counts:
data/sor_research.sqlite:      research_memory 160, campaigns 348, research_candidates 14, workers 4
data/alpha_registry.sqlite:    alpha_cards 8, alpha_events 656          (same 22-table schema)
data/sor_research_lake.sqlite: research_candidates 49                   (same schema, 5 migrations)
data/research_memory.db:       DOES NOT EXIST — referenced by 4 organs:
  scripts/run_promotion_queue.py:47   scripts/run_generation_diversity.py:46
  scripts/max_audit.py:1467, :1665    (CandidateStore(ROOT/"data/research_memory.db") — a PATH
                                       passed where CandidateStore.__init__ expects a Database)
```
`run_promotion_queue.py` guards with `if not _DB.exists(): return []` and its docstring calls the
empty queue "a fact worth reporting" — but the queue is not empty, the *pointer* is wrong: 14+49
scored candidates sit in the two real stores. Both phantom-path organs are installed in live cron
(lines 57, 107 — daily 07:53/09:23; new lines, no logs yet, first fire today). This is the exact
L1.28a proving-instance shape (two sources of truth for one number) applied to the desk's
research memory: the organ that moves validated work toward capital reads a store nothing writes.

**DM-3 — the desk owns FOUR lineage-capable schemas and has ZERO lineage edges in any of them.**
```
$ sqlite: research_memory.predecessor_id non-null → 0 of 160
$ sqlite: alpha_cards → successor_id None, predecessor_id None (all 8 cards)
$ python3 … alpha_pipeline.json alpha fields → [alpha, category, crowding_risk, expected_half_life,
   expected_sharpe, gates, orthogonality, retire_check, stage, survived]   ← no parent field at all
$ grep -n "save\|load\|persist" libs/alpha_factory/alpha_family_tree.py …  → (empty; in-memory only)
```
Ancestry can only be captured at hypothesis-creation time, and no creation path captures it. The
newly-wired `run_alpha_factory.py` will therefore build a **flat forest of 8 roots** — its
`record_lineage(…, mutation_type="pipeline")` calls (`:209`) attach every alpha to nothing, and
`best_lineage()` degenerates to "best single node". Alpha genealogy is now wired (D7 progress)
and will run tonight — *on data that contains no genealogy*. The fix is upstream of the factory:
populate `--predecessor` in the research_memory CLI calls (the flag exists, used 0/160 times) and
`predecessor_id` on card derivation.

**DM-4 — the tamper-evident trials ledger is empty in all three stores; the gauntlet accepts a
ledger and every caller passes None.**
```
$ libs/store/trials.py — full hash-chained writer/reader (row_hash, seq) for trials_ledger
$ libs/validation/gauntlet.py:73  ledger: TrialsLedger | None = None,   :74 trials_multiplier=7.0
$ trials_ledger rows: sor_research 0, alpha_registry 0, lake 0
$ scripts/experiment_registry.py:8 — its own docstring records "trials_ledger 0 rows"
```
The DSR's `n_trials` therefore arrives per-run via `n_trials_override` and dies with the process;
the desk's *lifetime* trial count — the number the whole two-stage law's multiplicity accounting
rests on — has no persistent, append-only home. The infrastructure to make it untamperable was
built (hash chain!), migrated into three databases, and never fed a single row. This is the
gate-optimality duty's "audit effective-vs-raw trial count" made structurally impossible: there
is no store to audit.

**DM-5 — the moat's only raw→feature transformation is unscheduled and stale; the 6.6G ingests
daily and converts never.**
```
$ crontab -l | grep -c micro_factory → 0;  not in daily_research_cycle.py, not in run_intelligence_cycle.py
$ ls -la data/micro_features.json → Jul 27 18:21 (3.5 days stale; one-shot manual runs only)
$ data/feature_library.json: n_features 9, computed_unused 5, n_proposals 447
$ consumers of micro_features.json → scripts/research_exchange.py (the [desk_brief] step that has
  failed every cycle since install with a shell-quoting error — 07-30 U9)
```
The pipeline exists end-to-end — L2 depth → `micro_factory.py` (spread/depth/imbalance/
concentration/slope over 60h × 5 symbols) → feature library with mechanism tags → consumer — and
every stage after collection is either unscheduled, stale, unused (5 of 9 features), unbuilt
(447 proposals), or crashing (the consumer). L1.8's "idle data is a defect" describes the moat
itself: ~0.5G/day in, ~0 features/day out since 07-27.

**DM-6 — the execution tape is structurally invisible to the freshness organ.** (Second confirmed
instance of the 07-30 U1 frame error, and the reason the frozen tape never alarmed.)
```
$ scripts/data_vitals.py:218  {"kind":"DIR_GLOB","path":"data/moat","glob":"**/*.jsonl.gz", …}
$ scripts/data_vitals.py:297  … (ROOT/"data").glob("*.jsonl")        ← top-level only
$ tape = data/moat/execution_tape/cashcarry_trades.jsonl             ← nested AND not .gz: matches neither
$ python3 … data/data_vitals.json → mentions execution_tape: False
$ scripts/coverage_audit.py:34  {p.name for p in (ROOT/"data").glob("*.jsonl")}   ← same shape
```
The one dataset L1.11 names irreplaceable is the one dataset the vitals organ cannot see. The
07-30 sweep asked "where else is a measurement organ's scope narrower than its claim?" — this is
where, found by following the question; the systematic glob audit (M3 of 07-30) remains undone.

**DM-7 — information_value.jsonl: 1,244 rows, 1,244 empty `lesson` fields.** (Updates 07-30 U8:
the store is alive — it grew 810 → 1,244 and wrote at 02:40 07-30 — and 100% contentless on its
titular field: `python3 …` → `rows 1244, empty lesson: 1244`. An organ named *information value*
that has never recorded a lesson is signal-economy debt: every row is triage cost with no
payload. Either populate `lesson` at write time or drop the field and rename the organ's claim.)

**DM-8 — "historical monopolisation" (L1.11) is a catalog, not a monopoly.**
```
$ wc -l data/8btc_era_thread_catalog.jsonl → 713 (2013-era CN threads, GBK-decoded, tids+titles)
$ grep -rln 8btc --include=*.py scripts libs → (no conversion code; catalog producer only)
$ docs/research/prospector_coverage.md:14 → "Era-archaeology … 69k thread catalog built …
   1 thread mined (8btc thread-53689) … still UNSTARTED — first item next run"
```
The desk's only pre-regulation-era holding is 713 locally-catalogued thread titles out of a
declared 69k-thread universe, with exactly one thread's content ever extracted. L1.11a made this
ground permanently in-scope; the §33 backlog knows about it (panel_inbox row). No new defect
beyond what §33 already tracks — recorded here because *moat-wise* the asymmetric-history claim
is currently 0% manufactured.

**DM-9 — the fusion verdict-overrule bug survives in code even though today's data mooted it.**
`scripts/fusion_engine.py:147` still computes `verdict = "FUSION ADDS VALUE" if lift > 0.02 …`
without consulting the artifact-gate verdict printed beside it (`:160`). All lifts are negative
today, so the headline happens to be honest; the first future combo with lift > 0.02 and
SUSPECT-LOOKAHEAD will re-print the 07-27 lie. Two engines now share this artifact
(`fusion_engine.py` 4×/day via intelligence cycle, `run_fusion_search.py` daily) — worth checking
the new one inherited the gate correctly before it ever prints a positive.

**DM-10 — anomaly_memory: one row, ever.** `wc -l data/anomaly_memory.jsonl` → 1 (writer:
`scripts/alpha_lifecycle.py`). A memory organ instantiated 20+ days ago holding a single record
is either monitoring nothing or connected to nothing — given DI-13 (write-rate doubling with no
anomaly recorded anywhere), it is the latter: the desk's anomaly stream demonstrably fired this
week and this store did not hear it.

**DM-11 — THE COMPOSED-KNOWLEDGE LAYER IS WRITE-ONLY, END TO END.** Discovered by chasing U6/U8
during the sweep; it unifies five smaller findings into the subsystem's central defect pattern:
```
$ grep -rln "knowledge_engine.json\|causal_edges" --include=*.py scripts libs
scripts/knowledge_engine.py                     ← the producer. Zero other consumers.
$ grep -rln "negative_knowledge.json" --include=*.py scripts libs
scripts/negative_knowledge.py                   ← the producer. Zero other consumers.
$ grep -rn "alpha_events" scripts api app       → (no production caller; the reader method
                                                   libs/alpha/registry.py:160 has zero invokers)
$ ls data/cro_ai_logs/graveyard_resurrect.log   → does not exist (cron 08:21 daily, in manifest
                                                   ×2 — the organ meant to ACT on revivals has
                                                   never left a log)
+ DM-7 (1,244 rows, 0 lessons)  + DM-3 (predecessor flag, 0 uses)  + DM-4 (trials ledger, 0 rows)
```
The desk regenerates its causal graph, its permanence-graded graveyard (`revivable_now: 10`),
and its lifecycle event stream every day — and **no downstream organ reads any of them**. Last
sweep's summary was "finished capabilities sit one import short of working"; measured one layer
deeper, the moat's memory organs *talk only to themselves*. The knowledge exists, is genuinely
proprietary, and currently informs zero decisions. (Softens K2 accordingly: the revival
*machinery* is scheduled; it has never been seen to act.)

**DM-12 — LIVE CATCH AT 01:34 DURING THIS AUDIT: a fill occurred and every record missed it.
The execution record has survivor bias by construction.** (Corrects this report's own D5 delta
row, which initially read the frozen tape as "zero fills"; testnet book — no real capital at
risk — but the mechanism ports to live unchanged.)
```
$ cat data/cashcarry_error.log     (1 line — the file's only line, ever)
2026-07-31T01:34:24 unfilled leg MOVEUSDT spot_ok=True fut_ok=False
  spot_res={'status': 'FILLED', 'executedQty': '47306.0'} fut_res={'status': 'REJECTED', …}
$ stat -c '%y' data/moat/execution_tape/cashcarry_trades.jsonl → 2026-07-28 15:20  (silent)
$ stat -c '%y' data/cashcarry_positions.json → 01:32:20, n positions: 0, 'MOVE' absent (silent)
```
At 01:34 today the executor bought 47,306 MOVEUSDT spot and its futures hedge was REJECTED. The
open-fail path (`run_cashcarry_executor.py:1097`) logs one line and returns — the action string
is literally *"not tracked, verify manually"* (`:802`). Three consequences, each verified:
(a) **the tape records completed pairs only** — one-legged failures, the analytically richest
execution events, never reach `execution_tape.append`, so the Execution Reality Model's input
excludes its own tail by construction; (b) **the SPOT-EXCESS orphan detector cannot see this
orphan** — it iterates `pos.items()` (`:550`), i.e. *tracked* symbols only, and `pos` is empty;
the 2026-07-26 fix ("must never sit UNSEEN again") covers excess on tracked names, and an
open-fail on an untracked name is invisible to it — the documented incident class recurring
exactly one iteration deeper; (c) **cashcarry_error.log has no consumer or fence** — a
first-ever risk event class fired 25 minutes into this audit and the only reason anyone knows
is that a cold audit happened to be reading the file. On a live venue this exact path strands
real inventory (it already did once: the 07-19 incident stranded ~$2,150 on the close side,
which is why `_filled()` exists). Escalation: execution-growth seat, with this line as the
repro; the moat's share is (a) — fix the tape to record *attempts and partials*, not successes.

---

## 2. WHAT WE DON'T KNOW — the ignorance ledger

**U1 (carried from 07-30, sharpened).** The systematic glob audit of self-measurement organs is
still undone, and its expected yield went UP: this sweep found instances #2 and #3 of the frame
error (data_vitals misses the tape by glob, DM-6; coverage_audit.py:34 top-level-only). ~15
organs self-measure this desk; 3 audited, 3 defective. The remaining 12 are unmeasured.

**U2 (reframed by DM-12 mid-sweep).** Originally "why no fills since 07-28"; falsified at 01:34
— a spot leg filled and its hedge was REJECTED. The open questions now: (a) WHY was the MOVEUSDT
futures order rejected (margin? symbol state? filter? — the venue answered, so it is not the
418 ban class); (b) whether completed pairs genuinely stopped for 2.5 days or earlier one-legged
attempts also went unrecorded (the error log's single-line history cannot say); (c) whether the
orphaned 47,306 MOVE spot units are still held. All three are execution-growth's to diagnose;
the moat consequence — a record that cannot answer (b) — is DM-12/M4's to fix.

**U3. Whether stablecoin_supply survives publication-lag scrutiny.** Its collector keys
DeFiLlama circulating supply and Binance kline by same calendar day (`collect_stablecoin_supply
.py:49,:57`), the screen *does* declare publication-lag handling (`screen_idle_axes.py:495`),
and today's fusion shows standalone IC 0.1661 — still the strongest single axis and still on a
live forward clock. Whether the exact live-clock construction was shift-tested was not verified
here. Cheap to check; on the validation seat's ground.

**U4. Whether tonight's first run of the newly-wired steps completes.** `alpha_factory` (180s
cap, imports 15 engines, runs CPCV) sits after `research_pipeline` (7200s) and `autodiscovery`
(1800s) in the step list, so first artifacts land ~04:30–08:45. Unknowable at write time;
verify commands in §4 T1.

**U5 — RESOLVED during the sweep, negatively.** The FX lake is monitored by nothing:
`grep -n "lake\|parquet" scripts/data_vitals.py` → empty. Instance #4 of the U1 frame error,
confirmed. (1.5G + a 49-candidate store, outside every vitals scope.)

**U6 — RESOLVED during the sweep, negatively.** `alpha_events` (656 rows) has a reader *method*
(`libs/alpha/registry.py:160`) and zero production callers — folded into DM-11. The genealogy
backfill source for DM-3 exists and is unread.

**U7. Why 340 of 348 campaigns have sat `queued` for 39 days.** `SELECT status, COUNT(*) FROM
campaigns` → done 7, leased 1, queued 340 (created 06-20→06-22, never started). Dead workers,
deliberate pause, or an abandoned surface? The campaign queue is a declared ceiling running at
2.3% utilisation with no named binding constraint — an L1.28a defect whichever way it resolves,
but its diagnosis belongs to the research-engine seat. Not resolved here.

**U8 — RESOLVED during the sweep, negatively.** No generator consults the causal graph — the
KE artifact has zero consumers beyond its producer (DM-11). The "inventory risk → liquidity
withdrawn: UNTESTED (1 of 270)" edge is a ready-made hypothesis vein no organ has ever read.

---

## 3. WHAT COULD MATTER MOST — ranked opportunities

### M1 — Back up the moat; the tape TODAY, the streams this week. [rank 1 — irreversibility]
**What:** (a) immediate: copy `data/moat/execution_tape/` (136KB) + the three sqlite stores into
the existing off-box path (the desk already operates a VPS — `requirements-vps.txt`; free-first
satisfied, no paid service). (b) daily rsync of `data/moat/` (6.6G, then ~0.5G/day incremental);
(c) a monthly restore drill (T4). **Why:** DM-1 — irreplaceable L2 + own fills, one disk, zero
copies, and a ~29-day countdown to the 80% disk guard sacrificing collection. Add (d) a disk-
runway line to data_vitals. **Cost:** hours. **Failure mode:** rsync of open .gz files — sync
yesterday-and-older only. **Retirement:** never (hygiene). **ROI:** protects every future finding
at once; the only item on this list whose absence can zero the subsystem.

### M2 — Re-point the three phantom-store readers; declare ONE canonical experiment db. [rank 2]
**What:** `run_promotion_queue.py:47`, `run_generation_diversity.py:46`, `max_audit.py:1467/:1665`
→ `data/sor_research.sqlite` (and fix max_audit's `CandidateStore(Path)` type bug — it needs
`Database(path)`); then a written decision: which of the three same-schema sqlites owns which
tables, enforced by a check. **Why:** DM-2 — the promotion queue currently reports an empty queue
"successfully" while 63 candidates exist; the L1.28a two-sources-of-truth shape on the desk's
research record. **Cost:** <1h for the re-point; half a day for the consolidation decision.
**Validation:** T5. **Failure mode:** the lab db genuinely lives elsewhere on a future box —
make the path a config constant, not a guess.

### M3 — Kill the two leaky Upbit copies. [rank 3 — third consecutive sweep, now 1-line each]
`fusion_engine.py:66` and `signal_halflife.py:55` import `upbit_daily_close_keyed` from
`libs/research/upbit_data.py` (built for exactly this, 07-29). Then delete the stale
signal_halflife kimchi rows (byte-identical ×4 days) or version the file. Every day unfixed
writes more contaminated verdicts into the decay archive — 5 fusion runs/day now (D1 note).

### M4 — Fence the tape; fix the vitals globs; finish the TCA schema. [rank 4]
**What:** (a) data_vitals: add `data/moat/execution_tape/*.jsonl` (and `data/lake` — U5) to
scope; (b) a staleness rule: tape mtime > 48h while executor heartbeat < 10min → alert; (c) carry
07-30 M1: emit the `_tca()` fields on all open paths, not just close (the close path proves the
pattern works — K4); (d) NEW from DM-12: the tape must record **attempts and partials** — the
open-fail path appends a row with `event: "open_fail"`, both leg results, and the TCA fields of
whichever leg filled. An execution-reality dataset that excludes failed executions is a
survivor-biased model of exactly the thing it exists to measure. (e) a fence on
`cashcarry_error.log`: any new line pages within one cycle. **Why:** DM-6 + D5 + DM-12; the
fills that DO come must never again land fieldless, and a frozen tape must be visible.
**Cost:** ~4h total (the CRO's own estimate for the TCA half was 3h, 7 cycles running).

### M5 — Capture lineage at creation; pilot on funding_carry. [rank 5]
**What:** (a) screen/mission templates pass `--predecessor rm-…` (the CLI flag exists, used
0/160); (b) card derivation writes `predecessor_id`; (c) backfill ONE family (funding_carry: 8
cards, git history, graveyard rows) and run `run_alpha_factory`'s `best_lineage()` against it.
**Why:** DM-3 — the factory wired tonight will report a flat forest until ancestry exists in its
inputs; four schemas wait empty. **Compounding multiplier:** every future experiment becomes
part of a searchable ancestry the day this lands. **Validation:** T6.

### M6 — Schedule micro_factory; wire its unused features into the screen queue. [rank 6]
One cron line un-idles 6.6G (DM-5). Then the 5 computed-unused features (depth5/depth10/slope/…)
get Stage-A screens via the existing `stage_a_exec` path — they are the desk's only features
manufactured from genuinely proprietary raw data, and none has ever met the screen harness.

### M7 — Feed the trials ledger. [rank 7]
Gauntlet callers construct `TrialsLedger(Database("data/sor_research.sqlite"))` and pass it;
backfill the 420-trial campaign as one batch. **Why:** DM-4 — the two-stage law's multiplicity
number needs a persistent, append-only home; the hash-chained implementation exists and is
tested. **Interaction:** makes the gate-optimality duty's effective-vs-raw trial audit possible.

### M8 — Fusion negatives → graveyard via the writers that already exist; fix the verdict line. [rank 8]
Six graveyard-writing organs exist (`graveyard_resurrect`, `research_autopsy`, `alpha_lifecycle`,
`screen_idle_axes`, …) — the fusion engines are just not among their sources. Route both engines'
negatives through one of them with permanence grades + re-entry conditions (K2's machinery), and
fix `fusion_engine.py:147` to respect the artifact-gate verdict (DM-9).

### M9 — Regime-label history: the 3-line append, FIFTH sweep. [rank 9 by cost, #1 by shame]
Live proof captured this sweep: at 00:11 today the rule said `trend:bear` while the HMM said
`bull/low_vol` (`hmm_gmm_agree: false`) — with `regime_confidence: 1.0`. That disagreement, an
unrecorded dataset flagged since 07-26 (R0006), was destroyed again at the next overwrite. Also
worth a look while in there: confidence 1.0 under model disagreement is its own small defect.

### M10 — The systematic scope audit of every self-measurement organ. [rank 10, compounding]
Carried (07-30 M3) and strengthened: 3 of 3 organs audited so far had scope defects
(module_justification, data_vitals, coverage_audit). Enumerate all ~15, compare each glob/scope
to its claim, floor the denominators (L1.0 fence). Until this lands, every desk self-score has
an unknown denominator — U1 is the highest-leverage unknown on this page for the third sweep.

### M11 — information_value: populate `lesson` or retire the field. [rank 11]
1,244/1,244 empty (DM-7). Either the writer computes a lesson at append time (it has the
outcome, family, and prior in hand) or the field is dropped and the organ renamed to what it is
(a survival-rate counter). An organ that claims lessons and records none is exactly the L1.21
noise the signal economy forbids.

### M12 — Start era-archaeology conversion: 1 of 713 threads → 1 screened axis. [rank 12]
DM-8: the L1.11a ground is catalogued, unconverted, and already §33-tracked (panel_inbox). The
first real conversion (thread content → structured series → Stage-A screen with the harness) is
the existence proof that decides whether the 69k-thread universe deserves a collector or a
graveyard row. Depth parity applies: 713 titles is breadth; one mined thread is not depth.

---

## 4. WHAT WE TEST NEXT — concrete experiments

**T1 — First-outcome check on the 17:11 wiring (TODAY, after ~08:45).**
`ls -la web/alpha_factory.json data/event_study_listings.json && python3 -m json.tool
web/alpha_factory.json | head -30`. Success: both exist, factory reports ≥1 recommendation and a
(currently flat) lineage; event study prints BOTH exit-rule verdicts (fixed window + triple
barrier). Failure: either absent → the wiring commit failed its first outcome; row a finding.
Also check `data/signal_halflife.jsonl` gained exactly 2 rows (one per signal), not 4 — 4 means
the duplicate scheduler survived the DI-1 fix window.

**T2 — Honest fusion re-run.** Import the close-keyed helper (M3), re-run
`scripts/fusion_engine.py`, diff the artifact. Prediction registered now: kimchi standalone IC
falls from 0.0849 toward ~0.06, all lifts stay negative, verdicts stay SCREEN-UNDERPOWERED.
Success: artifact regenerated leak-free + 5 graveyard rows with re-entry conditions (M8).

**T3 — Tape-staleness fence proof.** After M4, force the condition (tape untouched 48h is
already true today) and verify the alert actually fires in the next data_vitals run — the
outcome, not the config (the desk's four-times-burned lesson).

**T4 — Restore drill.** Pull yesterday's backup to /tmp, replay one day of BTCUSDT L2 through
`micro_factory.book_features`, diff against production `micro_features.json` values for the same
window. Success: identical features from restored data. This is the only test that proves M1
worked; a backup that has never restored is a hope, not a capability.

**T5 — Promotion queue against the real store.** After M2: one run of `run_promotion_queue.py`;
success = the queue artifact lists ≥1 of the 63 candidates with `capacity_usd > 0`, and the
"empty queue" line disappears from its report.

**T6 — Lineage pilot query.** After M5's backfill: `AlphaFamilyTree.best_lineage()` on
funding_carry returns a chain of depth ≥3 (idea → variant → deployed sleeve), and
`dna_distance()` between the deployed alpha and its nearest graveyard sibling is finite. Success
criterion is a *queried answer used in one real decision* (e.g. the factory's next
recommendation cites the lineage), not the data structure existing — U4 of 07-29 stays the bar.

---

## SIX PERSPECTIVES

**1. INTERNAL (measured, not configured).** Collection: alive and compounding (K4: +0.5G/day, 10
days of L2 depth, 2080 files today). Conversion: stalled (DM-5: features/day = 0 since 07-27;
5 of 9 features computed-unused; 447 proposals unbuilt). Memory: adopted and strong (K1/K2/K6)
but fragmented (DM-2) and lineage-empty (DM-3). Reality anchoring: the tape froze for 2.5 days
and nothing noticed (DM-6). Net: the moat ingests excellently, remembers well, combines honestly
(and negatively), and converts almost nothing — L1.8's exact "idle data" defect, measured.

**2. EXTERNAL (how a world-class team would differ).** Nothing exotic — four hygiene standards:
(a) TCA on every fill from day one (here: 0.8% coverage, 7 cycles ranked #1, undone); (b) ONE
experiment store with enforced foreign keys (here: 3 + a phantom); (c) offsite backup with
restore drills as a precondition for calling any dataset an asset (here: none, ever); (d)
genealogy derived from the lifecycle event stream they already emit (here: 656 events, unread).
The gap to world-class in this subsystem is discipline wiring, not invention — which is the
cheap kind of gap.

**3. FUTURE (2–3y redesign pressure).** Own-timestamp L2 + own fills + regime labels is
precisely the training corpus for the execution/microstructure models that are becoming
commodity-trainable; the desk is accidentally accumulating the right proprietary corpus and
keeping it on one disk. The KE corpus (outcome-labelled experiments with mechanism tags) is
already retrieval-augmentation-ready — its weakness is the retrieval index (sim 0.159 on-point
hits), which embedding-based retrieval would fix cheaply. The redesign that matters is not new
data; it is making the existing corpus survivable (M1) and queryable (M5/M10).

**4. CONTRARIAN (test the premise).** What if multi-source fusion — the constitution's named
manufacturing target — simply does not work at the desk's current axis quality? The evidence now
points that way twice: 5/5 combos negative today, 4/5 on 07-27, across two engine designs. The
honest current position: *the moat's realized value to date is negative-knowledge manufacturing
(graveyard + KE + permanence grades), not signal fusion* — and that is a legitimate moat (nobody
can buy our graveyard), but it is not the one the constitution describes. Second contrarian
strike: the du-based pride metric ranks bybit L2 (5.3G) as the crown jewel, but per-byte
irreplaceability ranks the 136KB tape and the 749KB experiment store first — the desk's own #1
information-advantage scoring weights size, and size is the most replaceable property here.

**5. GREENFIELD.** Rebuild from validated knowledge = one WAL sqlite with FK-enforced lineage +
one append-only tape with the v2 TCA schema on every path + vitals globbing `**/*` + offsite
sync + the KE/graveyard corpus as the retrieval layer. Historical baggage carried today: three
same-schema sqlites from three build eras (autodiscovery lab, alpha registry, FX lake), ~40
top-level jsonl stores, and two fusion engines sharing one artifact file. Migration cost is low
— every store except the L2 streams is under 1MB. Replaceability score of current design:
high (which is the polite way of saying: little would be lost).

**6. FRONTIER.** Constrained honestly: no web sweep was run from this read-only seat. From
locally-verifiable frontier: the desk's own arXiv feed step (`research_feed`, in-cycle) is the
standing channel and its exploitation by this subsystem was not assessed; the parquet lake
(1.5G) exists and moat queries do not use columnar scans; event-study methodology (new
yesterday) matches current public standard practice (pre-registered windows, cohort correction).
No unverified external claims asserted. Marked: partially investigated.

---

## NEGATIVE-SPACE SWEEP — never looked at, until (or including) now

1. **Restore-from-backup has never been attempted** — trivially true (no backup exists), and
   four sweeps of this subsystem never tested survivability until today (DM-1).
2. **Nobody has ever queried `alpha_events`** (656 rows, the desk's richest lifecycle stream) —
   for genealogy backfill, for churn stats, for anything (U6).
3. **The rule-vs-HMM disagreement stream** — destroyed nightly for the fifth sweep running, and
   live-disagreeing at this very audit's timestamp (M9).
4. **Cross-store reconciliation** — three sqlites + 40 jsonl stores, never compared row-for-row;
   the kimchi triple-verdict (three stores, three verdicts) remains the proving instance.
5. **Queue age** — nothing asks "how old is the oldest queued campaign?" (measured today: 39
   days, 340 rows, U7).
6. **The tape has never been joined to the L2 stream it trades on** — fill timestamp × book
   state at that timestamp = the desk's adverse-selection dataset, free, both halves on the same
   disk, never computed (blocked at 4 usable TCA rows today, unblocked the day M4 lands).
7. **What fraction of the 447 feature proposals is computable from held data** — never measured;
   the library's `blocked` field exists per-feature and has never been aggregated.
8. **Pattern-mining the 1,244 information_value outcomes** — a survival-prior dataset
   (prior_survive vs survived per family) that has never once been fit, even descriptively.
9. **Cross-asset transfer from the moat** — 26y of FX daily (lake) beside 10d of crypto L2;
   no transformation has ever touched both.
10. **Sqlite store backup** — narrower than #1 and worse: `alpha_registry.sqlite` has live
    `-wal`/`-shm` sidecars on the same single disk; a crash mid-checkpoint is a corruption risk
    the moat files don't even have.

---

## PROACTIVE BATTERY — all 10 moves, with what each produced

1. **CONTINGENCY BEFORE FAILURE** — FIRED: the moat disk's replacement is *nothing* (DM-1);
   named the two failure modes (death and fullness) and the 29-day countdown on the second.
2. **ADJACENCY** — RAN: given DI-1 (duplicate scheduler), asked where else double-writes corrupt
   derived stores → signal_halflife confirmed worst (4 dup rows), breadth_expansion 1 dup,
   6 other stores clean. Bounded the blast radius instead of assuming it.
3. **CONFIG VS OUTCOME** — RAN, three hits: promotion queue installed-but-reads-phantom (DM-2);
   trials ledger built-but-empty ×3 (DM-4); factory/event-study wired-but-first-run-pending (U4,
   T1). One positive: fusion re-ran and its artifact CHANGED honestly (K3).
4. **REGRESSION SWEEP** — RAN on yesterday's 17:11 wiring commit: it lengthened an already
   ~6.5h cycle by 2 steps; put a 15-engine import behind a 180s cap (timeout risk, U4); and the
   intelligence cycle now runs the STILL-LEAKY fusion engine 4×/day — the commit that added
   honest fusion search also multiplied contaminated-artifact production (D1 note).
5. **COST INVERSION** — RAN, nothing found: no paid path proposed or implied; M1's backup target
   is the desk's existing VPS, not a paid service.
6. **GENERALISE THE RULE** — RAN: "the close path emits TCA, open paths don't" generalises to
   *every multi-path writer needs one shared record schema with an assertion* — the same shape
   as the five-copies Upbit failure (§42), now found in the tape writer.
7. **AUTONOMY CHECK** — RAN and FIRED: graveyard_resurrect is scheduled (cron 08:21, manifest
   ×2) and `data/cro_ai_logs/graveyard_resurrect.log` does not exist — the resurrection organ
   has NEVER been seen to run. Configured-not-observed, the exact failure the check exists for
   (folded into DM-11).
8. **NEGATIVE SPACE** — RAN: 10 items above; #6 (tape × L2 join) is the cheapest genuinely-new
   dataset this desk could manufacture this month.
9. **SCOPE THE NEGATIVE RESULT** — RAN on the fusion double-negative: the ROUTE (equal-weight
   z-composites over 5 same-mechanism-family axes) failed; the CAPABILITY (registry-gated
   combination search) is fine and now exists properly. The graveyard rows (M8) must carry that
   scoping or "fusion doesn't work" will over-generalize.
10. **RATCHET CHECK** — FIRED three times: features/day has no floor artifact (currently 0);
    tape row-count/staleness has no fence (DM-6); campaign-queue utilisation (2.3%) is measured
    nowhere. All three are L1.0 "metric with no floor" defects inside the moat subsystem.

---

## LEDGER COMMANDS — queued for the synthesis seat (this seat is read-only on state)

Per the no-orphaned-recommendation law every M-item needs a row; this seat cannot write state,
so the exact commands are staged here for the synthesis seat to run verbatim (as prior sweeps'
were). Top-6 by rank; M7–M12 follow the same pattern from §3.

```
scripts/recommendations.py add --source deep_sweep_data_moat_0731 --title "M1 back up the moat: tape+sqlites today, moat rsync daily, monthly restore drill; disk-runway line in vitals" --detail "DM-1: zero backup, irreplaceable L2+tape, ~29d to 80% disk guard" --due 2026-08-01
scripts/recommendations.py add --source deep_sweep_data_moat_0731 --title "M2 re-point 3 phantom-store readers to sor_research.sqlite; fix max_audit CandidateStore(Path) type bug; declare canonical db" --detail "DM-2: promotion queue reads nonexistent research_memory.db while 63 candidates sit in real stores" --due 2026-08-01
scripts/recommendations.py add --source deep_sweep_data_moat_0731 --title "M3 kill 2 leaky Upbit copies (fusion_engine.py:66, signal_halflife.py:55) via upbit_daily_close_keyed import" --detail "D1 third sweep; 5 contaminated runs/day now" --due 2026-08-01
scripts/recommendations.py add --source deep_sweep_data_moat_0731 --title "M4 tape-staleness fence + vitals glob fix (execution_tape, lake) + TCA fields on all open paths" --detail "DM-6+D5: frozen tape invisible; 0.8% TCA coverage, 7 cycles ranked #1" --due 2026-08-02
scripts/recommendations.py add --source deep_sweep_data_moat_0731 --title "M5 lineage capture at creation (--predecessor in screen templates, predecessor_id on cards) + funding_carry backfill pilot" --detail "DM-3: 4 lineage schemas, 0 edges; factory genealogizes a flat forest" --due 2026-08-03
scripts/recommendations.py add --source deep_sweep_data_moat_0731 --title "M6 schedule micro_factory daily + Stage-A screens for 5 computed-unused micro features" --detail "DM-5: only raw->feature transform unscheduled, stale 3.5d; 6.6G idle" --due 2026-08-02
scripts/track_findings.py add --source deep_sweep_data_moat_0731 --finding "regime disagreement live: rule bear vs HMM bull, hmm_gmm_agree false, confidence 1.0 (M9, fifth sweep)"
scripts/track_findings.py add --source deep_sweep_data_moat_0731 --finding "information_value.jsonl 1244/1244 lessons empty (DM-7)"
scripts/track_findings.py add --source deep_sweep_data_moat_0731 --finding "trials_ledger 0 rows x3 stores; gauntlet ledger=None everywhere (DM-4)"
```
(Exact flag names may need adjustment to the CLIs' current signatures — synthesis seat verifies
with `--help` before running; the content above is the row.)
