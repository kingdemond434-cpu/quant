# WEEKLY DEEP COLD AUDIT — META-AND-BLINDSPOTS — 2026-07-29

_Auditor: retry after BRAIN_AUTH_FAILED stubs on 2026-07-26, 2026-07-28, and earlier today.
This is the FIRST completed meta-and-blindspots audit in desk history — a fact that is itself
Finding M1. Read-only; every claim carries its proving command._

Charter: untested research assumptions, habit-persisting workflows, misleading metrics,
blind-spot transfer from one outside field (this sweep: CONTROL THEORY), institutional curiosity,
research trajectory.

## SCORES
- current_capability_pct: 45 — strong cycle-level meta mechanisms (recursion rule, forecast
  calibration, ledgers, resurrection queue) but the weekly integrator has never run, the gate
  sensors are uncalibrated, and the doctrine genome is never diffed against reality.
- practical_ceiling_estimate: 85
- ceiling_gap: 40
- opportunity_cost_1y: HIGH. One falsified doctrine claim (kimchi) has steered every organ for
  6 days already; uncalibrated gates make the desk unable to know whether its central strategic
  narrative (price-space dead, axis-space rich) is true; a never-run synthesis means zero weekly
  compounding of audit knowledge. Dominant term: misallocated discovery effort, compounding weekly.
- confidence: 0.75
- unknown_unknown_score: 0.7 — this subsystem was never audited before; the first hour of the
  first audit found two structural never-ran organs. Base rates for this layer are unknown.
- info_gain_if_investigated: high (each finding below names its measurement)
- expected_alpha_contribution: indirect but large — gate calibration (M8/T2) directly changes
  which alphas live or die; the 420-re-screen (T5) can revive a written-off search space.
- expected_compounding_contribution: HIGHEST of the eight subsystems — every fix here multiplies
  every future audit, cycle, and screen.
- ceiling_expansion: the ceiling assumption is "the meta-layer is prose reviewed by LLM organs."
  That is methodological, not technological: making doctrine claims machine-checkable (T3) and
  gate error-rates measured (T2) moves the ceiling itself.

---

## 1. WHAT WE KNOW (validated strengths, each with its proving command)

**S1. The self-interrogation recursion rule is real and growing.**
`wc -l docs/research/self_interrogation_patterns.md` → 155 lines; 20 angles + 10 proactive
battery moves. `git log --follow` on the file shows genuine accretion: protocol installed
07-21, angle 11 (geometric-growth realization), angles 12–18 (profit-forensic, from principal
probes), angle 19 (objective-frame, 07-23), angle 20 (de-contamination, 07-23), battery 07-26.
The principal's angles were internalized permanently, exactly as the recursion rule demands.

**S2. Negative results are logged at scale.** `python3 scripts/research_memory.py report` →
144 rows in 30d; 93 construction/failure + 26 hypothesis/failure + 2 dataset/failure + 1
mission/failure = 122/144 (85%) negative outcomes recorded. The graveyard discipline is not
aspirational; it is the majority of the memory.

**S3. The desk forecasts its own experiments and calibrates.** `research_state.json
.forecast_calibration` → n_resolved=30, Brier 0.0722, bias −0.2 (under-confident), applied as
shrinkage on future p_success. A meta-capability most institutional desks do not have. (Note the
sign: systematic UNDER-confidence is timidity in probability space — the shrinkage direction
should be audited once n grows.)

**S4. Institutional curiosity is partially mechanized.** `data/graveyard_resurrection_queue.json`
→ 42 entries, cause-of-death taxonomy, treatments ("PRIME RESURRECTION: killed at DAILY horizon
only"). A consumer exists and ran: `data/horizon_discovery.json` (07-27) re-tested 8 killed axes
(wiki_btc en/ja/ko/ru/zh, defi_tvl, dex_volume, protocol_fees) across 12 horizons under
Bonferroni α=0.0042 — all dead at all horizons, a clean, correctly-multiplicity-counted negative.

**S5. GAP-register re-rank discipline is real.** `grep -n "Re-ranked" docs/GAP_REGISTER.md` →
three stamped re-ranks in the last 48h (07-28T20:30Z, 07-29T06:20Z, 07-29T16:20Z), each with
substantive measured-state narrative. Row #90 (the clobbered principal page) was found by
checking the artifact against the register's claim — the register catching its own false status
is the mechanism working.

**S6. Blind-spot origin telemetry shows the intended trend.** `data/blind_spot_ledger.jsonl`
(59 rows): principal-origin by date 07-22: 12 → 07-24: 2 → since: 0, while self-origin sustains
(17, 5, 4, 10, 6). By this metric the desk is out-finding the principal. (But see M5 — the
metric under-counts principal finds delivered as doctrine.)

**S7. Closed-loop generation exists where §33 governs.** `data/mine_generation_priors.json` →
source classes reweighted by measured conversion (n_seen=6, conversion 0.5, latency 0.25d).
Young (one class), but the feedback loop is wired — the desk knows HOW to close loops. That is
what makes the open loops in M4/M8 inexcusable rather than unavoidable.

---

## 2. WHAT WE DON'T KNOW (ignorance ledger)

**U1. The artifact gate's error rates.** The angle-20 de-contamination gate is doctrine-mandated
("BAKED IN: never bypass it") and has ≥1 confirmed false pass: kimchi passed de-contamination on
07-23 ("vindicated — same-period −0.136, residual IC 0.142") and was retracted on 07-29 as ~73%
timestamp artifact (`research_memory` rm-20260729T111104: "Kimchi screen IC +0.2249 was ~73%
timestamp artifact (Upbit candle_dat…)"; commit 02f2917 "kimchi edge retracted as artifact").
The gate tests ONE artifact class (same-period contamination); timestamp misalignment is a
different class it never tested. False-block rate: entirely unknown. Nobody has ever calibrated
any gate against synthetic known-truth — the one attempt was itself mis-wired (R0017: "injecting
SR_true=+0.5 realises SR=−2.32").

**U2. Whether price-space is actually picked clean.** The 420/0 result ran through gates now
known to reject 100% of everything (R0016: "pbo and reality_check reject 420/420 (100.0%)
because they are campaign-level statistics" mis-applied per-candidate; 2026-07-29 validation-
stats audit: the DSR bar needs true SR≈5 at T=310/N=420). A gate rejecting 100% carries zero
information (GATE-OPTIMALITY duty), so 420/0 is exactly what a broken bar produces whether or
not edges exist. The desk does not know its own historical false-negative rate.

**U3. Whether doctrine text produces behavior.** Doctrine injection is open-loop: no measurement
exists of duty compliance (e.g., what fraction of digger runs actually executed screen-on-
discovery? what fraction of organ runs logged battery moves?). The blind-spot ledger suggests
some compliance; the rate is unmeasured.

**U4. Incident base rates.** Dead-man fires: incident numbering reaches #6 in 9 days
(INCIDENT_20260719_DEADMAN, _20260722_DEADMAN5, _20260727_DEADMAN6) but files exist for only
#1, #5, #6 (`grep -rn "DEADMAN2\|DEADMAN3\|DEADMAN4" data/*.md` → nothing). No cross-incident
synthesis exists; whether the class is shrinking is unknown.

**U5. Whether audit findings survive the week.** No SYNTHESIS report has ever been produced
(`ls docs/research/deep_sweep/ | grep -i synth` → empty, exit 1) — so auditor-disagreement
adjudication (E), the capability map (B), the portfolio (C), and the engine-CAGR question (G)
have never been computed even once.

**U6. The meta-layer's own cost and capacity.** Production rate of obligations (recommendations,
register rows, inbox items) vs. disposal capacity has never been measured. Current reading:
31/39 recommendation rows open past grace. Is that a discipline failure or a capacity deficit?
Unknown — nobody has measured either side. (Mitigating context: `research_state.json
.binding_constraint` = "calendar-time data accumulation (not engineering throughput)" — while
data accrues on calendar time, meta-work is nearly free. When the constraint shifts, the
meta-tax must be measured.)

**U7. The load-bearing assumptions themselves.** No assumptions inventory exists anywhere in the
repo (negative-space sweep: no file lists what the research stack assumes). Unlisted and
untested: daily-bar sufficiency; UTC-close alignment convention; next-day-BTC default target
(the universal duty commands a sweep; adherence is unmeasured); linear-IC screening (misses
nonlinear/interaction structure by construction); the Kelly-shrink constant and the ruin≤2%
model's distributional assumptions (angle-13 status: measured or guessed — unanswered); the
static T-bill hurdle; artifact-gate completeness (U1). None has a written falsifier.

---

## FINDINGS (all six perspectives; feeds sections 3–4)

**M1 [INTERNAL — the audit's own machinery]. The meta-auditor and the synthesis lead are
structurally starved and have NEVER run.** Mechanism, from `scripts/run_deep_sweep.py`:
`SUBSYSTEMS` is a fixed-order dict iterated sequentially; meta-and-blindspots is 8th of 8; the
synthesis prompt runs 9th. The dual-pool comment admits it: "the first ones drain fable, the
rest land on opus-5." When the pool drains mid-run, the tail dies. Resume makes it structural:
on catch-up, earlier failures re-fire FIRST (same dict order), re-draining the pool before the
tail's turn. Evidence: `head -1` of all 24 sweep reports → meta-and-blindspots is the ONLY
subsystem at 3/3 AUDITOR FAILED (0726, 0728, 0729); 0728 shows the clean drain pattern
(positions 1–3 OK, 4–8 all failed); on the 0729 catch-up, positions 4–7 succeeded and position
8 failed again. Synthesis: 0/3 ever. Consequences: the audit's self-improvement loop (F), the
prioritized portfolio (C → improvement_inbox), and engine-CAGR (G) have never executed — the
weekly audit produces 8 reports and compounds nothing. And no counter tracks per-subsystem
failure streaks, so a 3-week-dead auditor was invisible (`grep -rn "consecutive" scripts/
organ_catchup.py` → nothing; catchup re-fires wholesale but never counts).

**M2 [INTERNAL]. The §41 recommendation ledger fires and nothing consumes the firing.**
`python3 scripts/recommendations.py report` → "39 total | 5 implemented | 1 rejected | 2
scheduled | 31 open", every open row printed as "DEFECT [UNDISPOSED past grace]", oldest 3.4d
against a 24h grace. The ledger was built precisely so recommendations cannot die in reports;
they now die in the ledger instead, because disposal is bound to no cadence slot and no organ.
An alarm with P(action|alarm) → 0 is alarm fatigue by construction (see M8d). Among the ignored:
R0016/R0017/R0023/R0030 — the gate-calibration cluster this audit's M3 turns on.

**M3 [CONTRARIAN — deepest finding]. The desk's strategic narrative is currently supported by
zero valid evidence.** The narrative — "the edges are in untouched axes, not the picked-clean
price space" — rests on two pillars: (a) 420 price-family hypotheses → 0 survivors, and (b) the
kimchi premium's Stage-A screen (IC +0.148, Sharpe 1.3). Pillar (a) ran through gates that
reject 100% of candidates (R0016; DSR bar needing SR≈5 per the 07-29 validation-stats audit) —
zero information. Pillar (b) was retracted 2026-07-29 as ~73% timestamp artifact
(rm-20260729T111104; commit 02f2917). The allocation may still be RIGHT — literature priors
genuinely favor unexplored axes — but the desk believes it for reasons that are now known-false,
and the doctrine still recites both pillars as live evidence to every organ (see M4). Follow-on
defect: the retracted kimchi axis is STILL accruing a Stage-B forward clock and occupying a Holm
slot (`data/axis_shadow_state.json` → kimchi_premium ACCRUING, forward_days 7/40, ann_sharpe
−4.6, m_concurrent=4, holm_bar 2.24) — a retracted screen is raising the promotion bar for
every live candidate. Retraction must free the slot.

**M4 [INTERNAL + FUTURE — misleading metrics class]. The doctrine genome is never diffed against
reality; empirical claims fossilize.** Two confirmed drifts, one grave: (a)
`grep -n kimchi ops/principal_doctrine.txt` → line 89 still cites "IC +0.148 and momentum timing
Sharpe 1.3 — beating every price-only sleeve" with no retraction marker (`grep -n retract` →
nothing), six days of every organ being injected with a falsified claim — and the same paragraph's
own point (4) warns about EXACTLY the artifact class that killed kimchi ("a daily FX close is NOT
the crypto UTC close"). (b) §33 doctrine cites `data/mine_conversion_ratchet.json`; the file does
not exist (`ls` → No such file); the ratchet actually lives at `docs/research/
conversion_record.json` (`grep -n "MINE_RATCHET =" scripts/max_audit.py` → line 1878). The law
still binds because max_audit enforces the real path, but doctrine-vs-implementation drift has no
detector. FUTURE redesign: every empirical number, path, and citation in doctrine text becomes a
row in a machine-checkable claims manifest; max_audit diffs it weekly (defect class:
doctrine-claim-stale). With LLM organs, doctrine IS code — it deserves CI.

**M5 [INTERNAL — misleading metric]. The blind-spot origin metric under-counts the failure
signal it exists to detect.** The 07-26 principal deliveries (PROACTIVE BATTERY DUTY — "the
principal kept finding gaps the desk could not" — and the UNIVERSAL DUTY SET) were themselves
principal-found meta-gaps, yet the ledger shows 07-26: {self: 4}, principal: 0 that day, 0 since
07-24. Gaps the principal delivers as doctrine bypass `scripts/blind_spot.py` entirely, so the
"principal-found is the failure signal" gauge can read green while the principal supplies the
largest gaps out-of-band. Fix: every principal doctrine/directive automatically logs a
principal-origin ledger row (the doctrine change IS the evidence a gap existed).

**M6 [EXTERNAL — what a world-class desk would do that we don't]. Recurring incidents get
one-off autopsies, never a series analysis — normalization of deviance.** Six dead-man fires in
nine days (numbering to #6), three incident files, zero cross-incident synthesis, no base-rate
trend, no "days since last fire" telemetry. Same shape one layer up: organ death by quota/auth
is now weather — the 0726 sweep's 8 crash stubs were noted "in passing" in improvement_inbox
item 65 as "another organ's crash residue." Aviation/nuclear practice: any recurring alarm class
gets a series review with a rate target and an owner. The desk autopsies losses (L2.6) but not
recurrences.

**M7 [GREENFIELD]. Rebuilt from scratch, the meta-layer would be ONE work queue, not five
written stores.** Current stores: recommendation_ledger.json (39 rows), GAP_REGISTER.md (91
rows), improvement_inbox.md (1023 lines, ~65 numbered items, 2 carrying "STATUS: inbox item",
~7 matching implemented/wired/done), panel_inbox.md (683 lines), adoption_queue.md (trigger-
conditioned, by design), plus micro_audit_inbox and feed_inbox. §35's own insight — "being
written down is not being driven" — now applies to the meta-layer itself: one drive organ (the
register) and N parallel written stores whose overlap and leakage are unmeasured (R0015, itself
undisposed, already flags two producers sharing deep_sweep/). Greenfield design: one queue, one
schema (source, ERV, owner, SLA, disposition), N views. Historical-baggage score: high — each
store was the right fix for its week; nobody has consolidated because consolidation is nobody's
subsystem. (This audit does not recommend a big-bang rewrite; it recommends the leakage
measurement first — see T6.)

**M8 [FRONTIER — blind-spot transfer: CONTROL THEORY].** The desk's meta-layer is a control
system built by intuition; control theory names its failure modes precisely and supplies
standard remedies:
  (a) **One-way ratchets are integral action without anti-windup.** When the plant saturates
  (quota outage, absorbing-state book, unclearable gate), the integrator keeps winding: bars
  tighten against organs that cannot respond, guaranteeing either rationalization or gaming.
  Remedy: conditional integration — the ratchet pauses ONLY on a measured exogenous-saturation
  artifact (e.g., BRAIN_AUTH_FAILED stub on disk), never on soft excuses, and every pause is
  logged. The constitution's ratchet-check (battery move 10) checks the floor holds; control
  theory adds: check the floor is not demanding output from a saturated actuator.
  (b) **Gates are sensors, and sensors get calibrated against reference standards.** The desk
  has never run a known-truth battery through its own instruments; the one synthetic probe was
  mis-wired (R0017, SR_true=+0.5 → realized −2.32 — a normalization trap). Metrology discipline:
  quarterly reference battery, published false-pass/false-block matrix per gate (T2). This
  converts GATE-OPTIMALITY from aspiration to measurement.
  (c) **Open-loop vs closed-loop actuators.** §33 generation is closed-loop (priors reweight by
  measured conversion — S7). Doctrine injection is open-loop (U3): text goes out, no behavior
  measurement comes back. The desk already owns the loop-closing pattern; generalize it
  (battery move 6) to doctrine compliance itself.
  (d) **Alarm fatigue is detection theory.** 31 undisposed DEFECT rows, permanently-firing
  gates (R0016), recurring dead-man fires: alarm classes whose P(action|alarm) has collapsed.
  Standard remedy: track fired-vs-acted per alarm class; any class under an action-rate floor
  is REDESIGNED (merged, demoted, or given an owner), never tolerated (T6).

**M9 [CONTRARIAN + negative space]. No assumptions inventory exists** (U7). The desk that wrote
"no assumption ships unverified" cannot produce a list of its own load-bearing assumptions.
Concrete unlisted examples with cheap falsifiers: daily-bar sufficiency (falsifier: re-screen
one known-dead and one live axis at 4h/1h bars); UTC-close convention (falsifier: shift close
±1h, measure IC delta — kimchi died of exactly this, unlisted); linear-IC-only screening
(falsifier: distance-correlation/MI screen on already-ingested axes — nonlinear structure is
invisible to every screen the desk has ever run); Kelly-shrink and ruin-2% constants
(angle-13 audit: measured or guessed). T7 creates the inventory.

**M10 [INTERNAL — trajectory verdict, per charter]. Cycle-level compounding is real; week-level
compounding does not exist.** FOR: recursion-rule accretion (S1), forecast calibration (S3),
generation priors (S7), resurrection→horizon loop closed with correct multiplicity (S4), 47
cycles logged. AGAINST: the designated week-level integrator — the sweep synthesis with its
engine-CAGR question (G) — has run 0/3 times (M1); no velocity metric (screens/week,
survivors/week, latency trend) exists anywhere. The desk improves within cycles and cannot yet
SEE whether weeks improve on weeks. Verdict: trajectory positive at cycle grain, unmeasured at
week grain, and the measuring organ is the starved one.

**M11 [INTERNAL — environment fragility, found in passing].** `python3 scripts/
horizon_search.py` → ModuleNotFoundError: numpy on the system interpreter. The 07-27 output
exists, so a working interpreter path exists somewhere (venv/cron env), but any organ that
shells bare `python3 scripts/<x>.py` dies. Same failure class as M1 (organ death), different
trigger. Not deeply chased in this read-only pass — flagged for the infrastructure auditor with
the reproducing command.

**Perspective coverage:** INTERNAL M1/M2/M5/M10/M11; EXTERNAL M6; FUTURE M4(redesign);
CONTRARIAN M3/M9 (+U6 meta-tax question); GREENFIELD M7; FRONTIER M8. Negative-space sweep
produced U7/M9 (no assumptions inventory), U3 (no compliance measurement), U4 (no incident
series), U6 (no meta-cost measurement), M10 (no week-grain velocity metric).

---

## 3. WHAT COULD MATTER MOST (ranked: impact × confidence / (cost × maintenance))

1. **Fix sweep starvation + synthesis (M1).** Rotate auditor start index weekly (start = week
   number mod 8); on resume, run SYNTHESIS FIRST if ≥6 reports exist, then failures; track
   per-subsystem failure streaks (streak ≥2 → its own alarm). Cost: ~20 lines in one script.
   Compounding multiplier: unlocks every future week's knowledge integration. THE top item.
2. **Gate reference-standard battery (M8b, T2)** — repairs R0017's normalization trap first.
   Directly answers whether the desk's instruments can see edge at all; prerequisite for
   trusting ANY screen result, positive or negative. Compounding multiplier.
3. **Doctrine claims manifest + weekly diff (M4, T3).** Kimchi (retract/annotate doctrine line
   89) and the ratchet path are rows one and two, fixable today. Stops the prompt genome
   steering organs with falsified evidence. Compounding multiplier (doctrine-as-code CI).
4. **Free the kimchi Holm slot + re-derive the axis-first narrative (M3).** Immediate: retire
   the retracted axis's forward clock (or re-register it as its own de-artifacted
   reconstruction). Then T5 (420 re-screen under calibrated gates) settles the narrative with
   valid evidence either way.
5. **Bind §41 disposal to a cadence slot (M2, T4).** Cheap; without it every other ledger
   improvement produces more ignored alarms.
6. **Alarm-to-action telemetry (M8d, T6)** — generalizes M2's fix to every alarm class; the
   detection-theory floor makes alarm-design self-correcting.
7. **Assumptions inventory (M9, T7)** — cheap to seed, permanent negative-space closure; UTC/
   resolution falsifiers are hours of work and one (timestamp) already killed an "edge."
8. **Incident series review (M6)** — dead-man class first: rate, trend, owner, target.
9. **Principal-doctrine auto-logging (M5)** — three lines in the doctrine-update workflow;
   restores the failure-signal metric's integrity.
10. **Meta-cost measurement (U6)** — production vs disposal capacity per week; decides whether
    the meta-layer needs consolidation (M7) or just owners.

Interactions: 2→4 (calibrated gates are the precondition for the 420 re-screen); 5↔6 (same
mechanism, one general); 3 consumes 4's retraction as its first manifest row; 1 unlocks the
weekly measurement that makes 10 answerable.

## 4. WHAT WE TEST NEXT (experiments with success criteria and retirement conditions)

- **T1 (M1):** Patch run_deep_sweep.py order rotation + synthesis-first-on-resume + failure-
  streak counter. Success: 8/8 auditors + synthesis produced for two consecutive weekly sweeps.
  Validation: `head -1` all reports + synthesis exists. Failure mode: rotation hides a
  systematically-broken auditor — the streak counter is the guard. Retire the streak alarm only
  after 4 consecutive full sweeps.
- **T2 (M8b):** Reference-standard battery: synthetic series with SR_true ∈ {0, 0.5, 1.0, 2.0}
  × artifact class ∈ {none, timestamp+1d, same-period contamination, volume-coupled}, run
  through axis_screen AND the gauntlet gates. Success: published false-pass/false-block matrix;
  every gate's acceptance on knowns strictly inside (0%, 100%). Prerequisite: fix R0017's
  injection normalization (the current probe realizes SR −2.32 from SR_true +0.5 — the
  instrument for testing instruments is itself broken). Quarterly cadence thereafter (§36 row).
- **T3 (M4):** docs/DOCTRINE_CLAIMS.json manifest: {claim, source-line, verifying command,
  last-verified}. Seed with kimchi line 89 (mark RETRACTED, point at rm-20260729T111104) and
  the §33 ratchet path. max_audit gains doctrine-claim-stale. Success: a deliberately-seeded
  stale claim fires within one audit cycle.
- **T4 (M2):** First slot of the daily cycle: dispose ≥5 ledger rows or log the measured reason.
  Success: open-past-grace ≤5 within 7 days and held for 14. Retirement: never (it's a cadence
  duty); the count target retires when steady-state open<grace.
- **T5 (M3):** After T2 passes: re-screen the top decile (≤50) of the 420 price-family under
  calibrated per-candidate gates. Every cell a logged trial (Stage-A only, zero promotion
  authority, novelty-gated against the graveyard). Success: EITHER outcome is knowledge —
  survivors ⇒ the picked-clean narrative is revised and doctrine updated via T3; zero ⇒
  "price-space dead" finally carries valid evidence and the graveyard entry cites it.
- **T6 (M8d):** data/alarm_telemetry.jsonl: every alarm class logs fired/acted weekly. Any class
  with action-rate <20% over 2 consecutive weeks triggers a mandatory redesign row in the
  register. Success: zero alarm classes below floor after 4 weeks (by redesign, not by
  deletion — the §34 denominator rule applies to alarms too).
- **T7 (M9):** docs/research/ASSUMPTIONS.md seeded with the 8 assumptions in U7, each with
  owner, falsifier, cheapest test, status. Run the two cheapest falsifiers this week (UTC-shift
  IC delta; 4h-bar re-screen of one dead + one live axis). Success: file exists, referenced by
  the screen harness docs, 2 falsifiers executed and logged to research_memory.
- **T8 (M6):** DEADMAN series review: one page — all 6 fires, cause taxonomy, rate trend,
  the fix that recurred vs held, days-since-last telemetry on the health page. Success: next
  fire (if any) cites the series doc and updates it; alarm if file silent >30d while fires
  continue.

_Disposition per §41/§35: this report's recommendations require ledger rows. The auditor ran
read-only; the next live cycle must row T1–T8 (one row each, T1–T3 tier-1) — until rowed, by
the desk's own law, these findings are invisible._

_Meta-note under the honesty guard: this audit found the audit system itself to be its
subsystem's largest defect (M1). That is expected for a first-ever run of a starved organ, and
it is exactly why the rotation fix is ranked #1 — the next meta-audit should be about the
research process, not about whether the auditor runs at all._
