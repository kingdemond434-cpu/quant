# DEEP COLD AUDIT — LAUNCH-READINESS (money path AS WIRED)

STATUS: COMPLETE
Auditor: weekly deep cold sweep, subsystem = launch-readiness
Date: 2026-07-31 (audit ran same day as the 08:35Z book freeze — the ruin-rail path has live evidence today)
Method: outcome-not-config. Every claim carries its proving command + output. READ-ONLY audit.

## SCORES

- **current_capability_pct: 38%.** The halt/pager/refusal architecture is real and was battle-tested LIVE today (kill honored every 60s tick, ladder escalated and froze correctly, reply channel polling verified 28s-fresh). But the launch-specific wiring — the part that fires exactly once — carries 6 CRITICAL defects (F1, F2, F3, F4, F13, F14+F15) any one of which either blinds the Tier-3 rail, misleads the principal, or requires launch-day money-path code edits.
- **practical_ceiling_estimate: 95%.** Every defect found is ordinary engineering (file paths, a connector factory, state-reset script, evidence bridges); no external blocker exists except spot live keys + the deposit, both principal acts.
- **ceiling_gap: 57 points**, nearly all closable in 2-4 focused days.
- **opportunity_cost_1y:** asymmetric and large in BOTH directions. Launching as-wired: the live book trades with the deadman watching a dead testnet account (F14/F15) — tail cost is the entire live book, unwatched, plus the credibility of every rail. Not launching: the whole first-year live compounding stream AND Gate-0's forward evidence (already frozen once at 26.42/28 days by exactly this class of defect). The fix bundle is days; the costs are unbounded/permanent. This is the desk's current L1.13 dominant bottleneck.
- **confidence: 0.85** — every load-bearing claim above was verified by running a command or reading the artifact this session; the three subagent sweeps were spot-checked on their critical claims (deadman pinning, spot keyfile name, drill void) by direct reads.
- **unknown_unknown_score: 0.35** — live-venue behavioral unknowns (real slippage, partial fills, maintenance windows, key-permission surprises) are irreducible until first live fills; the WIRING unknowns are now substantially enumerated.
- **info_gain_if_investigated: high** — the first live week is itself the highest-information experiment the desk can run (L2.10 reality-gap chain currently has zero live links).
- **expected_alpha_contribution: decisive but indirect** — launch is the transmission mechanism for EVERY validated edge; a blind rail or false board taxes all of them.
- **expected_compounding_contribution: highest on the desk right now** — L1.20: research exists to improve deployed capital; deployment is the binding constraint on compounding starting at all.
- **ceiling_expansion:** the 95% ceiling assumes "launch = flip this stack as designed". The assumption that defines it is ORGANIZATIONAL, not technical: one executor, one state file, testnet and live sharing artifacts. A redesign (single equity oracle, single board, connector factory, separate live data-root) would push the ceiling to ~99% and remove the entire cutover-reset class permanently.

## 1. WHAT WE KNOW (validated strengths, each with its proving command)

1. **The kill switch is honored mid-loop, every 60s, and survives venue outage.** `run_cashcarry_executor.py:1453-1476` — KILL checked at every heartbeat tick (not startup-only), close-all is idempotent, exceptions defer rather than crash (the 2026-07-31 418 fix, verified in code + exercised live today). Kill-switch consumer map (subagent, spot-checked): no meaningful TOCTOU on the order path.
2. **The pager chain delivers, and delivery is ledgered.** `tail data/alert_delivery.jsonl` → ntfy `ok:true "http 200"` at 13:32Z and 14:00Z; `data/.reply_poll_state.json` polled 2026-07-31T14:08:11Z (28s before my check); watchdog:219 runs run_alerts every 3-min tick.
3. **The reply channel exists end-to-end in code, correctly scoped.** `run_alerts.py:127-178` — any titleless reply writes PAGE_ACK; REARM runs `run_live_guard.py --rearm` and unlinks ONLY a live_guard-authored kill (`startswith("live_guard freeze")` — deadman kills preserved); processed before page computation so a fresh ack suppresses that tick's escalation.
4. **The ladder did its job today, and its state is inspectable.** `data/derisk_state.json`: nominal→flatten_to_neutral (7,109s unacked)→full_flatten_disarmed (28,732s), latched, rearm_required — exactly per spec; `live_guard.json` reports it every 5 minutes.
5. **The deadman rail is alive, armed, and independently measuring venue truth.** `data/deadman_heartbeat` fresh (pid 1463355 running); `deadman_state.json`: eq 6,226.85 vs fire line 4,067 (0.65×HWM), breaches 0, disarmed_live false; watchdog:190 respawns it if stale >5m. (Armed FOR THE TESTNET — see F15 for what that means at cutover.)
6. **The capital-event machinery refuses every dishonest path.** `libs/risk/capital_events.py:125-149` — $0 deposits refused, unsigned events refused, reason <12 chars refused, breach-live re-base without capital refused absent PRINCIPAL-OVERRIDE; proven live: `record_capital_event.py --show` REFUSED to fabricate equity ("UNVERIFIABLE... refusing"). Drilled (ruin_rail_reentry, 4/4).
7. **The live connectors are triple-gated inert, keyfile-only, capability-whitelisted (one exception, F19).** Proven by outcome: `signed call REFUSED, not armed: keys_present=True, live_enable_flag=False, vps_verified=False` printed during this audit's --show run.
8. **live_guard fails closed and never self-promotes.** `run_live_guard.py:132-137` unreadable venue → treated as naked; :119-122 never auto-lifts another writer's kill; :14-16 promotion evaluated, never taken; single-kill-file architecture (no parallel halt to disagree).
9. **The executor holds the single-book invariant without respawn storms.** :1413-1432 PID-owned heartbeat, standby-not-exit (the 14,225-restart and 190-spawns/hour incidents both have fixes present in code).
10. **Open/close discipline is measured, not vibes.** Churn guard (24h floor from measured bucket P&L), entry gate per-symbol cost bar p90 fail-closed, bleed denylist from forensics, orphan-cover confirm+cap+cooldown+circuit, basis-blowout stop, water-fill concentration cap — each carries its incident citation in code.
11. **Drills exist, pass, and are order-inert by construction.** `data/drill_report.json` 19/19 PASS ×3 runs; `tests/ops/test_drills.py::test_no_drill_can_place_an_order`.
12. **The S2 drill criterion is fail-closed** (sentinel −1, found by mutation testing twice independently — staging.py:77-83).
13. **Freeze-exit criteria are now mostly real artifacts:** `data/cost_model.json` (54KB, fresh 08:36) and `data/forecast_log.json` exist with real writers; fills measured on row timestamps not mtime (the inverted-mtime bug is documented and dead).

## 2. WHAT WE DON'T KNOW (ignorance ledger)

1. **No live fill has ever occurred.** Real slippage, partial-fill behavior, maker-fill rates, fee tier, transfer latencies at Binance mainnet: all unmeasured. The cost model is testnet-calibrated; L1.5 execution physics for the live venue is an assumption until week one.
2. **REARM has never fired end-to-end.** `data/principal_replies.jsonl` does not exist → zero replies ever processed. The ONLY un-freeze path (and today's freeze awaits exactly it) is unexercised. (Its transport trust boundary — anyone with the ntfy topic can publish REARM — is a ledgered limitation, unprobed.)
3. **Whether the venue-vs-book divergence shadow is calibrated on a scope mismatch.** claim_verifier (HIGH, in PRINCIPAL_ACTION.md): venue $6,227 is FUTURES scope, book $18,670 is TOTAL molded scope — the gap #19 breaker being calibrated on `pct_diff` between them may never/always fire. Unresolved.
4. **Per-symbol minNotional viability at $200** (F25): never computed against the actual top-N funding universe.
5. **Launch-hour interactions:** venue maintenance windows, the 8h funding-cycle boundary vs the 600s rebalance cadence, first-tick behavior with an empty live account — none studied or drilled.
6. **Watchdog vs systemd double-spawner behavior at cutover** (`quant-cashcarry.service` mapping in watchdog:58 + direct `_spawn` path): which owns the executor after a reboot mid-launch-week is untested.
7. **Whether `data/gate0_complete` is INTENDED as a human-touched milestone** — no doc says who creates it or when (F9/F22); as-written the freeze-exit gate is unsatisfiable and nobody can tell from outside.
8. **Second-family check (L1.33): SOLO.** The GPT-family partner seat is DOWN (OpenRouter balance −$0.59); every verdict in this report is single-family and labeled SOLO, not CONFIRMED. The $25 top-up on the principal's page would restore cross-family verification for the next sweep.

## 3. WHAT COULD MATTER MOST (ranked by expected impact × confidence / (cost × maintenance))

Each item: exactly-what → evidence → benefit → complexity → validation → failure mode → retirement. All are conversion-queue candidates (L1.28b); none loosens any rail; every one must land BEFORE the flip except #9/#10.

**R1. Build the live deadman (F14+F15) — the single highest-EV item on the desk.** Exactly-what: a live-mode of run_deadman_switch (base URLs + keys by mode flag), plus mandatory state re-init at cutover (delete deadman_state.json; baseline re-captures on first 3 polls). Evidence: `run_deadman_switch.py:32-35` pinned testnet; usdt_baseline 105,914 vs live reality. Benefit: the Tier-3 rail actually watches the live book. Complexity: low (config + one state deletion in the cutover script). Validation: drill = seed a live-shaped state file, assert fire-line arithmetic; then observe 3 polls on the funded account before first order. Failure mode: mode flag misread → still watching testnet — so the cutover check must ASSERT the deadman's polled equity ≈ deposited amount before LIVE_ENABLE is touched. Retirement: never (survival rail).

**R2. One cutover-reset script + runbook rewrite (F15, F16, F17).** Exactly-what: `scripts/cutover_to_live.py` (or a runbook section) that archives-then-resets: cashcarry_positions.json (state), realized_spot_pnl, execution-tape live/testnet separation (new tape file or a `venue:` field every reader filters on), deadman state, and creates `data/gate0_complete` if that is the intended latch — each step printing venue-verified before/after. Benefit: kills the entire "testnet artifact greens a live gate" class (3 gates today). Complexity: low-medium. Validation: run against a copy of data/; assert change-window reads 0 live fills, freeze-exit fills_4wk goes False, gate0 board connector_verified goes NOT-READY. Failure mode: partial reset worse than none → script is all-or-nothing with a dry-run mode. Retirement: after first clean live week.

**R3. Fix the capital-event reader path + add live-only guard (F1).** Exactly-what: point `record_capital_event.py:_STATE` and `check_gate0_ready.py:140` and `check_change_window.py:96` at `data/cashcarry_positions.json` (the file the executor writes), AND make the freshness rung refuse a persist whose writer was in testnet mode (one `mode` field in the persist). Evidence: `--show` printed inception $0.00 this session. Benefit: the launch-day command sees truth; the refusal actually protects; the board's ruin-rail row becomes measurable. Complexity: trivial + one field. Validation: `--show` prints inception $5,000 and the real breach history. Failure mode: fixing the filename WITHOUT the mode guard re-opens testnet-equity→live-rail contamination (named above). Retirement: never.

**R4. Make the two boards one, with honest inputs (F2, F3, F21).** Exactly-what: check_gate0_ready derives keys_present from `binance_live.is_armed()[1] + binance_spot_live.is_armed()[1]` component-wise (not a secrets glob), connector_verified from live-tape rows only, capital_fraction from config-capital ÷ LIVE deposited equity (refuse if unknown); live_guard feeds s1_entry_met a real capital fraction and the config `top` for symbol_count; principal_signoff read from ONE artifact by both (gate0_signoff.json exists — make stage_state/promote read it). Benefit: the principal sees one truthful board; the gate stops being welded. Complexity: low. Validation: board vs live_guard.json agree on all five criteria for 24h. Failure mode: silent re-divergence → add a fence comparing the two outputs (or delete one). Retirement: the comparison fence retires when the boards are literally one code path.

**R5. Ship the connector switch so launch day edits config, not code (F7, F17).** Exactly-what: a factory (`libs/execution/venue.py: get_connectors(mode)`) reading ONE mode source (stage_state or a data/VENUE_MODE file), imported by the executor; testnet and live both behind it; the live pair verified by the existing triple-gate. Benefit: L1.38 compliance on the day it matters; the flip becomes auditable state, not a diff. Complexity: medium (mechanical; interfaces already mirror by design). Validation: run the executor in dry mode against the factory in both modes; diff behavior zero on testnet. Failure mode: a missed direct import — fence: grep-test that forbids `import binance_.*testnet` outside the factory. Retirement: never (it IS the flip).

**R6. Wire drill evidence to its consumers (F4) + drill the misses (F12).** Exactly-what: run_drills.py (or live_guard) writes `drill_pass_streak_weeks` + `critical_drill_failures` into ramp_state.json evidence after each run; add drills for: 418 mid-flatten (replay today's incident), REARM end-to-end (fake ntfy poll fixture), duplicate capital event, stop-order rejection, equity-source disagreement. Benefit: the 8-week ramp clock STARTS (it is a prerequisite for ever leaving 10% sizing — every idle week is L1.28a idle capacity), and the drill set covers the failures the desk has actually had. Complexity: low for the bridge; medium for new drills. Validation: live_guard.json ramp.checks.d_drill_streak flips measurement from 0.0; new drills red-then-green on injected breakage. Failure mode: backfilling streak from the 3 existing runs would fake 8 weeks — start the clock at 0 from the bridge commit. Retirement: never.

**R7. Confirmation count before guard-freeze, and an entries-only halt distinct from flatten (F5).** Exactly-what: live_guard requires N=2 consecutive failed venue reads (10 min) before writing KILL for the `<unreadable>` case (REAL naked positions keep the 60s clock); optionally add a `CASHCARRY_PAUSE_OPENS` file the executor honors as top=0-but-hold (machinery exists: `pause_opens`). Benefit: a single HTTP timeout can no longer round-trip the whole book through market orders (today's incident chain, generalized). Complexity: low. Validation: drill = unreadable-venue one tick (no kill), two ticks (kill). Failure mode: N too high delays a real naked-position response — REAL naked detection is untouched. Retirement: never.

**R8. Config fail-safe direction (F18).** Exactly-what: on config read failure, `_live_params` returns the LAST-GOOD persisted params (or holds/pauses), never argv; watchdog's respawn args drop `--capital 4500` for a small default. Validation: corrupt the config in a test → executor holds prior sizing. Retirement: never.

**R9. Key-permission probe (F24) + connector-boundary kill check (F20) + transfer-whitelist decision (F19).** Exactly-what: at key placement and daily, call `GET /sapi/v1/account/apiRestrictions`, page on withdrawal-enabled or missing IP whitelist; add `if _KILL.exists(): raise` to binance_live signed POSTs; either move universalTransfer behind its own reviewed module with kill-check or delete it (it has its own caps but violates the whitelist promise). Complexity: low each.
**R10. Small closures:** arm the second alert channel registry (F11 — data/secrets/alert_channels.json, one telegram webhook suffices); split finish_setup step 6 into two prompts (F23); make the S1-gate-met page append via `libs/ops/principal_page.py` instead of the `not exists()` guard (F10 — the append-safe helper ALREADY EXISTS, built 07-30 for exactly this clobber class); fix check_freeze_exit_sources to verify the writer WRITES (grep the writer for the artifact path) (F22); minNotional pre-check in `_entry_gate` using exchange filters already fetched (F25); document data/FREEZE semantics (F27).

**Compounding-multiplier flag:** R2+R5 (cutover script + connector factory) are multipliers — they convert every future venue addition and every future stage transition from a hand-executed ritual into tested code. R4 (one board) multiplies the value of every future criterion added to it.

## 4. WHAT WE TEST NEXT (concrete experiments, success criteria, retirement)

1. **Cutover dress rehearsal on a COPY of data/** (needs R2): run the reset script, then assert: deadman state re-inits with live-scale baseline; change-window reads 0 live fills; freeze-exit fills_4wk=False; gate0 board shows connector_verified NOT-READY + ruin_rail measurable. Success = all four flip correctly with zero manual edits. Retirement: after first live week.
2. **REARM end-to-end drill** (needs R6): inject a synthetic titleless reply into a poll fixture; assert PAGE_ACK written, ladder cleared, live_guard-authored KILL unlinked, deadman-authored KILL preserved. Success = full chain, zero manual steps. Also validates the one path today's real freeze is waiting on.
3. **418-mid-flatten replay drill**: seed BINANCE_BAN_UNTIL in-window + a fake close-all rejection; assert the KILL loop defers calmly (no respawn storm, no crash), ban latch honored, page emitted once. Success = today's incident replays green.
4. **First-live-day canary sequence** (write into the runbook): keys placed → apiRestrictions probe green → deadman observes deposit-scale equity for 3 polls → record_capital_event (now reading truth) → LIVE_VPS_VERIFIED → LIVE_ENABLE → ONE minimum-notional canary round-trip (binance_live has the order surface; the canary spec already names this) → verify fill lands in the LIVE tape partition and stops appear venue-side → then top=4. Success criteria at each arrow; any red = stop, page, stay flat.
5. **Stop-placement first-fire test**: on the canary position, assert `place_stop_market` accepted and visible in open_orders within one guard tick (the code path has never executed anywhere — F6). Success = live_guard reconcile reports the position stop-covered with n_positions=1.
6. **Board agreement soak** (needs R4): 24h with gate0 board == live_guard stage_gate on all five criteria. Success = zero divergent rows; then delete the duplicate implementation.

## PERSPECTIVE COVERAGE TABLE

| Perspective | Verdict |
|---|---|
| Internal | COVERED — the halt/pager/refusal core is genuinely strong and was live-exercised today (WHAT WE KNOW 1-13); the launch-specific wiring is measurably broken in 6 critical places (F1-F4, F13-F15). |
| External (Tier-1 cohort) | COVERED — Jane Street/Optiver-class practice: no venue cutover without a dress rehearsal on cloned state, no gate whose inputs a change can't fail, dual-control arming (two humans or human+timer) for the arm flag, and a reconciliation that REFUSES scope-mismatched comparisons. Negative exemplars: the rail that would have stopped an Alameda-class death here is the deadman — and F14/F15 mean at cutover it would NOT fire; that is precisely the control-group lesson (the rail existed, the wiring didn't). RenTech ceiling: their equivalent of this subsystem is decades of one-book, one-truth accounting — the six-equity-number state (F13) is the furthest point from that exemplar this desk has. Grade vs cohort: rails design B+, rails WIRING D, board integrity F until R4. |
| Future (~2-3y) | COVERED — with cheap agentic compute the whole cutover becomes a continuously-rehearsed simulation: a nightly clone-and-cutover drill against a venue mock, every board line property-tested ("can this input make it fail?"), and the reality-gap chain (L2.10) auto-attributing live-vs-backtest divergence from fill one. The board should be GENERATED from the gate code (one source), not written twice. |
| Contrarian | COVERED — tested the assumption "the rails protect us": the strongest rail (deadman) protects the WRONG VENUE at cutover; the safety architecture's depth (10+ interlocking mechanisms) is itself a hazard — F5 shows safety layers amplifying (entries-freeze → full flatten → 418 ban → longer outage). Also tested "more gates = safer": three parallel gate implementations produced three different answers and TWO of them lie in the green direction (F2). The contrarian conclusion stands: fewer, single-sourced mechanisms would be SAFER than the current redundancy. |
| Greenfield | COVERED — rebuilt from validated knowledge only: ONE equity oracle module (venue-read, mode-aware, every consumer imports it — kills F13's six numbers), ONE board generated from staging.py's own checks, ONE connector factory (R5), data/live/ vs data/testnet/ artifact roots (kills the entire F16 contamination class structurally), rails unchanged (they are the validated part). Historical-baggage score: HIGH — cashcarry_state vs cashcarry_positions, three signoff artifacts, and merged tapes are all accretion, not design. |
| Frontier | COVERED — nothing newly-public changes this subsystem's math this quarter; the relevant "frontier" is internal (the desk's own drill harness pattern from 07-30 generalizes to every wired path — R6). One external item: Binance's apiRestrictions endpoint (F24) is free verification the desk doesn't consume; adopt it. |

## NEGATIVE-SPACE SWEEP (questions never asked, mandatory)

Never asked anywhere in the repo before this audit: Does any code verify key PERMISSIONS (F24)? What happens to the six equity readers ON THE DAY the deposit lands (each re-anchors differently — only partially answered by R0071a)? Who creates gate0_complete (F9)? Can the change-window fence distinguish live from testnet fills (F16)? What does the ladder do to an OPEN order at a rung transition (undrilled, F12)? Is there a venue maintenance calendar check before the launch hour? What is the funding-cycle phase at the planned first-open? Does anything cap how many times the orphan-cover circuit can fire across DAYS (per-hour only)? Documented empty seams (checked, nothing found): no hardcoded live URLs outside binance_live/binance_spot_live (`grep -rn "fapi.binance.com" --include=*.py` → only the live connector pair); no order-capable module without has_keys gating; conviction/LLM traders have zero connector references (paper-only confirmed).

## PROACTIVE BATTERY (which moves ran, what each produced)

(1) Contingency-before-failure: named the deadman's missing live replacement (R1) and the pager's unarmed second channel (F11). (2) Adjacency: the phantom-file shape (record_capital_event) was swept for siblings → found 2 more readers of the same phantom (check_gate0_ready, check_change_window); the welded-gate shape (stage_gate) → found again inside check_freeze_exit_sources (F22). (3) Config-vs-outcome: ran `record_capital_event.py --show`, read gate0_readiness.json/live_guard.json/drill artifacts rather than trusting their scripts. (4) Regression sweep: today's own fixes made two things worse — capital=200 shrank the paper evidence stream 22.5x mid-accrual (F25 tail), and the reply channel added an unauthenticated-by-design trading-control surface (ledgered, WHAT-WE-DON'T-KNOW 2). (5) Cost-inversion: apiRestrictions is a FREE probe not consumed (F24); the OpenRouter outage blocks the second family (SOLO labels). (6) Generalise-the-rule: kill-flag checks live only in callers → connector-boundary rule proposed (F20/R9). (7) Autonomy check: REARM recovery is CONFIGURED, never SEEN to work (test #2). (8) Negative space: section above. (9) Scope-the-negative-result: venue-side stops "never ran" is a ROUTE failure (testnet lacks the endpoint wrapper), not a capability failure — the live code exists and is drillable (test #5). (10) Ratchet check: the drill streak (the 8-week ramp prerequisite) has not started counting — every week it stays unwired is a week added to the earliest possible ramp-up date; the bridge (R6) is the ratchet's ignition.

## LEDGER OBLIGATIONS (owed by the synthesis seat — this audit ran READ-ONLY)

R1-R10 and test set 1-6 each owe a recommendations.py row (L2.3/L1.28b; use `--expect` for id allocation — two sessions collided on ids yesterday). The claim-verifier scope-mismatch item (WHAT WE DON'T KNOW 3) is already on the principal page; it needs a row if it lacks one. This report's findings F1-F27 are the evidence bundle; none is disposed by being written here.

## FINDINGS LOG (raw, in discovery order — synthesized into the four outputs above)

### F1 [CRITICAL] The launch-day capital-event tool reads a phantom state file — R0071a's fix is severed by a filename mismatch
- `scripts/record_capital_event.py:41` sets `_STATE = data/cashcarry_state.json`. That file DOES NOT EXIST: `ls data/cashcarry_state.json` → `No such file or directory`.
- The executor persists venue-truth equity into a DIFFERENT file: `run_cashcarry_executor.py:41` `_STATE = data/cashcarry_positions.json`, which right now holds `last_combined_equity: 8682.27` at `2026-07-31T14:10:23Z` (fresh, testnet).
- Proven by running the tool: `.venv/bin/python scripts/record_capital_event.py --show` → `inception (raw state) $0.00`, `inception (effective) $0.00`, `combined equity UNVERIFIABLE` — against a real inception of $5,000 and a real −37% breach history.
- Consequences: (a) rung 1 of the R0071a equity ladder (fresh executor persist) can never fire; (b) the ruin-breach REFUSAL in `capital_events.rebase` computes `breach_live` against `st.get("start_futures_equity", eq)` = defaulted-to-equity → 0% drawdown → the refusal designed to stop clearing a live ruin stop without capital is silently disabled; (c) `--show` misinforms the principal on launch morning.
- Grim irony: the filename bug also blocks the OTHER bug it hides — a fresh TESTNET persist would otherwise be served as "venue truth" to a LIVE capital event (rung 1 has no live/testnet discriminator). Fixing the filename without adding a live-only guard re-opens testnet→live rail contamination.
- Same phantom path is read by `scripts/check_gate0_ready.py:140` (board row `ruin_rail_clear` → permanently BLOCKED-UNKNOWN "state unreadable on this box" — wrong: this IS the box) and `scripts/check_change_window.py:96` (the sterile-cockpit fence's live-fills input).

### F2 [CRITICAL] The Gate-0 board is green on phantom evidence: 5/6 READY, "principal owes nothing" — while spot live keys do not exist
- `data/gate0_readiness.json` (written 14:09Z by the hourly cron): `keys_present: READY — "4 live-venue credential file(s)"`. The 4 matches (`_keys_present()` globs `*binance*|*api*`) are: `binance_live.json` (real, futures), `binance_spot_testnet.json` (testnet), `binance_testnet.example.json` (an EXAMPLE file), `binance_testnet.json` (testnet). **`binance_spot_live.json` is absent** — the spot leg of a cash-and-carry has no live credentials, and the board counts an example file as a live credential.
- `connector_verified: READY — "523 recorded fills"` — all 523 are TESTNET fills; zero live round-trips exist. The row's own action text ("fund the account and let the executor take one real round trip") contradicts its READY status.
- `capital_fraction_le_010: READY — "capital $200 / equity $18,676 = 1.1%"` — $18,676 is TESTNET faucet equity (via `_desk_equity_usd()` → NAV chain reading the paper book). At a realistic launch deposit ($1k–$2k), capital=$200 is 10–20%, i.e. the bar may actually FAIL — the board cannot see that because it divides by fiction.
- Meanwhile `run_live_guard.py`'s stage_gate (the gate that feeds `staging.s1_entry_met`) reports the same criteria as `principal_signoff=False, keys_present=False, connector_verified=False`. TWO parallel implementations of the same gate names, opposite verdicts, both published every few minutes. Board-vs-reality failure in its purest form.

### F3 [CRITICAL] The principal's signoff went to an artifact the real gate never reads
- `data/gate0_signoff.json` exists (signed 2026-07-30T22:28Z, "principal (Zaid)") and claims "Satisfies the principal_signoff criterion of libs/execution/staging.py:s1_entry_met".
- The actual gate evidence is built in `run_live_guard.py:239`: `"principal_signoff": bool(_load("data/stage_state.json").get("principal_signoff"))`. `data/stage_state.json` = `{"stage": "S0", "note": ...}` — no such key. The gate reads False.
- `check_gate0_ready.py:_principal_signoff()` reads the signoff file's mere EXISTENCE → READY. So the human act landed in the artifact only the cosmetic board consults; the operative gate is blind to it. On launch day this either blocks S1 mysteriously or gets "fixed" by hand-editing stage_state.json under pressure.

### F4 [CRITICAL] Drill evidence flows into a void: both consuming gates read a file nothing writes (subagent-verified, spot-checked)
- `scripts/run_drills.py` (3 drills: host-death naked clock, derisk ladder, ruin-rail re-entry; all critical) writes `data/drill_log.jsonl` (3 runs, all pass, oldest 2026-07-30 22:52 — the system is <1 day old) and `data/drill_report.json` (19/19 checks pass).
- `libs/execution/ramp_gate.py:68` reads `drill_pass_streak_weeks` from an `evidence` dict; `libs/execution/staging.py:83` reads `critical_drill_failures` (sentinel −1 = refuse). The ONLY producer of that evidence dict is `run_live_guard.py:_ramp()` reading `data/ramp_state.json` — **which does not exist**, and no code bridges drill_log/drill_report → ramp_state. `grep -rn "drill_report\|drill_log" --include=*.py .` (excl. author+tests) → empty.
- Net: `d_drill_streak_ge_8w` is permanently False (the 8-week clock has not even STARTED counting), and S2's `critical_drill_failures` reads −1 forever. The drills are a dangling producer; the gates are dangling consumers; the cron line additionally redirects stdout to a third, never-created file (`data/cro_ai_logs/drills.log`), so the scheduled path has produced zero artifacts to date (first cron fire due 2026-08-01 03:40).

### F5 [HIGH] "Freeze entries" is wired to a full-flatten: a single failed venue read can liquidate a healthy hedged book
- `run_live_guard.py:132-137`: a venue read exception → fail-closed ReconcileReport with `<unreadable>` breach beyond grace → `freeze_entries=True` → `_freeze(True,...)` writes `data/CASHCARRY_KILL`.
- The executor's KILL semantic is not "no new entries" — it is CLOSE EVERYTHING (`run_cashcarry_executor.py:1453-1476`, `_rebalance(0,0,0)`).
- So one HTTP timeout on one 5-minute guard tick = full market-order liquidation of a delta-neutral book (round-trip cost on the entire book), with no N-consecutive-failures confirmation and no distinction between "cannot see the book" and "book is naked". Today's real incident (08:35Z ladder → full flatten → mid-flatten 418 ban) proves the amplification chain end-to-end, and the 418 ban itself was likely triggered BY the flatten hammering. The guard's docstring intent ("naked >60s freezes NEW ENTRIES and pages") is unimplementable with the only halt primitive being nuclear.

### F6 [HIGH] Venue-side protective stops have never once executed: `place_stop_market` exists only in the live connector
- `grep "def place_stop_market" libs/execution/*.py` → only `binance_live.py:358`. The testnet connector the running executor imports has NO such method, and the executor guards the stop path with `hasattr(fut, "place_stop_market")` (`run_cashcarry_executor.py:993`) → silently skipped for the entire paper history.
- Therefore the first-ever real execution of the stop-placement code path is launch day, with real money, after zero rehearsals. live_guard's reconcile ("every live position must carry a venue-side stop") likewise has never seen a real position (n_positions=0 vacuously green).

### F7 [HIGH] The testnet→live connector flip has no wired mechanism in the executor
- `run_cashcarry_executor.py:28-29` imports `binance_spot_testnet as spot` / `binance_testnet as fut` at module top. `binance_live.py` is a deliberate drop-in mirror ("callers that already work against the testnet module work against this one unchanged") — but NOTHING selects between them: `grep -rn "import binance_live"` → only record_capital_event.py (equity read), run_live_guard.py (guard), sub_accounts.py (probe). No order-placing module imports it.
- So launch day requires EDITING THE EXECUTOR'S IMPORTS (or an equivalent by-hand swap) — a money-path code change on the highest-risk day, exactly what L1.38 (sterile cockpit) forbids, with no committed, tested switch to flip instead.

### F8 [MEDIUM] Paper book quietly re-sized from $4,500 to $200 today; launch sizing may be sub-viable per-name
- `data/cashcarry_config.json` (`capital: 200, top: 4`, committed bbf324a "gate 0: launch sizing... config is live-tunable") overrides the running process's `--capital 4500` (`_live_params`, config wins). Deliberate for launch — but it ALSO sizes the PAPER book now, so the forward-evidence stream (fills/day, cost-model accrual) shrank ~22x mid-stream today.
- At $200/top=4 with cap_frac=0.35, per-name notional ≈ $50-70. Binance USD-M minNotional is $5-20 per symbol but SPOT minNotional is commonly $5-10 — viable but thin; a couple of filter rejections would leave the book unable to fill its 4 names. Nobody has computed launch-sizing viability against venue minimums with the ACTUAL $200 (the drill/negative-space class: fires once, on day one).

### F9 [MEDIUM] `data/gate0_complete` — the freeze-exit gate's gate0 criterion — has no writer anywhere
- `grep -rn "gate0_complete" scripts libs` → readers only: `max_audit.py:1257` (reads), `run_cadence.py:160,214` (reads; and lists its writer as "scripts/max_audit.py", which is false — max_audit only reads it).
- `data/freeze_exit_status.json` (`met: false, why: gate0=False`) cites `criteria_sources.gate0 = ["data/gate0_complete", "scripts/max_audit.py"]` and boasts "Every criterion must read an artifact something in this repo WRITES. check_freeze_exit_sources() fences that" — the fence was satisfied by a name-match on a READER. If creating this file by hand is the intended human latch, no runbook says so (checked docs/playbooks/go_live.md — see F10).

### F10 [MEDIUM] The S1-gate-met principal page can never fire: it is guarded on `PRINCIPAL_ACTION.md` not existing, and that file is a permanently-occupied kitchen sink
- `run_live_guard.py:294-298` writes the "Gate 0 preconditions MET — your sign-off is the only remaining step" page only `if ... not _PRINCIPAL.exists()`. `data/PRINCIPAL_ACTION.md` exists continuously (it currently carries REARM instructions, A/B/C, YES/NO, purchase asks, escalations). When the mechanical gate finally goes green, the dedicated notification is unreachable.

### F11 [MEDIUM] Pager redundancy is unarmed: 33 of 63 delivery-ledger rows are `ok:false` "NOT-ARMED"
- `data/alert_delivery.jsonl`: every page logs `channel:"none", ok:false, detail:"NOT-ARMED: data/secrets/alert_channels.json absent or empty"` before the ntfy 200. The gap #38 registry channels (telegram/webhook/email) were built and never armed; ntfy + healthchecks-fail is the entire delivery surface for a launch week where every rail terminates in a page.

### F12 [HIGH] Drill negative space (subagent-verified): the failure the desk actually had TODAY has no drill
- Undrilled, each with evidence of absence from `run_drills.py`: venue 418/IP-ban mid-flatten (happened 08:35Z today; `data/BINANCE_BAN_UNTIL` exists), API-key revocation mid-session, partial-fill + connector crash, stop order rejected by venue, duplicate capital event (deposit recorded twice), equity-source disagreement, kill-switch honored mid-loop, pager delivery failure, REARM reply-channel end-to-end (the ONLY un-freeze path — `data/principal_replies.jsonl` absent = zero replies ever processed), venue maintenance window, ladder transition during open order.
- The 3 existing drills are all <24h old and all pure-function drills; nothing drills a wired path end-to-end.

### F13 [CRITICAL] The desk runs SIX simultaneous "book equity" numbers, spread across the money path (subagent-verified, spot-checked)
- Order sizing: $200 (`data/cashcarry_config.json:capital`, wins over argv). Executor ruin rail: eq_c $8,682 vs inception $5,000 (`cashcarry_positions.json`). Deadman venue truth: $6,226 / HWM $6,257 (independent, stricter — the $2,456 gap to the executor's number is `realized_spot_pnl=2930.43`, a state figure the deadman does not credit). Capacity/validation layer: **$18,675.73** — `capacity_policy.live_book_usd()` reads the NAV chain's `molded_curve_usd`, a SIMULATED curve whose own `_note` says "venue truth is the dead-man's combined_equity"; `_desk_equity_usd()` returns it to every capacity band. `research_state.json`: start 15,000/equity 18,669 (molded). Fallback: `DEFAULT_BOOK_USD = 50_000` (`capacity_policy.py:101`) when the NAV ledger is stale >7d — a $50k phantom as the loosest-possible-gate fallback.
- The L1.28a proving instance (two equity sources) was "fixed" into a LADDER — and the ladder's top rung now serves a self-disclaiming simulated number to the entire capacity/promotion apparatus, 3.0x venue truth.

### F14 [CRITICAL] At live cutover the deadman's frozen testnet baseline silently DISARMS the ruin rail
- `deadman_state.json:usdt_baseline=105,914.87` is the median faucet-stablecoin balance captured at state init and frozen. Equity formula: `fut_eq + legs_v + (usdt_now − 105,914)`. On a real account holding a few $k of stables, combined_equity ≈ **−$100k**, and `should_fire()` returns False on equity ≤ 0 (`run_deadman_switch.py:180`) — the rail can never fire. Silent disarm, not false fire — the worse direction. The required remedy ("clear data/deadman_state.json after any manual top-up") exists ONLY as a code comment (line 118); no runbook step, no cutover automation, no fence detects a testnet-scale baseline on a live-armed box.

### F15 [CRITICAL] The deadman switch is hard-pinned to TESTNET in its entirety — after cutover the Tier-3 rail watches a dead paper account
- `run_deadman_switch.py:32-35`: `_FUT_BASE = "https://testnet.binancefuture.com"  # PINNED testnet -- never live`, `_SPOT_BASE = testnet.binance.vision`, keys from `binance_testnet.json`/`binance_spot_testnet.json`. Its `_flatten()` (line 215+) places reduce-only orders against the TESTNET. Post-launch: ruin detection monitors the wrong venue (blind to live ruin; able to freeze the live book via CASHCARRY_KILL for testnet-noise reasons), and its flatten cannot touch the live book. There is no live variant and no cutover step that creates one. Combined with F14 this makes the ruin rail doubly dead on launch day. (Its `_flatten` also swallows all exceptions with bare `except: pass` — the L1.40 SILENT-EXCEPT class that stranded ~$2,150 once already.)
- Executor-state contamination is the same family: `cashcarry_positions.json` carries testnet `realized_spot_pnl=2930.43` and `start_futures_equity=5000` into any live eq_c; latent extra: `state.get("start_futures_equity", eq)` would re-base the rail to CURRENT equity if the key ever went missing.

### F16 [HIGH] The testnet fill tape greens three different LIVE gates
- `data/moat/execution_tape/cashcarry_trades.jsonl` (523 testnet fills) is the artifact read by: (a) gate0 board `connector_verified` (F2); (b) freeze-exit `fills_4wk` ("≥4 weeks of LIVE fills" — `run_cadence.py:_freeze_exit_met`, coverage measured on row timestamps of the merged tape); (c) `check_change_window.py:_n_fills` — the `<20 live fills` sterile-cockpit window would read 523 on launch morning and never engage. No design exists for separating live from testnet rows at cutover (no venue/mode field consulted by any of the three readers).

### F17 [HIGH] The go-live runbook contradicts the wiring and omits every cutover reset
- `docs/playbooks/go_live.md` claims "go-live is a CONFIG FLIP, not a rushed real-money coding session" — while `docs/LIVE_CONNECTOR_SPEC.md:10` documents the actual flip as `import binance_live as fut/spot`, i.e. EDITING THE EXECUTOR'S IMPORTS (F7), a money-path code change on launch day (L1.38's exact scenario).
- The runbook's precondition list is stale ("protective stops NOT YET BUILT as of 2026-07-18", "pager ladder NOT YET BUILT" — both built since) and it contains ZERO of the cutover resets this audit found mandatory: deadman state deletion (F14), executor state reset (F15), tape live/testnet separation (F16), `data/gate0_complete` creation (F9). `grep -rn "deadman" docs/playbooks/*.md` → no hits.
- One thing it gets RIGHT that I verified: the spot keyfile name matches the code (`binance_live_spot.json` in both runbook line 28 and `binance_spot_live.py:24`).

### F18 [HIGH] Config-loss fail-open: a corrupt/unreadable config re-sizes the live book 22.5x
- `watchdog.py:187-188` respawns the executor with hardcoded `--capital 4500`; `_live_params` "silently falls back to the argv values" on any config read error. Launch sizing lives ONLY in `cashcarry_config.json` ($200). One corrupt write/partial pull of that file → the live book silently sizes at $4,500 (≈2-4x a realistic launch deposit, gross violation of the ≤10% Gate-0 bar). The fail direction on config loss must be HOLD/de-size, never revert-to-larger-legacy-argv.

### F19 [MEDIUM-HIGH] `sub_accounts.py` wraps a live universalTransfer, violating the connector's capability whitelist one module over
- `binance_live.py:18-21` promises "no withdrawal, transfer, sub-account or key-management function and never will — those endpoints are simply never wrapped HERE". `libs/execution/sub_accounts.py:70` calls `binance_live._signed(".../sub-account/universalTransfer")` — a live money-mover using the same signing machinery, gated by is_armed + its own caps, checking NO kill flag, probed daily by cron (05:48). The whitelist-by-construction claim is defeated by import.

### F20 [MEDIUM] No kill-flag enforcement at the connector boundary
- `binance_live.py` order/transfer functions never consult CASHCARRY_KILL/DEADMAN_FIRED; the halt lives entirely in callers. Today the only mainnet caller is `run_live_guard.flatten_all` (de-risk, fine), but any future `place_market` caller that forgets the check bypasses every halt. One `if _KILL.exists(): raise` inside `_signed` POSTs would make the halt structural.

### F21 [MEDIUM] The stage gate as continuously published is welded in both directions
- `run_live_guard.py:234-241` promo_evidence feeds `staging.s1_entry_met` with `capital_fraction = ramp size_fraction` (the 0.1 ramp step constant — always exactly at the ≤0.10 bar; the criterion can never fail, and it measures the wrong quantity entirely) and never injects `symbol_count` (defaults 0 — the criterion can never pass). Observed in `data/live_guard.json`: `capital_fraction_le_010=True` with everything else False, every tick. A gate with an always-true check and an always-false check carries zero information (gate-optimality duty); launch will bypass it via a hand-assembled `staging.promote(evidence)` call, leaving the published gate decorative.

### F22 [MEDIUM] `check_freeze_exit_sources()` — the anti-phantom fence — self-greens by checking that the WRITER FILE EXISTS, not that it writes
- `run_cadence.py:169-183` validates each freeze-exit criterion's writer by `(_ROOT_DIR / writer).exists()`. For gate0 the declared writer is `scripts/max_audit.py`, which EXISTS but only READS `gate0_complete` (max_audit.py:1257). The fence built on 2026-07-30 to kill the phantom-artifact class contains the phantom-artifact class. `data/gate0_complete` remains writer-less (F9); the freeze-exit gate remains mechanically unsatisfiable, indistinguishable from "not yet earned".

### F23 [MEDIUM] The arm switch's two-flag separation collapses to one keystroke in the setup script
- `deploy/finish_setup.sh:84-93` (step 6): a single interactive `[y/n]` ("Is THIS host the durable production box?") touches BOTH `data/LIVE_VPS_VERIFIED` AND `data/LIVE_ENABLE`. The runbook and connector docstring treat these as deliberately separate decisions ("keys exist" ≠ "trading is armed" ≠ "host is durable"). Human-gated (not auto-arm — verified: the script is only ever invoked manually), but the separation the design paid for is one hurried keypress wide.

### F24 [MEDIUM] Nothing verifies placed live keys are actually trade-only/withdrawal-disabled/IP-whitelisted
- The runbook DEMANDS those key properties; `grep -rn "apiRestrictions" libs scripts` → nothing. Binance exposes `GET /sapi/v1/account/apiRestrictions`. A key created with withdrawal enabled, or without IP whitelist, passes every gate the desk has. One read-only probe at key placement (and daily thereafter) closes it.

### F25 [LOW-MEDIUM] Launch sizing was never checked against per-symbol venue minimums, and no minNotional pre-check exists anywhere
- `grep -n "minNotional\|MIN_NOTIONAL" run_cashcarry_executor.py + all four connectors` → zero hits. At $200/top=4 (per-name ≈ $50-70): most USD-M perps clear their 5-20 USDT minimums, but e.g. BTCUSDT futures minNotional is 100 USDT — a top-4 funding rank for such a symbol means the futures leg REJECTS while the spot leg (5-10 USDT min) can fill: the one-legged-fill class (`TOPUP-FAIL ... verify manually`, the 2026-07-19 $2,150 stranding family), on day one. Also: the same config commit silently shrank the PAPER book's evidence stream 22.5x mid-Gate-0-accrual (fills/day, cost-model refresh now accrue at $200 scale).

### F26 [LOW] BINANCE_BAN_UNTIL guards only the data path
- Written/read by `libs/data/crypto_source.py` only; `binance_live._signed` never consults it. Executor's KILL loop absorbs venue errors calmly (fixed today), so severity is low, but a live 418 would still be hammered by any non-executor caller.

### F27 [OBSERVATION] `data/FREEZE` (watchdog full-stop flag, operator-placed, no code writer) reaps ALL supervised processes INCLUDING the deadman rail with no respawn
- `watchdog.py:150,170`. Full-desk stop semantics include stopping the independent ruin watcher while positions may exist. Correct for host evacuation; worth one runbook line so it is never used as a "pause trading" lever.
