# WEEKLY DEEP COLD AUDIT — DATA-MOAT SUBSYSTEM — 2026-07-29

_Auditor: weekly deep cold sweep (data-moat seat), READ-ONLY. Third attempt this cycle: the
07-28 run was cut off after section 1; the 07-29 01:16 run died at model auth with 0 report
bytes. This run re-verified the delta since the last COMPLETE report (2026-07-26) and finished
the sections the dead runs never reached. Scope: NOT "what data do we have" but "what
information can only exist because of how WE combine data" — proprietary transformations,
composed/hierarchical features, graph representations, embeddings, event reconstruction,
market-state fingerprints, research memory, experiment lineage, feature ancestry, alpha
genealogy. Method: OUTCOME-NOT-CONFIG — every claim carries its proving command + output._

**Status: COMPLETE.**

## SCORES

- **current_capability_pct: 62%** (was 45% on 07-26). The combination-layer charter gaps named
  then are closing measurably: novelty gate LIVE with logged verdicts, tape screening STARTED
  (1/270 constructions), a causal graph + alpha genome runs daily, and the usage side of the
  moat — 07-26's top unknown — is now MEASURED (module_justification, unobserved_observables).
  Still missing: unified trial accounting, lineage fields unexercised, interactions/events
  absent, regime history still destroyed daily, screen-kill permanence leak (new, U7).
- **practical_ceiling_estimate: 85%** of what a solo-operator free-first desk sustains
  (unchanged — the ceiling is organizational triage bandwidth). **ceiling_gap: 23 pts.**
- **opportunity_cost_1y:** MED-HIGH, now partly QUANTIFIED: 84 already-computed candidates sit
  unadjudicated (one-time option, low prior — §2 U1); 269/270 tape constructions unscreened at
  a measured ~6 verdicts/day (≈45 days if sustained — acceptable IF sustained); regime labels
  destroyed for a 3rd straight sweep; and the trial-count integrity gap taxes every future DSR
  denominator (compounding cost, not one-time).
- **confidence: 0.85** on findings (every one command-verified today), **0.6** on ranking.
- **unknown_unknown_score: 0.30** (down from 0.40): store contents AND store usage are now both
  mapped; the residual U-U mass is prompt-organ behavior (invisible to code audit) and
  cross-store consistency (graveyard vs NK vs memory vs candidate rows never reconciled).
- **info_gain_if_investigated: HIGH** for E1 (84 re-adjudication) — a permanent number
  whichever way it lands, and it directly informs the pending R0033 principal ruling.
- **expected_alpha_contribution: MED direct** (84 re-adjudication is cheap but low-prior; the
  269 remaining tape cells and the untested interaction family are the real surface).
- **expected_compounding_contribution: HIGH** — M1/M2 (candidate-aware gates + one trial
  ledger) multiply the value of every future campaign by making its accounting honest and its
  gate informative; M4 (screen-kill → graveyard flow) makes the novelty gate self-sustaining.
- **CEILING EXPANSION:** the 85% remains organizational, not technological. Two lifts: (i) the
  stepwise gate migration itself — a gauntlet that CAN pass a candidate makes discovery
  throughput meaningful where today's measured pass-rate is 0.0% (north_star:
  validated_alpha_discovery_rate 0.0); (ii) retrieval/embeddings over research memory stays
  correctly deferred until ~1k rows (now 140) or a second concurrent consumer of recall.

---

## 0. DELTA SINCE 2026-07-26 — recommendation-by-recommendation outcome check

The 07-26 report ranked O1–O7 and specced E1–E5. Outcome audit (artifact, not intention):

- **O1 tape screen — STARTED (the biggest win).** `data/knowledge_engine.json` genome A004
  "liquidity withdrawal (moat)": lifecycle DECONTAMINATED, "1 of 270 constructions tested";
  causal edge recorded honestly: "liquidity withdrawn → volatility rises: REFUTED_AS_LEAD, raw
  rho +0.303 → residual +0.015 (t +0.28) after orthogonalising to same-period RV" — the first
  tape construction was a vol-clustering artifact and the decontamination gate caught it.
  `data/research_cio.json` (fresh 02:35) schedules 40 depth/replenish_rate cells under
  M_LIQUIDITY_WITHDRAWAL at priority 1.022 (its #1 blind spot, coverage 0.004), and
  `scripts/stage_a_executor.py` RUNS the queue daily: `data/stage_a_verdicts.jsonl` = 38
  append-only verdict rows (32 on 07-28, 6 on 07-29, latest 2026-07-29T02:35). Commit
  0d482b0 "UTILISATION CLOSED END-TO-END: Stage-A executor RUNS the queue".
- **O2 novelty gate — LANDED (advisory, as designed).** `data/idle_axis_screen.json` carries
  per-candidate novelty verdicts (3 candidates, novelty_score 0.865–0.881, n_priors 46,
  is_redundant false); `scripts/screen_idle_axes.py:_graveyard_priors()` compiles priors from
  docs/graveyard.md rows + a live-sleeve roster at screen time;
  `scripts/screen_fred_macro_axis.py` imports the same builder and adds pre-registration-card
  priors (DRY, not duplicated). Residual gap: priors = the graveyard DOC + 4 hand-coded
  entries; the 420 machine-readable candidate rows and 140 research_memory rows are NOT in the
  prior set, and there is no canonical `data/graveyard_priors.json` artifact (see U7 for why
  this matters structurally).
- **O3 interaction screen — NOT DONE.** `grep -rln interaction libs/research libs/features`
  → only audit/sweep prompt text, no implementation. Carried.
- **O4 regime-label history — NOT DONE, THIRD CONSECUTIVE REPORT.**
  `ls data/crypto_regime_history.jsonl` → does not exist; `data/crypto_regime.json` is a 293-
  byte snapshot overwritten daily (mtime 2026-07-29 00:03). Aggravating factor found today:
  SEVEN live consumers act on this label (`grep -rln crypto_regime libs scripts` →
  run_crypto_portfolio, run_regime_allocation, run_regime_engine, run_crypto_testnet,
  knowledge_engine, blindspot_max, classify_regime) while its history is unrecoverable, and
  today's snapshot again shows rule-vs-HMM DISAGREEMENT (trend:bear vs hmm bull/low_vol,
  hmm_gmm_agree=false) — the disagreement stream itself is an unrecorded dataset. The sibling
  data-intelligence sweep flags the same gap (its negative-space #5, register row R0006 open
  2.8d). A 3-line append fix has now outlived three audits.
- **O5 event-study harness — NOT DONE.** `grep -rln event_study libs scripts` → nothing. Carried.
- **O6 trials-ledger decision — NOT DONE AS SPECCED, then OVERTAKEN by a sharper live finding
  (see §1 K12, §2 U1–U2).** `trials_ledger` still 0 rows in all six databases (python sqlite
  loop over sor_research/alpha_registry/sor_crypto/sor_autodiscovery/lake/lake_v2);
  `libs/validation/gauntlet.py:89` still falls back to n_trials=2.
- **O7 v1 queue hygiene — NOT DONE.** sor_research.sqlite campaigns: done=7, leased=1,
  **queued=340** (frozen since 2026-06-22). Carried as a low-rank hygiene item.
- **E5 alignment sentinel — PARTIAL.** `data/data_vitals.json` (fresh 02:35) scores every
  collector with a temporal_alignment DQS component and n_dead=8 below threshold — collector-
  level health exists; the specific composed-series skew monitor (FX-age vs crypto-close age on
  kimchi/cny) still does not.
- **Beyond the recommendations, the desk built the moat's MEASUREMENT layer in 3 days** (none
  of it asked for by the 07-26 sweep, all of it on-charter): knowledge_engine (causal edges +
  genome + revival triggers), research_cio (north star + information-advantage scores + blind
  spots + construction schedule), feature_library (447-cell grammar), conversion_engine,
  stage_a_executor, module_justification (per-module merit verdicts), unobserved_observables
  (fields owned but never read), data_vitals (DQS), dependency_graph. 07-26's U7 ("the usage
  side of the moat is unmapped") is now ANSWERED with numbers: tally {CRITICAL 71, KEEP 39,
  SCAR_TISSUE 44, DUPLICATE 13, INERT 87, PREMATURE 6} and n_unread=70 fields
  (`data/module_justification.json`, `data/unobserved_observables.json`, both fresh 02:35).

## 1. WHAT WE KNOW — validated strengths (each with proving command)

**K1. The graveyard's negative-knowledge moat deepened materially this week.** 43 table rows
(`grep -c "^| " docs/graveyard.md`), with five research commits since 07-26 adding the entire
trader-skill/copytrading family kill (d5c40f9, 67c3953, 1b1b70d, 9c65eb8, 92975da): pre-
registered gapped controls on 8k+ Hyperliquid traders, a 229-trader multi-year on-chain
Sharpe→forward test (rho −0.019, t −0.28), self-caught design flaws documented mid-experiment,
structural walls recorded (userFills 2000-cap), and do-not-retest-without conditions attached.
"Selecting past winners selects luck", proven on cryptographically-verifiable records with
losers included — negative knowledge of a quality that costs real compute to replicate.

**K2. Machine-actionable negative knowledge with evaluated revival triggers.**
`data/negative_knowledge.json` fresh 02:35, 43 records; knowledge_engine revival watchlist
carries per-kill triggers with live status ("micro-cap funding carry: trigger = measured
round-trip < funding×periods, check: cost_model, status WAITING").

**K3. A living causal graph + alpha genome runs daily and is epistemically honest.**
`data/knowledge_engine.json` (02:35): corpus_size 213, 9 causal edges each carrying an
epistemic state with evidence — including self-critical states: "liquidity withdrawn →
volatility rises: REFUTED_AS_LEAD"; "inventory risk binds → liquidity withdrawn: UNTESTED, 1
of 270 constructions tested". Genome tracks 4 alphas with lifecycle + blocker ("A002 OI/LS:
FORWARD_REGISTERED, verdict due 2026-08-07").

**K4. Research memory is alive and captured this week's biggest finding within hours.**
`SELECT COUNT(*), MAX(created_at) FROM research_memory` → 140 rows, latest
2026-07-29T06:17 — and that latest row IS the gate-constant discovery ("The autodiscovery
gauntlet's pbo and reality_check gates are campaign constants…"). The memory organ recorded
the discovery about the desk's own instruments the morning it was made.

**K5. The feature library grammar is live and unblocked.** `data/feature_library.json`
(02:35): 9 registered features, 447 grammar proposals (OBSERVABLE × TRANSFORM × WINDOW ×
NORM), blocked=0, computed_unused=5 — it knows which of its own features nothing consumes.

**K6. Experiment lineage is git-anchored and growing.** `wc -l data/experiment_registry.jsonl`
→ 413 rows (385 on 07-28), each with commit hash, decision, declared-vs-present artifacts
(latest row: artifacts_present 15/15).

**K7. The utilisation loop is CLOSED for the first time: scheduler → executor → append-only
verdict stream.** research_cio ranks blind spots by information-advantage × coverage;
stage_a_executor consumes the schedule; verdicts append to `data/stage_a_verdicts.jsonl` (38
rows, ran today 02:35, latest verdict "oi_ls_history.jsonl:oi_value SCREEN-WEAK n=599"). This
is the organ 07-26 said didn't exist (its O1 asked for a one-off script; the desk built the
general engine instead).

**K8. The novelty gate produces logged verdicts in production screens** (evidence in §0 O2).
The universal-duty requirement "screen against the graveyard before spending compute" now has
artifacts, not just doctrine.

**K9. The desk measures its own dead knowledge.** module_justification: 87 INERT + 13
DUPLICATE of 260 modules, re-scored daily; unobserved_observables: 70 owned-but-never-read
fields named per file+field with code_references=0 each. Knowing what you built that does
nothing is itself moat-layer metadata few shops keep, and it re-scores nightly.

**K10. The proprietary tape keeps compounding and is now ranked the desk's #1 information
advantage by its own scoring.** `du -sh data/moat/*` → bybit 4.3G (2.8G on 07-26), fut 741M,
spot 381M; 558 files already today (`find data/moat -type f -newermt 2026-07-29 | wc -l`);
research_cio information_advantage[0] = data/moat, score 1.026, "snapshots at OUR timestamps;
nobody else has these."

**K11. Stage-B forward clocks are honest to the point of self-harm — which is exactly the
integrity the moat needs.** `data/axis_shadow_state.json`: 4 clocks ACCRUING under Holm bar
2.24 (m_concurrent=4), and the desk's flagship composed axis is currently reported LOSING:
kimchi_premium day 7/40, cum_return −3.0%, ann_sharpe −6.07, nw_t −0.78. No massaging, P&L
from the clock's first row, "eligibility != deployment" in the artifact itself.

**K12. NEW NUMBER (this audit's contribution): of the 420-candidate campaign, exactly 84
candidates (20%) were rejected by the multiplicity-family gates ALONE.** Computed from
`sor_crypto.sqlite research_candidates.rejection_reason`: all 420 failed dsr+pbo+reality_check
(confirming those gates acted as campaign constants — the uncommitted
`libs/validation/stepwise.py` measured campaign PBO 0.6159 and White RC p 0.4220, which alone
force 420/420); 336 ALSO failed ≥1 per-candidate gate (walk_forward 244, fragility 203, cpcv
195, capacity 182, expected_value 171) and remain honestly refuted; **84 passed all five
per-candidate evidence gates** (no short-circuit — rejection strings list full gate sets) and
died solely on statistics now proven uninformative per-candidate. Their profile: family
{trend 34, momentum 34, vol_expansion 9, mean_reversion 5, cross_asset 2}, in-sample
annual_sharpe median 2.67, **oos_sharpe median 0.02, max 0.09** — so the recovery prior is
LOW; the correction is to the ACCOUNTING (84 move from "refuted" to "never validly tested"),
which is precisely the SCREEN-WEAK vs SCREEN-UNDERPOWERED distinction the desk already
enforces at Stage A but not, until now, at the gauntlet.

## 2. WHAT WE DON'T KNOW — ignorance ledger

**U1. Whether any of the 84 unadjudicated candidates survives a valid per-candidate
multiplicity test.** The instruments exist as of this morning (stepwise.py: cscv_candidate_pbo,
romano_wolf_stepdown — uncommitted, tests written, production flip principal-gated per R0033
in `docs/research/recommendation_ledger.json`). Expectation from the OOS profile: near-zero
survivors — but "expected zero" and "measured zero" are different assets, and the desk's
most-cited fact ("420/0") currently overstates its own evidence by 84 candidates.

**U2. The desk cannot state its own cumulative trial count.** Trial accounting is fragmented
across: an OVERWRITTEN artifact (`scripts/screen_idle_axes.py:510` → `OUT.write_text` —
yesterday's 23 DSR-counted cells vanish from the structured record on the next run), the
append-only stage_a_verdicts.jsonl (38 rows, correct pattern), experiment_registry (413 rows
but `grep -c idle_axis` → 0 — cells not registered), and trials_ledger (0 rows × 6 DBs, while
`libs/store/trials.py` still claims "Nothing is validated without a ledger entry first" and
gauntlet falls back to n_trials=2). Nobody can answer "what N feeds the next DSR?" with a
query today.

**U3. Lineage fields have never been exercised.** research_memory.predecessor_id: 0 of 140
rows non-null. Alpha ancestry exists only as prose in the KE genome. Whether chain-lineage
would ever be QUERIED (vs merely stored) is honestly unknown — which is why the pilot in E5
is sized at one construction family, not a platform.

**U4. Whether the 270-construction tape sweep will complete before attention moves.** Measured
rate: 32 verdicts day 1, 6 on day 2 (timestamps in stage_a_verdicts.jsonl). At 6/day the
M_LIQUIDITY_WITHDRAWAL family alone completes in ~45 days. No completion target or rate floor
exists anywhere; the CIO re-ranks nightly, so a new shinier blind spot could silently starve
the tape sweep — the exact data-paralysis shape max_audit already fired once on this dataset.

**U5. Composed-series alignment drift remains unmonitored between screens** (partial DQS
coverage only — §0 E5). The kimchi clock is LIVE (K11) and its forward P&L realness depends on
FX-vs-crypto timestamp alignment staying sound day after day; a silent FX-staleness regression
would flow straight into promotion evidence.

**U6. The regime-disagreement stream** (today: rule bear vs HMM bull) has never been recorded,
so its information content — is disagreement itself a regime signal? — is unknowable until O4
finally lands (three sweeps and counting).

**U7. NEW — screen-kill permanence leak.** The idle-axis screen's powered SCREEN-WEAK verdicts
on onchain_activity (deep 2010-history variants, powered=True) appear in NO permanent negative
store: `grep -c onchain docs/graveyard.md` → 0; `'onchain_activity' in negative_knowledge.json`
→ False; nothing but the overwritable idle_axis_screen.json + one research_memory prose row.
And because novelty-gate priors compile from graveyard.md, a kill that skips the graveyard is
INVISIBLE to the gate — the screen's author hand-patched this by hard-coding
"collector:onchain_activity_throughput" as a prior in the script, which works for these 3 axes
and scales to nothing. The new screen fleet produces kills faster than the permanent stores
ingest them; the moat leaks exactly at its newest seam.

**U8. information_value.jsonl remains capture-without-meaning:** 810 rows, 810 empty `lesson`
fields (was flagged 07-26 at fewer rows; the pipe still grows without ever writing the field
that would make it knowledge).

**U9. One Stage-B slot may be idle by accident.** Commit fbedf2b derived concurrent-clock
capacity = 5; axis_shadow_state shows 4 clocks. Whether the empty 5th slot is deliberate
sequencing or an unnoticed clock-saturation gap (a reported DEFECT under the saturation duty)
is not recorded anywhere I could find.

## 3. WHAT COULD MATTER MOST — ranked opportunities

Ranking = expected impact × confidence / (cost × maintenance). ◆ = compounding multiplier.

**M1 ◆ — Land the candidate-aware multiplicity migration and adjudicate the 84.** [rank 1]
- What: principal rules on R0033 (already paged); commit stepwise.py + the validation.py
  wiring (diff exists, 99 lines, thresholds unchanged); then re-run stored candidate returns
  through cscv_candidate_pbo + romano_wolf_stepdown and write each of the 84 a per-candidate
  verdict row.
- Why: the gate-optimality duty's named defect, measured: two gates with zero per-candidate
  information decided 20% of the desk's flagship negative campaign. Every FUTURE campaign
  inherits the fix — today's gauntlet pass-rate is a structural 0.0%
  (research_cio north_star: 385 experiments, 15 survived screening, forward_tested 0,
  validated_alpha_discovery_rate 0.0), so objective #2's denominator is being spent into a
  gate that cannot pay out.
- Evidence: §1 K12 histogram; stepwise.py docstring's synthetic test ("same two gates pass
  EVERY pure-noise candidate" when the campaign contains one strong winner — too strict AND
  too loose, decided by the batch).
- Complexity: LOW (code exists; the re-run is stored-returns arithmetic). Dependencies:
  principal ruling (constitution pt 5 — correctly not self-applied). Validation: Romano-Wolf
  FWER at same alpha; report survivors (expected ≈0) + per-candidate p landscape.
- Failure modes: over-correction narrative ("price space lives!") — guard: 336/420 stay
  refuted on per-candidate evidence and the 84's OOS median is 0.02; any survivor earns a
  FORWARD CLOCK only, never capital (two-stage law unchanged).
- ROI: high (permanent gate integrity + possible free recoveries). Confidence: 0.9.
  Maintenance: none beyond normal gauntlet upkeep. Monitoring: per-gate accept/reject
  histogram each campaign (the E4 view). Retirement: when the 84 all carry verdicts.
- Horizons: 1w = ruling + commit; 1m = re-adjudication done; 1y = every campaign's gate
  informative; 3y = the negative-knowledge moat rests entirely on valid instruments.

**M2 ◆ — ONE append-only trial ledger for the whole screen fleet.** [rank 2]
- What: every stage_a_screen call (idle/fred/executor/digger paths) appends one row
  {ts, name, axis, construction, horizon, n_eff, ic, verdict, powered, artifact_path} to a
  single ledger (stage_a_verdicts.jsonl already has the right shape — generalize it), and
  either trials_ledger starts receiving gauntlet rows or its docstring claim is deleted and
  gauntlet's n_trials=2 fallback becomes fail-loud (O6's original either/or, now urgent
  because the fleet GREW: 23 idle cells + 38 executor verdicts + fred cells this week alone).
- Why: U2 — multiplicity is the desk's scarcest statistical resource and its accounting
  currently cannot survive a file overwrite. Also makes the §33 trial-logging duty auditable
  in one query.
- Complexity: LOW. Validation: "SELECT count(*) per family" answers the next DSR's N.
  Failure modes: double-counting across paths → the name field is already unique per cell.
  ROI: compounding. Confidence: 0.85. Retirement: never (it's an archive).

**M3 — Finish the tape sweep at a stated rate, with starvation alarm.** [rank 3]
- What: a completion target for the 270 M_LIQUIDITY_WITHDRAWAL constructions (e.g. ≥5
  verdicts/day floor; alarm line in the daily cycle report if the day's stage_a_verdicts
  append count = 0 while the schedule is non-empty).
- Why: U4 — the desk's #1-scored information advantage finally has a screening path; the only
  failure mode left is silent starvation, which is the desk's oldest leak shape.
- Complexity: TRIVIAL (a counter + one report line). Confidence: 0.8. Retirement: when
  270/270 carry verdicts; the alarm generalizes to whatever family the CIO schedules next.

**M4 ◆ — Automatic screen-kill → graveyard/NK ingestion (close the permanence leak).** [rank 4]
- What: a small post-screen hook: every powered SCREEN-WEAK/TIMING-ARTIFACT verdict appends a
  graveyard table row (name, verdict, tag, mechanism-of-death from the screen record) and an
  NK record; novelty priors then inherit it for free (they compile from graveyard.md).
- Why: U7 — the moat's newest seam leaks; hand-patched priors don't scale; §33 says a kill
  without its graveyard entry is not a kill.
- Complexity: LOW. Validation: re-run `_graveyard_priors()` count before/after — onchain
  kills appear without hand-coding. Failure modes: graveyard noise from underpowered cells →
  ingest POWERED negatives only (SCREEN-UNDERPOWERED stays out, per existing doctrine).
  Confidence: 0.85. Maintenance: near-zero.

**M5 — Regime-label history append.** [rank 5 by cost-adjusted value; rank 1 by shame]
- Third consecutive sweep. 3 lines in classify_regime.py + optional deterministic backfill.
  Seven live consumers act on a label whose history is destroyed nightly, while rule-vs-HMM
  disagreement (live today) is an unrecorded candidate dataset. Escalation: this now belongs
  in the carry-over brief as a [SKIPPED]-class item — a live cycle must either do it or write
  WHY not (register row R0006 is already open 2.8d).

**M6 — Lineage pilot: first predecessor chain.** [rank 6]
- When the tape sweep iterates a construction (it will — 270 cells are variations), the
  research_memory row for iteration N carries predecessor_id of iteration N-1. One family,
  one chain, then judge whether anyone ever queries it before building more. Closes U3 with
  evidence either way. Complexity: TRIVIAL.

**M7 — Mechanism-priored interaction screen** (carried from 07-26 O3, still the cheapest
untested composition family; now cheaper: feature_library's grammar + stage_a_executor can
enumerate and run pair cells natively). **M8 — event-study harness** (carried, O5; unlock/
listing/liquidation event data still idle). Both remain right and remain undone; re-ranked
below M1–M4 because this week proved the desk's scarce resource is conversion attention, and
M1/M2/M4 protect the integrity of everything the fleet already produces.

**Opportunity cost of NOT acting, 1y:** M1 unfixed = every campaign's gate stays a 0%-pass
constant and objective #2's throughput buys nothing bankable; M2 unfixed = the next
overwrite/re-run silently corrupts the DSR denominator for all concurrent candidates; M4
unfixed = the novelty gate decays as unrecorded kills accumulate (re-testing dead ground burns
shared budget exactly as the universal duty warns); M5 unfixed = a 4th sweep writes this
paragraph again with another ~90 labels gone.

## 4. WHAT WE TEST NEXT — concrete experiments

**E1 — Adjudicate the 84 (executes M1's second half).** Input: stored candidate returns from
the 420 campaign. Run cscv_candidate_pbo + romano_wolf_stepdown at unchanged thresholds
(PBO 0.5 / alpha 0.05 FWER). Success: 84/84 carry per-candidate verdicts logged as trials;
survivors (expected 0–2) get pre-registered forward clocks, NOT capital; the graveyard's
420-campaign summary line is amended to "336 refuted per-candidate + N re-adjudicated".
Validation: the synthetic-winner test in tests/validation/test_stepwise.py must pass in CI
first. Retirement: verdict delivery = done, permanently.

**E2 — Trial-accounting unification probe (executes M2).** After wiring, one query must
answer: total Stage-A cells ever, by family, by month, and the N handed to the last gauntlet
run. Success criterion: the number from the query equals the number reconstructed by hand from
artifacts (I reconstructed 23+38+3-axis cells this audit — that hand-count is the fixture).
Failure mode to test: re-running screen_idle_axes must ADD rows, not replace history.

**E3 — Tape-sweep completion clock (executes M3).** Success: cycle report carries
"tape sweep: X/270, rate r/day, ETA"; alarm fires on a zero-append day with non-empty
schedule (test by fixture). Retirement: 270/270.

**E4 — Screen-kill ingestion round-trip (executes M4).** Take yesterday's two powered
SCREEN-WEAK onchain cells; run the hook; success = graveyard row + NK record + novelty-prior
count increments, no hand-coding; then delete the hand-coded prior from screen_idle_axes.py
and verify the gate still flags an onchain re-proposal as redundant.

**E5 — Regime history + disagreement stream (M5; unchanged spec from 07-26 E3, now with a
deadline owner).** Success: crypto_regime_history.jsonl exists and grows daily; first
consumer = disagreement-rate table in the cycle report; backfill from bars proves the engine
deterministic. Retirement: never (archive).

## PERSPECTIVE COVERAGE LOG (six mandatory + negative space + battery)

- **INTERNAL (measured, not configured):** §0 delta audit — 2 of 7 recommendations landed, 1
  partial, 4 undone (one at third strike); the new measurement layer (module_justification 87
  INERT, unobserved_observables 70 fields) turned 07-26's top unknown into numbers; K12's
  84/336 split computed from raw rows; forward clocks report their flagship axis losing
  (kimchi −3.0% @ day 7) — the desk does not flatter itself.
- **EXTERNAL (how a world-class team improves this):** Romano-Wolf/CSCV per-candidate FWER is
  textbook at any serious stat-arb shop — the desk arrived late but arrived by AUDITING ITS
  OWN GATE, which most shops never do; the remaining external gap is the unified trial ledger
  (M2) and standing interaction screens (M7): both are pod-shop table stakes. A pod shop
  would also never let a #1-ranked proprietary dataset sit at 1/270 screened — they'd staff
  the sweep to completion (M3).
- **FUTURE (2-3y):** with local embeddings + agentic replay, the winning asset is the
  SUBSTRATE being captured now: multi-year tape at own timestamps, causal edges with epistemic
  states, kills with mechanisms, append-only verdict streams. All five capture disciplines
  now run; the deferred retrieval layer stays correctly deferred (lifting condition: ~1k
  memory rows, now 140, or a second concurrent recall consumer). Nothing to buy today.
- **CONTRARIAN (core assumptions tested):** (i) "420/0 proves price-family is dead" — PARTLY
  GATE ARTIFACT, now measured: 84 were never validly adjudicated; the conclusion probably
  survives (OOS median 0.02) but currently overstates its evidence — this is the sweep's
  central contrarian finding and it came from testing the desk's most-repeated sentence.
  (ii) "Composition is the edge" — still n=1 validated and that n=1 is currently LOSING its
  forward test (kimchi −3.0%, day 7/40); honest posture stays "best-evidenced class", not law.
  (iii) "More enforcement organs = more integrity" — contrarian caution: the desk added ~9
  meta-organs in 3 days while 4 object-level recommendations (regime history, interactions,
  events, queue hygiene) sat undone; meta-work is compounding only if object-level conversion
  doesn't starve — the §33 backlog discipline exists precisely for this, and this report's
  M-ranking deliberately puts object-level integrity (M1–M4) above any new meta-organ.
- **GREENFIELD (rebuild with only validated knowledge):** keep — axis_screen harness (now
  with the powered-both-branches fix), append-only verdict streams, KE causal graph + genome,
  CIO scheduler, conversion ledgers, graveyard-with-mechanism. Would NOT rebuild: 22-table
  schema × 8 sqlite files with trials_ledger empty in all (one research DB would do), the
  v1 campaign queue (340 frozen rows), dual doc/JSON negative stores requiring manual sync
  (M4 exists because of this split). Baggage score: moderate and shrinking; the new organs
  all went append-only JSONL, which is the right greenfield instinct.
- **FRONTIER (recently publicly possible):** the frontier item this week is INTERNAL
  adoption of 2005/2014-era methods (Romano-Wolf stepdown, CSCV) — correctly prioritized over
  novelty. Checked for newer: no paper/repo/API surfaced this sweep that beats finishing
  stepwise + the tape sweep; DuckDB over the jsonl estate (07-26 suggestion) remains unadopted
  and remains merely-nice (this audit's cross-store queries took minutes in stdlib sqlite3 —
  the pain threshold that would justify it has not been hit). Recorded as an honest
  nothing-new-found beyond these.
- **NEGATIVE-SPACE SWEEP (never built/asked, verified absent today):** interaction engine
  (grep §0 O3), event studies (§0 O5), regime history (§0 O4), composed-series skew sentinel
  (§0 E5), predecessor chains (U3: 0/140), information_value lessons (U8: 810/810 empty),
  frontier profiles beyond CN (`ls data/frontier_profiles` → CN.json only — carried for the
  digger charters, outside this seat), embeddings/retrieval (deferred with explicit lifting
  condition), a cumulative-trial query (U2), a clock-saturation record for the 5th Stage-B
  slot (U9).
- **PROACTIVE BATTERY moves run:** (2) ADJACENCY — the research_memory null-pipe shape
  re-swept: trials_ledger still 0×6, predecessor_id 0/140, info_value lessons 0/810, plus the
  NEW instance: overwritten screen artifacts (U2) — one shape, four live instances; (3)
  CONFIG-VS-OUTCOME — every organ verified by artifact mtime + row count; caught
  idle_axis_screen.json as overwrite-not-archive and the SCREEN-INTERESTING verdict standing
  in an artifact the fixed code would no longer produce (axis_screen.py:144-160 documents the
  self-caught fix; no clock was started on the bad verdict — verified against
  axis_shadow_state, try_premium absent); (4) REGRESSION SWEEP — what the stepwise change
  makes worse: campaign-level statistics stop gating, so a wholesale-overfit SEARCH PROCEDURE
  is now flagged only diagnostically — mitigated because they remain computed and reported;
  stated in validation.py's own docstring; (5) COST INVERSION — nothing paid in this
  subsystem; the entire moat layer runs on self-recorded + free data; (8) NEGATIVE SPACE —
  list above; (9) SCOPE THE NEGATIVE RESULT — kimchi's negative forward week is a ROUTE
  result (7 of 40 days), not a capability verdict; do NOT scope it up; conversely the 84 are
  an INSTRUMENT failure, not an alpha discovery — do not scope it up either; (10) RATCHET
  CHECK — conversion/coverage records ratchet correctly; regime snapshot STILL has no floor
  (third strike, M5); the new stage_a_verdicts stream is append-only (has a floor by
  construction). Moves 1, 6, 7 ran and produced nothing beyond the above; reported as such.

## CROSS-SUBSYSTEM HANDOFFS

- The stepwise/R0033 ruling and E1 re-adjudication belong to validation-stats; this report
  contributes the 84/336 blast-radius split and the OOS profile (recovery prior low).
- The 8 dead collectors (data_vitals n_dead=8, coinmetrics DQS 0.0) and the cny clock's 0
  aligned forward days belong to data-intelligence (its kill/root-cause thread already covers
  the cny flatline).
- U9 (5th Stage-B slot) belongs to the research-engine seat's clock-saturation duty.

## §33/§41 NOTE

This audit ran READ-ONLY (per its charter): no ledgers written, no state touched, report file
only. Its items M1–M8 and experiments E1–E5 are carded output owed register/ledger rows by the
next live cycle (`scripts/recommendations.py add`, source deep_sweep-2026-07-29-data-moat) —
same handoff convention as the sibling data-intelligence sweep, whose finding #8 (sweep→ledger
handoffs get dropped) applies to this very sentence: if the next two live cycles do not row
these items, that is the defect to report, by name. Note M1 is ALREADY partially rowed (R0033,
raised 06:17 today by the live cycle) — the ruling, the commit, and E1 remain open; M5 is
already rowed as R0006 (open 2.8d, third-sweep repeat).
