# VPS runtime truth, read 2026-08-05 — two clocks, and a correction

Read live off the box (95.216.191.70, `/home/quant/quant-platform`, branch
`claude/llm-auto-upgrade-verify-gcjac3`). The research container cannot see VPS runtime state,
and today it was wrong twice in a row because of that — both corrections are recorded here
rather than quietly fixed.

## Correction 1: the recorders WERE running

Three recorder processes (`run_recorder.py`, `run_recorder_bybit.py`, `run_recorder_spot.py`)
have been up since 2026-08-04, alongside the live executor (`--capital 4500`), the deadman
switch, `screen_moat.py --files 24 --loop`, and the daily research cycle. **The moat tape stands
at 10 GB across 28,361 files.** Every claim made in this repo today that "the recorders have
never run" was an artifact of reading a checkout instead of the box.

## Correction 2: the two moat "survivors" are NOT an edge — their OOS sits at the noise ceiling

`reports/moat_campaign.json` (2026-08-02, 48 candidates, n_obs 1,065) reports
`n_survivors: 2` — and taken alone that reads like a breakthrough. The rows say otherwise:

| candidate | in-sample ann. Sharpe | DSR | PBO | RC p | **OOS Sharpe** |
|---|---|---|---|---|---|
| flow_momentum:ADAUSDT | 97.3 | 0.99999998 | 0.000 | 0.007 | **0.103** |
| flow_momentum:BCHUSDT | 84.3 | 0.9999997 | 0.000 | 0.023 | **0.098** |
| flow_momentum:1000CATUSDT | 69.8 | — | 0.001 | 0.086 | 0.068 (failed RC) |

An in-sample annualised Sharpe of 97 that delivers 0.10 out of sample is a 99.9% collapse — the
classic 1-minute-bar overfit signature. And 0.10 is *exactly* the ceiling the artifact itself
names: "the 2026-08-01 daily-bar campaign topped out at 0.100 across 129 textbook mechanisms."
**These two clear the screen's gates while landing at the same out-of-sample ceiling as every
mechanism that failed.** The screen was right to call them screen survivors with zero promotion
authority; nothing here is closer to deployable than the public-data work. Recorded as a
measured negative, not a lead.

What it DOES establish: the moat pipeline runs end-to-end on real depth data and produces
gate-level verdicts. The instrument works. n_obs 1,065 on a 10 GB tape is a thin first pass —
the honest next step is re-running as the tape deepens, with the pre-registered forward clock,
not celebrating a 0.10.

## The carry 40-day fast-track: 2 of 3 conditions met, regime evidence is the blocker

`web/cashcarry_shadow.json`, day 40 of the 40/90 window (forward since 2026-06-26):

- **NW t-stat 2.45** (naive 4.87) — clears the ≥1.65 bar. The Newey-West correction is doing
  real work here: the autocorrelation factor is **4.04**, so the naive t was inflated ~2×.
- **forward 14.71 vs backtest 3.33** — clears fwd ≥ 0.5× backtest. But 14.71 is the
  phantom-Sharpe shape the 2026-07-19 forensic documented (variance collapse on a delta-neutral
  molded curve produced a fake 16.09 then). It is not evidence of a 14 Sharpe edge.
- **regime evidence: FAILED.** `inversion_days: 0, basis_dislocation_days: 0`, and the variance
  fallback did not clear either — the 40-day window sat in the calmest quartile of the
  backtest's rolling-40d funding-vol distribution.

Verdict: **not promotable.** `promotion: None`. The gate is refusing to certify forty calm days,
which is exactly the condition it was written to enforce ("40 calm days test a market mood, not
an edge"). It becomes eligible when a regime event arrives (a top-20 funding inversion day or a
>3σ basis dislocation) or the window's funding volatility rises out of the calmest quartile —
or on the standard 90-day path, which needs no regime evidence.

## The one urgent operational gap

The recorders are **hand-started processes, not systemd units**, and the box reports
*"System restart required."* A reboot kills all three and the 10 GB tape stops growing with
nothing to restart it. `ops/deploy_vps.sh` installs the units that survive reboot; until it
runs, the desk's single un-replicable asset depends on the box not restarting.

Also standing: the VPS runs the sibling branch, ~100 commits behind today's work, with five
generated docs locally modified (including `recommendation_ledger.json`) — a plain `git pull`
would fight the box, so the update needs the generated files handled deliberately.

## Register row #58 — operator evidence RECEIVED 2026-08-05

The row's ask ("the operator OWES a crontab -l paste + systemctl list-timers") is satisfied:
both were pasted live this session. Measured against the manifest: the quant crontab carries
~30 lines (enforcement matrix, constitution core, promotion queue, max_push, utilisation,
conversion fence, micro_factory, live_guard */5, conviction trader, charts, paper book,
announcements, llm_trader, law gate, capability hunts, seat-fallback diggers...) and systemd
holds the digger/CRO timers plus — as of tonight — the three recorder units installed and
enabled by hand. The scheduler manifest's reconstruction can now be trued against this paste;
remaining drift between manifest and box is the deploy_vps.sh run that installs the missing
units (watchdog, alerts, cadence, daily-max, moat-miner/screen, deadman).
