# Capability hunt — 2026-08-12 seat 2 (BUILDER)

**Verdict: SOLO run.** The GPT-9 seat returned `HTTPError: HTTP Error 402: Payment Required`, so
only the Claude family proposed. Per L1.33 this is recorded **SOLO — never citable later as
cross-family corroboration.** The single-family status raised the verification bar rather than
lowering it: with no independent family to contest the proposal, its own pre-registered falsifier
became the only adversary available, and it was run first.

---

## What was proposed

**Proposal A (Claude family)** — *Internal double-entry: a check that reads ≥2 of the desk's own
published artifacts and fires when they contradict each other about the same named quantity.* The
desk built double-entry reconciliation against the **venue** (`run_venue_reconcile.py`, after a
13-model panel demanded it) and never once built one against **itself**.

**Proposal B (GPT-9)** — unavailable (402).

---

## Adjudication

No divergence to judge, so the discipline moved to steps 2 and 3: *verify it does not exist*, and
*run the falsifier the proposal itself pre-registered*.

**Prior art: none.** `grep -rln "reconcil"` over `scripts/` and `libs/` returns only desk-vs-venue
work. `grep "^def check_"` over `max_audit.py` filtered for contradiction/disagreement/consistency
vocabulary returns zero. Nothing existed unwired, so there was nothing to wire instead.

**The falsifier fired, and it killed the proposal as written.** The proposal said: *"Build the
index and count. If fewer than ~20 names are published by ≥2 artifacts, or if >70% of pairs are
same-name-different-meaning collisions, the correct build is a hand-registered ~10-criterion
money-path reconciler instead. One hour of measurement decides it."*

Measured, before any code was written:

| quantity | measured |
|---|---|
| artifacts scanned (`data/**.json` + `web/*.json`) | 590 |
| distinct leaf names | 10,003 |
| names published by ≥2 artifacts | 4,523 |
| names in scalar disagreement | 418 |
| **genuine same-meaning contradictions in a random sample of 25** | **0** |

`why` has 78 publishers of free text. `window_days` is `1.0` in `execution_economics.json` and
`90` in `replacement_rate.json` and **both are correct**. `ready` is a bool in one artifact and a
count of 11 in another. The general fence would have emitted ~418 findings of which essentially
all are noise, and a detector that cries wolf gets acked into silence — exactly how the
phantom-paths fence spent its first months (R0356). **The general index was rejected (R0540,
disposed REJECTED) and the proposal's own fallback was built.**

---

## What was built

**`libs/ops/claim_registry.py` + `scripts/check_claim_consistency.py`** — L1.61, a hand-registered
money-path claim reconciler. Six claims, every one structural (no prose regex), every one a fact
two organs genuinely both assert.

Status values: `OK` / `PARTIAL` (exit 0 — the gap is a work queue, because a fence red from day
one gets switched off) / `CONTRADICTED` (exit 2) / `UNMEASURED` (exit 2 — an empty comparison set
is the one result that must never read as agreement, L1.28a). Subject to L1.57 via
`scanned=n_compared`, which counts what the **run** compared, never `len(CLAIMS)`.

Wired in the same commit: law in `docs/CONSTITUTION.md`, doctrine line in
`ops/principal_doctrine.txt` (so it *reaches* every organ at spawn — L1.36 REACHING), mapping in
`build_enforcement_matrix.py`, registration in `check_build_standard._GOVERNED` (L1.41),
`ops/crontab.manifest` with EVIDENCE + CONFIDENCE, **and installed to the live crontab** — the box
was drifted, so the line was added surgically rather than via `reconstitute_cron.sh`. 21 tests,
including wiring tests that fail if the matrix mapping or the schedule is removed.

---

## What the first run said

`CONTRADICTED — 6/6 claims compared, 4 contradicted, 0 unresolved` (exit 2). **A first run that
reports a defect is success.** All four are on the only path to live capital: `gate0_readiness.json`
(the board a *human* reads) and `live_guard.json` (the executor-side evaluator) call the *same*
function, `libs/execution/staging.py:s1_entry_met`, and build its `evidence` dict from *different
sources*.

Two distinct classes, because they demand opposite repairs:

**FABRICATED-SIDE (2).** `principal_signoff` — the guard reads
`data/stage_state.json['principal_signoff']`, **a key no code anywhere writes**; consent actually
lives in `data/gate0_signoff.json`, signed 2026-07-30 ("I agree to sign off"). The guard is
structurally incapable of ever seeing a signoff. `symbol_count_4_5` — reaches the gate *only* via
the `data/ramp_state.json` evidence spread, a file that has never existed (L1.55's own proving
instance), so `int(...)` defaults to 0 while `cashcarry_config` has `top=4`.

**SAME-NAME-DIFFERENT-QUESTION (2).** `keys_present` means "credential *files* on disk" to one
board and "the venue *connector object* was constructed" to the other. `connector_verified` means
"531 fills in the execution tape" vs "a *canary probe* succeeded recently". **Both readings are
honest and neither is wrong on its own terms** — they feed one function whose evidence dict has no
declared contract. The *contract* is the defect.

The direction cuts the wrong way and that is the point: the guard is fail-closed so nothing moved,
but the board a **human** reads reports the mechanical preconditions for live capital green while
the machine says four of five are not met.

---

## The fence's own first run was wrong, and that is recorded rather than quietly fixed

The first implementation labelled **all four** contradictions `FABRICATED`, inheriting L1.55's
`measured: False` flag — which is **block-level**. Four of the five criteria are computed *after*
and therefore *override* the absent input, so `keys_present` and `connector_verified` are genuine
measurements. Labelling them fabricated would have sent a reader to repair an input those values
never read. **An inference wearing a measurement's clothes is the exact defect this fence exists
to catch**, one level up. The taint is now registered per criterion from a read of the producer's
code, and `test_block_measured_flag_alone_does_not_taint_a_real_measurement` pins the regression.

A second self-inflicted defect, same class: the exit line passed `fence="check_claim_consistency"`
while the denominator registry joins on the value `governed_fences()` yields — the filename **with
its suffix**. The fence read as UNDECLARED while declaring correctly: a producer/consumer name
mismatch, in the exit line of the fence built to detect producer/consumer mismatches. Fixed by
letting `caller_name()` derive the joinable name; now `DECLARED — 6 x claims compared`.

---

## What was refused

- **The general cross-artifact index** — R0540, disposed **REJECTED** with the measurement above.
  Revisit only with a per-name semantic declaration, never by widening the index.
- **Repairing `run_live_guard`** — NOT done, deliberately. `check_change_window.py` reports
  **STERILE** with `RAIL_BREACH: CASHCARRY_KILL present`. Repointing the guard at
  `gate0_signoff.json` is a money-path **improvement** in the **opening** direction, which L1.38
  stages and which this desk's own lesson L0133 ("check the direction of a fence repair") names.
  L1.38 always permits *repairs*; this is not one. Staged as R0536/R0537. The detector shipped;
  the actuator waits for the window.
- **The equity claim** — the desk holds **three** incompatible values for the denominator of every
  Kelly fraction, capacity runway and idle-cost figure: `$17,945` (gate0_readiness), `$13,155`
  (frozen in the signoff, authorising a 10% ceiling of $1,315) and `$4,500` (run_growth_audit
  default, 10% = $450, where print-impact measured the desk is a **spread taker**). Not registered
  as an L1.61 claim because **no two artifacts publish it structurally** — only in prose. That
  absence is itself the finding, rowed as R0539.

---

## Ledger

R0536, R0537 (FABRICATED-SIDE repairs, staged behind the change window) · R0538 (the Gate-0
evidence contract) · R0539 (the three-book sizing denominator) · R0540 (general index, REJECTED).

## Gates

`ruff` clean · `mypy` clean (623 files) · 21/21 tests · enforcement matrix **0 orphans** ·
`check_law_families` 6/6 · `check_build_standard` 80/80 · `check_denominator_attrition` OK ·
scheduler-manifest clean for this entry.
