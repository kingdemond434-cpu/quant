# Capability hunt -- seat 4, 2026-08-13

**Family coverage: SINGLE-FAMILY (SOLO).** The GPT-9 seat returned
`HTTPError: HTTP Error 402: Payment Required`. Proposal B does not exist. Everything below is
first-family (Claude) only and is **not** citable later as cross-family corroboration (L1.33).
The partner's unavailability is now a dated, measured fact for the third consecutive seat.

---

## What Proposal A proposed

**A measured TIME denominator for `axis_screen`'s power figure.** `n_eff = len(zv) / (t_deflator x
xs_deflator)`. Since 2026-08-11 `xs_deflator` carries a `breadth_basis` of
MEASURED/UNMEASURED/SINGLE-SERIES (L1.62). On the same line `t_deflator = float(horizon_days)`
(or `overlap_periods`) is a **declared constant with no basis field and no way to report itself as
unmeasured**. Proposal: measure lag-1 serial dependence of the pooled-IC summands, add
`time_basis`, require it for `powered`, fence it as `check_time_denominator.py`.

### All four of its verification claims were TRUE

| Claim | Verified |
|---|---|
| No `check_*.py` references `t_deflator`/`overlap_periods`, while `check_panel_breadth.py` exists | confirmed -- grep empty |
| `screen_venue_subsidy.py:576-579` confesses the gap in prose | confirmed verbatim |
| `type2_cost.autocorr_deflator` has zero production callers | confirmed -- declaration + tests only |
| `panel_breadth.py` already materialises the product matrix | confirmed (`m = prod[:, idx]`, :182) |

---

## The falsifier fired. Proposal A is REFUTED.

Proposal A pre-registered its own kill condition: *"Run `measure_panel_persistence` on the desk's
own 139-symbol futclose panel BEFORE building the fence. If the measured lag-1 rho of the product
terms is ~0, `horizon_days` is already sufficient and this is not worth building."*

Measured on that exact panel (139 symbols x 1,898 dates, 2021-06-01..2026-08-11), 4 constructions
x 3 horizons x 3 target kinds. Bartlett factor `(1+rho)/(1-rho)`, i.e. how far `n_eff` is
overstated:

| target kind | Bartlett median | Bartlett max |
|---|---|---|
| cross-sectional RELATIVE return | **1.000** | 1.245 |
| ABSOLUTE return | **1.000** | 1.118 |
| LEVEL (realized vol) | 1.144 | 2.625 |

**11 of 12 return-target cells sit at exactly 1.000** (rho <= 0, clipped). The declared constant is
already correct for return targets, and the proposal named its own killer in advance: *"the
summands are signal x target, and a near-white target can decorrelate a persistent signal."* That
is exactly what happens -- the forward-return leg is near-white and dominates the product.

**The one surviving scoped finding:** LEVEL targets (realized vol, and by construction funding and
basis levels) do carry serial dependence -- max 2.625, i.e. `n_eff` overstated 2.6x and
`min_detectable_ic` understated 1.6x. No screen on disk currently uses a level target, so nothing
is presently mis-verdicted. Rowed, not built (see REFUSED).

### The falsifier's first answer was itself an artifact -- and that is what produced the build

The un-floored run reported **rho = +0.856** on `M2_oi_growth|h=5` (Bartlett 12.9). Chasing it:

- requiring >= 10 finite symbols per date: **rho = -0.0597** (n falls 311 -> 299)
- **98.1% of the lag-1 numerator came from 5 date-pairs** out of 310
- removing those 5 dates: rho = +0.14
- cross-sectional MEDIAN instead of mean: +0.886 (so not an outlier-symbol effect -- a thin-date effect)

Twelve dates out of 311 -- 4% of the input -- determined 98% of a statistic that read as strong,
clean and publishable. **I nearly built a fence on it.**

---

## What I built instead

### The defect class, generalised and measured

A per-date cross-sectional collapse (`mean(axis=1)`, a demean, a rank, a z-score) over a thin
cross-section is one symbol's noise. Consecutive thin dates give the resulting **series** structure
the data never had.

**Why the desk's existing guard does not catch it, and this is the whole point.** The dominant
idiom here is `if panel.shape[1] < N`. That counts **declared columns** -- the panel's width -- and
a ragged panel's width is a property of its widest date. A 373-column panel clears `shape[1] >= 8`
on a date carrying six finite names. It reads like a breadth guard, is documented like one, and
checks a number that **cannot fall when the cross-section empties**. That is the L1.57 defect -- a
denominator counting what the author wrote down rather than what the run found -- one axis over.

### Built

| Artifact | What it does |
|---|---|
| `libs/research/cross_section_floor.py` | `measure_cross_section` / `apply_floor`. Floors on **finite values per row**, never `shape[1]`. Statuses MEASURED / UNMEASURABLE; an UNMEASURABLE panel returns an **all-False** mask, so a caller who ignores the status gets no dates rather than every date (L1.28a). Counts and publishes every dropped date (L1.60). |
| `scripts/check_cross_section_floor.py` | AST fence over `libs/` + `scripts/`. Classifies every collapse site FLOORED / **NEAR-MISS** / UNFLOORED. `-> data/cross_section_floor.json` |
| `docs/research/CROSS_SECTION_FLOOR_RATCHET.json` | coverage floor, ratchets UP only |
| `tests/research/test_cross_section_floor.py` | 21 tests, all green |

**Repair (the live defect).** `scripts/run_derivative_shadow.py::_forward_returns` is *declared*
the locked mirror of `scripts/backfill_oi_ls_oos.py` -- the backfill even says so in a comment.
Every line from `pr_ret = prices.pct_change().shift(-1)` down is byte-identical between them. The
**only** difference was the two-line cross-section floor, present in the OOS validation and absent
from the **live forward clock**. The OOS evidence was earned on a construction the live clock does
not run (L1.61 class), and the half that feeds promotion was the unfloored one. Now floored via the
shared instrument, with `_MIN_SYMBOLS` held equal to the mirror's by test, not by comment.

**Measured before fixing, and reported honestly: this is LATENT, not currently biting.** The live
panel is 46 dates x 373 symbols with a thinnest cross-section of **99** -- zero dates below the
floor, and the published number is bit-identical before and after. It is closed anyway because the
roster changes as the clock accrues and a construction that disagrees with its own out-of-sample
validation is not something to leave running because today's data happens to be kind.

### First run -- a defect in the fence itself, caught before shipping

The fence's first run scored my own repair **FLOORED** -- because the *comment explaining the fix*
contained the string `notna().sum(axis=1)`. Prose about a guard read as the guard. A fence
satisfiable by **describing** the fix would certify exactly the files whose authors thought hardest
about the problem and then did nothing. Fixed by matching `ast.unparse`d code with docstrings
stripped; pinned by `test_fence_is_not_satisfied_by_a_comment_describing_the_fix`.

### First run -- the real numbers

```
cross-section floor: PARTIAL -- 17/49 collapse sites floored (34.7%),
13 near-miss, 19 unfloored, over 1106 files [0 unreadable]
```

Two independent methods (a broad code sweep and this AST fence) converged on the same sites:
`libs/alpha_factory/evaluator.py:86-88`, `libs/alpha_factory/wq_operators.py:105/118/119`,
`scripts/run_cashcarry_shadow.py:102`, `scripts/run_derivative_backtest.py:84`,
`scripts/screen_idle_axes.py:212-213`, `libs/research/crypto_regime.py:27`,
`libs/validation/baselines.py:70`.

**PARTIAL exits 0 by design.** Roughly half the collapses in this repo are unguarded; a fence red
on day one gets switched off (L1.43). Coverage is a ratchet whose gap is the work queue (L1.0).
Only **REGRESSED** (floored count falls) and **UNMEASURED** (zero sites found -- WS-005) fail.

---

## What I refused to build, and why

**The proposed `time_basis` + `check_time_denominator.py` fence.** Its premise was refuted by its
own pre-registered falsifier on the desk's own panel: median Bartlett 1.000 across 24
return-target cells. Building a law, a fence, a cron line and permanent maintenance for a
correction measured at **zero** in the median cell would be activity theatre, and L1.59 presumes
doctrine expansion negative-ROI absent a demonstrated loophole. Rowed with the measurement
attached so the next session does not re-derive it.

**A new law L1.63.** The cross-section floor joins the **L1.62 family** instead. It is the same
defect one axis over, the instrument sits beside `panel_breadth.py`, and the fence follows
`check_panel_breadth.py`. L1.59 freezes doctrine expansion, L2.9 says upgrade before building, and
the constitution core is hash-sealed. The matrix entry carries the reasoning.

---

## Verification

| Gate | Result |
|---|---|
| `ruff check .` | All checks passed |
| `python -m mypy` | Success, 628 source files |
| `pytest tests/research/test_cross_section_floor.py` | 21 passed |
| `build_enforcement_matrix.py` | **fences with no governing principle: 0** |
| `check_scheduler_manifest.py` | my line = manifest-only (cron not yet installed); DUPEs pre-existing |
| `check_timidity_language.py` | L1.28 injected: True |

## Evidence (committed -- `reports/` and `data/` are gitignored, so the raw runs live here)

- `20260813_s4_falsifier_summand_persistence.json` -- 24 cells, floored and un-floored rho side by
  side, so the thin-date artifact is reproducible rather than merely described.
- `20260813_s4_falsifier_target_kinds.json` -- the relative / absolute / level comparison that
  refuted the proposal and scoped its one survivor.

## Ledger rows

- Time denominator (Proposal A as proposed) -- **REJECTED**, refuted by pre-registered falsifier.
- LEVEL-target serial dependence -- **SCHEDULED**, the scoped survivor; act when a screen first
  uses a level target.
- 32 unfloored/near-miss collapse sites -- **SCHEDULED**, the ratchet gap, near-miss first.

## Next ground

The **near-miss** sites are the priority, not the unfloored ones: they already look guarded to a
reader, so they are the ones a future session will trust without checking.
`libs/alpha_factory/wq_operators.py` is the sharpest -- peer groups are deliberately narrow, so a
group of 3 with 2 NaN yields a one-name "z-score" feeding the 898,560-cell sweep.
