# CAPABILITY HUNT PROPOSALS 20260813 slot 0

LENS: REGIME-CONDITIONED EDGE -- an edge that exists only in a nameable, DETECTABLE regime (high-funding, high-vol, post-liquidation, low-liquidity) we could switch on and off. What regime do we not yet detect, and what edge would it gate?

## A -- Claude family

## THE DEEP PROPOSAL

**MISSING CAPABILITY: a FUNDING-STATE LIBRARY — named, causal, backfilled, episode-counted regime series in the space where this desk's only surviving edge lives (funding rich/poor/inverted, basis, OI/positioning) — wired as a second axis into the three gates that already issue "regime-robust" certificates and set the sizing ceiling.**

**WHY IT IS INVISIBLE TODAY.** Not because "regime" is missing — because it is everywhere and green. An AST walk of the full transitive closure found 6 wired regime paths and 30 orphaned regime modules. Every *reusable* wired gate partitions by **realized-volatility terciles of BTC price**: `libs/autodiscovery/regime.py:36 regime_robust` (blocks REGISTRY promotion), `libs/risk/sleeve_allocation.py min_regimes_positive` (STRONG=2/DURABLE=3, a sizing ceiling), `scripts/check_promotion_gate.py:191 two_regimes` (LIVE at 15% of book). All three read vol terciles; none reads funding. So a promotion verdict prints `regime_robust=True` and every dashboard reports regime intelligence as covered. **A missing partition is invisible exactly when the existing partition returns a green verdict** — L1.57's "a verdict without a denominator", one level up: a robustness verdict without a *relevant* denominator. Three further reinforcers: (1) the one funding-space regime criterion on the desk is 25 hand-written lines inside `run_cashcarry_shadow.py:99-114`, bespoke to the single pre-registered hypothesis, a *window-coverage check* rather than a state series, and available to no other candidate — the desk proved it knows funding-state evidence matters ("40 calm days test a market mood, not an edge") and never generalised it; (2) the labeller already exists — `libs/research/crypto_regime.py:22 regime_labels` computes a lagged, no-look-ahead **funding rich/poor** axis whose docstring says "funding carry shines in funding-rich, leveraged markets" — and is read only by `run_crypto_portfolio.py`, which is not in cron; (3) the desk's own prioritiser cannot fund the fix: `alpha_economics.ev_score` prices a conditioning overlay as `est_sharpe` = refinement, and `btc_correlation_regime_carry_conditioning` died at **EV 0.0003**. The gap is *maintained by a ranking function*, not overlooked.

**MECHANISM.** `libs/research/state_library.py` emits `data/state_library/{axis}.jsonl` — one row per (date, axis, state), computed as-of that date with no future data, from series already on disk: `data/funding_settlements/*.parquet` (**211,531 rows, 2019-09-10→2026-08-01, 7.0 years**), `data/lake/bronze/crypto/{SYM}/D1` (300 symbols, `funding` + `basis` columns), `data/oi_ls_universe.jsonl` (**256,625 rows, 5.9 years, 365 symbols**). Each axis publishes `episodes` (distinct non-overlapping runs), `share`, and a `causal_basis` field — HMM/GMM labels are **refused**, because a full-sample-refit label assigns day *t* a state fitted on data after *t*, which is a look-ahead in the *conditioning* variable (the desk's own recorded lesson from `pct_circ_now`, and it fails toward a false null). Consumers: (a) `regime_robust(returns, axes=("vol","funding"))` — **conjunctive, never a loosening**: an edge must still clear vol *and* now declare its funding-state coverage; (b) `data/state_conditional_candidates.json` gets its first producer, so `libs/validation/state_conditional.Preregistration` becomes satisfiable (MIN_STATE_OCCURRENCES=8, MIN_STATE_SHARE=0.05 — trivially clearable on 7 years, structurally unclearable today); (c) fence `check_state_library.py` → `MEASURED / PARTIAL / UNMEASURED`, with `OVER-CERTIFIED` when a live sleeve's evidence sample sits >90% inside one state of any declared axis.

**WHAT IT WOULD HAVE CAUGHT.** `data/state_conditional_candidates.json` **does not exist**, so the branch built for conditional alpha (GAP row 105) is *unfeedable*, not merely unfed — while `libs/validation/gate_power.py` measured F3 keeping only **~50% of conditional edges at any effect size** (0.44→0.55 across planted effects 0.05→0.40, versus 1.00 for stable edges). That is a structural discard rate that no effect size fixes, sitting under a 434/0 campaign whose L1.25 diagnostic stopped at two campaign-constant gates. Concretely: all four `regime_artifact` entries in `data/graveyard_resurrection_queue.json` carry the treatment *"RESURRECT-ABLE: died in one regime. Re-test conditioned on regime"* — **unexecutable today**, since no state series exists to condition on and `state_conditional` correctly refuses a state named after the death. And the live risk: R0372 banked (rm-20260805T135340-cb85e2) that **2026 YTD BitMEX XBTUSD funding is −1.23%/yr with 49.4% of settlements negative against a 2016-2025 mean of +11%/yr** — the funding regime flipped for majors in the year the desk deployed, and no gate on this desk can express that sentence.

**ROI.** Direct: the only mechanism that can say "this edge's evidence came from a funding state that has ended" — a replacement-rate early warning (L1.30 currently reads OK at 13 births/7 deaths, every one certified in the wrong space) for the *one* deployed sleeve. Cascade: removes F3's measured ~50% conditional ceiling → attacks 434/0 on the axis the diagnostic never checked; unblocks 4 resurrection entries at zero screening cost (already paid for); gives `state_conditional`, `sleeve_allocation`, `check_promotion_gate` and `crowding` one vocabulary, so "the funding regime changed" stops landing in the execution residual bucket. Anti-timidity half: W1/W2/W3 only ever *cap*; a correct funding-space certificate can **raise** a ceiling for an edge robust where it matters (L1.51 — price the clamp, then lift it).

**COST.** ~1.5–2 days: the labeller exists, the data is 7 years deep, the consumer interfaces exist. Maintenance ~0 (one cron append + one fence). Competes with: the 244-row repair backlog (repair-mode is ON — this is a proposal, not a build), and **GAP row 24** (edge-decay lab, deadline 08-15) with which it should **merge** — row 24 branches on (fill-ratio, realized-vol), which is the same question asked in a third space that is also not funding.

**FALSIFIER.** Run it before building. Compute carry-sleeve daily returns on the desk's own panel, partition by the funding axis and by `vol_regime_labels` terciles, and compare between-group separation. **If vol terciles already separate carry returns as well as funding state does, the existing certificate captures what matters and this is dead** — record that vol is a sufficient proxy for funding state in crypto. Second falsifier: if the desk's evidence sample is already ≥33% `funding_poor` *and* carry there is not distinguishable from zero, the certificate was never at risk of over-issuing. Both computable in an afternoon from data on disk; running the falsifier first is what corrected the number in capability-hunt s3.

**NOVELTY-CHECK:** `grep -rn "regime_labels\|crypto_regime" libs/autodiscovery/ libs/risk/ libs/validation/ scripts/check_promotion_gate.py scripts/run_sleeve_allocator.py` → only `vol_regime_labels`; the funding axis is wired into **no** promotion or sizing gate. `ls data/state_conditional_candidates.json` → does not exist. `grep -rn "funding regime\|funding_regime" docs/GAP_REGISTER.md docs/research/recommendation_ledger.json` → no row proposes a funding-state partition of the robustness certificate.

---

## BRAINSTORM

**Regime lens (primary), continued**
1. **Funding as price, OI as quantity — the identification regime.** A funding level alone cannot distinguish a demand shift (retail leverage, sticky, comes back) from a supply shift (competing basis desks, permanent). The sign of d(funding)/d(OI) identifies which curve moved. `libs/research/crowding.py` is price-residual-only and *structurally cannot* make this call; row 24's lab branches on (fill-ratio, vol) — also not the market's quantity. **S** → ledger, then the state library's second axis.
2. **The Bybit `meta` stream is a 12-second OI+funding+mark panel, 34 symbols, 23 days (~5.3M records) — and it is unconsumed.** `build_bars.py:25` plumbs the OI field but `data/bars/` emits no `oi` column (1,396 rows total). Highest-resolution positioning series on the box by two orders of magnitude, feeding nothing. **S** → §33 conversion.
3. **Funding-clamp saturation as a *state*, not a censor.** R0293 shipped censoring (labels saturated prints). The regime reading was named as "secondary" and never built: at the cap, funding can no longer pull perp to index, so basis must close via spot flow or forced deleveraging — a different return distribution, not just a bad measurement. **A** → state library axis 3.
4. **Regime *duration/hazard*, not level.** Carry P&L is funding accrued minus basis convergence at exit, so entry EV depends on E[remaining settlements | regime age τ], which nobody computes. Every meter here is a level meter. `libs/portfolio/decision_intelligence.py:133` already has `transition_posterior` with duration hazards — orphaned. **A** → ledger.
5. **`regime_transition=` in `libs/validation/revalidation.py:167` has zero callers** — only `structural_break` is ever passed, so half the revalidation trigger is dead code on the capital-strip path. **B** → fence.
6. **The two live regime producers disagree right now and publish confidence 0.0**: `data/crypto_regime.json` says `trend:bear`, `hmm_regime: bull/low_vol`, `hmm_gmm_agree: false`, `leverage_multiplier: 1.0`. Harmless only because nothing consumes it — an L1.61 claim-consistency pair with no registry row. **B** → claim registry.
7. **`scripts/run_regime_engine.py:4` asserts "the executor sizes by regime (it already reads that file)". It does not** — the consumer was `run_crypto_testnet.py`, retired. `ops/memory/crypto-desk-state.md:95` repeats it. A false wiring claim in desk memory. **B** → fix docstring + memory.
8. **`data/crypto_regime_history.jsonl` has 6 rows with 3 missing days** (08-06, 08-07, then 08-13). R0006 built an append-only fingerprint to stop overwrite loss; nothing fences its continuity. **B** → freshness contract.

**Adjacent — bars the desk already moved and never re-adjudicated**
9. **A THRESHOLD RECALIBRATION IS A NAMED ENABLING CHANGE, AND NOTHING RE-ADJUDICATES.** `alpha_economics._EV_THRESHOLD` moved 0.05 → **0.002** on 2026-07-31 (R0023/R0034). GAP row 8's three FRED macro hypotheses were rejected at EV **0.0039–0.013 against the 0.05 bar** — all three clear today's bar by 2–6×, and nobody re-ran them. `graveyard_resurrect.py` classifies by *cause of death*, never by "the bar that killed it has since moved". This is arguably a second deep proposal: a bar-change → re-adjudication sweep, cheap, and it manufactures births (L1.30) from experiments already paid for. **S** → ledger, high.
10. **Every rejection should record the bar value it was measured against**, so #9 is computable rather than archaeological. **A** → graveyard schema.

**Data already owned, never read (each is edge already paid for and declined — L1.28a)**
11. `data/lake/bronze/etf_flows/` — 21 HTMLs, **no code reference anywhere**, while GAP row 76's headline structural break is *the Jan-2024 spot-ETF DiD that cut carry 36% across exchanges and 97% on CME*. The desk holds the flow series for its single most important documented regime boundary and has never opened it. **S** → axis watchlist.
12. `data/lake/bronze/fed/` — 63 Treasury General Account CSVs, 30 MB, zero references. TGA drawdowns/refills are a first-order dollar-liquidity driver and a natural macro state axis. **A**
13. `data/bybit_l2_samples/bybit_ob200_20250821.zip` — 156 MB of OB200 depth, **no reference anywhere in the repo**. A depth-200 day is the only sample that can validate whether top-20 book-walk cost estimates are biased. **A**
14. `data/cot_btc_panel.json` (828 KB), `data/lake/bronze/mining/` (hashrate/difficulty/revenue), `data/lake/bronze/crossasset/` (VIX + UST curve), `data/lake/bronze/wikipedia/` (7 pageview series) — all zero code references. **B each** → §33 backlog.
15. `data/deribit_surface.parquet` is referenced only by a comment in `libs/research/cro_role.py:440` *stating it has never been read*. A defect that documented itself and stopped. **B**
16. **`data/geckoterminal_trades.jsonl` retains ~1.3 days** at 32 MB/65k rows. Irreplaceable tape is being destroyed hourly under a law that says acquisition is never cut (L1.28b(f)). Rotate to the lake. **A**
17. **`data/lake/bronze/crypto/{SYM}/H8/` last bar 2026-06-20 — 8 weeks stale while D1 is current.** The 8-hour bar is the *native funding period*; the desk's funding-clock work (L1.47) has no current 8h price series. Silent partial feed break; only 10 symbols exist at H8 at all. **A** → fence.
18. **`collect_bitmex_funding.py` is not in cron and its output is 13 days stale** — 10.2 years, the deepest funding series on the box and the evidence base for R0372's regime claim. **B**
19. **300 D1 symbols exist, median depth 790 rows, only 5 exceed 2,500.** Every cross-sectional screen quietly runs on a universe whose median member has ~2 years. Publish per-cell depth beside every panel IC. **A** → ties to L1.62.
20. **CME: 8.5 years OHLCV + 4.88M `BTC_FUT_statistics` rows (settle/OI), one-shot pull ending 2026-07-20, `pull_cme.py` not in cron.** The only *regulated-venue* basis anchor the desk owns, going stale. **A**

**Execution / money path**
21. **The cash-carry executor conditions on zero regime variables** — verified across 61 modules. Even the cheapest version (suspend new opens when the aggregate top-20 funding mean is ≤0) has never been tested; the entry gate is per-symbol net-bps only. **A** → ledger.
22. **`libs/risk/gate.py`'s `risk_gate`/`crisis_controller` is imported into the executor process via `libs/risk/__init__.py:37` and never called** — only `app/demo_runner.py` and `app/readiness.py` call it. A crisis controller loaded on the money path and wired to nothing. **A**
23. **`libs/risk/vol_target.py:37 regime_adjusted_volatility` has zero callers**; `libs/research/regime_trend.py` (257 lines) has zero importers outside its test. Capability built, unwired — L1.28a. **B**
24. **The leverage optimizer is quarantined "in both directions"** (`run_cashcarry_executor.py:146-154`, gap #14) — a clamp with no lifting condition on the record. Price it under L1.51. **B**

**Validation / discovery rate**
25. **F3's ~50% conditional-edge ceiling is a measured, standing loss with no owner.** `audit_gate_power.py` produces the number; nothing routes it into the 434/0 diagnostic or into a sizing/queue decision. **S** → ledger (this is the deep proposal's twin).
26. **The Holm forward queue is ordered but the regime-coverage requirement applies to exactly one hypothesis.** Generalise `run_cashcarry_shadow.py`'s "not the calmest quartile" test to every forward clock, or record out loud that 11 of 12 slots may be promoted on calm windows. **A**
27. **`gate_reachability` reports `regime_robustness` as a DEAD-BRANCH gate** (behind a `CandidateStatus.REGISTRY` precondition that has never held) — known under L1.49, but note the compound: the *only* regime gate in the candidate gauntlet has never executed **and** the regime gate that does run (W1) is in the wrong space. Two independent failures on the same axis. **A**
28. **Positive control for the conditional branch**: plant a synthetic funding-conditional edge and confirm the new axis *keeps* it, exactly as `certify_gauntlet.py` does for the main path. A branch never shown to pass a known-good conditional edge is unvalidated. **A**

**Adversary / negative-exemplar sweep**
29. **The desk is the short leg, and carry predicts liquidations on that side** (GAP row 76, BIS Table 7). `data/liquidations.parquet` is now 35 days / 70,727 rows / 1,832 events per day — the Table-7 replication that was a 17-day overfit in July is now clock-startable. **A**
30. **Carry skew worsens with leg count (−0.700 at 1 leg → −0.977 at 3) and the book runs `top: 10`** on the opposite assumption. Measurable today on desk history; ruin-adjacent, since negative skew is what log-wealth punishes hardest. **S** → row 76's leg (2), still unmeasured.
31. **Venue funding dispersion as a routing edge**: `data/tail_funding_divergence.jsonl` and `data/hyperliquid_funding.parquet` (with `bn_funding` and `spread` already joined) exist and are read by nobody. Same trade, better-paying venue — capacity-tiny, §42-shaped. **A**
32. **`data/perpdex_funding.jsonl` — 184,463 rows, 5.0 years, 20 pairs across aster/lighter, zero consumers.** A perp-DEX funding panel is a structurally different participant population from CEX perps: the cleanest available instrument for separating venue-local demand from market-wide leverage demand. **A**

**Meta**
33. **Nothing on this desk measures the *space* its regime concept spans.** The state library should publish its own coverage — which axes exist, which are causal, which have ≥8 episodes — so "we condition on regime" becomes a number instead of a word. **A**
34. **An `OVER-CERTIFIED` status is missing from every gate vocabulary.** Gates report pass/fail/unmeasured; none can say *"passed, in a space that does not bind."* That verdict is what would have flagged this whole finding on day one. **A**

*Next in this seam, unwritten for context: (a) whether the desk's `_BLEED_BPS` denylist is itself regime-conditioned — a name that bleeds in a funding-poor state is denied permanently, and `n` only grows through opens, so the L1.45 re-entry cycle recurs on a state axis; (b) a survey of which of the 30 orphaned regime modules can be retired versus re-aimed, since an unmeasured orphan population is itself an L1.43 quiet-detector question.*


## B -- GPT-9 family (independent)

(GPT-9 seat unavailable: HTTPError: HTTP Error 402: Payment Required. This run is SINGLE-FAMILY -- treat its proposal as unconfirmed by an independent family, and note that in the record.)
