# CAPABILITY HUNT PROPOSALS 20260819 slot 3

LENS: CAPACITY & COMPOUNDING -- what lets the book carry more risk-adjusted size or compound FASTER: a decorrelated sleeve, a cost-tier cut (every bp is pure CAGR), a funding-harvest cadence, a capacity band we are leaving on the table.

## A -- Claude family

Hunt complete. I found one capability that clears the bar, verified absent from the ledger (619 rows), the gap register, the max-push queue, the vault, and the code.

The short version: the desk's only deployed sleeve runs inside a margin construction that nobody ever chose — spot wallet + separately-margined USDT-M short — and no artifact measures what that construction costs in capacity. The desk itself mined (card 31, 8btc thread-172717) the proof that an alternative construction has *no liquidation boundary at all*, then routed that fact to signal-space and never asked the capital-structure question about its own book.

---

**MISSING CAPABILITY:** A margin-topology comparator — the measured number `notional_per_equity` (harvestable carry notional per dollar of desk equity) for every construction available at current equity, with the live construction's status DECIDED vs INHERITED.

**WHY IT IS INVISIBLE TODAY:** The construction was never decided — it was inherited from which connectors got built first (`binance_spot_testnet` + `binance_testnet`, one venue, two wallets). Every fence and incident repair since has asked "is the accounting right GIVEN the topology" (R0053, R0234, gap rows 19/34, launch-readiness L10) — none asks "is the topology right." This is the L1.55 distinction (restraint CHOSEN vs INHERITED) applied to the capital structure itself, and it's the L1.45 cycle-blindness shape: every repair individually correct, the topology never questioned. The one place capital efficiency IS modeled, `scripts/run_capital_plan.py:36`, hardcodes `_PM_EFFICIENCY = 1.8` — a constant wearing a measurement's clothes (L1.57 family) — and applies it at every equity level including $3,846, where its own `pm_steps` note says PM is ineligible. So the desk models only the construction it *cannot* use at seed, while the two it *can* use today (Multi-Assets Mode; COIN-M 1x self-collateralized inverse short) are modeled nowhere: `multiAssetsMargin` appears in the repo exclusively as an equity-accounting hazard, and `list_perp_symbols()` hard-filters `quoteAsset == "USDT"` (F24, 2026-08-01 sweep, recorded as "a one-line finding").

**MECHANISM:** `libs/portfolio/margin_topology.py` + `scripts/check_margin_topology.py` → `data/margin_topology.json`. One row per construction — (a) current split-wallet, (b) Multi-Assets Mode (coin collateral in the futures wallet backing the short), (c) COIN-M 1x self-collateralized inverse, (d) Portfolio Margin, (e) Bybit UTA netting (tape already archived) — each carrying `{notional_per_equity (MEASURED from venue terms + the executor's actual margin sizing, never assumed), liq_distance_sd, funding_book_bps (USDT-M vs COIN-M per name, first consumer of the R0462 backfill), fee_rt_bps, transfer_ops_per_month (the split topology's rebalance/orphan tax, counted from the executor journal), eligibility, forward_evidence_restart_days}`. Fence statuses: `DECIDED` / `INHERITED` (live or shadow book running on a construction with no decision row — the failing state) / `UNMEASURED` (inputs absent; never resolves clean, L1.28a) / `INELIGIBLE-LISTED` (PM at seed stays visible with an eligibility clock rather than dropping out of the denominator, L1.60). Wires into `report_arming.py`'s cost-levers section and replaces the hardcoded 1.8 in `run_capital_plan.py` with the measured value. Arithmetic of the prize: construction (a) at the gated 3× needs ~$1.33 equity per $1 notional (spot fully funded + short margin) → ~0.75 notional/equity; (b)/(c) ≈ 0.90–1.00 because the long leg IS the collateral and appreciates in lockstep with the short's loss — at COIN-M 1x the liquidation price is literally unreachable (the 8btc testnet experiment the desk itself mined: liq price "一亿" ≈ +∞). That is a **+20–33% permanent multiplier on carry CAGR at identical equity, with liquidation risk moving DOWN** — the rare capacity raise that costs no ruin budget. Honesty constraint built in: a construction switch restarts forward evidence (L2.10), so the artifact prices `forward_evidence_restart_days` deployment-race style (L1.18a), which is exactly why the verdict is cheapest NOW — pre-Gate-0 the change window is OPEN and every week of shadow accrued on the wrong construction is evidence that must be re-earned.

**WHAT IT WOULD HAVE CAUGHT:** Three paid incidents, all topology artifacts: (1) 07-13 NOMUSDT — futures-wallet equity read −40.9% while the book was −$55, because the wallet holding the short cannot see the hedge; (2) 07-19 dead-man TRUE fire — the $1.8–2.6k accounting break across wallets, still UNRESOLVED (gap row 34), plus the flatten of a solvent book; (3) 07-30 F6 — `multiAssetsMargin=False` valued $5,000 of USDC at zero, pinned high-water at $209.43, disarmed the survival rail at every equity (R0053). Under (b)/(c) one wallet holds the whole hedge, so the ruin rail's denominator is the truth natively rather than by accounting patch.

**ROI:** Direct: +20–33% relative CAGR on the only deployed sleeve, permanent, no new alpha, no rail touched. Cascade: converts R0462's COIN-M backfill from research axis into a money-path consumer (§33 conversion parity); deletes the constant-as-measurement defect in the capital plan; shrinks the orphan-leg/rebalance machinery class the executor spends hundreds of lines defending; gives gap-19/34 rails a truer denominator; seeds the treasury view the `capital_topology` aspect wants. Multiplies every future sleeve too — construction choice recurs per sleeve.

**COST:** ~6h for comparator + fence + capital-plan rewire; low maintenance (public venue terms + own artifacts). Competes with repair-window drain — but it is itself a conversion (makes a scheduled row's output consumed) and is time-decaying (cheapest before Gate-0).

**FALSIFIER:** Comparator returns <5% net uplift for every alternative at both seed and $25k equity — i.e., measured executor margin already runs near VENUE_MAX (0.91 efficiency), Multi-Assets haircuts/collateral-list exclusions plus thin COIN-M alt books (20 perps) plus forward-restart cost eat the gap. Then the verdict is KEEP-CURRENT, the build collapses to a priced rejection, and the fence is retired as a one-shot measurement. Also falsified if Binance's Multi-Assets collateral terms exclude the carry universe's coins outright — the comparator's first input is reading those terms.

NOVELTY-CHECK: `grep -rn "margin_topology|notional_per_equity|capital_intensity" .` → zero hits; ledger scan (619 rows, keys multi-asset/coin-m/portfolio margin/margin construction) → 5 hits, all accounting or data-backfill (R0024/R0053/R0234/R0462/R0504), none evaluate construction; `vault_search "multi-assets margin mode decision"` → only the accounting-hazard rows (PREMORTEM row 3, launch-readiness L10); `COINM_CONVEXITY_PREREGISTRATION.md` is the RV *trade* (EV-REJECTED) and says "measurement, not construction-shopping" about its own trigger; `GROWTH_UNLOCK_LADDER.md` levers = fees/compounding-hygiene/cross-venue/clocks, construction absent.

---

**THE BRAINSTORM** (raw generation; builder rows the strongest via `scripts/recommendations.py` — I'm a read-only seat):

1. Funding-book router — per-name harvest routing USDT-M vs COIN-M vs USDC-M, richest book net of fees/depth, for the already-validated sleeve (execution optimization, not the rejected RV trade) — A — ledger, consumes card 31.
2. Bybit UTA as construction (e) — unified account nets spot+perp at zero equity floor; the "cross-venue family" ladder lever doubles as a capital-efficiency competitor — A — ledger.
3. Capital-plan PM constant defect — `_PM_EFFICIENCY=1.8` hardcoded AND applied at PM-ineligible equity levels; publishes fiction to the principal-facing plan — B — ledger (fence via the comparator).
4. Quarterly-delivery carry — locked basis to expiry, zero funding-flip risk, 212 expired quarterlies of history already archived for the backtest; decorrelated variant of the validated mechanism — A — axis watchlist → gauntlet.
5. COIN-M `liquidationSnapshot` screen — 118 symbols of liquidation tape that exists ONLY on the CM book (card 31 evidence noted the file, nobody proposed the screen); §42 too-small-for-funds ground — A — axis watchlist.
6. Yield-bearing collateral audit — BFUSD/Simple-Earn-as-collateral terms: does any Binance instrument pay while margining the short (second yield stream on the same dollar); licence/terms read first — A — axis watchlist.
7. Staked spot leg — ETH carry with the long leg staked (BETH as Multi-Assets collateral?) stacks ~3%/yr on the leg; extends the known §82 idle-yield blindness into the carry construction specifically — A — axis watchlist.
8. Equity-step event trigger — on every NAV doubling, auto-refire PM eligibility, sub-viable capacity bands (L1.18a kills), venue fee tier, and the topology comparator; today nothing re-evaluates on equity growth, which is an information-arrival cadence defect (L1.28c) — A — fence.
9. Sub-viable resurrection keyed to NAV — capacity kills graded SUB-VIABLE at $1k are silently permanent; L1.16a re-entry condition should be an equity threshold written at kill time — A — fence extension.
10. Harvest-compounding cadence — re-anchor `--capital` per funding settlement rather than weekly (ladder lever 2 has no cadence argument; L1.28c says phase counts) — B — ledger.
11. Margin-buffer autopilot — measured minimum futures-wallet buffer vs rally tail from the desk's own recorder tape, replacing static buffer; every freed dollar is notional — B — ledger (subsumed if topology switches).
12. Split-topology tax meter — count and price orphan-leg events, margin top-up transfers, and false rail reads from the executor journal per month; the comparator's `transfer_ops_per_month` input, valuable standalone as an L1.51 clamp price — B — fence.
13. Fee-basis audit fence — every live fill's fee vs cheapest-achievable schedule (BNB burn on, maker where intended, right quote book); turns gap row 59 from checklist into a fence — B — fence.
14. USDC-M promo tiers — Binance USDC-quoted perps run 0-maker promos; same sleeve, cheaper book; quote-asset choice is a fee lever distinct from VIP tier — B — fee register.
15. Pre-settlement entry timing — L1.47 measured close-phase concentration; the open-side twin: enter before predicted-rich settlements using the premiumIndex path already collected (graveyard/novelty check first) — A — hypothesis queue.
16. Coin-accrual compounding differential — COIN-M funding pays in coin (auto-compounds with price), USDT-M pays in USDT (waits for re-anchor); measurable drift between the two at equal rates — C — comparator input.
17. Transfer-cost matrix for the cross-venue family — on-chain fees + transit time = priced unhedged window, pre-built from public schedules before the Bybit/Hyperliquid sleeve pre-registers — B — axis watchlist.
18. Stress-haircut ladder — Alameda lens: venue can raise collateral haircuts in crisis; the comparator must carry each construction's haircut-under-stress, not just today's — B — comparator requirement.
19. COIN-M delisting-unwind ground — §42 names delisting unwinds; the 30 delisted CM perps + archive give the uncrowded inverse-book variant a free backtest — B — axis watchlist.
20. Treasury one-view — where every desk dollar sits, what it earns, what it backs (the tier-1 gap: no treasury function exists); seeds from the comparator + `capital_topology` aspect — B — ledger.
21. Funding-skew as stress dial — COIN-M/USDT-M funding skew widening = stablecoin-trust stress (card 31's clientele prior) reused as a risk-conditioning input for the existing sleeve, not a trade — B — feature into sleeve conditioning.
22. Dust-quantization loss — venue min-notional quantizes seed-scale weights; measure target-vs-achievable weight drag per rebalance — C — TCA measurement.
23. PM-eligibility clock — pre-registered unlock (ladder-style) so the 1.8× fires same-day on eligibility instead of being renegotiated — B — ledger, ties to 8.
24. F24 upgrade — the `quoteAsset=="USDT"` universe filter now has a capital consequence (router needs CM/USDC universes); promote from one-line finding to a row — B — ledger.
25. Idle margin-wallet USDT — the buffer earns zero by construction; price the flexible-earn-while-collateral option net of availability risk (idle_yield is blind to the margin wallet today) — C — extends idle_yield.
26. Rally replay harness — replay recorder tape through construction (a) vs (b)/(c) to measure actual top-up latency vs liquidation clock; the NOM-class death, priced — B — comparator evidence.
27. Covered-call overlay on the spot leg — Binance options premium as third yield stream; likely fails on options-book depth at seed, falsifier is the measured spread — B — axis watchlist.
28. Multi-venue counterparty split as ruin input — everything sits at one venue (executor's own FTX note); the comparator should carry a venue-concentration term so capital efficiency never silently argues for deeper single-venue concentration — B — comparator requirement.
29. Construction-per-sleeve register — every future sleeve declares its construction at pre-registration (the way cost_basis is declared), so INHERITED can never recur — B — pre-registration template field.
30. Shadow-on-target-construction — if the comparator verdict is switch, move the *shadow* book to the target construction immediately so forward evidence accrues where live will run; zero capital, kills the restart cost — A — same ledger row as the deep proposal.

Context remains, but the turn must end somewhere: next threads I was about to generate — (31) whether Binance's Multi-Assets collateral list covers the current carry universe's coins at all (the comparator's first empirical input, checkable keylessly), and (32) an inverse-book maker-rebate check on CM alt pairs where thin books sometimes carry richer maker schedules.


## B -- GPT-9 family (independent)

(GPT-9 seat unavailable: HTTPError: HTTP Error 402: Payment Required. This run is SINGLE-FAMILY -- treat its proposal as unconfirmed by an independent family, and note that in the record.)
