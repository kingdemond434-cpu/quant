# Capability hunt 2026-08-19 s3 -- BUILDER record

**Verdict label: SOLO.** The GPT-9 seat returned `HTTPError: HTTP Error 402: Payment Required`
(logged in `data/second_family_log.json`, 2026-08-19T01:39Z), so only the Claude family proposed.
Nothing below is cross-family corroborated (L1.33): the delta between families is normally the
finding, and there is no delta to read. Recorded as a dated, measured fact, not as agreement.

## What the families proposed

**Proposal A (Claude).** A margin-topology comparator: the measured `notional_per_equity` for
every margin construction available at current equity, with the live construction graded DECIDED
vs INHERITED. Its argument: the only deployed sleeve runs spot wallet + separately-margined
USDT-M short -- a construction nobody chose, inherited from connector order -- and every fence
and incident repair since (R0053, R0234, gap rows 19/34) asked "is the accounting right GIVEN the
topology", never "is the topology right". Full text with the 30-item brainstorm:
`docs/research/capability_hunt/20260819_s3_proposals.md`.

**Proposal B (GPT-9).** Unavailable (402 Payment Required).

## Adjudication

With one proposal there is no convergence signal, so Proposal A was judged on its own evidence.
Its structural claims were verified by reading the code, not by trusting the proposal:

| Claim | Verified |
|---|---|
| `run_capital_plan.py` hardcodes `_PM_EFFICIENCY = 1.8`, applies it at EVERY level incl. the $3,846 seed | TRUE (`:36,76`) -- and `pm_steps` itself says PM has an eligibility floor |
| `multiAssetsMargin` appears in the repo exclusively as an equity-accounting hazard | TRUE (`libs/execution/collateral.py`, `binance_live.py:196`, `run_deadman_switch.py:126`) |
| `libs/portfolio/` has no margin-construction model | TRUE -- `construction.py` is sleeve-WEIGHT construction (Sharpe/corr), `capacity_allocation.py` is cross-family capacity |
| Ledger rows on multi-asset/COIN-M/PM are accounting or data-backfill only | TRUE (R0024/R0053/R0234/R0462/R0504 spot-checked; R0462 is the COIN-M *data* backfill, scheduled) |
| The executor pins venue leverage 3 | TRUE (`run_cashcarry_executor` `fut.set_leverage(sym, 3)`, two call sites) |
| `leverage_target.json` carries growth_optimal 2.25 / ruin_cap 2.05 | TRUE (read live) -- both rungs sit ABOVE the inherited construction's 0.75x topology ceiling |
| Novelty: `margin_topology`/`notional_per_equity` absent from code, ledger, vault | TRUE (grep hits only the hunt's own proposals doc) |

## What I built (L1.64)

**A MARGIN CONSTRUCTION NOBODY CHOSE IS A CEILING NOBODY MEASURED.**

- `libs/portfolio/margin_topology.py` -- the pure comparator. One `ConstructionRow` per
  construction: (a) split_spot_usdtm (LIVE, npe = 1/(1+1/L) = 0.75 at the executor's own L=3),
  (b) multi_assets_usdtm (self-collateralised, npe -> 1.0, haircut-dependent), (c)
  coinm_inverse_1x (npe = 1.0 exactly, liquidation unreachable -- the desk's own mined 8btc
  card-31 evidence), (d) portfolio_margin (the deleted 1.8 constant NOT carried forward; terms
  are a dated read or the row is UNMEASURED), (e) bybit_uta (listed, unmeasured, L1.60).
  Grading: DECIDED / DECIDED-STALE (equity doubled, L1.28c) / DIVERGED (decision names X, book
  runs Y -- L1.61 class) / INHERITED / UNMEASURED (a decision against zero measured alternatives
  is paperwork, L1.28a). L1.51 pricing with both CAGR rungs, dollar figures REFUSED on the
  molded paper book, per-$10k rates published instead.
- `scripts/check_margin_topology.py` -> `data/margin_topology.json`. Exits 2 on everything but
  DECIDED, declares `scanned=` at the exit site (L1.57), calls `guard()` (L1.42), declares its
  inputs via `Inputs` (L1.55), records every skipped/stale term (L1.60/L1.44 -- a stale
  collateral table mints no MEASURED rows; an unstamped one is refused, L1.46). `--collect`
  refreshes `data/margin_topology_terms.json` from keyless endpoints, every attempt recorded.
- `scripts/run_capital_plan.py` REWIRED: `_PM_EFFICIENCY = 1.8` DELETED. Efficiency per level now
  comes from the comparator's level table (best MEASURED construction eligible at that level);
  absent artifact falls back to the inherited construction's measured floor (0.75), never to the
  1.8 fiction (L1.28a). `CAPITAL_LEVELS` authority moved to the lib; the plan imports it (the
  two-copies class, L1.61). Dashboard keys preserved (`web/index.html` reads
  `effective_capital_pm` -- the KEY stays, the VALUE is now measured).
- `scripts/report_arming.py`: third cost lever `margin_topology` beside bnb_burn/maker_routing.
- Law in `docs/CONSTITUTION.md` (##L1.64) + `ops/principal_doctrine.txt`; mapping in
  `build_enforcement_matrix.py`; `_GOVERNED` row in `check_build_standard.py`; two
  `ops/crontab.manifest` lines (daily grade 07:29, weekly --collect Mon 06:37) with EVIDENCE +
  CONFIDENCE.
- Tests: `tests/portfolio/test_margin_topology.py` (18) + `tests/governance/
  test_margin_topology_fence.py` (7). The ones that fail if the wiring is removed: the AST test
  pinning the deleted `_PM_EFFICIENCY` assignment and the `CAPITAL_LEVELS` import (substring
  fences are satisfied by comments -- s4 lesson, applied); the regex mirror of the executor's
  `set_leverage(sym, 3)`; INHERITED/UNMEASURED not in `_PASSING`; stale-terms refusal;
  paper-book dollar refusal.

## First run: a defect, which is the success condition

```
margin topology (L1.64): INHERITED -- 2 measured / 3 unmeasured of 5 constructions; book runs split_spot_usdtm
  MEASURED     split_spot_usdtm:   npe=0.75  cov=100%  funding=USDT-M
  UNMEASURED   multi_assets_usdtm: npe=?     cov=?     funding=USDT-M
  MEASURED     coinm_inverse_1x:   npe=1.0   cov=3.8%  funding=COIN-M
  UNMEASURED   portfolio_margin:   npe=?     cov=?     funding=USDT-M
  UNMEASURED   bybit_uta:          npe=?     cov=?     funding=USDT-M (Bybit)
  best alternative: coinm_inverse_1x x1.3333 (liquidation risk FALLS with the raise)   [exit 2]
```

Three measured findings from the first run, each sharpening the proposal:

1. **The universe is 527, not ~139.** COIN-M 1x covers 20/527 bases = **3.8%** of the USDT-M
   carry universe per-name. The +33% multiplier is real but narrow; the construction that could
   carry the whole book is Multi-Assets, and that is exactly the one whose terms are unread.
2. **The Multi-Assets collateral table has NO keyless endpoint** -- both candidates 404, refusals
   recorded in `fetch_log`. The proposal's falsifier said "the comparator's first input is
   reading those terms"; that read requires a keyed or manual dated read, now named as the
   blocking next action rather than assumed away.
3. **Dollar pricing correctly refused**: equity basis is MOLDED-PAPER (pre-Gate-0), so the clamp
   price publishes per-$10k rates only (L1.51). At the current gated-leverage floor (0.25) the
   if-validated rate rounds to ~$0/day -- the honest statement that the topology ceiling BINDS AT
   RAMP, not at the floor; the durable number is the structural multiplier 1.333.
4. **My own instrument overclaimed, and was caught before shipping** (the L1.63 discipline:
   re-read the instrument, not its output). The rewired capital plan's first output showed
   `eff 1.0` at every level -- COIN-M's PER-NAME npe lent to the whole book despite 3.8%
   coverage: the 1.8 fiction swapped for a 1.0 fiction. Fixed before commit: every book-level
   number is now COVERAGE-BLENDED (`blended_npe`: covered slice on the alternative, rest
   inherited), so today's honest book numbers are CM book x1.013 (+1.3%, not +33%) and the
   uplift ranks by BOOK multiplier. The +33% book prize requires the construction that covers
   the whole universe -- Multi-Assets -- which is exactly the one whose collateral table is the
   unmeasured blocker. The measurement re-aimed the prize at its real gate.

## What I refused / did not build, and why

- **No account-mode flip, no wallet migration, no sizing change.** The fence measures and fails;
  the switch is a principal-grade act with a forward-evidence restart (L2.10). KEEP-CURRENT is a
  legitimate verdict of the decision row this fence demands.
- **No new law-family, no new collector organ.** `--collect` lives inside the fence script
  (one governed organ, both duties); PM/Bybit/Multi-Assets terms stay UNMEASURED until a dated
  read exists, and the artifact names that read.
- **Brainstorm routing (30 items):** the strongest are ledgered this run (see below); items 12,
  16, 18, 26, 28 are comparator INPUTS folded into the artifact schema (`transfer_ops_per_month`
  and stress-haircut land with the terms reads, not as separate organs); the remainder stay in
  the proposals doc as governed inventory, re-surfaceable by the next hunt, NOT silently dropped.

## Verification

`ruff` clean; `python -m pytest --co -q` collects; `python -m mypy` clean; 25/27 -> 27/27 new
tests green after two instrument fixes (a `relative_to` crash on tmp paths; the substring fence
tripped by its own docstring -- both fixed before commit, not after); `build_enforcement_matrix`
0 orphans; `check_build_standard` passes with the new `_GOVERNED` row; `check_constitution_core`
intact (L1.64 appended outside the sealed clauses); `check_timidity_language`,
`check_scheduler_manifest`, `check_law_families` green. (Numbers from the gate run recorded in
the commit that lands this file.)

## Ledger rows (this run)

- The INHERITED finding itself + decision routing (owner: principal-adjacent; KEEP-CURRENT legitimate).
- Multi-Assets collateral-table dated read (the blocking UNMEASURED input; keyed/manual).
- Funding-book router USDT-M vs COIN-M per name (brainstorm 1; consumes R0462's backfill when it lands).
- Bybit UTA terms read as construction (e) (brainstorm 2).
- Shadow-on-target-construction (brainstorm 30; kills the forward-restart cost if the verdict is switch).

## Next in this seam, unwritten

(a) The comparator prices the SPLIT construction's top-up/orphan tax at zero until the executor
journal meter (brainstorm 12) exists -- its npe=0.75 is therefore an UPPER bound on the inherited
construction, stated in the row's basis. (b) Quarterly-delivery carry (brainstorm 4) is a
different EDGE, not a construction -- it belongs to the axis watchlist, not this law. (c) When
R0462's COIN-M funding backfill lands, `funding_book_bps` per name becomes measurable and the
3.8% coverage stops being the binding objection for the names it does cover (§42: too small for
funds is the desk's named ground).
