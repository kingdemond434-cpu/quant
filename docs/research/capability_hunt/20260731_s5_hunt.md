# Capability hunt — 2026-07-31 slot 5 — BUILDER record

## Family verdicts (L1.33)

- **Claude family (Proposal A):** consumption-time freshness contracts — every decision-path
  read declares its max tolerated age at the read site, feeding a self-building registry joined
  into a blast-radius verdict. Deep proposal + 20-line brainstorm. Full text preserved in
  `20260731_s5_proposals.md` (committed).
- **GPT-9 family (Proposal B):** seat UNAVAILABLE (HTTP 400). **This run is SINGLE-FAMILY and
  the verdict is SOLO** — explicitly not "confirmed"; one family's agreement with itself is not
  cross-family corroboration and must never be cited as such. The seat outage is a dated,
  logged fact (`data/second_family_log.json` pattern).

## Adjudication

No convergence test was possible (one family). Judged on L2.7: the proposal carries five
already-hand-found instances of its class from the desk's own record — the strongest evidence a
missing capability can have short of convergence — plus a live money-path compound (guard death
= fail-open + unevaluated stage demotion + a KILL file only the dead guard could write, with
nothing paging on its age). Every load-bearing claim was verified against source before
building: `_structurally_bleeding`/`_rt_bps` had no age check, `_refresh_guard` fail-opens by
documented design, `run_alerts._checks()` had no live_guard age condition, and no
`read_fresh`/freshness helper existed anywhere (novelty grep clean). Change window verified
**OPEN** (pre-launch) before touching the executor.

## Built (commits `97967d6`, `2935d03`)

1. **`libs/ops/fresh.py`** — `read_fresh(path, max_age_h, caller=, kind=, guardian=, mode=)`.
   Content `generated` outranks mtime (deploys lie fresh); `kind='state'` judged by GUARDIAN
   liveness, never own age; caller names its degrade direction; `strict` raises `StaleRead`.
   Registry `data/freshness_contracts.jsonl` self-builds from reads, marker-throttled
   (contracts 6h, stale events 15min) exactly like `lawful.guard`'s TTL trick. Recording is
   best-effort by design (telemetry must never break a money-path read); a silent registry
   surfaces at the fence as UNMEASURED.
2. **`scripts/check_freshness.py`** — the L1.44 fence. Verdicts FRESH / STALE-CONSUMED (fails,
   caller named) / STALE-UNREAD (reported, producer fences own it — no double-fire) / MISSING /
   FOREIGN (test-leak guard). Statuses STALE-CONSUMED / UNWIRED / UNMEASURED all exit 2; zero
   contracts can never read OK. Wiring-regression check on the bootstrap sites (law surface,
   checked against the repo; state surface checked against the root — the same law/state split
   that keeps state fences out of commit gates). Feeds `run_max_push` at
   `money_path_correctness` leverage (1.00) via `_from_freshness()`.
3. **Executor wiring** (4 sites): `_structurally_bleeding` (48h; stale denylist STILL DENIES),
   `_rt_bps` (48h; stale measured cost may only TIGHTEN — `max(measured, default)`, closing the
   optimistic direction where a stale "cheap" reading admits opens),
   `_refresh_guard` (0.25h — the existing 900s inline rule, now a recorded contract),
   `_is_live` (state-kind, guardian=`data/live_guard.json` — behavior unchanged, dead guardian
   now visible).
4. **`run_alerts` guard-death page** — `live_guard_dead` (>15min = 3 missed 5-min ticks) and
   `live_guard_missing`, content-generated over mtime, consequence spelled out in the page text.
   The hunter's highest-urgency partial, shipped in full.
5. **Law on both surfaces** — CONSTITUTION **L1.44** (with anti-timidity reading inline:
   throttles nothing, can only demand fresher inputs; the symmetric half — a provably-too-tight
   contract is loosened on evidence, never by muting the fence) + doctrine paragraph (family
   REACHING). `validation_honesty` family += L1.44. Enforcement matrix += L1.44 (65 principles /
   72 fences, 0 orphans). `_GOVERNED` += check_freshness (build standard 28/28). Manifest line
   (hourly :52, data-arrival bound per L1.28c, evidence+confidence) + installed in the live
   crontab (scheduler-manifest: OK, drift closed same session).
6. **`check_calibration` `generated` stamp** — brainstorm #2 verified TRUE live (the artifact
   had no content stamp = age-uncheckable, the exact class the law names) and fixed in-run.
7. **`.bak-20260716` disposition** (brainstorm #16) — **the verify-first caveat caught a trap**:
   5 of 7 blobs existed NOWHERE in git history (`git log --find-object`); naive deletion would
   have irreversibly destroyed the pre-hardening originals `institutional_knowledge.md` cites.
   Archived those 5 into `docs/archive/pre-hardening-20260716/`, deleted the 2 duplicates,
   updated the reference.

## First-run evidence (the L1.41 bar: expect a fence to fire)

- First run, empty registry: **UNMEASURED, exit 2** — correct refusal, not OK-on-absent-input.
- End-to-end drive (the real executor functions, real artifacts): 4 contracts self-recorded;
  forensics 9.26h/48h, cost_model 9.24h/48h, live_guard 0.015h/0.25h, stage_state judged by
  guardian (0.015h) **not** its own 354h age. Fence re-run: **OK — 4 FRESH**, exit 0.
  `_refresh_guard` end-to-end returned the guard's live restrictive command
  (`size_frac 0.0, limit_only`) — semantics preserved exactly.
- 15 new tests; `tests/governance/` + `tests/test_hedge_and_risk.py` = 216 green. Constitution
  seal intact after the L1.44 edit (5 clauses verified); law guard green; timidity fence green
  (L1.44 classified via inline anti-timidity reading); law families 6/6.

## Refused / resolved without building

- **#19 funding-snapshot contract** — verified: the executor takes funding via
  `libs.data.crypto_source.current_funding` (live fetch, no file read). No gap; no row.
- **#4 promotion-date ledger** — already rowed previously (see `check_replacement_rate.py`
  docstring); duplicate row would burn triage.
- **#18 stale-read telemetry as research data** — built into the registry by construction
  (every stale event is a dated record); no separate build needed.
- **#9 deploy-epoch mtime guard** — the content-`generated`-first rule closes the main hole;
  the remainder is generalized as R0168 (stamp `generated` into every fence artifact).

## Ledger (L2.3 — nothing recommended is forgotten)

Implemented: R0153 (main build, 97967d6), R0154 (guard page, 97967d6), R0155 (calibration
stamp, 97967d6), R0156 (.bak archive, 2935d03). Scheduled with enforced dues, sequenced by ROI
against the measured repair-mode service rate: R0157 bias-consume + R0161 flock-skip (08-03);
R0158 keep-last-good denylist + R0166 silent-except sweep + R0169 guard-affirm inversion
(08-04); R0159 min-content + R0163 NAV-48h + R0168 generated-sweep (08-05); R0160 forensics
out of web/ + R0164 two-writer audit (08-06); R0162 schema-version + R0165 state-file
enumeration (08-07); R0167 registry absorption + blast-radius liveness (08-08).

Blind spot logged (origin **self** — the desk's own organ found it, not the principal).
Research memory: `rm-20260731T175614-ffc3dc` (construction/validated).
**L1.29 forecast logged:** p=0.7 that `check_freshness` reports a real (non-test)
STALE-CONSUMED or STALE-UNREAD contract by 2026-08-14 — the fence grades its own yield.

## For the next run

- The Claude hunter named its resume points: (a) does `run_cadence.py`'s stage-gated firing
  mis-fire on the 354h stage file; (b) which of the 23 PROPOSE-state wiring scripts are
  freshness producers whose absence already breaks a contract; (c) does the Holm snapshot
  consumer re-read per-evaluation or cache per-day. Generation resumes there.
- Non-executor consumers (max_push queue readers, Holm snapshot, run_cadence stage read) are
  NOT yet contracted — the bootstrap covered the money path first. R0167 absorbs them with the
  registry unification.
- GPT-9 seat was down for this slot; when it returns, this capability is a natural CONTESTED
  probe (ask the second family what the freshness design missed).

## Post-record addendum (same session, merge events)

- Origin diverged twice mid-session (concurrent VPS lineage). Merged both times; the
  recommendation-ID allocator race hit both merges: their R0149/R0150/R0151 collided with rows
  our lineage had already allocated. Resolved by union — their rows preserved verbatim as
  **R0170/R0171/R0173** with `renumbered_from` markers, zero references needed patching
  (verified by grep on their changed files). Their allocator-race fix
  (`test_recommendation_allocator.py` + `recommendations.py`) is now merged in.
- The merge surfaced a pre-existing red on BOTH parents: `test_upbit_single_source` — the R0060
  look-ahead leak had TWO surviving copies (`fusion_engine.py`, `signal_halflife.py` keying the
  Upbit OPEN stamp, ~15h look-ahead; one printed a contaminated kimchi row during the refutation
  audit). Fixed in-session per L1.40 (found bug = fixed same run): both now import
  `upbit_daily_close_keyed`. Rowed and disposed as **R0172** (implemented). Governance suite
  133/133 green post-fix.
- Final state pushed and verified: origin/master `3ac76b3`, 0 ahead.
