# Panel rulings -- cross-run finding memory (the panel's graveyard)
Updated 2026-07-25. CHECK THIS BEFORE triaging new panel output: a finding matching a prior REJECT with no NEW evidence is already ruled -- skip it, log 'already ruled', do not re-litigate. Supersede a ruling only with new code/market evidence.

## REJECTED (7)
- [nvidia (nemotron-3-ultra)/audit] proposed a one-time Tier-3 exception (bounded auto-reset flag read by the dead-man at startup) to avoid indefinite flat-book if paging stays broken and the principal is unreachable -- HARD REJECT per protocol: any change to scripts/run_deadman_switch.py requires explicit principal sign-off, full stop -- an autonomously-authored auto-reset mechanism is exactly the kind of self-authorized rail-loosening the Tier-3 rule exists to prevent, regardless of how bounded the proposal sounds. Noted for principal awareness only, not queued
- [qwen (qwen3-max-thinking)/micro_audit] run retrospective calibration on 63 historical predicted decisions now for immediate EV-gate refinement -- PREMATURE: verified oldest decision-ledger entries are ~15 days old (earliest 07-04), below the 30-day maturity floor decision-outcome-scoring requires; scoring them now would fabricate resolved-looking outcomes for still-open bets, a calibration-honesty violation (NEVER CERTIFY COMPLETENESS discipline)
- [deepseek (deepseek-v4-pro)/micro_audit] deploy $100 real capital to Binance mainnet within 24h via a thin adapter to start the live-fill clock now -- HARD REJECT: directly violates the 2026-07-18 execution-lockdown Gate-0 sequencing (connector must clear property/mutation testing + failure injection + breaker report before any live capital); todays dead-man fire on TESTNET is itself live evidence for why that gate exists -- rushing live capital before the reliability bar is cleared is the exact failure mode the lockdown prevents
- [google (gemini-3.1-pro-preview)/micro_audit] decision-outcome-scoring cadence mathematically locks the desk in a 10-month structural freeze -- false premise verified against scripts/run_cadence.py:222 (28-day CHECK cadence, not one-row-per-month) -- ~100 ledger entries from 07-04 cross the 30-day maturity floor together in early August, so >=10 resolved rows is achievable in one scoring pass ~08-03, not 10 months out
- [multiple(2/3: grok,openai-gpt5.6-terra)/micro_audit] HFTUSDT concentration breach (~3.7x the 35% cap) remains unresolved/decision-pending; deferring capital-utilization fix while the breach is open is simultaneous ruin-risk and forgone growth -- force-trim now -- FALSIFIED against code: HFTUSDT was already closed 2026-07-17T14:03:56Z (data/cashcarry_trades.json, net +$21.71), swept up in the dead-man fire #3 flatten -- before this audit's 07-18T08:01Z run. The audit dossier evidently carried the stale GAP#15 gap-register text (still said decision-pending as of 07-16) rather than a live position check. Verified 07-18: book has 6 positions, largest 26.0% of $1,740 deployed notional, none over the 35% cap. GAP#15 marked closed in docs/GAP_REGISTER.md.
- [moonshotai (kimi-k2.6) + deepseek (deepseek-v4-pro)/tier1] hard monthly cap of 3 pre-registered generation tests -- superseded by principal's 2026-07-17 throughput amendment (ledger 2026-07-17-throughput-amendment-and-connector-spec) which replaces numeric change/test caps with complexity budget + independence gate + auto-revocation
- [moonshotai (kimi-k2.6)/tier1] permanently retire/delete the dynamic-leverage optimizer, replace with deterministic hard-coded sizing -- over-correction: 07-16 incident already mitigated by the executor clamp (scripts/run_cashcarry_executor.py _dynamic_capital, verified in code -- can de-risk below but never lever above operator capital). Permanent deletion forecloses the Dynamic Leverage doctrine + AGGRESSION CLAUSE. GAP#14's root-cause-then-30-day-gated-reenable is the proportionate fix, already adopted and brain-owned.

## IMPLEMENTED (5)
- [multiple(4+/12: grok,openai-terra,qwen,glm)/audit] CRO proposed fix direction ("track legs via executor authoritative position state") destroys dead-man independence and trades false-positive risk for false-negative risk -- CONFIRMED, sound risk-asymmetry argument consistent with the Tier-3 independence rationale already in institutional_knowledge.md. Fix direction in GAP register row 34 corrected to pure venue-native valuation (all spot balances, no executor coupling) + quiescence/plausibility bounds. Still principal-gated (Tier-3), not built this cycle
- [multiple(12/12)/audit] CRO first-pass diagnosis ("~$1.8-2.6k gap = modest execution slippage, no catastrophic loss") is wrong/premature -- the gap is 36-52% of high-water and unattributed to any specific fill/fee/funding record -- CONFIRMED near-unanimous, high-quality adversarial pushback with a concrete falsification path (double-entry reconciliation). CRO diagnosis in GAP register row 34 corrected same cycle; reset gated on reconciliation, not just pager delivery. Logged to data/PRINCIPAL_ACTION.md
- [multiple(3/3: llama,openai-gpt5.6-luna,cohere)/micro_audit] leverage-optimizer gate is structurally weak (confidence>0 only); recommend hard leverage/notional cap independent of optimizer confidence -- already fixed 2026-07-16 (same day as the incident, before this 07-17T08:01 audit ran): verified in code at scripts/run_cashcarry_executor.py:106-124 (_dynamic_capital) -- min(optimizer_notional, operator_default), can de-risk below but never lever above operator capital, regardless of confidence. The auditors' dossier context did not make the shipped fix visible; no further action needed. GAP#14 (root-cause the confidence-jump itself + design the >=30-live-day re-enable gate) remains open and brain-owned.
- [grok (x-ai/grok-4.5)/tier1] brain must rule whether 07-13 dead-man incident/restart contaminates the deployed forward-shadow clock -- ruled NOT contaminated -- verified in scripts/run_cashcarry_shadow.py that the shadow clock computes forward returns from the market-data funding/basis panel via cashcarry_returns(), fully decoupled from the live executor's operational state; the 3-day flat book affected only the informational web/portfolio.json deployed-equity tracker, not the promotion gate. Ledgered 2026-07-17-shadow-clock-contamination-ruling.
- [grok (x-ai/grok-4.5)/tier1] crowding/capacity decay monitor with automatic Kelly haircut on funding compression -- matches engineering_backlog top-ROI open item carry_crowding_monitor (roi 0.153); implemented this cycle 2026-07-17

## QUEUED (6)
- [multiple(~12/12)/audit] single-channel ntfy alerting is confirmed insufficient even after the encoding fix -- needs second independent channel + delivery-confirmation canary + external liveness watcher -- Real gap, alerting infra not risk-path. Logged as GAP register row 38, queued high-priority alongside row 37 as the two top items from this incident
- [multiple(8+/12)/audit] orphan-cover reconciler path is an unbounded, unauthenticated market-order mechanism -- needs confirm-window, notional cap, non-market execution, cooldown -- Real gap, risk-path (executor code, not Tier-3), independence-gated -- no other risk-path change this cycle so no conflict, but too large to build same-cycle alongside the incident response. Logged as GAP register row 37, queued high-priority
- [google (gemini-3.1-pro-preview)/micro_audit] recorder tracks perp-only, missing spot pairs, invalidating the pre-live cost model for a spot+perp delta-neutral desk -- VERIFIED against code: scripts/run_recorder.py _SYMBOLS is futures-only, no spot depth/trade collection anywhere. Real gap, additive fix, no risk-path touch. Logged as GAP register row 35, queued for next recorder-upgrade slot (engineering budget this cycle went to the pager fix + incident autopsy)
- [google (gemini-3.1-pro-preview) + moonshotai (kimi-k2.6)/tier1] venue-truth disconnect circuit breaker: |mark_NAV - exchange_NAV| > 1-2% triggers halt+flatten -- genuinely new (2/11 corroborated), not yet in gap register -- added as GAP#19. Deferred from same-cycle implementation: risk-path code needing a dedicated build with property/mutation testing (v8 8.2 bar), architected to NOT duplicate/race the Tier-3 dead-man rail (two-writers-on-one-rail was the 07-11 false-fire root cause) -- a books-vs-venue RECONCILIATION check, distinct from the dead-man's drawdown-from-HW SURVIVAL check.
- [multiple(7/11)/tier1] build TCA / recalibrate execution cost model from realized fills -- matches GAP#4 + engineering_backlog execution_tca_fill_log (roi 0.128), data-gated on ~2wk fills
- [multiple(8/11)/tier1] ship staged-arming live connector + $100 canary before ~08-05 gate -- matches GAP#2 (docs/LIVE_CONNECTOR_SPEC.md frozen 2026-07-17); already queued, no new action

## FLAGGED (11)
- [nvidia (nemotron-3-ultra:free)/audit] Shrunk-Kelly S^2/(S^2+SE^2) ignores selection/multiplicity bias; add a bias term B (e.g. from DSR) to the denominator -- Sizing formula is frozen/post-Gate-0. The gauntlet's DSR already deflates for multiplicity at validation; whether the forward SIZING term should double-count that needs the full roster + live calibration rows, not a degraded free-seat pass. Queued for post-freeze sizing review.
- [nvidia (nemotron-3-ultra:free)/audit] Regime-evidence fast-track gate can deadlock in a structurally low-funding-vol regime; proposes replacing the hard regime gate with a regime-aware sizing HAIRCUT to promote at day 40 anyway -- DEGRADED free-seat run. Verified vs code: GAP #1 already tracks the fast-track clock (NW-t=2.5 today, regime_ok False). Relaxing the regime gate into a haircut effectively LOOSENS a validation gate -- HARD line (contract point 5: never relax validation-gate strictness). Also structural/frozen under EXECUTION LOCKDOWN. Not acted on; re-evaluate only on full funded roster + post-Gate-0.
- [ai (z-ai/glm-5.2)/audit] three equity measures (shadow Sharpe 15.7, mark-based -0.73pct, venue-truth -36pct) disagree catastrophically; shadow structurally cannot see venue-truth friction and could inflate Kelly sizing when regime_ok flips true -- Legitimate, ties to GAP #14 (leverage pipeline root-cause, already open) and the row-34 reconciliation. Not actionable until the reconciliation (row 34) and GAP #14 forensics converge -- flagged for that work, not a new standalone item
- [google (gemini-3.1-pro-preview)/audit] shrunk-Kelly shadow-N contamination risk: live execution drag will lag into the pooled SE while shadow N stays optimistic, oversizing into structural live decay -- Legitimate statistical concern for the testnet-to-live transition, not actionable pre-Gate-0 (no live data yet to compute a realized_slippage_penalty from). Flagged for the connector/Gate-0 build spec and the >=30-live-day re-enable gate work (GAP #14)
- [qwen (qwen3-max-thinking)/micro_audit] blind spot: no cross-regime stress-testing protocol for queued POST_GATE0_MANIFEST entries before activation -- legitimate question, flagged for next monthly governance / post-Gate0 activation review, not actionable while frozen
- [qwen (qwen3-max-thinking)/micro_audit] biweekly digging cadence cap rejected on estimated time constraints without time-tracking evidence -- same finding as deepseek above, merged -- see GAP register row 36
- [deepseek (deepseek-v4-pro)/micro_audit] blind spot: no quantitative decay/capacity model for the single deployed carry strategy -- legitimate question, flagged for next monthly governance (edge-decay laboratory, GAP register row 24, already covers this)
- [deepseek (deepseek-v4-pro)/micro_audit] biweekly digging cadence diverts brain cycles from the connector critical path, an unjustified conservatism in deployment speed -- VERIFIED the underlying cost basis (~90 min/day) is a stated estimate not an instrumented measurement (ledger 2026-07-18-digging-cadence-biweekly); the existing reversal_condition is real but currently unverifiable except by subjective judgment. Logged as GAP register row 36 (add time-tracking instrumentation), not urgent -- todays actual slippage was incident-caused not digging-caused
- [google (gemini-3.1-pro-preview)/micro_audit] blind spot: no cross-sleeve correlation/covariance discipline as new research organs mine independent mechanisms -- legitimate governance question, flagged for next monthly kill-committee/tier-convergence review, not actionable same-cycle
- [ai (x-ai/grok-4.5) + openai (gpt-5.6-terra)/micro_audit] blind spot: no schema-contract/freshness-SLA/drift checks on the newly-live recorder + crowding.json feeds; can a deterministic replay reproduce the HWM/latch-equity/rebaseline figures from raw retained events? -- legitimate data-lineage question, not urgent (recorder v1 is 1 day old, no promotion path yet depends on it). Logged to GAP register for a future cycle rather than actioned same-day -- not a survival-risk item.
- [multiple(9/11)/tier1] Frankenstein synthesizer likely producing zero measurable improvement; recommend 30-60 day falsification test -- strong cross-model consensus; not actioned this cycle (not the monthly governance window, cycles_logged=28/~30) -- flagged for next monthly governance review item 6
