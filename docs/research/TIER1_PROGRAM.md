# Tier-1 programme ledger

Updated 2026-09-08 · 113 items · generated by `scripts/check_tier1_program.py --render` (do not edit by hand; edit the JSON).

Status vocabulary: EXISTS-LIT = code runs on a named clock and its artifact has a consumer; EXISTS-DARK = code exists, no clock or no consumer; PARTIAL = part exists (gap named); MISSING = nothing; LANDED = built in this programme, with commit.

## Acceptance properties

- **AP1 No fixed discovery frontier** — MISSING. Measure: A new asset, mechanism, feature, dataset, language, model class, execution idea or research method reaches the docket without a code change: count of frontier cells opened per week by the system itself (hunt_frontier vectors, universe registry additions, data_universe_map entries, new families registered) vs by a human session.
- **AP2 No unmeasured intelligence** — MISSING. Measure: Every LLM seat, miner, dataset, generator and infrastructure change has a row with incremental survivors and incremental robust E[log W] attributed to it (compiled artifact per_source/seats, hunt_frontier yield, compute_ledger cost); anything without a row is EXISTS-DARK by definition.
- **AP3 No semantic breaks** — MISSING. Measure: A hypothesis id can be followed deterministically source -> compiler row -> gauntlet cell -> certificate -> forward clock -> allocation fraction -> intent -> broker order -> position -> deal -> realized R -> lesson/graph update; measured as the share of closed strategy deals with a complete chain (target 100%).
- **AP4 The portfolio creates research missions** — MISSING. Measure: The allocator's unfilled heat and the book's missing exposures (session, regime, factor beta, horizon, drawdown states) are written as missions that generators consume; measured as missions issued per week and candidates tagged with the mission that caused them.
- **AP5 The machine improves the machine** — MISSING. Measure: Generators, validators, schedulers and allocation methods run as A/B arms with equal compute and the inferior arm is retired by survivor yield / realized Elog; measured as the number of arms with a recorded verdict.

## Census

| phase | EXISTS-LIT | EXISTS-DARK | PARTIAL | MISSING | LANDED |
|---|---|---|---|---|---|
| 0 Close the truth loop: attribution, deadman, stable adoption, node separation, durable jobs, data freshness, research lineage | 1 | 2 | 13 | 1 | 5 |
| 1 Remove the research throughput ceiling: worker queue, multi-fidelity screening, EVSI scheduler, research DAG, scalable forward clocks, content-addressed cache, experiment DB | 1 | 1 | 8 | 0 | 0 |
| 2 Maximum breadth: the eight generators (LLM mechanism, symbolic, evolutionary, RL, residual, regime, causal, execution) | 1 | 1 | 20 | 2 | 0 |
| 3 Alpha knowledge graph: research, failures, mechanisms, strategies, data, correlations, certificates, live results as one memory | 0 | 0 | 8 | 0 | 0 |
| 4 Adversarial scientific loop: falsifier, replicator, leakage prosecutor, statistics prosecutor, synthetic nulls, positive controls | 0 | 2 | 12 | 0 | 0 |
| 5 Adaptive capital brain: regime posterior, decay posterior, joint scenarios, tail dependence, state-dependent Elog, execution-cost prediction, contextual allocation, H <= 20% | 0 | 0 | 16 | 3 | 0 |
| 6 Execution intelligence: routing competition, slippage prediction, fill probability, self-footprint, broker microstructure | 0 | 3 | 2 | 0 | 0 |
| 7 Recursive research improvement: agents compete for compute by downstream economic value; the machine redesigns itself | 1 | 2 | 8 | 0 | 0 |
| **all** | 4 | 11 | 87 | 6 | 5 |

## Items

### Phase 0 — Close the truth loop: attribution, deadman, stable adoption, node separation, durable jobs, data freshness, research lineage

- **I1 Scheduling architecture as-is (VPS timers, box tasks, hourly_cycle legs, crontab live vs RETIRED, duplicated organs)** — PARTIAL
  - gap: There is no single machine-readable schedule graph across the two machines: the box manifest carries 14 UNDECLARED triggers and contradicts its own installer for MT5-Gauntlet, the cron plane is decommissioned but still holds 158 rows, and no check reports the same organ scheduled on two schedulers (four schedules exist for run_frontier_rotation.sh alone).
  - ops/*.timer — 82 committed timer units; ops/*.service — 87 committed service units (MEASURED, `ls | wc -l`)
  - ops/crontab.manifest — 3,438 lines; 158 uncommented 5-field cron rows; 86 uncommented `SYSTEMD` rows (83 `.timer` + 3 `.service`); 109 `# RETIRED` headers, 0 `# DOMINATED` (MEASURED)
  - ops/crontab.manifest:1-8 — the whole cron plane is headed '*** DECOMMISSIONED 2026-08-23: the box this manifest targets (Hetzner, 95.216.191.70 ...) is fully torn down' (MEASURED)
  - ops/crontab.manifest:12-16 — 'RECONSTRUCTED FROM IN-REPO EVIDENCE 2026-07-29 -- NOT yet verified against the live box ... this manifest is the DR FLOOR, not ground truth' (MEASURED)
  - clock: VPS: 82 systemd user timers (ops/*.timer) + scripts/run_manifest_dispatch.py every 5 min firing 177 allowlisted manifest rows. Box: 26 desks/mt5/ops/box_tasks.manifest tasks, of which desks/mt5/scripts/Install-QuantWindows.ps1 registers 14; desks/mt5/research/hourly_cycle.py runs 59 legs per hour under MT5-Hourly. · artifact: ops/crontab.manifest, desks/mt5/ops/box_tasks.manifest, desks/mt5/reports/BOX_TASKS.json, data/scheduler_manifest_report.json, desks/mt5/data/compute_ledger.jsonl · consumer: scripts/check_scheduler_manifest.py, scripts/check_box_tasks.py, scripts/check_unit_parity.py, scripts/run_manifest_dispatch.py, deploy/reconstitute_cron.sh
  - next: Add a duplicate-organ check to scripts/check_scheduler_manifest.py that groups all live rows (cron + SYSTEMD + box_tasks + hourly_cycle `_costed` names) by executed script and fails on any script with >1 live schedule that does not share one flock/job_lock name — the (d) check at scripts/check_scheduler_manifest.py:19-21 already does exactly this for cron lines only.
  - landed: 7264e420 desk-sync-clean and the desk branch converged after two days of silent aborts, 738632ff refresh_status.json published every tick
- **I5 SLOs (research latency, data freshness, forward utilisation, capital pricing, attribution, deployment SHA)** — PARTIAL
  - gap: Only two of the six SLO families are stated as numbers with a floor (data freshness per artifact, deployment SHA equality); research latency has no target at all, and capital pricing and execution attribution are measured only as UNMEASURED/0.
  - RESEARCH LATENCY SLOs (<1h start, <10m screen, <24h gauntlet): ABSENT. No numeric latency target exists anywhere; the nearest artifact is desks/mt5/reports/RESEARCH_PRODUCTIVITY.json (2026-09-04T20:18) which reports stage COUNTS and `bottleneck: "deepening"` with `deepening {queued: 882, decided_total: 0, backlog: 882}` and `conversion.intake_to_compiled: 0.0` — throughput, never latency (MEASURED)
  - DATA FRESHNESS: MEASURED and published two ways. Producer side, scripts/check_job_manifest.py:59-101 gives each of 16 artifacts a max_age_hours (0.5h for web/desk_state.json, 3.0h for shadow states, 26h for decay_live/forward_reconcile). Consumer side, scripts/check_freshness.py:5-18 joins {artifact age × consumer contract × recent read events} from data/freshness_contracts.jsonl and returns FRESH / STALE-CONSUMED / STALE-UNREAD (MEASURED)
  - FORWARD ORPHAN CLOCKS: scripts/diagnose_forward_slots.py:50 — 'A slot whose owner cannot be named is reported as ORPHAN' -> web/forward_slot_diagnosis.json. It DIAGNOSES AND NEVER RECLAIMS (:19). No `orphan_clocks == 0` target is asserted anywhere (MEASURED)
  - UTILISATION: scripts/check_utilisation.py:1-21 measures every declared ceiling against its limit and treats UNMEASURED as zero, but declares no >95% number; scheduled at ops/crontab.manifest:245 (`27 */6 * * *`) and allowlisted in scripts/run_manifest_dispatch.py (MEASURED)
  - clock: quant-job-manifest.timer (*:07/30); check_freshness + check_utilisation via quant-manifest-dispatch.timer allowlist; quant-live-readiness.timer (*:45); quant-desk-refresh.timer (180s) · artifact: data/job_manifest.json, data/JOB_MANIFEST_ALARM.txt, web/forward_slot_diagnosis.json, desks/mt5/data/RELEASE.json, desks/mt5/reports/GROWTH_ATTRIBUTION_WEEKLY.json · consumer: web/desk.html via web/desk_state.json; scripts/check_live_readiness.py; libs/ops/release.verify() in the gateway
  - next: Add three latency rows to the JOBS table in scripts/check_job_manifest.py:59 — time-to-first-screen, time-to-gauntlet-verdict and time-to-certificate, computed from the timestamps already in desks/mt5/data/hypothesis_graph.jsonl — so research latency joins the one alarm that already has a consumer.
- **I6 Observability graph: one clickable SOURCE→…→P&L path; what web/desk_state.json publishes today** — PARTIAL
  - gap: The page is a set of flat counters with no edges: SCREEN, PROMOTER, ALLOCATOR, BROKER and P&L are not stages in the JSON at all, seven of the funnel counters read null or 0, and nothing on the page is clickable, so no reading can be walked backwards to the hypothesis or source that produced it.
  - web/desk_state.json has 16 TOP-LEVEL KEYS: generated_at, identity, account, research, shadow, execution, issues, health, equity_curve, disclaimer, stall_watch, readiness, breadth, stats, pipeline, decay (MEASURED)
  - FUNNEL COUNTERS live in `pipeline` (18 keys): docket_candidates 23465, discovered_backtested 147, certified null, certificates_unrunnable 0, certified_gate_failed null, forward_observations 0, historical_observations 0, forward_clocks 0, promotion_ready 0, promotion_ready_names [], live 0, forward_detail [], gauntlet_last_judged null, gauntlet_last_unmeasured null, census_basis 'UNAVAILABLE (reports/UNIVERSAL_SURVIVORS.json did not reach this host, so no ten-gate census could be taken)' (MEASURED)
  - The `research` block is entirely null: candidates_tested null, historical_survivors null, canonical_survivors null, gate_failures {}, survivors [] (MEASURED)
  - `readiness: {status: "UNMEASURED", blocking: ["readiness has not been assessed"]}`; `issues: {}`; `breadth: {}`; `equity_curve: []` — four empty or unmeasured blocks (MEASURED)
  - clock: hourly_cycle leg `publish_state` (desks/mt5/research/hourly_cycle.py:1304); quant-desk-refresh.timer every 180s (ops/refresh_desk_state.sh:57); box task MT5-DeskState (desks/mt5/ops/box_tasks.manifest:83, trigger UNDECLARED) · artifact: web/desk_state.json · consumer: web/desk.html, scripts/check_job_manifest.py (freshness), ops/pull_desk_state.sh
  - next: In scripts/build_zentech_state.py, emit a `trace` array joining the top N pipeline rows to their `source`/`parent`/`fate` from desks/mt5/data/hypothesis_graph.jsonl — the file is already on disk and the builder already opens the desk data directory.
- **I7 Point-in-time data lake (bronze/silver/gold, event_time/available_at/ingested_at/revision_id/source/license/quality)** — PARTIAL
  - gap: The PIT schema is defined (5 fields, revision_of, a sealed high-water mark) and applied to 1 row in 4,769 — 0.02% — with event_time at literally 0.0 and zero revisions ever recorded; bronze/silver/gold does not exist and desks/mt5/data/lake/ is an empty directory.
  - NO LAKE. desks/mt5/data/lake/ EXISTS AND IS EMPTY — zero files, zero subdirectories (MEASURED, `ls -la`). There is no bronze/silver/gold tier anywhere in the tree
  - THE PIT CENSUS IS REAL AND ITS ANSWER IS ~ZERO. desks/mt5/reports/PIT_CENSUS.json (2026-09-05T20:38:40): `total {rows: 4769, stamped: 1, stamped_frac: 0.0002, field_frac {event_time: 0.0, available_time: 0.0002, ingested_time: 0.0002, source_version: 0.0002, payload_hash: 0.0002}, n_revised: 0, max_revision_n: 0}` — ONE stamped row out of 4,769, and event_time is 0.0 everywhere (MEASURED)
  - desks/mt5/data/pit_high_water.json: sealed 2026-09-05T01:38:55, measured 2026-09-05T13:24:55, `stamped_frac 0.0002, rows 4769, stamped 1, sources 44`, 'raised by the gate whenever a census measures higher, NEVER lowered by code' (MEASURED)
  - THE PIT GATE IS NOT ON ANY DESK SCHEDULE: scripts/check_pit.py appears in .github/workflows/ci.yml:129 and nowhere in ops/crontab.manifest, ops/*.service, desks/mt5/research/hourly_cycle.py or desks/mt5/ops/box_tasks.manifest (MEASURED)
  - clock: PIT gate: .github/workflows/ci.yml on push only (NOT a timer). Bars/tape: box tasks MT5-Hourly, MT5-TickRecorder, MT5-TickIntegrity, MT5-TapeFeatures. COT: quant-cot-fetch.timer (Fri 20:30) + quant-cot-zcache.timer (Tue,Wed,Sat 07:35) · artifact: desks/mt5/data/universe/*.parquet, desks/mt5/data/cot*/*.parquet, desks/mt5/data/tape/, desks/mt5/reports/PIT_CENSUS.json, desks/mt5/data/pit_high_water.json · consumer: external_gauntlet / run_external_backtest read the parquet panels; PIT_CENSUS -> pit_high_water ratchet -> CI gate
  - next: Stamp at ingest rather than auditing after: add the five PIT fields in desks/mt5/side_channels/seed_miners.py's row writer so every new discovery row is born stamped — the census at scripts/check_pit.py:63 will then ratchet stamped_frac upward on its own.
- **I8 Data moat inventory vs blueprint list** — PARTIAL
  - gap: The macro/text/positioning half of the moat is broad (67 live source directories) while the market-microstructure half the strategies actually trade is thin: 6 sub-hourly bar files, a 2-file tape directory, no quote/OI/volume data for MT5 instruments, and no execution_quality.json ever produced.
  - MARKET / multi-TF OHLC — PRESENT (narrow): desks/mt5/data/universe/ 117 parquet here / 251 H1 files on the box (web/desk_state.json health.midnight.universe.h1_bar_files 251); only 6 sub-hourly files (4 _M15, 1 _M5, 1 _M1) (MEASURED)
  - MARKET / ticks — PARTIAL: recorders are declared hourly (desks/mt5/ops/box_tasks.manifest:67-69 MT5-TickRecorder / MT5-TickIntegrity / MT5-TapeFeatures) but desks/mt5/data/tape/ holds 2 files in this checkout; desks/mt5/research/hourly_cycle.py:257-262 records the class ('the whole leg returned {"error": ...} ... Ticks cannot be backfilled') (MEASURED / INFERRED for box contents)
  - MARKET / spreads + contract specs + swaps — PRESENT: desks/mt5/data/cost_surface.json; desks/mt5/data/intelligence/broker_swaps/ (744 rows per PIT_CENSUS); box task MT5-ContractTerms hourly (desks/mt5/ops/box_tasks.manifest:78); desks/mt5/reports/SPREAD_PROVENANCE.json (MEASURED)
  - MARKET / quotes (L1/L2 book) — ABSENT for MT5: only desks/mt5/data/tape/depth_probe.json; docs/research/data_provenance.json's moat_depth.jsonl is the RETIRED bybit book (MEASURED)
  - clock: quant-seed-miners.timer (*:22), quant-hourly-discovery.timer (*:35), quant-cot-fetch.timer, quant-cot-zcache.timer, quant-sge-premium.timer, box MT5-Tick* tasks, run_intelligence_cycle via dispatcher · artifact: desks/mt5/data/intelligence/<67 source dirs>/, desks/mt5/data/{universe,cot,cot_tff,cot_disagg,observables,tape}/, data/data_universe_map.json · consumer: desks/mt5/research/miner_candidate_compiler.py (reads data/intelligence/** per CLAUDE.md:97-100), external_gauntlet, run_external_backtest
  - next: Give the tick tape its own freshness row in scripts/check_job_manifest.py:59 (desks/mt5/data/tape/, ~2h limit, consumer=tape_features/execution_twin) so a silent tick recorder is caught by the one alarm with a consumer, instead of only by reading the directory.
- **I18 The five acceptance properties — what the repo can currently MEASURE** — PARTIAL
  - gap: Properties 2 and 3 are the binding ones and both fail at the same place — the money end. With `live: 0`, `matched_fills: 0` and 0 EARNS across 71 modules, no chain can be closed from certificate to P&L, so every downstream property (agent value, portfolio-driven missions, A/B of generators) is measured only as UNMEASURED.
  - (1) NO FIXED DISCOVERY FRONTIER — PARTIAL/MEASURABLE. New GROUNDS and DATASETS can be added by editing registries (desks/mt5/data/deep_forest_sources.json 502 grounds; data/data_universe_map.json 190 sources) and libs/research/hunt_frontier.py:10-14 keeps NAMED_ONLY/BLOCKED as a priority queue rather than an exclusion — but data_universe_map.json has NO PRODUCER (scripts/daily_max.py:101-107), so incorporation is a human/LLM edit, not automatic. New METHODS: libs/research/bandit.py's 11 arms are a hard-coded tuple (MEASURED)
  - (2) NO UNMEASURED INTELLIGENCE — NOT MET, AND THE SHORTFALL IS ITSELF MEASURED. desks/mt5/reports/MODULE_RENT.json: 70 of 71 modules UNMEASURED, 1 NOT_BINDING, 0 EARNS, 0 COSTS. desks/mt5/reports/CAPABILITY_STATUS.json stage_counts: only 7 of 50 nodes reach MEASURED (19 WIRED, 2 RUNNING, 22 DECISION_AFFECTING). desks/mt5/reports/MODEL_ZOO.json entrants 0. data/llm_spend.jsonl does not exist. Per-miner yield IS measured (data/MINER_YIELD_ALARM.txt names 6 zero-yield sources) (MEASURED)
  - libs/ops/capability_graph.py:1038-1054 records that the earlier '7 MEASURED' headline was itself audited and de-credited — 'all ten MEASURED nodes were free passes and not one appeared in any ledger … An absent ledger reads UNMEASURED, never MEASURED (L1.28a)' (MEASURED)
  - (3) NO SEMANTIC BREAKS — PARTIAL. The chain exists in pieces: source→hypothesis in desks/mt5/data/hypothesis_graph.jsonl (16 MB, per-row source/parent/fate); hypothesis→experiment in desks/mt5/data/research_queue.json (20,152 rows with status); certificate→forward in the shadow ledgers; code→fill in libs/ops/release.py:14-35 (release_id stamped on every intent, `accepts()` enforcing running-SHA == sealed SHA). It BREAKS at fill→P&L: web/desk_state.json `execution.matched_fills: 0`, `markout_usable: false`, and desks/mt5/reports/execution_quality.json has `ever_produced: None` (MEASURED)
  - clock: quant-capratchet-auto.timer (*:0/12) for the capability graph; hourly_cycle legs model_skill/model_league; quant-manifest-dispatch.timer for meta_research_review and check_gate_reachability; quant-job-manifest.timer for the artifact/consumer census · artifact: desks/mt5/reports/CAPABILITY_STATUS.json, desks/mt5/reports/MODULE_RENT.json, desks/mt5/reports/MODEL_SELF_IMPROVEMENT.json, desks/mt5/data/hypothesis_graph.jsonl, docs/research/recommendation_ledger.json · consumer: scripts/check_reachability, scripts/check_conversion.py, libs/research/bandit.py, docs/PRINCIPAL_ACTION.md
  - next: Produce desks/mt5/reports/execution_quality.json — the single MISSING artifact with `ever_produced: None` whose named consumers are 'promoter (promotion gate), dashboard' (scripts/check_job_manifest.py:63); its producer already exists at desks/mt5/mt5desk/shadow_execution.py:45 and is on no schedule.
- **A8 Research CI/CD — immutable certificates with full SHA provenance + reproduce command** — PARTIAL
  - gap: The RELEASE carries seven kinds of hash, but the CERTIFICATE carries none of them — a survivor row cannot say which code, data, cost model or universe produced it — and the one command that would re-derive a certificate exists, is scheduled nowhere, and has never written its artifact.
  - libs/ops/release.py:257-281 `_describe()` — the release record carries live_sha/code_sha, parent_sha, tree_sha, config_hash, survivor_registry_hash, allocator_hash, money_path_hash, money_path_files (per-file digests), canon_sha256, immutable_manifest.sha256_16, allocator_certificate, dependency_hash, data_schema_version, seal_rule, non_code, and a derived release_id (MEASURED)
  - libs/ops/release.py:302-310 `seal()` makes the seal a manifest-only commit to escape the chicken-and-egg; SEAL_RULE at libs/ops/release.py:116 (MEASURED)
  - desks/mt5/mt5desk/release_identity.py:272-376 `verdict()` — the box-side enforcement: running SHA vs code_sha, money-path per-file drift, signed judge manifest, canon digest; Identity.allows_new_risk() at line 124 is 'the only question the gateway asks'. Writes desks/mt5/data/release_identity.json (MEASURED)
  - desks/mt5/research/hourly_cycle.py:1300-1301 — `_costed('release_identity', ...)` runs desks/mt5/mt5desk/release_identity.py every hour; the comment at desks/mt5/research/hourly_cycle.py:1293-1299 records that before this it was unscheduled and last written 2026-09-05T23:17 with ok=false and 1,074 paths of drift (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1300 (release_identity); ops/quant-recertify-canon.timer 06:20 (recertify_canon). NONE for desks/mt5/scripts/reproduce_certificate.py · artifact: desks/mt5/data/RELEASE.json, desks/mt5/data/release_identity.json, desks/mt5/data/IMMUTABLE_MANIFEST.json; desks/mt5/reports/reproduction_verdicts.json NEVER PRODUCED · consumer: desks/mt5/mt5desk/decision_core.py reads release_identity.verdict (named at desks/mt5/mt5desk/release_identity.py:86); the gateway refuses new risk on it. No consumer for reproduction verdicts
  - next: Stamp `release_id` and `canon_sha256` from libs/ops/release.build() onto each survivor row at the point desks/mt5/scripts/external_gauntlet.py writes a certificate, so every certificate names the release it was minted under.
- **A15 Alpha discovery latency — structural change -> hypothesis -> certification -> capital** — PARTIAL
  - gap: Three of the four legs are instrumented (hypothesis->certification via research_productivity, certification->clock via axis_clock_registry, shadow->capital via promotion_latency), but the state ledger that would join them end-to-end holds two rows stamped nine milliseconds apart, and the structural-change -> hypothesis leg is measured nowhere.
  - libs/research/promotion_latency.py:1-24 — SHADOW -> LIVE latency decomposed into CLOCK, QUEUE WAIT (the cohort cap MAX_FORWARD_SLOTS=12) and a third component, each with its PROVENANCE attached, explicitly to stop an unmeasured component being quoted as measured. Caller scripts/run_promotion_queue.py:125, scheduled ops/crontab.manifest:200 (`53 */6 * * *`) (MEASURED)
  - libs/research/conversion_velocity.py:103 — 'Days spent traversing each adjacent pair of stages. None when either end is missing.' Consumer scripts/run_wealth_report.py:52-53 ('how long does evidence take to become a position?') (MEASURED)
  - libs/research/alpha_state.py:1-35 — the state machine DISCOVERED -> IMPLEMENTED -> TESTED -> STATISTICALLY_VALID -> SHADOW -> OOS_VALIDATED -> INDEPENDENCE_CHECKED -> PORTFOLIO_VALIDATED -> CAPITAL_ELIGIBLE -> LIVE -> MONITORED, with per-transition evidence keys and an append-only ledger (libs/research/alpha_state.py:156-226) (MEASURED)
  - MEASURED: data/alpha_state_ledger.jsonl contains 2 rows, both for a single alpha_id 'cat|ratio|1h|all|funding|oi', history [['IMPLEMENTED','2026-09-05T02:16:39.816948'], ['TESTED','2026-09-05T02:16:39.825876']] — nine milliseconds apart, so no real latency has ever been recorded
  - clock: ops/crontab.manifest:200 (run_promotion_queue, `53 */6`); ops/quant-forward-clock.timer (every :00,:30); research_productivity via desks/mt5/research/daily_cycle.py:333 (_state_research_feedback) · artifact: desks/mt5/reports/RESEARCH_PRODUCTIVITY.json, data/alpha_state_ledger.jsonl (2 rows), data/axis_clock_registry.json (1 axis) · consumer: scripts/run_growth_audit.py:258,289 reads promotion_latency; scripts/run_wealth_report.py:52 reads conversion_velocity; desks/mt5/research/conversion_ledger.py:8 reads RESEARCH_PRODUCTIVITY
  - next: Have desks/mt5/research/promoter.py append an alpha_state.advance() row (via libs/research/alpha_state.Ledger, libs/research/alpha_state.py:205) at each door transition it already computes in _door_status, so data/alpha_state_ledger.jsonl carries real per-rung timestamps for every promoted sleeve.
- **E1 Attribution chain research_id → … → realized_R** — PARTIAL
  - gap: The chain has two breaks. Downstream (intent→fill) was broken by construction and is being repaired right now in the working tree via position_id/entry_order; upstream is still missing entirely — no research_id, certificate_id or sleeve_id ever reaches the order comment, the intent row, the deal row or the dataset row, and there is no intent_id at all, so the only upstream link is the 31-char sleeve NAME.
  - desks/mt5/data/hypothesis_graph.jsonl — 30,313 BORN rows carrying `id`, `region`, `family`, `params`, `source` (e.g. "fund_playbook:Renaissance:A"), `parent`, `fate` (MEASURED, 16.0 MB)
  - desks/mt5/data/sleeve_registry.json — `sleeves.<key>.identity.sleeve_id` (e.g. "1903a4cc90212b4c29d5") plus `code_hash`, `cost_hash`, `behaviour_hash`, `data_venue` (MEASURED)
  - desks/mt5/research/promoter.py:1278-1284 — the sleeve row written to sleeves.json is `{name, symbol, lot, risk_frac, risk_frac_source, admission, status, promoted_at, shadow_exp, family, side, certificate_drift}`: NO certificate id field (MEASURED)
  - desks/mt5/mt5desk/gateway.py:100 `MAGIC = 341953` — one magic for every sleeve, so magic carries no identity (MEASURED)
  - clock: MT5-Gateway, one pass per minute (desks/mt5/scripts/Install-QuantWindows.ps1:126-131); the markout leg is daily_cycle STEPS "markout" (desks/mt5/research/daily_cycle.py:148-164, 470) reached through hourly_cycle's daily() leg (desks/mt5/research/hourly_cycle.py:1019) · artifact: desks/mt5/data/{decision_ledger,order_intents,live_ledger}.jsonl + desks/mt5/reports/markout.json — ALL ABSENT, and absent from git history · consumer: mt5desk/markout.compute → scripts/build_zentech_state.py:684 → web/desk_state.json; libs/research/decision_dataset.join; research/counterfactual_replay; research/execution_twin
  - next: Add a deterministic `intent_id` and the sleeve's `certificate` key to the row `gateway._record_intent` writes (desks/mt5/mt5desk/gateway.py:648-661), derived from libs/research/decision_dataset.row_id so the intent and the decision share one address; the certificate key must first be carried onto the sleeve row by desks/mt5/research/promoter.py:1278-1284.
  - landed: 14dd6ae9 closing deals carry position_id/entry_order/entry_deal; markout joins on them and slips against the entry fill; attribution_chain.json
- **E5 Counterfactual live ledger** — EXISTS-DARK · gate: box-paste
  - gap: The whole counterfactual apparatus is built, scheduled, and writes artifacts with real consumers — and has produced n=0 on every arm since it was written, because it reads the same decision/intent/deal ledgers that do not exist. Nothing is wrong with the organ; it has never been given an input.
  - desks/mt5/research/counterfactual_markout.py:1-24 — FilterValue(reason) = −(sum of counterfactual R of vetoed brackets), replayed with `replay2` (a second engine, so the counterfactual is not produced by the deciding code); NOT_TRIGGERED excluded, never counted as zero (MEASURED)
  - desks/mt5/research/action_counterfactuals.py:1-40 — for trades TAKEN: `hold` at +1/+6/+12/+24/+48 bars, `opposite` (−r_multiple), `sizing` at 0.5x/1.0x/1.5x/2.0x, each as E[log(1 + h·k·r)] with a t-statistic; verdicts SIZE_UP_EARNS / SIZE_DOWN_EARNS / HOLD_LONGER_EARNS / EXIT_EARLIER_EARNS / AS_TRADED / UNMEASURED (MEASURED)
  - desks/mt5/research/counterfactual_replay.py:1-33 — "entered / skipped / 0.5x / 1.0x / 1.5x / market / limit / delayed / fixed TP / trail / hold / partial"; produces VETO / SIZING / EXECUTION / EXIT / MISSED-TRADE alpha; every alpha signed ALTERNATIVE minus DESK (MEASURED)
  - desks/mt5/research/daily_cycle.py:323-348 — "counterfactual_markout", "action_counterfactuals", "counterfactual_replay", "missed_growth", "execution_intelligence" are legs of `_state_research_feedback`, itself STEPS entry "state_research_feedback" (desks/mt5/research/daily_cycle.py:472) (MEASURED)
  - clock: daily_cycle STEPS "state_research_feedback" (desks/mt5/research/daily_cycle.py:472), reached hourly through hourly_cycle.daily() (desks/mt5/research/hourly_cycle.py:1019) · artifact: desks/mt5/reports/COUNTERFACTUAL_WORLD.json, reports/FILTER_VALUE.json, reports/MISSED_GROWTH.json, data/missed_growth.jsonl — all written, all UNMEASURED/empty · consumer: desks/mt5/reports/MODULE_RENT.json (`"ledger": "desks/mt5/reports/MISSED_GROWTH.json"` on every rail row), libs/ops/module_rent, research/missed_growth, the deepening queue (sizing_hypothesis / exit_hypothesis tasks)
  - next: Nothing in this organ. It becomes lit the day desks/mt5/data/decision_ledger.jsonl first reaches this branch — add that path plus order_intents.jsonl and live_ledger.jsonl to the `$relPaths` publish list in desks/mt5/scripts/sync_shadow_to_git.ps1:293-319 (they must also be allowlisted in .gitignore, as reports/markout.json already is at .gitignore:336).
- **E6 MT5 deadman** — MISSING · gate: principal
  - gap: There is no rail of any kind watching the live MT5 account: the only deadman polls pinned Binance testnet endpoints, and the box task named 'MT5-FusionDeadman' runs exactly that file. A drafted MT5 organ (proposals/fusion_deadman.py) has sat complete and unscheduled since 2026-08-26.
  - scripts/run_deadman_switch.py:32-33 `_FUT_BASE = "https://testnet.binancefuture.com"`, `_SPOT_BASE = "https://testnet.binance.vision"` — both PINNED testnet, both crypto (MEASURED)
  - scripts/run_deadman_switch.py:107-173 `combined_equity(state)` polls /fapi/v2/account, /fapi/v2/positionRisk and spot /api/v3/account; :176-212 `should_fire` fires on `_CONSECUTIVE = 5` readings below `_RUIN_FACTOR = 0.65` of high-water, `_MIN_HW = 500.0`, `_POLL_SEC = 60.0`, `_HW_CONFIRM = 3`; :215-246 `_flatten()` writes data/CASHCARRY_KILL, market-flattens futures reduce-only, sells spot to USDT, pages ntfy (MEASURED)
  - scripts/run_deadman_switch.py:4-5 — "TIER-3 NEVER-TOUCH … this file may not be modified, disabled or removed autonomously … explicit principal sign-off only" (MEASURED)
  - docs/LAWS.md:276-279 — "Standing defect, flagged 2026-08-25: the rail watches retired crypto-testnet endpoints and protects no live MT5 risk — repointing it is principal-gated work, queued, not autonomous." (MEASURED)
  - clock: NONE for MT5. The crypto rail has a service unit deliberately left unenabled and an UNDECLARED box task named MT5-FusionDeadman · artifact: NONE for MT5. The crypto rail writes data/deadman_state.json, data/deadman_heartbeat, data/CASHCARRY_KILL, data/DEADMAN_FIRED — none present · consumer: desks/mt5/scripts/check_gold_live.py:100-104 reads data/CASHCARRY_KILL as a blocking condition — i.e. the MT5 desk already listens for a kill file that only the crypto rail can write
  - next: Schedule desks/mt5/proposals/fusion_deadman.py in DRY-RUN ONLY (its default; `--live` is opt-in) as a new task row in desks/mt5/scripts/Install-QuantWindows.ps1's `$tasks` table, so it writes desks/mt5/data/fusion_deadman_state.json and proves its readings against the box for a period before anyone discusses arming it. scripts/run_deadman_switch.py is NOT touched. PRINCIPAL-GATED for anything beyond dry-run.
- **E7 Chaos engineering / fault drills** — PARTIAL
  - gap: Six of the twelve failure modes have a deterministic tested outcome (broker rejects, disk full, clock change, release corrupt, locked file, missing tasks after reboot); quote freeze, stale data, network down, gateway crash, OOM and duplicate scheduler have guards in code but no test and no drill — and the one real drill, ops/reboot_drill.ps1, is never executed on a schedule.
  - MT5 DIES / TASKS LOST AFTER REBOOT — ops/reboot_drill.ps1:4-6 (`Get-Process terminal64`), :8-19 (eleven `$required` tasks, re-enables Disabled ones), :23-28 (FAIL when `account.source_age_seconds > 900`). Pinned by desks/mt5/tests/test_installer_covers_the_reboot_drill.py:101 `test_every_required_task_is_registered_or_explicitly_excluded`, :116, :128, :139, :160 (MEASURED). The drill itself is scheduled NOWHERE — grep for reboot_drill finds only source comments and the test (MEASURED)
  - BROKER REJECTS — desks/mt5/tests/test_placement_health.py, 27 tests: :72 the 10015 diagnosis names the cause, :92 one bad pass does not pause, :99 repeated total rejection pauses the desk, :107 the pause file says why and how to clear it, :146 an unavailable bracket does not count toward the pause, :221 a two-week-old streak is not evidence about today, :259 two sleeves refused in one pass are one pass. Constants: MAX_TOTAL_REJECTIONS=2 (desks/mt5/mt5desk/decision_core.py:197), REJECTION_STREAK_WINDOW_H=24.0 (:206) (MEASURED)
  - DISK FULL — desks/mt5/tests/test_stall_watch_disk.py, 14 tests: :30 never terminates on a human decision, :44 a real ladder not one pool, :78 unrecoverable data refused at every threshold, :90 the tape is named unrecoverable, :96 the floor is still five gigabytes. Plus scripts/disk_guard.py on ops/quant-disk-guard.timer (every 6h at :12) (MEASURED)
  - CLOCK CHANGE — desks/mt5/tests/test_broker_clock.py, 7 tests: :59 recovers the offset it was shifted by, :73 the offset is signed not wrapped, :79 circular average, :86 a flat profile has no rollover to find, :101 two anchors that disagree are not one measurement. Artifact desks/mt5/data/broker_clock_measured.json (MEASURED)
  - clock: The DETECTORS are scheduled (MT5-StallWatch every 10 min, quant-disk-guard every 6h, quant-artifact-monotonic every 5 min, quant-job-manifest at *:07/30); the DRILLS are not — ops/reboot_drill.ps1 has no task and no timer anywhere in the repo · artifact: desks/mt5/data/stall_watch.json, data/JOB_MANIFEST_ALARM.txt, web/refresh_status.json · consumer: desks/mt5/research/issue_board.py:109 reads JOB_MANIFEST_ALARM.txt as a STALLED issue; scripts/build_zentech_state.py carries stall_watch into web/desk_state.json
  - next: Give ops/reboot_drill.ps1 a clock: add it as an `Kind = "ps1"` row in the `$tasks` table of desks/mt5/scripts/Install-QuantWindows.ps1 (read-only apart from re-enabling a Disabled task, so it is safe to run while trading) and have it append its PASS/FAIL to a file the issue board already reads.
  - landed: 738632ff ops/refresh_desk_state.sh publishes web/refresh_status.json (behind, conflicts, streak)
- **E9 Capital attribution to research (source → mechanism → hypothesis → certificate → sleeve → € realized)** — PARTIAL
  - gap: Four of the six hops are recorded (source→hypothesis→certificate, and sleeve identity), but two are broken: the sleeve row the gateway actually trades carries no certificate id, and the € realized end never exists because live_ledger.jsonl has never been written — so research_pnl's growth term is always attributed from the allocator's FORECAST weight, never from realised money.
  - SOURCE → HYPOTHESIS: desks/mt5/data/hypothesis_graph.jsonl — 30,313 rows, each BORN record carrying `id`, `region`, `symbol`, `family`, `params`, `source` ("fund_playbook:Renaissance:A"), `parent` ("Renaissance|stock book approximately balanced long/short…"), `fate`, `why`, `at` (MEASURED)
  - HYPOTHESIS → CERTIFICATE: desks/mt5/research/research_pnl.py:1-28 joins trials to certified growth, reading `source`/`kind`/`provenance` first, then the canon's `hunt`, else "unknown" — "said, never guessed"; growth = expected_log_per_day × (sleeve weight / total_heat) summed over FUNDED sleeves whose certificate names the source (MEASURED)
  - desks/mt5/research/forward_reconcile.py:308 `certificate_id = str(row.get("certificate") or key)` — the certificate key is `SYM.family.selector` (MEASURED)
  - CERTIFICATE → SLEEVE: desks/mt5/data/sleeve_registry.json — identity carries `sleeve_id`, `code_hash`, `cost_hash`, `behaviour_hash`, `data_venue`, `frozen_at`, `forward_start`, `status` (MEASURED). BUT desks/mt5/research/promoter.py:1278-1284 writes NO certificate id onto the sleeve row — only `certificate_drift` and `admission` (MEASURED)
  - clock: daily_cycle._state_research_feedback legs "research_pnl" and "allocator_attribution" (desks/mt5/research/daily_cycle.py:333, 342) · artifact: desks/mt5/data/hypothesis_graph.jsonl (PRESENT, 16 MB), desks/mt5/data/sleeve_registry.json (PRESENT), desks/mt5/reports/RESEARCH_PNL.json (ABSENT), reports/allocator_attribution.json (PRESENT but UNMEASURED) · consumer: the research bandit (marginal_by_source), reports/MODULE_RENT.json proposer rows, the deepening queue
  - next: Add the certificate key to the sleeve row in desks/mt5/research/promoter.py:1278-1284 (it is already in scope as `key`), so the one hop between a funded sleeve and the certificate that justified it stops being an inference from a name-shaped string.
- **E10 Infrastructure as measured alpha (economic attribution for freshness / slippage / latency)** — EXISTS-DARK
  - gap: The rent ledger exists, runs, writes and is read — and 70 of its 71 modules are UNMEASURED with zero EARNS and zero COSTS, because every rent rule bills against MISSED_GROWTH, which has one row. Slippage and data freshness are not modules on the list at all, so even a working ledger would not price them.
  - desks/mt5/reports/MODULE_RENT.json — EXISTS (80,686 bytes, generated 2026-09-05T18:49:17Z), 71 modules, `rule`: "rent = Elog_with - Elog_without on the module's own ledger; a module that reads COSTS with n >= 10 in 3 consecutive weekly windows is NAMED under `retire`", unit log-wealth/day (MEASURED)
  - MODULE_RENT kinds (MEASURED, counted from the artifact): rail 19, proposer 23, execution_algo 6 (`execution_algo:market`, `:twap`, `:iceberg`, `:sniper`, `:pullback`, `execution_choice_model`), allocator_component 6, state_dimension 4, data_source 8 (`data_source:broker_clock`, `:event_calendar`, `:weekday_calendar`, `:data_prospector`, `:world`, `:tick_tape`, `:vol_archive`, `:fill_corpus`), ai_organ 5
  - MODULE_RENT `by_kind` (MEASURED): every kind is 0 EARNS / 0 COSTS; 70 of 71 modules read UNMEASURED and exactly one reads NOT_BINDING (`state_shrinkage`)
  - MODULE_RENT `consumers` field, verbatim: "libs/ops/capability_graph.stages (MEASURED evidence), scripts/check_reachability (warns on a node whose rent is COSTS), the principal" (MEASURED)
  - clock: daily_cycle STEPS "module_rent" (desks/mt5/research/daily_cycle.py:473) and "missed_growth" (:338), reached hourly via hourly_cycle.daily() (desks/mt5/research/hourly_cycle.py:1019) · artifact: desks/mt5/reports/MODULE_RENT.json, reports/MISSED_GROWTH.json, data/missed_growth.jsonl, data/compute_ledger.jsonl — all present · consumer: libs/ops/capability_graph.stages, scripts/check_reachability, desks/mt5/reports/ISSUE_BOARD.json
  - next: Add a `data_source` rent row for tape/bar FRESHNESS keyed on the SLO breaches scripts/check_job_manifest.py already counts (its JOBS registry at scripts/check_job_manifest.py:59-95 has both the age limit and the named consumer), so freshness gets a log-wealth line instead of only a red/green flag — the change lands in libs/ops/module_rent.py.
- **E11 Data freshness guarantees / SLOs** — EXISTS-LIT
  - gap: Every SLO is on a producer-side clock with a named consumer and a real alarm — but the registry gauges only ARTIFACT AGE, so an artifact that is rewritten on time with UNMEASURED content (execution_quality, decay_live, COUNTERFACTUAL_WORLD) reads perfectly green; and calendar freshness is absent from the registry because the calendar is computed rather than fetched.
  - scripts/check_job_manifest.py:57-95 `JOBS: dict[str, tuple[float, str]]` — artifact → (max_age_hours, WHO CONSUMES IT), with the comment "The consumer is named so 'nothing reads this' is a checkable claim rather than an impression" (MEASURED)
  - Rows measured verbatim: `desks/mt5/reports/execution_quality.json: (36.0, "promoter (promotion gate), dashboard")`, `desks/mt5/data/decay_live.json: (26.0, "dashboard, gateway risk")`, `desks/mt5/data/forward_reconcile.json: (26.0, "operator audit")`, `reports/shadow/shadow_state.json: (3.0, …)`, `web/desk_state.json: (0.5, "dashboard (Dell/phone)")`, `data/forward_clock_ratchet.json: (1.5, …)` (MEASURED)
  - scripts/check_job_manifest.py:55 `ALARM = ROOT/"data"/"JOB_MANIFEST_ALARM.txt"`; consumed by desks/mt5/research/issue_board.py:109 `("JOB_MANIFEST_ALARM.txt", "STALLED", "the job manifest disagrees with what runs")` → desks/mt5/reports/ISSUE_BOARD.json (MEASURED)
  - Scheduled: ops/quant-job-manifest.timer `OnCalendar=*:07/30` → quant-job-manifest.service `ExecStart=… scripts/check_job_manifest.py` (MEASURED)
  - clock: quant-job-manifest.timer, quant-artifact-monotonic.timer, quant-bar-span.timer, quant-perishability.timer, quant-disk-guard.timer, MT5-StallWatch · artifact: data/JOB_MANIFEST_ALARM.txt, desks/mt5/data/stall_watch.json, data/freshness_contracts.jsonl, desks/mt5/reports/ISSUE_BOARD.json · consumer: desks/mt5/research/issue_board.py:109 → reports/ISSUE_BOARD.json → the dashboard and the daily cycle
  - next: Extend scripts/check_job_manifest.py's JOBS rows with an optional emptiness predicate (the file already has the byte-identical heuristic at :180) so an artifact whose payload is UNMEASURED/n=0 is reported as such rather than as fresh.
- **E12 Release identity and adoption** — PARTIAL · gate: principal
  - gap: The adopt→seal→gate→publish loop is complete, scheduled and consumed — but there is no rollback path of any kind (nothing can restore a previous seal), the current seal carries tested_sha null and ci_run_id null so the 'tested SHA' link of its own stated invariant is empty, and the published verdict is three days stale against the seal, which is the state in which the desk places nothing.
  - ADOPTION AT :12 — desks/mt5/scripts/Install-QuantWindows.ps1:466-485: task `MT5-AdoptRelease`, trigger `-Once -At ((Get-Date).Date.AddMinutes(12)) -RepetitionInterval (New-TimeSpan -Hours 1)`, running scripts\desks/mt5/scripts/Adopt-And-Seal.ps1; "Adopt the branch's code in place, re-seal the release, restart the gateway on the new seal" (MEASURED)
  - desks/mt5/scripts/Adopt-And-Seal.ps1:21-33 — the ordered safety property: desks/mt5/scripts/Adopt-Release.ps1 lands the tree by rewriting file CONTENTS in place; seal only when HEAD is not already code_sha and only on a tree with no tracked CODE modification; commit RELEASE.json ALONE ("that pure-seal commit is what `release.accepts` recognises"); restart MT5-Gateway (MEASURED)
  - desks/mt5/scripts/Adopt-And-Seal.ps1:139 — calls `release.accepts(sys.argv[1], release.load() or {})` so the box applies the same rule the gateway does; :151-163 refuses to seal with dirty CODE paths, using the same STATE_PREFIXES; :164 `release.seal(by='Adopt-And-Seal')`; :168-170 the lone `git add -- desks/mt5/data/RELEASE.json` (MEASURED)
  - Concurrency: desks/mt5/scripts/Adopt-And-Seal.ps1:90-96 waits up to 9 min on MT5-ShadowSync task state; :105-109 takes the named mutex `Local\MT5-GitWriter` and holds it through the seal commit (MEASURED)
  - clock: MT5-AdoptRelease hourly at :12 (desks/mt5/scripts/Install-QuantWindows.ps1:466-485); the verdict is re-measured on every MT5-Gateway pass (desks/mt5/mt5desk/gateway.py:2018) and published by MT5-ShadowSync every 15 min · artifact: desks/mt5/data/RELEASE.json (sealed 2026-09-08T02:05Z), desks/mt5/data/release_identity.json (verdict 2026-09-05T23:17Z, REFUSED) · consumer: desks/mt5/mt5desk/gateway.py:2018 NEW_RISK_OK (the only gate on new risk), desks/mt5/scripts/check_gold_live.py:125-148, desks/mt5/scripts/Adopt-And-Seal.ps1:139, scripts/build_zentech_state.py
  - next: Have desks/mt5/scripts/Adopt-And-Seal.ps1 record the PREVIOUS code_sha into RELEASE.json (a `previous_code_sha` field written by libs/ops/release.seal) so a rollback has a target to name; the rollback script itself is PRINCIPAL-GATED.
  - landed: burn_in.py: one row per hourly pass, streak against the 30-day target (this commit)
- **E13 Gateway correctness properties with tests** — PARTIAL
  - gap: Four of the six properties are pinned by tests (cancel-hour, per-lane close, Friday close, rejection streak window, plus the per-session TTL). Two are not: placement idempotence has three mechanisms and no test at all, and GATEWAY_PAUSED is tested only as a source-code string — and the pause file actually tracked in this repo sits at desks/mt5/GATEWAY_PAUSED, a path no reader on the desk ever opens.
  - IDEMPOTENCE OF PLACEMENT — three mechanisms, ZERO tests. (1) once-per-day guard: desks/mt5/mt5desk/gateway.py main loop `if st["brackets"].get(s["name"]): continue`; (2) recovery on restart: the pending-order match `abs(o.price_open - spec["buy_stop"]["price"]) < 0.5` then `log(f"recovered [{s['name']}] bracket for {day_key}")` at desks/mt5/mt5desk/gateway.py:2257; (3) process lock: desks/mt5/research/run_gateway_loop.py:51-56 (5-minute stale steal). grep of desks/mt5/tests for `recovered`/`double-bracket`/`brackets[` returns no gateway test (MEASURED)
  - VENUE CLOCK — desks/mt5/tests/test_broker_clock.py, 7 tests over `estimate_offset` (signed not wrapped :73, circular mean :79, flat profile refused :86, disagreeing anchors are not one measurement :101); artifact desks/mt5/data/broker_clock_measured.json; libs/research/decision_dataset.py:41-42 carries broker_clock as WORLD STATE "so the broker hour can be reconstructed from the minute" (MEASURED)
  - BUT: CLOSE_HOUR/CANCEL_HOUR/GOLD_WINDOWS are UTC constants (desks/mt5/mt5desk/decision_core.py:112-113 `CANCEL_HOUR = 20.5`, `CLOSE_HOUR = 19.5`), and desks/mt5/mt5desk/gateway.py:1975-1977 derives `hour` from the TICK timestamp in UTC — while desks/mt5/scripts/check_gold_live.py:20 says "The gold windows fire at 07:00, 13:00 and 17:00 broker time". No test pins a UTC+3 summer offset against CLOSE_HOUR or the signal hours (MEASURED)
  - CLOSE_HOUR / CANCEL_HOUR BY LANE — desks/mt5/tests/test_gateway_adapter.py:508 `test_the_end_of_day_close_leaves_the_scalp_lane_s_positions_to_their_own_exit`, :531 `test_the_scalp_tag_is_the_lane_s_own_and_only_the_scalp_lane_s`, :539 `test_main_scopes_the_daily_close_and_not_the_friday_one`, :550 `test_no_bracket_is_sent_at_or_after_the_cancel_hour`; implementation desks/mt5/mt5desk/gateway.py:876-881 `scalp_position_tags`, :2303 `if hour >= CANCEL_HOUR`, :2306 `if hour >= CLOSE_HOUR` with `keep_tags=keep` (MEASURED)
  - clock: The properties are exercised by MT5-Gateway every minute (desks/mt5/scripts/Install-QuantWindows.ps1:126-131); the tests run in CI (.github/workflows/ci.yml) and are not on a box timer · artifact: desks/mt5/data/gateway_state.json (armed, brackets with per-leg retcodes, placement_health, last_reconcile 2026-08-17T00:05:02Z), desks/mt5/logs/gateway.log · consumer: scripts/build_zentech_state.py → web/desk_state.json; desks/mt5/scripts/check_gold_live.py:106; the promoter
  - next: Add a test to desks/mt5/tests/test_gateway_adapter.py that runs main() with the pause file present and asserts zero order_send calls and zero state writes, and a second that runs the bracket loop twice in one day against a terminal already holding the matching pending orders and asserts the second pass takes the `recovered` path and sends nothing.
- **X1 VPS branch and desk branch converged; automatic merge publishes its status** — LANDED
  - ops/refresh_desk_state.sh:52 — writes web/refresh_status.json every tick (MEASURED)
  - CLAUDE.md — TWO BRANCHES, ONE SILENT ABORT standing fact (MEASURED)
  - tests/ops/test_refresh_desk_state_publishes_its_status.py (MEASURED)
  - clock: quant-desk-refresh.timer · artifact: web/refresh_status.json · consumer: scripts/build_zentech_state.py health.vps_refresh; any session
  - landed: 7264e420, 738632ff
- **X2 LLM seats feed the docket through the compiler's tree; per-seat conversion is a field** — LANDED
  - scripts/kimi_hunter.py — _donate writes data/intelligence/kimi/discoveries_*.json (MEASURED)
  - desks/mt5/research/miner_candidate_compiler.py — STRUCTURED_HYPOTHESIS branch, declared instruments before prose, seats block (MEASURED)
  - ops/run_seed_miners_hourly.sh — INTEL_PATHS carries data/intelligence (MEASURED)
  - desks/mt5/tests/test_compiler_reads_the_seats.py (MEASURED)
  - clock: quant-kimi-hunter.timer, quant-deepseek.timer, hourly_cycle:compile_candidates · artifact: desks/mt5/data/hypotheses/miner_candidates.json[seats] · consumer: merge_hypotheses -> gauntlet; the dashboard
  - landed: 8934dacd, 738632ff
- **X3 Attribution join: closing deals carry the position id and entry order; markout joins on them; chain materialised** — LANDED · gate: box-paste
  - gap: Runs on the box only after adoption (the paste); upstream keys are waves W7/W5b.
  - desks/mt5/mt5desk/gateway.py — _position_entry(pid) and the ledger row's position_id/entry_order/entry_deal/close_order/magic (MEASURED)
  - desks/mt5/mt5desk/markout.py — _JOIN_KEYS, _entry_fill, attributed_deals, chain (MEASURED)
  - desks/mt5/research/daily_cycle.py — writes reports/attribution_chain.json (MEASURED)
  - desks/mt5/tests/test_markout.py (MEASURED)
  - clock: MT5-Gateway, hourly_cycle:daily · artifact: desks/mt5/reports/attribution_chain.json · consumer: scripts/build_zentech_state.py execution.attributed_share; burn_in.py
  - landed: 14dd6ae9
- **X4 Burn-in telemetry: one row per hourly pass on sealed-vs-running code and fills, streak against a 30-day target** — LANDED · gate: box-paste
  - gap: Empty until the box adopts and runs the leg.
  - desks/mt5/research/burn_in.py (MEASURED)
  - desks/mt5/research/hourly_cycle.py — _costed("burn_in", ...) after release_identity (MEASURED)
  - scripts/build_zentech_state.py — health.burn_in (MEASURED)
  - desks/mt5/tests/test_burn_in.py (MEASURED)
  - clock: hourly_cycle:burn_in · artifact: desks/mt5/reports/burn_in.jsonl, desks/mt5/reports/burn_in.json · consumer: the dashboard; the programme ledger
  - landed: this commit
- **X5 Programme ledger with a self-checking gate** — LANDED
  - scripts/check_tier1_program.py (MEASURED)
  - tests/scripts/test_check_tier1_program.py (MEASURED)
  - landed: 848c7424

### Phase 1 — Remove the research throughput ceiling: worker queue, multi-fidelity screening, EVSI scheduler, research DAG, scalable forward clocks, content-addressed cache, experiment DB

- **I2 Event bus + durable task queue + resource-aware workers + artifact store + dependency DAG** — PARTIAL
  - gap: There is no event vocabulary at all — timers fire on wall clocks and legs run in hard-coded source order, so a DATA_UPDATED or GAUNTLET_PASSED transition cannot wake a downstream stage; the one real durable lease queue (campaign_queue) has no live driver.
  - NO EVENT BUS. Grep for `DATA_UPDATED|HYPOTHESIS_CREATED|FORWARD_MATURED|ALPHA_DECAYED|event_bus|EventBus` across all *.py/*.md/*.json returns ZERO code hits. `GAUNTLET_PASSED` exists only as a research-queue row *status* (web/desk_state.json:56 `"GAUNTLET_PASSED": 49`; desks/mt5/data/research_queue_archive.json) (MEASURED)
  - DURABLE QUEUE EXISTS, DARK: libs/ops/campaign_queue.py:1-13 — lease-based SQLite-WAL queue with content-hash dedup, `BEGIN IMMEDIATE` atomic claim, `reclaim_stale`, bounded retries. libs/ops/workers.py:21-51 WorkerRegistry with heartbeats and stale-worker reaping (MEASURED)
  - Its drivers are unscheduled: libs/ops/research_daemon.py:27-28, scripts/run_supervisor.py:28 and scripts/smoke_orchestration.py:16 import it; only scripts/run_research_tick.py:25 has a schedule — ops/crontab.manifest:1247 `44 5 * * *`, a cron row on the decommissioned plane, and it is NOT in the 177-token dispatcher allowlist (MEASURED)
  - THE REAL QUEUE IS A JSON FILE: desks/mt5/data/research_queue.json holds 20,152 rows — QUEUED_CANONICAL_GAUNTLET 17,749, GAUNTLET_REJECTED 2,374, BLOCKED_CANONICAL_DOCKET_MISSING 19, DONE 10; desks/mt5/data/research_queue.jsonl is 53.7 MB / 47,150 lines (MEASURED)
  - clock: quant-manifest-dispatch.timer (boot+3min then every 5min), quant-organ-catchup.timer (*-*-* *:00/5:00); campaign_queue/WorkerRegistry: NONE · artifact: desks/mt5/data/research_queue.json(l), data/manifest_dispatch_state.json, desks/mt5/data/compute_ledger.jsonl, data/fence_outcomes.jsonl · consumer: research_queue -> external_gauntlet/merge_hypotheses/build_zentech_state; manifest_dispatch_state.json -> NONE (self-documented at scripts/run_manifest_dispatch.py:65-67); compute_ledger -> libs/research/... none joins it (MODEL_ZOO.json reports '0 usable run(s)')
  - next: Make desks/mt5/research/hourly_cycle.py `_costed` append its leg name + outcome to an append-only `desks/mt5/data/events.jsonl` alongside the compute row (one line in the existing `_costed` at desks/mt5/research/hourly_cycle.py:557-607), giving the first machine-readable stage-transition stream without adding any schedule.
- **I3 Node separation: LIVE / RESEARCH-control / WORKER pool, jobs declaring CPU/RAM/deadline/EVSI** — PARTIAL · gate: hardware
  - gap: Separation is by machine accident, not by declaration: the money node also runs the 59-leg research cycle and the hourly gauntlet on 8 GB, and a job's spec carries a memory floor but no CPU count, no deadline and no EVSI, so no scheduler can rank two competing jobs.
  - TWO NODES, MEASURED. CLAUDE.md:32-34 — 'VPS: `ubuntu-4gb-hel1-5`, Hetzner Helsinki, 95.216.191.70, user `quant`, NO sudo by design'. CLAUDE.md:54-63 — the box is 8 GB by four independent readings ('phys 142MB free / virt 11719MB', 'one 4882 MB process leaving 280MB free'), the Hetzner CX32 shape 4 vCPU / 8 GB / 80 GB (MEASURED)
  - THE LIVE NODE IS THE BOX AND ALSO CARRIES RESEARCH. desks/mt5/ops/box_tasks.manifest:47-49 money lane = MT5-Gateway (continuous), MT5-FusionDeadman, MT5-TerminalBoot; :55-64 research lane = 8 tasks including MT5-Gauntlet and MT5-Hourly (the 59-leg cycle); :67-79 data lane = 11 recorders (MEASURED)
  - 8 GB BOX RESEARCH BUDGET: desks/mt5/scripts/external_gauntlet.py:172 `HEADROOM_SHARE = float(os.environ.get("GAUNTLET_HEADROOM_SHARE", "0.5"))`; :137-138 'THE ROOM THE BOX ACTUALLY HAS ... return max(need, min(HEADROOM_SHARE * float(free), HEADROOM_CAP_MB))'; :203 `by_mem = int(MEMORY_BUDGET_MB // PER_WORKER_MB)` sizes the worker pool from measured memory; :1504-1508 defers cells mid-flight when RSS exceeds the budget (MEASURED)
  - VPS BLAST-RADIUS BOUND: ops/quant-heavy.slice `MemoryHigh=2400M`, `MemoryMax=2800M`, `MemorySwapMax=0` on a '3814MB box'; 19 committed services join the slice. Its comment records the failure it fixes: 'quant-external-panel was oom-killed at a 668M peak while its own ceiling was 800M ... The BOX was at 138MB free' (MEASURED)
  - clock: Box: MT5-Gateway (1 min), MT5-Hourly (hourly), MT5-Gauntlet (hourly) via desks/mt5/scripts/Install-QuantWindows.ps1:126-193. VPS: 82 timers, 19 of them inside quant-heavy.slice. · artifact: desks/mt5/data/.job_locks/*.peaks.json, external_gauntlet result['memory_budget_mb'] (desks/mt5/scripts/external_gauntlet.py:1581), data/stall_watch.json · consumer: desks/mt5/research/job_lock.py admission, external_gauntlet worker sizing, web/desk_state.json `stall_watch` block
  - next: Extend the job spec in desks/mt5/research/job_lock.py to accept `cpu`, `deadline_s` and `evsi` alongside the existing declared MB and record all four in the .peaks.json ledger, so the admission decision becomes a rankable tuple instead of one number.
- **V1 The ten-gate gauntlet as it exists** — EXISTS-LIT
  - gap: Gate 9 (lockbox) recomputes gate 7's walk-forward OOS Sharpe, so the ten gates are nine plus a restatement, and PBO/SPA are one matrix-level number broadcast identically onto every candidate (external_gauntlet.py:1161-1162) rather than per-candidate statistics. Throughput is the second gap: 42 cells judged per pass against a 23,465-cell docket.
  - desks/mt5/scripts/external_gauntlet.py:971 — `run_gauntlet(cells, hunt_name, meta)` docstring 'Run full 10-gate gauntlet on a list of cells' (MEASURED)
  - desks/mt5/scripts/external_gauntlet.py:653-710 — GATE 1 `partition_at_economic_prior` + `symbol_is_tradeable` (:623) + `canonical_symbol` (:608) run BEFORE build; rejects carry `terminal_gate:'economic_prior'` / `'symbol_eligibility'` and `downstream_status:'NOT_RUN_TERMINAL_GATE_1_REJECT'` (MEASURED)
  - desks/mt5/scripts/external_gauntlet.py:1146 — GATE 2 `in_sample_screen`: `sharpe_ratio(arr) > 0.0` (MEASURED)
  - desks/mt5/scripts/external_gauntlet.py:1150-1158 + :54 `DSR_THRESHOLD = 0.95` — GATE 3 `deflated_sharpe`: `deflated_sharpe_ratio(arr, n_trials, variance_of_sharpes, threshold=0.95)` (MEASURED)
  - clock: hourly_cycle:external_gauntlet · artifact: desks/mt5/reports/universal_gates_external.json (desks/mt5/scripts/external_gauntlet.py:1605-1606); desks/mt5/reports/UNIVERSAL_SURVIVORS.json (:1637); desks/mt5/reports/POWER_CURE_CANDIDATES.json (:1805, measured n=9 at 2026-09-06T03:15) · consumer: desks/mt5/research/research_productivity.py:88 reads universal_gates_external.json; desks/mt5/research/shadow_admission.py + desks/mt5/research/promoter.py read UNIVERSAL_SURVIVORS.json / the canon (66 survivors measured in desks/mt5/data/UNIVERSAL_SURVIVORS.canon.json); libs/research/funnel_census reads the same report
  - next: In desks/mt5/scripts/external_gauntlet.py's `run_gauntlet` result dict, emit a `gate_independence` block recording, per sweep, the count of verdicts where `stages['lockbox']['lockbox_sharpe'] == stages['walk_forward']['oos_sharpe']` and the per-candidate PBO/SPA values that would have been computed by libs/validation/per_candidate — measurement only, with the pass/fail decision still taken on the existing thresholds.
- **V2 Research productivity / acquisition score for the next experiment** — PARTIAL
  - gap: The full product exists only at ARM granularity with DECLARED costs; the per-experiment ordering (voi_order) has no cost, no ΔElog, no breadth and no forward-latency denominator, and the gauntlet docket is not ordered by value at all. The measured compute denominator (compute_ledger.jsonl) has no reader.
  - libs/research/bandit.py:22 — the arm-level score IS the blueprint form: `score_j = E[dElogW_j] x P(valid survivor_j) / (compute + data + latency + multiplicity)_j`; :141-148 `COST` is a DECLARED table of unit costs, not measured seconds ('Costs are declared units, not measured seconds', :29-30) (MEASURED)
  - desks/mt5/reports/RESEARCH_BANDIT.json — generated 2026-09-07T18:40, `graph_rows: 30313`, 11 arm shares, per-arm `p_survivor`/`worth`/`cost`/`score_mean` (MEASURED, live)
  - desks/mt5/research/deepening_worker.py:388-476 `voi_order` — the per-TASK score is `p * worth * novelty * direction` (:474): P(certify|family) from `funnel_census` Beta posterior (:406-412), blended with `GraveyardModel.premortem` p_survivor (:425-442), `worth` in {1.0,1.5,2.0,3.0} by source (:456-459), `novelty = 1/(1+n_failed)` from `hypothesis_graph.prior_failures` (:463-464), `direction = bandit.arm_weight(...)` (:470-471). There is NO compute-cost denominator, NO E[ΔElog], NO breadth and NO forward-latency term (MEASURED)
  - desks/mt5/scripts/external_gauntlet.py:1453-1458 — the gauntlet docket is ordered by `(_cursor.get(sym,''), sym, timeframe, family)`, i.e. longest-unbuilt symbol first for frame-cache locality and anti-starvation, not by any value score (:1444-1447) (MEASURED)
  - clock: bandit: desks/mt5/research/daily_cycle.py (research_bandit leg); voi_order: hourly via desks/mt5/research/hourly_cycle.py:1064 `_costed("deepen", deepen)`; research_productivity: desks/mt5/research/daily_cycle.py:332; compute_ledger `rank`: NONE · artifact: desks/mt5/reports/RESEARCH_BANDIT.json; desks/mt5/data/hypotheses/deepening_worked.jsonl; desks/mt5/reports/RESEARCH_PRODUCTIVITY.json; desks/mt5/data/compute_ledger.jsonl · consumer: RESEARCH_BANDIT.json -> desks/mt5/research/deepening_worker.py:470, desks/mt5/research/hourly_discovery.py:128, desks/mt5/research/daily_cycle.py:256 (all via `bandit.arm_weight`); RESEARCH_PRODUCTIVITY.json -> only libs/ops/capability_graph.py:454-460 and scripts/check_absolute_ceiling.py (staleness/coverage, not decision); compute_ledger.jsonl -> NONE
  - next: Add a cost denominator to `voi_order` in desks/mt5/research/deepening_worker.py by dividing the existing score by `libs.ops.compute_ledger.cost_by_run()['deepen']['mean_wall_s']` per task class (falling back to 1.0 when uncosted, which leaves the current order unchanged) — a pure re-ordering that spends measured seconds first, changing no gate.
- **V3 Durable EVSI priority queue for gauntlet cells per compute-second** — PARTIAL
  - gap: The durable queue and the EVSI arithmetic exist in two different files that never meet: the gauntlet's persistent cell queue is ordered by starvation rotation, and the EVSI scorer runs on four hand-typed experiments that are not gauntlet cells.
  - desks/mt5/scripts/external_gauntlet.py:729-760 — durability half EXISTS: `CACHE_DIR = REPORTS/'gauntlet_cache'` (content-addressed per `_cache_key`, :763) plus `BUILD_CURSOR = data/hypotheses/gauntlet_build_cursor.json` (:733) stamped by `_save_build_cursor` (:750-760); an unreadable cursor reads as EMPTY so no symbol stops being built (:739-741) (MEASURED)
  - desks/mt5/scripts/external_gauntlet.py:1498-1502 — 'Out of build budget: defer, never drop. The next sweep finds everything this run cached and starts from here', and deferred cells are published as `NOT_RUN_BUILD_BUDGET_DEFERRED` verdicts (:1561-1570). State therefore survives restarts (MEASURED)
  - desks/mt5/scripts/external_gauntlet.py:1453-1458 + :1444-1447 — the ORDERING is starvation rotation (longest-unbuilt symbol first), explicitly chosen to keep symbol groups contiguous for the frame cache; no value or information term enters the key (MEASURED)
  - desks/mt5/research/deepening_worker.py:69 `WORKED = data/hypotheses/deepening_worked.jsonl` append-only, :646-647 `pending = voi_order([t for t in tasks if task_id(t) not in done])`, :662-669 time-budgeted with 'the remaining tasks carry to the next hourly pass in VOI order -- nothing is dropped'. A durable, restart-surviving priority queue EXISTS — for LLM deepening tasks, not for gauntlet cells (MEASURED)
  - clock: external_gauntlet cursor+cache: desks/mt5/research/hourly_cycle.py:1101; deepening queue: desks/mt5/research/hourly_cycle.py:1064; experiment_design: desks/mt5/research/hourly_cycle.py:1254 `_costed("experiment_design", experiment_design)`; run_research_intake: ops/crontab.manifest:3416 (45 10 * * *) but never produced · artifact: desks/mt5/data/hypotheses/gauntlet_build_cursor.json + desks/mt5/reports/gauntlet_cache/; desks/mt5/data/hypotheses/deepening_worked.jsonl; desks/mt5/reports/EXPERIMENT_DESIGN.json · consumer: gauntlet_build_cursor.json -> desks/mt5/scripts/external_gauntlet.py:1448 `_build_cursor()` (self); deepening_worked.jsonl -> desks/mt5/research/deepening_worker.py:646 `worked_ids()`; EXPERIMENT_DESIGN.json -> desks/mt5/research/issue_board.py:77 STALENESS check only (3600s tolerance), no decision consumer
  - next: In desks/mt5/research/experiment_design.py, replace the hardcoded `queue` list in `run()` with rows built from the deferred cells already published in desks/mt5/reports/universal_gates_external.json (`downstream_status == 'NOT_RUN_BUILD_BUDGET_DEFERRED'`), costing each at the file's own measured ~22 s/cell — publishing an EVSI-ordered view of the real docket alongside the existing rotation, changing no threshold.
- **V8 Multi-fidelity funnel, Stage 0 through Stage 8** — PARTIAL
  - gap: Stages 2..8 exist and are ordered; Stage 0 (logical validity in ms) and Stage 1 (ultra-cheap falsification on a small sample) are written (pre_filter, successive_halving) and never run, and Stage 4 is one perturbation (3x cost) instead of a battery — so the docket pays full-cost build for every cell.
  - STAGE 0/1 (logical validity, ultra-cheap falsification): libs/research/pre_filter.py:1-12 exists ('rejects ONLY on cheap, unambiguous evidence; anything borderline ESCALATES') — importers are tests/research/test_pre_filter.py only. libs/research/successive_halving.py:1-14 exists — only caller scripts/run_research_intake.py:155, recorded NEVER-PRODUCED in desks/mt5/reports/MINER_MAINTENANCE.json:43. Neither runs (MEASURED)
  - The cheapest thing that DOES run first is Gate 1: desks/mt5/scripts/external_gauntlet.py:653-710 `partition_at_economic_prior` with the tradeability + chart-existence limbs, applied BEFORE any signal is constructed (:655) (MEASURED)
  - STAGE 2/3 (coarse then full-cost backtest): a single stage — `build_cell` (:423) then `daily_series(df, sigs, costs)` at full modelled cost (:322), with the 3x arm computed in the same pass (:996-1003). There is no coarse tier (MEASURED)
  - STAGE 4 (adversarial perturbations): only stress_costs 3x runs inside the gauntlet (:1191-1194). entry_shift/side_flip/random_entry/label_shuffle live in libs/validation/redteam.py with no live caller (see V4) (MEASURED)
  - clock: Stages 1(gate-0)/5: desks/mt5/research/hourly_cycle.py:1101. Stage 6: ops/shadow-forward.timer + desks/mt5/research/hourly_cycle.py + desks/mt5/research/daily_cycle.py. Stage 7/8: desks/mt5/research/hourly_cycle.py:1158 (pf_allocator) and :1176 (promoter). Stages 0/1 cheap screens: NONE. · artifact: universal_gates_external.json, POWER_CURE_CANDIDATES.json, UNIVERSAL_SURVIVORS.json, backups/moat/shadow_ledgers/*shadow_state.json, desks/mt5/data/sleeves.json · consumer: desks/mt5/research/promoter.py reads the shadow states + canon; pf_allocator reads sleeves.json; shadow_admission reads UNIVERSAL_SURVIVORS.json
  - next: Insert `libs.research.pre_filter.pre_filter` between `partition_at_economic_prior` and the build loop in desks/mt5/scripts/external_gauntlet.py (around :1469), recording every decision to its append-only ledger and ESCALATING anything borderline to the full gauntlet exactly as its docstring specifies — a staging change that only reorders which cells the 2700 s budget reaches first.
- **V11 Forward-clock tiers with separate multiple-testing budgets** — PARTIAL
  - gap: One flat cohort of 12 Holm-corrected seats with a live orphan rule, but no tiering: there is no cheap Tier-C virtual-clock class, no per-tier multiple-testing budget (family_multiplicity is built and only reporting), and enrolment is by certificate membership rather than by EVSI x ΔElog / maturation time.
  - libs/research/slot_registry.py:49 `MAX_FORWARD_SLOTS = 12` — 'That cohort size is therefore the single most load-bearing integer on the desk' (:6); :506-523 `seats_free`, `over_cap`, `idle_slots` derived from it (MEASURED)
  - ONE TIER ONLY: no Tier A/B/C exists; grep for tier-partitioned clock budgets returns nothing. libs/validation/family_multiplicity.py:1-27 supplies the machinery that WOULD give separate budgets ('a new clock in the FLOW family costs the MOMENTUM family exactly nothing') and its only production reader is scripts/report_breadth.py:43 (MEASURED)
  - ORPHAN RULE IS LIVE: desks/mt5/research/forward_reconcile.py:21-35 — a live clock must be BOTH enrolled by a running engine AND backed by a ten-gate certificate; `RETIRED_ORPHAN`, `RETIRED_GATE_FAIL`, `RETIRED_UNRECONSTRUCTIBLE`; ':119-137 `enrolled_keys()` returns None for UNKNOWN and never conflates it with the empty set' so an unreadable engine cannot retire the whole book (MEASURED)
  - MEASURED clock census, backups/moat/shadow_ledgers/shadow_state.json: 49 rows — 31 RETIRED_ORPHAN, 17 ACTIVE, 1 RETIRED_UNRECONSTRUCTIBLE; qquant_shadow_state.json 1 ACTIVE; scalp_shadow_state.json and external_shadow_state.json 0 rows (MEASURED)
  - clock: shadow_forward: ops/shadow-forward.timer + ops/shadow-forward.service, desks/mt5/research/hourly_cycle.py and desks/mt5/research/daily_cycle.py legs, desks/mt5/scripts/Install-QuantWindows.ps1; forward_reconcile: desks/mt5/research/daily_cycle.py · artifact: backups/moat/shadow_ledgers/*shadow_state.json; desks/mt5/data/forward_reconcile.json; data/axis_clock_registry.json; docs/research/CLOCK_RETIREMENTS.json · consumer: desks/mt5/research/promoter.py (reads every lane's clock verdict); scripts/check_live_readiness.py:207; libs/research/slot_registry.derive_slots -> libs/research/promotion_latency.py:114
  - next: Have desks/mt5/research/forward_reconcile.py additionally emit, per enrolled clock, its `family_of(...)` label and the family's own `bh_bar` from libs/validation/family_multiplicity — publishing the partitioned budget beside the flat 12-seat cohort so tiering can be evaluated on measured cohorts before any seat count changes.
- **V12 Sequential evidence — SPRT / e-values / anytime-valid stopping** — EXISTS-DARK
  - gap: The anytime-valid machinery is implemented, type-I-verified and unwired; every forward decision is taken at the fixed 14-day / 50-trade boundary, so a clock that is already decisively negative still burns its full calendar and its seat.
  - libs/research/anytime_valid.py:1-30 — a full e-process with Ville's inequality, `e_value(returns)` (:43) and a graduation scan (:102 `if e_value(r[:t]) >= 1.0/alpha`); MEASURED Monte Carlo in its own docstring: 'null (no edge): 3/300 paths crossed = 1.0% at alpha=0.01 -> type-I control VERIFIED; strong (Sharpe ~2): 6/40 graduated, MEDIAN 132 days -> SLOWER than the fixed 90d; weak (Sharpe ~0.3): 0/40 graduated' (MEASURED)
  - grep for importers of `libs.research.anytime_valid` outside the module returns ONLY tests/research/test_anytime_valid.py:19 and tests/validation/test_e_bh.py:19 — the docstring in libs/validation/fdr.py:74 claiming 'four live consumers' is not borne out by the tree (MEASURED)
  - libs/validation/fdr.py exposes `e_benjamini_hochberg` / `merge_evalues`; production importers of libs/validation/fdr.py use only `benjamini_hochberg` (libs/autodiscovery/validation.py:74, libs/validation/screen_select.py:47) (MEASURED)
  - The rule actually in force is a FIXED sample size: desks/mt5/research/forward_verdict.py:23 'The BAR IS FIXED here and does not move: 50 trades, or 14 days with 20. That schedule is canon' and :61 (MEASURED)
  - clock: NONE · artifact: NONE · consumer: NONE (tests only)
  - next: Compute `libs.research.anytime_valid.e_value` over each active clock's ledger inside desks/mt5/research/forward_verdict.py and write it onto the row as `e_value`/`e_crossed_at` beside the unchanged 14-day/50-trade verdict — a strictly additional statistic that can only ever support an EARLIER kill, never an earlier pass.
- **V21 Trial accounting — every trial in a family with effective-trials correction** — PARTIAL
  - gap: Two independent trial counters coexist: a live lifetime ledger at 30,727 trials that tightens the proposers and the allocator, and the certifying gauntlet's fixed 597-trial charge which never reads it — and the hash-chained SQL ledger designed to be the single source has 0 rows.
  - libs/research/experiment_ledger.py:1-17 — `N_trials^life` and `N_trials^family` as a JOIN of the hypothesis graph (judged cells), every proposer's `tests_run` (screened cells, including culled) and preregistration cards, deduplicated by identity; 'CONSUMERS. `proposer_common.deflate` reports `t_deflated_lifetime` ...; `pf_allocator.search_trials` takes the larger of the gate report's count and the lifetime family count ... Both are tightenings' (MEASURED)
  - desks/mt5/reports/EXPERIMENT_LEDGER.json (2026-09-04T22:54): `lifetime_trials: 30727`, `judged_cells: 23443`, `screened_cells: 7284`, `preregistered_cards: 0`, and a 23-family breakdown (discovered 20341, clock_transition 6348, cross_asset_residual 1704, ...) (MEASURED)
  - Live consumers confirmed: desks/mt5/research/pf_allocator.py:657 `from libs.research.experiment_ledger import lifetime`; desks/mt5/research/proposer_common.py:237 and desks/mt5/research/anomaly_factory.py:139 `from libs.research.experiment_ledger import family_trials` (MEASURED)
  - THE GAUNTLET DOES NOT READ IT: desks/mt5/scripts/external_gauntlet.py:1082-1093 calls `gate_policy.charged_trial_count`, which at desks/mt5/research/gate_policy.py:173-175 returns the SPEC CONSTANT `fixed_campaign_trials(597)`; the measured census (`calibrated_census_report`, desks/mt5/mt5desk/canonical.py:161 `effective_trials`) is still computed and REPORTED (:1095, :1271 `trial_census`) but does not set the bar (MEASURED)
  - clock: EXPERIMENT_LEDGER.json is written by `lifetime(write=True)` from desks/mt5/research/pf_allocator.py:657, scheduled at desks/mt5/research/hourly_cycle.py:1158 `_costed("pf_allocator", ...)` · artifact: desks/mt5/reports/EXPERIMENT_LEDGER.json; data/alpha_registry.sqlite `trials_ledger` table (measured 0 rows) · consumer: desks/mt5/research/proposer_common.py:237 (`t_deflated_lifetime`), desks/mt5/research/pf_allocator.py:657 (winner's-curse shrinkage), desks/mt5/research/anomaly_factory.py:139
  - next: In desks/mt5/scripts/external_gauntlet.py's result dict (beside the existing `trial_census` at :1271), also record `lifetime_trials` and `family_trials` from libs.research.experiment_ledger for every family in the sweep, so each certificate carries what the lifetime count WOULD have charged next to the sealed 597 — reporting only, with the sealed policy bar untouched.
- **A13 Research opportunity cost — what was NOT tested because resources went elsewhere** — PARTIAL
  - gap: The cost side is instrumented but nearly empty (4 rows), and no artifact ever names WHAT was not tested: the compiler's MAX_ROWS_PER_PASS shortfall is printed to stdout, and the deepening backlog is a count with no per-row 'deferred because X won the budget'.
  - libs/ops/compute_ledger.py:1-33 — 'THE COMPUTE ALLOCATOR'S DENOMINATOR: what every research run actually cost', append-only, wall clock and CPU, 'a crashed run that consumed forty minutes consumed forty minutes' (MEASURED)
  - MEASURED: desks/mt5/data/compute_ledger.jsonl contains 4 rows, last at 2026-09-06T12:04:54 ({run: 'deepen', kind: 'hourly_cycle', outcome: 'BrokenPipeError', wall_s: 1056.659, cpu_s: 214.85}). Four rows is not a denominator
  - Consumers of the ledger: libs/ops/allocators.py, desks/mt5/research/model_zoo.py, desks/mt5/frontier_intel/ontology.py, scripts/check_absolute_ceiling.py (MEASURED)
  - desks/mt5/research/miner_candidate_compiler.py:78 MAX_ROWS_PER_PASS = 1,000,000, with the shortfall handled at desks/mt5/research/miner_candidate_compiler.py:178-180: `print(f"compiler: MAX_ROWS_PER_PASS ... reached; older ...")`. MEASURED: the shortfall goes to stdout only — the written artifact (compiler OUT block at line 787-800) records rows_accounted / executable_candidates / deepening_tasks but no deferred count
  - clock: desks/mt5/research/hourly_cycle.py `_costed(...)` wraps every leg through libs/ops/compute_ledger; deepening via desks/mt5/ops/box_tasks.manifest:64 MT5-Deepening · artifact: desks/mt5/data/compute_ledger.jsonl (4 rows); desks/mt5/reports/RESEARCH_PRODUCTIVITY.json (backlog counts) · consumer: libs/ops/allocators.py, scripts/check_absolute_ceiling.py, desks/mt5/frontier_intel/ontology.py
  - next: Write the deferred-row count and the oldest deferred timestamp into the compiled artifact at desks/mt5/research/miner_candidate_compiler.py:787 (the OUT.write_text block) instead of only printing them at line 178.

### Phase 2 — Maximum breadth: the eight generators (LLM mechanism, symbolic, evolutionary, RL, residual, regime, causal, execution)

- **I9 Autonomous data discovery (source → licence → schema → PIT audit → instrument mapping → information scan → alpha screen)** — EXISTS-DARK
  - gap: Every stage of the pipeline exists as a rubric a human or an LLM applies by hand — there is literally no code that writes data/data_universe_map.json, and the six seats that would write it report `last=never` on missing credentials, so the discovery loop is a schedule pointing at a file nothing can produce.
  - data/data_universe_map.json (updated 2026-08-30, 414 KB) holds 190 SOURCES with per-source fields grade (162), provides (87), url (86), failure_modes (65), replaces (60), class (49), license (45), origin (42), cadence (38), verification (36), method (34), yield (31), provenance (29) (MEASURED)
  - Grades are real licence/PIT verdicts, e.g. 'RESTRICTED-PENDING-CONSENT (EN-sI) -- kihon-yakkan Art.14(15) consent-required off-service reuse ... NO INGEST', 'excluded-licensed (RE-GRADED 2026-08-29 ... the ADOPTION is WITHDRAWN on the publisher's own stated licence terms)', 'verified-clean (with a named coverage hole)'. Distribution: verified-clean 31, catalogued 17, adopted-pending-verify 15, REDUNDANT 5, UNVERIFIED 3 + ~110 one-off long-form grades (MEASURED)
  - THE FILE HAS NO PRODUCER. scripts/daily_max.py:101-107 — 'Grepping for a writer of data_universe_map.json returns readers only: acquire_data, max_audit and run_cadence all consume it and nothing produces it. The defect names an artifact with NO PRODUCER'. Confirmed by grep: readers at scripts/acquire_data.py:58, scripts/max_audit.py:2287, scripts/run_cadence.py:999, libs/ops/blind_trigger.py:34, libs/research/orphan_scan.py:68; zero writers (MEASURED)
  - It is therefore written by the LLM dig seats by hand: ops/dataaxis_dig_prompt.txt, ops/prospector_dig_prompt.txt, ops/litminer_dig_prompt.txt fed by quant-seat-dataaxis.timer (05:00 UTC), quant-seat-prospector.timer (06:00), quant-seat-litminer.timer (07:00), quant-dataaxis.timer (14:00), quant-prospector.timer (18:00), quant-litminer.timer (19:00) (MEASURED)
  - clock: quant-seat-dataaxis.timer, quant-seat-prospector.timer, quant-seat-litminer.timer, quant-dataaxis/prospector/litminer.timer (6 timers + 4 cron fallbacks) — but the seats die on missing credentials · artifact: data/data_universe_map.json (190 sources), docs/research/data_axis_watchlist.md, docs/research/prospector_watchlist.md, desks/mt5/reports/DATA_PROSPECTOR.json · consumer: scripts/acquire_data.py:58, scripts/max_audit.py:2287, scripts/run_cadence.py:999, libs/ops/blind_trigger.py:34, libs/research/orphan_scan.py:68
  - next: Write the missing producer: have scripts/acquire_data.py (which already parses the map at :58) append newly probed sources back into data/data_universe_map.json with grade=UNVERIFIED and the probe result — turning the map from a hand-written document into an artifact with one machine writer.
- **I17 Multilingual / language-blind literature mining: languages and source ecosystems covered vs named-only** — PARTIAL
  - gap: Coverage is genuinely broad in the registry (26 languages, 51 regions) but 44% of grounds are English and the per-language seats were consolidated into one unified dig with no per-language yield accounting — nothing reports which of the 26 languages produced a survivor, so the non-English half cannot be defended or defunded on evidence.
  - THE GROUND REGISTRY IS LARGE AND REAL. desks/mt5/data/deep_forest_sources.json holds 502 GROUNDS across 51 REGIONS in 26 LANGUAGES, each with region, language, route, kind, weight, why, url and alt (MEASURED)
  - Language distribution of the 502 grounds: en 219, zh 30, es 27, ja 26, zh-Hant 26, ru 22, pt 19, ko 17, vi 12, fr 12, id 11, tr 11, ar 10, de 10, pl 10, th 9, it 6, nl 5, sv 5, fi 4, he 3, uk 3, no 2, hi 1, sw 1, da 1 (MEASURED)
  - Region grounds: us 40, institutional 41, cn 30, jp 26, ru 22, br 19, in 19, kr 17, tw 16, de 13, au 13, vn 12 … down to bd/lk/co/pe at 2 (MEASURED)
  - Its own header states the mandate and the fences: 'China first, then every region at the same depth … grounds whose robots.txt names this agent family are read as search-index snippets only (OP-041). Concepts are reimplemented; nothing is copied. Crypto-exchange grounds are forbidden and fenced in code (libs.research.mechanism_claims.FORBIDDEN_VENUES)' (MEASURED)
  - clock: hourly_cycle legs `deep_forest` (:1194) and `world_crawler` (:1288); quant-frontier.timer (15:00), quant-seat-frontier.timer (08:00 UTC), quant-seat-frontier2.timer (06:20 UTC), quant-midnight-frontier.timer (00:00 Europe/Dublin) · artifact: desks/mt5/data/deep_forest_sources.json (502 grounds), desks/mt5/data/intelligence/{china,korea,minfx_jp,followme_cn,mylivefx_br,readitrades_africa,…}/, desks/mt5/reports/DEEP_FOREST.json, docs/research/search_operator_library.md · consumer: desks/mt5/research/miner_candidate_compiler.py (reads data/intelligence/**), libs/research/hunt_frontier.py ground-state accounting
  - next: Have desks/mt5/research/deep_forest_miner.py stamp each donated row with its ground's `language` and `region` so libs/research/hunt_frontier.py's YIELDED/EMPTY/NAMED_ONLY/BLOCKED accounting becomes per-language — the fields are already in deep_forest_sources.json.
- **A12 Mechanism recombination — a library of primitives composed into new mechanisms** — PARTIAL
  - gap: Six mechanism vocabularies exist and one of them is genuinely live in the miners, but there is no composition operator: mechanisms are assigned single labels and never combined, and the artifact five modules read as the board (data/mechanism_board.json) has never been produced on this tree.
  - libs/research/mechanism_claims.py:1207-1210 MECHANISM_CLASSES = ('policy','calendar','positioning','inventory','flow','carry','cross_asset','microstructure','reversion','momentum') with _CLASS_TERMS in ~20 languages (libs/research/mechanism_claims.py:1211+) (MEASURED)
  - libs/research/mechanism_census.py:1-40 — the ECONOMIC-mechanism taxonomy with 32 named classes, MEASURED names including 'liquidity provision / immediacy', 'price continuation / underreaction', 'index reconstitution / rebalance flow', 'collateral rule change / forced deleveraging', 'fiscal / tax calendar forced flow', 'commodity inventory / convenience yield', 'positioning / crowding unwind' — i.e. most of the blueprint's primitive list under different names
  - libs/research/semantic_space.py:52-79 EVENTS = ('liquidity_shock','benchmark_flow','macro_release','session_transition','options_hedging','forced_deleveraging','inventory_rebalance','volatility_shock','carry_change','cross_market_move','positioning_extreme'); CONTEXTS, QUALITIES, DIRECTIONS, OUTPUTS complete a 5-axis product space (MEASURED)
  - libs/research/alpha_schema.py:58+ EVENTS — each carries its PAYER and `evidence`, 'an edge with no payer is a pattern'; libs/research/mechanism_ontology.py:126-200 CORE_MECHANISMS with contracts and `compatible()` pruning combinations the contract forbids (MEASURED)
  - clock: mechanism_claims via desks/mt5/research/hourly_cycle.py:1288 world_crawler + desks/mt5/research/daily_cycle.py:293 deep_forest_miner/repo_miner; mechanism_board via scripts/daily_research_cycle.py:244 (ops/crontab.manifest:977); mechanism_census via ops/crontab.manifest:2439 (`35 9 * * *`) · artifact: libs/research/mechanism_claims outputs land in desks/mt5/data/intelligence/*/ ; data/mechanism_board.json and data/mechanism_census.json ABSENT on this tree · consumer: desks/mt5/side_channels/world_crawler.py:504, desks/mt5/research/deep_forest_miner.py:65, desks/mt5/research/repo_miner.py:123 consume extract_claims; libs/autodiscovery/generators.py:95 and libs/validation/family_multiplicity.py:56 consume mechanism_census.CONSTRUCTION_CLASS
  - next: Make scripts/mechanism_board.py fall back to libs/research/mechanism_census.TAXONOMY when data/research_erv.json and data/research_autopsy.json are absent, so data/mechanism_board.json exists for its five readers instead of silently never appearing.
- **G1 Engine A — mechanism-driven LLM synthesis seats** — PARTIAL
  - gap: Both LLM seats are scheduled hourly, yet neither has ever landed a row in the compiler's tree: kimi's donation door was only wired on 2026-09-08 and its target dir does not exist, and deepseek's YAML hypotheses live at data/intelligence/hypotheses/ where the compiler's `_rows` finds no `discoveries` list. meta_architect and research_exchange (the arbitration/attribution half) are on no scheduler at all.
  - libs/ops/deepseek_cycle.py:73 — `SEED_ROLES: tuple[tuple[str, str], ...]` declares 36 seat roles (cold_alpha_inventor, survivor_assassin, graveyard_resurrection, regime_specialist, validation_red_team, multilingual_intelligence, cross_domain_transfer, negative_space_miner, unknown_unknown_explorer, alpha_recombination, portfolio_complementarity_hunter, experiment_designer, natural_experiment_hunter, …) (MEASURED)
  - libs/ops/deepseek_cycle.py:706 — `DONATE_DIR = "data/intelligence/deepseek"`; `_donate()` at :709 writes `discoveries_<ts>.json` in the miner contract (MEASURED)
  - ops/quant-deepseek.timer — `OnCalendar=*-*-* *:20:00 UTC`, `Persistent=true`; ops/quant-deepseek.service `ExecStart=/bin/bash .../ops/run_deepseek_factory.sh` (MEASURED)
  - git log data/intelligence (commit 9dda1f7c 'deepseek flywheel 2026-09-07') adds data/intelligence/hypotheses/H-2026090{6,7}-00*.yaml; 72 H-*.yaml files present on disk (MEASURED)
  - clock: ops/quant-deepseek.timer (:20 hourly); ops/quant-kimi-hunter.timer (:40 hourly) + ops/crontab.research.manifest 3-hourly/2x-week rows; desks/mt5/research/hourly_cycle.py:1193 'frontier' and :1242 'frontier_implementer' legs. meta_architect + research_exchange: NONE · artifact: data/intelligence/hypotheses/H-*.yaml (72 files, deepseek); data/intelligence/deepseek/discoveries_*.json and data/intelligence/kimi/*.json (both ABSENT); desks/mt5/reports/FRONTIER_INTELLIGENCE.json + FRONTIER_GAPS.json · consumer: desks/mt5/research/miner_candidate_compiler.py (walks data/intelligence/**) → desks/mt5/data/hypotheses/miner_candidates.json → desks/mt5/research/merge_hypotheses.py / desks/mt5/research/research_productivity.py / desks/mt5/research/nightly_catchup.py. But the compiler has recorded ZERO rows from either seat.
  - next: Add a `SYSTEMD`/timer row for scripts/research_exchange.py `score` and assert in desks/mt5/research/miner_candidate_compiler.py that `seat_summary()` writes a non-null `seats` block with a row count per SEAT_SOURCES entry, failing loud when both are 0 — the file to touch is desks/mt5/research/miner_candidate_compiler.py (seat_summary/main).
  - landed: 8934dacd kimi donates to data/intelligence/kimi; STRUCTURED_HYPOTHESIS; seats block, 738632ff VPS hourly sync commits data/intelligence
- **G2 Engine B — symbolic regression / formulaic alpha over a grammar** — EXISTS-LIT
  - gap: The grammar and its search are live, but the alpha101 corpus the desk mined (data/brain_hunter_s1*) was never turned into operators or families, and there is no cross-sectional panel: `group_rank`/`group_zscore`/`xsec` exist as operators but the desk has one instrument per cell (search_populations.py:410), so the panel operators are structurally unreachable.
  - libs/research/alpha_grammar.py:53-54 — `BAR_TERMINALS = ("close","open","high","low","ret","range","body","activity","spread","atr","vol","flow")`; :55 `DRIVER_TERMINALS = ("usd","rates","risk","gold","oil","growth")` (cross-asset return terminals); :60 `EXTERNAL_TERMINALS` (positioning, event, macro, fundamental, state_prob, xsec, fill_state) (MEASURED)
  - libs/research/alpha_grammar.py:64-69 — `UNARY`, `WINDOWED = ("delay","delta","mean","std","min","max","ts_rank","zscore","decay","sum","bars_since_max","bars_since_min","atr_norm","ts_backfill","scale")`, `BINARY_WINDOWED = ("corr","residual","cov","group_rank","group_zscore")`; :70 `WINDOWS = (2,3,5,8,12,24,48,120,240)`; :71 `MAX_DEPTH = 5` — every operator the blueprint names (MEASURED)
  - libs/research/alpha_grammar.py:555 `type_of`, :623 `well_typed`, :701 `dimension_of`, :860 `unit_of`, :911 `well_formed` — typed + dimensional + unit algebra so ill-formed trees are never proposed (MEASURED)
  - libs/research/search_populations.py:723-732 — `POPULATIONS` registry of nine, including `"symreg": symreg` (:235) and `"program_synthesis"` (:253), `"bayesian"` (:310) (MEASURED)
  - clock: quant-hourly-discovery.timer · artifact: desks/mt5/reports/alpha_evolution.json + desks/mt5/data/intelligence/alpha_evolution/discoveries_*.json · consumer: desks/mt5/research/miner_candidate_compiler.py → desks/mt5/data/hypotheses/miner_candidates.json → desks/mt5/research/merge_hypotheses.py
  - next: Add the ~15 alpha101 formulas whose fields the desk actually holds (close/open/high/low/volume/vwap-proxy) as literal `Expr` trees in `libs/research/alpha_grammar.CANON` so the novelty term measures against them and `search_populations.symreg` seeds from them.
- **G3 Engine C — evolutionary / GP search with robust-Elog fitness** — PARTIAL
  - gap: The engine is fully LIT and the fitness carries 7 of the blueprint's 9 terms plus 3 extras (tail, cost, fragility) — but TURNOVER and market CROWDING are absent from `WEIGHTS`, so a high-turnover or already-crowded expression pays no explicit fitness price (libs/research/crowding.py and crowding_hazard.py exist and are not read by alpha_fitness).
  - desks/mt5/research/alpha_evolution.py:93 `POP, GENS, ELITE, DEPTH = 40, 5, 8, 3`; :432 'SELECTION IS MULTI-OBJECTIVE. The elite is the Pareto front over the fitness terms, ordered by crowding distance'; :452 complexity cap `ag.complexity(e) > 14` (MEASURED)
  - libs/research/alpha_grammar.py:1095 `mutate`, :1124 `crossover` — the GP operators (MEASURED)
  - libs/research/alpha_fitness.py:107-110 — `WEIGHTS = {"delta_elog":1.0, "oos":0.5, "novelty":0.5, "tail":2.0, "state_breadth":0.5, "capacity":0.5, "cost":1.0, "fragility":1.0, "complexity":0.03, "multiplicity":0.5}`; :113 `PENALTIES = frozenset({"cost","fragility","complexity","multiplicity"})` (MEASURED)
  - libs/research/alpha_fitness.py:232 `delta_elog_term` calls `libs.portfolio.robust_elog.marginal_delta_elog` — robust Elog is literally the unit of account; :261 `oos_term` (stability); :275 `novelty_term`; :339 `state_breadth`; :370 `capacity_term`; :463 the deflated-Sharpe `E[max Sharpe over N trials]` trial penalty (MEASURED)
  - clock: desks/mt5/research/hourly_discovery.py:81 ORGANS (quant-hourly-discovery.timer, *:35) + desks/mt5/research/daily_cycle.py:260 _proposers; mutation_yield via desks/mt5/research/daily_cycle.py:323 · artifact: desks/mt5/reports/alpha_evolution.json; data/intelligence/alpha_evolution/discoveries_*.json; desks/mt5/reports/RESEARCH_BANDIT.json · consumer: desks/mt5/research/miner_candidate_compiler.py (donations); hourly_discovery._weight + daily_cycle._proposers (bandit shares); survivor_distiller (mutation_yield weights)
  - next: Add `"turnover"` and `"crowding"` keys to `libs/research/alpha_fitness.WEIGHTS` (in `PENALTIES`) with term functions reading the candidate's position-change rate and `libs/research/crowding.py`, so the same score() the evolution already calls charges them.
- **G4 Engine D — RL alpha search with marginal portfolio-utility reward** — PARTIAL
  - gap: There is a 1-step contextual bandit over research DIRECTIONS (reward = allocator marginal dE[log W]) and a GFlowNet over the grammar (reward = alpha_fitness, whose top term is marginal_delta_elog) — but no RL agent that treats a SEARCH TRAJECTORY as an episode with state, action and credit assignment; no value function, no policy over which candidate to expand next.
  - grep -riE 'q_learning|policy gradient|actor.critic|\bPPO\b|REINFORCE|replay buffer|td.error|epsilon.greedy' over libs/, desks/mt5/research/, scripts/ returns NO match — no sequential RL agent exists (MEASURED)
  - libs/research/generators.py:6-25 — `GFLOWNET (\`GFlowNet\`). A generative flow network over the grammar, trained by trajectory balance … training drives log Z + sum log P_F(a|s) toward log R(x) with R = exp(beta x fitness): at the optimum a tree is drawn with probability proportional to its reward' (MEASURED)
  - libs/research/search_populations.py:228 `def gflownet(ctx, n)` — 'The trained flow network, sampling in proportion to what the history rewarded'; registered at :725 `"gflownet": gflownet` (MEASURED)
  - desks/mt5/research/alpha_evolution.py:377 `ctx.history = [(r["params"]["expr"], float(r["fitness"])) for r in ev.rows.values() if "fitness" in r]` — the GFlowNet's reward IS the alpha_fitness score whose dominant term is `delta_elog` = marginal_delta_elog against the live book (MEASURED)
  - clock: bandit: desks/mt5/research/daily_cycle.py 'research_bandit' step (STEPS at desks/mt5/research/daily_cycle.py:462) + desks/mt5/research/hourly_discovery.py:126 _weight. GFlowNet: inside alpha_evolution's population mix (hourly_discovery ORGANS) · artifact: desks/mt5/reports/RESEARCH_BANDIT.json; desks/mt5/data/generator_weights.json (desks/mt5/research/alpha_evolution.py:89) · consumer: desks/mt5/research/hourly_discovery.py:126 `_weight()` and desks/mt5/research/daily_cycle.py:255 `arm_weight()` set per-proposer `budget_s`
  - next: Make `libs/research/search_populations.run()` return per-population realised dE[log W] of what it produced and feed it back as the population weights (the `weights` arg at :738 is already there but is fed by yield counts, not by the allocator's marginal) — the file to touch is libs/research/search_populations.py `_order`.
- **G5 Engine E — residual alpha mining on the book's unexplained returns** — PARTIAL
  - gap: The residual searched is the residual against a PEER-INSTRUMENT factor panel (factor_residual_engine.DriverSet / pca_residual over ≤8 symbols), not against the BOOK's own daily P&L. The only book-conditional term anywhere is `alpha_fitness.delta_elog_term`, which SCORES a finished candidate rather than defining the search target, and factor_residual has donated exactly one artifact since 2026-09-04.
  - desks/mt5/research/orthogonal_sweep.py:79 `OUT = BASE/"data"/"hypotheses"/"orthogonal_candidates.json"`; :420-431 `FACTOR_BASKET_MAX = 8` and the comment that `pca_residual` REFUSES below four factors and `cross_asset_residual` wants 2+ (MEASURED)
  - desks/mt5/research/hourly_cycle.py:1062 `sw = _costed("sweep", sweep)` → :791 `_producer("orthogonal_sweep", "desks/mt5/research/orthogonal_sweep.py")` — hourly (MEASURED)
  - desks/mt5/data/hypotheses/orthogonal_candidates.json exists, mtime 2026-09-08 21:05 (MEASURED)
  - desks/mt5/research/factor_residual_engine.py:73 `INTEL = _DESK/"data"/"intelligence"/"factor_residual"`; :74 `REPORT = _DESK/"reports"/"factor_residual.json"`; :143 `residual_z(ret, ds)`; :78-79 `BETA_WIN = 240`, `Z_WIN = 240` (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1062 'sweep' (orthogonal_sweep); desks/mt5/research/daily_cycle.py:462 STEPS 'factor_residual'; desks/mt5/research/daily_cycle.py:323 'missed_growth'. desks/mt5/research/residual_alpha.py / desks/mt5/research/residual_sleeve.py: NONE · artifact: desks/mt5/data/hypotheses/orthogonal_candidates.json; desks/mt5/reports/factor_residual.json + data/intelligence/factor_residual/discoveries_*.json; desks/mt5/reports/MISSED_GROWTH.json · consumer: desks/mt5/research/miner_candidate_compiler.py (factor_residual rows); desks/mt5/research/merge_hypotheses.py (orthogonal_candidates); external_gauntlet
  - next: Add a driver set built from the allocator's sleeve-return matrix (reports/pf_allocation.json daily R) as an extra `DriverSet` inside desks/mt5/research/factor_residual_engine.py `run()`, so one sweep per day searches the residual of the live book rather than only of the peer panel.
- **G6 Engine F — adversarial regime mining, P(alpha>0 | state)** — PARTIAL
  - gap: The state machinery is real and LIT but the SEARCH is three dimensions wide and five buckets deep. Of the blueprint's 13 axes (rvol, vol-of-vol, spread, liquidity, session, gap, macro proximity, USD state, correlations, trend strength, autocorrelation, entropy, jump prob) only 'session' and 'event' have a point-in-time labeller; rvol, spread, liquidity, USD state, correlations and entropy are all computed elsewhere (state_vector.liquidity, libs/regime/liquidity_state.py, economic_drivers.ROLES) but have no `build_labeller` branch, so P(alpha>0|state) is never asked of them.
  - libs/regime/ holds libs/regime/asset_state.py, libs/regime/bayesian.py, libs/regime/broker_clock.py, libs/regime/drift.py, libs/regime/event_state.py, libs/regime/feature_admission.py, libs/regime/features.py, libs/regime/gmm.py, libs/regime/hmm.py, libs/regime/hysteresis.py, libs/regime/liquidity_state.py, libs/regime/state_admission.py, libs/regime/state_vector.py, libs/regime/transitions.py (MEASURED)
  - libs/regime/state_vector.py:40 `class StateVector` with `global_state`, `assets`, `factors`, `session`, `event`, `liquidity`, `gaps`, `freshness`, `conditioning` (MEASURED)
  - desks/mt5/research/state_vector_build.py:64 `OUT = BASE/"data"/"state_vector.json"`; :86 `ASSET_CLOCKS = ("weekly","daily","H4","H1","M15","M5")`; hourly leg at desks/mt5/research/hourly_cycle.py:1018 `s = _costed("state_vector", state_vector)` (MEASURED)
  - libs/regime/state_admission.py:53 `K_BUCKET = 40.0`, :55 `MIN_TEST_TRADES = 150`, :59 `ADMIT_T = 2.0`, :60 `GRAVEYARD_T = -2.0`; :119 `judge()` with `dimensions_tried` deflation; :218 `build_labeller(name)` (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1018 'state_vector'; desks/mt5/research/daily_cycle.py:462 STEPS 'state_admission' → desks/mt5/research/state_admission_run.py; desks/mt5/research/daily_cycle.py:323 'regime_coverage'; ops/quant-regime-monitor.timer. desks/mt5/research/regime_discovery.py: NONE · artifact: desks/mt5/data/state_vector.json; desks/mt5/reports/STATE_ADMISSION.json; desks/mt5/reports/REGIME_COVERAGE.json; desks/mt5/data/regime_state.json · consumer: pf_allocator (refuses to condition on a graveyarded dimension, per desks/mt5/research/state_admission_run.py:1-5); gateway.regime_hibernate reads regime_state.json; regime_coverage merges uncovered buckets into the deepening queue
  - next: Add an `rvol` branch to `libs/regime/state_admission.build_labeller` (quintile of trailing 24-bar realised vol from the trade's own symbol bars, computed strictly before the trade's timestamp) and put it in `state_admission_run.DEFAULT_DIMENSIONS` — one dimension turns three into four and proves the labeller contract generalises.
- **G7 Engine G — causal / mechanism discovery** — PARTIAL
  - gap: Granger-style incremental information, permutation nulls, lead-lag and an admitted-edge graph are all LIT — but transfer entropy, conditional mutual information and constraint-based causal-graph discovery (PC/FCI style, with conditioning sets) do not exist by any name; every edge is a pairwise lagged linear/rank test with own-lag controls only, so a common-driver confound is never separated from a direct cause. Event studies exist only as the `event_reaction` family, not as a standalone study engine.
  - libs/research/causal_graph.py:670 `incremental_information(x, y, lag, own_lags=3, n_perm=200)` — 'Does X_{t-lag} add to Y's own lags? deltaR2 of the regression of Y_t on [1, Y_{t-1..t-p}, X_{t-lag}] over [1, Y_{t-1..t-p}], with a permutation p-value' built by CIRCULAR SHIFT — this is Granger causality under another name (MEASURED)
  - libs/research/causal_graph.py:635 `lagged_xcorr`, :747 `state_dependence`, :776 `stability`, :794 `nonlinearity`, :811 `measure_edge`, :515 `path_order`, :519 `path_strength`, :529 `summarise_path`; :242 `class CausalGraph`; :110 `ALPHA = 0.05`, :113 `MIN_N = 500`, :116 `MAX_LAG = 6`, :117 `N_BOOT = 200`, :118 `N_PERM = 200` (MEASURED)
  - grep -riE 'transfer.entropy|conditional mutual information|conditional_mi' across libs/ and desks/mt5/: NO match (MEASURED)
  - desks/mt5/research/world_causal_graph.py:67 `GRAPH = _DESK/"data"/"world_causal_graph.json"`, :68 `REPORT = reports/WORLD_CAUSAL_GRAPH.json`, :443 `conditioning_hints`, :469 `top_paths` (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1188 'causal_graph' leg; desks/mt5/research/hourly_discovery.py:81 ORGANS['world_causal_graph']; anomaly miner via the anomalies tree (compiled by desks/mt5/research/hourly_cycle.py:1063 'compile_candidates') · artifact: desks/mt5/data/world_causal_graph.json + desks/mt5/reports/WORLD_CAUSAL_GRAPH.json; desks/mt5/data/intelligence/anomalies/*.json · consumer: desks/mt5/research/state_vector_build.py:335 world_conditioning → StateVector.conditioning; libs/research/search_populations.causal_derived; miner_candidate_compiler (anomalies)
  - next: Add a `conditional_information(x, y, z, lag)` function beside `incremental_information` in libs/research/causal_graph.py (same ΔR² + circular-shift null, with the candidate confounder z in the base regression) and have `world_causal_graph.measure` call it against the already-admitted parents of the target.
- **G8 Engine H — execution-alpha discovery** — PARTIAL
  - gap: Execution is thoroughly MEASURED (spread/activity by weekday×hour, P(fill|state), E[slip|state], AlphaCapture, markout) but almost nothing is DISCOVERED: microstructure_miner ran 83 tests and proposed 0 cells, and there is no registered family whose signal is an execution state — reject probability and order-to-fill latency have no research target at all, and on this box the tick tape the surfaces need is not present.
  - desks/mt5/mt5desk/fill_surface.py:1-13 — `C = f(symbol, session hour, spread, vol, event, size, direction, order type)`, `E[C|X], Var(C|X)`, and `P(fill | distance, spread, vol, hour)` for passive/limit entries; 'A ridge regression … for slippage, a logistic on the …' (MEASURED)
  - desks/mt5/research/microstructure_miner.py:1-13 — 'per instrument, spread and activity by (weekday, hour) … Written to reports/MICROSTRUCTURE_SURFACES.json and consumed by cost_surface … and by the liquidity state' (MEASURED)
  - desks/mt5/reports/MICROSTRUCTURE_SURFACES.json exists (19,188 bytes, 2026-09-04 21:00); desks/mt5/reports/microstructure_miner.json shows `tests_run: 83, cells_proposed: 0` (MEASURED)
  - desks/mt5/data/intelligence/microstructure/ holds 52 donation files, latest discoveries_20260908_0102.json (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1187 execution_twin, :1274 execution_resolver, :1059 moat_miner; desks/mt5/research/daily_cycle.py:462 STEPS 'execution' (shadow_execution) and :323 'execution_intelligence'; desks/mt5/ops/box_tasks.manifest MT5-TickRecorder / MT5-TapeFeatures / MT5-MoatMiner · artifact: desks/mt5/reports/MICROSTRUCTURE_SURFACES.json; desks/mt5/data/execution_quality.json; desks/mt5/reports/SPREAD_PROVENANCE.json; desks/mt5/reports/markout.json · consumer: cost_surface + libs/regime/liquidity_state (surfaces); the promotion gate via scripts/build_gauntlet_survivors.py and desks/mt5/research/execution_resolver.py (execution_quality.json); pf_allocator via libs/portfolio/execution_cost.cost_ratio
  - next: Register an `execution_state` family in desks/mt5/mt5desk/families_orthogonal.ORTHOGONAL_FAMILIES whose entry condition is a fill-surface percentile (cheap spread × deep activity bucket from MICROSTRUCTURE_SURFACES.json), so the execution surfaces can propose cells through the same door as every other family.
- **G9 Novelty reward with an existing-exposure hurdle** — PARTIAL
  - gap: Three of the four blueprint terms exist: Edge (delta_elog), ResidualAlpha (tail_novelty / residual_novelty), Novelty (1−max|ρ|). λ3·ExistingExposure does NOT: the marginal Elog re-solve prices correlation but is a smooth continuous discount, and no code anywhere raises the hurdle as a function of how many sleeves of the same FAMILY the book already owns — 'the 11th trend signal has a higher hurdle' is not implemented.
  - libs/research/alpha_fitness.py:107-110 — `novelty` weight 0.5 in `WEIGHTS`; :86 comment 'novelty 0.5 — 1 - max|rho| against canon and population, in [0,1]' (MEASURED)
  - libs/research/alpha_fitness.py:275 `novelty_term(z, refs)` — 'Feature-space novelty, so part of the search is always hunting a different KIND of money'; returns 0.0 with 'no reference alphas to be new against: novelty unmeasured' on an empty desk (MEASURED)
  - libs/research/alpha_fitness.py:232 `delta_elog_term` → `marginal_delta_elog(list(book.sleeves), cand, hard_cap=book.hard_cap)` — the ONLY term that prices existing exposure, and it does so by re-solving the whole book (MEASURED)
  - libs/research/alpha_fitness.py:302 `tail_term` returns `tail_novelty = 1 - |corr(bad_i, bad_p)|` on the book's worst-decile days (MEASURED)
  - clock: Inside alpha_evolution (desks/mt5/research/hourly_discovery.py:81 ORGANS) and every proposer that calls libs.research.alpha_fitness.score() · artifact: The `novelty`, `tail_novelty` and `delta_elog` terms carried on every row of desks/mt5/reports/alpha_evolution.json and data/intelligence/alpha_evolution/discoveries_*.json · consumer: alpha_evolution's NSGA-II selection (desks/mt5/research/alpha_evolution.py:432); libs/autodiscovery/novelty.NoveltyGate; miner_candidate_compiler (the compiled rows carry the terms)
  - next: Add an `existing_exposure` penalty key to `libs/research/alpha_fitness.WEIGHTS`/`PENALTIES` computed as the candidate family's share of certified sleeves in UNIVERSAL_SURVIVORS.canon.json (which measures 32/66 'discovered', 20/66 session_range_breakout), so a proposal into a saturated family is charged for it.
- **G10 Alpha Zoo — families registered vs executable** — PARTIAL
  - gap: 76 registered = 76 executable (no dead registrations), but only 5 families have ever produced a certificate, and 15 of the blueprint's ~44 mechanism classes have no registered family at all — most importantly opening range, jump, vol-of-vol, realized range, cross-sectional, futures-curve, options and any exit/execution family, so those classes cannot even be TRIED.
  - COUNT REGISTERED = 76. desks/mt5/research/run_hunt16.py:340 `FAMILIES = {…}` — 14 (dav_ema_trend_pullback, dav_supertrend_trail, dav_breakout_fakeout, dav_fakeout_reverse, dav_squeeze_expansion, dav_bb_meanrev, dav_keltner_break, dav_t3_trend, dav_rsi_div_rev, dav_macd_qqe, dav_abc_structure, dav_momentum_cont, dav_hull_trend, dav_range_filter_adx) (MEASURED)
  - desks/mt5/mt5desk/families.py — 31 `def family_*`: usd_session_shock, comex_settlement, asia_momentum, dow_effect, momentum_volgate, session_range_breakout, monday_gap, london_close_momentum, level_breakout, failed_breakout, cot_net_fade, cot_change_fade, cot_change_momentum, cot_comm_follow, mean_reversion_rsi, mean_reversion_bollinger, trend_ma_cross, volatility_squeeze, range_reversion, volume_spike, overnight_drift, pullback_entry, pin_bar_reversal, engulfing_reversal, ict_fvg, retail_overlap_reversal, adx_channel_hybrid, dip_pullback_pct, dip_rsi_reclaim, dip_atr_shock, macro_swing (MEASURED)
  - desks/mt5/mt5desk/families_orthogonal.py — 31 in `ORTHOGONAL_FAMILIES`: :639 carry, relative_value, vol_transition, liquidity_regime, cot_positioning, lvc_asia_london (6); :1139 turn_of_month, calendar_month, overnight_gap_decay, vol_mean_reversion, correlation_regime, orderflow_imbalance, cross_asset_residual, macro_conditional, event_reaction, drawdown_conditional (10); :1271 discovered; :1385 pca_residual; :1408 `ORTHOGONAL_FAMILIES.update(EDGE_QUEUE_FAMILIES)` = desks/mt5/mt5desk/families_edge_queue.py:264 hedging_demand_close, fx_fixing_reversal, session_handoff, liquidity_gamma_reversal (4); :1433 generic, :1443 regime_transition, :1452 clock_transition, :1460 multi_speed_trend, :1468 ensemble, :1477 spread_state, :1486 formula, :1494 lead_lag, :1502 style_premia (9) (MEASURED)
  - COUNT EXECUTABLE = 76. desks/mt5/mt5desk/executables.py:107 `resolve_family(fam)` tries hunt16 → `families.family_<fam>` → `families_orthogonal.ORTHOGONAL_FAMILIES`; :71 `GATEWAY_FAMILY_POPULATIONS = ("hunt16", "families", "orthogonal")` — all three populations are executable; :96 `GATEWAY_FAMILY_TIMEFRAMES = ("M1","M5","M15","M30","H1","H4","D1")`; :144 `gateway_can_execute` returns True whenever `executor_gap` is None (MEASURED)
  - clock: The zoo is swept by desks/mt5/research/hourly_cycle.py:1062 'sweep' (desks/mt5/research/orthogonal_sweep.py) and run_hunt16/run_hunt* from desks/mt5/research/research_supervisor.py (desks/mt5/ops/box_tasks.manifest TASK MT5-ResearchSupervisor, trigger=UNDECLARED) · artifact: desks/mt5/data/hypotheses/orthogonal_candidates.json; desks/mt5/data/UNIVERSAL_SURVIVORS.canon.json; desks/mt5/reports/TRIAL_ALLOCATION.json · consumer: desks/mt5/mt5desk/gateway.py + desks/mt5/research/promoter.py via executables.gateway_can_execute; desks/mt5/research/trial_allocator.py
  - next: Register `family_opening_range` and `family_jump` in desks/mt5/mt5desk/families_orthogonal.ORTHOGONAL_FAMILIES (both are price-only, need no new data, and orthogonal_sweep will enumerate them the next hour) — the file to touch is desks/mt5/mt5desk/families_orthogonal.py.
- **G11 Exit research as first-class trials** — PARTIAL
  - gap: Exit research exists as three separate engines and a bandit arm, but exit variants are not counted as TRIALS: `trial_allocator` and `multiplicity` key on (family × asset_class) with no exit dimension, so a 216-arm exit sweep costs the docket's deflation budget nothing. Of the ten exit types the blueprint lists, only fixed TP/SL, ATR, time (ttl), trailing, break-even and partial are searched — vol-scaled, regime-change, cross-asset and decay exits have no arm.
  - desks/mt5/research/exit_study.py:1-11 — 'Tests exit-management variants … TTL-only (baseline), breakeven trail after +1R, trail after +0.5R, and TP2 (partial close at +1R, rest to target/TTL)'; :105 writes `reports/exit_study.json` (MEASURED)
  - desks/mt5/research/hourly_cycle.py:1284 `xs = _costed("exit_study", lambda: _producer("exit_study", "desks/mt5/research/exit_study.py"))` — an hourly leg since the 2026-09-06 'TWELVE THAT NOTHING RAN' block (MEASURED)
  - desks/mt5/research/exit_sweep.py:1-30 — the three-stage exit machine (target rr → bank `bank_frac` → `bank_protect_k` → `runner_trail_k` → ttl), 'MATCHED DRAWDOWN. Every arm is solved to the same 35% budget', 'THE TRIAL COUNT IS PRINTED. 216 exits per sleeve is 216 chances to find a lucky one'; :56 `OUT = _DESK/"reports"/"exit_sweep.json"` (MEASURED)
  - desks/mt5/research/exit_accounts.py:1-12 — captured / left_on_table / adverse_survived per sleeve and per exit reason; :64 `REPORT = _DESK/"reports"/"EXIT_ACCOUNTS.json"`; runs from desks/mt5/research/daily_cycle.py:323 (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1284 'exit_study'; desks/mt5/research/daily_cycle.py:323 'excursions' and 'exit_accounts'. desks/mt5/research/exit_sweep.py: NONE · artifact: desks/mt5/reports/EXCURSIONS.json (present); reports/exit_study.json, reports/exit_sweep.json and reports/EXIT_ACCOUNTS.json (all ABSENT on disk) · consumer: libs/research/bandit SOURCE_ARM → exit_improvement budget share; nothing reads exit_study.json / exit_sweep.json / EXIT_ACCOUNTS.json anywhere
  - next: Add `exit_rule` as a third key in `desks/mt5/research/trial_allocator.py`'s cell-type tuple so exit variants enter the same certified/tried yield table as families, and the deflation the gauntlet charges includes them.
- **G12 Portfolio-combination search and portfolio alpha synthesis** — PARTIAL
  - gap: 'A conditioned on B' exists as the `trade_when` grammar operator and 'many weak signals combined' exists as `family_ensemble` + `weak_signal_compiler`, but weak_signal_compiler has run 0 tests, the general combination enumerator (libs/alpha_factory/combination_engine.py) has no scheduled caller, the hourly recombination engine's 60 artifacts have no consumer, and 'A entry + B exit' has no representation at all.
  - libs/research/alpha_grammar.py:67 — `BINARY = ("add","sub","mul","div","max2","min2","trade_when")`; `trade_when(condition, signal)` IS 'A conditioned on B' and is inside the searched space (MEASURED)
  - libs/alpha_factory/wq_operators.py:223 `def trade_when(condition: pd.Series, signal: pd.Series)` (MEASURED)
  - desks/mt5/mt5desk/family_ensemble.py:1-17 — 'A weighted vote over MEMBER cells … the members' individual results are irrelevant; only the combination faces the gauntlet, as ONE candidate with ONE certificate'; registered at desks/mt5/mt5desk/families_orthogonal.py:1468 `ORTHOGONAL_FAMILIES["ensemble"]` (MEASURED)
  - desks/mt5/research/weak_signal_compiler.py:1-20 — members are 'validity-passing, power-deficient cells', frozen combination judged out of sample, 'propose only if … beats its strongest single member out of sample'; :45 `SOURCE = "weak_signal_ensemble"`, :46 `REPORT = reports/weak_signal_compiler.json` (MEASURED)
  - clock: `trade_when` inside alpha_evolution (desks/mt5/research/hourly_discovery.py:81); weak_signal_compiler in desks/mt5/research/daily_cycle.py:260 _proposers and hourly_discovery ORGANS; alpha_recombination via ops/hourly-controller.timer. combination_engine: NONE · artifact: desks/mt5/data/intelligence/recombinants/REC-*.json (60); desks/mt5/reports/weak_signal_compiler.json (0 tests) · consumer: family_ensemble certificates go through the gateway (executables.resolve_family). RECOMBINANTS: NONE — the compiler mis-parses them into atom rows.
  - next: Make desks/mt5/side_channels/alpha_recombination.py write its REC rows under a `"discoveries": [...]` key in the miner contract (as libs/ops/deepseek_cycle._donate does) so miner_candidate_compiler._rows picks up the recombinant rather than its atoms.
- **G13 Representation learning / latent state / regime foundation model** — PARTIAL
  - gap: A contrastive/InfoNCE encoder with a proper alignment-uniformity diagnostic and an OOS `representation_gain` scorer EXISTS and is dead code: it has no importer outside a test and no scheduler. The only live representation is ml_layer's k=4 PCA over ret/vol windows on 6 series, whose output nothing but the dashboard reads. There is no shared market-state embedding that families or the allocator consume, and the desk's own frontier ranks REPRESENTATION_LEARNING as its single largest capability gap.
  - libs/models/embedding.py:1-9 — 'A self-supervised market embedding and the historical-analogue engine built on it … trained with a masked-reconstruction pretext'; :35 `class MarketEncoder`, :163 `class WindowPCA`, :202 `_infonce(v1, v2, w, …)`, :230 `class ContrastiveEncoder`, :307 Wang & Isola alignment/uniformity, :84 `analogues(z_now, z_hist, forward, k=20)`, :371 `representation_gain(windows, forward, encoder=...)` (MEASURED)
  - libs/models/embedding.py has NO production importer: grep for `models.embedding`, `MarketEncoder`, `ContrastiveEncoder` across the tree matches only the file itself and desks/mt5/tests/test_models_moe_adapt.py (MEASURED)
  - desks/mt5/research/ml_layer.py:118 `representation(close, k=4)` (PCA on the train slice only), :144 `pretext(close, mask=16)`, :186 `mixture(close, n_experts=3)`, :247 `distil(close, student_k=3)`; :50 `REPORT = reports/ML_LAYER.json`, :55 `HORIZONS = (2,4,8,16,32,64,128)` (MEASURED)
  - desks/mt5/research/hourly_cycle.py:1256 `mll = _costed("ml_layer", ml_layer)` → :978 `_producer("ml_layer", "desks/mt5/research/ml_layer.py")` — hourly (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1256 'ml_layer' only. libs/models/embedding.py: NONE · artifact: desks/mt5/reports/ML_LAYER.json (per-series representation/pretext/mixture/distillation verdicts); desks/mt5/data/state_vector.json · consumer: ML_LAYER.json is read by scripts/build_zentech_state.py (dashboard) — the representation is not consumed by any allocator, gate or family. libs/models/embedding.py: NONE (test only).
  - next: Have `desks/mt5/research/ml_layer.representation()` call `libs.models.embedding.ContrastiveEncoder` alongside `WindowPCA` and report `representation_gain` for both, so the contrastive encoder is exercised on the hourly leg and its OOS gain is on the board next to PCA's.
- **G14 Separation of information / prediction / timing / sizing / portfolio / execution / exit layers** — PARTIAL
  - gap: Six of the seven layers have dedicated, scheduled engines, so the separation exists de facto — but there is no declared layer taxonomy, no per-layer trial ledger, and no per-layer research budget: the bandit's 11 arms cut the space by mechanism source, not by layer, so 'how much of last month's research went into sizing' is unanswerable. The TIMING layer (hour_surface/hour_prior) is entirely unscheduled and has produced nothing.
  - INFORMATION: libs/research/information_value.py; libs/regime/state_vector.py:40 StateVector (session/event/liquidity/factors/conditioning/freshness/gaps); desks/mt5/research/state_vector_build.py:64 OUT data/state_vector.json — desks/mt5/research/hourly_cycle.py:1018 (MEASURED)
  - PREDICTION: desks/mt5/research/ml_layer.py (desks/mt5/research/hourly_cycle.py:1256), desks/mt5/research/model_zoo.py:52 `REPORT = reports/MODEL_ZOO.json` + :53 `LEAGUE = reports/CHALLENGE_LEAGUE.json` (desks/mt5/research/hourly_cycle.py:1191), desks/mt5/research/forecast_contract.py (desks/mt5/research/hourly_cycle.py:1190) (MEASURED)
  - TIMING: desks/mt5/research/hour_surface.py:42 `OUT = reports/hour_surface.json` and desks/mt5/research/hour_prior.py:42 `OUT = reports/hour_prior.json`; desks/mt5/research/session_phase.py — but hour_surface/hour_prior are on NO scheduler and neither artifact exists on disk (MEASURED)
  - SIZING: desks/mt5/research/sizing_study.py, desks/mt5/research/book_sizing.py; libs/portfolio/kelly_surface.py; desks/mt5/mt5desk/sizing.py (MEASURED)
  - clock: Each layer's engines are individually scheduled (desks/mt5/research/hourly_cycle.py legs 1018/1187/1190/1191/1256/1284, desks/mt5/research/daily_cycle.py:462 STEPS) — but as a set of organs, not as declared layers. Timing layer: NONE · artifact: One artifact per engine (state_vector.json, ML_LAYER.json, MODEL_ZOO.json, pf_allocation.json, execution_quality.json, EXCURSIONS.json). No per-layer roll-up artifact exists. · consumer: libs/research/bandit.py allocates budget across 11 arms, of which only execution_improvement and exit_improvement correspond to a blueprint layer
  - next: Add `"timing"` and `"sizing"` arms to `libs/research/bandit.ARMS` and map hour_surface / hour_prior / sizing_study into `SOURCE_ARM`, then schedule desks/mt5/research/hour_surface.py as a leg in desks/mt5/research/daily_cycle.py `_state_research_feedback` — one edit makes the missing layer both budgeted and run.
- **G15 Persistent specialist agent roles as scheduled organs** — PARTIAL
  - gap: Five of the six blueprint roles exist as DeepSeek seed roles on an hourly timer, and four physical seats have their own timers — but the cartographer's artifact is the missing one: alpha_periodic_table.py (the mechanism × axis white-space matrix) has no caller anywhere, and the running white-space walker (frontier_intel/unknowns.py) reports zero unknown firms and zero unknown capabilities, so the desk has no maintained map of where it has not looked.
  - libs/ops/deepseek_cycle.py:73 `SEED_ROLES` — 36 roles, of which the blueprint's six map as: mechanism scientist = `cold_alpha_inventor` ('economically plausible hypotheses from first principles'); literature miner (multilingual) = `multilingual_intelligence` ('mine CN/KR/JP/RU/PT/ES/AR/TR/VI/ID/HI ecosystems') + `external_replication_factory`; cross-domain transfer = `cross_domain_transfer`; mutation agent = `alpha_recombination` + `near_survivor_repair`; cartographer = `negative_space_miner` ('low-coverage cells across data x mechanism x asset x venue x horizon x regime x representation x execution x label') + `unknown_unknown_explorer`; hypothesis synthesizer = `experiment_designer` (MEASURED)
  - libs/ops/deepseek_cycle.py:66 comment — 'Roles are SEEDS, not permanent bureaucracy — spawnable, splittable, mergeable, retirable … so the allocator can rank them by measured marginal contribution' (MEASURED)
  - SCHEDULED: ops/quant-deepseek.timer `OnCalendar=*-*-* *:20:00 UTC`; ops/quant-seat-dataaxis.timer 05:00; ops/quant-seat-prospector.timer 06:00; ops/quant-seat-frontier2.timer 06:20; ops/quant-seat-litminer.timer 07:00; ops/quant-seat-frontier.timer 08:00; ops/quant-video-hunter.timer 10:30; ops/quant-blindrediscovery.timer `*-*-01 20:00`; ops/quant-brain-chain.timer 09:00; ops/quant-midnight-frontier.timer 00:00 Europe/Dublin (MEASURED)
  - ops/crontab.research.manifest carries fallback cron rows for every seat, gated on `systemctl is-enabled quant-<seat>.timer` (MEASURED)
  - clock: ops/quant-deepseek.timer (:20 hourly, 36 roles); ops/quant-seat-{dataaxis,prospector,frontier,frontier2,litminer}.timer; ops/quant-blindrediscovery.timer; ops/quant-video-hunter.timer; desks/mt5/research/hourly_cycle.py:1253/1280/1282 · artifact: data/intelligence/hypotheses/H-*.yaml; data/cro_ai_logs/seat_*.log and frontier_unified_*.log; desks/mt5/reports/RESEARCH_ORG.json; desks/mt5/reports/FRONTIER_INTELLIGENCE.json · consumer: miner_candidate_compiler (seat donations, currently 0 rows); desks/mt5/research/frontier_intel/implementer.py --apply (desks/mt5/research/hourly_cycle.py:1242) turns a scored gap into a challenger under frontier_intel/challengers/
  - next: Add `alpha_periodic_table` as a leg in desks/mt5/research/daily_cycle.py `_state_research_feedback` (it needs no network and writes a matrix of empty cells) so the white-space map is produced on a cadence and can be joined to regime_coverage's uncovered buckets.
- **G16 Quality-diversity search (MAP-Elites archive)** — MISSING
  - gap: There is no archive keyed on behaviour descriptors. The blueprint's six-axis grid (mechanism × horizon × regime × asset × factor exposure × execution style × tail behaviour) has no representation: the search keeps one Pareto front of 8, so a candidate that is the best thing the desk has ever found in 'Asia + high-vol + short-horizon' is discarded the moment eight globally-better trees exist.
  - grep -riE 'map.?elites|quality.diversity|behaviou?r.?descriptor|elite.?archive|illuminat' across libs/, desks/mt5/ and scripts/ returns ZERO matches (MEASURED)
  - The only 'elite' in the search is a single global list: desks/mt5/research/alpha_evolution.py:93 `POP, GENS, ELITE, DEPTH = 40, 5, 8, 3`; :382 and :474 keep `[:ELITE]` by fitness; :432 'the elite is the Pareto front over the fitness terms, ordered by crowding distance' — one front, no descriptor grid, no per-niche champion (MEASURED)
  - libs/research/alpha_fitness.py:673 `crowding_distance(rows, front)` and :702 NSGA-II ordering — diversity is preserved WITHIN a Pareto front, never across an archive of behaviour cells (MEASURED)
  - libs/research/search_populations.py:723-732 `POPULATIONS` — nine populations sharing ONE budget and deduped by structural hash (:741 'THE DEDUPE IS ACROSS POPULATIONS'), which spreads the search but keeps no archive (MEASURED)
  - clock: NONE · artifact: NONE (alpha_habitat's reports/market_fingerprints.json has never been written on this box) · consumer: NONE
  - next: Add a descriptor-keyed dict to `desks/mt5/research/alpha_evolution.Evaluator` — cell = (mechanism_class, horizon bucket from `hold_bars`, top regime label from data/state_vector.json, symbol asset_class) — and keep the best expression per cell alongside the existing `ELITE` list, so the elite becomes an archive rather than a single front.
- **G17 Cross-frequency research (D1 state → H1 setup → M5 execution)** — PARTIAL
  - gap: Timeframe IS a fixed per-family/per-certificate parameter, exactly what the blueprint says it should not be — and the data cannot support the alternative: 101 symbols have H1 only, 4 have M15, 1 has M5/M1, and NO symbol has a D1 or H4 file, so 'D1 state → H1 setup → M5 execution' is buildable for XAUUSD alone and only by resampling the daily leg from H1.
  - DATA RESOLUTION, MEASURED: desks/mt5/data/universe contains 117 files — 101 `*_H1.parquet`, 4 `*_M15.parquet` (AUDCAD, AUDNZD, NZDCAD, XAUUSD), 1 `XAUUSD_M5.parquet`, 1 `XAUUSD_M1.parquet`, plus universe.json/canon/stumps. There is NO `*_D1.parquet`, NO `*_H4.parquet`, NO `*_M30.parquet` and NO `*_W1.parquet` anywhere (MEASURED)
  - TAPE, MEASURED: desks/mt5/data/tape/ contains only depth_probe.json and triangle_executable.json — no tick partitions on this checkout (MEASURED)
  - desks/mt5/research/state_vector_build.py:86 `ASSET_CLOCKS = ("weekly","daily","H4","H1","M15","M5")` and :89 `BAR_SUFFIXES = ("M5","M15","H1")` — the state builder declares six clocks and can only load three file suffixes; :80 `FACTOR_CLOCKS = ("daily",)` (MEASURED)
  - desks/mt5/research/orthogonal_sweep.py:202 `_resample_rule(timeframe)` and :535 `timeframes_of(symbol, meta)` — higher clocks are RESAMPLED from H1 rather than sourced (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1229 'timeframe_coverage' (the fence). The causal hierarchy itself: NONE · artifact: desks/mt5/data/state_vector.json carries per-clock AssetStates for the three suffixes that exist · consumer: pf_allocator and the gateway read state_vector.json; executables.executor_gap enforces the certificate's own chart
  - next: Extend `desks/mt5/research/refresh_tail.py` / the universe pull to write `<SYM>_D1.parquet` for the book's symbols (a D1 resample of H1 is already computed in orthogonal_sweep._resample_rule), so `state_vector_build.BAR_SUFFIXES` can carry the daily clock it already declares in ASSET_CLOCKS.
- **G18 Negative-correlation / drawdown alpha, liquidity alpha, regime transitions, time-of-day capital** — PARTIAL
  - gap: The drawdown-alpha FEEDSTOCK is LIT (the book's worst periods are published and read), but the two engines meant to search it — tail_alpha_search and transition_alpha — are scheduled and have run ZERO tests, and the time-of-day capital market is the sharpest miss: a measured +0.164R hour at t=+12.9 carries 0.04% of heat while a −0.032R hour carries 8.53%, and the module that would turn that into an allocator prior is on no clock and has never written its file.
  - DRAWDOWN ALPHA — LIT: desks/mt5/research/drawdown_alpha.py:1-8 quotes the mandate ('Find another edge that earns +40 precisely during those -80 periods'); :61 `OUT = BASE/"reports"/"DRAWDOWN_ALPHA.json"`; scheduled at desks/mt5/research/daily_cycle.py:323; artifact present (162,915 bytes, 2026-09-05 17:29); consumers desks/mt5/research/survivor_neighbourhood.py and libs/ops/capability_graph.py (MEASURED)
  - desks/mt5/mt5desk/families_orthogonal.py:1139 registers `"drawdown_conditional": family_drawdown_conditional` — an executable family for the same idea (MEASURED)
  - TAIL SEARCH — DARK: desks/mt5/research/tail_alpha_search.py:37 `SOURCE = "tail_alpha"`, :38 `REPORT = reports/tail_alpha_search.json`; scheduled in desks/mt5/research/daily_cycle.py:260 and desks/mt5/research/hourly_discovery.py:81 — but reports/tail_alpha_search.json is 152 bytes with `tests_run: 0, cells_proposed: 0`, and there is no data/intelligence/tail_alpha donation dir (MEASURED)
  - LIQUIDITY ALPHA: desks/mt5/mt5desk/families_orthogonal.py:639 `"liquidity_regime": family_liquidity_regime`; desks/mt5/mt5desk/families_edge_queue.py:264 `"liquidity_gamma_reversal"`; libs/regime/liquidity_state.py; desks/mt5/research/state_vector_build.py:241 `liquidity_state(symbols, now, …)` (MEASURED)
  - clock: desks/mt5/research/daily_cycle.py:323 'drawdown_alpha'; desks/mt5/research/daily_cycle.py:260 + desks/mt5/research/hourly_discovery.py:81 'transition_alpha' and 'tail_alpha_search'. hour_surface / hour_prior: NONE · artifact: desks/mt5/reports/DRAWDOWN_ALPHA.json (present, 2026-09-05); reports/tail_alpha_search.json and reports/transition_alpha.json (present but zero-yield); reports/hour_surface.json and reports/hour_prior.json (ABSENT) · consumer: DRAWDOWN_ALPHA.json → desks/mt5/research/survivor_neighbourhood.py + libs/ops/capability_graph.py. hour_surface.json → desks/mt5/research/hour_prior.py only (its producer's producer). tail_alpha / transition_alpha → miner_candidate_compiler, which has recorded 0 rows from either.
  - next: Add `"hour_surface"` and `"hour_prior"` to the module list in `desks/mt5/research/daily_cycle.py:323` `_state_research_feedback` (both are pure readers of the shadow ledgers, no network, no terminal), which makes reports/hour_prior.json exist for the pf_allocator branch that already looks for it.
- **G19 Automated simplification of survivors and alpha distillation** — PARTIAL
  - gap: Model-level distillation runs hourly and is admitted, and complexity is penalised inside the search — but there is no pass that takes an ALREADY-CERTIFIED survivor and re-judges it with a condition or a parameter removed: survivor_distiller only steps parameters toward the survivor median (it adds neighbours, never subtracts terms), and the latent-factor compression in libs/portfolio/latent_factors.py never becomes a meta-strategy certificate.
  - MODEL DISTILLATION — LIT: desks/mt5/research/ml_layer.py:247 `distil(close, student_k=3)`; reports/ML_LAYER.json (2026-09-06) shows `distillation: {name: distilled_student, admitted: true, skill: -0.00066, baseline: -0.00696, n_oos: 3828}`; scheduled at desks/mt5/research/hourly_cycle.py:1256 (MEASURED)
  - COMPLEXITY PRESSURE INSIDE THE SEARCH: libs/research/alpha_fitness.py:107-110 `complexity` weight 0.03 in `PENALTIES`; libs/research/alpha_grammar.py:933 `complexity(expr)`; desks/mt5/research/alpha_evolution.py:452 hard cap `ag.complexity(e) > 14` (MEASURED)
  - desks/mt5/research/survivor_distiller.py:1-23 — it is a PRIOR + NEIGHBOUR proposer ('prior[family][param] = (median, IQR) among CERTIFIED vs among FAILED/BURIED … neighbours(cert) = K untested cells one grid step from the certificate'), named operators `step_<param>_up`, `step_<param>_down`, `swap_<param>` for mutation_yield to bill; :45 SOURCE, :46 `REPORT = reports/SURVIVOR_DISTILLER.json` (MEASURED)
  - MEASURED: desks/mt5/data/intelligence/survivor_distiller/ holds 52 donation files (latest discoveries_20260906_2239.json), but reports/SURVIVOR_DISTILLER.json is ABSENT from desks/mt5/reports/ (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1256 'ml_layer' (distillation); desks/mt5/research/daily_cycle.py:260 + desks/mt5/research/hourly_discovery.py:81 'survivor_distiller'. alpha_clusters, meta_architect: NONE · artifact: desks/mt5/reports/ML_LAYER.json (distillation verdict); data/intelligence/survivor_distiller/discoveries_*.json (52 files); reports/SURVIVOR_DISTILLER.json ABSENT · consumer: ML_LAYER.json → scripts/build_zentech_state.py (dashboard only). survivor_distiller donations → miner_candidate_compiler → the gauntlet.
  - next: Add a `simplify(cert)` generator to desks/mt5/research/survivor_distiller.py that emits, per certificate, the cells with each optional condition dropped in turn (tagged operator `drop_<param>`, which mutation_yield already knows how to bill), so parsimony is proposed through the same door as every other mutation.
- **G20 Research-ensemble disagreement rewarded** — MISSING
  - gap: Nothing anywhere converts cross-engine agreement/disagreement into a score or a budget. The four engines write into one docket keyed on the same node id, so a cell proposed by the symbolic search AND the causal graph is indistinguishable in the queue from one proposed by a single crawler; agreement is not rewarded and disagreement is not flagged as high-information.
  - libs/research/alpha_fitness.py:107-110 `WEIGHTS` has no disagreement term; libs/research/bandit.py:51 `ARMS` has no disagreement arm; libs/research/alpha_grammar.py has no disagreement construct (MEASURED by absence)
  - NEAREST MISS 1: libs/research/alpha_frontier.py:263 `def useful_disagreement(reviews: Sequence[Mapping[str, object]])`, :272 returns `"disagreement_rate": disputed / len(by_case)` — this measures disagreement between REVIEWERS of a case, not between the statistical / LLM / symbolic / causal engines, and no scheduled organ calls it (MEASURED)
  - NEAREST MISS 2: libs/research/cohort_independence.py:26-32 — 'They answer the same question from opposite directions, and where they disagree the disagreement is the finding: a participation ratio far above the equicorrelation estimate means the cohort has a few strong clusters rather than uniform redundancy' — stated in prose, never computed as a scored quantity; the module's importers (libs/research/panel_breadth.py, libs/research/effective_breadth.py, crowding.py, libs/research/marginal_admission.py, libs/portfolio/concentration.py) all consume `n_eff`, not the disagreement (MEASURED)
  - NEAREST MISS 3: libs/research/agents.py:9 — 'nothing in the arrangement makes disagreement …'; desks/mt5/research/research_org.py:7 'P54 — THE ROLES ARE SEPARATE, AND THE SEPARATION IS THE MECHANISM. Four roles, and no agent may …'; :54 `ROLES = ("ADVOCATE","SKEPTIC","REPLICATOR","VALIDATOR")` — separation is enforced so disagreement is POSSIBLE, but the verdict is a pass/fail review, not an information score (MEASURED)
  - clock: NONE · artifact: NONE · consumer: NONE
  - next: In `desks/mt5/research/miner_candidate_compiler.py`, after `record_candidates(...)`, count distinct `source` values per compiled `node_id` and write it onto the candidate as `n_independent_sources` — one field turns the existing dedup collision into a measurable agreement signal that `trial_allocator` can order on.
- **G21 Portfolio-generated research missions / unused-heat intelligence** — PARTIAL
  - gap: The unused-heat loop is wired at both ends — the allocator's gaps become queue rows and the queue has real consumers — but the mission never names a GENERATOR: all 47,150 queue rows carry `kind: None` and no producer field, so a row saying 'nothing pays in Asia + low vol' cannot be routed to the engine that could answer it. And the half that prices the missing heat is dark: portfolio_gap.py is a scheduled step that has never written reports/portfolio_gap.json, and every rail in MISSED_GROWTH.json reads 'no allocator pass on this host'.
  - desks/mt5/research/portfolio_gap.py:1-12 — 'THE PORTFOLIO GAP — every unfilled point of heat becomes a research request … one that says "4% unfilled, and the desk owns nothing that trades the 00-04 UTC band outside JPY" is a search'; :39 `OUT = BASE/"reports"/"portfolio_gap.json"`; :284 `OUT.write_text(...)`; scheduled at desks/mt5/research/daily_cycle.py:323 (MEASURED)
  - MEASURED: desks/mt5/reports/portfolio_gap.json DOES NOT EXIST on this box — the scheduled leg has never produced its artifact (MEASURED)
  - desks/mt5/research/opportunity_gap.py:1-16 — G decomposed by cause into SUPPLY / CONVERSION / BREADTH / CAPITAL / EXECUTION / DELIVERY, each with a named fix; hourly leg at desks/mt5/research/hourly_cycle.py:1258; artifact desks/mt5/reports/OPPORTUNITY_GAP.json present (2026-09-06 00:53) (MEASURED)
  - desks/mt5/research/regime_coverage.py:1-20 — 'Where the book has no edge, said precisely enough that research can go and find one … THE INSTRUCTION IS MACHINE-READABLE. Each uncovered bucket is written into the deepening queue as a research task naming the state, the families already tried there and what they returned, and the families structurally absent from it'; artifact REGIME_COVERAGE.json shows 3 uncovered buckets each with a `families_never_tried_here` list (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1258 'opportunity_gap' and :1250 'queue_compact'; desks/mt5/research/daily_cycle.py:323 'regime_coverage', 'missed_growth', 'portfolio_gap', 'alpha_breadth', 'drawdown_alpha'; desks/mt5/research/hourly_discovery.py:81 'trial_allocator' · artifact: desks/mt5/reports/OPPORTUNITY_GAP.json; REGIME_COVERAGE.json; MISSED_GROWTH.json; TRIAL_ALLOCATION.json; data/research_queue.jsonl; data/MINER_YIELD_ALARM.txt; data/PRINCIPAL_ACTION.md. reports/portfolio_gap.json ABSENT · consumer: research_queue.jsonl → desks/mt5/research/trial_allocator.py, desks/mt5/research/research_loop.py, conversion_ledger.py, desks/mt5/research/meta_desk.py; REGIME_COVERAGE uncovered buckets → the deepening queue → desks/mt5/research/deepening_worker.py
  - next: Have `desks/mt5/research/regime_coverage._merge_into_queue` stamp each task with a `target_engine` chosen from `libs/research/bandit.SOURCE_ARM` (e.g. an uncovered state → `conditional_state_edge` → transition_alpha), so the deepening queue's rows address a generator instead of a general audience.

### Phase 3 — Alpha knowledge graph: research, failures, mechanisms, strategies, data, correlations, certificates, live results as one memory

- **A1 AlphaGenome — one canonical object every strategy compiles into** — PARTIAL
  - gap: Four incompatible shapes coexist (8-field compiler candidate, 8-field certificate, 11-field graph node, 22-field StrategyArtifact) and the only rich one is built in memory inside promoter.artifact_of and thrown away except for its hash; trial_family_id, effective_trials, novelty_vector and live_attribution exist on no object at all.
  - libs/research/strategy_artifact.py:28-54 — `class StrategyArtifact` is the nearest canonical object: strategy_id, mechanism, source, family, params, symbols, timeframes, entry, exit, execution, state_conditioning, data_requirements, feature_ids, cost_assumptions, validation_certificate, lockbox_certificate, shadow_evidence, factor_exposures, expected_posterior, risk_response, schema_version, version_hash. Its docstring calls it 'the only object that may reach the allocator' (MEASURED)
  - libs/research/strategy_artifact.py:63-86 — `from_certificate(key, cert)` builds one from a canon row; libs/research/strategy_artifact.py:89-106 `validate()` refuses no-family/no-symbol/no-cost-basis (MEASURED)
  - desks/mt5/research/promoter.py:198-236 — `artifact_of(row)` is the ONLY production importer; it returns `{version_hash, ok, problems}` and discards the object. desks/mt5/research/promoter.py:239-253 `save_sleeves` stamps only that dict onto LIVE rows of sleeves.json (MEASURED)
  - libs/research/strategy_artifact.py:13 names `research/strategy_artifacts.py` as the registry writer — that file does not exist on this tree (MEASURED: `ls desks/mt5/research/strategy_artifacts.py` -> No such file)
  - clock: desks/mt5/ops/box_tasks.manifest:63 TASK MT5-Hourly -> desks/mt5/research/hourly_cycle.py; alpha_genome via desks/mt5/research/daily_cycle.py:324 (_state_research_feedback, STEPS line 472); promoter via desks/mt5/research/daily_cycle.py:470 and desks/mt5/research/hourly_cycle.py:1176 · artifact: desks/mt5/reports/ALPHA_GENOME.json + desks/mt5/data/sleeves.json (row['artifact'] = version_hash only) · consumer: desks/mt5/research/regime_coverage.py:63 (GENOME) reads ALPHA_GENOME.json; libs/ops/module_rent.py:216 rents it; nothing reads a full StrategyArtifact
  - next: Add a `strategy_artifacts.jsonl` writer inside desks/mt5/research/promoter.py:239 `save_sleeves` that persists `a.to_dict()` (not just version_hash) for every LIVE row, so one full genome per traded sleeve exists on disk.
- **A2 Alpha Knowledge Graph — typed nodes/edges and queryability** — PARTIAL
  - gap: The graph has exactly one edge type (parent) and no dataset/regime/contradiction/correlation edges, so a query like 'unexplored mechanisms relating US rates -> USDJPY during Asia never tested below H1' cannot be expressed: there is no timeframe axis on a node, no dataset node, and no regime edge.
  - libs/research/hypothesis_graph.py:94-181 `class Graph` with rows()/current()/buried()/prior_failures()/lineage()/census(); LEDGER = desks/mt5/data/hypothesis_graph.jsonl (libs/research/hypothesis_graph.py:34). MEASURED on disk: 30,313 rows, 16 MB
  - MEASURED census of desks/mt5/data/hypothesis_graph.jsonl: fates {FAILED: 30208, BORN: 57, CERTIFIED: 48}; sources {external: 30256, fund_playbook: 57}; newest row `at` = 2026-09-03T01:44:12
  - libs/research/hypothesis_graph.py:56 — the ONLY edge is the scalar `parent` field plus `fate`. Repo-wide grep for the blueprint edge names `uses_data|applies_to_symbol|tested_in_regime|mutated_from|failed_because|correlated_with|contradicts|survived_as` returns zero code hits (MEASURED; only prose uses of 'contradicts')
  - libs/research/memory.py:37-43 — the typed memory: MEMORY_DIR = desks/mt5/data/memory, KINDS = ('fact','hypothesis','failure','survivor','execution','regime','method'), with `recall`/`digest`/`prompt_context`. MEASURED: desks/mt5/data/memory DOES NOT EXIST on this tree, so no kind ledger has ever been written
  - clock: desks/mt5/research/hourly_cycle.py:759 compile_candidates leg (the only scheduled writer path into the graph) and desks/mt5/research/hourly_cycle.py:733 causal_graph leg; knowledge_engine at ops/crontab.manifest:832 · artifact: desks/mt5/data/hypothesis_graph.jsonl (30,313 rows) · consumer: desks/mt5/research/deepening_worker.py:416,463 (prior_failures in voi_order); libs/research/bandit.py:313,334 (per-arm Beta evidence); desks/mt5/research/mutation_yield.py:81; desks/mt5/research/revival_engine.py:93; libs/research/memory.py:275
  - next: Add a typed `edges: list[dict]` field to `Node` in libs/research/hypothesis_graph.py:48 and have record_candidates emit `uses_data`/`applies_to_symbol`/`mutated_from` edges from fields the compiler already has (source_url, symbol, parent).
- **A3 Negative-knowledge moat / graveyard queried before generation** — PARTIAL
  - gap: The per-failure record keeps symbol/family/params/region/source/parent/fate/why/gates but NOT sample size, cost sensitivity, correlation profile, where it worked vs broke, or a reopening condition; and the generator-side annotation demonstrably is not landing (632/632 candidates carry no premortem, and no compiler row has ever reached the ledger).
  - libs/research/hypothesis_graph.py:141-159 — `buried()` indexes FAILED/BURIED rows by coarsened parameter region (REGION_WIDTH at libs/research/hypothesis_graph.py:38-42), and `prior_failures(symbol, family, params)` returns {region, n_failed, gates_failed, last_why} (MEASURED)
  - libs/research/graveyard_model.py:98-159 — `GraveyardModel.fit()/premortem()` over ten declared failure classes (CLASSES at line 37) with `first_test` falsifier per class (FIRST_TEST at line 50) (MEASURED)
  - desks/mt5/research/miner_candidate_compiler.py:765-786 — the generator-side query: prior_failures stamps `prior_failures_in_region` and `region`, then GraveyardModel().fit(g.rows()) stamps `premortem` on every candidate, then record_candidates registers them BORN (MEASURED)
  - MEASURED CONTRADICTION: desks/mt5/data/hypotheses/miner_candidates.json (mtime 2026-09-08 21:05) contains 632 candidates and ZERO carry `premortem`, `prior_failures_in_region` or `region` — the per-key count is exactly 632 for the eight base fields and 0 for all three enrichment fields. The whole try-block at desks/mt5/research/miner_candidate_compiler.py:765 is therefore not landing
  - clock: desks/mt5/research/hourly_cycle.py:1063 compile_candidates leg + desks/mt5/ops/box_tasks.manifest:64 MT5-Deepening (deepening_worker.voi_order reads prior_failures at desks/mt5/research/deepening_worker.py:463) · artifact: desks/mt5/data/hypothesis_graph.jsonl; docs/graveyard.md · consumer: desks/mt5/research/deepening_worker.py:416-470 (VOI ordering), libs/research/bandit.py:313 (arm posteriors), desks/mt5/research/mutation_yield.py:81, desks/mt5/research/revival_engine.py:93
  - next: Replace the bare `except Exception: pass` around the premortem block in desks/mt5/research/miner_candidate_compiler.py:777-783 with a printed reason, and log the outer exception at line 785 to the compiled artifact so the reason the graph write fails is visible.
- **A4 Research memory — retrieval-conditioning / lesson injection at runtime** — PARTIAL
  - gap: Lesson memory is genuinely live, but the half that conditions on the desk's own hypotheses/failures/certificates (libs/research/memory.py) has never written a single row, so the deepening worker's prompt carries doctrine and lessons but no graveyard/certificate retrieval.
  - libs/research/desk_memory.py:1-54 — the compounding lesson corpus: BUDGET_CHARS (line 102), score = COST_WEIGHT x (1 + log2(recurrence)), routed per organ by `corpus(organ=...)`, `reach()` reports what fraction is read by somebody (MEASURED)
  - docs/desk_lessons.jsonl — 254,986 bytes, mtime 2026-09-08 (MEASURED: live and growing)
  - ops/brain_env.sh:683-688 — runtime injection: `_ORGAN` derived from the calling run_<organ>.sh, then `_MEMORY=$(.venv/bin/python scripts/learn.py render --organ "$_ORGAN")` appended to `_DOCTRINE`. Sourced by 20+ organ runners (ops/run_cro_ai.sh, ops/run_frontier_miner.sh, ops/run_deepseek_factory.sh, ops/run_capability_hunt.sh, ops/run_litminer_dig.sh, ...) (MEASURED)
  - scripts/learn.py:273-301 — subcommands render/add/recur/graduate/audit; scripts/lessons.py:1-40 — search, --id, --tag, --orphans over the same ledger (MEASURED)
  - clock: quant-prompt-prefix.timer (04,16:50 UTC) for the prefix; lesson injection is event-driven at every organ launch via ops/brain_env.sh; libs/research/memory.build_from_artifacts via desks/mt5/research/daily_cycle.py:368-375 · artifact: docs/desk_lessons.jsonl, data/prompt_prefix.json; desks/mt5/data/memory/*.jsonl NEVER PRODUCED · consumer: every organ that sources ops/brain_env.sh (doctrine injection); scripts/max_audit.py:2571 reads desk_memory.unreached; desks/mt5/research/deepening_worker.py:236 would read memory.prompt_context
  - next: Promote `build_from_artifacts()` out of the trailing try/except in desks/mt5/research/daily_cycle.py:368 into its own named STEP in the STEPS tuple (desks/mt5/research/daily_cycle.py:462-474) so its failure is logged and visible rather than swallowed.
- **A6 Discovery Frontier tensor (Asset x Timeframe x Mechanism x State x Dataset x Execution x Horizon)** — PARTIAL
  - gap: Six partial coverage maps on different axes and no join: no artifact carries Asset x Timeframe x Mechanism x State x Dataset x Execution x Horizon as one cell key, there is no Dataset or Execution axis at all, and the cell-status vocabulary is 4 values (hunt_frontier) or 2 (covered/uncovered) rather than the seven the blueprint names.
  - libs/research/semantic_space.py:22-40 and libs/research/alpha_schema.py:20-45 — a FIVE-axis coordinate EVENT x CONTEXT x QUALITY x DIRECTION x OUTPUT(horizon), 11 x 15 x 8 x 6 x 6 = 47,520 cells, with `coverage()` reporting attempts per REGION and 'BARREN REQUIRES EVIDENCE (LAWS L1.51)' so under-attempted regions read UNTESTED not barren (MEASURED)
  - libs/research/semantic_space.py consumers: libs/research/edge_queue.py:31 (Coordinate), scripts/compile_proposals.py:391, scripts/run_research_intake.py:137-154 (scheduled ops/crontab.manifest:3416, `45 10 * * *`) (MEASURED)
  - desks/mt5/research/regime_coverage.py:1-21 — the STATE axis with real coverage semantics: per bucket `n_trades`, `n_covering`, `families_tried`, `families_never_tried_here`, `clusters_never_tried_here`, and machine-readable research instructions written into the deepening queue. MEASURED desks/mt5/reports/REGIME_COVERAGE.json (2026-09-04): dimensions ['global','event','weekday'], n_buckets 5, n_uncovered 3
  - data/strategy_coverage.json (2026-09-05) — MEASURED: n_families 11, n_hunted 4, n_thin 7, n_unhunted 0, status THIN, next_family 'ORDER-FLOW-POSITIONING'; law L1.32/L1.31
  - clock: regime_coverage via desks/mt5/research/daily_cycle.py:325; timeframe_coverage via desks/mt5/research/hourly_cycle.py:1229; strategy_coverage/exploration via ops/crontab.manifest:588; semantic_space via scripts/run_research_intake.py (ops/crontab.manifest:3416). No scheduler runs desks/mt5/research/backfill_coverage.py · artifact: desks/mt5/reports/REGIME_COVERAGE.json, data/strategy_coverage.json, data/strategy_breadth.json, desks/mt5/data/intelligence/frontier_coverage.json; coverage_search_results.json is empty; data/hunt_coverage.json and frontier_coverage per-cell tensor absent · consumer: REGIME_COVERAGE feeds the deepening queue (regime_coverage.py:16-18) and reads ALPHA_GENOME clusters (regime_coverage.py:63); strategy_coverage read by scripts/check_exploration.py and the breadth fences
  - next: Extend the bucket key in desks/mt5/research/regime_coverage.py (which already joins state x family x cluster) with `timeframe` from the certificate's shadow_spec, giving the first three-axis cell and a coverage denominator that is not implicitly H1.
- **A9 Feature genealogy — do many alphas descend from the same variable (fake breadth)** — PARTIAL
  - gap: Breadth is measured at family/mechanism/cluster level and at return-correlation level, but never at VARIABLE level: feature_ids is empty on every artifact, so 'these twelve alphas are all the 20-day range' is not a question any module can answer.
  - desks/mt5/research/alpha_genome.py:170-190 `run()` — MEASURED output desks/mt5/reports/ALPHA_GENOME.json: n_sleeves 45, n_clusters 4, structural_breadth 0.089. That number IS the fake-breadth reading, computed BEFORE returns (desks/mt5/research/alpha_genome.py:5-7: 'both are STATISTICAL and both can miss a duplicate ... This is the STRUCTURAL view')
  - libs/research/breadth.py:66 `effective_breadth(n, rho)` and desks/mt5/reports/EFFECTIVE_BREADTH.json (2026-09-05) — the returns-based k_eff, with an honest gaps block naming 19 sleeves dropped for no price history (MEASURED)
  - data/strategy_breadth.json (2026-09-05) — MEASURED: status NARROW, n_families 11, n_hunted 4, hunted_fraction 0.364 against a floor of 0.5
  - desks/mt5/research/feature_roi.py:1-25 — FeatureROI_j = dE[logW | F_j] / (acquisition + compute + maintenance + multiplicity), joining feature-store sidecars with the conditioning ledger and RESEARCH_PNL share_of_heat, writing a lifecycle state onto every sidecar; scheduled desks/mt5/research/daily_cycle.py:346 (MEASURED)
  - clock: alpha_genome + feature_roi via desks/mt5/research/daily_cycle.py:324,346 (_state_research_feedback); alpha_breadth via desks/mt5/research/daily_cycle.py:331 · artifact: desks/mt5/reports/ALPHA_GENOME.json (structural_breadth 0.089), desks/mt5/reports/EFFECTIVE_BREADTH.json, desks/mt5/data/effective_breadth.jsonl, desks/mt5/reports/FEATURE_ROI.json · consumer: regime_coverage.py:63 reads ALPHA_GENOME; libs/research/breadth_credit.py:218 reads effective_breadth; libs/research/bandit.breadth_credit() prices duplicate clusters into the research budget
  - next: Populate `feature_ids` in libs/research/strategy_artifact.from_certificate (libs/research/strategy_artifact.py:81) from the certificate's params key set plus params['factor_symbols'], then count alphas per feature_id in desks/mt5/research/feature_roi.py.
- **A10 Mechanism genealogy — one trade wearing different code; correlation clusters in allocator/promoter** — PARTIAL
  - gap: The structural genealogy is computed and read by research, but its cluster ids never reach the allocator or the promoter, so the book can hold 20 sleeves in one cluster and nothing in the sizing path knows it — contrary to alpha_genome's own docstring claim about the allocator artifact.
  - desks/mt5/research/alpha_genome.py:136-167 `cluster()` — union-find over (mechanism, direction_bias, clock) plus a shared currency leg or factor role; the docstring at desks/mt5/research/alpha_genome.py:17-21 states the test exactly: 'would these lose money on the same day for the same reason' (MEASURED)
  - MEASURED output: desks/mt5/reports/ALPHA_GENOME.json largest_clusters[0] = 'breakout/with/asia/CAD+EUR+GBP+JPY+USD' with n=20 members (session_range_breakout across CADJPY/EURJPY/GBPJPY at rr=1.5/2.0/2.5), and cluster[1] = 'reversion/against/asia/...' with n=12 overnight_gap_decay rows sharing ONE param hash p=44136fa355b3678a. That is literally one trade wearing twelve names
  - desks/mt5/research/alpha_genome.py:49-64 MECHANISM_CLASS — 34 declared family->mechanism mappings, with UNCLASSIFIED reported rather than guessed (MEASURED: unclassified_families is [] in the current artifact)
  - libs/research/alpha_clusters.py:1-25 — the complementary DECLARED taxonomy: 15 payer-level clusters written from outside the book, explicitly to name EMPTY ground that a clustering of the book cannot see (MEASURED)
  - clock: desks/mt5/research/daily_cycle.py:324 (_state_research_feedback runs alpha_genome), STEPS entry desks/mt5/research/daily_cycle.py:472; desks/mt5/research/daily_cycle.py:463 for the bandit that consumes cluster identity · artifact: desks/mt5/reports/ALPHA_GENOME.json · consumer: desks/mt5/research/regime_coverage.py:63 (clusters_tried / clusters_never_tried_here per state bucket); libs/research/bandit.breadth_credit via libs/research/breadth_credit.py
  - next: Read `n_clusters` and the per-sleeve cluster name from desks/mt5/reports/ALPHA_GENOME.json inside desks/mt5/research/pf_allocator.py and publish them beside k_eff in the allocator artifact, as desks/mt5/research/alpha_genome.py:23-25 already promises.
- **A11 Automatic theory construction — reusable rules across survivors, then hypotheses from the theory** — PARTIAL
  - gap: Theory construction exists only as within-family parameter priors and motifs; no module induces a cross-survivor, state-conditioned rule, and the one module whose docstring promises works-when/fails-when (scripts/knowledge_engine.py) is scheduled daily against four artifacts that do not exist and has never written its output.
  - desks/mt5/research/survivor_distiller.py:1-22 — the closest thing to it: `prior[family][param] = (median, IQR)` among CERTIFIED vs FAILED with a separation statistic, `motifs[family]` = categorical values and hold/session clusters survivors share, and `neighbours(cert)` = K untested cells stepped TOWARD the survivor median and away from regions the graph holds; operators named `step_<param>_up/down`, `swap_<param>` so mutation_yield can bill them (MEASURED)
  - Scheduled in desks/mt5/research/daily_cycle.py:261-263 (_proposers list) with a bandit-scaled budget (desks/mt5/research/daily_cycle.py:266-270). MEASURED artifacts: desks/mt5/data/intelligence/survivor_distiller/discoveries_20260906_2157.json and _2239.json (1.6 KB each)
  - Consumer: libs/ops/capability_graph.py:795 declares 'desks/mt5/data/intelligence/survivor_distiller/' as a 'miner_candidate_compiler intake glob', so distilled neighbours re-enter the candidate stream (MEASURED)
  - desks/mt5/research/mutation_yield.py:6 — bills each named operator's yield, closing the loop from theory to measured productivity (MEASURED; run at desks/mt5/research/daily_cycle.py:333)
  - clock: desks/mt5/research/daily_cycle.py:261 _proposers (survivor_distiller), desks/mt5/research/daily_cycle.py:333 (mutation_yield); ops/crontab.manifest:832 for knowledge_engine (dark) · artifact: desks/mt5/data/intelligence/survivor_distiller/discoveries_*.json; data/knowledge_engine.json NEVER PRODUCED · consumer: desks/mt5/research/miner_candidate_compiler.py intake glob (declared at libs/ops/capability_graph.py:795); desks/mt5/research/mutation_yield.py bills the operators
  - next: In desks/mt5/research/survivor_distiller.py, add a `motifs_by_state` pass that groups CERTIFIED cells by shadow_spec['condition']/selector and reports the states where a family's survivors concentrate, then emit those as neighbours conditioned on the state.

### Phase 4 — Adversarial scientific loop: falsifier, replicator, leakage prosecutor, statistics prosecutor, synthetic nulls, positive controls

- **V4 Falsifier agent — fastest experiment that could kill each hypothesis** — EXISTS-DARK
  - gap: The falsifier scheduler and the placebo battery are written, ordered by kill-rate per second, and never called — so no certificate has ever been asked entry_shift / side_flip / random_entry / label_shuffle / truncation / usd_residual. Only cost stress and the param/state neighbourhood run.
  - libs/validation/falsifiers.py:1-23 — exactly the blueprint object: 'every "maybe it's just X" becomes a test, ordered by information per second', `order = argsort(-P(kill|class)/cost_s)` with the pre-mortem's class first; :33-42 `CATALOGUE` = cost_surface(0.5s, COST_DEATH), half_stability(1.0s, STATE_FRAGILE), truncation(2.0s, LEAKAGE), usd_residual(1.0s, CORRELATION_DUPLICATE), tail_worst_decile(0.5s, TAIL_FAILURE), placebo_battery(20.0s, LEAKAGE); :43-44 `DEFAULT_KILL` priors (MEASURED)
  - grep for `validation.falsifiers` / `import falsifiers` across the tree returns ZERO importers — the module has no caller of any kind, production or test (MEASURED)
  - libs/validation/redteam.py:1-21 — per-certificate placebos `entry_shift`, `side_flip`, `random_entry`, `label_shuffle`, 'runs on every new one'; its only in-tree importer is libs/validation/falsifiers.py:148 (itself dark) plus desks/mt5/tests/test_validation_layer.py:35 (MEASURED)
  - libs/validation/lookahead.py:1-13 truncation sentinel + `recursive_check`; libs/validation/lookahead_audit.py:1-26 (noise-replacement + one-bar-delay probes); libs/validation/shift_leak.py `shift_ic` — importers are tests only (tests/validation/test_lookahead_audit.py, tests/research/test_shift_leak_detector.py) (MEASURED)
  - clock: libs/validation/falsifiers.py: NONE. libs/validation/redteam.py: NONE (reachable only through falsifiers). survivor_neighbourhood: desks/mt5/research/daily_cycle.py:331 leg. stress_costs: hourly via external_gauntlet leg. · artifact: NONE for libs/validation/falsifiers.py / libs/validation/redteam.py; desks/mt5/reports/SURVIVOR_NEIGHBOURHOOD.json for the neighbourhood arm · consumer: NONE for libs/validation/falsifiers.py
  - next: Add a leg to desks/mt5/research/hourly_cycle.py that runs `libs.validation.falsifiers` over the certificates in desks/mt5/reports/UNIVERSAL_SURVIVORS.json and writes desks/mt5/reports/FALSIFIER_VERDICTS.json — recorded beside the certificate as a defect report (which is what libs/validation/redteam.py:17-20 already specifies), withdrawing nothing and gating nothing.
- **V5 Replication agent — independent reimplementation that must agree** — PARTIAL
  - gap: A second backtester exists and is trustworthy, but nothing ever runs a certificate through both engines and refuses on disagreement — there is no IMPLEMENTATION DISAGREEMENT / UNMEASURED state on any certificate, and no independent second implementation of any FAMILY.
  - libs/validation/replay2.py:1-23 — a genuine second implementation: 'A second implementation written from the CONTRACT rather than from the first engine's source ... DELIBERATELY SIMPLE. No vectorisation, no shared helpers, no import from `engine`.' `compare` returns per-trade R from both engines and where they part company; 'A certificate whose two replays disagree by more than tolerance is not certified by either; it is a defect report' (MEASURED)
  - desks/mt5/research/counterfactual_markout.py:78-79 `from libs.validation.replay2 import replay` — the ONLY production consumer, and it uses the second engine to replay refused brackets, never to cross-check `mt5desk.engine.run_backtest` on a certificate (MEASURED)
  - desks/mt5/tests/test_validation_layer.py:95-96 — `replay2.compare([t.r_multiple for t in bt.trades], [t.r for t in r2])` is the only place the two engines are actually differenced, and it is a unit test, not a certification step (MEASURED)
  - desks/mt5/scripts/external_gauntlet.py:1286-1292 `_REPRO` / `_repro_active()` and :1615-1632 — the `--only` reproduction path re-runs the canonical ten gates on a restricted docket, writes to a caller-named path and 'Wrote no certificate and touched no authority file'. It runs the SAME code, so it reproduces execution, not implementation (MEASURED)
  - clock: replay2 via counterfactual_markout in desks/mt5/research/daily_cycle.py:320 list; verify_universal_state: supervisor/hourly loop per its own docstring; external_gauntlet --only: NONE (operator-invoked) · artifact: desks/mt5/reports/UNIVERSAL_STATE_VERIFY.json + reports/universal_state_verify.log; the --only reproduction writes to `report_to` (desks/mt5/scripts/external_gauntlet.py:1620) · consumer: UNIVERSAL_STATE_VERIFY.json is read at brain session start per its docstring and docs/UNIVERSAL_PROMOTION_PROTOCOL.md; the --only report has no standing consumer
  - next: In desks/mt5/scripts/external_gauntlet.py's certificate-writing block (around :1704-1736), for each passing cell replay its signals through `libs.validation.replay2.replay` and store `replay2_agreement: {max_abs_r_gap, n_trades_both}` on the certificate row — recorded, not gating, so a disagreement becomes visible before it becomes a decision.
- **V6 Leakage prosecutor — per-feature point-in-time timestamps** — PARTIAL
  - gap: The ratchet exists and has never switched on, because 4,768 of 4,769 rows carry no stamp and no dataset holds a PIT certificate — so the leakage prosecutor is a contract with a measured 0.02% compliance rate. Broker-side and strategy-availability clocks are not modelled at all.
  - libs/data/pit.py:58-63 — `STAMP_FIELDS = (event_time, available_time, ingested_time, source_version, payload_hash)` and `REVISION_FIELDS = (revision_id, revision_of, revision_time, revision_n, revision_reason)`; :17-19 `usable_at(row, t)` 'a row with no stamp is NOT usable at any time'; :186 `latest_as_of(rows, key_fields, t)` gives the vintage that existed at t (MEASURED)
  - libs/research/information_decay.py:105,:219-223 — `publication_lag_s` per InformationClass and `available_time_of(cls, event_time)`; :343 raises `PITViolation` when published_time precedes event_time (MEASURED)
  - libs/data/pit_certificate.py:1-35 — the seven adversarial dataset questions (leak, revision, timestamps, availability, survivorship, truncation, schema), `authority` true only when all seven PASS, 'NO CERTIFICATE -> NO PROMOTION AUTHORITY'; certificates live at desks/mt5/data/pit_certificates/<dataset>.json (MEASURED)
  - desks/mt5/data/pit_certificates/ DOES NOT EXIST on this tree — zero datasets have been through the seven questions (MEASURED)
  - clock: scripts/check_pit.py runs in .github/workflows/ci.yml:129; the judge-side ratchet runs hourly inside the external_gauntlet leg (desks/mt5/research/hourly_cycle.py:1101) · artifact: desks/mt5/reports/PIT_CENSUS.json; desks/mt5/data/pit_certificates/ (absent) · consumer: PIT_CENSUS.json read by scripts/check_pit.py census + the CI gate; `is_stamped` read by desks/mt5/scripts/external_gauntlet.py:1331 and desks/mt5/research/proposer_common.py:319
  - next: Extend desks/mt5/reports/PIT_CENSUS.json (scripts/check_pit.py) with a per-source `blocks_certification` boolean computed from the same `is_stamped` predicate desks/mt5/scripts/external_gauntlet.py:1331 uses, so the census names exactly which sources must be re-donated before the already-written ratchet fires — measurement and ordering only.
- **V7 Statistical prosecutor — which tests run, where, on whom** — PARTIAL
  - gap: Two disjoint statistical stacks: the MT5 ten gates (broadcast PBO/SPA, fixed-597 DSR, no bootstrap, no permutation, no stepdown) and the autodiscovery stack (Romano-Wolf, per-candidate PBO, block bootstrap, synthetic nulls) which is the one the calibration harness actually certifies — so the certified instrument is not the one that issues certificates.
  - ON EVERY MT5 CANDIDATE (desks/mt5/scripts/external_gauntlet.py:1144-1204): DSR (libs/validation/dsr.deflated_sharpe_ratio, fixed 597 trials / fixed variance 0.014863), PBO (libs/validation/pbo.probability_backtest_overfitting), SPA (libs/validation/reality_check.hansen_spa), CPCV (libs/validation/cpcv.CPCV, 6 groups / 2 test), walk-forward (libs/validation/revalidation.WalkForwardEngine) (MEASURED)
  - desks/mt5/scripts/external_gauntlet.py:1099-1117,:1161-1162 — PBO and SPA are computed once on the matrix and written onto EVERY verdict; libs/autodiscovery/validation.py:861-880 `gate_discrimination` documents exactly this defect: 'when campaign PBO is 0.6159 against a 0.50 bar, that gate reads False for every candidate in the campaign -- all 420 of them' (MEASURED)
  - IMPLEMENTED BUT NOT ON THE MT5 PATH: Romano-Wolf stepdown (libs/validation/stepwise.py — importers are libs/autodiscovery/validation.py:80, scripts/audit_gate_power.py, scripts/measure_matrix_window.py only); per-candidate CSCV-PBO + FWER p (libs/validation/per_candidate.py — importer libs/validation/ensemble_gate.py:121 only); stationary block bootstrap (libs/validation/bootstrap.stationary_block_indices — importers libs/validation/per_candidate.py:48, portfolio MC, monte_carlo_survival) (MEASURED)
  - libs/validation/bar_permutation.py:1-30 — the Aronson/Masters gap-and-intrabar shuffle null, explicitly noting 'None of them is this asset's own bars, temporally scrambled'; consumers are scripts/measure_permutation_null.py and tests only, and scripts/measure_permutation_null.py is on no schedule (MEASURED)
  - clock: DSR/PBO/SPA/CPCV/WF: desks/mt5/research/hourly_cycle.py:1101 (external_gauntlet). Romano-Wolf/per-candidate/bootstrap/synthetic nulls: ops/quant-certify-gauntlet.timer (OnCalendar=*:10) via scripts/certify_gauntlet.py. bar_permutation / e-BH / family_multiplicity partition: NONE. · artifact: desks/mt5/reports/universal_gates_external.json; reports/gauntlet_certification.json · consumer: universal_gates_external.json -> desks/mt5/research/research_productivity.py:88, funnel_census; gauntlet_certification.json -> scripts/run_type2_report.py:282-296, libs/validation/campaign_design.py:7, tests/validation/test_gate_admits_good.py:111
  - next: Point scripts/certify_gauntlet.py's control cohort at `desks.mt5.scripts.external_gauntlet.run_gauntlet` as a second scored path (it already runs a controlled A/B over two paths in one pass) and add the resulting `mt5_ten_gate_path` block to reports/gauntlet_certification.json — measuring the production certifier without altering any threshold.
- **V9 Synthetic-null calibration and positive controls** — PARTIAL
  - gap: The false-certification rate is measured hourly on the WRONG instrument (autodiscovery validate), and the hourly canary suite that would measure the right one runs against a stand-in gate that rejects everything — so the MT5 ten-gate certifier's false-certification rate has never been measured.
  - scripts/certify_gauntlet.py:1-22 — known-GOOD / known-NULL controls pushed through the REAL validate(); reports/gauntlet_certification.json (generated 2026-08-25T23:30, status COMPLETE) records `legacy_welded_path.null_false_pass_rate: 0.0`, `min_passing_true_sharpe: null`, `sole_blocking_gate_counts: {reality_check: 12}`, and `per_candidate_path.pass_rate_by_true_sharpe` {2:0.0, 3:0.0, 5:1.0, 7:1.0} (MEASURED)
  - It runs on `libs.autodiscovery.validation.validate` (imports at scripts/certify_gauntlet.py:50-55), NOT on `external_gauntlet.run_gauntlet` — and data/decision_ledger.json:3609 records the known consequence: 'certify_gauntlet is the sharpest: the desk certifies its gate on a matrix the production path no longer uses' (MEASURED)
  - desks/mt5/research/adversary.py:10-24 — five poison canaries (pure_noise, lookahead, survivor_biased, cost_blind, overfit_params), 'The canary rejection rate MUST stay at 100%', fixed `CANARY_SEED = 20260906` (:69) so a canary that starts passing is a change in the GATES (MEASURED)
  - desks/mt5/research/adversary.py:260-268 `_default_gate` returns False unconditionally; :271-272 `run(gate=None)` falls back to it; :292 `main()` calls `run()` with NO gate. desks/mt5/reports/ADVERSARY.json (measured_at 2026-09-06T00:42) therefore reads `"gate_source": "stand-in (rejects everything; this run proves nothing about the real gates)"` with all five canaries `rejected: true` (MEASURED)
  - clock: certify_gauntlet: ops/quant-certify-gauntlet.timer (OnCalendar=*:10) + ops/quant-certify-gauntlet.service. adversary: desks/mt5/research/hourly_cycle.py:1192 `_costed("adversaries", adversaries)` -> :904 `_producer("adversary", "desks/mt5/research/adversary.py")`. gate_power `run_controls`: scripts/run_research_review.py:54, called only from ops/run_research_cycle.sh:88. · artifact: reports/gauntlet_certification.json; desks/mt5/reports/ADVERSARY.json; data/CANARY_ALARM.txt (desks/mt5/research/adversary.py:60, written only when a canary survives) · consumer: gauntlet_certification.json -> scripts/run_type2_report.py:283, libs/validation/campaign_design.py, tests/validation/test_gate_admits_good.py:111; ADVERSARY.json -> desks/mt5/reports/TIER6_CLOSURE.md rows and the capability graph (staleness), no decision consumer
  - next: In desks/mt5/research/adversary.py:292 `main()`, pass a gate that builds a one-cell docket and calls `external_gauntlet.run_gauntlet` (importable on the desk box) instead of leaving `gate=None`, so `gate_source` reads 'injected' and the 100%-rejection constant becomes a statement about the real gates.
- **V10 Synthetic-market laboratory and adversarial synthetic worlds** — EXISTS-DARK
  - gap: The lab is built and scheduled, and its artifact has never appeared — so the detection floor, the one progress metric the file argues cannot be gamed, has never been published. No adversarial worlds (lead-lag, regime shift, jumps, cost breaks) exist at all.
  - libs/hypmax/laboratory.py:1-27 — 'THE SYNTHETIC MARKET LABORATORY ... Generate a price path with a KNOWN edge planted in it at a known strength, run the desk's own screen over it, and ask whether the screen found it'; exports `BENCHMARK_STRENGTHS`, `LabResult`, `detection_floor`, `false_positive_rate`, `measure_screen`, `no_edge_path`; :24-27 'Synthetic data never validates a strategy ... Its only legitimate use is measuring an INSTRUMENT' (MEASURED)
  - scripts/calibrate_gauntlet.py:51-52 — the only importer: `from libs.hypmax.laboratory import BENCHMARK_STRENGTHS, detection_floor`; :16-19 defines power / FPR / detection floor as the pair (MEASURED)
  - scripts/calibrate_gauntlet.py:54 `OUT = ROOT / 'data/gauntlet_calibration.json'` — that file DOES NOT EXIST anywhere in the tree, while data/ holds 215 other artifacts (MEASURED)
  - scripts/run_cadence.py:768-784 — the leg exists and checks `Path('data/gauntlet_calibration.json').exists()`, printing 'cadence: gauntlet-calibration rc=... NO ARTIFACT' otherwise (MEASURED)
  - clock: scripts/run_cadence.py:776 inside ops/quant-cadence.timer / quant-cadence.service (ExecStart=scripts/run_cadence.py) · artifact: data/gauntlet_calibration.json — declared, never produced on this tree · consumer: scripts/max_audit.py:7724 `_COVERAGE_ORGANS` includes 'calibrate_gauntlet'; no consumer of the artifact itself because it has never existed
  - next: Make scripts/run_cadence.py's calibrate_gauntlet leg write a BLOCKED artifact (rc + last stderr line) to data/gauntlet_calibration.json when the run fails, the same shape scripts/certify_gauntlet.py uses, so the reason the floor has never been measured becomes an artifact instead of a log line.
- **V13 Complexity penalty / MDL in fitness or gating** — PARTIAL
  - gap: Complexity is a 0.03-weight node-count penalty inside the evolutionary SEARCH only; no gate, no certificate and no promotion decision prices parameters, conditions, feature count, turnover or execution dependency, so the `overfit_params` canary has nothing to fail against.
  - libs/research/alpha_fitness.py:6 and :102-113 — `Fitness = w1 dE[logW_P] + w2 OOS + w3 Novelty + w4 Tail + w5 StateBreadth + w6 Capacity - w7 Cost - w8 Fragility - w9 Complexity - w10 Multiplicity`; `WEIGHTS['complexity'] = 0.03` ('node count'), `PENALTIES = {cost, fragility, complexity, multiplicity}` (MEASURED)
  - libs/research/alpha_fitness.py:560 `complexity: int = 0` on the candidate, :613-614 `cx = _take('complexity', float(max(0, int(candidate.complexity))), f'{int(candidate.complexity)} nodes')` (MEASURED)
  - libs/research/alpha_grammar.py:933-936 `def complexity(expr)` — recursive expression node count, the MDL-shaped primitive (MEASURED)
  - Consumers: libs/research/search_populations.py:61 (`nsga2_order`) and desks/mt5/research/alpha_evolution.py:75 — i.e. the SEARCH ranks by it (MEASURED)
  - clock: alpha_evolution: ops/quant-hourly-discovery.service and desks/mt5/research/daily_cycle.py · artifact: desks/mt5/reports/ALPHA_GENOME.json (alpha_evolution's output) · consumer: libs/research/search_populations.nsga2_order (search ordering)
  - next: Record, per certificate written in desks/mt5/scripts/external_gauntlet.py (~:1704), a `complexity: {n_params, n_conditions, expression_nodes, turnover_per_day}` block computed from the cell's own params and daily series — measurement stored on the certificate, with no threshold attached, so the canary and the fitness function can be reconciled against real survivors.
- **V14 Data-ablation research — marginal information value per dataset/feature** — PARTIAL
  - gap: Ablation exists only over GATES (once, in 2026-08). The dataset/feature ablation organ is scheduled daily and has priced zero features because the feature warehouse is empty, so no source has a measured marginal information value per survivor.
  - desks/mt5/research/feature_roi.py:79 `REPORT = desks/mt5/reports/FEATURE_ROI.json`; the artifact states its own formula: 'FeatureROI_j = dE[logW | F_j] / (acquisition + compute + maintenance + multiplicity), in log-wealth per day per declared cost unit' (MEASURED)
  - desks/mt5/reports/FEATURE_ROI.json (generated 2026-09-05T11:58): `blocks: 0, features: 0, ledger_rows: 0`, every count bucket 0, and `gaps.warehouse`: 'no feature blocks under .../desks/mt5/data/features: the warehouse is empty on this host, so there is nothing to price' — the organ has never priced a feature (MEASURED)
  - libs/ops/allocators.py:106-109 — level 1 INFORMATION gap names the missing thing exactly: 'What closes it is the ABLATION the principal asked for: train with and without the source and difference the two, which needs the source's own feature set versioned' (MEASURED)
  - libs/autodiscovery/data_opportunity.py:1-8 — forward-looking `expected_alpha_value / implementation_cost` ranking of datasets the desk LACKS; it is a prior, not an ablation of held data (MEASURED)
  - clock: feature_roi: desks/mt5/research/daily_cycle.py:346 (inside `_state_research_feedback`'s organ list); data_opportunity: no direct schedule; gate ablation (scripts/audit_gate_power.py): NONE · artifact: desks/mt5/reports/FEATURE_ROI.json (0 features priced); reports/gate_subset_audit.json (gate ablation, dated 2026-08-01) · consumer: FEATURE_ROI.json -> libs/ops/module_rent.py:217; gate_subset_audit.json -> scripts/report_gate_audit.py
  - next: Have desks/mt5/research/feature_roi.py fall back to an input-level ablation over the sources already named on candidate rows (`params.input_source`: ff_calendar_vintage, cot_point_in_time, ...) by differencing certificate counts and ΔElog with and without each source, and publish it as `source_ablation` in FEATURE_ROI.json — measurement from artifacts already on disk.
- **V15 Benchmark tournament — every survivor must beat simple alternatives** — PARTIAL
  - gap: No MT5 certificate is ever compared to buy-and-hold, trend, carry, breakout, MR, an ensemble or an exposure-matched random baseline; the one gate that would do it lives on the other validation path and fails OPEN when no benchmark is passed, and the shuffled-label tournament runs on no schedule.
  - libs/validation/baselines.py:1-10 — `baseline_scorecard` scores a candidate against buy-and-hold and equal-weight: 'a strategy can clear DSR and still lose to buy-and-hold ... in which case it is complexity with no reason to exist' (MEASURED)
  - libs/autodiscovery/validation.py:774-777 — a `beats_baselines` gate exists; :390-410 `_beats_baselines` returns True when `benchmark is None` — a deliberate fail-OPEN, and :805-806 records it as UNMEASURED rather than passed when no benchmark was supplied (MEASURED)
  - The MT5 ten gates contain NO baseline comparison: desks/mt5/scripts/external_gauntlet.py:1144-1204 has economic_prior, in_sample_screen, deflated_sharpe, pbo, reality_check_spa, cpcv, walk_forward, stress_costs, lockbox, expected_value and nothing else; gate_spec.yaml lists the same ten (MEASURED)
  - libs/validation/random_baseline.py:1-27 — the exposure-matched monkey test ('This shuffles the RULE and keeps the data'), with the explicit argument that it and the permutation test fail differently and 'the desk needs both'; importers are tests/validation/test_random_baseline.py only (MEASURED)
  - clock: baselines via libs/autodiscovery/validation.validate (certify_gauntlet timer path only); scripts/run_tournament.py: NONE; random_baseline: NONE · artifact: NONE for the tournament; reports/gauntlet_certification.json carries the autodiscovery-path baseline gate state · consumer: NONE for the tournament
  - next: Compute `libs.validation.baselines.baseline_scorecard` and `libs.validation.random_baseline`'s exposure-matched monkey beat-rate for each passing cell in desks/mt5/scripts/external_gauntlet.py and store them on the certificate as a `baselines` block (recorded, not gating) — the buy-and-hold series for a cell's own symbol is already in the frame cache.
- **V16 Independent validator — production code cannot certify itself** — PARTIAL
  - gap: Independence is enforced over the JUDGE'S BYTES (hash manifests, cross-box code identity, a reproduction mode that cannot write authority) but never over the JUDGE'S NUMBERS — no second implementation reproduces a certificate's gate statistics, so an arithmetic error in the judge is invisible to every fence.
  - scripts/check_immutable_evaluator.py:1-15 — 'research agents may not modify the test they failed'; :28-41 `IMMUTABLE` hashes desks/mt5/scripts/external_gauntlet.py, desks/mt5/research/universal_gate.py, libs/validation/redteam.py, libs/validation/replay2.py etc. into desks/mt5/data/IMMUTABLE_MANIFEST.json; a change requires a human `--sign` (MEASURED)
  - desks/mt5/research/verify_universal_state.py:1-12 and :27-30 — hashes eight promotion-critical files against `origin/<branch>` blobs, fail-closed `ok=false` on ANY discrepancy, writes reports/UNIVERSAL_STATE_VERIFY.json plus an append-only log; MEASURED note at :83-99 that the box now REPORTS drift rather than merging (MEASURED)
  - desks/mt5/scripts/external_gauntlet.py:1615-1632 — 'A REPRODUCER THAT CAN WRITE CERTIFICATES IS NOT A REPRODUCER. Its verdict must be able to DISAGREE with the record without altering it' — the `--only` path exits before the authority block and writes only to the caller's path (MEASURED)
  - desks/mt5/research/hourly_cycle.py:1106 `recertify_canon` leg + ops/quant-recertify-canon.timer — canonical recertification is scheduled (MEASURED)
  - clock: check_immutable_evaluator: CI / fence battery (ops/run_fence_battery.sh); verify_universal_state: supervisor loop on every box per its docstring; recertify_canon: desks/mt5/research/hourly_cycle.py:1106 + ops/quant-recertify-canon.timer · artifact: desks/mt5/data/IMMUTABLE_MANIFEST.json; desks/mt5/reports/UNIVERSAL_STATE_VERIFY.json + reports/universal_state_verify.log · consumer: IMMUTABLE_MANIFEST.json -> scripts/check_immutable_evaluator.py (fence, rc=1 on drift); UNIVERSAL_STATE_VERIFY.json -> read at every brain session start per CLAUDE.md / AGENTS.md / docs/UNIVERSAL_PROMOTION_PROTOCOL.md
  - next: Add a CI job to .github/workflows/ci.yml that runs `desks/mt5/scripts/external_gauntlet.py --only <cell>` for one canon certificate and asserts the reproduced `stages` dict equals the stored one bit-for-bit — turning the existing reproduction mode into a scheduled, consumed check rather than an operator tool.
- **V17 Model-risk red team, and the infrastructure that defeats it** — PARTIAL
  - gap: The defensive infrastructure (fence, authority ladder, protected artifacts, prompt ratchet, immutable evaluator) is complete and enforced; the offensive half is one hourly canary suite pointed at a stand-in gate, with no agent attempting to game the scoring, p-hack a candidate, or get a constructed candidate past the promoter.
  - DEFENCES ARE REAL AND LIVE: libs/ops/deepseek_cycle.py:380-396 `fence(action)` — 'Every fenced action REFUSES ... Checked by NAME rather than by inspecting intent, because intent arrives as text from a model and text inside a model response is DATA, never an instruction' (MEASURED)
  - libs/ops/agent_authority.py:1-31 — the L0..L6 ladder (PUBLIC_READ .. NORMAL_EXECUTION), `escalate` refuses a level change justified by CAPABILITY, promotion one rung at a time, `least_privilege_gap` sized by blast radius (MEASURED)
  - libs/ops/protected_artifacts.py:1-32 — 'A TEST RUN IS AN OBSERVATION AND MUST NEVER BE A WRITE TO THE THING OBSERVED', with the measured 2026-08-13 incident (next_law_number.txt 60 -> 43, principal_doctrine.txt index block lost, trade_forensics_latest.json overwritten with n_closes: 0) (MEASURED)
  - libs/doctrine/prompt_ratchet.py:122-123 + docs/research/PROMPT_RATCHET.json — high-water mark over the RULES a governed prompt asserts, 'Raised automatically when a prompt gains a rule; NEVER lowered by code'; retirement only via a dated signed entry in PROMPT_RATCHET_WAIVERS.json; fenced by scripts/check_prompt_ratchet.py (MEASURED)
  - clock: adversary: desks/mt5/research/hourly_cycle.py:1192 -> :904 `_producer("adversary", "desks/mt5/research/adversary.py")`; prompt ratchet: ops/quant-prompt-prefix.timer + scripts/check_prompt_ratchet.py; immutable evaluator: ops/run_fence_battery.sh; deepseek_cycle: ops/quant-deepseek.timer · artifact: desks/mt5/reports/ADVERSARY.json; data/CANARY_ALARM.txt; desks/mt5/data/IMMUTABLE_MANIFEST.json; docs/research/PROMPT_RATCHET.json; data/agent_authority.json · consumer: CANARY_ALARM.txt gates desks/mt5/research/adversary.py's own exit code (:300-307); IMMUTABLE_MANIFEST + PROMPT_RATCHET are read by their fences; ADVERSARY.json has no decision consumer
  - next: Extend desks/mt5/research/adversary.py's canary set with a `promoter_gaming` canary — a synthetic sleeve whose forward ledger is constructed to satisfy exactly the 14-day / 50-trade / exp>0.05R / maxDD>-25R bar on manufactured trades — run against `promoter`'s own predicate and reported in ADVERSARY.json; a new measurement, no threshold touched.
- **V18 Research chaos engineering — inject corrupt data, certification must fail** — PARTIAL
  - gap: Two of the eight chaos classes are covered and covered well (future-leak and fill-ambiguity metamorphically, over every family; duplicates/crossed/stale on the tick tape). Revised macro vintages, missing bars, timezone errors, stale spreads and symbol remaps are never injected into the certification path with an asserted refusal.
  - desks/mt5/tests/test_no_lookahead.py:1-25 — a permanent metamorphic invariant over EVERY registered family with no exception list: SIGNAL leakage ('corrupt everything after bar i, regenerate, and demand that every signal dated at or before bar i is byte-identical') and FILL leakage ('widening the fill bar's extremes ... stretching the high alone must not turn a loss into a win'); it exists because a shipped leak 'survived a full 3,168-cell hunt' (MEASURED)
  - desks/mt5/tests/test_tick_integrity.py:228 `test_duplicates_crossed_quotes_and_staleness_are_all_measured` and :338 `test_the_duplicate_rate_is_still_reported_after_the_fold_that_removed_them` — duplicates, crossed quotes and staleness are injected and asserted measured (MEASURED)
  - desks/mt5/tests/test_trial_matrix_alignment.py:164 `test_duplicate_dates_within_a_cell_are_summed_not_dropped` — duplicate observations in the gate matrix (MEASURED)
  - libs/validation/lookahead_audit.py:1-26 — the two mechanical probes (replace-the-future-with-noise, and the rigged current-candle signal that scores Sharpe > 100 until the one-bar delay is applied); consumers are tests/validation/test_lookahead_audit.py and tests/research/test_overlays.py only (MEASURED)
  - clock: .github/workflows/ci.yml (pytest) — test_no_lookahead and test_tick_integrity run in CI · artifact: CI status; no standing artifact · consumer: CI gate
  - next: Add a test alongside desks/mt5/tests/test_no_lookahead.py that runs `external_gauntlet.run_gauntlet` on one cell twice — once on clean bars and once after deleting a random 5% of bars and shifting the index by one hour — and asserts the second run produces an UNMEASURED or refused verdict rather than a numerically different PASS.
- **V19 Prediction calibration — stated forward-success probability vs realised** — PARTIAL
  - gap: The scoring rules, the belief contract and the logs all exist, and there is nothing to score: 0 beliefs published, 0 live sleeves, and no survivor is ever assigned a forward-success probability — so the '70% class succeeds 70% of the time' check has no input.
  - libs/validation/calibration.py:1-17 — proper scoring rules implemented: log score, closed-form CRPS for N(mu,s), Brier, and PIT with its KS distance from uniform ('each is minimised in expectation by the TRUE distribution and nothing else') (MEASURED)
  - libs/self_improvement/forecast_calibration.py:9,:19 — 'no fabricated calibration. Pure/deterministic; the store is data/forecast_log.json', `_LOG = Path('data/forecast_log.json')` (MEASURED)
  - desks/mt5/research/forecast_contract.py:1-38 — every belief carries model_id, `at`, subject, kind (Brier/MAE/CRPS), value, horizon_s, confidence and features, and 'A belief that cannot be scored is rejected at publication'. MEASURED output: desks/mt5/reports/FORECAST_CONTRACT.json (2026-09-06T00:36) reads `"beliefs": 0, "models": {}` — nothing has ever published a scoreable belief (MEASURED)
  - desks/mt5/data/decay_live.json (2026-09-04T02:38): `live_sleeves: 0, roster_state: 'NO_ROSTER'`, `verdicts: {}`, with the reason 'sleeves.json does not exist -- its only writer is desks/mt5/research/promoter.py, so nothing has ever been promoted to live risk' (MEASURED)
  - clock: forecast_contract: desks/mt5/research/hourly_cycle.py:1190 `_costed("forecast_contract", forecast_contract)`; decay_live + forward_reconcile: desks/mt5/research/daily_cycle.py · artifact: desks/mt5/reports/FORECAST_CONTRACT.json (0 beliefs); desks/mt5/data/decay_live.json (0 live sleeves); desks/mt5/data/forward_reconcile.json; data/forecast_log.json; desks/mt5/data/pf_forecast_log.jsonl · consumer: pf_forecast_log.jsonl -> libs/research/decision_dataset.py:126,:458 and libs/research/counterfactual_world.py:610; forward_reconcile.json -> the shadow reconciler and the dashboard; FORECAST_CONTRACT.json -> issue_board staleness only
  - next: Have desks/mt5/scripts/external_gauntlet.py stamp each certificate with `p_forward_success` taken from `libs.research.funnel_census`'s existing Beta posterior for that cell's family (the same posterior desks/mt5/research/deepening_worker.py:406-412 already reads), then score it later with libs/validation/calibration.brier — a recorded prediction, no gate.
- **V20 Structural-break detector ensemble** — PARTIAL
  - gap: One two-sample shift detector (KS + variance ratio) plus a stationarity wrapper that is optional-dependency gated; there is no ensemble of independent detectors and no rule anywhere that converts a detected break into reduced weight on older forward observations.
  - libs/research/dist_shift.py:1-18 — 'has the measuring stick itself changed'; method is 'two-sample, non-parametric, tiny -- a two-sample KS statistic plus a variance-ratio'; 'a detected shift NEVER promotes and never auto-demotes ... recommends a CONFIDENCE HAIRCUT -- downward only'; explicitly 'a library the existing revalidation path and axis screens call. It owns no cadence, no state file, no pager' (MEASURED)
  - libs/research/information_decay.py:1-20 — per-InformationClass half-lives and publication lags so 'weekly COT' does not read as new every minute; `Information_j(t) = Value_j x Decay_j(age_j)` (MEASURED)
  - libs/research/collapse_detector.py:1-20 — four-axis generator-collapse entropy monitor (MECHANISM / FEATURE / MARKET / ...), which detects search collapse rather than a market break (MEASURED)
  - libs/validation/revalidation.py contains the walk-forward engine and is one of the modules matching 'structural break/CUSUM/breakpoint' (MEASURED)
  - clock: dist_shift: called by revalidation / axis screens, owns no cadence by design; collapse_detector: no direct schedule found; information_decay: consumed by state_vector/pf_allocator paths · artifact: NONE dedicated (dist_shift is library-only, by declaration) · consumer: revalidation path and axis screens (per libs/research/dist_shift.py:10-11)
  - next: Add a second, independent detector (a CUSUM of the standardised daily series) beside the KS/variance-ratio pair in libs/research/dist_shift.py and return both verdicts plus their agreement flag, so the confidence haircut it already recommends is conditioned on two detectors agreeing rather than one — strictly more evidence required, never less.

### Phase 5 — Adaptive capital brain: regime posterior, decay posterior, joint scenarios, tail dependence, state-dependent Elog, execution-cost prediction, contextual allocation, H <= 20%

- **P1 The allocator as it is: objective, heat law, and the path from fraction to order** — PARTIAL · gate: principal
  - gap: The whole chain is wired and hourly-scheduled, but the one book that has ever sent a live order — the three `"lot": "auto"` GOLD_WINDOWS rows — never reaches the allocator's h_i: gateway.py:2168 branches to `gold_lot` before the `from_book` path, so the allocator can bill gold's heat at h_i while the venue gets fixed-fractional-plus-floor. And on the money box the artifact itself was measured non-existent as of 2026-09-07, so every sleeve fell back to `ramped_fraction`.
  - desks/mt5/research/pf_allocator.py:1-33 — module docstring: 'THE CAPITAL BRAIN -- robust posterior E[log W] over every validated edge'; 'It solves for PER-SLEEVE HEAT, not weights'; three clocks --mode fast/normal/heavy (MEASURED, from source)
  - desks/mt5/research/pf_allocator.py:80 — `OUT = BASE / "reports" / "pf_allocation.json"`; :86 `ARMED = BASE / "data" / "PF_ALLOCATOR_ARMED"`; :88 `FORECASTS = BASE / "data" / "pf_forecast_log.jsonl"` (MEASURED)
  - libs/portfolio/robust_elog.py:114-168 — `WorldConfig` defaults: n_worlds=256, n_rows=384, block_days=5.0, crisis_prob=0.06, crisis_vol_mult=2.5, crisis_common_share=0.55, decay_prob=0.30, cost_uncertainty=0.50, robust_lambda=0.50, cvar_alpha=0.20, redundancy_lambda=0.15, regime_min_days=60, max_elements=12_000_000 (MEASURED)
  - desks/mt5/mt5desk/gateway_config_fallback.py:94 `HEAT_TARGET = 0.20`; :105 `HEAT_HARD_CEILING = 0.30`; :112 `MAX_SLEEVE_HEAT_SHARE = 0.25`; family cap 60% documented at :113+ (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1158-1159 leg `pf_allocator` (`_producer("pf_allocator", "desks/mt5/research/pf_allocator.py", "--mode", _allocator_mode_for_the_hour())`, mode chooser at desks/mt5/research/hourly_cycle.py:541), run by MT5-Hourly (desks/mt5/ops/box_tasks.manifest:63, installer desks/mt5/scripts/MT5Hourly.cmd). Not present in ops/crontab.manifest (grep for 'pf_allocator' returns nothing) and not in desks/mt5/scripts/Install-QuantWindows.ps1. · artifact: desks/mt5/reports/pf_allocation.json (+ desks/mt5/data/pf_forecast_log.jsonl, desks/mt5/data/pf_allocator_cache/daily_r.parquet) · consumer: decision_core.allocator_heat/allocator_rank/book_from_allocation → gateway.allocator_heat/allocator_book/cap_by_heat; promoter.allocation_view; missed_growth (ALLOC); allocator_attribution; portfolio_gap
  - next: In desks/mt5/mt5desk/gateway.py:2168, let the gold branch take the allocator's fraction when one exists — `promoted_lot(..., from_book=True)` with `risk_frac=_book[name]` — and keep `max(that lot, gold_min_lot())` so the 0.02 floor still binds upward. Gold then gets whatever h_i the optimiser earned it (which on the last recorded solve was 1.7% of a 20.5% book) and can never be sized below the floor.
- **P2 Marginal-Elog admission as THE capital criterion** — PARTIAL
  - gap: It is universal for the three FORWARD lanes and only there. The armed gold book bypasses it entirely (rostered LIVE by decision_core.roster, sized by gold_lot), an incumbent already in the book is never re-priced by a marginal (its heat is taken as the reading), and the scan only runs on `--mode heavy` inside a 180 s budget, so a long tail of candidates carries `unscored` and cannot be admitted this pass.
  - libs/portfolio/robust_elog.py:744-787 `marginal_delta_elog(current, candidate, *, hard_cap, target, max_per_sleeve, cfg)` — 'dG = G*(current + candidate) - G*(current)'; BOTH BOOKS ARE RE-SOLVED; returns `delta_robust`, `candidate_heat`, `reallocation`, `admit = ext.robust_score > base.robust_score and got > 1e-5` (MEASURED)
  - desks/mt5/research/pf_allocator.py:1481-1692 `marginal_admission(ev, worlds, cfg, *, incumbent, bounds, total_heat, order, prefer, budget_s, iterations, margin_frac)` — 'dE[log W]_i = E[log W | book + i] - E[log W | book], on ONE world population'; basis 'equal_heat' or 'free' (MEASURED)
  - desks/mt5/research/pf_allocator.py:1404 `ADMISSION_BUDGET_S = 180.0`; :1408 `ADMISSION_ITERATIONS = 120`; :1411 `ADMISSION_MAX_AGE_S = 26 * 3600`; :1416 `ADMISSION_MARGIN_FRAC = MARGIN_FRAC` (= 0.02, libs/portfolio/allocator_proof.py:67) (MEASURED)
  - desks/mt5/research/pf_allocator.py:1608-1610 — a candidate the 180 s budget did not reach is written to `doc["unscored"]` with 'NOT admitted -- an unmeasured marginal is not a positive one' (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1158 (allocator, mode heavy whenever the carried scan is missing/stale/incomplete — desks/mt5/research/hourly_cycle.py:541-553, `_rostered_but_unpriced`) then desks/mt5/research/hourly_cycle.py:1176 leg `promoter`; also desks/mt5/research/daily_cycle.py:470 STEPS ("promoter", _promote); also the gateway's 22:00 UTC pass (desks/mt5/research/promoter.py:3) · artifact: the `admission` block inside desks/mt5/reports/pf_allocation.json (candidates / admitted / refused / unscored / universe / incumbent_elogw_per_day / margin_per_day); desks/mt5/data/sleeves.json · consumer: promoter.allocation_view → admission_of → capital_verdict → reconcile_capital → data/sleeves.json → gateway.load_sleeves
  - next: In desks/mt5/research/pf_allocator.py, drop `budget_s` from a wall-clock cutoff to a per-candidate warm-start budget and let the `prefer` rotation run to completion over the pass (it already carries last pass's unreached names first at :1596-1601) — more candidates priced per hour, none refused for compute, no threshold touched.
- **P3 Per-sleeve posterior distributions and thousands of joint worlds** — PARTIAL
  - gap: Only the MEAN is a posterior. Variance, skew, tail loss, capacity, tail dependence and per-sleeve decay are either constants (decay_prob 0.30, crisis_common_share 0.55), inherited from the bootstrap, or refused (capacity ceiling UNMEASURED); and the population is 256 worlds, an order of magnitude below 'thousands', trimmed further by `max_elements = 12_000_000` on a 4 GB box.
  - libs/portfolio/robust_elog.py:62-110 `SleeveEvidence` fields: `daily_r`, `family`, `symbol`, `forward_days`, `live_days`, `cost_r`, `cost_bias_r`, `n_trials`, `state_r`, `state_key` (MEASURED)
  - libs/portfolio/robust_elog.py:237-371 `_posterior_mu` — hierarchical draws; trial deflation `sr0 = 0.5*sqrt(2 ln N)/sqrt(n)` relieved by OOS `sr0 * (1 - oos/(oos+250))`; `k_sleeve, k_family = 60.0, 120.0`; `k_state = 40.0`; `se = s/sqrt(obs) * (2 - lam_s)` (MEASURED)
  - libs/portfolio/robust_elog.py:372-503 `sample_worlds` — 'Each world is a JOINT draw -- the same world that gives a sleeve a decayed edge also gives it a worse fill and puts it in the crisis regime'; stationary bootstrap `_stationary_bootstrap_index` (Politis-Romano, block_days=5.0) preserves higher moments and runs (MEASURED)
  - WORLD COUNT IS 256, NOT THOUSANDS: WorldConfig.n_worlds = 256 (libs/portfolio/robust_elog.py:120-121, 'at alpha=0.20 this is 51 worlds in the tail average'); desks/mt5/research/pf_allocator.py:22 'sampling 256 worlds over 109 sleeves costs 0.3 s' (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1158 leg `pf_allocator` (every pass calls sample_worlds via pf_allocator.run) · artifact: the `evidence`, `crisis_calibration`, `drift_overlay` and `hazard_shrink` blocks of desks/mt5/reports/pf_allocation.json · consumer: pf_allocator.optimise / score_book / marginal_admission; allocator_proof.contest; kelly_surface.surface
  - next: In libs/portfolio/robust_elog.py add a per-sleeve `decay_prob_i` field to `SleeveEvidence` defaulting to `cfg.decay_prob`, and have pf_allocator populate it from `hazard_by_sleeve` (already read at :1276) instead of applying the hazard as a post-hoc mean shrink — the sleeves the drift monitor says are healthy then stop paying the 30% blanket decay and get MORE heat.
- **P4 Robust Kelly under the cap: Kelly distribution / median / p5 / crisis Kelly** — PARTIAL
  - gap: The surface gives growth quantiles at a fixed heat grid, not a distribution of the optimal fraction. There is no f*_w per world, hence no median/p5 Kelly, and no Kelly solved on the crisis sub-population alone — so 'what would Kelly be if the crisis world is the one we are in' has no number.
  - desks/mt5/research/pf_allocator.py:1739-1858 `kelly_fraction(ev, cfg, *, deployed, bounds, seed)` — 'f_eff = H_deployed / H_full_kelly' against a point-estimate reference (n_trials=1, crisis_prob=0, decay_prob=0, cost_uncertainty=0, robust_lambda=0, redundancy_lambda=0); reference population capped at n_worlds=96, n_rows=200; status BOUND vs MEASURED when the reference pins on `REFERENCE_CAP` (MEASURED)
  - desks/mt5/research/pf_allocator.py:1818-1846 — the SHRINKAGE LADDER: winners_curse_deflation, crisis_worlds, edge_decay, cost_uncertainty, cvar_blend, redundancy_charge, each measured alone, plus a `per_sleeve_bounds` note (MEASURED)
  - libs/portfolio/kelly_surface.py:89-122 `surface(worlds, book, *, tolerance, alpha, fractions)` — per fraction: `mean_growth`, `robust_growth` (alpha-quantile, alpha=cvar_alpha=0.20), `worst_decile_growth` (0.1 quantile), `p_ruin`, `p_dd_over_tolerance`, `dd_median`, `dd_p90`; returns `f_opt`, `f_robust`, `f_tail`, `heat_tail_max`, `heat_sampled_max` (MEASURED)
  - libs/portfolio/kelly_surface.py:125-157 `_derivatives` — dG/dH and d2G/dH2 by central difference, 'Keep adding risk while this remains positive' (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1158 leg `pf_allocator` (kelly_fraction + kelly_surface run inside pf_allocator.run) · artifact: the `kelly` and `kelly_surface` blocks of desks/mt5/reports/pf_allocation.json; `heat.survival` for the envelope · consumer: heat_policy.resolve (survival ceiling), libs/portfolio/aggression.explain (tail_safety component), missed_growth.measure_survival_ceiling
  - next: In libs/portfolio/kelly_surface.py add `surface(..., subset=worlds.crisis)` and publish a second `crisis` block beside the main one in pf_allocation.json — a crisis-conditional f_opt/f_robust that can be COMPARED to the blended one, so a book whose crisis Kelly is HIGHER than its blended Kelly stops being under-sized by the mixture.
- **P5 Orthogonality: Alpha Dependency Graph → effective breadth** — PARTIAL
  - gap: Six of the ten blueprint channels exist (return correlation, downside/stress correlation, tail co-drawdown, factor exposure, mechanism/cluster occupancy, regime dependence); event/timestamp overlap, mechanism-similarity distance, feature lineage and execution collision are not edges anywhere. And there are two disconnected breadth numbers — EFFECTIVE_BREADTH.json (1.324, read by research) and latent_factors.effective (read by the heat law) — so the published breadth does not gate capital.
  - desks/mt5/research/alpha_breadth.py:1-49 — four readings: `exposure_full_sample`, `exposure_systematic`, `exposure_stress`, `realised_returns`; headline is the MINIMUM of the measured ones; `OUT = BASE / "reports" / "EFFECTIVE_BREADTH.json"` (:84) (MEASURED)
  - libs/research/effective_breadth.py:136 `exposure_neff`, :171 `exposure_breadth`, :213 `factor_breadth(k_factors=3)`, :258 `lagged_vol_regime`, :313 `conditional_breadth`, :370 `realised_breadth`; floors `MIN_PAIR_OVERLAP = 20` (:92), `MIN_PANEL_OBS = 250` (:97), `MIN_SCALE_OBS = 20` (:101), `MIN_REGIME_OBS = 60` (:106) (MEASURED)
  - MEASURED VALUE: desks/mt5/reports/EFFECTIVE_BREADTH.json — `effective_breadth: 1.324`, `n_nominal: 31`, `binding_reading: exposure_systematic`, `breadth_ratio: 0.0427`, `mean_pairwise_corr: 0.2118`, `max_pairwise_corr: 0.9891`; desks/mt5/data/effective_breadth.jsonl has 4 rows, all 1.324, last 2026-09-05T17:30 (MEASURED)
  - CLUSTER OCCUPANCY: libs/research/alpha_clusters.py — 15 `AlphaCluster` entries, `TARGET_MIN = 8` (:60), `TARGET_MAX = 15` (:61), classification DECLARED not inferred; EFFECTIVE_BREADTH.json records `n_clusters_occupied: 4`, `n_clusters_empty: 11` (MEASURED)
  - clock: desks/mt5/research/daily_cycle.py:338 list inside `_state_research_feedback` (desks/mt5/research/daily_cycle.py:319), STEP at desks/mt5/research/daily_cycle.py:472, and daily_cycle itself is desks/mt5/research/hourly_cycle.py:1019 leg `daily` under MT5-Hourly (desks/mt5/ops/box_tasks.manifest:63) · artifact: desks/mt5/reports/EFFECTIVE_BREADTH.json + desks/mt5/data/effective_breadth.jsonl · consumer: libs/research/breadth_credit.py:81 (research compute credit), desks/mt5/research/drawdown_alpha.py:62; NOT the allocator — pf_allocator uses its own libs/portfolio/latent_factors.effective
  - next: Add a `timestamp_overlap` reading to desks/mt5/research/alpha_breadth.py computed from the shadow ledgers' entry_time fields (Jaccard of trading minutes per pair) — it needs no new data and it will RAISE measured breadth wherever two same-mechanism sleeves fire in different hours, which the correlation reading currently prices as one bet.
- **P6 Meta-alpha: P(alpha_i works now) as an edge-reliability posterior** — PARTIAL
  - gap: There is a hazard (P(breaks next horizon), drift-channel-derived, often None under its own floors) but no single P(works now) posterior fusing recent expectancy, hit rate, vol, spread, correlation regime, macro state, alpha age, crowding and execution degradation, and nothing reduces those nine into one number the allocator multiplies the edge by. Alpha age is not recorded at all.
  - NO MODULE COMPUTES P(alpha works now): grep across the tree for 'P(alpha', 'works now', 'edge reliability', 'reliability posterior', 'meta_alpha' returns only desks/mt5/research/meta_desk.py:22 ('11  decay_detector  meta-alpha: predict which sleeve is about to die') and desks/mt5/docs/FRONTIER_AI_LAYER.md:60 — both prose (MEASURED)
  - THE INVERSE EXISTS AND IS CONSUMED: libs/research/perishability.py:584-609 `edge_hazard(components, horizon_days=HAZARD_HORIZON_DAYS, scale_days=HAZARD_SCALE_DAYS, min_channels=HAZARD_MIN_CHANNELS)` — 'hazard_i(t) = P(edge breaks next horizon | history)'; returns None under its floors, and 'no SLEEVE-scoped channel' is a refusal (MEASURED)
  - desks/mt5/research/pf_allocator.py:1276-1291 `hazard_by_sleeve(drift)` reads `DRIFT.json`'s `hazard_by_sleeve`; :1292-1313 `apply_hazard_shrink(ev, haz)` tilts the evidence; the pair is billed as a rail by missed_growth.measure_hazard_shrink (desks/mt5/research/missed_growth.py:300) (MEASURED)
  - STATE CONDITIONING IS THE 'MARKET STATE' HALF: libs/portfolio/robust_elog.py:333-368 the fourth hierarchy level, `k_state = 40.0`, fed by pf_allocator._state_returns (:783) over session phase + admitted event/weekday dimensions (:843) (MEASURED)
  - clock: the hazard half: drift_monitor in desks/mt5/research/daily_cycle.py:337 list; edge_confidence in desks/mt5/research/hourly_cycle.py:1252 leg `edge_confidence`. No scheduler for a reliability posterior, because none exists. · artifact: desks/mt5/reports/DRIFT.json (`hazard_by_sleeve`), desks/mt5/reports/EDGE_CONFIDENCE.json · consumer: pf_allocator.hazard_by_sleeve/apply_hazard_shrink (DRIFT.json); EDGE_CONFIDENCE.json is read by desks/mt5/research/issue_board.py only
  - next: Write `p_works_now` into libs/research/perishability.py as `1 - hazard` fused with `capital_modifiers.category`'s state ratio and `execution_cost.cost_ratio`, publish it per sleeve in DRIFT.json, and have pf_allocator use it to RAISE `forward_days`-equivalent weight where it is high (rather than only shrinking where hazard is high) — so a sleeve the market state currently favours is sized up, not merely not sized down.
- **P7 Alpha decay model: per-alpha decay posterior, half-life, pre-emptive replacement hunting** — PARTIAL
  - gap: The demotion half is lit and correct; the MODEL half is absent. There is no per-live-alpha decay posterior, no fitted half-life, no aging fields (research/market/live age, last revalidated), and the allocator applies one 30% decay probability to every sleeve. Replacement begins only AFTER retirement.
  - desks/mt5/research/decay_monitor.py:1-42 — the wired demotion organ; `FADE` at n>=20 and trailing t<=0 (risk halved), `RETIRE` at n>=20 and t<=-2.5, hard rail at trailing maxDD <= -25R at any n; artifacts `data/decay_live.json` every run and `data/decay_actions.jsonl` append-only (MEASURED)
  - desks/mt5/mt5desk/sizing.py:28 `FADE_FACTOR = 0.5` pinned to decay_monitor.FADE_FACTOR; :33-63 `decay_factor` applied OUTSIDE `clamp_risk_frac` because the clamp floors at `BASE_RISK_FRAC = 0.03` (:23) — the documented bug where 'a HEALTHY sleeve and a FADED one both sized 3.0 lots' (MEASURED)
  - MEASURED STATE OF decay_live.json (desks/mt5/data/decay_live.json, checked_at 2026-09-04T02:38:50): `"live_sleeves": 0`, `"roster_state": "NO_ROSTER"`, `"roster_why": "sleeves.json does not exist -- its only writer is desks/mt5/research/promoter.py, so nothing has ever been promoted to live risk"`, `"verdicts": {}` (MEASURED)
  - CONFIRMING: `find` for sleeves*.json across the tree returns nothing, while .gitignore:301 explicitly un-ignores `desks/mt5/data/sleeves.json` — so its absence is real, not a gitignore artifact (MEASURED)
  - clock: desks/mt5/research/daily_cycle.py:471 STEPS ("decay", _decay) → decay_monitor.run; daily_cycle runs from desks/mt5/research/hourly_cycle.py:1019 leg `daily` under MT5-Hourly (desks/mt5/ops/box_tasks.manifest:63) · artifact: desks/mt5/data/decay_live.json (present, regenerated 2026-09-08 21:05), desks/mt5/data/decay_actions.jsonl · consumer: gateway via data/sleeves.json `decay_faded` → decision_core.ramped_fraction / sizing.decay_factor; freshness fence (desks/mt5/research/daily_cycle.py:489 names decay_live.json at a 26h limit)
  - next: In desks/mt5/research/decay_monitor.py fit an exponential to each sleeve's trailing expectancy series and publish `half_life_days` + `t_to_fade` per sleeve into decay_live.json; feed it to pf_allocator as the per-sleeve `decay_prob_i` from P3 — sleeves with a long fitted half-life then stop being charged the blanket 30% haircut and earn more heat.
- **P8 State-dependent allocation w_i(state_t)** — PARTIAL
  - gap: The state enters the POSTERIOR (k_state=40) and picks WHICH ALLOCATOR is authorised, but the per-state heat curve is scored on a proportionally scaled global book (`basis: scaled_candidate`), so the COMPOSITION w_i is not re-optimised inside a state. Rollover is not a state dimension at all, and 'session' can be barred entirely by the admission graveyard.
  - desks/mt5/research/pf_allocator.py:1207-1275 `_live_state()` → (session phase, per-sleeve trades, broker UTC offset); 'RETURNS (None, {}, 0) ON ANY DOUBT'; the offset is read via `session_phase.broker_utc_offset_h()`, never assumed (MEASURED)
  - desks/mt5/research/pf_allocator.py:783-842 `_state_returns(name, phase, trades_by_sleeve, broker_utc_offset_h)`; :843-870 `_admitted_extra_dims()` narrows the bucket by every admitted non-session dimension read from reports/STATE_ADMISSION.json (MEASURED)
  - libs/portfolio/robust_elog.py:333-368 — the STATE level of the hierarchy, `k_state = 40.0`, 'UNCERTAINTY WIDENS, IT NEVER NARROWS' (`se = se + |conditioned - post_mean|`) (MEASURED)
  - REGIME-CONDITIONED WORLDS: libs/portfolio/robust_elog.py:145-160 `regime_labels`/`regime_probs`, `regime_min_days = 60`; desks/mt5/research/pf_allocator.py:440-562 `regime_state(...)` with `REGIME_MIN_SHARE = 0.08`, `REGIME_MAX_SHARE = 0.60`, `REGIME_FORECAST_H = 1`, `REGIME_TERM_STRUCTURE = (1, 2, 5, 21)` (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1158 leg `pf_allocator`; state_admission at desks/mt5/research/daily_cycle.py:465 STEPS ("state_admission", _state_admission) · artifact: `heat.state`, `heat.state_curves`, `proof_by_state`, `state_vector` blocks of desks/mt5/reports/pf_allocation.json; reports/ALLOCATOR_PROOF.json `by_state`; reports/STATE_ADMISSION.json · consumer: gateway.allocator_book → allocator_proof.select / allocator_blend.select_blend → the sizing book; heat_policy.resolve (state curve → per-state heat)
  - next: In desks/mt5/research/pf_allocator.py:1347 `state_growth_curves`, re-`optimise` the composition inside the current state bucket only (one extra solve for the live state, not all buckets) and publish it as `state_book` — the state the desk is actually in then gets a book solved for it instead of a scaled copy, which can only raise conditional growth.
- **P9 Contextual bandits for allocator exploration among validated sleeves** — MISSING
  - gap: bandit.py decides which RESEARCH ARM gets compute; nothing explores among VALIDATED SLEEVES for capital. A sleeve that the admission scan refuses by a hair gets zero heat forever with no exploration allocation to resolve the ambiguity, and the bandit's own dE[log W] term is flat (worth=1.0 everywhere) because no certified sleeve is attributable to an arm.
  - libs/research/bandit.py:1-36 — 'The research-direction bandit: which KIND of research deserves the next unit of compute'; `ARMS` (:51-55) are eleven RESEARCH DIRECTIONS (new_mechanism, mutate_survivor, combine_survivors, conditional_state_edge, execution_improvement, exit_improvement, cross_asset_signal, alt_data_hypothesis, failure_derived, model_architecture, external_screen) — not sleeves (MEASURED)
  - libs/research/bandit.py:149 `PSEUDO = 20.0`, :150 `EXPLORE = 0.20`, :154 `MIN_JUDGED = 10`; `score_j = E[dElogW_j] x P(valid survivor_j) / (compute + data + latency + multiplicity)_j`; Thompson sampling in `allocate` (:197) (MEASURED)
  - desks/mt5/reports/RESEARCH_BANDIT.json (2026-09-07T18:40) — shares over the eleven arms; every arm shows `certified: 0` and `worth: 1.0`, i.e. the dE[log W] term is flat because no certified sleeve is attributable to an arm (MEASURED)
  - libs/research/breadth_credit.py:8-20 — 'its E[dElogW] term -- `worth` -- came back 1.000 for all eleven arms, because `_marginal_by_arm` needs a `pf_allocator.json` with certified sleeves attributable to arms and there is not one … the budget is decided by the DECLARED COST TABLE: corr(share, 1/cost) = 0.87' (MEASURED)
  - clock: for the RESEARCH bandit: desks/mt5/research/daily_cycle.py:463 STEPS ("research_bandit", _research_bandit) → desks/mt5/research/research_bandit.py → libs/research/bandit.run. NONE for allocator exploration. · artifact: desks/mt5/reports/RESEARCH_BANDIT.json + desks/mt5/data/research_budget.json (research only) · consumer: desks/mt5/research/deepening_worker.py:470 `arm_weight`, desks/mt5/research/hourly_discovery.py:128, desks/mt5/research/daily_cycle.py:269 (proposer time budgets), libs/ops/module_rent.py:65, desks/mt5/research/research_pnl.py:47
  - next: Add an `explore` block to desks/mt5/research/pf_allocator.py's admission scan that assigns a small Thompson-sampled heat to candidates whose `delta_robust` is inside `margin_per_day` (already computed at :1585) instead of refusing them — ambiguous candidates get funded and measured rather than being permanently parked at zero.
- **P10 Shadow alpha farm: every validated-but-unfunded strategy runs continuously** — PARTIAL
  - gap: 56 clocks do run continuously, but the lifecycle vocabulary is three incompatible sets (promoter LIVE/STANDBY/UNMEASURED, clocks PROMOTION_CANDIDATE, alpha_state's eleven rungs), the alpha_state ledger is effectively empty (2 rows), and the LIVE end of the farm is empty (0 funded sleeves) — so 'no evidence gap' is true for shadow and false for the funded lane.
  - desks/mt5/data/forward_reconcile.json (checked_at 2026-09-04T02:35:08) — `"enrolled": 56`, `"certified_clocks": 52`, `"certified_pairs": 32`, `"unreachable_certified": {"n": 0}`, plus REVIVED_CERTIFIED actions (MEASURED)
  - backups/moat/shadow_ledgers/ holds ~80 `ledger_*.json` per-clock forward ledgers across three lanes (shadow_state.json, qquant_shadow_state.json, scalp_shadow_state.json, external_shadow_state.json) (MEASURED)
  - libs/research/alpha_state.py:1-34 — the state machine: DISCOVERED → IMPLEMENTED → TESTED → STATISTICALLY_VALID → SHADOW → OOS_VALIDATED → INDEPENDENCE_CHECKED → PORTFOLIO_VALIDATED → CAPITAL_ELIGIBLE → LIVE → MONITORED, with DEGRADED/RETIRED reachable from anywhere; 'NO SKIPPING, INCLUDING UPWARD' (MEASURED)
  - BUT data/alpha_state_ledger.jsonl holds exactly 2 rows, both for the same `alpha_id` 'cat|ratio|1h|all|funding|oi' (IMPLEMENTED then TESTED) — the state machine has essentially never been driven (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1125-1126 leg `enrol_clocks` (`_producer("shadow_forward", "desks/mt5/research/shadow_forward.py")`); desks/mt5/research/daily_cycle.py:466-467 STEPS ("shadow", _shadow), ("qquant_shadow", _qquant_shadow); desks/mt5/ops/box_tasks.manifest:57-60 MT5-Shadow / MT5-QQuantShadow, both `trigger="UNDECLARED"` with `installer="NONE"` · artifact: backups/moat/shadow_ledgers/ledger_*.json, desks/mt5/reports/shadow/*.json, desks/mt5/data/forward_reconcile.json · consumer: promoter (lane clocks), pf_allocator.scalp_evidence / certified_evidence, pf_allocator._live_state (reads the same ledgers for state conditioning)
  - next: Have desks/mt5/research/promoter.py write each roster transition into data/alpha_state_ledger.jsonl through libs/research/alpha_state.advance — one append per pass — so the four-lane picture becomes a single queryable ledger and every validated-but-unfunded sleeve is visibly accruing evidence rather than being invisible outside its own lane file.
- **P11 Hierarchical transfer learning / partial pooling across symbols, sessions, horizons** — PARTIAL
  - gap: Partial pooling exists and is correctly two-sided (sleeve↔family↔zero, plus state), but the grouping variable is the MECHANISM only. There is no instrument, asset-class, session or horizon level, so evidence on XAUUSD reaches XAGUSD only if both carry the same mechanism string, and a new instrument in a proven family inherits nothing through the allocator's prior (only through family_evidence's separate promotion path).
  - libs/portfolio/robust_elog.py:311-331 — the pooling: family mean weighted by `fam_w = eff`, then `post_mean = lam_s * m + (1 - lam_s) * (lam_f * fam_vec)` with `k_sleeve, k_family = 60.0, 120.0`; the outer prior is ZERO (no edge). This IS partial pooling, three levels deep (MEASURED)
  - libs/portfolio/robust_elog.py:333-368 — a fourth, narrower level: state, `k_state = 40.0`, sleeve's OWN state returns only ('evidence about this sleeve, never a borrowing from another') (MEASURED)
  - THE HIERARCHY IS MECHANISM, NOT INSTRUMENT: desks/mt5/research/pf_allocator.py:744-752 — 'FAMILY IS THE MECHANISM, NOT THE SYMBOL. … The mechanism is what shares a prior; the instrument is what shares a correlation, and correlation is handled by the worlds.' `fam = "session_bracket" if … else "_".join(parts[1:-1])`, `symbol=parts[0]` is carried but never pooled on (MEASURED)
  - SO XAUUSD DOES NOT UPDATE XAGUSD unless they share a mechanism family; there is no metal / FX / index / asset-class level, no session level above the sleeve, and no horizon level (MEASURED — grep for a symbol- or asset-class-keyed pooling term in robust_elog returns nothing)
  - clock: desks/mt5/research/hourly_cycle.py:1158 leg `pf_allocator` (the posterior runs every pass); family_evidence via the shadow/promoter chain · artifact: `mu_draws` inside the world tensor (not published per-level); the `evidence` block of pf_allocation.json; backups/moat/shadow_ledgers/FAMILY_EVIDENCE.json · consumer: pf_allocator.optimise (the posterior IS the input); promoter's forward verdict (family_evidence)
  - next: Add a second grouping key to libs/portfolio/robust_elog._posterior_mu — pool `mechanism x asset_class` alongside `mechanism`, with its own `k_class` — and populate `asset_class` in pf_allocator.sleeve_evidence from the universe registry. A certified mechanism on a new metal or cross then starts with its family's mean rather than at zero, which is the shrinkage relief that lets a new symbol earn heat sooner.
- **P12 Capacity-aware Elog(C): expected growth as a function of capital per alpha** — PARTIAL · gate: data
  - gap: Capacity is a scalar floor + a refused ceiling, not a function. There is no Elog(C) curve per alpha, so the desk cannot say which of its edges will die as the account grows, and the envelope's capacity clause is permanently inert.
  - desks/mt5/research/capacity.py:1-45 — measures the FLOOR exactly (`min_lot_risk_eur(symbol, dist)` → the equity below which the venue's minimum lot overrides policy) and REFUSES the ceiling: 'CEILING UNMEASURED, and said so rather than estimated. Impact needs realised fills and `execution.matched_fills` is 0 -- nothing has ever filled.' Publishes `headroom_multiple` as 'how long does this edge remain ours' (MEASURED)
  - desks/mt5/research/pf_allocator.py:955-980 `_capacity_ceiling()` returns None unless reports/CAPACITY.json carries `ceiling_status == "MEASURED"` and a positive `ceiling_heat_fraction`; 'an unmeasured capacity is not infinite capacity' (MEASURED)
  - libs/portfolio/kelly_surface.py:174 — the envelope's `capacity(h) > 0` clause is real and is fed `capacity_max=_capacity_ceiling()` at desks/mt5/research/pf_allocator.py:2088, so today it is always None and the clause never binds (MEASURED)
  - libs/portfolio/capacity_allocation.py:1-31 — 'NOTHING CAPPED A WEIGHT BY CAPACITY … `libs/execution/book_walk.capacity_at_impact` measures capacity from the desk's own recorded depth'; 'CAPACITY IS A HARD CONSTRAINT, NOT A PENALTY'; pure numpy, no I/O (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1228 `_costed("capacity", lambda: _producer("capacity", "desks/mt5/research/capacity.py"))` · artifact: desks/mt5/reports/CAPACITY.json (`ceiling_status`, `headroom_multiple`, per-sleeve floor equity) · consumer: pf_allocator._capacity_ceiling → kelly_surface.envelope's capacity clause (currently inert because ceiling_status is not MEASURED)
  - next: In desks/mt5/research/capacity.py sweep account equity across a decade-scale grid and publish `elog_at_capital` per sleeve using the existing `min_lot_risk_eur` floor and the modelled cost — a curve that costs no new data and immediately identifies the tiny-account edges worth harvesting hardest NOW while they are still ours.
- **P13 Margin-capital optimisation in the objective** — PARTIAL · gate: box-paste
  - gap: The margin clause is written, tested and unfed — the one argument that would make it bind is never passed from pf_allocator. Swap/financing is measured per sleeve and never enters the returns the allocator maximises, and collateral efficiency / simultaneous-position requirements are not modelled at all.
  - libs/portfolio/kelly_surface.py:63-66 `MAX_MARGIN_USE = 0.50` — 'Account margin the book may consume before the broker's own feasibility binds … Only applied when the box has actually measured margin use'; the clause is implemented at :202 (`mu = {...margin_use...}`) and :213-215 (`bad.append("margin_use")`) (MEASURED)
  - IT IS NEVER FED: desks/mt5/research/pf_allocator.py:2087-2089 calls `_envelope(rows, alpha=cfg.cvar_alpha, fallback=HEAT_HARD_CEILING, capacity_max=_capacity_ceiling())` — no `margin_use` argument. Repo-wide grep for `margin_use` outside kelly_surface returns only desks/mt5/tests/test_survival_envelope.py:103-117 (MEASURED)
  - SWAP/FINANCING IS MEASURED AND NOT IN THE OBJECTIVE: desks/mt5/mt5desk/financing.py:1-25 — '46.4% OF THE 15,932 BACKTESTED TRADES … HOLD THROUGH AT LEAST ONE ROLLOVER STAMP, at a mean of 0.788 charged nights per trade'; breakeven swap per sleeve (gold_asia 1,734.31, gold_london_am 329.85, gold_afternoon 176.05 per lot per night) (MEASURED)
  - desks/mt5/swap_exposure.json exists (7,389 bytes, 2026-08-x) but robust_elog's cost terms are `cost_r` and `cost_bias_r` only — spread at the fill hour (libs/portfolio/execution_cost.py) — with no swap/carry term (MEASURED)
  - clock: the envelope runs every pf_allocator pass (desks/mt5/research/hourly_cycle.py:1158); nothing schedules a margin measurement into it · artifact: `heat.survival` block of pf_allocation.json (with `binding` naming the clause); desks/mt5/swap_exposure.json · consumer: heat_policy.resolve reads the survival ceiling; the margin clause has no producer, so it has no consumer
  - next: Pass a measured `margin_use` map into libs/portfolio/kelly_surface.envelope from desks/mt5/research/pf_allocator.py:2087 (built from `mt5.account_info()` margin per unit heat, or omitted when the terminal is unreachable) — the envelope then binds on a BROKER fact instead of falling back to the recorded 0.30 constant, which on a book with margin headroom will license MORE heat than the constant does.
- **P14 Tail-state allocation: normal vs crisis correlation modelled separately** — PARTIAL
  - gap: Crisis correlation is a single scalar common-factor share (measured and ratcheted upward, which is right) applied uniformly to every sleeve, so there is no separate crisis correlation MATRIX and no factor-specific shock. A USD shock — the one co-explosion this book's FX-heavy composition is most exposed to — is not a modelled state, and the measured stressed k_eff currently equals the unstressed one.
  - libs/portfolio/robust_elog.py:488-499 — the crisis overlay: a common factor carrying `crisis_common_share` of each sleeve's variance ('no correlation matrix to estimate, no positive-definiteness to repair, and the tails stay the real ones'), then `* crisis_vol_mult`, then an asymmetric mean shift `- |sd| * 0.25` (MEASURED)
  - Defaults: `crisis_prob = 0.06`, `crisis_vol_mult = 2.5`, `crisis_common_share = 0.55` (libs/portfolio/robust_elog.py:124-128); a one-factor overlay at share s has pairwise correlation exactly s (MEASURED)
  - libs/portfolio/conditional_covariance.py:1-30 — `calibrate` MEASURES `crisis_common_share` and `crisis_vol_mult` from the book's own stress regime (the regime with the highest mean sleeve volatility, data-driven, no label parsing) and 'takes the maximum of the standing constant and the shrunk measurement … It can only ratchet upward' (MEASURED)
  - pf_allocator publishes `crisis_calibration` with `crisis_common_share`, `crisis_vol_mult`, `stress_regime` and `by_regime: {n_days, mean_corr, mean_vol, diversification_ratio}` (desks/mt5/research/pf_allocator.py:2650-2660) (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1158 leg `pf_allocator` (calibration + overlay run inside every pass); drift_monitor at desks/mt5/research/daily_cycle.py:337; edge_confidence at desks/mt5/research/hourly_cycle.py:1252 · artifact: `crisis_calibration` and `drift_overlay` blocks of pf_allocation.json; reports/DRIFT.json; reports/EDGE_CONFIDENCE.json · consumer: robust_elog.sample_worlds (the overlay IS the consumer); heat_policy.resolve via effective heat's tail term
  - next: In libs/portfolio/conditional_covariance.py, calibrate `crisis_common_share` per FACTOR BLOCK (USD leg, JPY leg, metals) using libs/risk/fx_factors exposures instead of one book-wide scalar, and pass a per-sleeve share vector into robust_elog.sample_worlds — sleeves outside the shocked block then keep their real independence in crisis worlds and stop being penalised for a shock they do not share.
- **P15 Dynamic opportunity-set forecasting + market ecology / crowding models** — PARTIAL
  - gap: Everything measures where the opportunity ISN'T (the gap, the empty clusters, the binding cause). Nothing forecasts where alpha is likely to EMERGE next, and the crowding/ecology model — which is the piece that would predict decay speed and capacity per mechanism — has been unscheduled since its crons were retired on 2026-09-05 and its state file has not moved since 2026-08-21.
  - BACKWARD-LOOKING GAP MEASUREMENT EXISTS: desks/mt5/research/pf_allocator.py:1095-1131 `opportunity(free, book, target)` — `opportunity_density = sum of POSITIVE marginal dE[log W]`, `heat_gap`, `positive_marginal_by_session`, `positive_marginal_by_family`, `research_request` ('4% unfilled, nothing in the Asia session outside JPY') (MEASURED)
  - desks/mt5/research/portfolio_gap.py:220 writes `{"kind": "heat_gap", "priority": 1}` into `research_requests`; :245-252 publishes `target_heat`, `held_heat`, `heat_gap`, `n_certificates` (MEASURED)
  - desks/mt5/reports/OPPORTUNITY_GAP.json (2026-09-06T00:53) — five causes with binding flags: SUPPLY 23,465 (not binding), CONVERSION 0 (BINDING), BREADTH UNMEASURED, CAPITAL 0, EXECUTION UNMEASURED (BINDING) (MEASURED)
  - EMPTY-CLUSTER TARGETING IS THE NEAREST THING TO FORECASTING: libs/research/alpha_clusters.py declares 15 phenomena from outside the book and names the 11 empty ones; desks/mt5/research/alpha_breadth.py:29-36 writes them into the deepening queue as research tasks (MEASURED)
  - clock: portfolio_gap + opportunity_curve + alpha_breadth via desks/mt5/research/daily_cycle.py:338-345 (`_state_research_feedback`); the crowding lane: NONE (crontab rows RETIRED, no cycle leg) · artifact: desks/mt5/reports/OPPORTUNITY_GAP.json, reports/PORTFOLIO_GAP (via portfolio_gap.py), the `opportunity` block of pf_allocation.json; desks/mt5/data/crowding_state.json (stale) · consumer: the deepening queue (empty clusters, alpha_breadth); desks/mt5/research/hour_surface.py:21 cites `portfolio_gap.research_requests`; crowding_state.json has NO consumer
  - next: Add a `crowding_miner` leg to desks/mt5/research/hourly_cycle.py beside the other producers so desks/mt5/data/crowding_state.json is fresh again, and have libs/research/crowding_hazard.hazard consume it — the mechanisms it rates LOW publicity become explicit hunting priority rather than an 18-day-old file nobody reads.
- **P16 Automatic replacement planning (predicted half-life → successor search starts now)** — MISSING
  - gap: The desk can detect decay (decay_monitor) and can guess at it (edge_hazard, crowding_hazard), but nothing converts a predicted decay into a successor hunt. The slot is refilled only after the incumbent has already been retired, so the replacement lead time is exactly zero.
  - desks/mt5/research/decay_monitor.py:31-36 — 'REPLACEMENT IS THE PIPELINE'S OWN JOB. A retired sleeve frees its slot; the daily promoter fills slots from matured forward candidates the same day (same-day law, back half). Re-entry for the retired sleeve is ONLY through a fresh forward window' — replacement is triggered by RETIREMENT, never by a prediction (MEASURED)
  - NO PREDICTED HALF-LIFE PER ALPHA: grep for `half_life` across the desk returns only libs/research/information_decay.py (:101 `half_life_s`, :116 `_cls`, :249 `0.5 ** (age / c.half_life_s)`) which is about DATA classes, not alphas (MEASURED)
  - THE NEAREST FORWARD SIGNAL IS A HAZARD, AND IT MOSTLY REFUSES: libs/research/perishability.py:584-609 `edge_hazard` returns `{"hazard": None, "verdict": UNMEASURED}` under `HAZARD_MIN_CHANNELS` or with no SLEEVE-scoped channel — 'one book-level number repeated across fifty sleeves is not fifty per-edge verdicts, and capital must not move on it' (MEASURED)
  - IT DRIVES SIZING, NOT SEARCH: desks/mt5/research/pf_allocator.py:1276-1313 `hazard_by_sleeve` / `apply_hazard_shrink` tilt the posterior; nothing turns a hazard into a research task (MEASURED — no writer to the deepening queue keyed on hazard)
  - clock: NONE for replacement planning (decay_monitor itself is scheduled at desks/mt5/research/daily_cycle.py:471, but only for FADE/RETIRE verdicts) · artifact: NONE · consumer: NONE
  - next: In desks/mt5/research/decay_monitor.py, emit a `successor_hunt` task into the deepening queue for every live sleeve whose fitted half-life (from P7) is under 2x its remaining forward window, keyed on its mechanism and symbol — the successor search then starts while the incumbent is still earning, so capacity is never idle between a retirement and its replacement.
- **P17 Adaptive risk distribution under the fixed cap + unused-heat intelligence** — PARTIAL
  - gap: The mechanism is complete on paper and inert in practice: every rail's opportunity cost reads UNMEASURED because no allocator pass has written pf_allocation.json on the money box, so the calibration loop has never weakened a rail, and the heat-gap research request has no verified consumer that turns it into a hunting mission.
  - THE FLOOR IS FILLED, NOT REPORTED AS A SHORTFALL: desks/mt5/research/pf_allocator.py:1134-1200 `fill_floor(book, ev, target, ub, family_of, *, cfg, worlds)` — 'the resolved heat -- 20% floor, growth above it to the ceiling -- is what the book HOLDS'; bounds yield in order `drawdown_bound` → `family_cap` → `share_cap` → proportional scale, and 'Every relaxation is returned in the note and billed by `missed_growth` as that rail's opportunity cost' (MEASURED)
  - desks/mt5/research/pf_allocator.py:1149-1152 — `share_cap = MAX_SLEEVE_HEAT_SHARE * target` (0.25 x target); the fill never overrides the ruin guard (MEASURED)
  - desks/mt5/research/pf_allocator.py:1163-1170 — `optimise(..., hard_cap=max(HEAT_HARD_CEILING, target), ...)` so 'a policy that measured 38% solved against a 30% cap and quietly delivered 30%' cannot recur (MEASURED)
  - THE GAP BECOMES A RESEARCH REQUEST: desks/mt5/research/pf_allocator.py:1126-1131 `research_request: [f"{gap:.2%} of the heat target is unfundable by the current library at its per-sleeve bounds"]`, broken out `by_session` and `by_family` (:1113-1120) (MEASURED)
  - clock: fill_floor + opportunity inside desks/mt5/research/hourly_cycle.py:1158 leg `pf_allocator`; missed_growth and portfolio_gap at desks/mt5/research/daily_cycle.py:338/342 inside `_state_research_feedback` (STEP at desks/mt5/research/daily_cycle.py:472) · artifact: `floor_fill`, `opportunity`, `aggression` blocks of pf_allocation.json; desks/mt5/reports/MISSED_GROWTH.json + data/missed_growth.jsonl; data/rail_calibration.json · consumer: libs/portfolio/rails.rail_multiplier reads rail_calibration.json back into the rails; portfolio_gap.research_requests is cited by desks/mt5/research/hour_surface.py:21 (INFERRED — no direct queue writer verified)
  - next: Wire desks/mt5/research/portfolio_gap.py's `research_requests` (already carrying `kind: heat_gap` and the missing session/family from pf_allocator.opportunity) into the deepening queue the way alpha_breadth writes empty clusters — the unfundable heat then becomes an explicit hunt for the missing exposure type instead of a number in a report.
- **P18 Time-of-day capital market: each session runs its own capital auction** — MISSING
  - gap: There is one heat budget per pass for the whole clock. The session narrows a sleeve's posterior (k_state=40) and picks which allocator is authorised, but no session holds its own budget and no two sessions ever bid against each other, so an hour with an unusually rich opportunity set cannot draw heat away from a thin one.
  - desks/mt5/research/hour_surface.py:25 — 'ADVISORY. It moves no capital and gates nothing; it writes reports/hour_surface.json'; `OUT = BASE / "reports" / "hour_surface.json"` (:42) (MEASURED)
  - desks/mt5/research/hour_prior.py:3-4 — 'WHY THIS EXISTS. `desks/mt5/research/hour_surface.py` has been measuring expected R by hour of day and writing hour_surface.json, and NOTHING has ever read it -- the only importer is its own producer.'; :70 'hour_surface.json absent -- no prior, not a neutral prior' (MEASURED)
  - CONFIRMED DARK: repo-wide grep for `hour_prior` / `hour_surface` outside those two files returns a single hit — desks/mt5/research/pf_allocator.py:2613, a COMMENT naming `hour_surface` as one of the readers of `marginal_delta_elog` (i.e. the dependency runs the other way) (MEASURED)
  - THE SESSION ENTERS ONLY AS A POSTERIOR LEVEL AND A PROOF BUCKET, NOT AS A BUDGET: libs/portfolio/robust_elog.py:333-368 (`k_state = 40.0`, session phase narrows the sleeve's own mean) and libs/portfolio/allocator_proof.py:136 `state_id` / :363 `select` (which allocator is authorised in this state) (MEASURED)
  - clock: NONE for a session capital market. hour_surface has no cycle leg; hour_prior has no importer. · artifact: desks/mt5/reports/hour_surface.json (advisory, unread) · consumer: NONE
  - next: In desks/mt5/research/pf_allocator.py, solve the book once per session bucket at that bucket's own `state_growth_curves` optimum and publish a `session_book` map keyed by phase; the gateway already reads `heat.state` (desks/mt5/mt5desk/gateway.py:287), so a session whose curve peaks above the global one gets MORE heat during its own hours rather than the blended average.
- **P19 Compounding attribution and true P&L decomposition** — PARTIAL
  - gap: The decomposition is scheduled, has an artifact and has a consumer, but eight of its nine terms read UNMEASURED because `sleeves_with_realized_evidence` is 0 and no allocator pass wrote ALLOCATOR_PROOF scores on the money box. There is also no per-sleeve compounding/timing term and no explicit 'chance' term — both fall into the residual.
  - desks/mt5/research/allocator_attribution.py:1-31 — the four error terms: EDGE `sum_i h_i * (realized_mu_i - expected_mu_i)`, CORRELATION `expected_book_var - realized_book_var`, EXECUTION `realized cost/heat - modelled cost/heat`, REGIME (charged BEFORE edge error), plus RESIDUAL 'Reported, never distributed' (MEASURED)
  - desks/mt5/research/allocator_attribution.py:38-68 — the NINE-TERM growth decomposition: ALPHA, SELECTION, STATE, SIZING (the anti-timidity ledger against the 20% floor / 30% ceiling), DIVERSIFICATION (nominal minus effective heat), EXECUTION, EXIT, COST, VETO — each `{value | UNMEASURED, basis, why}` (MEASURED)
  - desks/mt5/research/allocator_attribution.py:70-88 — a TENTH `_entry_term` from reports/EXCURSIONS.json, and `weekly_table`; 'the nine-term dict is a pinned contract'; the identity is stated with the terms it cannot close named by unit (MEASURED)
  - MEASURED STATE: desks/mt5/reports/allocator_attribution.json (2026-09-05T11:58) — `forecast_passes: 93`, `realized_basis: "UNMEASURED"`, `sleeves_with_realized_evidence: 0`; edge/correlation UNMEASURED ('0 scored day(s), need 5'), execution UNMEASURED, `regime: 0.5938`; selection UNMEASURED ('no allocator pass has written them on this host'); diversification UNMEASURED (MEASURED)
  - clock: desks/mt5/research/daily_cycle.py:342 `"allocator_attribution"` inside `_state_research_feedback` (desks/mt5/research/daily_cycle.py:319), STEP at desks/mt5/research/daily_cycle.py:472; daily_cycle runs from desks/mt5/research/hourly_cycle.py:1019 leg `daily` under MT5-Hourly · artifact: desks/mt5/reports/allocator_attribution.json + desks/mt5/reports/GROWTH_ATTRIBUTION_WEEKLY.json · consumer: desks/mt5/research/feature_roi.py — desks/mt5/research/daily_cycle.py:343-345 states it 'reads the conditioning ledger, RESEARCH_PNL's share_of_heat and allocator_attribution's state term'
  - next: In desks/mt5/research/allocator_attribution.py add a `compounding` term per sleeve — `h_i,t` from data/pf_forecast_log.jsonl (93 passes are already on disk) crossed with the shadow ledgers' realised daily R — so the timing half is measurable from the FORWARD clocks today instead of waiting on live fills that do not yet exist.

### Phase 6 — Execution intelligence: routing competition, slippage prediction, fill probability, self-footprint, broker microstructure

- **P20 Netting layer: 'NET WOULD SEND' rather than routing** — EXISTS-DARK
  - gap: Confirmed: netting is measurement-only by design and, in fact, has produced no measurement at all — no theoretical-position ledger, no intent ledger, no NETTING report. It is also scoped to `family_market`/`scalp_market` sleeves, which excludes the gold bracket book, the only book that has ever sent a live order. No netting decision has ever changed a real order, and none has ever even been recorded.
  - desks/mt5/mt5desk/gateway.py:1367-1371 `_net_routes(symbols)` — 'The ONE order per symbol the venue would see if the sleeves were netted -- measured and logged as NET WOULD SEND, never placed'; the log line is at desks/mt5/mt5desk/gateway.py:1386-1389 (MEASURED)
  - desks/mt5/mt5desk/gateway.py:1305-1313 `_netting_book()` — 'Today that order is MEASURED and logged, never sent (see `_net_routes`)' (MEASURED)
  - desks/mt5/mt5desk/netting.py:17-36 — 'Routing itself is a money-path change and is left to a separate, box-verified step; this measures first'; '`route` turns the delta into the ONE order the venue should see; the book never sends anything, and today the gateway still sends per sleeve' (MEASURED)
  - THE MEASUREMENT DOES NOT EVEN COVER THE BOOK THAT TRADES: desks/mt5/mt5desk/gateway.py:2102-2104 `_net_routes({s["symbol"] for s in sleeves if s.get("exec") in ("family_market", "scalp_market")})` — the gold bracket sleeves (`exec` unset, `lot: "auto"`) are excluded, and they are the only sleeves that have ever placed a live order (MEASURED)
  - clock: desks/mt5/mt5desk/gateway.py:2102 inside the gateway pass — MT5-Gateway, `trigger="continuous loop"`, desks/mt5/ops/box_tasks.manifest:47 (installer NONE) · artifact: intended: desks/mt5/data/theoretical_positions.jsonl, data/order_intents.jsonl, reports/NETTING.json, reports/NETTING_BOOK.json — ALL ABSENT · consumer: NONE — the log line has no reader, and the four artifacts do not exist
  - next: Include the bracket sleeves in the desks/mt5/mt5desk/gateway.py:2102 `_net_routes` symbol set (drop the `exec` filter) so the theoretical book finally records the gold legs; that produces the savings measurement desks/mt5/mt5desk/netting.py:22 says routing is waiting on, without sending anything differently — and the measured saved spread is the evidence that would license routing and therefore MORE effective heat per unit of cost.
- **E2 Execution policy competition actually routed** — PARTIAL · gate: principal
  - gap: Nine policies and five child-order algorithms are priced and ranked, but exactly two order shapes reach the venue (pending stop for gold, market for family/scalp) and the choice is hard-coded, not argmax; staged, time-sliced, wait-for-liquidity and every limit variant exist only as unsent child plans. The competition is also not data-driven: the fill surface can never fit because its input ledger does not exist.
  - desks/mt5/mt5desk/execution_policy.py:43-44 `POLICIES = ("MARKET", "PASSIVE_LIMIT", "AGGRESSIVE_LIMIT", "PULLBACK", "STOP", "SPREAD_CONDITIONED", "EVENT_DELAY", "SPLIT", "SKIP")` — nine policies priced (MEASURED)
  - desks/mt5/mt5desk/execution_policy.py:134-144 `choose(c, surface)` returns `max(ps, key=lambda p: p.utility)` with `alternatives` — the argmax exists (MEASURED)
  - desks/mt5/mt5desk/execution_registry.py:274-276 `REGISTRY = {"market", "twap", "iceberg", "sniper", "pullback"}`; :280-311 `compete()` ranks by utility with SKIP at zero (MEASURED)
  - desks/mt5/mt5desk/execution_registry.py:15 `utility = p_fill x edge_kept x edge_r - cost_r` in R by the intent's stop distance (MEASURED)
  - clock: The competition runs inside every MT5-Gateway pass (desks/mt5/scripts/Install-QuantWindows.ps1:126-131) for the family/scalp lanes only; nothing schedules a router · artifact: `policy_advice` field on desks/mt5/data/order_intents.jsonl rows (desks/mt5/mt5desk/gateway.py:1705-1709, 1945-1949) — file ABSENT · consumer: libs/research/decision_dataset (candidate_actions), research/counterfactual_replay — both read the absent intent ledger
  - next: Route the family-market lane through `execution_registry.best()` only for the algorithms whose children are all `market` (market, twap, sniper) — i.e. add the send loop in desks/mt5/mt5desk/gateway.py run_family_sleeves behind an explicit one-file arm switch beside GENERIC_EXEC_ENABLED. PRINCIPAL-GATED (it is a money-path routing change).
- **E3 Netting layer: theatre or routing** — EXISTS-DARK
  - gap: Confirmed theatre: the netting book computes the one net order and logs 'NET WOULD SEND', and the execution registry's write path is reachable only from an armed filled market order that has never occurred, so both the theoretical-position ledger and the algo-outcome ledger are empty files that do not exist.
  - desks/mt5/mt5desk/gateway.py:1403-1406 `_net_routes` docstring: "the ONE order per symbol the venue would see if the sleeves were netted -- measured and logged as NET WOULD SEND, never placed" (MEASURED)
  - desks/mt5/mt5desk/gateway.py:1422-1424 `log(f"[netting] {symbol} NET WOULD SEND {r.get('side')} {r.get('lots')} …")` — a log line, no order_send anywhere in the function (MEASURED)
  - desks/mt5/mt5desk/netting.py:20-22 — "Routing itself is a money-path change and is left to a separate, box-verified step; this measures first"; :34-36 "the book never sends anything, and today the gateway still sends per sleeve" (MEASURED)
  - desks/mt5/mt5desk/gateway.py:1425-1432 — the ledger-vs-terminal disagreement is "reported, not corrected" (MEASURED)
  - clock: `_net_routes` runs on every MT5-Gateway pass (desks/mt5/mt5desk/gateway.py, called from main); `netting.savings_report` runs as the daily_cycle leg "execution_intelligence" (desks/mt5/research/daily_cycle.py:338) · artifact: desks/mt5/logs/gateway.log lines only; reports/NETTING.json, reports/NETTING_BOOK.json, data/theoretical_positions.jsonl, data/execution_algo_outcomes.jsonl all ABSENT · consumer: desks/mt5/research/execution_intelligence.py (imports netting + execution_registry) — reads artifacts that have never been written; nothing consumes the log line
  - next: Make the theoretical book durable independently of arming: `netting.TheoreticalBook` already persists to NETTING_LEDGER on `set_target`, so remove the shadow-only guard by ensuring `_book_target` is called on the BRACKET lane too (desks/mt5/mt5desk/gateway.py, the bracket loop in main) — this writes data/theoretical_positions.jsonl in shadow and gives netting.book_savings a real input without sending anything.
- **E4 Broker simulator from own fills** — PARTIAL · gate: box-paste
  - gap: The simulator is built from a SIMULATED tape replay (shadow_execution) rather than from own fills, and the own-fill corpus that would replace it has never accumulated a single row; the fitted surface also lacks four of the blueprint's inputs entirely — news proximity, latency, broker state and reject probability — and latency_ms is consumed by decision_dataset but written by nobody.
  - desks/mt5/mt5desk/shadow_execution.py:45 `OUT = BASE/"reports"/"execution_quality.json"`, :274-275 written every run (MEASURED)
  - desks/mt5/mt5desk/shadow_execution.py:11-17 — the chain it reconstructs: decision (bar close) → real bid/ask quote → simulated STOP at the intended level → fill at the far side of the spread → markouts at +1/+5/+15/+60 min (MEASURED)
  - desks/mt5/mt5desk/shadow_execution.py:264-283 — the report carries `decisions`, `filled`, `unfilled`, `rejection_rate`, and per `by_symbol_session` cell `slippage_R`, `spread_at_fill`, `markouts_R.m15` (MEASURED)
  - desks/mt5/mt5desk/shadow_execution.py:288-299 `is_stale(path, hours=STALE_HOURS)` — "Promotion gate: execution evidence must exist and be recent, or nothing may promote"; called by scripts/check_live_readiness.py:150 (MEASURED)
  - clock: execution_quality.json: daily_cycle STEPS "execution" (desks/mt5/research/daily_cycle.py:104-107, 469) inside hourly_cycle's daily() leg (desks/mt5/research/hourly_cycle.py:1019). fill_corpus/twin: desks/mt5/research/hourly_cycle.py:698-706 and :1187 `_costed("execution_twin", execution_twin)`. Tape: MT5-MoatRecorder (10 min), MT5-MoatSilver (hourly), MT5-MoatMiner (hourly) · artifact: desks/mt5/reports/execution_quality.json (ABSENT), data/fill_corpus.jsonl (ABSENT), data/execution_twin_cases.jsonl (ABSENT), reports/FILL_SURFACE.json (ABSENT); C:\moat\bronze tick tape (exists off-repo, evidenced by data/moat_coverage.json) · consumer: scripts/check_live_readiness.py:150 (is_stale promotion gate), desks/mt5/research/execution_resolver.py:51, scripts/build_gauntlet_survivors.py:42, scripts/check_job_manifest.py:64 (36h SLO), desks/mt5/research/execution_intelligence.py
  - next: Add `latency_ms` (wall clock around the `mt5.order_send` call) to the row written by desks/mt5/mt5desk/gateway.py:648-661 `_record_intent` — it is already read at libs/research/decision_dataset.py:588 and is the one blueprint feature that is unrecoverable after the fact.
- **E8 Edge monetization ratio (realized / predicted executable edge)** — EXISTS-DARK · gate: box-paste
  - gap: The ratio is defined exactly as the blueprint asks, is per-sleeve/session/symbol, is trended with a slope, is scheduled and has three named downstream consumers — and it has never produced a single number, because its only input is the fill corpus, which has never had a row. The three artifacts the blueprint names do not carry it: forward_reconcile is certificate bookkeeping, decay_live reports zero live sleeves, and trade_forensics is a dead crypto book.
  - libs/execution/alpha_capture.py:1-4 — `AlphaCapture = realised edge / predicted FRICTIONLESS edge`; :13-19 denominator discipline (`frictionless_edge_r` states its preference order, every row records which basis it used; non-positive predicted edge returns UNMEASURED, never a figure); :21-24 leakage decomposed into spread, slippage, commission with the RESIDUAL named; :26-35 adverse selection reported separately at every horizon with `transient_r` (MEASURED)
  - desks/mt5/research/execution_intelligence.py:11-24 — computes AlphaCapture "per sleeve, session and symbol", appends one point per daily pass to data/alpha_capture_history.jsonl, and reports the SLOPE in reports/ALPHA_CAPTURE.json ("A capture ratio measured once is a fact about last month; a capture ratio with a slope is a control loop") (MEASURED)
  - desks/mt5/research/execution_intelligence.py:26-31 — the input is data/fill_corpus.jsonl, written by the hourly twin; "An empty or absent corpus is reported as UNMEASURED with the reason … never as a capture ratio of zero" (MEASURED)
  - data/fill_corpus.jsonl, data/alpha_capture_history.jsonl and reports/ALPHA_CAPTURE.json are ALL ABSENT (MEASURED)
  - clock: daily_cycle._state_research_feedback leg "execution_intelligence" (desks/mt5/research/daily_cycle.py:338) and "allocator_attribution" (:342); the corpus builder is the hourly leg execution_twin (desks/mt5/research/hourly_cycle.py:1187) · artifact: desks/mt5/reports/ALPHA_CAPTURE.json + data/alpha_capture_history.jsonl — ABSENT and never in git history · consumer: libs/portfolio/aggression.py:71-82 takes `execution_quality` as an input and records the gap "no live fills measured on this host"; libs/doctrine/portfolio_law.py:216-233 and libs/portfolio/capital_competition.py:82,119 consume an execution-quality scalar
  - next: Give the ratio a denominator it can already reach: make desks/mt5/research/execution_intelligence.py fall back to the SIMULATED fills in desks/mt5/reports/execution_quality.json (slippage_R and spread_at_fill per symbol/session, already produced by the scheduled shadow_execution leg) when data/fill_corpus.jsonl is absent, labelled `basis: shadow_tape` so it can never be mistaken for a live capture ratio.

### Phase 7 — Recursive research improvement: agents compete for compute by downstream economic value; the machine redesigns itself

- **I4 Dead-architecture policy: automated census of dead/superseded/stale organs, and automatic deletion or disabling** — PARTIAL
  - gap: The census is real and running but the verdict is always UNMEASURED (70/71 modules) and nothing acts on it: no organ is ever disabled, no timer masked, no source dropped — retirement happens only when a human writes a `# RETIRED` header by hand.
  - FOUR INDEPENDENT CENSUSES RUN AND PUBLISH. (1) libs/ops/capability_graph.py:24-33 checks DEAD_PRODUCER, DEAD_CONSUMER, ADVISORY_ONLY, UNMEASURED_AUTHORITY, STALE_DECISION, UNDECLARED over 50 nodes / 99 artifacts -> desks/mt5/reports/CAPABILITY_STATUS.json (generated 2026-09-05T01:22:19), `findings: []`, `stage_counts {MISSING:0, CODED:0, WIRED:19, RUNNING:2, DECISION_AFFECTING:22, MEASURED:7}` (MEASURED)
  - (2) libs/ops/module_rent.py -> desks/mt5/reports/MODULE_RENT.json (2026-09-05T18:49): 71 modules, verdicts `{UNMEASURED: 70, NOT_BINDING: 1}`, `retire: {}`, `costs: []`, `earns: []`. Its own rule line: 'a module that reads COSTS with n >= 10 in 3 consecutive weekly windows is NAMED under `retire`. This report names; a person or the capability-graph check retires. No sacred modules' (MEASURED)
  - (3) scripts/check_job_manifest.py:59-101 declares 16 artifacts each with a named consumer and a max age, and alarms MISSING / STALE / FROZEN / IDLE / NO-CONSUMER. data/job_manifest.json (checked_at 2026-09-08T18:36:21) summary: `{MISSING: 13, STALE: 3}`, `retired: {}`; 13 rows carry `ever_produced: None` — declared and never produced on this host (MEASURED)
  - data/JOB_MANIFEST_ALARM.txt (2026-09-08T18:36:21, 18 lines) names each with its consumer, e.g. 'MISSING desks/mt5/reports/execution_quality.json: declared but NEVER produced -- an owed build. Consumer: promoter (promotion gate), dashboard' and 'STALE web/desk_state.json: 44.8h old, limit 0.5h' (MEASURED)
  - clock: quant-job-manifest.timer (*:07/30), quant-capratchet-auto.timer (*:0/12), quant-miner-conversion.timer (05:40 UTC), quant-desk-tasks.timer (*:9/15), quant-daily-max.timer (05:30) · artifact: desks/mt5/reports/CAPABILITY_STATUS.json, desks/mt5/reports/MODULE_RENT.json, data/job_manifest.json + data/JOB_MANIFEST_ALARM.txt, data/MINER_YIELD_ALARM.txt, desks/mt5/reports/BOX_TASKS.json · consumer: libs/ops/capability_graph.stages reads MODULE_RENT; scripts/check_reachability warns on a COSTS node; scripts/max_audit.py reads the miner scan; docs/PRINCIPAL_ACTION.md for escalation
  - next: Give libs/ops/module_rent.py's `retire` list an actuator: a check that masks the systemd timer of any named module (`systemctl --user mask`) after the third consecutive weekly COSTS verdict, writing the mask to data/ so it is reversible — the naming logic already exists, only the action is missing.
- **I10 Agent scoreboard: per LLM/miner/source AgentValue = realized utility / resources, with budgets reallocated by yield** — PARTIAL
  - gap: Yield is measured per miner and per source and budget is reallocated per research ARM, but the two are never joined and the compute denominator holds 4 rows — so AgentValue = utility/resources cannot be computed for any LLM, miner or source, and no budget has ever moved on a per-agent yield number.
  - THE NUMERATOR IS MEASURED PER MINER. scripts/check_miner_conversion.py:8-19 measures DISCOVERIES / NOVEL (deduplicated by economic exposure, not title) / TESTED / SURVIVORS / CONVERSION / ZERO-YIELD per miner, on quant-miner-conversion.timer (05:40 UTC, ops/crontab.manifest:3338) (MEASURED)
  - Its alarm fires: data/MINER_YIELD_ALARM.txt (2026-09-07T08:39:31) 'ZERO-YIELD: forexfactory, github, github_topics, reddit, seasonality, shipping produced 20+ rows and no survivor in 14d -- noise at cost (III.16)' (MEASURED)
  - PER-SOURCE CONVERSION IS PUBLISHED: desks/mt5/data/hypotheses/miner_candidates.json (compiled_at 2026-09-06T11:47:01) — `per_source` 86 sources, `rows_accounted: 178864`, `executable_candidates: 632`, `deepening_tasks: 25042`. In the top slice only `cot` produced candidates (rows 22 → candidates 7); mql5_signals 1,190 rows → 0 candidates, youtube 673 → 0, quantconnect 294 → 0, reddit 239 → 0 (MEASURED)
  - THE DENOMINATOR IS ESSENTIALLY EMPTY. libs/ops/compute_ledger.py exists exactly to be the denominator (:12-16 'it CANNOT be fixed by writing the ranking formula, because the formula divides by hours and this desk has never recorded an hour') and desks/mt5/data/compute_ledger.jsonl holds FOUR rows (MEASURED)
  - clock: quant-miner-conversion.timer (05:40 UTC), quant-dig-roi.timer (*:09,39), quant-seat-launch-yield.timer (10:00 UTC), quant-cert-yield.timer (*:14,44); research_budget written by the daily cycle bandit · artifact: data/MINER_YIELD_ALARM.txt, desks/mt5/data/hypotheses/miner_candidates.json, desks/mt5/data/research_budget.json, desks/mt5/reports/RESEARCH_PRODUCTIVITY.json, desks/mt5/data/compute_ledger.jsonl (4 rows) · consumer: scripts/max_audit.py, libs/research/bandit.py -> daily_cycle + deepening_worker; MINER_YIELD_ALARM -> the principal page
  - next: Feed libs/ops/compute_ledger.rank() the numerator it asks for by passing `per_source` survivor counts from desks/mt5/data/hypotheses/miner_candidates.json as `value_by_run` — both files exist and the join is one call in scripts/check_miner_conversion.py.
- **I11 Tournaments between agents/models with compute proportional to survivor yield; AI-vs-no-LLM baseline; human-vs-AI benchmark** — EXISTS-DARK
  - gap: The league runs hourly, publishes today's timestamp and reports zero entrants because nothing registers a model against a shared window; there is no arm that runs the pipeline with LLMs disabled, so the incremental value of the LLM layer over the symbolic/statistical one has never been estimated.
  - THE TOURNAMENT FRAME EXISTS AND HAS ZERO ENTRANTS. desks/mt5/reports/MODEL_ZOO.json and desks/mt5/reports/CHALLENGE_LEAGUE.json (both measured_at 2026-09-08T17:12:39 — fresh today): `entrants: 0`, `buckets: {}`, `cost_per_hour: 0.02`, `cost_basis: "declared default 0.02/h -- the compute ledger holds 0 usable run(s), too few to price an hour from"` (MEASURED)
  - The ranking rule is written and correct: 'dElog after compute and complexity rent. Accuracy is not a ranking: a model 2% more accurate at four times the compute has not earned its place'; fairness clause lists any unequal pairing as INCOMPARABLE rather than ranking it (desks/mt5/reports/MODEL_ZOO.json) (MEASURED)
  - Producer: desks/mt5/research/model_zoo.py:52 REPORT, reading desks/mt5/data/model_skill_track.jsonl (:207); scheduled as hourly_cycle leg `model_league` (desks/mt5/research/hourly_cycle.py:1191) (MEASURED)
  - PAIRWISE CHALLENGER EXISTS FOR SIGNALS, NOT AGENTS: libs/signal_engine/champion_challenger.py:3 'Compares a live champion variant against a challenger and recommends promotion only if…' — variants of a strategy, never two generators (MEASURED)
  - clock: hourly_cycle leg `model_league` (desks/mt5/research/hourly_cycle.py:1191); quant-deepseek.timer (*:20 UTC) · artifact: desks/mt5/reports/MODEL_ZOO.json, desks/mt5/reports/CHALLENGE_LEAGUE.json, desks/mt5/data/model_skill_track.jsonl · consumer: desks/mt5/research/issue_board.py:74 tracks MODEL_ZOO.json freshness (3600s) — a staleness reader, not a decision consumer
  - next: Enrol the two generators that already exist as league entrants: have desks/mt5/research/model_zoo.py read the `external_screen` (non-LLM) and LLM-seat survivor counts from desks/mt5/data/hypotheses/miner_candidates.json `per_source` as its first two entrants — that alone yields the AI-vs-no-LLM delta the blueprint asks for.
- **I12 Meta-research agent (weekly) generating A/B-tested improvements (Research Controller A vs B)** — PARTIAL
  - gap: The weekly review runs and its findings reach a tracked ledger, but every improvement it proposes is adopted as a single global change — there is one Research Controller and no mechanism to run a variant against it, so no proposed improvement has ever been measured against the arm it replaced.
  - THE DIRECTIVE AND ITS EXECUTOR EXIST. docs/research/META_RESEARCH_DIRECTIVE.md:1-16 — 'Status: PERMANENT. Principal directive, 2026-07-28 … its computable parts are executed by `scripts/meta_research_review.py`. Mechanical beats memorised: a check that runs cannot be skipped' (MEASURED)
  - Sections cover exactly the blueprint's questions: §1 Research Capital Allocation (ranked ERV table), §2 Bottleneck Analysis ('the SINGLE greatest limiter'), §3 Information Frontier Expansion (META_RESEARCH_DIRECTIVE.md:33-50) (MEASURED)
  - IT IS SCHEDULED, AND IT RUNS: ops/crontab.manifest:1183 `47 3 * * *`; the cron plane is dead (scripts/run_manifest_dispatch.py:3-8) but the row IS in the dispatcher allowlist at scripts/run_manifest_dispatch.py:195 — '"scripts/meta_research_review.py": "§12 meta-research review; live rc=0"' — so it fires every day via quant-manifest-dispatch.timer. It is also called from scripts/run_cadence.py:571-573 which reads data/meta_research_review.json (MEASURED)
  - 'WHICH GATES REJECT NOTHING' IS ANSWERED BY A SEPARATE FENCE: scripts/check_gate_reachability.py, allowlisted at scripts/run_manifest_dispatch.py with the live result quoted — 'live rc=2 -- 2 dead branch(es), 8 unsatisfiable, 2 zero-bit acceptors' (MEASURED)
  - clock: quant-manifest-dispatch.timer firing the allowlisted `scripts/meta_research_review.py` row (daily 03:47); also invoked inside scripts/run_cadence.py:571 · artifact: data/meta_research_review.json, desks/mt5/reports/MODEL_SELF_IMPROVEMENT.json, docs/research/recommendation_ledger.json · consumer: scripts/run_cadence.py:573, docs/research/recommendation_ledger.json (777 rows), scripts/check_conversion.py
  - next: Add a `controller_variant` field to the budget written by libs/research/bandit.py (desks/mt5/data/research_budget.json) and have the deepening worker split its queue by it, so two allocation policies can run concurrently and be compared on survivors per compute-hour.
- **I13 Model-of-models: posterior reliability per model/agent conditional on problem type (macro vs scalp)** — PARTIAL
  - gap: Reliability is tracked for three internal PREDICTORS (one of which has negative skill at n=537) and for research arms, but never per LLM/agent, and the one bucketing dimension that exists (`buckets`) is horizon rather than problem type and is empty — so nothing conditions on macro vs scalp.
  - THE PREDICTOR SCOREBOARD EXISTS AND RUNS. desks/mt5/reports/MODEL_SELF_IMPROVEMENT.json (2026-09-05T22:30:18, status BREACH) tracks 3 named predictors with owner, baseline, kind and n: research_forecast (NO_SKILL, skill -0.176985, n=537, baseline 'the base rate of resolved claims coming true', owner libs/self_improvement/forecast_calibration.py); gauntlet_survival (UNMEASURED, n=0, baseline 'persistence', owner desks/mt5/research/shadow_admission.py); allocator_expected_gain (UNMEASURED, n=0, owner desks/mt5/research/pf_allocator.py). `measured: 1, unmeasured: 2, min_n: 20, min_skill_gain: 0.02` (MEASURED)
  - Brier-based reliability machinery exists: libs/self_improvement/forecast_calibration.py:360-373 returns `{brier, reliability: 1-brier, bias, hit_rate_posterior}` with 'reliability=1-Brier; bias>0 means forecasts were too high' (MEASURED)
  - PER-ARM POSTERIORS EXIST BUT ARE OVER RESEARCH KINDS, NOT MODELS: libs/research/bandit.py:24-26 — 'P(valid survivor) is a Beta posterior per arm from the hypothesis graph's fates, shrunk toward the pooled rate with PSEUDO pseudo-counts so an arm that has never been tried is optimistic, never certain' (MEASURED)
  - CONDITIONING BY PROBLEM TYPE IS DECLARED BUT EMPTY: desks/mt5/reports/MODEL_ZOO.json has a `buckets: {}` field and a fairness rule 'ranked only against models that faced the same window, the same HORIZON BUCKET, the same cost model and a comparable sample' — the bucket dimension is horizon, not macro-vs-scalp, and it holds zero buckets against `entrants: 0` (MEASURED)
  - clock: hourly_cycle legs `model_skill` (desks/mt5/research/hourly_cycle.py:1189) and `model_league` (:1191) · artifact: desks/mt5/reports/MODEL_SELF_IMPROVEMENT.json, desks/mt5/data/model_skill_track.jsonl, desks/mt5/reports/MODEL_ZOO.json · consumer: desks/mt5/research/issue_board.py:74 (freshness only); no allocator or router reads a per-model reliability
  - next: Register the LLM seats as predictors in desks/mt5/reports/MODEL_SELF_IMPROVEMENT.json's list with `tags: ["macro"]`/`["scalp"]` — the file already carries a `tags` field per predictor and a MIN_N gate, so per-lane posteriors follow with no new machinery.
- **I14 Research-marketplace economics: budgets per agent / data source / search ground flowing to certified marginal Elog** — PARTIAL
  - gap: The marketplace prices research KINDS with a declared cost table and a placeholder E[dElogW]=1.0 for every arm that has certified nothing; it does not price agents, data sources or search grounds at all, and the two ledgers that would supply the real numerator and denominator (MODULE_RENT, compute_ledger) read 0 EARNS and 4 rows.
  - A REAL BANDIT MARKETPLACE EXISTS OVER RESEARCH ARMS. libs/research/bandit.py:22 states the objective exactly as the blueprint does: `score_j = E[dElogW_j] x P(valid survivor_j) / (compute + data + latency + multiplicity)_j`, with Thompson sampling plus a uniform EXPLORE floor 'the machine may not become trapped by its own history' (:31-33) (MEASURED)
  - Its artifact is live: desks/mt5/data/research_budget.json (generated_utc 2026-09-07T18:40:16) with 11 shares summing to 1.0 — alt_data_hypothesis 0.1807, cross_asset_signal 0.1791, new_mechanism 0.167, exit_improvement 0.0986, execution_improvement 0.0806, model_architecture 0.0681, failure_derived 0.0573, conditional_state_edge 0.0544, mutate_survivor 0.0488, combine_survivors 0.0468, external_screen 0.0187 (MEASURED)
  - It has real consumers: libs/ops/capability_graph.py:785 — 'daily_cycle proposer budgets and deepening_worker'; libs/research/bandit.py:33-34 'the deepening worker weights its VOI order by it, the daily cycle scales each proposer's time budget by it' (MEASURED)
  - THE COST SIDE IS DECLARED, NOT MEASURED: libs/research/bandit.py:29 — 'Costs are declared units, not measured seconds: a declared table is auditable, a timer is not.' Meanwhile the timer ledger exists and holds 4 rows (desks/mt5/data/compute_ledger.jsonl) (MEASURED)
  - clock: libs/research/bandit.py runs inside desks/mt5/research/daily_cycle.py (box task MT5-CycleNoon / MT5-CycleMidnight, desks/mt5/scripts/Install-QuantWindows.ps1:259,271) · artifact: desks/mt5/data/research_budget.json · consumer: desks/mt5/research/daily_cycle.py proposer budgets; desks/mt5/research/deepening_worker.py VOI ordering
  - next: Replace libs/research/bandit.py's declared cost units with the measured `cpu_s`/`wall_s` already in desks/mt5/data/compute_ledger.jsonl via libs.ops.compute_ledger.cost_by_run() — one import in libs/research/bandit.py turns an auditable table into a measured denominator.
- **I15 Scaling laws: survivor production vs CPU-hours / tokens / data volume; data scaling; research scaling** — EXISTS-DARK
  - gap: Every piece of the scaling law is built — cost recorder, per-run aggregation, value-per-hour ranking, stage conversion ratios — and the ledger has four rows from a single hour, so no curve of survivors against CPU-hours, tokens or data volume can be drawn.
  - THE DENOMINATOR MODULE EXISTS AND IS EXPLICITLY BUILT FOR THIS. libs/ops/compute_ledger.py:1-4 quotes the principal — 'ComputeValue_j = P(success_j) * E[dElog_j] * InformationGain_j / GPUhours_j' — and :29-32 names the unit honestly: 'NOT GPU-HOURS, because this desk has no GPU. The unit is the one it actually spends: wall-clock seconds' (MEASURED)
  - IT HAS 4 ROWS. desks/mt5/data/compute_ledger.jsonl in full: search (wall 720.115s, cpu 0.14s), sweep (720.113s, 0.17s), compile_candidates (16.197s, 13.58s), deepen (1056.659s, 214.85s, outcome 'BrokenPipeError: [Errno 32] Broken pipe') — all four from 2026-09-06 11:35-12:04, against 59 legs that call `_costed` (MEASURED)
  - The consequence is stated by its own consumer: desks/mt5/reports/MODEL_ZOO.json `cost_basis: "declared default 0.02/h -- the compute ledger holds 0 usable run(s), too few to price an hour from"` (measured 2026-09-08T17:12:39 — today) (MEASURED)
  - compute_ledger.cost_by_run() (:145-167) already computes hours + failure_rate per named run and rank() (:170-206) computes value_per_hour — the scaling curve is one join away and refuses to fabricate: 'nothing has been costed … which is the honest state, not a zero' (:202-205) (MEASURED)
  - clock: compute_ledger writes from hourly_cycle `_costed` (desks/mt5/research/hourly_cycle.py:557-607, 59 call sites); RESEARCH_PRODUCTIVITY from hourly_cycle leg (desks/mt5/research/research_productivity.py); MODEL_ZOO from leg `model_league` · artifact: desks/mt5/data/compute_ledger.jsonl (4 rows), desks/mt5/reports/RESEARCH_PRODUCTIVITY.json, desks/mt5/reports/MODEL_ZOO.json · consumer: desks/mt5/research/model_zoo.py (reports it cannot price an hour); nothing else reads the ledger
  - next: Find out why 55 of 59 `_costed` legs left no row: the wrapper at desks/mt5/research/hourly_cycle.py:557-607 catches BaseException and `_append` swallows OSError (libs/ops/compute_ledger.py:78-79) — add the leg's exception to stderr in `_append` so a silently failing ledger write becomes visible in the hourly log.
  - landed: compute_ledger._append now reports a failed write to stderr (this commit)
- **I16 Institutional external review: independent code/statistical review artifacts, tracked to closure** — EXISTS-LIT
  - gap: Closure tracking is genuine, but 219 of 777 rows are open with a 10-day median age (RESEARCH_PRODUCTIVITY.json recommendations block) and nothing distinguishes an externally-sourced finding from a self-sourced one in the fence — `source: principal` (48) and `source: panel` (5) get the same SLA as `source: cycle` (422).
  - docs/research/recommendation_ledger.json holds 777 recommendations with per-row `id, source, summary, roi_bps, raised, status, reason`. Status distribution: implemented 333, open 219, rejected 108, scheduled 98, done 18, screened 1 (MEASURED)
  - Sources are genuinely mixed and include external and principal input: cycle 422, deep_sweep 150, principal 48, max_audit 38, capability_hunt_s5 25, cro 12, owed-work 7, proactive_battery 6, panel 5, claude 5, self 5 (MEASURED)
  - Rejections carry re-verified reasons, not dismissals — R0001's reason re-checks each claim with file:line: 'CPCV at libs/autodiscovery/validation.py:624, campaign_fdr via orchestrator.py:328-330, LockedHoldout constructed orchestrator.py:290 and opened at :354-359 … Only hansen_spa is dead' (MEASURED)
  - CLOSURE IS FENCED HOURLY: scripts/check_conversion.py:1-20 reads this exact ledger ('the de-facto winning queue -- the sweep's M10 finding is that split stores recreate the defect, so this fence reads ONE store') and separates backlog_open / backlog_scheduled / owed / past_due; scheduled at ops/crontab.manifest:260 (`33 * * * *`) and allowlisted at scripts/run_manifest_dispatch.py:52 (MEASURED)
  - clock: quant-manifest-dispatch.timer, quant-deepseek.timer · artifact: docs/research/recommendation_ledger.json (777 rows), data/cro_ai_logs/conversion_fence.log, docs/audit_shards/shard_*.md · consumer: scripts/check_conversion.py (hourly fence), scripts/recommendations.py, libs/research/recommendation_forecast.py, docs/PRINCIPAL_ACTION.md escalation
  - next: Add a per-source breakout to scripts/check_conversion.py's output so external rows (principal, panel, deep_sweep, cro) get their own owed/past_due counts — the `source` field is already on every row of docs/research/recommendation_ledger.json.
- **A5 Research counterfactuals — what process would have found this survivor earlier** — PARTIAL
  - gap: Operational counterfactuals are lit and rich; the research counterfactual the blueprint asks for — replay the search process against a known survivor and price the delay — does not exist, so hunt_frontier yield and dig ROI feed budgets only through raw yield, never through a counterfactual 'this was reachable N months ago'.
  - libs/research/counterfactual_world.py:1-24 — prices every arm of one DECISION (entered/skipped/0.5x/1.0x/1.5x/market/limit/delayed/fixed TP/trail/hold/partial) into Veto/Sizing/Execution/Exit/Missed-Trade alpha (MEASURED)
  - desks/mt5/research/counterfactual_replay.py:1-24 — the clock and watermark for that, appending data/decision_dataset.jsonl and reports/COUNTERFACTUAL_WORLD.json; run from desks/mt5/research/daily_cycle.py:337 (_state_research_feedback) and desks/mt5/research/hourly_cycle.py:1276-1277 (MEASURED)
  - desks/mt5/research/action_counterfactuals.py:1-22 — counterfactuals on trades TAKEN (hold horizons, opposite side, sizing multipliers) in E[log] terms; desks/mt5/research/daily_cycle.py:323 (MEASURED)
  - scripts/check_dig_roi.py:1-24 — a research-side ROI meter: 'TESTABLE candidates added to the docket', breadth, certificates minted, forward clocks started, cost in launches, 'Yield per launch is the ratio the principal is asking for'. Scheduled ops/quant-dig-roi.service:13 (MEASURED)
  - clock: desks/mt5/research/hourly_cycle.py:1276 counterfactual_world leg + desks/mt5/research/daily_cycle.py:323,337 legs (trading counterfactuals); ops/quant-dig-roi.timer for dig ROI. NONE for research counterfactuals · artifact: desks/mt5/reports/COUNTERFACTUAL_WORLD.json, data/decision_dataset.jsonl; data/cro_ai_logs/dig_roi.log · consumer: desks/mt5/research/missed_growth.py and the veto rails read COUNTERFACTUAL_WORLD's VETO_ALPHA (desks/mt5/research/daily_cycle.py:334-337 comment); no consumer for a research counterfactual because none is produced
  - next: Add a `--survivor <cert_key>` mode to scripts/check_dig_roi.py that walks libs/research/hypothesis_graph.lineage() back from the certified node and reports the earliest source and date at which its region was first reachable.
- **A7 Exploration budget split (exploit/adjacent/cold) enforced by a controller** — PARTIAL
  - gap: A binary explore/exploit split (EXPLORE=0.20) is enforced across 11 arms, but the three-way exploit/adjacent/cold split is nowhere, and the desk's own declared allocation policy (ops/research_allocation_policy.json, with exploration and falsification FLOORS) is read only by an unscheduled script.
  - libs/research/bandit.py:53-57 ARMS (11 research directions); libs/research/bandit.py:149-150 `PSEUDO = 20.0`, `EXPLORE = 0.20`; libs/research/bandit.py:197-238 `allocate(...)` = Thompson shares plus a uniform EXPLORE fraction spread on top (MEASURED)
  - libs/research/bandit.py:332-360 `run()` writes desks/mt5/data/research_budget.json and desks/mt5/reports/RESEARCH_BANDIT.json. MEASURED: research_budget.json exists (2026-09-07 18:40) with 11 non-uniform shares, e.g. alt_data_hypothesis 0.1807, cross_asset_signal 0.1791, new_mechanism 0.167, external_screen 0.0187
  - libs/research/bandit.py:363-378 `shares()` / `arm_weight()`; enforced by desks/mt5/research/deepening_worker.py:470 (`direction = arm_weight(src, kind)` inside voi_order), desks/mt5/research/daily_cycle.py:256-270 (each proposer's budget_s scaled by arm_weight and clipped to [300s, 3600s]), desks/mt5/research/hourly_discovery.py:128 (MEASURED)
  - libs/research/bandit.py:302-329 `breadth_credit()` — the marginal dk_eff an arm buys the CURRENT book, so a duplicate scores below a weaker but independent mechanism (MEASURED)
  - clock: desks/mt5/research/daily_cycle.py:307-316 `_research_bandit` (STEPS entry at desks/mt5/research/daily_cycle.py:463) -> research_bandit.run(); check_exploration at ops/crontab.manifest:588 · artifact: desks/mt5/data/research_budget.json + desks/mt5/reports/RESEARCH_BANDIT.json; data/exploration_status.json · consumer: desks/mt5/research/deepening_worker.py:470, desks/mt5/research/daily_cycle.py:256-270, desks/mt5/research/hourly_discovery.py:128 (all call arm_weight)
  - next: Add an `adjacent` vs `cold` label to libs/research/bandit.SOURCE_ARM (libs/research/bandit.py:59-80) — adjacent = mutate_survivor/combine_survivors/conditional_state_edge, cold = new_mechanism/alt_data_hypothesis — and assert the three group shares against ops/research_allocation_policy.json floors inside bandit.allocate().
- **A14 Exploration regret — does the controller repeatedly miss productive regions** — PARTIAL
  - gap: The controller tunes explore/exploit from measured survivor rates, but no counterfactual regret is ever computed — nothing measures whether the arm the bandit starved would have paid — and the module that defines RESEARCH_REGRET (libs/research/frontier.py:351) has only test callers.
  - libs/research/bandit.py:164-196 `evidence()` — per-arm Beta posterior over CERTIFIED/FAILED from the hypothesis graph, shrunk to the pooled rate with PSEUDO=20 pseudo-counts so an untried arm is optimistic; libs/research/bandit.py:197-238 `allocate()` Thompson-samples P(best arm) and adds EXPLORE=0.20 uniform (MEASURED)
  - libs/research/bandit.py:240-285 `_research_pnl_worth()` / `_marginal_by_arm()` — E[dElogW] per arm from the allocator's realised marginal growth, 1.0 where an arm has certified nothing (MEASURED)
  - MEASURED: nowhere in libs/research/bandit.py is a regret computed. A grep of libs/research/bandit.py for 'regret' returns zero hits; the module reports shares, arms, breadth_credit and a rule string only (libs/research/bandit.py:341-351)
  - libs/research/frontier.py:88-90 REGRET_CATEGORIES = ('RESEARCH_REGRET','CAPITAL_REGRET','EXECUTION_REGRET','LATENCY_REGRET','INFRASTRUCTURE_REGRET','MODEL_SELECTION_REGRET','IDLE_RESOURCE_REGRET'); libs/research/frontier.py:351-375 `frontier_regret(best_known_surplus, selected_surplus, by_category)` measured 'against what was KNOWN, not against hindsight' (MEASURED)
  - clock: desks/mt5/research/daily_cycle.py:463 ('research_bandit' STEP) -> research_bandit.run() -> libs.research.bandit.run(). NONE for frontier_regret · artifact: desks/mt5/reports/RESEARCH_BANDIT.json (arms, shares, breadth_credit) and desks/mt5/data/research_budget.json; no regret artifact · consumer: desks/mt5/research/deepening_worker.py:470, desks/mt5/research/daily_cycle.py:256, desks/mt5/research/hourly_discovery.py:128 read the shares; nothing reads a regret
  - next: Call libs/research/frontier.frontier_regret() inside libs/research/bandit.run() with best_known_surplus = max arm score and selected_surplus = the realised weighted score, and add the result to the doc written to desks/mt5/reports/RESEARCH_BANDIT.json.

