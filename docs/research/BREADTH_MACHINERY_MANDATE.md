# Canonical standing objective: breadth machinery

Principal order, 2026-09-14. This supersedes the earlier breadth **propose-only** brief.
All controllers and research organs use the same implementation, queues and evidence. Midnight
must PROPOSE AND IMPLEMENT MACHINERY increasing economically useful independence, not merely
emit cards. Deliver code, regression tests, existing scheduler wiring and consumer artifacts.
Keep operational repair and origin-first delivery duties; do not replace Claude's current agenda.

## Baseline to remeasure, not permanent constants

The principal supplied this trading-box snapshot dated 2026-09-14: 65 live sleeves, n_eff 5.6,
mean absolute Pearson correlation 0.163; 58 certificates all H1/asia, one certified chart/session
axis out of 76; 28 symbols (27 FX/exotic, one commodity), three certificate families; no forward
clock in UTC hours 17–23; 47/47 recent live deals XAUUSD. Seventeen new certificates added no axis.
Verify these claims against current authoritative artifacts before using them as measurements.
The 1/rho value is a model-dependent approximation, not an absolute attainable-independence ceiling.
Neither different labels nor adding symbols proves return independence; uncertainty and overlap
matter. n_eff and axes certified are diagnostics subordinate to robust net E[log W], not new
metrics to game. Correlated additions can still have marginal value: measure it rather than
assigning them zero by fiat.

Reported unused inventory: 58 uncertified symbols across indices (16), Fusion crypto CFDs (14),
commodities (11), soft commodities (11), energy (3), bonds (3); native H1 files reportedly present.
Seven charts M1/M5/M15/M30/H1/H4/D1, with 248 symbols reportedly holding M5/M15/M30/H4, and 32
registered price-only families. Refresh registry, chart/session coverage and actual bar readiness.
These counts and examples are a dated starting point, never a hardcoded search boundary.

## Standing implementation work, ordered by measured marginal value

1. **Axis-aware research allocation.** Extend the existing external_gauntlet.allocate_by_yield
   and exploration floor to asset class x chart x session, with family and mechanism coverage.
   Keep measured zero yield distinct from UNMEASURED; never use absence of a certificate as a
   measured yield denominator. Preserve exploitation selections; use resumable execution budgets
   and actual headroom for additive exploration. Verify selected cells really reach tests.
2. **Return-correlation admission research.** Reuse existing return ledgers and independence/
   portfolio machinery to compare frozen candidate forward returns with the live book on aligned,
   adequately overlapping observations. Start ADVISORY ONLY: no veto, size change or capital
   permission. Report missing data, sample size and uncertainty. The supplied snapshot had only
   19 daily marks against the existing Monte Carlo minimum of 60: do not shorten the requirement
   or zero-fill missing returns. Measure marginal contribution, not parameter-hash novelty alone.
3. **Cross-asset generators.** Extend existing registered generators and the canonical intake
   to maintain testable cells on underexplored executable non-stock classes and charts/sessions.
   Examples include UST10Y, UST05Y, UKGILT, US500, NAS100, GER40, SUGAR, CORN, XAGUSD,
   XTIUSD, XBRUSD and XNGUSD, subject to current Fusion registry and usable inputs. No new
   parallel registry, bare ticker lists, or repeated H1/asia FX proposals as breadth delivery.
4. **Point-in-time data genealogy.** Prove the existing PIT lane covers each consumer; repair
   gaps in as-of availability, revisions, vintage lineage and immutable snapshots. Retrieval
   today does not establish historical availability. Fetch lawful missing inputs before deferring;
   preserve genuinely unavailable history as a blocker or prospective collection requirement.
5. **Forced-flow models.** Implement falsifiable public mechanism classes such as index
   rebalances, month/quarter-end duration extension and expiry mechanics through existing
   families. Validate dated public rules, point-in-time eligibility and instrument exposure;
   a cash index or futures mechanism does not automatically transfer profitably to a Fusion CFD.
   No confidential-firm implementation or unsupported institutional endorsement is evidence.

Audit known failure classes first: empty or nested parameter envelopes, dropped mechanism fields,
unmodelled allocator axes, Windows path separators and unreadable ratchets. Reproduce and test
remaining failures; do not rebuild fixes Claude already landed. A silent empty result is not proof
of no edge. Reconcile source -> exact rules -> cells -> tests -> certificates -> forward by identity.

## Binding delivery and authority

Additive research only: never remove, shrink, cap or gate existing live deployments by fiat.
Build machinery, mint no certificate manually and allocate no capital. Every candidate faces the
existing ten gates: economic_prior, in_sample_screen, deflated_sharpe, pbo, cpcv, walk_forward,
reality_check_spa, stress_costs, lockbox, expected_value. Preserve the sealed 597-trial campaign
policy where applicable and its trial ledger; no resetting, hiding new searches or rewriting
gate constants under a breadth mandate. Any policy discrepancy is explicit, not silently resolved.
Single-name equities stay in the separate news/earnings event lane, excluded from statistical
hypothesis generation. Crypto-exchange-native hunting is excluded; Fusion crypto CFDs are allowed.

Cards are by-products: use registered family, named executable symbols, explicit chart/session,
exact rules/parameters, mechanism_status=NAMED, economic mechanism_note (counterparty, constraint,
persistence), public_source when available, provenance and a preregistered falsifier. Do not label
a statistical pattern NAMED merely to pass gate one. Retain costs, PIT, full search accounting,
negative controls and untouched forward clocks. Missing observations remain UNMEASURED.

Every delivery records axis opened, dated baseline and resulting measured count, scheduled clock,
artifact AND consuming job, test results, falsifier and what it adds. Selection is not testing;
testing is not certification; a certificate is not proven independence. Ship through GitHub origin
and approved adoption per ops/midnight_delivery_contract.txt. Verify code content and consumers,
not equal repository HEADs (hosts legitimately commit different runtime evidence). Resume until
runtime-verified or concretely blocked. Never claim a prompt change completed these five builds.
