# GAP REGISTER — live ranked list of known inefficiencies & missing capabilities

_MERGE 2026-08-04 (two account lines united; nothing dropped): the governance line's newest
re-rank is 2026-08-01T17:35Z (directly below); the execution line's 2026-08-02T13:20Z re-rank —
the newest overall, and the owner of the current RANK list — sits below the principal-override
paragraph. That line's rows 81-87 are recorded here as 96-102; see the renumber notes at the
foot of the register._

_Re-ranked 2026-08-18T21:20Z (owed-work batch3, repair-mode)._ **FULL DECISION PASS over every
open row past the 7-day escalation bar** (gap-register-rerank-stale: 67 rows owed
implement/defer/retire). Two re-pricing facts govern most of it: **Constitution 224 (6b8b61a9,
08-17) retired the crypto tape/venue stack and moved the desk to MT5** — roughly a third of open
rows die on that one event; and **the fork is the live line** (master..HEAD 1083 / HEAD..master 0).
Every row was checked against the tree, not the memory of it; key by id+title (17 duplicate ids).

**CLOSED as already-done (status flip, evidence named):** #3 pager (alert_delivery.jsonl ok:true
08-18), #5 breadth clocks (matured; first verdict 08-07), #13 offsite backup (origin push live),
#17 external heartbeat (hc channel rows today), #32 topup (_topup_plan executor:465), #33 pager
unicode, #37 orphan-cover caps (executor:85-93), #40 ensure_recorder (rewritten AND subject
retired), #42 churn (_MIN_HOLD_H=24 + hysteresis), #48 acquisition ROI (CME dropped, reweighted),
#51 pins (22/22 == VPS; CI on the box), #84=#99 funding-502 (2d0afa46, pre-dated the rows),
#87weld (per-candidate gates at validate():533-534; 08-17 sweep = 3 Stage-A survivors), #88forked
(branch IS the live line), #89 closed-on-arrival, #91net (sweep+studies ran on-box 08-17),
#92sweep (full sweep executed; residual _EFFECTIVE_N conservative), #92weld (R0044/R0041/R0017 all
implemented), #93 roster (roster_capabilities.json 08-17), #94/#95/#96near/#97 (run_research_review
is the wired consumer; near_survivor consumed twice), #99video (log rows since 08-13 + purchase
verdict), #101ladder (run_live_ladder writes alpha_state), #102brain (brain_hunter ran s2/s3),
#111 (desk_host.py; slot_registry:453 'GAP 111 closed'), #70 pdf extractor (landed 2026-08-18,
tests green), #74/#75 deep_sweep governance (1d64e79c), #29 residual (75 decisions scored ->
122/234, 0 due; memory index refreshed this cycle).

**RETIRED with reason (17 of these on Constitution 224):** #1 (carry forward-record; re-open
MT5-scoped), #6 (factory day-30 superseded by rebuilt stack + 08-17 sweep), #9 (kimi watch ->
scorecard governance), #10 (venue-equity tile: feed is the deliverable), #14 (leverage-optimizer
quarantine permanent on retired venue; MT5 sizing is stop-aware), #18 (moat recorder retired),
#19 (divergence breaker: scope-invalid calibration + venue retired), #22 (negative-space explorer
-> deep_sweep instruction + blind-rediscovery), #23 (semantic clustering -> mt5desk/canonical.py
participation-ratio N), #24 (decay lab -> MT5 regime monitor + champion/retire), #26 (video
purchase: log's own verdict DO-NOT-BUY), #30 (recorder schema-contract; re-open under mt5desk.tape
if wanted), #34 (deadman leg/cash residual dies with venue; Tier-3 proposal recorded unbuilt),
#36 (duty-cost logging -> R0088 09-03), #44 (8h challenger verdict moot-by-retirement), #59 (VIP
tiers moot; MT5 cost work active a21a3bb9), #60 (ADL is Binance-futures; re-disposition R0304-07
likewise), #62/#63 (CN builds funded-by-other-means; CNKI/Xueqiu connectors not funded on measured
yield), #71 (gate-optimality substantially delivered; open fresh row for the McLean-Pontiff
haircut if literature candidates return), #76 (BIS carry paper: port Jan-2024 boundary to family
notes; no resize-up will occur), #78 (Kaiko/LMAX recorder builds forbidden by retirement), #81=#96
(Hetzner volume: growth stopped, ~13GB freed, disk 38%), #85edge_gate (dead risk code ->
graveyard; ThresholdBook lives in capacity_policy), #87=#102 provenance trio (unmeasurable-by-
no-trades; MT5 deal ledger unblocks the successor), #88wallet (on-chain gets no agent hours;
graveyard venue-retired with re-entry condition), #91carry (REARM A/B/C dissolves; contaminated
window stays graveyarded; deadman untouched), #107 (replacement latency -> MT5 shadow-forward/
auto-promote/champion bench), #109 (breadth-vs-depth executed on MT5).

**DEFERRED with date/trigger:** #2 -> 2026-08-23 (OpenRouter 402 is the named blocker; §7 fuzz +
pre-mortem are panel work), #4 -> 08-30 (rides R0084; if R0084 re-scopes MT5, fold in), #11 ->
first MT5 live scale-up (Irish jurisdiction known), #12 + #108 -> 2nd promoted MT5 sleeve, #20+#21
-> 08-31 (one build, 402-blocked), #28 -> 09-03 (rides R0088), #55 -> 08-31 (operator handover
note), #61 -> 08-20 (rides R0092/R0126), #64 -> 2026-11-15 (row's own pre-committed falsification
date; fold query family into next prospector prompt edit), #67 -> principal PAGE (Upbit 2x lapsed
+ CM EXCLUDE ruling; bundle with #80 as ONE §13 governance session), #69 -> 08-19 (NAVER key; on
lapse, kill the card 'operator step refused' per the row), #79 -> 08-27 (L1.46 residual only),
#80 -> principal PAGE (bundled), #85=#100 -> 08-23 (len(history)>=K audit; capability-hunt shaped),
#86=#101 -> 08-30 (early-return sweep), #86stacks -> 08-25 (declare autodiscovery.validation
canonical, retire gauntlet.py to library, one-line principal ack — parked condition resolved,
needs its own careful change), #73 -> 08-25 (panel-feed 110k measurement; zero model calls),
#104 -> 08-25 (re-scoped to undelivered (b)-(g); (a) 402-blocked), #105 + #106 -> 08-25 (NAV/
engine-P&L from the MT5 deal ledger — first genuinely fillable date), #110 -> 09-01 (sibling-sweep
mechanism design), #112 -> 08-27 (on schedule).

**The single highest-leverage principal page** stays the bundled item: OpenRouter top-up (unblocks
#2/#20/#21/#104a + the whole panel rail) + the §13 session (#67 Upbit 2x-lapsed + Coin Metrics +
#80 anti-bot standard). Recorded in data/PRINCIPAL_ACTION.md 2026-08-18 alongside the Appendix-A
reseal block.

_Re-ranked 2026-08-13T02:05Z (daily cycle, repair-mode)._ **#1 stays PRINCIPAL REARM + A/B/C**
(human-gated; `web/cashcarry_live.json` read fresh: `n_carries 0`, `mode live-paper`, bleed verdict
the diagnosed re-base accounting echo; `web/growth_audit.json` `conservatism_defects: []`, every
gap RAIL- or HUMAN-justified). **Rank unchanged below #1; what moved this cycle moved by being
CLOSED:** the §37 brief's dominant RECURRING class -- seven `rec-owed-R00xx` items -- had one
mechanism, and it is now fenced. `scripts/recommendations.py dispose()` moved a scheduled row's
due date IN PLACE with no history and no counter, so a row re-snoozed the day before it came due
left the `owed` population with no work done and no trace, defeating the file's own stated
anti-gaming guarantee. Measured over `--first-parent` ledger history: **39 of 152 ever-scheduled
rows (26%) had their due date moved and 38 are still scheduled**. Closed by `libs/ops/deferral.py`
(6c64b608): every move recorded with its reason, unreasoned re-schedule refused, chronically
deferred rows can no longer leave `owed` by moving the date. Adds ZERO rows on the day it lands
(owed 40 / backlog 226 bit-identical before and after), so it cannot be red-from-day-one and
switched off. NEW this cycle: R0548 (max_audit's live-defect set is not stable within a cycle --
14 at 00:30 vs 17 on re-run, the delta being exactly the three §37 SKIPPED items, so
absent-from-audit was reading as fixed) and R0549 (`paywall-unhunted` VERIFIED live, llama.fi).

_Re-ranked 2026-08-11T20:58Z (daily cycle, repair-mode)._ **#1 stays PRINCIPAL REARM + A/B/C**
(human-gated; `web/cashcarry_live.json` read fresh this cycle: `n_carries 0`, `mode live-paper`,
bleed verdict = the diagnosed re-base accounting echo, venue-ledger truth published). **Rank
unchanged below #1; what moved this cycle moved by being CLOSED, not re-ordered:** the top
instrument defect on the board (welded sub-daily sharpe-ceiling -- the merge dropped the rescale
from BOTH lineages, every strong intraday cell auto-branded SUSPECT-LOOKAHEAD, moat positive
control dead 6d) was fixed, live-verified, and its 1,220 welded-era verdicts reset (9e11c7d);
crown-jewel recorder hang supervision now covers all three recorders (R0282); the OI/LS
366-symbol universe ingestion is RUNNING (e2828b8, clears the #100-adjacent construction-mismatch
precondition); the dated-deferral clock false-positive class is fixed at the root
(mine_conversion "u" retention). #100/#101/#102 stand as ranked -- #100's remedy is partially
delivered by the denominator now published on the entry-gate detector (post_gate_opens_examined);
the general sweep it demands stays open. Ledger: 23 rows dispositioned this cycle, 3 added
(R0442-R0444), backlog draining at 17.3/day vs 12.9 arrivals.**

_Re-ranked 2026-08-05T03:40Z (daily cycle)._ **#1 stays PRINCIPAL REARM + A/B/C** (human-gated;
`web/cashcarry_live.json` read fresh this cycle: `n_carries 0`, `deployed_notional 0.0`,
`risk.action pause_opens`, `dd_from_peak_pct -17.61`). **#1b ENTERS AND IS CLOSED THIS CYCLE, and
it outranked every open row because it was a precondition of #1 itself:** the structural-bleed
denylist had silently become a **no-op**, so replying `REARM` — which auto-executes, with nothing
human reviewing the denylist — would have re-opened the two incident-#6 symbols at full size.
`_structurally_bleeding` read only `worst_symbols`, a **14-day rolling window over the book's own
closes**; the book paused 2026-08-01, the window emptied, and on a *freshly regenerated* artifact
the gate returned `False` for COOKIEUSDT and 1000CATUSDT — the two symbols its own comment calls
"currently-blocked". The dated **$100 / 3-probe / after-2026-08-15** re-entry protocol written for
exactly those two was unreachable code. Fixed tighten-only (`36f6f28`, code in `9e3ff47`);
verified both symbols now block, controls unchanged, and the door still opens 2026-08-16
(`allowed=True, probe 1/3`), so the exclusion is not absorbing.

_Deadline decisions 2026-08-05T07:20Z (owed-work batch 3) — the three DEADLINE-PASSED rows the mechanical pass flagged (#70, #74, #75) each get an exit here, per the register's own rule that a passed deadline owes implement / defer-with-a-deadline / retire-with-reason:_

- **#70 — DEFERRED to 2026-08-12 AND ESCALATED into the recommendation ledger as R0358.** The register was not driving it (deadline 2026-08-02 passed with the prototype still uncommitted), so it now sits in the ledger, which carries a 24h disposition clock and feeds max_audit. Not retired: its value is already MEASURED, not estimated — the extractor corrected three recorded numbers on the first paper it read, and each correction widened a prior the desk had recorded too narrowly.
- **#74 — DEFERRED to 2026-08-19.** A diagnostic, not a build, and it ranks 35th on the register's own list. It is genuinely blocked behind the same evidence #75 needs (the deep_sweep tree), so sequencing it after #75 is real ordering, not avoidance.
- **#75 — DEFERRED to 2026-08-19, paired with #74.** Governing `docs/research/deep_sweep/` is an artifact-governance change of the same shape as the `_TERMINAL_ARTIFACTS` / findings-scope split already carried this week; doing it alongside #74 means one pass over that tree rather than two.


**THE RANKING LESSON: a guard fed by a rolling window of our OWN activity disarms itself during a
pause — and the pause is usually caused by the thing it guards against.** This re-weights every
row that credits a guard with protection: the guard's *evidence source* is now part of the claim.
It also silently voided half of **R0057**, which deleted the absolute per-8h funding floor on
2026-07-31 reasoning that "the cost gate plus the structural-bleed denylist carry all of its
protection" — one of those two was already a no-op. Encoded as standing probe angle **15 DORMANCY
DISARM** and as `max_audit.check_dormancy_disarm` (governed by L1.40, proven falsifiable:
pre-fix FIRES on both symbols, post-fix silent).

_Re-ranked 2026-08-01T17:35Z (daily cycle, PM)._ **#1 stays PRINCIPAL REARM + A/B/C** (unchanged,
human-gated; `live_guard.json` read fresh this cycle: `armed false`, `stage S0`, 0 positions).
**#1c ENTERS AND IS IMMEDIATELY CLOSED, and it outranked everything the register held:** bare
`pytest` exited **3 with 102 INTERNALERROR lines and ZERO tests collected, repo-wide**. Not red —
NOT RUNNING. Four files under `tests/` ended in a module-level `raise SystemExit`, which pytest
evaluates during COLLECTION, so one such file aborts the whole session. CI is the desk's declared
"mechanical safety net: nothing ships that fails it", and it was passing nothing while appearing
to. Fixed + fenced in `6f54899` (`tests/test_suite_collectable.py`); collection RC 3 → 0 verified.

**THE RANKING LESSON IS THE DURABLE PART, and it re-weights this whole register.** The class was
diagnosed at 16:44Z (`0240cfa`, lesson L0057), the ONE instance was repaired, no fence was built —
and it recurred **three times in the next 21 minutes** (`6c81187`, `ba19f22`, and the concurrent
recommendation-worker at 17:05Z). `0240cfa`'s own message predicted it: L0057 renders at 659 chars
against **156 chars of free injection budget**, so it "does not fit and reaches no organ".
**32 of 57 desk lessons are currently over budget** — that is the MAJORITY of what this desk
believes it has learned, reaching nothing. So: a register row whose only enforcement is a written
lesson should be ranked as UNENFORCED, not as closed. Rows citing a fence or a test keep their
rank; rows citing "we know better now" do not. Rowed as R0346 with the write-time fix.

_Re-ranked 2026-08-01T15:10Z (daily cycle)._ **#1 stays PRINCIPAL REARM + A/B/C** (unchanged,
human-gated). One new entry enters directly beneath it, and one measurement changes how the whole
register should be read.

**#1b NEW — R0320: an accounting re-baseline DISSOLVED an active risk rail.** Journal-verified
from `journalctl -u quant-cashcarry` this cycle, not inferred: `12:10:22 RISK-PAUSE-OPENS drawdown
-17.6%<=-15%` (`net=$-1860.22`, `carries=0`) → `12:22:51` `capital_events` RESTART moves start
equity `+4790.70` → `14:19:29 'open BNBUSDT 0.01'` — **opens resumed with zero trades in between**.
A drawdown rail is a RATIO, so moving its denominator clears it without any risk having fallen.
Testnet/paper today (`run_cashcarry_executor.py:29-30` hard-imports testnet), which is the only
reason this is #1b and not #1. It ranks above the general queue because it is a RAIL-SEMANTICS
defect that becomes live-critical at the same moment REARM does, exactly like R0217 below it. NOT
fixed this cycle by choice: the correct post-capital-event baseline (high-water vs start-equity vs
event-adjusted) is a genuine design question with rail-safety consequences, and DEFERRAL DISCIPLINE
names "unresolved uncertainty" — not session length — as the valid reason. Dated, not parked.

**READ THE REGISTER DIFFERENTLY FROM TODAY.** The §37 carry-over brief that has been steering
cycle priority was measured this cycle at a **57% false-positive rate** (26 of 47 items carried
unexpired dated acks; the top-12 shown to the brain FIRST was **12/12 acked**). Fixed in this
commit. Any prioritisation inherited from a pre-2026-08-01 cycle was ranked against that noise —
so a row that looks neglected may simply never have been visible under it. Row age before today is
weak evidence of anything.

_Re-ranked 2026-07-31T21:05Z (sixth cycle of the day)._ **#1 stays PRINCIPAL REARM + A/B/C**
(page live, book flat+frozen, `live_guard.json` read fresh this cycle: `armed false`,
`rung full_flatten_disarmed`, `requires_manual_rearm true`, 0 positions). Two evidence-driven
movements, both from measurements taken this cycle rather than a re-weighing:

**#1a NEW — R0217 venue-side protective stop is INERT, and it is a REARM blocker.**
`run_cashcarry_executor.py:1015` gates the stop rail on `hasattr(fut, "place_stop_market")`;
`fut` is `binance_testnet` (line 29) and `place_stop_market` is defined **only** in
`binance_live.py`. "The rail that survives host death" returns `[]` unconditionally. Its own
docstring discloses it — disclosed-but-never-gated is an open defect, not documentation. It costs
nothing while the book is flat and becomes live-critical the moment REARM puts capital behind it,
so it is ranked immediately under the REARM ask it gates rather than in the general queue.

**R0206 CLOSES — and it closes by REFUTING its own hypothesis, which moves the carry
investigation rather than ending it.** The proposed mechanism (ranking by funding = ranking by
widest premium ⇒ the sleeve shorts an ongoing squeeze) is **refuted and reversed**: on 164 symbols
2019-09-08..2026-07-31, the top-4-by-funding basis leg is **+0.65 bps/day (t +3.11)** — converging,
mildly *favourable* — and — the non-tautological half — the **basis leg alone** improves
monotonically across funding deciles (d1 −1.89 → d10 +0.61 bps/day), in all four constructions.
(Net by decile also rises, but that is partly mechanical: funding is the ranking variable, so the
funding leg *must* rise with rank. The basis leg carries no such mechanism.) The entry rule is
**exonerated**, so the live −51.74 bps `price_pnl` is redirected to
execution/accounting exactly as the card pre-committed. The gap that remains is the honest
finding and is now rowed as **R0219**: paper-gross +7.77 bps/day vs live −58.27 bps/round-trip is
an **L2.10 reality gap of ~66 bps**, and the next repair hour goes to decomposing it against
`trade_forensics` + `tca`, **not** to re-engineering selection. The +7.77 figure is gross of
costs on a current-universe panel and is recorded as NOT an edge.

**Staleness:** #71 blocked on a principal/panel ruling since 07-26 (5 days) — named again, not
silently carried. #91 remains `paged-tier3`. #88 due 08-12, unchanged.

_Previous stamp: Re-ranked 2026-07-31T18:10Z (fifth cycle of the day)._ Rank order UNCHANGED — **#1 stays
PRINCIPAL REARM + A/B/C** (page live, book flat+frozen; the reply now also gates **R0058 TCA
persistence**, scheduled to land in the REARM→first-fill gap because the L1.38 window reads
RAIL_BREACH — zero TCA data is being destroyed while flat, and the fix must precede fill #1).
Conversion queue: every past-grace row disposed this cycle (R0056 implemented-by-machinery,
R0058 pinned as above, R0060 closed with a permanent single-source fence + 10 contaminated
half-life rows retracted); max_audit **10 → 0 live defects**, 32 acks each dated with a
falsifier. New row: **R0174 session edit-mutex** (due 08-02) after a sweep-and-recover race
between two live sessions in one tree. capability_hunt/ scoped as a §35/§36 class exclusion
(findings row same-run; 07-31 proof R0153–R0173 all disposed).

_Previous stamp: Re-ranked 2026-07-31T17:05Z (fourth cycle of the day)._ **Deploy pipeline HEALED and two-session
work UNIFIED** — the evening session's orphaned 35-file fix set (R0144–R0146 skip-reads-green
family) was recovered, gated, merged with the remote discretionary program (R0135–R0143 +
organ-liveness), and pushed; puller `up-to-date`, crontab reinstalled at the new manifest hash,
executor recycled onto current code, forecast `20260731-deploy-pipeline-repair-heals` graded
TRUE. Rank movements, artifact-backed: **#1 stays PRINCIPAL REARM + A/B/C** (unchanged, page
live; every idle-capital hour attributable to that one reply — R0026's decomposition now shows
the clean-regime price leg at **+0.46 bps**, so the sleeve's economics do not block resume).
**Conversion queue rank falls**: 16 rows disposed this cycle (R0009/13/26/31/42/47/50/52/63 +
R0144-46/149/152 + 2 panel rows scheduled), forward cohort **12/12 accruing** (WALCL clock fills
the kimchi slot). **08-01 batch owns tomorrow**: R0002 doctrine 87.6k→16k (measured 3× worse
than rowed), money-path trio R0020/R0025/R0062, R0043 angle-20 headline._

_Previous stamp (13:50Z), kept for the freeze record: **the 08:05 headline is superseded: the
book FROZE at 08:35Z** — pager-ladder full-flatten (root cause: a retired-credentials auth check
paged false-positives for 12d into a ladder whose ack file had NO writer; Binance 418 mid-flatten
crash-hammered by systemd until the ban-latch fix). Movements this cycle, each artifact-backed:

- **#1 priority is now PRINCIPAL REARM**: reply channel BUILT+LIVE (`run_alerts._poll_replies` →
  data/PAGE_ACK + REARM/A/B/C/YES/NO commands; announce page delivered 13:26Z ntfy 200). Book +
  forward clock idle until his reply — every idle-capital hour is now attributable to a single
  human action, correctly.
- **Scheduler single-sourced** (R0070 herd/dupes closed): live crontab == manifest (drift OK,
  75 lines), 6 double-scheduled organs deduped, deep-sweep daily-vs-weekly contradiction resolved
  to WEEKLY per its recorded ruling, wiring-agent flap+server-wiring fixed (ratchet fence,
  organ-shape filters, hash-staggered slots).
- **Reject recovery loop LIVE** (rejection-shadow-unscored closed): `_forward_score` implemented
  (smoke 10/10 on backdated cutoffs), repointed to sor_crypto (434 rejects; June FX store
  unscorable here, ledgered), wired daily 06:50. First scoreable cohort ~08-10.
- **Generation: 2 new QUEUEs + 10 L1.16a re-opens** (recalibrated-gate victims, EV 0.002-0.019)
  seed 6 of 9 clock-starved axes; screens = R0126 (due 08-04, harness R0127 first). energy/mining
  honestly below bar (0.0011/0.0002).
- Max-audit page writer: newest URGENT now owns line 1 (5th demotion-family member fixed).

_Re-ranked 2026-07-31T08:05Z (daily cycle). **The rank moved because the money path came back to
life and the queue got its missing enforcement wire.** Movements, each verified by artifact:

- **BOOK RESUMED — the top conservatism defect closed.** `_MIN_FUNDING` deleted (R0057/R0066,
  commit 55da0ec, 4 lock-tests) + executor restarted TWICE onto fixed code (first restart preceded
  the edit — daemon-stale-code caught in the act; final pid 1541537). First tick 07:21Z:
  `carries=3` (BTC/BNB/FIL — exactly the majors the floor vetoed). Growth-audit
  `carry_capital_utilization` NONE-gap → closed by deployment. Watch item: one `OPEN-FAIL ETHUSDT
  (spot_ok fut_ok=False)`; venue shows no orphan ETH across subsequent ticks; deep semantics is
  R0020 (due 08-01).
- **§42 X1 WIRE LANDED (the queue's missing cross-session repair path).** `max_audit` now fires
  `rec-owed-R<id>` per overdue ledger row (12 oldest + true-total summary), which feeds the §37
  carry-over brief automatically. 27 overdue rows ALL disposed this cycle (1 implemented,
  1 rejected with reason, 25 scheduled with enforced dues). P1 prediction (9 rows due 08-01) now
  has its counter-mechanism; check outcome 08-02 as registered.
- **Certification read (R0077): the gate question is ANSWERED.** `reports/gauntlet_certification.json`:
  legacy path admits NOTHING at any true Sharpe up to 15 (sole weld: reality_check); per-candidate
  path admits from **SR≥5** with 0% null false-pass. At T=310/N=420 the bar is honest and the
  SAMPLE is the constraint → the T-lever is now specced AND flowing:
  `docs/research/BITMEX_DECADE_INGEST_SPEC.md` + **11,148 rows of XBTUSD funding 2016→now on disk**
  (`data/bitmex_funding.jsonl`). Tier-3 flip ask unchanged, restored on the page (R0033, 08-07).
- **EV gate recalibrated (R0023/R0034):** 0.05 → 0.002 with derivation + 3 reference lock-tests —
  the old bar sat 5x above the best HONESTLY-scored real family, so honesty auto-rejected and two
  generation cycles were blocked. Generation unblocks next cycle.
- **Resurrection engine wired (L1.16a):** `graveyard_resurrect.py` was a true orphan (zero-reader
  queue); now weekly in the manifest, queue rebuilt (n=42, shortlist: defi_health, multilingual
  wikipedia attention; options-VRP unlock noted, Deribit verified).
- **DATED EXITS for the stale-row sweep (register rule, item-level):** #1 live-track-record,
  #5 data-breadth clocks, #6 factory-pilot-30d = time-gated clocks → DEFER to their own maturity
  dates (they close by calendar + evidence, not work); #3 pager-delivery → partially evidenced
  (digest 2/4 + retry added 55da0ec; full verify folded into next real page); #4 fill-quality
  ledger → 08-03 data batch. Full row-body edit pass with per-row dates is FIRST mechanical item
  of the 08-01 cycle (ledgered); header dates alone do not satisfy the item-level rule and are
  not claimed to.

_Prior stamp:_ Re-ranked 2026-07-30T12:45Z (R0017 cycle). **The rank moved because the instrument that was
supposed to certify the gate turned out never to have asked its question, and because a refuted
sleeve was found holding a capacity slot.** Two closures and one correction:

- **R0017 CLOSED (was the blocking precondition for #87's remaining half, R0040/R0044).** Its stated
  cause was WRONG and that is the substance, not a footnote: the old probe's injection arithmetic
  (`mu = SR_true*sd/sqrt(365)` + Student-t) is CORRECT, not "mis-wired". Realised Sharpe came out
  exactly linear in `SR_true` with a CONSTANT −2.89 offset because `seed=7` was reused for all 13
  rows, and `SE(annualised Sharpe)` at T=310 is `sqrt(365/310)=1.085` — so "SR_true=+0.5 realises
  −2.32" is a −2.6σ draw of ONE noise realisation, and the sweep's smoothness is exactly what
  disguised that as a sign error. The probe handed the gauntlet a candidate whose REALISED Sharpe was
  −2.32 and recorded that the funnel cannot promote a good candidate. The question had never been put.
  Built `libs/validation/positive_control.py` (target sample Sharpe pinned BY CONSTRUCTION, swept over
  INDEPENDENT seeds; null cohorts deliberately left RAW so the DSR/CSCV deflation benchmark is not
  collapsed) + 25 tests + `scripts/certify_gauntlet.py`, which scores the legacy and per-candidate
  paths from ONE `campaign_gate_stats` pass — the CONTROLLED A/B whose absence got the 07-30
  migration reverted. Proving artifact: `reports/gauntlet_certification.json`.
- **NEW #96 (renumbered from #94 -- that id is the lockbox row) CLOSED SAME CYCLE — a REFUTED hypothesis was holding Stage-B slot 1 of 12.**
  `kimchi_premium` was retracted 2026-07-29 as a ~73% timestamp artifact (registry `E-02f2917dfb`,
  decision REFUTED) but the retraction never reached `run_axis_shadows.py` `_AXES`, so it kept
  ACCRUING at forward `ann_sharpe −5.13`. That made `derive_slots()` read the cohort FULL
  (`idle_slots=0`) — and **that reading is the evidence commit b70298d used to justify the
  clock-saturation defect as "capacity-blocked at 12/12, not idle"**. Nine verified axes were denied
  forward clocks by a dead sleeve, and the Holm bar sat at 2.64 instead of 2.61 for eleven legitimate
  clocks. Verified live: m 12→11, `idle_slots` 1. Proving command:
  `.venv/bin/python -c "from libs.research.slot_registry import derive_slots; print(derive_slots()['idle_slots'])"`.
- **CORRECTION to this register's own #87 entry, found by running the proving command it cites.**
  `grep -rln campaign_pbo_rc scripts/ libs/` returns 5 files, not "only the deprecated diagnostic":
  `max_audit.py` (the detector, matches by design), `measure_gate_histogram.py` (diagnostic),
  `libs/autodiscovery/validation.py` (defines the retained legacy path), `orchestrator.py`, and
  `run_options_vrp_backtest.py` — the last being the documented R0044 exception (its validated series
  is a row-mean that is no column of the matrix, so no column index exists). So #87 is substantially
  closed but its wording overstates; `max_audit`'s "19 paths still welded" is the STALE side of the
  same disagreement and needs reconciling (row it, do not re-litigate #87).
- **CONCURRENCY CAVEAT on everything above:** a second session (branch
  `claude/wonderful-darwin-7uiobi`) was committing to this working tree during the cycle — master
  moved e77b4b8→be6b3d5 and `run_crypto_portfolio.py` was rewritten at 12:15 while this cycle
  reasoned about it. R0048 rows the missing guard. Rankings below are as of this stamp only.

Everything else retains its 07-29T21:5xZ ranking._

_Re-ranked 2026-07-29T21:5xZ — THE LARGEST CLOSURE CYCLE ON RECORD, and the rank moved because
seven rows CLOSED rather than because opinion changed. What shipped, each with its proving command:

- **#87 CLOSED — the welded gate is OPEN.** Principal ruled; the per-candidate flip is applied at
  ALL 19 call sites (orchestrator + 18 scripts). `grep -rln campaign_pbo_rc scripts/ libs/` returns
  only the deprecated diagnostic. 143 autodiscovery+validation tests green. The 420/0 record is
  now known to be an instrument artifact and is no longer steering strategy.
- **#53 CLOSED — mutation testing installed AND measured on THREE money-path modules.**
  `scripts/run_mutation.py`. Per-target ratchet floors, each pushed the same session:
  `libs/validation/stepwise.py` **55% -> 90.0%** (all 4 survivors proven EQUIVALENT — CSCV PBO is
  rank-based), `libs/risk/gate.py` **23.5% -> 86.3%** (the worst on the desk: 10 tests for 210
  lines on the module that decides whether capital moves), `libs/execution/staging.py`
  **69.0% -> 83.3%** (100% of OBSERVABLE mutants). Aggregate `targets_at_bar` = 33% (1 of 3), which
  is the honest number to drive up. The v8 8.2 bar is no longer decorative.
  **AND IT FOUND A LIVE FAIL-OPEN:** the S2 gate read an ABSENT drill-failure record as ZERO
  failures, so full automation could be authorised on missing evidence — fixed with a refusing
  sentinel (strictly conservative: it can only decline a promotion, never authorise a trade).
  Owed next: binance_live (runner wired), the 7 gate survivors reachable only through the governor
  stack, and `libs/execution/retry.py` has NO test module at all — recorded as the finding rather
  than skipped. The SECOND HALF of the v8 8.2 bar (second-model-family fuzz) is a PANEL task
  blocked on the top-up; mutation testing does not substitute for it.
- **#58 CLOSED (agent half) — the repo can reconstitute the desk.** `ops/crontab.manifest`
  (20 cron + 13 systemd, every entry with file:line evidence + a CONFIDENCE tag),
  `scripts/check_scheduler_manifest.py`, `deploy/reconstitute_cron.sh` (idempotent, fenced).
  OPERATOR still owes the `crontab -l` paste by 08-05 — the manifest says so in its own header.
- **#56 CLOSED — and it was an ACTIVATION gap, not a build gap.** `libs/core/logging.py` (structured,
  correlation ids, secret redaction) already existed and the only module using it was itself.
  Money path now wired: binance_live (refused-unarmed calls, order ids, the ruin-rail stop, and the
  previously-SILENT 50-chunk bound), staging (transitions with reasons), risk/gate (one line at the
  single reject choke point). AST-scanned fence forbids key/secret/signature/query in any log call.
- **#38 CLOSED — the pager can now PROVE delivery.** `libs/ops/alert_channels.py` (registry +
  append-only ledger, titles hashed not stored), `scripts/run_alert_canary.py` (throttled synthetic
  page + silence detection -> `data/ALERT_CHANNELS_SILENT`), `run_alerts --status`. FAILED attempts
  do not clear silence — the 39/39 shape of 07-19 is still silence. Arming extra channels is a
  human step and both surfaces say NOT-ARMED out loud.
- **#39 CLOSED FOR REAL — it read closed and was open in effect.** The 07-22 union was BOOT-ONLY;
  with the book halted and flat the union is empty, so the moat was 20 majors with zero traded
  intersection. Both recorders now union the trade LOG, put traded names ABOVE majors when the cap
  binds (the old order evicted exactly the symbols the cost model needed), and refresh HOURLY
  in-flight with the weight budget re-checked against the actual count.
- **#52 CLOSED as a ratchet.** 263 files / **1118 errors** / 107 clean measured for the first time
  (the old '408 across 87' could only see reachable files); counts may only fall. Found and fixed a
  tooling defect in the same run: without `--explicit-package-bases` mypy aborts every batch and
  the first run scored 263 files UNCHECKABLE.
- **#29 (miners never ran) — CAUSE ISOLATED, and it is one purchase-free human step.**
  `scripts/check_miner_runway.py` reports 11 of 11 seats `creds-missing`, blast radius 11, with
  prompts/runners/scheduling all verified PRESENT. The miners are not misconfigured, they are
  unarmed (`ops/setup_brain_token.sh`).
- **#77 (COT panel unread) CLOSED — 41 years read, and it CANCELLED a purchase.** GHR replicated:
  pooled lagged NW-t **-0.64** across 6 contracts 1986-2026, so the queued crypto positioning
  acquisition is cancelled on evidence. Decay is UNMEASURABLE here (every pre-2000 Sharpe is
  NEGATIVE), so the borrowed -58% McLean-Pontiff prior stays explicitly BORROWED. Two parsing
  defects found in the screen's own code first, both of which had produced false negatives.

NEW LAWS, with fences (a law without a fence is prose): **L1.0 THE UNIVERSAL RATCHET** — every
metric's today is its floor, 100% is the target, the gap IS the work queue, self-initiated, and
100% subsystem breadth coverage in EVERY cycle; fenced by `scripts/check_ratchets.py`
(5 metrics live, floors recorded, structurally incapable of lowering one). Plus L1.11a asymmetric
information archaeology (time/geography/language/era as SEARCH DIMENSIONS, CN pre-ban and Arabic/
Gulf-MENA named explicitly), L1.16a opportunity resurrection (narrow door, named enabling change),
L1.24 information advantage not activity, L1.25 alpha-discovery persistence (zero survivors triggers
the 7-step diagnostic, never surrender), L1.26 investment-objective priority, L1.27 opportunity cost
of inaction, L2.0 the ratchet fence, L2.9 capability audit loop, L2.10 reality-gap detection.

NEW ORGANS, all cron-wired in the manifest: `run_execution_intel.py` (cross-feed cost-DRIFT
detector, recommend-only, never self-applies), `run_reality_gap.py` (backtest->shadow->live->
venue-truth, the 7.75x and 36.4% classes become standing detectors), `libs/research/dist_shift.py`
(has the measuring stick moved — downward-only haircuts), `libs/research/pre_filter.py`
(HYPOTHESIS_MAX #1; rejects charge trials, never skip the ledger), `check_ratchets.py`,
`check_miner_runway.py`, `check_mypy_ratchet.py`, `run_mutation.py`, `check_scheduler_manifest.py`.

WEEKLY GAP-MAX SWEEP INSTALLED AND SCHEDULED (autonomous, Tue 09:00 UTC, fresh session) with a
binding EFFORT FLOOR: at least as exhaustive as this session, no self-imposed quota, every ratchet
re-measured and pushed in the same run; a sweep reporting "nothing to do" without a per-metric
value/floor/distance-to-100% table has not run. `docs/WEEKLY_MAX_CYCLE.md` is its contract.

TOP OF THE RANK NOW (what actually blocks compounding, not what is merely open):
1 **#2 live connector** (07-31 deadline; #49 client-order-id is its hard prerequisite) ·
2 **#49** · 3 **#54 per-venue cap** (a NUMBER, must land before live keys) · 4 **#41/#37** risk-path
reconciler hardening · 5 **the three HUMAN steps that unblock the most**: miner credentials
(11 seats), the OpenRouter top-up (8 defects incl. the 13-seat panel and the v8 8.2 second-model
fuzz half), the `crontab -l` paste (#58's other half). Everything below retains its 07-29 order.

HONEST NOTE ON WHAT DID NOT MOVE: the deepest problem is unchanged and no amount of this cycle's
work touches it — across 420 candidates none was simultaneously profitable, walk-forward-stable,
capacity-viable and tail-acceptable. #87 proves two gates were lying, which RE-OPENS the question
rather than answering it: the campaign must be re-run through the per-candidate gates before anyone
claims the space is picked clean (L1.25 forbids reading zero survivors as absence of alpha)._


_Principal override 2026-07-16. Reviewed + re-ranked at the START of every daily AI cycle;
items stale >7 days MUST be escalated (implement / defer with deadline / retire with reason).
Ranked by expected E[log wealth] impact. Never empty without written justification._

_Re-ranked 2026-08-05T01:35Z (REAL re-rank). TWO MOVES, both from something MEASURED this cycle:

(1) **NEW #89 enters at 1: THE CONTROL CHANNEL WAS SEVERED AND THE DESK KEPT PAGING INTO IT.** This
outranks every capability gap below because it gates two others: the book is flat ($0 of $4,500
authorized) and the funnel is welded (434 tested / 0 survivors), and BOTH were "awaiting principal"
on decisions he had no way to answer. The fork deleted `_poll_replies` from `run_alerts.py`, so from
2026-08-02T08:42Z the pager was strictly ONE-WAY; the page was additionally truncated at 160 chars,
cutting off its own `REPLY:` lines, and buried under 180 undeduped robot pages sent after it. The
`gate-optimality` ack literally read *"lifts on his reply"* through 33 sweeps. FIXED + verified this
cycle (poll live 01:31:44Z, page re-sent 01:18:33Z, receipt `OwFuIXvwoNS5` against ntfy's own store,
BRAIN pages 17/15min -> 0). **The generalised gap, which is what stays open: a blocked-on-human item
must verify the REPLY PATH EXISTS before it may be re-carried.** Encoded as the standing question
"has this recovery been SEEN to work, or only configured?"

(2) **#88b (stale-code daemons) DROPS from 1 to 2 — same class, now with a second surface.** Yesterday
measured stale *processes*; today measured stale *branches*: `run_cashcarry_executor.py` here is a
strict SUBSET of master's function surface (11 functions missing, 0 unique, incl.
`_reconcile_protective_stops`, `_stop_plan`, `_resting_quotes`, `_realised_rt_bps`), while this branch
is simultaneously AHEAD of master on the validation gate. "The running code is not the committed code"
and "the committed code is not the best code we have" are one defect with two faces. Port per-function
under review (R0031) — a bulk merge in either direction destroys real work._

_Re-ranked 2026-08-04T22:05Z (REAL re-rank). THREE MOVES, each from something MEASURED today:

(1) **NEW #88b enters at 1: STALE-CODE DAEMONS — a committed fix is not a running fix.** A `--loop`
process holds its code in memory, so editing the file changes nothing until restart. Measured by
sweeping every long-lived organ process for start-time < last-commit-touching-its-script: **5 of 18
were stale**, and four of those are the money path and the moat. The cash-carry executor had run
since 08-01 14:16 and never picked up the 08-04 rails fix `423ccad`; `run_recorder_bybit.py` never
picked up its 08-02 disk guard; `mine_moat` started 16 minutes BEFORE the `closure` feature landed
and wrote 2.5 days of fresh artifacts from pre-closure code. All five restarted and verified. This
enters at 1 because it silently voids EVERY other row here: any gap "closed by a commit" may still
be open in production, and nothing on this desk was checking. Fix is mechanical and rowed (R0024).

(2) **#81 (moat disk deadline) falls 1 -> 2, and half its risk is discharged.** Its own text said
the bybit disk guard's "code half is fixed" — true on disk, INERT in the process, which is exactly
move (1). The fastest writer on the box now actually honours `libs.ops.disk.PAUSE_FRAC` (verified:
import at run_recorder_bybit.py:34, process restarted 21:59Z). Measured headroom is now **20.7GB
free / 47% used** at ~1GB/day ≈ 20 days, better than the ~15GB/two-weeks reading of 08-02. The
purchase half stays operator-owned with its dated deadline; the deadline does NOT move on this.

(3) **NEW #88c: the fork is the disease, the ENOENT outage was one symptom.** 75/133 scheduled
scripts were absent because the crontab came from master's manifest and the code did not. Restored
113 files today, but the tree is still 419 commits behind with ~88 master-only test files and 256
conflicted paths — and `deploy/pull_deploy.sh`, the mechanism that would re-sync it, was itself part
of the outage. `pyproject.toml` proved the merge cannot be take-theirs: each side had ratcheted a
DIFFERENT axis (our dependency upper-bounds vs master's stricter lint rules), so reunification is a
per-file union. Rowed as R0023. Every cycle spent on the fork widens it._

_Re-ranked 2026-08-02T13:20Z (REAL re-rank, discharging the debt the 07-26 stamp left standing).
FOUR EVIDENCE-DRIVEN MOVES, each from something MEASURED this cycle rather than re-weighed opinion:

(1) **#96 (moat disk deadline) enters at 1.** The moat is the desk's only unreplicable asset and it
now has a measured expiry: ~15GB of headroom at ~1GB/day is roughly two weeks, and `run_recorder_bybit.py`
-- the fastest writer on the box -- had NO disk guard while both Binance recorders stop at 80%. The
failure mode is not "the tape stops": it is that coverage is filled/total, so a frozen grid makes
coverage race to 100% and produces a GREEN number for the exact event that ends the asset. Every
unrecorded second is permanently unbuyable at any price, which makes this the only row here whose
cost RISES with delay and cannot be recovered by spending more later. The code half is fixed; the
remaining half is a purchase, which is why it is operator-owned with a dated deadline.

(2) **#2 (live connector) falls 2 -> 4 and its PASSED DEADLINE is discharged.** Its principal
deadline of 2026-07-31 went by two days ago with nothing anywhere reporting it -- caught only
because `scripts/rerank_gaps.py` landed this cycle and parses deadlines mechanically. A deferral
whose date has passed is not deferred, it is parked with extra steps, which this register's own
rule forbids. RE-DEFERRED WITH A NEW DATE: 2026-08-23, and the reason is specific rather than
"more time" -- the row's remaining work (venue-side reduce-only stops, no-naked-position reconcile,
pager de-risk ladder, 6h canary, mutation testing >=90%) is gated on the VPS being reachable for
canary round-trips, and the same box is now the binding constraint for #96. It falls to 4 rather
than holding at 2 because #96 competes for the same operator attention and expires sooner.

(3) **#71 (gate-optimality) rises 3 -> 2.** Unmoved in substance and re-verified: it is still the
answered defect blocking every promotion (campaign PBO 0.6159 and White RC p 0.4220 veto all 420
candidates regardless of individual merit). It rises only because #2 fell past it. Nothing this
cycle changed its evidence; saying so explicitly is the difference between a re-rank and a shuffle.

(4) **#49 falls 1 -> 3.** No new evidence against it -- it is displaced by a row whose cost rises
with delay, which is the one property that legitimately outranks a larger expected value. #49 will
cost the same next week; #96 will not.

NOT MOVED, AND THE REASON IS THE POINT: the other 46 open rows carry no new evidence this cycle,
so their relative order is preserved untouched. Re-weighing rows nobody measured anything about is
how a re-rank becomes a shuffle that looks like work -- the register's own record already contains
one stamp that had to label itself an insertion rather than claim a re-rank it had not performed.

ROWS 96-98 ADDED. 97 and 98 are entered as CLOSED with their fixes named: both were defects found
and fixed inside this cycle, and rowing them retrospectively is what §35 requires -- a finding that
never lands here is invisible forever, and "we fixed it before anyone could write it down" is the
most plausible-sounding version of that failure.

MECHANICAL PASS: 50 open rows checked, 1 needed a decision (#2, above). Deadlines, parked rows,
ownership and starvation are computed every cycle by `scripts/rerank_gaps.py`; this stamp covers
only the judgment half, which is re-prioritising against new evidence._

_[Merge note 2026-08-04: this stamp, its RANK list below, and its rows were authored on the
execution/coverage/moat account line with rows numbered 81-87; they are recorded in this register
as 96-102 because 81-95 were already assigned by the earlier two-account merge (see the RENUMBERED
notes near the foot of the register). References remapped; content otherwise unchanged.]_

_Re-ranked 2026-07-29T16:20Z (STEP-0 integrity cycle). **Two new rows, #90 and #91, and #90 is the
one that matters most because it invalidates a status this register has been asserting.** #87/#71
have read "paged for a YES/NO, awaiting a ruling" since 07-29 06:20Z. The ask was **not on the
page** — `run_external_panel.py` clobbered `PRINCIPAL_ACTION.md` with a bare `write_text` when it
paged an unrelated credits notice, so the principal was never shown the decision the entire
discovery pipeline is blocked on. Found by checking the artifact against what the register claimed
about it, which is the only reason it surfaced at all. Fixed, verified, ask restored (#90).
**#91**: the primary carry book is not idle, it is DEAD — its ruin rail is in an absorbing state
and the Gate-0 forward clock stopped days ago with no alarm anywhere; the rail fired on a total
that excludes $4,399.91 of verified on-venue assets. Detector built, decision paged, rail untouched.
Both rows share one shape — **a channel reporting health while carrying nothing** — which is now
the thing to hunt next cycle. Everything else retains its 06:20Z ranking._

_Re-ranked 2026-07-29T06:20Z (REAL re-rank from measured state — one move, and it is a big one).

**#71 (gate-optimality) is ROOT-CAUSED and drops from "blocked on a ruling" to "one YES away",
rising to #1. NEW #87 carries the mechanism.** The blocking framing since 07-26 was that fixing
the gate touches validation-gate strictness, which constitution pt 5 reserves to the principal.
That framing survived four sweeps because the question was only ever asked in ONE direction —
"would this loosen the gate?". Measured this cycle, it is the wrong question. `pbo` and
`reality_check` are **campaign constants**: neither `probability_backtest_overfitting` nor
`whites_reality_check` takes the candidate's own returns as an argument, so every candidate in a
batch receives an identical verdict. On the real 420 campaign, PBO=0.6159 and White RC p=0.4220
forced **420/420 rejections regardless of quality**. And holding 60 pure-noise candidates fixed
while adding one genuine winner to the batch flips the old gates to admit **60/60 pure nulls** —
so the status quo is not strict, it is a batch coin-flip that is *also* unboundedly loose, and
the loose direction opens precisely when the desk starts finding real edge. Fix built, 13 tests
green, thresholds numerically unchanged, production flip NOT self-applied — paged for a YES/NO
(`data/PRINCIPAL_ACTION.md` §1). Ledger `2026-07-29-pbo-rc-are-campaign-constants-not-candidate-gates`.

Everything below retains its 07-28 ranking; the carry unit-economics row (#85) and the cost-rate
brake (#86) remain #2 and #3 — they decide whether the only sleeve is viable, but they cannot run
while the book is deadman-halted, and #71 gates the entire discovery pipeline, which is not._

_Re-ranked 2026-07-28T20:30Z (REAL re-rank from measured state, not re-weighed opinion).
TWO EVIDENCE-DRIVEN MOVES this cycle, both from instruments that did not exist yesterday:

(1) **NEW #97 (renumbered from #85 -- that id is the edge_gate row) — the carry sleeve's unit economics are unestablished, and measure NEGATIVE. Ranked
#1.** Venue-truth fee attribution shipped this cycle (`commission_events` + `_fee_attribution`)
and re-measured 73 churn-free round-trips at **-58.27 bps net-of-fee**. Fees are only 12 of those
bps: the dominant term is `price_pnl` at **-51.74 bps**, which for a delta-neutral pair should be
~0 and does NOT amortize with hold time (flat -48→-64 bps across every bucket) while funding
accrues only ~3-9 bps/day. This outranks everything: it decides whether the desk's ONLY sleeve is
viable, and Gate 0 would otherwise size real capital on a record whose instrument was blind to its
own dominant cost. Not self-closable — root-causing the -52 bps (leg asymmetry vs entry/exit
spread vs basis drift vs the incident's naked-long-spot window) is the next action. R0026.

(2) **NEW #98 (renumbered from #86 -- that id is the parallel-stacks row) — no cost-rate brake exists between an alarm and the ruin rail. Ranked #2.** §40
fired ~27h before the churn loop was diagnosed and had no authority to stop anything; the only
mechanism that halted a $1,750 fee fire was the equity ruin rail at -35%, i.e. after the money was
gone. 94.1% of dead-man fire #6 was that software defect. Specced in the autopsy, deliberately NOT
built this cycle: the book is deadman-halted, so the guard cannot be exercised against live state,
and landing unexercised money-path code during a halt is exactly how the 07-16 change-induced
incidents happened. Deferred to the resume decision — **implement before the book trades again**.
R0025.

Staleness: #71 (gate-optimality, RANK-not-VETO) remains blocked on a principal/panel ruling since
07-26 — it touches validation-gate strictness, which constitution point 5 forbids me to self-fix.
Named again rather than silently carried._

_Re-ranked 2026-07-26T20:35Z (REAL re-rank, discharging the debt the 05:20Z stamp below recorded).
THREE EVIDENCE-DRIVEN MOVES, each from something measured this cycle rather than re-weighed opinion:
(1) **#71 (gate-optimality) rises 13 -> 3.** It is no longer a question, it is an ANSWERED defect
with a named mechanism: `pbo` and `reality_check` reject 420/420 because `campaign_pbo_rc` computes
them ONCE PER CAMPAIGN and `validate()` (lines 102-103) hands every candidate the campaign verdict
-- its own docstring says they are "identical for every candidate in a campaign". Measured campaign
PBO 0.6159 (gate <=0.50) and White RC p 0.4220 (gate <0.05) therefore veto all 420 regardless of
individual merit, while the per-candidate gates discriminate normally (walk_forward 58.1%,
fragility 47.9%, cpcv 43.3%, capacity 43.3%, expected_value 40.2%; sole-cause failures EMPTY).
Because campaign PBO rises with generation volume, this makes the screen's bar rise with generation
-- the exact thing TWO_STAGE_DISCOVERY_LAW forbids. A promotion path that cannot promote makes every
research hour downstream of it worthless, which is why it now outranks everything except the two
ruin-class rows. NOT self-fixable: touching it relaxes validation-gate strictness (constitution
point 5), so it needs a principal/panel ruling on RANK-not-VETO. Recommendation R0016.
(2) **#74 (deep-sweep silent failure) falls 27 -> 34** on evidence that it is materially smaller
than written: 4 of the 8 dimensions actually PRODUCED (448/485/382/909 lines), one was cut off and
says so in its own text, and one now names its cause ("out of usage credits") because commit
53cda72 fixed the contract. The residue is 2 dimensions still writing empty-stderr stubs, and the
root cause is QUOTA, not an unexplained crash -- the same pool exhaustion the organ-catchup
concurrency gate committed this cycle exists to stop. Smaller and understood, so it ranks lower.
(3) **#41 holds at 5, unmoved but re-verified**: still the only MEASURED money-loser here, and this
cycle found a SECOND live measurement defect on the same book (a venue 502 published as a $0.00
funding harvest, ledger 2026-07-26-carry-funding-silent-zero), which strengthens rather than
changes its case -- the carry book's accounting is where this desk's real losses keep being found.
Nothing is >7 days stale; no forced escalations. Rows #70-#74 inserted 2026-07-26T05:20Z by the literature deep-miner
(run 3 routing batch). HONEST SCOPE OF THAT SECOND STAMP: the five new rows were ranked against the
existing list, and the existing rows' RELATIVE order was preserved untouched — this was an insertion,
not a full re-rank, and it is labelled as one rather than claiming a re-rank that did not happen.
The next cycle owes a real re-rank. 44 open-status rows by direct count (the "41" measured at 01:00Z
excluded the operator/principal-owned rows #67-#69, which claim no engineering slot and are still
absent from the rank below for the same reason)._

_THE RANK IS THE ORDERED LIST BELOW, not the table order:
the table is keyed by row id and cross-referenced by id from code, ledger entries and specs
("reg #24", "gap #2"), so permuting it would silently break those references and destroy the
append-only reading order. The id table is the LEDGER; this list is the RANK. Ranked by expected
E[log wealth] impact OF THE NEXT UNIT OF EFFORT -- which is the decision the rank has to serve.

STANDING CONSTRAINT, claims no engineering slot: **#1 live track record (day 28/90)**. It is still
the binding constraint on sizing confidence and every scaling step, and no work moves it -- only
calendar time does. Ranking it first every cycle made the top of this list uninformative; it is
named here and then excluded from the effort ranking.

RANK (1 = next unit of effort):
1 #96 · 2 #71 · 3 #49 · 4 #2 · 5 #76 · 6 #54 · 7 #41 · 8 #78 · 9 #37 · 10 #53 · 11 #79 · 12 #72 · 13 #70 ·
14 #77 · 15 #60 · 16 #19 · 17 #14 · 18 #38 · 19 #3 · 20 #51 · 21 #23 · 22 #24 · 23 #73 · 24 #42 · 25 #32 ·
26 #61 · 27 #48 · 28 #75 · 29 #39 · 30 #64 · 31 #22 · 32 #50 · 33 #40 · 34 #58 · 35 #74 · 36 #30 · 37 #56 ·
38 #52 · 39 #29 · 40 #55 · 41 #28 · 42 #36 · 43 #62 · 44 #63 · 45 #20 · 46 #21 · 47 #4 · 48 #5 · 49 #6 ·
50 #59 · 51 #9 · 52 #99 · 53 #100 · 54 #101 · 55 #102.
(#99 rows micro_audit_inbox#1, untracked since 07-26; it ranks last only because its fix is one retry-and-refuse in a path that is not currently trading -- it rises the moment the connector ships. Rows #97 and #98 are CLOSED and claim no slot. Row #80 is principal-owned and claims none either,
same convention as #67-#69.)
_[Merge note 2026-08-04: this RANK list is the execution line's 2026-08-02T13:20Z re-rank
(renumbered, see foot of register). It predates awareness of the governance line's rows 81-95 and
R-rows, which are prioritised by the stamps at the top of this file.]_

WHERE THE FIVE NEW ROWS WERE PLACED AND WHY (literature run 3):
- **#72 at 7** — the only item on this list with NEGATIVE cost: it cancels a queued build (#43
  automated debate) on measured evidence and its other change is one line (`random.shuffle`). Work
  not done outranks work done cheaply.
- **#70 at 8** — ~90 lines already written, and it permanently lifts a cap on the desk's primary-
  literature access that had silently bounded two full runs. It already paid for itself once by
  catching three wrong numbers in a desk document.
- **#71 at 13** — highest expected value of the five (it is the GATE-OPTIMALITY duty's own answer,
  computed from the desk's own 420-hypothesis record) but the largest work, so it ranks below the
  cheap wins rather than above them.
- **#73 at 18** — cheap, two-sided payoff, and one branch REMOVES a recurring 110k-char-per-seat cost.
- **#74 at 23** — a diagnostic, not a build; ranks on the fact that a silent-failure mode which
  produces success-shaped artifacts corrupts every coverage measure downstream of it.

WHY THE TOP FIVE RANK THERE:
1. **#49 (no client order ID)** — ruin-class AND a hard prerequisite. On an ambiguous timeout the
   desk cannot tell "not placed" from "placed, reply lost", so a retry or restart-and-reconcile
   re-places the order; on a delta-neutral book a duplicated leg IS an unhedged directional
   position — the NOM shape that took -41%. It outranks #2 itself because #2's own 07-31
   no-naked-position invariant is unachievable without idempotent submission: sequencing, not
   preference.
2. **#2 (live connector)** — the gate between a paper book and any compounding at all; principal
   deadline 07-31. Its remaining scope is not features, it is the ruin rails themselves
   (venue-side reduce-only stops at the ruin line, no-naked-position reconcile surviving host
   death, pager de-risk ladder, 6h canary). Every downstream row's value is conditional on it.
3. **#54 (per-venue exposure cap)** — the largest protection-per-hour on the board. SYSTEM_REVIEW
   ranks counterparty concentration as FATAL ("an FTX-class failure is fatal to deployed capital
   regardless of strategy correctness"): a venue failure is a log-wealth zero, and no strategy
   quality defends against it. Re-verified 2026-07-26 that it does not exist (zero hits for
   per_venue|venue_cap|venue_exposure). The fix is a NUMBER. With one venue it binds at 100% and
   changes nothing today — which is precisely why installing it now is nearly free and retrofitting
   it on the day a second venue exists is not.
4. **#41 (spot leg unhedged during futures thrash)** — the only MEASURED money-loser on this
   register: -$1,837.68, fully attributed, concentrated in GTC/SHELL/ONE, with futures realized
   +$13.41 against it. Not a hypothesis, an autopsy. The churn guard (#42) and entry gate (#43)
   removed the trigger; the INVARIANT (spot never left unhedged across a futures re-hedge cycle,
   plus an alert when fut_fills/spot_fills exceeds ~3x) was never built, so recurrence is prevented
   by circumstance rather than by construction.
5. **#37 (orphan-cover market-order path)** — an unbounded, uncapped, un-cooled market-order
   mechanism on the live path: no size cap, no confirm window, no venue-health gate, no
   idempotency, no cooldown. 8+/12 panel models raised it independently. A transient REST desync
   can market-cover into a thin book, and during a real venue outage the cascade is itself a ruin
   path. Ranks below #41 only because #41 already happened.

WHAT MOVED, AND WHY (a re-rank is worthless if nothing moves):
- **#49 → 1** (added 07-25, never ranked): identified this cycle as a PREREQUISITE of the 07-31
  gate, not a parallel item.
- **#54 → 3** (was effectively last, buried by row number): fatal-class risk whose fix is one
  number and which is deployable before Gate-0. Row number was doing the ranking; that is the bug
  this ordered list fixes.
- **#53 → 6** (mutation testing): #2, #49, #37 and #19 all cite the v8 8.2 bar as their gate, and
  the bar has never been measured — so it currently gates nothing. A gate nobody has measured is a
  decoration, and four risk-path rows are leaning on it.
- **#23 → 13** (was parked, undated since 07-17): promoted on evidence that arrived after it was
  written — 420 candidates tested, ZERO survivors, gates rejecting >=98%. Clustering cuts the true
  tested N, which is the only lever that raises survivor probability WITHOUT lowering a bar.
- **#51 → 12** (dependency pins): cheap, and a precondition for trusting every other deadline here
  — while CI resolves latest and production runs pins, green CI is evidence about neither.
- **#1 → standing constraint, unranked**: see above.
- **OFF THE BOARD this cycle**: #7 RETIRED (detection is now continuous and free via max_audit; the
  paid-panel-on-incident residual is exactly the uncapped spend the 07-24 budget incident produced),
  #25 RETIRED (its premise was tested and REFUTED by the desk's own Monte Carlo — anytime-valid is
  slower, see 25-RESULT), #31 CLOSED (run_ci.py now runs the whole tests/ tree, verified in code),
  #57 CLOSED (atomic dead-man write shipped under principal sign-off, commit 932b0e3, verified).
- **NEW rows #60-#66** from the §35 finding sweep: 12 findings the desk had already made and
  written down, none of which any loop could see. Coverage 83% -> 100% by rowing them, with the
  source docs untouched.
- Every open row now carries a DATE in its plan (was 16 undated, of which the check could only
  display 8): the register's three exits are implement / defer WITH A DEADLINE / retire WITH A
  REASON, and an undated open row took none of them._
_(prior notes retained below)_
_Re-ranked 2026-07-21T09:30Z. No item >7 days stale (oldest open = 07-16, 5 days) — no forced
escalations. This cycle: #35 (recorder spot-blindness) CLOSED — spot-leg recorder built + live
(highest-ROI in-lockdown action, priority #1). Also restored the desk-wide CI lint gate (was
RED on arrival: 15 pre-existing ruff errors in today's earlier tooling scripts — fixed, CI green).
Standing #1/#2 (live track record / live connector, 07-31 deadline) remain the top priorities;
next engineering slot goes to #2's remaining scope. AUDIT-COVERAGE NOTE (build_audit_coverage.py
this cycle): 0 stale risk-path files (14d floor), 51/698 ever-audited — the 647 never-audited
long-tail is by-design rotation (TIER-0 decision surface sent every run), not a risk-path defect._
_(prior note retained below)_
_Re-ranked 2026-07-18T12:50Z. No item is >7 days stale (oldest = 07-16, 2 days old) — no forced
escalations this cycle. Changes: #15 HFTUSDT concentration breach CLOSED (position closed
2026-07-17T14:03:56Z in the dead-man fire #3 flatten; book verified clean today, largest
name 26.0% vs 35% cap); #29 NEW -- digging/self-improvement cadence duties (prospector,
lit-deepminer, blind-rediscovery, decision-outcome-scoring, memory-consolidation) have never
executed since being wired into run_cadence.py -- surfaced this cycle, ranked below the
lockdown's recorder/connector priority per standing instruction. Ranking otherwise unchanged:
#1/#2 (live track record / live connector) remain top since they gate everything downstream —
this cycle's engineering effort goes to #2._

| # | Gap | Why it matters (mechanism) | Trigger / plan | Owner | Added | Status |
|---|-----|---------------------------|----------------|-------|-------|--------|
| 88 | **Working tree on a forked branch -- 75/125 scheduled scripts absent** | 60% of the desk's cron-invoked organs die instantly on ENOENT, `run_live_guard.py`, `check_organ_liveness.py`, `run_drills.py`, `run_alert_canary.py` and the whole `check_*` fence suite among them. They still APPEND TO THEIR LOG on every fire, so the logfile mtime is minutes old and every freshness-shaped check in the desk passes on a dead organ -- the outage is invisible to exactly the instruments built to catch it. `deploy/pull_deploy.sh` is itself one of the missing files, so the tree cannot re-sync itself; that is why it ran ~6 days. Verified by reading log CONTENTS (all ENOENT), not mtimes. | Tree is on `claude/llm-auto-upgrade-verify-gcjac3`, forked from master at 3bf89cd (07-29); master is 419 commits ahead with 473 files this tree lacks. PLAN 2026-08-05 (next cycle, first item): merge master into the branch, union the diverged ledgers, renumber once, CI green, push. NOT done 2026-08-04 -- both sides changed substantial code (branch touches 150 files incl. `libs/execution/binance_live.py`), so it is a two-sided money-path merge needing its own session with CI on the result. No money at risk meanwhile: executor is testnet-pinned, stage S0 (verified). Branch fully pushed (origin == af6abe0) + tag `safety/pre-merge-20260804`. Detection is now permanent: `check_scheduled_scripts` in max_audit fires `scheduled-script-missing` (75/133) until resynced. | brain | 08-04 | open |
| 1 | **Live track record = 0 days** | The binding constraint on everything: sizing confidence, live keys, scaling. No feature work substitutes for calendar time. | Carry forward-validation day 21/40; fast-track gate ~2026-08-05 (needs NW-t>=1.65 + regime event; NW-t=1.86 today, regime_ok still False -- funding-vol 5.3e-05 vs 25th-pct bar 8.3e-05, 0 inversion/dislocation days). RULED 2026-07-17: the 07-13 dead-man incident/restart does NOT contaminate this clock -- verified in scripts/run_cashcarry_shadow.py that forward returns are computed from the continuous market-data funding/basis panel via cashcarry_returns(), fully decoupled from the live executor's operational state; the 3-day flat book only affected the informational web/portfolio.json deployed-equity tracker, never the promotion gate. No haircut applied. | brain | 07-16 | open |
| 2 | **Live connector not built** | When validation passes, the desk can't take the live step without it; building it AFTER the gate wastes gate time. | BUILT 2026-07-26 (§3-§6 complete and WIRED): `libs/execution/protective_stops.py` (venue-side reduce-only stops at the ruin line + no-naked-position invariant, 60s grace, clock persists across process death; partial/split-stop coverage compared by QUANTITY not presence), `libs/ops/derisk_ladder.py` (15m cancel+halve / 60m flatten / 4h disarm; monotonic, top rung latches until human re-arm), `libs/execution/canary.py` (6h probe; UNKNOWN history reads as degraded, never healthy), `libs/execution/ramp_gate.py` (the five numeric conditions, fail-closed on missing evidence; down-steps unconditional). THE FIX THAT MATTERED: `scripts/run_live_guard.py` -- the first production caller. Before it, `libs/execution/staging.py`, `binance_live.py` and `binance_spot_live.py` were imported by their own tests and NOTHING else for 8 days, while this row read PARTIAL and the audit read clean; `check_orphan_code` is package-granular and libs/execution is reachable via ea_bridge, so it could not see them. Now on the daily cycle, with `check_money_path_wired` (module-granular) blocking any money-path module that loses its production caller, and the `live_connector_prebuild` detector changed from `binance_live.py exists()` to an actual wiring check. MUTATION TESTING (row 53): `scripts/run_mutation_test.py` -- mutates a SCRATCH COPY, so TIER-3 `run_deadman_switch.py` is never written to. First measurement ever: staging 63%%, sizing 73%%. Every survivor was a gate boundary nothing pinned. 81 boundary tests later: staging 82.9%% raw / 100%% of killable (6 documented equivalent mutants), sizing 90.9%%. It found a REAL fail-open: `critical_drill_failures` defaulted to 0, so absent drill evidence satisfied an S2 safety gate -- now defaults to 1. STILL OPEN for Gate 0 -- PRINCIPAL DEADLINE RE-DEFERRED 2026-08-02 to DEADLINE 2026-08-23 (the 2026-07-31 date passed unreported; caught by scripts/rerank_gaps.py on the cycle it landed. Re-deferred with a specific reason rather than more time: the remaining work is gated on VPS reachability for canary round-trips, and that same box is the binding constraint for #96, which expires sooner): (a) §7 second-model-family fuzz/breaker report on the 5 money-path files -- a PANEL task (scripts/deep_review.py, 13 seats), not self-servable by the same model; (b) §7b 13-model pre-mortem; (c) DRILLS: CLOSED 2026-07-30 -- `scripts/run_drills.py` (weekly, manifest) exercises host-death naked-clock persistence, de-risk ladder monotonicity/latching/clock-skew, and ruin-rail re-entry; 19/19 checks pass and it emits the `critical_drill_failures` field s2_entry_met reads, which had NO producer at all. NO drill places or cancels an order. tests/ops/test_drills.py BREAKS each rail and proves the drill catches it -- a drill that cannot fail is theatre; (d) §5 canary is a signed READ not an order round-trip (placing orders on an unattended timer pre-Gate-0 is a larger risk than the one it mitigates -- revisit at S1). Keys remain a HUMAN step; every module is inert with no keyfile. AT S1/GATE-0 ENTRY (principal decision 07-17): generation cadence upgrades to WEEKLY, scoped to the newest live-minted data (own fills, slippage/adverse-selection, recorder tape); graveyard exclusion + pre-registration unchanged; test-count UNCAPPED -- multiplicity corrections scale with the true tested N (principal 2026-07-20). | brain | 07-26 | in-progress |
| 3 | **Pager delivery unverified on new topic** | Alerts only matter if they arrive; quota was exhausted 07-11→07-16 (silent death, see KB). | Test page auto-fires ~2h post-fix to ntfy.sh/Quant_Alerts; principal confirms receipt. Watch `.last_alerts.json` success timestamps for 48h. SECURITY: guessable public topic — rotate to suffixed topic after confirmation. | operator+brain | 07-16 | open |
| 4 | **Fill-quality ledger** | avg_fill() now records venue-truth entries; nothing yet aggregates realized slippage to calibrate _DEPTH_MULT and cost models — guard thresholds are hand-set. | DEFERRED WITH DEADLINE 2026-08-05 (dated 2026-07-26 -- the row carried no date since 07-16, which is the parked state §36(3) forbids). Trigger: >=100 closes recorded AFTER the 2026-07-22 entry-gate/min-hold ship, so the sample reflects the CURRENT open path rather than the churn regime #42/#43 removed. Scope narrowed by what already shipped: run_cost_model.py (#45) supplies PREDICTED per-leg book-walk cost and the 250-trade economics audit (#42) supplies realized net-by-holding-time -- neither calibrates `_DEPTH_MULT`, which is still hand-set. Remaining unique work is exactly that: realized entry-vs-ticker delta per name -> depth-guard multiplier. If the 08-05 sample is still thin, the honest exit is to retire this row into #39 (recorder-vs-book coverage) rather than re-defer. | brain | 07-16 | open |
| 5 | **Data-breadth clocks immature** | OI/LS/liquidation 19/40d, stablecoin 15/40d — derivative alpha column gated on these; nothing to build, only protect uptime. | Collectors now survive kill-idle (07-16 fix); verify daily in health.json. Clocks mature ~07-29 (OI/LS) / ~08-11 (stablecoin). DATA-TRIGGERED GENERATION (principal idea 07-17, adopted): the moment a family matures (or FRED/recorder opens), the brain fires a SCOPED PANEL_MISSION=generate run for that family only (graveyard-excluded, pre-registration mandatory) -- generation is triggered by fresh DATA, never by the calendar; daily generation stays rejected (390/0 evidence + DSR-deflation cost, pilot ~08-15 arbitrates scaling). | brain | 07-16 | open |
| 6 | **Factory pilot needs its 30 days** | Settles scale-or-not (compute rental, generation throughput) with evidence instead of vision. Do NOT re-litigate before data. | Clock started 07-16 → decision ~08-15 from survivors-per-1000 + info-bits. | brain | 07-16 | open |
| 7 | **Event-triggered instant audit** | Incidents currently wait ≤24h for the daily micro-audit; a deadman fire or CI-red could summon 1-3 auditors within minutes. | RETIRED 2026-07-26 WITH REASON (not deferred -- inventing a deadline nobody would keep is the parked state under another name). The row's own gate (micro-audit hit-rate >=1 verified actionable finding / 2 weeks) was MET -- micro-audits 07-18/07-19 produced rows #30, #35, #36. It is retired anyway because both halves of its premise moved: (a) DETECTION is now continuous and free -- scripts/max_audit.py sweeps organ liveness, stub deaths, CI, coverage floors and rotting findings every run with 48h auto-escalation to the principal page, so an incident no longer waits <=24h to be NOTICED; (b) the only remaining delta -- summoning 1-3 PAID external auditors within minutes of a DEADMAN_FIRED/CI-red event -- is precisely the uncapped-spend shape that caused the 2026-07-24 budget incident ($21.48 in one day) and is now blocked by the monthly envelope guard shipped in response. Spending the envelope on incident-time panels would defund the scheduled panel that finds structural defects. RE-ENTRY CONDITION (explicit, so this is reversible): if any future incident post-mortem shows external eyes were needed sooner than the next scheduled panel, re-open with a bounded per-incident cap inside the envelope. | brain | 07-16 | retired-2026-07-26 |
| 8 | **FRED macro feed — assessed, no survivor this pass** | Free macro family (rates/curve/VIX/dollar/Fed-BS/M2) for macro-crypto relative value; key placed by operator 07-16, collector live with real data (DGS10 4.55, T10Y2Y 0.41, VIX 15.67, DXY 120.5, WALCL, M2SL). | DONE 07-17: brain EV-gated 3 pre-registered hypotheses (macro_carry_regime_gate, dollar_strength_funding_dislocation, basis_momentum_carry_timing) via alpha_economics.py — all 3 REJECTED below EV threshold (0.0039-0.013 vs 0.05 bar); logged to research_agenda.json do_not_repeat. Overlay/conditioning ideas structurally score low in this formula (est_sharpe = refinement not new stream). Family stays live for future overlays; revisit only on a materially new mechanism. | brain | 07-16 | assessed (no survivor) |
| 9 | **kimi-k2.6 reliability watch** | 1 failure / 2 runs on 07-16; a flaky auditor pollutes scorecards. | Monthly governance: if hit-rate/response-rate lags roster, replace via scorecard rule (never catalog metadata). | brain | 07-16 | watch |
| 10 | **Venue-truth equity visibility** | Mark-based history before 07-16 understates the NOM damage; the deadman's independent measure was invisible outside its state file — the honesty gap that hid a −41% event. | DONE 07-16: `venue_truth` block in live_combined.json + `web/venue_equity.json` feed (equity, HW, fire line, breaches), refreshed every 3 min + fed to audit dossier. REMAINING: render it as a dashboard HTML tile (brain, small). | brain | 07-16 | feed-live, tile-pending |
| 13 | **NO OFFSITE BACKUP / disaster recovery** | Single Hetzner disk holds the ledger (32 decisions), knowledge base, memory, state, lake, and the new git history. One disk failure or account issue loses the desk's institutional mind; laptop copy is frozen at 07-12. | HUMAN CHOICE (both cheap): (a) Hetzner auto-backups (~€1/mo, one click in console), or (b) free private GitHub repo — operator creates repo + deploy token, brain wires `git push` into the daily git_snapshot step (code+docs+ledger offsite; lake excluded, re-fetchable). Recommend (b), or both. RESOLVED 2026-07-16: operator enabled Hetzner auto-backups (console-side; not verifiable from inside the guest -- operator should confirm the first snapshot appears in the Hetzner console within 24h). Optional upgrade later: free private GitHub remote for file-level git history offsite. | operator+brain | 07-16 | resolved (verify first snapshot) |
| 11 | **Tax-aware sizing** | Kelly on pre-tax returns overstates growth once live; frictions belong in the objective. | Activates AT LIVE CAPITAL: operator supplies jurisdiction/rate; shrunk-Kelly gains a tax-drag term. | brain (at live) | 07-16 | queued |
| 12 | **Net-exposure consolidation + Effective-N gate** | Only matters when >1 live sleeve trades real capital simultaneously. | Activates at multi-sleeve live. | brain (at live) | 07-16 | queued |
| 14 | **Leverage-optimizer confidence pipeline contaminated** | Confidence 0→0.89 in one day on a reset-re-anchored curve flipped dynamic sizing active at 8x — incident #2. The clamp is a tourniquet, not a fix. | BRAIN PRIORITY: root-cause the jump (suspect variance collapse on the post-reset window feeding kelly-shrink); design the re-enable gate (≥30 uncontaminated live days + principal sign-off); until then the executor clamp stands. UPGRADED 2026-07-18: the SAME contaminated pipeline (conf 0.92) was found sizing the book DOWN to ~$1,250 (25% deployed) -- the 07-16 clamp only capped the UPSIDE, so bad confidence under-deployed ~75% of authorized capital (the growth_defect alert was a TRUE positive, NOT depth-justified as first misdiagnosed). `_dynamic_capital` clamp UPGRADED to a full QUARANTINE: it now ignores the optimizer in BOTH directions and returns the operator's --capital until the root-cause + >=30-day re-enable gate ships. Root-cause + gate STILL OWED (this row stays open). | brain | 07-16 | open |
| 15 | **HFTUSDT position 1.28x book capital** | Real, delta-neutral, funding-positive, but single-name concentration ~3.7x the 35% cap; exit through a thin book costs real slippage. | RESOLVED 2026-07-18: HFTUSDT closed 2026-07-17T14:03:56Z (`data/cashcarry_trades.json`, net +$21.71) — swept up in the dead-man fire #3 flatten. Current book (verified 07-18): 6 positions, largest 26.0% of $1,740 deployed notional, all under the 35% cap. No concentration breach remains. | operator+brain | 07-16 | closed |
| 16 | **Reconciler fails silently** | Rejected re-hedge orders return '' and log nothing — COOKIE's broken pair sat invisible 75 min. Silent failure in the survival-critical loop. | DONE 2026-07-17: `_reconcile`'s `_do()` wrapper counts consecutive `_mkt_or_limit` failures per symbol (persisted `state["reconcile_fail_counts"]`, mirrors the existing `cooldown` pattern); 3rd consecutive failure emits a `RECONCILE-FAIL` action line + writes `data/cashcarry_error.log`. 2 new regression tests (tests/execution/test_reconcile_fail_counter.py), CI green. | brain | 07-16 | closed |
| 17 | **External heartbeat (off-box dead-man for the box)** | Deadman, pager, watchdog all die WITH the host; today's pager silent-death shows monitoring can fail invisibly. An outside service must notice silence. | Ping code SHIPPED in run_alerts (3-min tick, graceful skip). HUMAN (2 min): create a free healthchecks.io check (expect: 5-min period, 15-min grace, alert to phone/email), put its ping URL in data/secrets/heartbeat_url.json as {"url": "..."}. | operator (signup) | 07-16 | wired-awaiting-signup |
| 18 | **Mainnet market-data recorder (data moat)** | The one asset that cannot be backfilled: L2 top-20 @1s + aggTrades + funding/liquidations on top-5 mainnet symbols feeds future TCA, execution research, replay drills. Every non-recording week is permanently lost. | BRAIN: EV-gate with real numbers first (disk GB/mo, CPU on the 4GB box, cold-rotation to the Hetzner backup volume); if green, build as an independent systemd-style unit, watchdogged, integrity-reported in health.json. EV STRENGTHENED by tier1 panel consensus 07-17: public tick/depth recording enables a REALISTIC EXECUTION COST MODEL BEFORE live fills exist (name-specific slippage curves -> depth-guard + cost-model calibration pre-Gate-0) -- the recorder is not just future moat, it is pre-live TCA. STATUS: v1 BUILT+LIVE 2026-07-17 23:16Z per principal terminal directive (priority #1) -- run_recorder.py, 5 perps, depth@1s + aggTrades, heartbeat+pager+respawner; brain upgrades to websocket/parquet + cold rotation per spec. | brain | 07-16 | ev-gate-pending |
| 19 | **Venue-truth divergence circuit breaker** | Panel tier1 consensus find (07-17): executor-book PnL and dead-man venue-truth equity now BOTH exist as feeds, but nothing trips when they diverge -- the mark-vs-reality gap that hid a -41% event would still only surface via audits. A band breach should PAUSE NEW OPENS + page automatically. INDEPENDENTLY CORROBORATED 2026-07-17: 2/11 tier1 panel models (google/gemini-3.1-pro "Venue-Truth Disconnect Circuit Breaker", moonshotai/kimi-k2.6 "Venue-Truth-First Risk Measurement") proposed the same mechanism unprompted -- raises confidence this is a real gap, not a one-off idea. | BRAIN: small build -- compare live_combined net vs venue_truth delta each refresh tick; divergence > band (calibrate ~2x observed noise) -> RISK-PAUSE-OPENS + page (NOT a flatten -- must stay clearly distinct from the Tier-3 dead-man's drawdown-from-HW survival trigger; this is a books-vs-venue reconciliation check, a different mechanism, and must never write to the dead-man's state file -- two-writers-on-one-rail was the 07-11 false-fire root cause). Needs property/mutation testing per the v8 8.2 bar (same standard as the live connector) -- dedicated build, not a same-cycle patch. Independence gate: touches risk path -- do not co-window with other risk-path changes.  **SHADOW FINDING 2026-07-23 (observe-only sampler shipped: scripts/run_venue_divergence_shadow.py, cron */5):** the spec's LEVEL comparison is NOT ARMABLE -- both feeds are fresh and identically timestamped, yet sit ~36.4% apart BY CONSTRUCTION (live_combined marks a 15,000-based book; the dead-man measure is fut margin + TRACKED legs + USDT delta and deliberately excludes untracked faucet spot). A level-vs-level band would trip instantly and PERMANENTLY on a definitional offset. The correct signal is divergence of INCREMENTS: measured |d(mark)-d(venue)| = 0.0071% (early n), giving an armable band of ~0.014% (2x observed increment noise). Arming is still gated on >=200 clean samples spanning rebalances + >=1 regime event, plus the property/mutation bar and the independence gate. | brain | 07-17 | open-high-rank |
| 20 | **Stripped-context probe (quarterly)** | Every audit reads OUR narrative; narrative-blindness is the one blind spot the owner-question cannot catch. One auditor per quarter gets RAW artifacts only (health, trades tail, equity curves, positions -- zero docs/labels) and answers: what is this system and what kills it? | DEFERRED WITH DEADLINE: BUILD due 2026-08-31, FIRST FIRE in the 2026-Q4 window (dated 2026-07-26; undated since 07-17). Build = raw-artifact dossier generator (sanitizer-gated, no prose from docs/) + stripped.txt mission + run_cadence wiring (state key last_stripped_probe, 90d). Independence gate: panel subsystem. Deadline is 08-31 not sooner for a stated reason, not a shrug: July's engineering slot is owned by the #2 connector 07-31 deadline and the #49 prerequisite, and the panel subsystem is currently degraded (panel_verdicts 111h stale, 15 stub-deaths in 48h) -- wiring a new quarterly panel mission onto a rail that is not producing would be ceremony. Build it AFTER the panel is producing again; one build serves this row and #21. | brain | 07-17 | open |
| 21 | **Quarterly tier-1 gap-map regeneration** | If the register only ever grows from its own past, closed-gaps can mean a stale map while the tier-1 frontier moves. The target list itself must be re-derived, not just consumed. | DEFERRED WITH DEADLINE: BUILD due 2026-08-31, FIRST FIRE in the 2026-Q4 window (dated 2026-07-26; undated since 07-17). One panel seat re-derives the tier-1 capability list FROM SCRATCH -- no sight of this register -- then the brain diffs it against these rows; anything missing enters as a new open row with rank. State key last_gapmap_regen (90d) in cadence. Paired with #20 by design: same build (mission + cadence key + a no-register-context guard), same window, one slot -- and the same gating reason (panel rail must be producing first). | brain | 07-17 | open |
| 22 | **Negative-space explorer** | Research herds toward fashionable areas; nothing maps what has NEVER been tried. | DEFERRED WITH DEADLINE 2026-08-15 -- the monthly governance window (dated 2026-07-26; undated since 07-17). Cheapest of the four queued panel variants: it is a MISSION PROMPT plus a graveyard/family-map dossier, not a build, so it rides the next monthly panel rather than needing its own slot. Given family map + graveyard, name the 3 least-explored economically-plausible spaces + one falsifiable probe each. Ranked above #20/#21 for the same reason it is cheaper: it directly attacks the measured discovery bottleneck (17/20 ingested axes with ZERO screened hypothesis) instead of auditing the audit. Spec per standing rule before the window. | brain | 07-17 | open |
| 23 | **Semantic clustering pre-gauntlet** | Generated candidates include near-duplicates beyond content-hash reach; testing variants burns DSR budget. | DEFERRED WITH DEADLINE 2026-08-12 (dated 2026-07-26; undated since 07-17) -- and PROMOTED in this cycle's rank, because the evidence for it arrived after the row was written: 420 candidates tested, ZERO survivors, with dsr/pbo/reality_check each rejecting >=98%. Multiplicity corrections scale with the TRUE tested N, so near-duplicate variants are not free -- they deflate every genuine candidate's DSR while adding no information. Clustering is the one lever that raises survivor probability WITHOUT lowering a bar: embed + cluster candidates pre-gauntlet, test one representative per cluster, and count the cluster (not its members) as the tested unit. Build: spec per standing rule, then wire into run_discovery ahead of the gauntlet. Research lane, no risk path, CI-gated. | brain | 07-17 | open |
| 24 | **Edge-decay laboratory** | The deployed edge's #1 risk is slow crowding decay -- invisible to daily PnL. Formalizes carry_crowding_monitor (standing top research-ROI item): half-life estimate, funding-compression trend, crowding proxies, cost sensitivity -> sizing haircut hook (clamp-respecting). | DEFERRED WITH DEADLINE 2026-08-15 -- spec + build in the monthly window (dated 2026-07-26; undated since 07-17). SCOPE SHARPENED by a finding that landed after this row was written: improvement_inbox #55 (fill-rate decay as the alpha-decay DISCRIMINATOR) gives the lab its missing branch logic -- fill-ratio collapse with stable vol = permanent structural kill; stable fill-ratio with collapsed realized vol = a regime pause that must NOT be graveyarded. The lab therefore branches on (fill-ratio, realized-vol) pairs, not on P&L alone. Prerequisite is cheap and can land first: log per-sleeve fill-ratio + realized vol alongside P&L. This protects the ONE deployed edge against its #1 risk (slow crowding decay, invisible to daily PnL) and prevents false kills of a live vol-conditional edge -- the graveyard is permanent, so a wrong entry is unrecoverable. | brain | 07-17 | open |
| 25 | **Sequential / always-valid forward validation** | The ONE principled way to make forward shadows faster WITHOUT lowering standards: replace the fixed 40/90-day clock with always-valid inference (e-values / mSPRT / anytime-valid CIs) so a hypothesis GRADUATES the instant its evidence is decisive -- a strong edge can clear in weeks, a weak one takes longer or never, and type-I error stays controlled under optional stopping. | RETIRED 2026-07-26 WITH REASON -- the row's premise was TESTED and REFUTED by the desk's own measurement, so a deadline would be dishonest bookkeeping. See row 25-RESULT (2026-07-22): libs/research/anytime_valid.py was BUILT and Monte-Carlo'd. Type-I control verified (null 3/300 = 1.0% at alpha=0.01), but a Sharpe~2 edge graduated in a MEDIAN 132 days against the fixed 90-day clock, and a Sharpe~0.3 edge never (0/40). The reason is fundamental, not implementation: per-observation signal on daily returns is ~0.105, so the e-process grows ~0.0055/day and needs ~800 days to reach log(1/alpha). This row was opened as "the ONE principled way to make forward shadows FASTER"; it is strictly slower. Its genuine yield was kept, not discarded: e-values are adopted as the peek-safe statistic on the derivative shadow (#45) and as a stricter SECONDARY check for high-stakes promotions, and the search for legitimate accelerants it triggered produced #44 (8h funding panel: vif 1.008, ~sqrt(3)x evidence rate) -- which is the real speedup. RE-ENTRY CONDITION: revisit only if the desk ever validates on INTRADAY returns, where per-observation signal is high enough for an e-process to cross quickly. The 90-day clock stands. | brain | 07-18 | retired-2026-07-26 |
| 26 | **YouTube/video transcript access (paid, EV-gated)** | Direct transcript fetch is IP-blocked from the VPS (tested 07-18: RequestBlocked). High-signal quant video content is currently reached only via text mirrors -- good, not complete. | OPTIONAL SPEND, triggered only if the Prospector coverage log shows video-locked mechanisms are a recurring binding blocker: residential-proxy or paid transcript API (~-20/mo), EV-gated + principal-approved (spend = human decision). Do NOT buy pre-emptively. | brain->principal | 07-18 | ev-gated-optional |
| 27 | **Conservatism audit covers only 2 of N surfaces** | The AUTOMATED growth audit (the one that pages) checks capital-utilization + leverage only. Timidity on OTHER surfaces -- exploration-budget under-use, over-strict validation gates, symbol/breadth under-deployment, dig-depth timidity -- is caught only qualitatively (growth mandate + daily micro-audit), not by a numeric pager check. | BRAIN (post-freeze or as a monitoring enhancement): enumerate every conservatism surface in run_growth_audit as its own numeric check with an evidence-or-defect verdict, so the anti-timidity scan is automated across the WHOLE desk, not just capital+leverage. Each surface needs a lifting condition. Spec-prebuild per standing rule. | brain | 07-18 | closed-2026-07-21: scripts/max_audit.py is the desk-wide automated sweep (organ liveness, stub deaths, coverage floors, rotting findings, idle capabilities; ack-registry w/ 30d max; 48h auto-escalation to principal page); run_growth_audit keeps the capital/leverage numerics |
| 28 | **Full-depth random-component audit** | The cold panel sees a SANITIZED SUMMARY (security + context limits), not raw code -- so review breadth is complete but per-component DEPTH varies. No loop pulls ONE random component apart to the bone. | BRAIN (monthly): pick one random live/shadow component, audit it down to raw code + data lineage + execution logs -- verify point-in-time correctness, no look-ahead/leakage, live==research within bounds; any divergence -> root cause + fix same cycle. The internal-auditor complement to the external summary panel. DEFERRED WITH DEADLINE: first component audit by 2026-08-08, monthly thereafter (dated 2026-07-26; this row was parked undated since 07-18). Costs ZERO external spend -- the brain does it -- so unlike #20/#21 it is not gated on the panel rail; it is gated only on the connector slot. First target pre-selected to remove the "pick one later" excuse: `libs/execution/staging.py` + the S0/S1/S2 promotion path, because it is the component that will gate real money first. | brain | 07-18 | queued |
| 29 | **Digging + self-improvement cadence duties never executed** | data/cadence_state.json has never had last_prospector / last_lit_deepdive / last_blind_rediscovery / last_decision_scoring / last_memory_consolidation keys set -- these duties have literally never run since being wired into run_cadence.py, even though cadence_duties.md re-flags them every cycle. No pager floor fires because a never-run duty is deliberately exempt from the floor check (avoids paging for brand-new cadence items), so this can silently persist. | BRAIN: execute across upcoming cycles once recorder/connector/Gate-0 free up cycle time (explicit standing instruction: lockdown priorities own the cycle first). decision-outcome-scoring checked 07-18 -- zero ledger entries are yet >=30d old (earliest 07-04), so nothing to score yet; legitimately not a defect today, re-check ~08-03. Prospector/lit-deepminer/blind-rediscovery/memory-consolidation remain genuinely un-run and should get real cycle time as soon as the connector build has a stable increment shipped. | brain | 07-18 | open |
| 30 | **No schema-contract/replay-verification on recorder + venue-truth reference values** | Micro-audit 07-18 (2 auditors independently): no freshness-SLA/drift checks on `run_recorder.py` or `crowding.json`; and no proof that the dead-man's HWM/latch-equity/rebaseline numbers can be deterministically reproduced from raw retained events (timestamp-aligned, like-for-like valuation snapshots). Not urgent (recorder v1 is 1 day old, nothing yet promotes on it) but a real data-lineage gap once the recorder starts feeding TCA/execution research. | BRAIN: spec-prebuild a lightweight schema-contract check (null/monotonicity/freshness) on recorder output + a replay harness that reconstructs a known HWM/equity snapshot from raw events end-to-end. Low priority vs recorder/connector/Gate-0; pick up once those have a stable increment. | brain | 07-18 | open |
| 31 | **CI gate covers only ~5 of 15+ test directories** | Discovered 07-18 while wiring in new connector/staging tests: `scripts/run_ci.py`'s pytest step names 4 hardcoded top-level files + (now) `tests/execution/` -- everything under `tests/risk/`, `tests/portfolio/`, `tests/features/`, `tests/regime/`, `tests/autodiscovery/`, `tests/factory/`, `tests/ops/`, `tests/stage14/`, `tests/integration/` etc. is NEVER run by any automated process. A full-tree `pytest tests/` also currently fails to even COLLECT (duplicate basenames across dirs: two unrelated `test_regime.py`, two unrelated `test_registry.py` -- no `__init__.py` packages). Tests could silently rot in any of the ungated directories with nothing catching it. | BRAIN: (a) quick win -- add `__init__.py` to each test subdirectory (or configure pytest's `--import-mode=importlib`) to fix full-tree collection; (b) once collectible, widen `run_ci.py`'s pytest step to the whole `tests/` tree (or explicitly enumerate every directory) so nothing is silently unchecked. `tests/execution/` (risk-path) already added 07-18 as a partial fix. **CLOSED 2026-07-26, VERIFIED IN CODE (not assumed):** `scripts/run_ci.py` `_STEPS` now runs `pytest tests/ -q` over the WHOLE tree (comment dated 2026-07-25: "was 4 named files + tests/execution = ~147 of ~1099 tests"), so tests/risk (the ruin path) and tests/validation (the anti-false-positive path) are gated; full-tree collection works, which retires the duplicate-basename blocker. max_audit's own `ci-scope-partial` check no longer fires. Nothing owed. | brain | 07-18 | closed-2026-07-26 |
| 32 | **Held carries never resize up -> book can stick undersized** | The executor sizes opens from FREE capital only and never resizes a held carry (`if sym in pos: continue`, run_cashcarry_executor.py). Deliberate anti-churn / anti-2026-07-13-concentration design, but it has a failure mode: any carry opened during a low-free-capital window stays that small forever unless it rotates out. Exposed 07-18: with the optimizer capping capital at $1,250, 8 carries opened at ~$29 each; after the quarantine fix restored $4,500 the book still reads $1,150 deployed because those 8 are held and frozen small. VERIFIED post-fix (07-18 22:5x): does NOT converge to $4,500 -- `_alloc` spreads `free` across all 10 candidates by funding weight but only the ONE non-held name opens each cycle (others skipped as held), so each rotation deploys only ~$200-350 (observed: COOKIE opened $203 not ~$450) while the 8 frozen tiny carries never grow. Net: book CREEPS up and PLATEAUS well below $4,500. The `_dynamic_capital` fix was NECESSARY (removed the hard $1,250 cap) but NOT SUFFICIENT for full deployment -- this gap is the real binding constraint. | BRAIN (spec + TEST before live -- this is the ruin-incident function; NO same-cycle hot-patch): guarded resize-UP-only toward target allocation through the EXISTING open path (reuse the 0.35 water-fill cap + thin-book depth guard), hysteresis-banded to avoid churn, never sizes down. Property/mutation test to the v8 8.2 bar; independence-gated (risk path -- do not co-window with other risk-path changes). Meanwhile the quarantine fix + natural rotation redeploy the book. | brain | 07-18 | open |

## 2026-07-19 status updates (operator-directed audit-gap closure)

- **#14 (leverage pipeline)** — FORENSIC COMPLETE -> `docs/research/GAP14_ROOTCAUSE.md`. Root cause (evidence-backed, read fresh): variance-collapsed forward Sharpe (16.09) from the funding-smoothed molded curve + a fwd_days counter that never reset at the 07-16 incident; `shrink_fraction=S^2/(S^2+SE^2)` cannot defend a mis-measured point estimate, so conf=0.92 and `active=conf>0` flipped the executor off operator capital. Fix (mark-to-market fwd returns + Sharpe plausibility rail + counter reset + >=30d re-enable gate) QUEUED post-Gate-0. NO code changed (freeze); executor quarantine holds. Row stays open.
- **#19 (venue-truth reconcile guard)** — SPEC BUILT -> `docs/research/GAP19_RECONCILE_GUARD_SPEC.md`. PAUSE-not-flatten, never writes the dead-man state, property/mutation-tested before live. QUEUED post-Gate-0.
- **#32 (guarded resize-up)** — SPEC + TESTED IMPL -> `docs/research/GAP32_RESIZE_UP_SPEC.md` + `data/patches/gap32_topup.patch`. Built + unit-tested (7 tests) + exec suite green 07-19, then REVERTED from live to honor the freeze (operator: queue for post-Gate-0). Re-apply verbatim at Gate-0. Book stays ~20%% deployed meanwhile (safe direction, paper book).
- **#29 (digging duties never run)** — Prospector FIRST RUN executed 2026-07-19 via the VPS brain (zero cards survived -- honest 'nothing new'; Chinese frameworks = equity factor zoos/infra shells, no crypto-perp alpha; RSRS ev=0.0002 reject). `last_prospector` set; blind-rediscovery baselined 07-19; lit-deepminer + memory-consolidation left DUE next cycle.
- **#32 UPDATE (later 2026-07-19):** operator directed FULL DEPLOYMENT -> RE-APPLIED LIVE (not queued). Risk-gate bug (gated on action=='none'; normal state is 'ok' -> topups never fired) caught by LIVE verification (unit tests covered the pure fn only) + fixed. Book 20%->100% ($4,504), balanced, one-time settling transient, no ongoing churn (verified). Gate-0 re-enable of the leverage optimizer (gap #14) still separate.

## 2026-07-19 re-rank (incident cycle: dead-man fire #4, 14:27Z) -- no item >7d stale, no forced escalations. New rows below ranked ABOVE the standing #1/#2 lockdown pair only where they are themselves survival/alerting-critical; #1 (live track record) and #2 (connector) remain the calendar-time-gated top priorities otherwise.

| # | Gap | Why it matters (mechanism) | Trigger / plan | Owner | Added | Status |
|---|-----|---------------------------|----------------|-------|-------|--------|
| 33 | **Pager silently dead for 29h+ across a real dead-man fire (Unicode encoding bug)** | `scripts/run_alerts.py` `_push()` built the HTTP `Title` header with a raw emoji (`⚠️`/`✅`, added 2026-07-18T18:05Z); `urllib`/`http.client` encode headers latin-1, so every push since then raised `UnicodeEncodeError` BEFORE any network call -- 39/39 consecutive failures, zero successes, verified via `journalctl -u quant-refresh.service`. The principal was never paged about today's dead-man fire (or anything else) for the full window. The `--test` self-check path uses a separate ASCII-only string and never exercised the broken code path -- classic test/prod divergence. | FIXED this cycle (maintenance exception, not architecture): emoji stripped to ASCII markers at both call sites + defensive `title.encode('latin-1','ignore').decode('latin-1')` backstop in `_push()` so a future non-ASCII edit degrades gracefully instead of killing all paging silently. Verified: UnicodeEncodeError gone. Live re-test then hit a REAL `ntfy.sh` 429 (rate-limit) -- separate, not yet confirmed resolved; only 2 manual attempts logged today (no prior 429s in today's systemd logs), consistent with a short burst limit rather than exhausted daily quota, but unconfirmed pending a spaced-out retry. Residual: confirm a real push succeeds within 24h; if 429s persist beyond a burst window, treat as a recurrence of the 2026-07-16 quota-exhaustion class and consider a second delivery channel (the wired-but-unverified `heartbeat_url.json` external heartbeat only proves the BOX is alive, not that a SPECIFIC alert was seen). | brain | 07-19 | fixed-verifying |
| 34 | **Dead-man `combined_equity()` has a documented-but-unguarded leg/cash race during rapid rebalance churn -- AND the $1.8-2.6k gap is UNRESOLVED, not "modest slippage"** | Incident autopsy (2026-07-19 14:27Z fire, `data/INCIDENT_20260719_DEADMAN.md`): equity at latch read ~$785 (5 consecutive polls <65% of $5,047 HW); reconstructing from fresh venue reads ~4.5h later gives ~$2,409 -- futures margin balance stable ($4,247, zero positions/unrealized) both then and now, no orphaned spot bags, only ~-$25 realized PnL from the flatten itself. My FIRST-PASS read called the ~$1,838-2,638 gap "modest execution slippage/fees, no catastrophic loss." **CORRECTED same cycle by the full 13-model incident panel (12/12 responded, near-unanimous on this point)**: that framing is REJECTED -- the gap is 36-52% of high-water and is not yet attributed to any specific fill/fee/funding/transfer record; it must be treated as an UNRESOLVED accounting break (openai/gpt-5.6-terra, openai/gpt-5.6-luna-pro, google/gemini, ai/glm-5.2, mistralai independently converged on this) until a double-entry venue reconciliation closes it, NOT waved away as slippage. Root-cause mechanism for the LATCH TIMING itself (same failure CLASS as dead-man fire #3, 2026-07-17): `legs_v` counts spot value ONLY for symbols with a currently-live futures short; a close/orphan-cover/open burst in the 16 minutes before the fire (close GTCUSDT 14:12:56Z, orphan-cover GTCUSDT ~14:23Z, open ONEUSDT 14:22:59Z) plausibly dropped legs out of the tracked-shorts set mid-settlement. Book is testnet-only; zero real capital at risk regardless of which explanation is correct. Panel dossier: `docs/research/panel_inbox.md` (2026-07-19T23:39Z). | TIER-3 NEVER-TOUCH: `scripts/run_deadman_switch.py` may not be modified autonomously under any circumstance -- this row can only ever reach PROPOSE, never PATCH, regardless of Gate-0/freeze status. **CRO's original proposed fix direction ("track legs via executor's own authoritative position state") was REJECTED BY PANEL CONSENSUS** (grok-4.5, gpt-5.6-terra, qwen, glm-5.2 independently): coupling the dead-man to executor state destroys its independence and trades a false-positive-prone rail for a false-negative-prone one (executor state is exactly what can be wrong during the failure modes the dead-man exists to catch). **Corrected fix direction** (principal-gated, not yet built, purely venue-native): value ALL non-USDT spot balances (not filtered by short-state) directly from venue reads every poll, no executor coupling; add a quiescence/plausibility bound (freeze HWM updates and require extra confirming polls when any single component moves implausibly during detected high order-density windows); treat dead-man vs mark-book divergence itself as a page-worthy signal. IMMEDIATE NEXT STEP (does NOT touch Tier-3 code, buildable without sign-off): a read-only reconciliation script mapping the $1,838 USDT delta to specific Binance testnet `myTrades`/income records before any reset decision. Reset remains principal-only, now explicitly gated on that reconciliation (see `data/PRINCIPAL_ACTION.md`), not just on pager delivery. | brain->principal | 07-19 | open-tier3-diagnosis-corrected-by-panel |
| 37 | **Orphan-cover reconciler path is an unbounded, unauthenticated market-order mechanism** | Panel consensus (2026-07-19 incident audit, 8+/12 models independently): the reconciler's "orphan cover" (detect leg/cash mismatch -> immediate market-order force-close) has no size cap, no persistence/confirm-window check, no venue-health gate, no idempotency key, and no cooldown. On live markets this is a live-ammo path: a transient REST desync or partial-fill lag can be mistaken for a real mismatch and market-cover into a thin book (50-150bps plausible per false cover per multiple independent estimates), and repeated/cascading covers during a real venue outage could itself breach the ruin constraint. The 2026-07-19 14:23Z GTCUSDT orphan-cover is flagged as a plausible contributor to the incident's unresolved $1.8k+ gap (see row 34). | NOT Tier-3 (reconciler is executor code, autonomously modifiable) but IS risk-path -- independence-gated: do not co-window with other risk-path changes (none touched this cycle; the pager fix is alerting-infra, not risk-path, so no conflict). QUEUED, not same-cycle: requires a persistence/confirm-window check (>=2-3 consecutive independent polls before acting), a notional cap + min-dust floor, non-market execution (limit/IOC with a bounded slip ceiling), and a per-symbol cooldown after firing -- property/mutation-tested to the v8 8.2 bar before touching live-path code, per standing risk-path discipline. Spec-prebuild next available slot. | brain | 07-19 | queued-high-priority |
| 38 | **Single-channel alerting confirmed insufficient by unanimous panel consensus -- encoding fix alone does not close the gap** | Panel consensus (2026-07-19, effectively 12/12 models raised this): the Unicode fix (row 33) closes ONE client-side failure mode but ntfy.sh remains a single provider/channel/topic with no delivery confirmation, no independent liveness check of the alerting pipeline itself, and no fallback -- the immediate post-fix 429 is live proof the channel alone is not yet trustworthy for a rail that can flatten unsupervised. Converged proposal shape across models: a second independent channel (different provider/network path, e.g. email or a different push service), a periodic synthetic heartbeat/canary distinct from the main alert path, and treating "both channels silent" itself as a page-worthy condition via an external (off-box) watcher. | BRAIN: queued, not same-cycle (real build: second provider integration + canary process + external liveness check). The already-wired-but-unverified `data/secrets/heartbeat_url.json` (gap #17) covers box-liveness only, not per-alert delivery -- note the distinction explicitly in the build spec so the two don't get conflated. Spec-prebuild next available slot; this and row 37 are the two highest-priority queued items to come out of today's incident. | brain | 07-19 | queued-high-priority | closed-07-30 (libs/ops/alert_channels.py second channel + canary + delivery ledger; pager metric now FLATLINE-visible in check_ratchets) |
| 35 | **Recorder tracks perp depth/trades only -- no spot pairs, for a delta-neutral desk whose every trade is spot+perp** | Micro-audit 07-19 (google/gemini-3.1-pro), VERIFIED against code: `scripts/run_recorder.py` `_SYMBOLS = (BTCUSDT, ETHUSDT, SOLUSDT, BNBUSDT, XRPUSDT)` is futures-only (`fapi`); no spot depth/trade collection exists anywhere in the recorder. The stated purpose of recorder v1 (gap #18) is pre-live TCA / execution-cost-model calibration ahead of Gate-0 -- but every carry trade has an equal-weight SPOT leg, and spot liquidity/slippage on the smaller-cap carry names is plausibly the more binding cost, not the perp leg. A cost model built from perp-only ticks would silently mis-price half of every trade. | **DONE 2026-07-21**: built `scripts/run_recorder_spot.py` (standalone mirror of run_recorder.py, Bybit-recorder supervision pattern). Binance SPOT (api.binance.com), 20 liquid symbols (same set as the futures recorder), top-20 depth@4s + aggTrades@20s -> `data/moat/spot/{sym}/` gzip-jsonl hourly, own heartbeat `data/recorder_spot_heartbeat`, boot-time exchangeInfo symbol validation, SPOT weight guard (6000/min budget, 1740/min steady = 36%; SEPARATE per-IP bucket from the futures 2400/min budget). 10-min pgrep self-heal cron added (mirrors Bybit). VERIFIED LIVE from this VPS: spot endpoints reachable + correct shapes, all 20 symbols valid spot pairs, data flowing for all 20, heartbeat fresh, CI green. Ledger `2026-07-21-recorder-spot-leg-gap35`. Follow-up (small, queued): recorder_spot_heartbeat has no run_alerts staleness pager yet (parity with Bybit -- relies on cron pgrep-guard). | brain | 07-19 | closed-2026-07-21 |
| 36 | **Cadence-duty cadence decisions (e.g. biweekly digging) rest on estimated, not measured, brain time-allocation** | Micro-audit 07-19 (deepseek + qwen, independently): the 2026-07-18 biweekly-digging decision (`2026-07-18-digging-cadence-biweekly`) cites "~90 min/day" as its cost basis -- VERIFIED against the ledger entry: this is a stated estimate, not an instrumented measurement, and no code anywhere logs wall-clock/token time spent per cadence-duty category per cycle. The entry's own `reversal_condition` ("connector milestones slip due to digging time -> revert to 28d") is real but currently unverifiable except by subjective judgment, since nothing measures it. Not urgent (today's actual slippage was incident-caused, not digging-caused) but the self-correcting mechanism the desk already committed to is not yet self-verifying. | BRAIN (cheap, additive, no risk-path): have each `run_cadence.py` duty and the daily cycle log its own wall-clock (or token) cost per category to a small state file; monthly governance item 6 (self-improving governance) can then check the biweekly-digging reversal_condition against real numbers instead of impression. Low priority vs recorder/connector/Gate-0. | brain | 07-19 | open |

**Micro-audit 07-19 findings NOT promoted to a row** (triaged, logged to `data/panel_verdicts.jsonl`): (1) gemini's claim that decision-outcome-scoring cadence "mathematically locks the desk in a 10-month freeze" -- REJECTED, false premise: `scripts/run_cadence.py:222` checks every 28 days (not "one row per month"), and ~100 ledger entries dating from 07-04 will cross their individual 30-day maturity threshold together in early August, so >=10 resolved calibration rows is achievable in one scoring pass around 08-03, not 10 months out. (2) deepseek's "deploy $100 to Binance mainnet within 24h via a thin adapter" -- HARD REJECTED, directly violates the execution-lockdown Gate-0 sequencing (connector must clear property/mutation testing + failure injection first); today's incident is itself live evidence for why that gate exists. (3) qwen's "score 63 historical decisions now" -- REJECTED, premature: oldest ledger entries are ~15 days old, not the 30-day maturity floor: scoring them now would fabricate resolved-looking outcomes for still-open bets, a calibration-honesty violation. (4) three BLIND SPOT questions (cross-sleeve correlation discipline, decay/capacity modeling, cross-regime stress-testing of queued post-Gate-0 items) -- FLAGGED for the next monthly governance window (kill-committee + tier-convergence items), not actionable same-cycle.
| 39 | **Recorder universe does not intersect the traded book -- cost model is unusable for real sizing** | Cost model built 2026-07-22 from 1.1GB of recorded L2 (`scripts/run_cost_model.py`, 20 symbols, median pair-open 1.902 bps @ $500/leg). Coverage check: the book holds AAVE/AGLD/BICO/CELR/COOKIE/EDU/EGLD/MANA/PEOPLE/XLM; the recorder holds BTC/ETH/BNB/SOL/XRP + 15 majors. **Intersection = ZERO.** The desk records liquid majors and trades thin high-funding small-caps, so every measured cost number is inapplicable to actual sizing, and the real (worse) small-cap slippage remains unmeasured. | BRAIN/OPERATOR: point the recorder at the symbols the book ACTUALLY trades (or add them alongside the majors, disk permitting on the 4GB box). Majors give a liquid benchmark; the traded names give applicability -- the desk needs both, but currently has only the useless half for cost calibration. Re-run run_cost_model.py once traded names accrue >=24h. | brain | 07-22 | open-high-rank | closed-07-30 (recorder universe = positions ∪ recent trades ∪ candidates, refreshed) |
| 40 | **`ensure_recorder.py` uses heartbeat-age as a liveness proxy -- 10-minute blind window after every crash** | Found 2026-07-22 while restarting the recorder: `ensure_recorder` treats the recorder as alive if `data/recorder_heartbeat` is <600s old. A process that dies leaves a FRESH heartbeat behind, so the respawner reports "recorder: alive" and refuses to restart for up to 10 minutes while nothing is running -- observed directly (printed "alive" with zero recorder processes). Every recorder crash therefore costs up to 10 min of unrecoverable market data, and a crash-loop could be masked indefinitely if it dies right after each heartbeat write. | Cheap fix: check process existence (pgrep/pidfile) AND heartbeat age, not age alone. Same class as the 07-22 digger finding: an indirect health proxy reported success while the real thing was dead. | brain | 07-22 | open |
| 41 | **Hedge-failure mode: spot leg loses while the futures leg contributes ~0, during churn on the same symbol** | Gap #34 forensic (docs/research/GAP34_FORENSIC.md, 2026-07-22) resolved the "unexplained $1.8-2.6k": USDT is 100% attributed (residual -$0.0019) and the equity gap is the legs_v measurement artifact confirmed by incident #5. What REMAINS is real: combined_net -$1804.82 concentrated in just 3 symbols -- GTCUSDT (spot -1057.88 vs fut +0.28), SHELLUSDT (-429.62 vs +4.82), ONEUSDT (-363.46 vs -3.80) = -1849.66 total. Signature: futures P&L ~=0 against large one-directional spot losses + heavy futures churn (GTC 22 fut fills vs 5 spot). A delta-neutral pair whose hedge contributed nothing. NOTE: these are exactly the 3 names the CRO 07-19 topup deployed into, and that deploy produced trim<->topup churn -- the very 'rapid rebalance churn' trigger named in #34. | BRAIN: reconstruct the per-fill timeline for GTC/SHELL/ONE across the window; determine whether the futures short was absent, flat, or churning while spot was held (unhedged window), and whether close-ordering or the topup path creates it. This is a money-losing risk-path defect, not an accounting question. | brain | 07-22 | open-high-rank |
| 42 | **CHURN DRAG: 38% of carries are closed before capturing one funding payment -- costs ~-8.1%/yr** | Trade-economics audit 2026-07-22 over 250 closed trades: bucketed by hold time, <2h n=38 net -10.20 (-5.0 bps), 2-8h n=57 net -9.94 (-4.1 bps), 8-24h n=80 net +25.14 (+6.5 bps), >24h n=75 net +51.22 (+16.9 bps). **95 of 250 trades (38%) are held under one 8h funding period and lose money AS A CLASS** -- a <2h carry captured $0.43 of funding on $20,388 of notional while paying a full round-trip. CROSS-VALIDATED: the measured cost model predicts ~3.8 bps round-trip; realized <2h loss is 5.0 bps -- two independent methods agree, so the churn loss IS the execution cost paid for nothing. CAUSE = funding-sign flicker, not bad entries: median abs(funding_rate) at open is identical for fast vs slow closes (0.000114 vs 0.000111); COOKIEUSDT was opened 22x (16 fast closes), GTC 14x, MOVE 9x -- the same names cycle as funding crosses zero. Realized drag -$20.14 over 20d on $4,500 = **-8.1%/yr**, on a book whose deployed Sharpe is -0.06. | FIX (2 parts, economically justified): (1) MINIMUM HOLD -- do not close a carry before it has captured >=1 funding payment (8h) UNLESS a risk rail demands it (basis-stop / ADL / cooldown / risk-flatten / reconcile must always close immediately). Economics: holding through 8h of adverse funding costs ~1 bp (0.0001/8h) versus ~4-5 bps to round-trip, so holding strictly dominates. (2) FUNDING-SIGN HYSTERESIS -- require funding to be negative on N consecutive checks (or below a small negative band) before closing, instead of closing on the first negative print. Needs a test asserting rails can still close instantly. | brain | 07-22 | open-TOP-RANK |
| 43 | **Entry gate absent: desk opened carries at the exchange BASELINE funding rate, eating ~80% of gross profit** | Trade audit 2026-07-22 bucketed 250 closes by funding AT OPEN: the exact-0.000100 bucket (Binance DEFAULT rate = no funding premium at all) was n=50 net **-$176.24 (-92.7 bps)** while all other buckets combined earned +$232; total net was +$56, so baseline-rate entries consumed ~80% of gross profit. Root cause: `_ranked()` accepted ANY `v > 0`, with no minimum -- so names sitting at the exchange default were treated as tradeable carries and paid a full round-trip for zero premium. Compounding it, per-symbol realised economics show thin books bleed structurally: NOMUSDT -149 bps (the 07-13 ruin name), KNCUSDT -211 bps, ONEUSDT -92 bps, vs COOKIE +60, MOVE +76, EDU +246. | FIXED 2026-07-22: `_entry_gate()` requires expected funding capture over the MINIMUM HOLD to beat the symbol MEASURED round-trip cost. Floor derived not fitted: round-trip 4.5 bps / 3 funding periods in the 24h min-hold = 1.5 bps = `_MIN_FUNDING 0.00015`. Per-symbol cost comes from run_cost_model.py, so the gate auto-tightens on expensive books (OPUSDT measured 20.6 bps RT blocks weak funding) and loosens on cheap ones (BTCUSDT 0.018 bps), and self-improves as the recorder accrues traded names. Applied to NEW OPENS ONLY -- never to hold/target -- so raising the bar cannot force-close existing carries. 6 unit tests. | CRO | 07-22 | fixed |
| 34-RESOLUTION | **Gap #34 ATTRIBUTED -- the $1.8k is real, concentrated in 3 symbols, caused by futures-leg thrash leaving spot unhedged** | RESOLVED 2026-07-22 by fill-level attribution of data/deadman_reconciliation_20260719.json (25 carries). Totals: spot bought 17,696.62, sold 15,858.94, spot_net **-1,837.68**; futures realized only **+13.41**. In a hedged book that spot loss should be offset ~1:1 by the short -- it is not. CONCENTRATION: GTCUSDT -1,057.88, SHELLUSDT -429.62, ONEUSDT -363.46 = **-1,849.66 of the -1,837.68 total**; every other carry nets ~0 (ASTRUSDT pair -0.05). MECHANISM (fill counts): a clean carry is 2 spot / 2 futures fills (ASTRUSDT 2/2). GTCUSDT ran **5 spot / 22 futures** fills, SHELL 4/12, ONE 7/8 -- the futures leg was repeatedly covered and re-shorted while the spot leg sat, so spot was intermittently UNHEDGED and adverse moves landed with no offset. NOT missing money (spot_only_residual **-0.0019**, every cent attributed) and NOT a measurement artifact -- the 13-model panel was right to reject the CRO first-pass modest-slippage read. SAME PATHOLOGY as #42/#43: GTC and ONE are the identical names the churn audit flagged (GTC 14 opens / 8 fast closes, -26 bps; ONE -92 bps). | Root cause is addressed by the shipped #42 churn guard (24h min hold), #43 entry gate (funding + measured-cost floor) and #37 orphan cooldown, which together stop the leg-thrash that produced it. REMAINING: an explicit invariant that the spot leg is never left unhedged across a futures re-hedge cycle (atomic pair close), and an alert when fut_fills/spot_fills for a symbol exceeds ~3x. | CRO | 07-22 | attributed-root-cause-addressed |
| 25-RESULT | **Anytime-valid inference BUILT and MEASURED -- it is rigorous but SLOWER; the CRO's own 'faster validation' proposal is refuted by its own test** | Built libs/research/anytime_valid.py (mixture e-process, Ville's inequality, valid under daily peeking). Monte Carlo 2026-07-22: null (no edge) 3/300 paths crossed = **1.0% at alpha=0.01 -> type-I control VERIFIED**; strong edge (Sharpe~2) only 6/40 graduated with **median 132 days vs the fixed 90-day clock**; weak edge (Sharpe~0.3) 0/40 (correctly rejects). ROOT REASON (fundamental, not implementation): a Sharpe-2 edge on DAILY returns has per-observation signal ~0.105, so the e-process grows at ~mu^2/2sigma^2 ~ 0.0055/day and needs ~800 days to reach log(1/alpha). CONSEQUENCE: there is NO free lunch on validation speed -- cutting 90->40 lowers the evidence bar, anytime-valid raises it; you cannot have faster AND stricter. The only genuine accelerants are MORE OBSERVATIONS: higher-frequency returns or cross-sectional breadth. | ADOPT AS: a stricter SECONDARY check for high-stakes promotions, NOT as a replacement for the 40/90d clock and NOT as a speedup. The 90-day clock stands. Revisit only if the desk ever validates on intraday returns, where per-observation signal is high enough for an e-process to cross quickly. | CRO | 07-22 | built-measured-not-adopted-as-speedup |
| 44 | **Forward validation ran on DAILY returns while funding settles every 8h -- the desk was discarding 2/3 of its evidence resolution** | Found 2026-07-22 hunting legitimate validation accelerants after the anytime-valid MC proved no test buys speed on the same data -- the only honest lever is MORE OBSERVATIONS. The incumbent shadow (run_cashcarry_shadow.py) validates on D1 panels; funding settles 3x/day. BUILT: scripts/run_shadow_8h.py -- the IDENTICAL strategy function (cashcarry_returns, frequency-agnostic) on an exchange-native 8h funding/basis panel (free one-shot fetch, 40 syms, 4h-settlement symbols summed into true 8h blocks). MEASURED on the same 27-day window: 81 blocks vs 26 daily obs; **autocorr vif 1.008 at 8h vs ~3.6 at daily** -- blocks are nearly independent, so the sqrt(3) evidence speedup is realized almost in FULL rather than eaten by stickiness; annSh 8.11 vs incumbent 24.42 -- the 8h series carries the basis-MtM variance the molded daily curve smooths away (more honest, directly answering the kimi variance-censorship critique / gap #14 class); NW-t 2.2; e-value 28 (<100 bar, correctly not yet decisive). | CHALLENGER per constitution: writes web/cashcarry_shadow_8h.json alongside the incumbent, wired as its own daily-cycle step, NO consumer reads it until the challenger-vs-incumbent window closes. If adopted, the 40/90-day gates keep their evidence bars but reach them ~sqrt(3)x sooner in calendar time -- the legitimate version of the principal's 90->40 request. | CRO | 07-22 | challenger-live-measuring |
| 45 | **Candidate screening charged GUESSED costs (5/8/15 bps tier) that measurement proves wrong in both directions -- a silent thumb on the discovery scale** | Found 2026-07-22 answering the principal's 'why zero survivors now'. run_discovery screened every candidate against adv_tier_cost (5 bps/side ADV>$500M, 8 bps >$100M, 15 bps below). Measured reality (run_cost_model.py, real recorded books): BTC pair slippage 0.009 bps vs 5 bps/side charged -- with maker-first execution (2 bps futures maker) a daily-turnover sleeve pays ~3%/yr of PHANTOM cost, enough to kill genuine 0.6-0.9-Sharpe candidates at the margin. Meanwhile thin names are UNDERcharged (NOMUSDT realized -149 bps vs 15 assumed), flattering exactly the junk that later bleeds. | FIXED 2026-07-22: run_discovery now uses _measured_side_cost = 3 bps documented maker-first fee + MEASURED per-leg book-walk from data/cost_model.json, tier fallback for unmeasured names; run_cost_model.py wired into the daily cycle so measurement coverage grows automatically as the recorder accrues the traded universe. Shadows/forward clocks DELIBERATELY untouched (no methodology change on a running window). ALSO: peek-safe e-values wired into the derivative shadow (anytime-valid = the ONLY statistic safe to read daily; Sharpe stays unpublished until day 40). First live peek at 24d: ls_contrarian e=0.357, oi_divergence e=0.598 -- forward evidence currently AGAINST both (a real Sharpe-10 edge would read ~20+ by now), vindicating the DSR kill in real time. | CRO | 07-22 | fixed |
| 46 | **Growth path had no pre-registered unlock ladder -- every future leverage step would be renegotiated ad-hoc, adding weeks of latency between evidence and aggression** | Principal 2026-07-22 ordered the 80-120%/yr band unlocked by every lever short of faking validation. Built: docs/research/GROWTH_UNLOCK_LADDER.md -- seed-scale ruin schedule (6%/4%/2% by equity tier, Kelly-on-lifetime-wealth rationale), 4-step leverage ladder (1.0x -> 1.5x -> 2.5x -> 4.0x) each gated on pre-registered evidence with SAME-DAY unlock + automatic downshift; fee-tier engineering, compounding hygiene, cross-venue carry family, clock-saturation duty. CO-SUPREME OBJECTIVE #2 (max alpha-discovery rate) installed in the doctrine + all mission prompts with the honesty guard (discovery never declaration). Rejected + ledgered: leverage now, rail loosening, window-shortening. | CRO | 07-22 | installed |
| 47 | **Journey-level CAGR levers were undesigned: operator behavior, security/custody, records-from-inception, tax basis -- all in the cannot-retrofit class** | Angle-19 frame-wide sweep (principal order 2026-07-23: 'find every way before live, I wont tweak after'). The trading stack was maxed but the JOURNEY stack was unwritten: no bankroll/withdrawal policy, no drawdown-conduct or euphoria rules for the HUMAN, no absence protocol, no venue-diversification schedule, no security hardening plan (live keys + a plaintext PAT on the same box!), no allocator-grade track record, no tax-lot discipline (gap #11 waited until live = too late). Behavioral drag, one hack, one venue failure, or unprovable performance each cost more lifetime CAGR than any sleeve earns. | INSTALLED 2026-07-23: docs/OPERATOR_COMPACT.md (bankroll: zero withdrawals <$25k then max 20%/yr of profits; 10% profit ratchet to cold custody at each doubling; drawdown no-touch + euphoria no-skip rules; absence step-down 7d/14d; MUTUAL tweak-freeze) + docs/GO_LIVE_CHECKLIST.md (trade-only keys, PAT->deploy-key rotation BEFORE live keys, sub-account-per-sleeve, venue diversification <=100%/<=50%@50k/<=33%@100k, BNB fee prep, idle-cash yield EV-gated, jurisdiction ask activating #11 PRE-live) + scripts/run_nav_attest.py (hash-chained daily NAV, git-timestamped, running from TODAY on paper for inception continuity). | CRO | 07-23 | installed |
| 48 | **Data-acquisition ROI negative: 1 fundable hypothesis from 13 ingested axes (paid CME barely cleared; free macro axes ~0)** | The 2026-07-23 axis generate run EV-gated a pre-registered hypothesis on every idle axis: only cme cleared (0.0326, near-miss), 10 rejected as low-mechanism/narrow-breadth/overlay-penalty (same class as the 07-17 FRED kills). The desk ingested breadth its own economic gate will not fund a bet on -- free-first-protocol in reverse. | Reweight ingestion toward credible orthogonal-mechanism axes (crypto-native, on-chain, venue-native, regional) over macro-overlay axes that structurally score low in the est_sharpe=refinement formula; do not renew the paid CME feed unless its queued hypothesis clears Stage-A. Objective #2 = max discovery PER ingestion-dollar, not max axes. | brain | 07-23 | open |
| 49 | **Live order path sends NO client order ID (`newClientOrderId`)** | `binance_live.py:280/288` posts symbol/side/type/quantity with no client ID, while `execution/retry.py` documents the opposite guarantee. On an ambiguous timeout the desk cannot distinguish 'not placed' from 'placed, reply lost', so a retry or restart-and-reconcile re-places -- on a delta-neutral book a duplicated leg is an UNHEDGED DIRECTIONAL POSITION. | PREREQUISITE FOR GAP #2's 07-31 no-naked-position invariant -- that invariant is unachievable without idempotent submission. Deterministic id from symbol+side+intent+time-bucket; query-by-id before any re-place. Risk path: v8 8.2 bar (property + mutation + second-model fuzz), independence-gated, 6h canary. | brain | 07-25  **CLOSED 2026-07-29**: `libs/execution/idempotency.py` -- deterministic newClientOrderId on BOTH futures connectors (live + testnet), market AND post-only paths. Chunk index is part of the ID (a shared ID would have the venue accept chunk 1 and reject the rest, silently under-filling the leg). Executor threads a per-pair `cycle` token via `_pair_cycle`, so the ID is clock-independent -- the first implementation used a wall-clock bucket and its own test caught the boundary case where a retry straddling a grid line produces a different ID and the duplicate goes through. `is_duplicate_error` is fail-closed: an unrecognised error is never read as 'already placed'. 22 tests. | CLOSED 07-29 |
| 50 | **52 `utcnow()` calls; ruff DTZ and S rule families disabled** | `utcnow()` returns NAIVE datetimes and is deprecated in 3.12 (what the VPS runs), mixed with 92 aware `timezone.utc` uses. Naive-meets-aware is either a TypeError or silently wrong arithmetic -- on forward-clock day counts, 8h funding boundaries, freshness SLAs and §33 deferral expiry. A wrong day count silently corrupts a promotion gate. `S` (bandit) off = zero security linting on a repo handling exchange keys. | SPLIT + DATED 2026-07-26 (undated since 07-25). (a) DTZ tranche -- enable ruff DTZ, convert all 52 `utcnow()` to `datetime.now(UTC)`, suite green: DEADLINE 2026-08-08. Mechanical and CI-gated, and it is a correctness bug not a style one -- naive-meets-aware silently corrupts forward-clock day counts, 8h funding boundaries and §33 deferral expiry, i.e. the arithmetic that decides promotions. Risk-path files (executor, dead-man, recorders) go LAST and each in its own commit. (b) `S` (bandit) security lint -- SEPARATE pass, DEADLINE 2026-08-31: it will surface a triage backlog on a repo that handles exchange keys; enable, triage, do NOT bulk-suppress. | brain | 07-25  **CORRECTED + CLOSED 2026-07-29.** The premise was WRONG: this row came from grepping the string `utcnow(`, which matches the desk's OWN correct helper `libs.core.time.utcnow` (= `datetime.now(UTC)`, timezone-aware) as readily as the deprecated stdlib call. 30 of the 31 files were already correct; acting on this row would have meant 'fixing' 53 correct call sites. Verified: ZERO occurrences of bare `datetime.utcnow()` anywhere. ONE instance was real but a different defect -- `pd.Timestamp.utcnow()` in compute_performance.py is tz-aware (never a corruption risk) but is REMOVED in pandas 4; fixed to `Timestamp.now('UTC')`. New `max_audit.check_naive_datetime` guards both forms using AST rather than text -- its own first run flagged four matches inside its own docstring, which is exactly the failure that produced this row. | CLOSED 07-29 |
| 51 | **pyproject has 0 exact pins; requirements-vps.txt has 22** | CI resolves latest, production runs pins -- so CI green says nothing about production, in either direction. Already bit the desk: `ruff>=0.5` resolved to 0.15.8 and produced 36 errors that did not exist under the assumed version. | IMPLEMENT BY 2026-08-02 (dated 2026-07-26; undated since 07-25) -- the earliest deadline on the board, because it is hours of work and it is a PRECONDITION for trusting every other deadline here: while CI resolves latest and production runs pins, a green CI is evidence about neither. Already bit the desk once (`ruff>=0.5` resolved to 0.15.8 -> 36 errors that did not exist under the assumed version). Pin pyproject to the VPS set, run the full suite, and add a check that fails when the two files drift apart. | brain | 07-25  **PARTIALLY CLOSED 2026-07-29.** pyproject's 13 floors raised to the deployed pin set, so CI can no longer legally resolve a version production has moved past. New `max_audit.check_dependency_drift` enforces it three ways: floors may never sit below the pins, MAJOR drift from the pins is a defect, and pinned packages absent from the environment are named (a test needing them did not run). **It fired immediately and the finding is worse than the row claimed**: 18 of 22 packages differ in the dev container, including pandas prod=2.3.3 vs here=3.0.5 — a MAJOR version. A suite green against pandas 3.x is weak evidence about a desk running 2.3.3; the same code even emits different deprecation signals in the two. REMAINS OPEN: aligning the actual environments, and the CI wiring, are operator steps. | open-PARTIAL |
| 52 | **`scripts/` excluded from mypy -- 369 errors across 81 files** | `files = [libs, app, migrations]`. 162 scripts including the cash-carry executor, dead-man switch and both recorders never see the strictest gate in the repo. Measured backlog 2026-07-25: 369 errors / 81 files. | Incremental tranches, risk-path files LAST, each its own commit. Do NOT bulk-fix: editing the live executor to satisfy a type checker is a known way to inject bugs into working code. | brain | 07-25 | open | closed-07-30 (mypy ratchet check_mypy_ratchet.py: counts can only fall; scripts 46.4% clean and climbing -- progression owned by the ratchet, not this row) |
| 53 | **Mutation testing never installed -- the v8 8.2 bar is unmeasurable** | GAP #2 requires >=90% mutants killed on the 5 risk-path files. No mutmut/cosmic-ray present, so that bar has never been measured and is currently decorative: 1199 tests are of UNKNOWN strength -- they demonstrably execute code, nothing shows they constrain it. | MEASURED 2026-07-26 -- the bar is no longer decorative. `scripts/run_mutation_test.py` (purpose-built, not mutmut: mutmut edits the working tree in place and one target is TIER-3 `run_deadman_switch.py`, so this harness mutates a scratch copy and the real tree is never written to). A BASELINE check runs first -- an already-red suite kills every mutant and reports a perfect score, which is the one failure mode that turns the tool into a liar; it caught exactly that on the first run (a bad pytest flag). Results: sizing 73%%->90.9%%, staging 63%%->82.9%% raw / 100%% of killable after 81 new boundary tests. Six survivors are documented EQUIVALENT mutants (fail-closed defaults where any value in the region fails identically); the registry demands a written justification per entry and reports stale entries, so it cannot become a laundering mechanism. Found one real fail-open on the S2 gate (`critical_drill_failures` default 0 -> 1). REMAINING: binance_live/gate/deadman scores; the second-model-family half of the v8 8.2 bar is a PANEL task and stays open. | brain+panel | 07-26 | closed-07-30 (run_mutation.py harness + measured scores: gate 100%, staging 100% adj, stepwise 90%, sizing 86.2% floored; equivalence register + truncation guard; the second-model-family half stays with row #2 as a panel task) |
| 54 | **No per-venue exposure cap anywhere in risk or portfolio** | SYSTEM_REVIEW ranks counterparty concentration as fatal ('An FTX-class failure is fatal to deployed capital regardless of strategy correctness'). Per-name is capped at 35% and per-factor is capped; NOTHING caps the fraction of net worth inside one exchange. Zero hits for per_venue|venue_cap|venue_exposure. | IMPLEMENT BY 2026-07-31 -- the Gate-0/#2 deadline, HARD (dated 2026-07-26; undated since 07-25). Re-verified 2026-07-26: `grep -rn 'per_venue|venue_cap|venue_exposure' libs/ scripts/` still returns ZERO hits, so the cap genuinely does not exist. It must land BEFORE live keys, not after: a cap is a NUMBER (unlike the Bybit second venue, spec'd post-Gate-0), so there is no build risk and no reason it trails the connector. With one venue the cap binds at 100% and changes nothing today -- which is exactly why it is cheap to install now and expensive to retrofit the day a second venue exists. Set it, enforce it in the sizing path, alert on breach. | brain | 07-25  **CLOSED 2026-07-29**: `libs/risk/risk_controls.VENUE_CAP` + `venue_equity`/`venue_cap` args on `evaluate()`. Breach pauses OPENS on the breaching venue and never flattens -- yanking capital off an exchange in a panic realises losses and converts a concentration problem into a solvency one. Default 1.0 is the honest one-venue cap, binding exactly where the desk runs today, so the enforcement path is proven before it ever has to bite. 14 tests. | CLOSED 07-29 |
| 55 | **Principal key-person risk -- SYSTEM_REVIEW's #1 structural risk, never tracked** | 'One 18-year-old principal owns every forever-human action (deposits, keys, Tier-3 approvals) and the budget.' Named as the LARGEST structural risk in a doc that is read but never re-ranked, so it has never entered the weekly escalation loop. | Tracked here so it inherits the loop. Mitigations are operator-side (documented handover, key custody, a stated degraded-mode) -- no engineering fixes this, which is why it must be visible rather than solved. DATED 2026-07-26 (undated since 07-25): this row can never be "implemented", so it takes the DEFER-WITH-DEADLINE exit as a standing review, not a build. OPERATOR DELIVERABLE due 2026-08-31 -- a written handover note (where the keys are, who to contact, how to flatten the book without the AI) plus a stated degraded-mode; it is a one-page artifact, and its absence is the single-point-of-failure. RE-AFFIRMED at every monthly governance window, next 2026-08-15: an unchanged status is recorded as a decision, not allowed to become silence. | operator | 07-25 | open |
| 56 | **Library layer is silent: 1 of 318 modules uses logging** | Everything observable comes from script-level prints; below the script boundary there is no trail at all. The pager died silently 07-11 -> 07-16 (five days invisible). Post-incident forensics currently cannot reconstruct what a library function did. | Convention + wire the risk and execution paths first; let it spread as modules are touched. Do NOT bulk-add logging to 318 modules -- that produces noise, not observability. | brain | 07-25 | open | closed-07-30 (structured logging in risk/execution library paths) |
| 57 | **Dead-man state write is non-atomic (truncate-then-write)** | `run_deadman_switch.py:269` `_STATE.write_text(json.dumps(...))` on the TIER-3 survival rail, which re-reads its state every loop. A crash mid-write leaves truncated JSON on the one component that must never fail. `_foreign_writer_alive()` already guards the MULTI-WRITER case; the CRASH case is unguarded. Only 1 `os.replace` exists in the whole repo. | **CLOSED 2026-07-26 -- SHIPPED UNDER PRINCIPAL SIGN-OFF, VERIFIED IN CODE.** `git log scripts/run_deadman_switch.py` -> commit 932b0e3 "TIER-3: atomic state write on the dead-man rail (principal sign-off 2026-07-25)". Read fresh at close: the writer is now tempfile + `os.replace` (lines 60-62) with the rationale in the docstring ("write_text is truncate-then-write: the file is zeroed, then filled"). The row was stale, not parked -- it stayed open only because nothing reconciled it against the commit. Tier-3 discipline held: a human signed it off, no agent edited the rail. | principal+brain | 07-25 | closed-2026-07-26 |
| 58 | **No in-repo record of what is actually scheduled** | 5 systemd timers are committed; the recorder, spot recorder, executor, run_alerts, shadows, reconciler, the */5 divergence sampler and the pgrep self-heals all run from UNCOMMITTED VPS crontab. 119/162 scripts have no in-repo scheduler reference, so live and dead are indistinguishable from source -- to any auditor and to the desk in six months. Also holes DR: a restore from GitHub yields a desk that runs NOTHING. | DEADLINE 2026-08-05 (dated 2026-07-26; undated since 07-25). Split so neither half waits on the other: OPERATOR by 08-05 -- paste `crontab -l` (plus `systemctl list-timers`) into the repo as `ops/crontab.manifest`; that is a 2-minute action and it is the only part an agent in a sandbox cannot produce. BRAIN same cycle -- a check that diffs the manifest against the scripts it references and fails when a committed timer names a script that does not exist (and, on the VPS, when live crontab drifts from the manifest). Until then, live and dead are indistinguishable from source and a GitHub restore yields a desk that runs NOTHING -- so this is a DR hole, not just documentation. | operator+brain | 07-25 | open | closed-07-30 (ops/crontab.manifest + reconstitute_cron.sh + SELF-INSTALLING scheduler in pull_deploy -- manifest changes reach the live crontab within one 10-min tick) |
| 59 | **Fee-tier / VIP progression not modelled** | The executor's own comment calls fees 'the single biggest live drag' (~20-25bps round-trip). `libs/costs/model.py` has no notion of VIP tiers; the only fee references in the repo are two hardcoded VIP0 constants. Binance tiers step down materially with 30d volume, BNB balance adds ~10%. | Deterministic return improvement with ZERO research risk and zero DSR cost -- nothing has to be discovered or validated. Activates at live volume. DEFERRED WITH DEADLINE: review 2026-08-31, or at first live fill, whichever comes FIRST (dated 2026-07-26; undated since 07-25). An event-gate alone is what parked this row -- "activates at live volume" has no clock, so if Gate-0 slips the row simply never gets looked at. The calendar backstop forces the look regardless. Note the related live-today item is already tracked separately as the bnb-burn defect (fee burn ON, BNB balance 0, so the ~25% discount is inert). | brain | 07-25 | queued (at live volume) |
| 60 | **SYSTEM_REVIEW #7: the ADL heuristic can take the wrong branch, on the same reconciler path that already lost money** | The executor's ADL branch chooses between "sell the spot leg + 24h cooldown" and "re-short the deficit" from a single test: a missing short leg PLUS any venue force-order on that symbol within 2h. SYSTEM_REVIEW names three ways that test is wrong and none is guarded: (a) PARTIAL ADL is indistinguishable from full, so a still-hedgeable position gets liquidated at market; (b) a force order on an UNRELATED position of the same symbol satisfies the test, triggering a spot sale nothing justified; (c) the 2h window has no staleness bound, so a stale read keeps firing after the condition passed. Both branches are live-ammo, and they sit on the exact decision surface #34-RESOLUTION attributed -$1,837.68 to: futures-leg thrash leaving the spot leg unhedged. Written down 2026-07-12, never tracked -- invisible to the cycle for 14 days. | SPEC DUE 2026-08-08, folded into the #37 reconciler-hardening spec; build post-Gate-0 with #37. Same file, same risk path, same independence gate -- two specs for one decision surface would produce two designs. Scope: discriminate partial from full ADL by position DELTA rather than event presence; require the force order to match THIS position (id/qty), not merely the symbol; bound the window with an explicit as-of timestamp. Risk path: v8 8.2 bar (property + mutation), independence-gated, no same-cycle patch. | brain | 07-26 | open |
| 61 | **Cross-asset contagion / lead-lag never screened -- both the crypto (inbox #7) and non-crypto (inbox #6) forms** | improvement_inbox ranks "#7 Cross-asset contagion / lead-lag" in its TOP-5 for spec-prebuild (FRED landed with 3y history; VIX/dollar lead-lag with crypto), and frontier-menu #6 "CROSS-ASSET CONTAGION (non-crypto)" extends the same mechanism to free commodities/FX/equity (Yahoo, Stooq). Neither was ever screened. This is the named instance of the measured `data-utilization-paralysis` defect: 17/20 ingested axes carry ZERO screened hypothesis, `crossasset` among them -- the desk pays ingestion and maintenance for data its own economic gate has never been asked about. | ONE mechanism-first, pre-registered, screened hypothesis on the crossasset axis by 2026-08-15, tagged via research_memory.py --axis. The honest prior is stated in advance so the result cannot be spun either way: row #8 EV-REJECTED all 3 FRED macro hypotheses (0.0039-0.013 vs the 0.05 bar) and #48 found macro-OVERLAY axes structurally score low because est_sharpe treats refinement, not a new stream. The pre-registration must therefore express a LEAD-LAG mechanism with its own return stream, not another conditioning overlay -- and if the idea can only be written as an overlay, that IS the answer: retire the axis on the record instead of re-queuing it. The non-crypto (#6) extension is EARNED, not scheduled: it runs only if the crypto-side screen shows signal. | brain | 07-26 | open |
| 62 | **Chinese-language expansion: three builds queued since 07-18 with no gate and no date (OSINT source-list, CNKI/Wanfang literature deep-miner, community-specific connectors)** | The Chinese Quant Maximization package names three concrete builds -- OSINT CHINESE-LANGUAGE EXPANSION (WuBlockchain/ChainNews/WeChat/Zhihu into the OSINT scanner source list), LITERATURE DEEP-MINER: CNKI / Wanfang (Chinese quant-finance papers, theses, proceedings), COMMUNITY-SPECIFIC CONNECTORS (Xueqiu public API + BigQuant strategy library) -- on the thesis that untranslated communities are where unpriced edge hides. None had a register row, so none inherited the re-rank or the staleness bar; they sat in an inbox for 8 days while the doc that holds them is re-read every cycle. | DEFERRED WITH DEADLINE 2026-08-15 -- decided on MEASURED yield at the monthly window, not on the thesis. The one real datapoint cuts against the package's framing: Prospector session 1 (2026-07-19) ran CN-language queries and returned ZERO surviving cards, with yield skewed to A-share/equity even under crypto-specific framing (RSRS is A-share sell-side research, ev=0.0002 reject); the desk's own note concludes the assumption should read CN-quant-GENERAL, not CN-crypto-native. GATE: run one more CN session with CN-crypto and CN-equity yield tracked SEPARATELY (never pooled as "Chinese-language"), then at 08-15 either fund the three builds in EV order or retire them on the record. If funded, cheapest first: OSINT source-list (config, no new seat) -> CNKI/Wanfang (search within legal access; note the gap if paywalled, free-first protocol governs any purchase) -> connectors (a real build; sustainability rule applies -- monthly liveness + immutable Bronze archive). | brain | 07-26 | open |
| 63 | **Chinese-scraper guardrails are preconditions, not follow-ups: NLP normalization, anti-bot degradation, reputation weighting** | The same package attaches three guardrails to any CN seat or scraper: NLP NORMALIZATION (map 打板 / 贴水 / 割肉 to internal concepts BEFORE the Overfit Prosecutor, or the prosecutor adjudicates mistranslations); ANTI-BOT DEGRADATION (rotating proxy/UA, and on an anti-bot wall or CAPTCHA degrade gracefully to public RSS/API and FLAG the missed data -- CAPTCHA solving prohibited, never burn the 20-query budget on login walls); REPUTATION WEIGHTING (prioritise CNKI academic authors, Zhihu L6+, Xueqiu verified managers over retail noise). They are ordering-critical: a scraper shipped without them yields silently mistranslated, silently truncated, retail-weighted input that LOOKS like coverage -- the same false-coverage failure §35 exists to prevent, one layer down. | BLOCKING PRECONDITION on #62: no CN connector goes live without all three, so they carry #62's decision date 2026-08-15 and are built in the same slot if #62 is funded. NLP normalization is already spec'd (docs/research/NLP_NORMALIZATION_SPEC.md), so its residual is wiring, not design. Build the anti-bot rail ONCE, region-parameterised -- the frontier menu's OPERATIONALIZATION MANDATE requires a degradation protocol for every future regional seat (RU/KR/JP/BR), so a CN-only version would be rebuilt four times. Non-negotiable detail: a flagged miss must land in a log the desk actually reads; a scraper that quietly returns less is worse than one that fails loudly. | brain | 07-26 | open |
| 64 | **ABANDONED-BY-CAPACITY SCANNER never built -- the highest prior-density query family the desk has identified** | improvement_inbox proposal: hunt the specific sentence in ex-fund content -- "we used to run X, stopped when we got too big / it was too small to matter". That sentence marks an edge a fund VALIDATED with real money and then VACATED for being sub-scale: pre-validated, pre-uncrowded, and sized for exactly the capital this desk has. It is the only search family whose hits arrive with an institutional validation stamp attached, which is why its prior beats generic mining. Supporting evidence that the SHAPE works here, measured not assumed: the EN frontier miner's two most valuable 2026-07-25 finds (#55 fill-rate decay discriminator, #57 side/depth rail) both came from depth-2 REPLIES correcting an OP, not from OPs -- targeted pattern-matching inside old threads is where this desk's yield actually is. | IMPLEMENT BY 2026-08-15: a Prospector query FAMILY plus an NLP pattern-match over already-fetched text -- no new seat, no new budget, which is why it ranks above the CN builds despite being newer. FALSIFICATION AND RETIRE DATE PRE-COMMITTED from the finding's own text: if no card sourced this way survives the graveyard + EV gate by 2026-11-15 (two quarters), fold it into base Prospector and retire this row. Serves objective #2 (max discovery rate per query) directly. | brain | 07-26 | open |
| 65 | **SYSTEM_REVIEW #9: "the round-2 meta-question" -- where does the largest remaining leak live NOW** | The round-2 review's closing question: what did round 1 miss entirely, and did any round-1 FIX introduce a new failure mode of its own? It is recorded here because §35 is right that an untraced finding is invisible -- and recorded CLOSED, because it is a standing REVIEW PROMPT consumed by the external panel, not a defect anyone can implement. Rowing a question as though it were work is exactly the tidier-backlog outcome §36 warns about. | CLOSED 2026-07-26 WITH REASON. Both halves already have standing organs and both have PRODUCED. (a) "Largest remaining leak NOW" is answered continuously by scripts/max_audit.py (30 live defects today, 48h auto-escalation to the principal page) and by this register's re-rank -- today's answer is ranks 1-5 in the header above, derived, not guessed. (b) "Did a round-1 fix introduce a new failure mode" is answered YES with a measured instance: row #33 -- the 2026-07-18 emoji-in-header improvement to run_alerts killed 39/39 pager pushes for 29h across a real dead-man fire, latin-1 header encoding, never exercised by the ASCII-only self-test. The generic question needs no row; its specific instances get rows, which is what happened here. Re-asked by construction at every external panel round via docs/EXTERNAL_PANEL_DOSSIER.md. | brain | 07-26 | closed-2026-07-26 |
| 66 | **FALSE-CONSENSUS MINING -- retired with reason, not deferred** | improvement_inbox proposal (CRO-generated): extract crowd-consensus BELIEFS from communities ("X always pumps before Y"), test whether they hold, and fade the confidently-wrong ones. Genuinely novel as a SOURCE, and it is retired on the desk's measured state rather than on taste. | RETIRED 2026-07-26 WITH REASON + RE-ENTRY CONDITION -- a deadline here would be one nobody keeps, which is the parked state under another name. (1) Generation volume is NOT the binding constraint: 420 candidates tested, ZERO survivors, dsr/pbo/reality_check each rejecting >=98%. The bottleneck is the funnel and the mechanism prior, not the count of ideas entering. (2) It makes that constraint WORSE: multiplicity corrections scale with the true tested N, so a high-volume, low-mechanism-prior source deflates every genuine candidate's DSR -- and the desk's own law already states that combinatorial expansion is EARNED per axis only after a single-axis screen shows signal. (3) Its own author named the fatal practical problem ("hard to test cleanly -- belief != clean signal"), and it has no data path: it needs the community-text pipeline that #62/#63 have not built. (4) Opportunity cost is concrete: 17/20 ingested axes still carry zero screened hypothesis -- the desk owes screens on data it ALREADY pays for before buying a new idea source. RE-ENTRY CONDITION so this stays reversible: re-open when idle axes <=5 AND a community-text pipeline exists AND a falsifiable belief-to-return mapping can be stated in advance. Nothing is deleted -- the proposal stands in improvement_inbox exactly as written. | brain | 07-26 | retired-2026-07-26 |

| 67 | **Two §13 legitimacy RULINGS owed by the principal — each one blocks a verified free dataset the desk already has in hand** | Not engineering: both are one-line human decisions an agent may not self-approve, and each currently blocks a source that is technically DONE. (a) **Upbit Historical Market Data portal** — licence read in full: explicitly PERMITS *"non-commercial and private purposes such as developing one's own strategy and backtesting"*, explicitly PROHIBITS *"commercial use, redistribution, processing and sale"*. A prop desk trading only its own capital, redistributing nothing, sits on that line. Blocks the deepest free Korean-venue archive known to the desk (1m candles back to 2017-10-24, 786 markets, SHA256-verified, values ground-truthed against the live API). (b) **Coin Metrics community — CC BY-NC 4.0**. The data is INGESTED and screened under internal-research scope (`data/coinmetrics_flows.jsonl`, 9,866 rows); the open question is narrower: may a CC BY-NC series feed a PRODUCTION signal at a desk that trades its own capital and redistributes nothing. | **RULE BY 2026-08-15** (monthly governance window). Each answer is one line: Upbit → "research-only" or "full use"; CM → "research-only" or "production input permitted". Neither is urgent-by-decay (the Upbit portal is a static archive back to 2017; CM is T+1 and continuing), which is why this sits at 08-15 and #68 does not. If the ruling is "research-only" for either, that is a COMPLETE answer, not a blocker — say so and the cards close as research-scope-adopted. §33 deferrals dated to this row: data_axis_watchlist cards #1 and #7. | principal | 07-26 | open |
| 68 | **bitFlyer ToS unreadable from FOUR independent routes — and every day of delay destroys a day of the only recoverable history** | The mechanism is verified-clean (no native candle API; self-bucketing from `getexecutions` is the only path; the 31-day lookback wall was binary-searched to the exact execution id; ~51.5k executions/day; a full backfill is ~3,200 calls ≈ 32 minutes). The ONLY open field is the licence, and it cannot be read: `bitflyer.com` ToS host returns 403/dropped from the VPS (route 1), Wayback/CDX holds no terms captures (route 2), an off-box egress on a different IP also returns 403 on both en-jp and ja-jp (route 3, tested 2026-07-26 — this kills the "just our IP" hypothesis), and alternate hosts 404 or do not resolve (route 4). The reachable docs page carries rate limits only, no licence language. No attempt was made to defeat the block (§13 is a boundary, not a hurdle). So nothing prohibits use and nothing permits it — which fails §13's clear-permitted-usage test. | **CLOSE BY 2026-08-09** — deliberately 14 days, not the monthly window, because this item DECAYS: the 31-day rolling wall means each day without a recorder permanently destroys a day of Japanese-venue tape that no free or paid provider can reconstruct. ACTION IS ONE PAGE-READ: any human or any organ on a non-blocked network opens `bitflyer.com/en-jp/terms`, finds the data-usage/redistribution clause, and pastes it into watchlist card #3. If permitted, the ~32-minute backfill runs the same day and the forward recorder starts. If prohibited, the card is KILLED with that mechanism and the desk stops spending cycles on it — either answer is worth more than the current silence. §33 deferral dated to this row: data_axis_watchlist card #3. **CLOSED 2026-08-05 — MOOT: THE PAGE-READ THIS ROW ESCALATED IS NO LONGER OWED, BECAUSE THE LICENCE WAS ALREADY READ.** The JP miner resolved it 2026-08-01 without a human and without defeating anything: route 2 had not actually failed, it had been mis-addressed — the "Wayback holds no terms captures" finding used the wrong CDX **host and slug** (`bitflyer.com`/`terms` instead of `bitflyer.jp`/`terms-of-use`). Corrected, capture `20190601153535` of `bitflyer.jp/en-eu/terms-of-use` served the document on the first try. **VERDICT: `restricted-by-licence`** — bitFlyer retains all rights in *"data such as transaction prices … which can be acquired by various external APIs"*, bars *"any robot, spider, crawler, scraper, script"*, and limits use to *"internal purposes … of the Service"*. So the row's decay premise is void in the direction that costs nothing: no day of tape was lost while this sat open, because no recorder was ever ours to start. ARTIFACTS: `docs/graveyard.md` → `jp_bitflyer_direct_recording` (mechanism of death, clause verbatim); `docs/research/data_axis_watchlist.md` card #3 synced CLOSED 2026-08-04. The same clause pre-kills `/v1/getchats`, `/v1/getfundingratehistory`, and the Wayback-archived keyless 15-min BTC/JPY series — an archive copy is not a licence. Licensed substitutes already owned: Tardis `bitflyer` free 1st-of-month (2019-08-30→), GMO Coin keyless ticks (2018-09-05→), bitbank candles. **HONEST RESIDUAL:** the document read is the EU entity's 2019 ToS, not the JP entity's current 利用規約; re-entry condition is a JP-entity ToS or explicit permission that does NOT retain rights in transaction prices. **THIS DOES NOT ANSWER ROW #80** — #68 is the one fork of the §13-consistency question that resolves with *no gate defeated*, so it removes an example, not the ruling; #80 still needs the principal. | operator+brain | 07-26 | closed |
| 69 | **NAVER DataLab collector is built, wired and screen-harnessed — and has never run for want of a free API key** | The cheapest unconverted item in the entire §33 backlog, and it is not an engineering item at all: `scripts/collect_naver_krsearch.py` exists, is wired into the daily cadence, uses the audited `libs.research.axis_screen` harness, and no-ops gracefully without credentials. Run 2026-07-26 → `no NAVER_CLIENT_ID/SECRET -- graceful skip`; `data/secrets/naver.json` does not exist and no `NAVER_*` env var is set. Mechanism it would test: Korean retail search-attention as a companion to the kimchi-premium axis the desk already treats as real and orthogonal — the KR information ecosystem is distinct from Western crypto Twitter, and kimchi is the desk's single best-performing new axis (IC +0.148). The ToS-grey scrape of `datalab.naver.com`'s web endpoint was deliberately NOT taken: it is the exact class this desk excluded alongside Baidu Index and Weibo/Zhihu, and a fake artifact is worth less than none. **RE-PRICED 2026-08-12 (R0300, KR-s1 C): tier-3 UNDER-VALUED this key.** Probing the sibling endpoints (KR-s1 2026-08-01) shows the SAME free key also unlocks `/v1/search/blog.json` and `/v1/search/cafearticle.json` — the ONLY s13-licensed route into Naver blogs and cafes, both of which name ClaudeBot/Claude-SearchBot `Disallow: /` in robots.txt and are therefore HARD-STOPPED on every other path. One ~5-minute registration converts TWO hard-stopped grounds PLUS the DataLab attention axis: three orthogonal KR surfaces for one human step — the highest value-per-operator-minute item on the register. **The 401-without-key is SETTLED — verified 4x; do NOT re-verify it a fifth time.** The only remaining action is the key; any further agent probe of this row is quota spent re-measuring a constant. | **BY 2026-09-08 (re-dated 2026-08-25 after a SECOND lapse — 08-09 and 08-19 both passed. MT5-MANDATE RE-PRICE, 2026-08-25: the kimchi-companion + KR-crypto-grounds half of this row's value case is DEAD — banned universe, and no KRW/KOSPI instrument exists in the MT5 universe. What survives and still justifies the 5 minutes, at a LOWER ceiling: the key is the only §13-licensed route into Naver blogs/cafes, which host Korea's retail 해외선물 [overseas-futures: gold/index/FX] communities — an MT5-native KR intelligence ground — plus DataLab attention on MT5 instruments, e.g. 금시세/금값 vs XAUUSD)** — free NAVER Developers registration (NAVER account + phone verification; a human step, ~5 minutes), then drop `data/secrets/naver.json {"client_id": "...", "client_secret": "..."}`. Zero code is owed for DataLab: the first live Stage-A screen lands on the next cadence run with no further work. The blog/cafearticle surfaces DO owe a small collector on key arrival (`collect_naver_krsearch.py` reads only `/v1/datalab/search` — verified 2026-08-12); it is deliberately NOT pre-built because code that cannot be run against its endpoint until a key exists ships untested — the day the key lands, that build is the same session's work (L1.39). If registration turns out to require Korean-resident verification the desk cannot satisfy, that is a real finding — record it and KILL the card with that mechanism rather than re-deferring. §33 deferral dated to this row: data_axis_watchlist card #21. | operator | 07-26 | open |

_Rows 67-69 added 2026-07-26 by the §33 conversion pass on the mined-to-wired backlog. All five carded finds took a disposition: two Tier-1 vendor-replacements WIRED with artifacts on disk (`data/coinmetrics_flows.jsonl` 9,866 rows full-depth + `data/kaiko_vwm_reference_rate.jsonl` 132 fixings), three DEFERRED with dates. Every one of the three deferrals is blocked on a HUMAN step and on nothing else — a licence ruling, a page-read from a non-blocked network, a free API registration — so they are rowed here rather than left as dates in a research doc, because §33 dates expire back into the backlog while only this register escalates. Zero kills: nothing in this batch deserved a graveyard entry, and a kill without a mechanism is the cheap exit §33(10) prices out._

_Rows 60-66 added 2026-07-26 by the §35 pass: every one of them was a finding the desk had already MADE and written down (SYSTEM_REVIEW 07-12, improvement_inbox 07-18) and that no loop could see. Twelve untracked findings across two docs became five open rows, one closed and one retired -- deliberately fewer rows than findings, because #62/#63 and #61 are single programmes with a single gate, and a row per bullet would be a backlog wearing a register's clothes. Nothing was deleted from a source doc and no doc left scope: coverage rose by ROWING the work, which is the only legitimate direction (`findings-scope-shrank` exists to catch the other one)._

_Rows 49-59 added 2026-07-25 from a full-repo engineering audit (architecture, quality gates, security, money math, concurrency, supply chain, observability). They are recorded HERE rather than in a review doc because the daily cycle works the register: a finding that never lands here is invisible to it forever, however carefully it was found. Enforced by `max_audit.check_findings_tracked` (§35)._
| 70 | **The literature organ was capped at abstract-level evidence for two runs by a tooling claim that was never re-tested — and it was false** | `docs/research/improvement_inbox.md` #59. Two consecutive literature runs inherited the carry-over *"no PDF tooling on this box... and the HARD FREEZE forbids installs"* and left FIVE findings (LIT_a F1/F3/F5/F6/F7) at abstract level. Run 3 verified the premise (genuinely no pypdf/fitz/pdfminer/pdftotext/poppler, and `Read` on a local PDF fails because it shells to `pdftoppm`) and then noticed the premise did not imply the conclusion: **PDF text is in FlateDecode streams and the stdlib ships `zlib`.** A ~90-line pure-stdlib extractor reads academic PDFs with zero installs; prototype ran from /tmp, touched no repo file, is NOT committed. EVIDENCE IT MATTERED, immediate and unflattering: the first paper extracted (Hou-Xue-Zhang) **corrected three numbers** the desk had recorded from a search summary one day earlier — actual is 65% fail at t>1.96, **82% at the 2.78 multiple-test hurdle**, worst category **trading frictions 102/106 = 96.2%** (recorded: 64% / "85% at t-cutoff 3" / "liquidity 95 of 102 = 93%"). The recorded prior was NARROWER than the truth. | **BY 2026-08-02.** (1) Land the extractor as `scripts/pdf_text.py` — read-only, no deps, research-lane; source is in improvement_inbox #59. (2) Add an `interior_extracted` field to the literature-card schema so `[ABSTRACT]` and `[PRIMARY]` are distinguishable at the EV gate. (3) **The generalisable defect, which is the real item:** a capability limit was asserted once and inherited verbatim across runs, silently defining what the desk could know. Adopt a standing rule — *any recorded "this box cannot X" carries a re-test date and is retried before it is inherited a second time.* That is the NO-CEILING axiom applied to the desk's own tooling claims, where it demonstrably was not being applied. | brain | 07-26 | open |
| 71 | **GATE-OPTIMALITY: the desk corrects for trial COUNT but not for construction CHOICE — and in crypto the second is the larger error term** | `docs/research/improvement_inbox.md` #60/#61. Four independent literatures converge: (a) Fieberg et al., *Non-standard errors in the cryptocurrency world* (IRFA 2024) crossed 10 design decisions into **20,736 designs** over 43 crypto sorting variables and found non-standard errors *"clearly exceed standard errors"* — i.e. construction variance > sampling variance, and DSR/PBO touch none of it; (b) Chen & Zimmermann vs Hou-Xue-Zhang resolves to a definitional split — **reproduction (same protocol) ~98% success, protocol-robustness ~15-35%** — so "it replicated" almost never means "survives OUR construction"; (c) Jensen-Kelly-Pedersen get high replication by pooling to **13 themes**, meaning the anomaly NAMES are overfit and the themes are not; (d) Andrew Chen shows a literature-derived t-hurdle is **unidentified by construction** (hurdle models must extrapolate into the region publication bias hides), and recommends empirical-Bayes shrinkage + local FDR on the observed right tail instead. **The desk is currently running the most design-fragile protocol (single construction) in the asset class with the largest measured design variance, and calling the output a verdict.** | **BY 2026-08-16.** Four actions, two of them free. (1) FREE, adopt now: the gauntlet asks every literature-sourced candidate *"reproduced under the original protocol, or re-derived under OURS?"* — only the second counts. (2) FREE, adopt now: screening unit becomes the MECHANISM with several constructions pooled under ONE pre-registration, not N screens each burning a multiplicity slot (the desk's own record fits — 420 named price hypotheses died individually; the lone repeat survivor, funding/carry, is a THEME). (3) PILOT the pre-registered design GRID: enumerate defensible construction choices up front, run all, judge the distribution — compatible with §26.3, not a loosening of it, because pre-registration is what stops it becoming the re-roll §26.3 forbids. (4) **The GATE-OPTIMALITY duty's own answer:** do not import a literature t-hurdle; compute shrinkage + local FDR on the desk's OWN logged 420-hypothesis realized right tail, where it IS identified. Also lands the McLean-Pontiff standing haircut (published effects **-58%** post-publication, worse for fat in-sample Sharpes) into `alpha_economics.py` as an explicit multiplier on literature-sourced candidates. | brain | 07-26 | open |
| 72 | **The 13-seat panel filters out its own best findings by construction — and a queued proposal would make it worse** | `docs/research/improvement_inbox.md` #62. *The Cost of Consensus* (arXiv 2605.00914, N=10, R=3, GSM-Hard 1,017 + MMLU-Hard) measures **consensus collapse**: the correct answer was present in the generation pool **53.0%** of the time but team accuracy was **20.7%** — a **32.3pp oracle gap**, with correct→wrong vulnerability up to **70.0%**. Plurality voting discards correct reasoning the panel already produced. The desk's `_consensus()` surfaces only themes with `n>=2`, so **a finding raised by exactly 1 of 13 seats is filtered out of the consensus summary**, and the inbox header then tells the CRO "a lone claim needs code proof". Same shape as the desk's own §35 lesson one level deeper: the finding IS routed, then filtered. Separately, debate lost to isolated self-correction in every measured cell (-2.2pp, -6.0pp, -27.6pp, all p<0.001) at **2.1-3.4x the tokens**. | **BY 2026-08-09.** (1) **KILL queued inbox item #43 "automated debate (≈ panel)"** — negative-EV on measured evidence, and it would destroy the independence that makes panel agreement meaningful. A change not made is the cheapest win available. (2) Record the existing design as VALIDATED-BY-EVIDENCE (13 heterogeneous seats, parallel, no cross-talk = exactly the endorsed configuration; heterogeneity is what the literature found actually works) so a future cycle does not "improve" it into debate. (3) PILOT a SINGLETON CLAIMS section in the panel inbox + reword triage to *"a lone claim needs code proof — and so does a consensus claim; agreement among models that read the same dossier is CORRELATED, not independent, evidence."* Falsifier: zero singletons survive CRO verification over ~3 cycles → revert. (4) ONE LINE: randomise seat order — the panel concatenates in provider order and the CRO reads top-down, a position bias the desk imposes on itself. (5) REJECTED after reading, spend nothing: self-preference defences in `score_panel.py` — measured self-preference is **80-99% artifact** once controlled for a capability-matched proxy (MATH-500 -98.76%, MBPP+ -89%, MMLU -80%). | brain | 07-26  **CLOSED 2026-07-29** (items 3+4): SINGLETON CLAIMS section added to the panel inbox, triage wording made symmetric ('a lone claim needs code proof -- AND SO DOES A CONSENSUS CLAIM: agreement among models that read the same dossier is CORRELATED, not independent, evidence'), and seat order randomised per run. Falsifier stands: if zero singletons survive CRO verification over ~3 cycles, revert. | CLOSED 07-29 |
| 73 | **The panel ships ~110k chars of graveyard+rulings to all 13 seats every run, adopted on one observation and never measured since** | `docs/research/improvement_inbox.md` #63. `run_external_panel.py` feeds up to 60,000 chars of `docs/graveyard.md` + 50,000 of `panel_rulings.md` to EVERY seat on EVERY run. It was added after an observed "7 of 27 rulings rejected as re-proposals" in the 07-20 run, and its effect has never been checked — while every run is already logged to `data/external_panel_log.jsonl`. Meanwhile the AI-scientist literature's top proposed remedy IS a null-result database (*Dead Science Walking*, arXiv 2606.04220), whose authors concede they have **"no controlled tests comparing retrieval/generation with vs. without negative evidence."** The desk built the thing a year early and holds the log that would settle it. | **BY 2026-08-09.** (1) Measure re-proposal rate (findings matching an existing graveyard/rulings entry, as a share of total) BEFORE vs AFTER the feed landed, from the existing JSONL — no new model calls. Either outcome pays: it worked → the desk has evidence the field lacks and should publish it internally as a validated design; it did not → the desk is burning 110k chars × 13 seats per run on faith and should cut it. That two-sided payoff is the signature of a test worth running. (2) Extend rail #54's `primary_artifact` discipline from DATA cards to LITERATURE citations — currently a data grade needs a fetch-verified URL and a citation does not, though citations are exactly as forgeable (100 confirmed hallucinated citations across 51 accepted NeurIPS 2025 papers, [SUMMARY-ONLY], flagged not relied on). Row #70's three corrected numbers are the internal proof. A literature card without a fetch-verified URL is auto-graded UNVERIFIED. | brain | 07-26 | open |
| 74 | **An eight-dimension deep-sweep audit failed silently and left eight well-formed filenames that a file-counting coverage check would score as complete** | Found in passing by the literature deep-miner, 2026-07-26. `docs/research/deep_sweep/` holds eight files dated today — `20260726_{alpha-discovery,data-intelligence,data-moat,execution-growth,infrastructure,meta-and-blindspots,research-engine,validation-stats}.md` — each **4 lines**, reading `# AUDITOR FAILED (<dimension>)` followed by an EMPTY `--stderr--` block. Not this organ's output; another organ's crash residue. **The defect is not the crash — it is that the whole sweep died with no captured error and left artifacts shaped exactly like success.** Same failure shape §33 exists to prevent for mining: an artifact that exists but is empty reads as done. Unknown whether this has been failing silently before today. | **BY 2026-08-02.** (1) Diagnose why the auditor died and check its history for prior silent failures — an empty `--stderr--` on all eight dimensions suggests the failure is upstream of the per-dimension work (launcher/credential/quota), not eight independent crashes. (2) Fix the contract: an auditor that fails writes its ERROR or writes NOTHING — never a stub that looks like coverage. (3) Files deliberately left in place rather than deleted: they are the only evidence the run happened, and deleting them is the denominator trick §34 forbids. | brain | 07-26 | open |
| 75 | **The entire `docs/research/deep_sweep/` tree is UNGOVERNED — 15 artifacts claimed by no law, and the literature organ created 7 of them** | Measured directly by re-running `max_audit.check_artifact_governance`'s own logic: 15 files under `docs/research/deep_sweep/` are absent from `_DIG_DOCS`, `_DIG_DOCS_EXCLUDED`, `_FINDING_DOCS`, `_PRODUCER_CADENCE` and `_TERMINAL_ARTIFACTS`, and the string `deep_sweep` appears NOWHERE in `scripts/max_audit.py`. §36(2) says an artifact ungoverned "fires on the day it appears"; this tree has been accumulating since 2026-07-26 and the check has not been re-run. **SELF-IMPLICATING AND THAT IS THE ARGUMENT FOR IT:** 7 of the 15 are this organ's own ground files, and run 2 is the proof the gap bites — it wrote 17 resolved findings into these files and routed **zero** of them to the graveyard, inbox or register, and nothing anywhere noticed. Had `deep_sweep/` been in §33 scope, run 2's unrouted findings would have owed dispositions and the backlog gate would have fired on run 3's first command. Instead run 3 found them by reading the directory. | **BY 2026-08-02.** Classify the tree — and per the check's own words, *"'no law' must be a DECISION, never a default"*. RECOMMENDED: add `docs/research/deep_sweep/` to **`_DIG_DOCS`** (glob, since ground files are named per run/ground), NOT to `_TERMINAL_ARTIFACTS`. Rationale: terminal would be defensible only if routing-out were guaranteed at run end, and run 2 disproved that empirically. §33 scope is the choice that would actually have caught the failure that happened. Note the tension to resolve deliberately: `_DIG_DOCS` entries are exact paths, so this needs glob support or a directory-level rule — a small change to how scope is expressed, not to the law. SECOND, SEPARATE ASK: the ungoverned check is only as good as its last run — find out when `max_audit.py` last executed and whether `artifact-ungoverned` has been firing unread, because a governance check nobody runs is the same failure one level up. (Row #74 covers the 8 `AUDITOR FAILED` stubs inside this same tree; this row covers the tree's governance, which is the more general defect.) | brain | 07-26 | open |
| 76 | **⚠️ LIVE BOOK: the desk's ONLY repeat survivor is a published strategy with a dated, causally-identified 36% decay event — and the desk is on the losing side of the paper's own predictive result** | `improvement_inbox.md` #67/#68. [PRIMARY] Schmeling, Schrimpf & Todorov, "Crypto Carry", BIS WP 1087 (Oct 2025 version), https://www.bis.org/publ/work1087.pdf — **two of the four authors of the canonical FX carry-volatility literature, porting it to crypto.** The desk's funding/carry edge is not undiscovered; it is documented by the field's own authors. THREE things the desk is not currently holding: (1) the **January 2024 spot-ETF DiD cut carry by 36% of its mean across exchanges and 97% on CME** — a dated causal structural break, so any backtest or sizing spanning Jan-2024 mixes two regimes; (2) Table 7 — *"a rise in standardized carry by 10% predicts a 22% increase in total sell liquidations"*, and carry predicts **only SELL** liquidations — **the cash-and-carry trader is the short, and the desk IS the short**, so high carry partly forecasts the desk's own liquidation risk, not just its return; (3) from the BNP→Jurek→Daniel-Hodrick-Lu chain, carry-portfolio **skew WORSENS with breadth (−0.700 at 1 leg → −0.977 at 3)** because legs share the funding-liquidity factor — **the desk's book runs `top: 10` on the opposite assumption.** | **BY 2026-08-16, and BEFORE any carry resize-up.** (1) ADOPT-NOW, free: record Jan-2024 as a regime boundary on every carry backtest and sizing input; a pre-2024 carry mean is not the current one. (2) MEASURE, cheap, uses data already held: realized skew of the carry sleeve as a function of leg count on desk history — this is ruin-adjacent because negative skew is exactly what the log-wealth objective punishes hardest, and the diversification assumption behind `top: 10` has never been tested. (3) FORWARD CLOCK, not backtest, for the Table-7 liquidation replication: the regressor side is fully covered (`bronze/crypto/<SYM>/D1`, 267 symbols from 2019-09) but `liquidations.parquet` is **17 days**, so a replication today would be a 17-day overfit — the listener is live and accruing, start the clock and pre-register. HONEST NOTE: this row does NOT say the carry edge is gone. It says the desk has been treating a published, causally-compressed strategy as if it were private and stationary. | brain | 07-26 | open |
| 77 | **The data inventory reports ROW COUNTS as if they were SPANS, and omits the desk's best research panel entirely — research organs have been choosing what to test off a misleading map** | `improvement_inbox.md` #69/#70. Audited on disk during the LIT-b dig, not remembered. **Overstated:** `liquidations.parquet` = 33,867 rows but only **17 days / 15 symbols**; `hyperliquid_funding` **28 days**; `crypto_metrics` **28 days** — a "14k+ events" framing invites monthly-horizon work the span cannot support, and this run caught exactly that on a BIS Table-7 replication and downgraded it before it became a 17-day overfit. **Understated:** `data/lake/bronze/crypto/<SYM>/D1/*.parquet` — **267 symbols, daily, from 2019-09-08**, funding + basis + taker_buy_frac, all non-null — is **absent from the inventory** and is the desk's best panel (wider and longer than the BIS paper's own). So the map hid both which mechanisms are blocked AND which are unblocked. **The binding constraint on the whole forgotten-literature ground turned out to be HISTORY LENGTH, not mechanism supply** — a conclusion only reachable once the real spans were measured. SEPARATELY, a data-paralysis defect from the same audit: **`data/cot_zcache.parquet` is CFTC COT daily 2000→2026, 11 assets, 26 YEARS, and nothing reads it.** | **BY 2026-08-09.** (1) Every inventory entry carries **SPAN (first→last date) and BREADTH (symbol count)** alongside row count; add the bronze panel. A row count without a span is not a measurement, and organs are making testability decisions from it. (2) **Put the 26-year COT panel to work — it is the cheapest real research on the register.** It spans the publication dates of the hedging-pressure and carry literatures, so it can **MEASURE post-publication decay out-of-sample instead of assuming** the −58% McLean-Pontiff haircut the desk just adopted as a standing prior (#61) — converting a borrowed prior into a measured one, on free data already owned. It also supplies a positioning panel with real history while the crypto positioning series are 17–28 days deep. (3) Cheap gating test from the same chain: Gorton-Hayashi-Rouwenhorst REJECT hedging pressure (positions significant contemporaneously, **zero lagged**) — a ~1-day test of whether `ls`/`oi` adds anything over `funding`/`basis` could cancel or justify a multi-week data acquisition before it is spent. | brain | 07-26 | open | closed-07-30 (data_assets.json: measured spans, consumers per asset, bronze stores) |
| 78 | **Kaiko reconstruction: a shipped Tier-1 §33 "wired" artifact whose validation number does not support the claim it appears to make** | `improvement_inbox.md` #71. The Cboe/CFTC rule filing NAMES Kaiko's real BTC constituents — **Bitstamp, Crypto.com, Gemini, Kraken, LMAX Digital**. `data/kaiko_vwm_reference_rate.jsonl` was built from coinbase/kraken/bitfinex/bitstamp: **2-of-5 overlap**, with **Coinbase = 80% of the tape and NOT a Kaiko constituent**, while **Gemini — excluded for a documented, otherwise-sound reliability reason — IS one**. So "median 1.42 bps agreement" measures *this method vs the desk's own VWAP*, not *this reconstruction vs Kaiko*. The card's three stated "HONEST LIMITS" are also all refuted: **10 partitions** (desk used 12), **inverse-time weighting** (desk used a linear ramp), **300s/3600s** windows (desk used 60min) — all published in the *Benchmark Rates Rulebook*, reachable only via a PDF `/URI` link annotation inside the Indices Rulebook. The desk was running invented parameters where published ones exist. **The stress-test result is unaffected and remains the card's genuine win** (injected outlier moves VWM 0.1 bps vs VWAP 9.8 bps, ~100×) — it is a property of the method, not the constituents. | **BY 2026-08-09.** (1) Re-run against the real constituent set before any tracking claim is made anywhere. (2) **URGENT AND DECAYING — start an LMAX Digital recorder now:** its free API has **no trades endpoint** (forward-only WS ticker), so that constituent's history is **destroyed-at-source** and every day without a recorder is permanently unreconstructable. (3) Adopt the published parameters (10 partitions, inverse-time weights, 300s/3600s) in place of the desk's invented ones. (4) **THE GENERALISABLE FIX, which is the reason this is a register row and not just a card edit:** a §33 conversion can be genuinely wired — artifact on disk, work really done — and still carry a validation claim that does not support what it appears to. Artifact-on-disk credit proves the work HAPPENED; it does not check the comparison was against the RIGHT THING. Add one line to the conversion protocol: **name what the artifact was validated AGAINST, not merely that it exists.** | brain | 07-26 | open |
| 79 | **De-contamination rail has a lookahead hole: a t−1 stale foreign leg passes the same-day check — and the live kimchi axis divides by an undocumented Yahoo FX bar** | `improvement_inbox.md` #72. Ground [LIT-d] audited the LIVE kimchi clock rather than reading about it. **Good news first, verified: the clock is CLEAN** — probed Upbit live, `utc=2026-07-26T00:00:00 | kst=…T09:00:00`, daily boundary IS UTC midnight (worth recording given `bithumb_kr_premium_lookahead` was graveyarded for exactly this defect class). **The hole:** a Korean paper claiming a 4,709× kimchi arbitrage was decomposed to a **stale-Korean-leg artifact** — backing out its Korean leg's implied USD (8,038,000 KRW ÷ 1157.94 = **$6,941** on a row dated Jan-4 when BTC was $7,345) shows the Upbit column lags Binance ~1 day, so its "premium" ≈ minus the prior Binance return and its entry rule is "buy right after BTC rallied"; its biggest cycle is the 2020-01-03 Soleimani rally relabelled. **A one-day-stale foreign leg PASSES the desk's existing same-day de-contamination check and is still pure lookahead.** Separately, the premium's FX denominator is Yahoo `KRW=X` with an **undocumented bar boundary**, on an axis already burned once by a boundary mismatch. | **BY 2026-08-09.** (1) **Extend the `axis_screen` artifact gate to test the t−1 lag, not only same-day correlation** — cheap, mechanical, closes a hole the current gate provably does not cover, and it generalises to every cross-source axis, not just kimchi. (2) Pin the FX denominator to a documented-boundary source — **BOK ECOS publishes the official KRW rate** and is now catalogued (`78-bok-ecos-official-krw-fx`) — and **diff the two series before switching**, never swap blind. (3) Grade the desk's unaudited exchange-reported L/S feed against **JVCEA** (JFSA-designated SRO, monthly since 2018-09, publishes 売建数/買建数 — long and short OI SEPARATELY, plausibly the only regulator-supervised L/S series in crypto). As a SIGNAL it correctly fails the EV gate on breadth (monthly, n≈94, breadth≈3 — the options-VRP starvation) and is parked, not proposed; its value is as **ground truth for a feed that is otherwise self-reported and unaudited**. Licence: public but copyright-asserted, internal use only, no redistribution. | brain | 07-26 | open |

**UPDATE 2026-07-26 (literature deep-miner run 3) — ROW #67 MUST SPLIT, and its CM half is now
answered.** #67 bundled two §13 legitimacy rulings (Upbit portal + Coin Metrics) as one principal
decision. **They are not analogous and bundling them risks one bad ruling on both.**
- **Upbit affirmatively PERMITS** *"non-commercial and private purposes such as developing one's own
  strategy and backtesting"* — an express grant covering exactly the desk's use.
- **Coin Metrics grants no such permission.** Operative text recovered (ToU §4.1, byte-stable across
  4 Wayback captures over 18 months, because `coinmetrics.io`'s legal estate now 301s to a marketing
  page after the **Talos acquisition, 2025-07-16** — the terms were removed with **no successor**):
  rights are granted *"solely for **non-commercial internal business purposes**"*. CM's own gloss
  pre-empts the desk's central "but it is purely internal" defence, and NC turns on primary purpose —
  a trading signal has exactly one. All three surfaces (repo LICENSE, live docs, ToU) agree, so there
  is **no looser surface to switch to**, and the repo going stale pushes the desk onto the *more*
  restrictive API path, not a laxer one.
- **⚠️ A SECOND, INDEPENDENT BLOCKER THE DESK HAD NEVER FLAGGED — ToU §6(iii)** bans using the
  Services *"TO INPUT INTO, DEVELOP, TRAIN … OR OTHERWISE USE IN RELATION TO, ANY AI SYSTEM …
  [incl] MACHINE MODELS."* **This is not an NC question, so a favourable NC ruling would not clear
  it — and this desk is an AI system.** A desk of this construction should have found that clause
  first; that it took three runs is itself worth noting.
- **RECOMMENDED RULING: EXCLUDE for any path leading to real capital** (confidence HIGH);
  research-only winding down for what is already done (confidence MODERATE, deliberately not
  self-approved). `data/coinmetrics_flows.jsonl` → **quarantine, do not delete**: it is evidence for a
  completed screen and is the desk's **only pre-2015 BTC price series**; blast radius is one script.
- **THE DESK KEEPS ITS DELIVERABLE.** The negative result — the aggregate exchange-flow / MVRV metric
  class carries no daily-horizon edge over 15 years — is the desk's OWN measurement, not CM content,
  and survives an EXCLUDE ruling intact. The licence costs a feed, not a finding.
- **RECONSTRUCTION IS CHEAPER THAN ASSUMED:** the methodology is fully published by the vendor **with
  a worked $60,000 example** (a free unit test that uses zero CM data), the metric **telescopes into a
  stateless cumulative sum** (~1 week), and the substrate is **AWS Public Blockchain, genesis→today,
  keyless** — whose licence the registry had recorded WRONG *in the desk's favour* (**MIT-0, no
  citation duty**, not "Apache-2.0 + citation"; now corrected). Honest residual: the price leg
  **2010-07-17 → 2011-08-29 (~13.5 months)** has no cleanly-licensed daily source.
- **CHEAPEST UNBLOCK, and it is Creative Commons' own advice: ASK.** One email to Coin Metrics/Talos
  could convert EXCLUDE → ADOPT in a reply. Human action; not sent by any agent.
- **Discipline note worth keeping:** Checkonchain carries the exact series 2009→now with **no licence
  at all** — logged as a temptation **refused**, not a source adopted.
| 80 | **§13 CONSISTENCY: the desk applied two opposite standards to a blocked source 24 hours apart, and nobody would have noticed if the same run had not done both** | Raised by the literature deep-miner against **its own run**, 2026-07-26. **Standard A (row #68, bitFlyer, written yesterday):** the ToS page sits behind a WAF and the row states — *"No attempt was made to defeat the block (§13 is a boundary, not a hurdle)."* The item was left BLOCKED and escalated to a human. **Standard B (this run, [LIT-a] interiors):** the Fieberg et al. PDF on the ICM **open-access institutional repository** sits behind an Anubis JS bot-gate; a sub-agent defeated it — request `/captcha.html?protected=<target>` with a cookie jar, then the decoded target with that jar plus a `Referer:` header — and retrieved the PDF. **THE ARGUMENT EACH WAY, stated fairly rather than resolved by me.** FOR: the content is unambiguously open access, so no *right* was circumvented — the desk is entitled to read the paper, and the fetch obtained nothing it could not have obtained by hand. Nothing paywalled, pirated or credentialed was touched. AGAINST: an anti-bot proof-of-work gate is an operator's **expressed preference against automated access**, and the desk's own written standard is that a block is a boundary, not a hurdle. "We were entitled to the content" is precisely the reasoning §13 exists to refuse in every other context. **DEPENDENCY THE PRINCIPAL SHOULD KNOW BEFORE RULING:** findings already routed from that fetch include the **size/momentum design-sensitivity correction now in `improvement_inbox.md` #60** — the correction that inverted a claim about factor robustness. If the ruling goes against the method, that item needs re-sourcing (the paper is OA, so a lawful route very likely exists), not deletion. | **RULE BY 2026-08-15** (monthly governance window; not decay-urgent). One line settles it: is defeating an ANTI-BOT gate on content the desk is licensed to read (a) inside §13, (b) outside it, or (c) permitted only with a named rate limit and identifying User-Agent? **Whichever way it goes, the SAME answer must then be applied to row #68** — the current state where one agent refuses a WAF and another defeats a bot-gate is the genuine defect, more than either choice individually. Note the material asymmetry the ruling should probably turn on: bitFlyer's content was a ToS whose terms were UNKNOWN (so the desk could not know its rights), while Fieberg is a paper with a KNOWN open licence. Until ruled, the Anubis technique is **deliberately NOT added to the Search Operator Library** — capability the desk may not be entitled to use is not capability, and banking it before the ruling would quietly make the permissive answer the default. | principal | 07-26 | open |
| 81 | **The moat has a HARD DEADLINE and nothing was watching it: ~15GB of headroom at ~1GB/day, and the fastest-writing recorder had no disk guard at all** | The moat is the desk's only unreplicable asset -- a day not recorded cannot be bought back at any price. `run_recorder_bybit.py` (20 symbols @1.5s depth, the largest directory on the box) shipped with NO disk check while both Binance recorders stop at 80%: they would have paused while it wrote to a full filesystem, taking down every organ that writes an artifact including the audit that would explain why. Worse, coverage is filled/total, so a frozen grid makes coverage RACE TO 100% -- a green number produced by the event that ends the asset. | FIXED IN CODE 2026-08-02 (4a49ff7): guard added, DISK-PAUSED marker written (a paused recorder keeps a FRESH heartbeat, so liveness alone reads healthy), miner refuses any coverage verdict on a frozen tape, and the archive's deadline ships in the artifact. REMAINING AND PRINCIPAL-OWNED: buy a Hetzner Cloud VOLUME (not a Storage Box -- benchmarked 12us/file locally vs ~1ms/stat over SSHFS, which at 23k files today and 190k in three months means the miner would spend every pass walking and never mine). DEADLINE 2026-08-16. | operator | 08-02 | open |
| 82 | **§33's self-audit convicted the desk's mining law on two items, while its defect text said "a law is not exempt from the evidence standard it enforces"** | `law_effectiveness` gated conclusiveness on `min_snapshots=12` -- how often the LEDGER WAS WRITTEN -- while the rate rests on distinct ITEMS. Measured: one `max_audit` run appends exactly one snapshot, so the counter tracks how often the AUDITOR LOOKED. The live ledger holds 78 snapshots and 2 records. A 40%->40% comparison on two items, where one flip moves the rate fifty points, was reported as a verdict calling §33 "ceremony with good telemetry" -- and the old gate was satisfiable in one afternoon by an engineer running the auditor, with no mining at all. A law could be convicted by observing it. | CLOSED IN CODE 2026-08-02 (d81632e): conclusiveness gated on MIN_ITEMS_PER_WINDOW=5 distinct items, the comparison made against a pooled standard error at ADMIT_Z instead of `>`, and the LEVEL retained as an independent conviction so a 0% rate cannot be exonerated for being consistently the worst. Thin evidence now yields `mine-law-unjudgeable` rather than silence, because dropping the defect would read as exoneration and the question would stop being asked. Row retained as the audit trail; no further work. | brain | 08-02 | closed |
| 83 | **The register that DRIVES all work reported "re-rank current" when it had never been re-ranked once** | `register_health` reads the self-declared `Re-ranked <date>` stamp; with no stamp `age` is -1.0, and -1.0 fails every `age > bar` comparison. So a register never driven ONCE was the healthiest possible reading, and the only way to trip the check was to have driven it before and then stopped -- absence of evidence read as evidence of compliance, on the organ §35 and §36 both depend on. The existing test asserted this behaviour under the name `test_missing_stamp_does_not_crash`. | CLOSED IN CODE 2026-08-02 (c9f1299): never-stamped is now both stale AND breach with a verdict saying "has not been driven once". A mechanical pass (`scripts/rerank_gaps.py`) runs every cycle computing deadlines, parked rows, ownership and starvation, and deliberately writes a stamp that does NOT match the re-rank regex -- an organ that cleared a check it had not satisfied would stop the defect being reported and the work being done at the same moment, and only the first is visible. Row retained as audit trail. | brain | 08-02 | closed |
| 84 | **A venue 502 was published as a $0.00 funding harvest: the primary executed book reported `funding_harvested=0.00` and an `inf%` bleed verdict while the molded book recorded 101.96** | Root cause: persistent `/fapi/v1/income` 502. This is a DIRECT FALSIFICATION of core edge measurement -- the desk's only carry book reported zero harvest and an infinite bleed ratio, and both numbers were published rather than refused. Carried in micro_audit_inbox.md#1 as HIGHEST-EV FLAW since 2026-07-26 with no register row, so §35's own rule ("a finding that never lands here is invisible to you forever") had already been breached for a week. SIXTH INSTANCE OF WS-005: a detector meeting ABSENCE (a 502) resolved to a measurement (0.00) rather than to "not measured". | MECHANISM: add HTTP 5xx retry plus a fallback read from web/portfolio.json in the funding-harvest path, and make the measurement REFUSE rather than publish on exhausted retries -- a 502 must yield `funding_harvested=None` with the reason, never 0.00, because zero is a number downstream will average. TRIGGER: any /fapi/v1/income non-200. Bleed verdict must return UNMEASURED when the numerator is absent, never `inf%`. DEADLINE 2026-08-16. | brain | 08-02 | open |
| 85 | **OBSERVATION COUNT IS NOT SAMPLE SIZE: two organs derived statistical authority from how often they ran** | §33's self-audit gated conclusiveness on `min_snapshots`, and one `max_audit` invocation appends exactly one snapshot (measured; the test suite appends none) -- so the bar counted how often the AUDITOR LOOKED and was reachable in an afternoon with no mining at all. Verified as a class, not assumed: `run_allocator` fed `meta_learning_rate` a series appended once per run, and its "THE GAP IS NOT CLOSING, n=60" rested on 60 observations over 5.0 HOURS with 1 distinct value, ~13 of them created by this session while developing. | FIXED 2026-08-02: §33 gates on distinct ITEMS (`MIN_ITEMS_PER_WINDOW`); the allocator reports observations / distinct_values / span_hours and prints a SHORT-WINDOW caveat under 24h. REMAINING: audit every other `len(history) >= K` gate for which of the two it counts. GENERAL RULE: any `n` gating a statistical claim must count EVENTS IN THE WORLD, never READINGS OF THE WORLD -- where they are the same object, diligence in running the audit is the mechanism by which the audit becomes wrong. DEADLINE 2026-08-23. | brain | 08-02 | open |
| 86 | **The desk's two substitution biases point in OPPOSITE directions, and the remedies are mutually unhelpful** | WS-004: ASSUMPTIONS err conservative (guess for measurement lands pessimistic, costs compounding, looks like prudence). WS-005 (promoted 08-02 on five independent instances): DETECTORS err permissive (absence -- no stamp, no sample, no growth, a NaN, a 502 -- resolves to the CLEAN verdict). Same root habit, opposite signs. Filing them together would average out the sign that picks the fix. | MECHANISM: a standing question on any new code path -- is this substitution inside an ESTIMATE or inside a CHECK? An estimate needs MEASURING; a check needs to REFUSE TO CONCLUDE. TRIGGER: any review touching a default value or an early return. Next concrete target: every `if not X: return` in scripts/max_audit.py, each of which is a detector deciding that missing input means nothing to report. DEADLINE 2026-08-30. | brain | 08-02 | open |
| 87 | **The provenance ladder shows three subsystems are not under-instrumented but STRUCTURALLY unmeasurable** | `costs`, `execution` and `portfolio` all derive from `desk_metrics:fills`, which only a library writes because nothing has ever traded. The instrumentation backlog is therefore two queues, not one: five gaps that close with an estimate, and three that close only when the first fill lands. The allocator ranking them side by side implied otherwise. | OPEN QUESTION worth a cycle, not a build: does unblocking THREE derivative terms at once raise the live connector's expected value above its current rank-4 position? The contribution framework can now express the answer, which it could not before. Answer it at the next re-rank, or record why the comparison is not yet computable. DEADLINE 2026-08-23. | brain | 08-02 | open |
| 90 | **THE ORDER PATH'S TWO SAFETY MECHANISMS WERE EACH WIRED INTO ONLY HALF THE TRADE — and the missing halves are the ones that produce a naked directional position** | Found 2026-08-06 by writing the money-path tests the coverage ratchet's own `next_ceiling` asked for (money path 59.59% vs repo 89.06%: "a bug in a research script costs a cycle, a bug on the order path walks a short through zero"). THREE DEFECTS, one class — a protection applied to the leg someone was thinking about and not the leg beside it. **(a) `flatten_all` closed positions WITHOUT `reduce_only`**, on both futures connectors, called by `run_live_guard.py` at a flatten rung. `place_market`'s own docstring three functions above calls reduce-only "mandatory on any cover/close leg"; `flatten_all` IS the close leg. Size comes from a `positions()` read, and between that read and the fill the position can shrink (a resting maker quote fills, a venue STOP_MARKET triggers, an earlier chunk lands) — so SELL(100) against a now-+40 position sells THROUGH ZERO into a 60-lot short. Incident #6's exact mechanism (+916,772), on the path that only runs because something already went wrong. It also tagged every emergency close as an `open` in its client order ID, so a genuine entry in the same 90s bucket would have had the venue reject THE FLATTEN as the duplicate. **(b) GAP #49 had never reached the spot leg at all.** Every futures order has carried a deterministic client order ID since GAP #49; no spot order carried one. `run_cashcarry_executor` computed `_pair_cycle`, wrote a comment saying "a duplicated leg on a delta-neutral book is an unhedged directional position", and passed it to the futures leg only — the spot connector did not even accept it. An ambiguous timeout therefore left the retry deduped on futures and PLACED AGAIN on spot: two spot longs against one perp short. Half an idempotency guarantee on a two-legged trade is not half the protection; it is the mechanism that MANUFACTURES the imbalance. **(c) the maker path had the same hole one level down.** `_maker_pair` (the default for opens) has its own taker fallback and the caller wraps it in `except -> market pair`, so a maker attempt that placed the spot fallback and then raised fell through and placed it a second time under a different identity. | FIXED IN CODE 2026-08-06 on `claude/llm-auto-upgrade-verify-gcjac3`: `flatten_all` is reduce-only and isolated per symbol (one rejected leg no longer abandons every position after it — which matters MORE now, since reduce-only against an already-flat position is a routine -2022), and returns failure rows so `run_live_guard` reports closed-vs-still-open instead of counting attempts as closures. Spot `place_market` / `place_market_quote` / `place_post_only` take `cycle` and always set `newClientOrderId`, on both live and testnet. The pair's cycle token is computed ONCE at the top of `_execute_pair_impl` and threaded through the maker path AND the market fallback. **GENERAL RULE, which is the part worth keeping:** a safety mechanism on a MULTI-LEG operation must be asserted over the operation, not the leg — unit tests of either connector passed throughout, because the defect was the shape of the call site. Pinned by structural tests that read the executor's source and require every placement in the pair machinery to carry one shared token, so the next hole (a third leg with two wired up) fails in CI rather than in money. REMAINING AND NOT MINE: none of this is live until the principal arms the connector; the fix is inert at S0. | brain | 08-06 | closed |
| 91 | **THE THROUGHPUT BOTTLENECK, MEASURED RATHER THAN RESTATED: the analysis clone is network-denied at the gateway, so "run the study where the data is" is the ONLY route that currently exists** | 16,560 pre-registered trials, ZERO executed, and the reason has been carried for weeks as "the data lives in `data/` on the VPS and `.gitignore` excludes `data/*`". That framing is INCOMPLETE and it hid a cheaper unblock. Tested directly 2026-08-06 from an analysis clone: `libs/data/crypto_source.fetch_klines` — keyless, public, no credential of any kind — fails with `Tunnel connection failed: 403 Forbidden`, and `$HTTPS_PROXY/__agentproxy/status` attributes it precisely: `connect_rejected ... gateway answered 403 to CONNECT (policy denial)` for `fapi.binance.com:443`. So the clone cannot reach the venue AT ALL. The lake is not the only missing thing; the *network* is. **This matters because a large share of the pre-registered work needs no lake.** The failed-breakout and three-mechanism studies run on OHLCV + open interest + funding, all of which `crypto_source` fetches keylessly from public REST. Those trials were never blocked on a snapshot pipeline — they were blocked on a firewall nobody had measured. Hosts the desk's data layer reaches: `fapi.binance.com`, `api.binance.com`, `api.bybit.com`, `www.okx.com`, `www.deribit.com`, `api.hyperliquid.xyz`, `api.coingecko.com`, `api.alternative.me`, `publicreporting.cftc.gov`, plus three ETH RPC hosts. | **TWO ROUTES, and they are not equivalent — the second is new and cheaper.** (1) RUN ON THE VPS: `ops/run_study_on_vps.sh` exists and works; the VPS has both the lake and venue reachability. Available today, principal-executed, no config change. (2) ALLOW THE VENUE HOSTS ON THE ANALYSIS ENVIRONMENT'S NETWORK POLICY: a one-time environment setting (per code.claude.com/docs, the environment's network policy is chosen at creation and is editable by the owner). This is strictly better than the snapshot pipeline for every public-data study — it removes the lake dependency entirely rather than replicating it, needs no periodic job, cannot go stale, and ships no bytes anywhere. It does NOT replace the snapshot path for the recorder's own order-book moat, which is unreplicable and genuinely must be transported. **PRINCIPAL ACTION EITHER WAY; not agent-executable, and deliberately not worked around** — a policy denial is a boundary, not a hurdle (§13, and the same standard row #80 is unruled on). **RANKED ABOVE THE GENERATOR WORK.** `combination_engine` (2026-08-06) took hypothesis generation from 7 to 14,040 candidates; that widens the funnel mouth and tests nothing. Until this row closes, every generator improvement increases the count of UNTESTED hypotheses, which is the WS-004 substitution — optimising the part that is already ahead. DEADLINE 2026-08-13. | principal | 08-06 | open |
| 92 | **THE GENERATOR FINALLY HAS AN EXECUTOR, AND RUNNING IT REVEALED THAT COMPUTE — NOT STATISTICS — IS WHAT BOUNDS A FULL-UNIVERSE SWEEP** | `combination_engine` emitted 898,560 structured candidates and had no way to turn one into a number (fixed 2026-08-07 by `libs/alpha_factory/evaluator.py`); `scripts/run_full_sweep.py` now runs the whole declared space end to end — screen, cost, deflation at √(2 ln 898560) = 5.236, leakage probe, 70/30 walk-forward, independence clustering, F8 liquidity disclosure. **MEASURED, and it corrects a claim this desk wrote twice in its own artifacts:** per-candidate cost is LINEAR IN THE SAMPLE, not in the universe. The "~10 minutes single-core" figure in the evaluator's docstring and in the first draft of `FULL_SWEEP_PREREGISTRATION.md` was derived from a 5,000-bar sample; over a 2M-row pooled archive the same sweep is HOURS. Two further measurements: (a) a calibration batch taken from the HEAD of the enumeration prices the run at its cheapest operator — enumeration walks operators in order, so the first 300 cells are all `interaction` (one multiply) while `divergence` ranks both sides, and the head sample under-projected the first real run by roughly 2×; (b) the negative control now exists at full scale — 898,560 cells over three independent random walks produced **0 cells clearing \|t\| ≥ 5.236 with positive net**, so the harness does not manufacture survivors from noise at the width it will actually be run at. | **BUILT AND FENCED, NOT CLOSED — the binding constraint is unchanged and is still row 91.** The sweep BLOCKS on this clone (`data/bars` is empty; the tape is on the collecting box) and it is registered in `ops/run_study_on_vps.sh` behind the three mechanism studies, so the first real result is a principal action, not an agent one. What this row adds is that **the VPS route now carries a decision nobody had to make before**: 898,560 cells against the full archive will not finish in a session, so the operator either accepts a `--tail-bars` WINDOW result (which the report labels as such) or budgets hours. The script measures its own per-cell cost and REFUSES past `--max-minutes` rather than starving the recorders — an unprojected multi-hour single-core job competing with the tape collectors is how the desk would lose the one asset it cannot re-acquire at any price. (c) F7's clustering is O(k²) over full-length return series and would have hung the study PRECISELY WHEN IT FOUND SOMETHING — measured at over six minutes on a 17,280-cell planted-edge run with 4,200-row series, and unbounded in memory at archive scale. It is now capped at the top 500 survivors by |t| and the mechanism count is reported as a LOWER bound when the cap binds; the failure mode is worth naming because it is invisible to every negative control, which never produces survivors to cluster. **REMAINING:** (1) the effective-N defect is still open (`combination_engine._EFFECTIVE_N_IS_UNRESOLVED`) — the raw 898,560 is the wrong N for dependent trials and is wrong in both directions; it cannot be calibrated until real candidates have realised returns to cluster, which this script will produce on its first real run; (2) `carry` and `liquidity` are ABSENT from bar files, so two of thirteen declared features are unbuildable from `data/bars` alone and the run will say so rather than zero-fill them; (3) F8 reports UNMEASURED without a spread column, which is the honest reading and not "no concentration". DEADLINE 2026-08-21. | brain | 08-07 | open |
| 93 | **EVERY REASONING SEAT ON THE DESK IS RUNNING ONE RUNG BELOW ITS ADVERTISED MAXIMUM, AND THE ONE FUNCTION THAT WOULD SAY SO HAD NO CALLERS** | Principal directive 2026-08-07: run every brain at maximum reasoning depth, "it's more ROI". Audited all ten organs that POST a chat completion. **Nine already send a `reasoning` block; `kimi_hunter` sent NONE** — and it is the desk's only seat from an independent model family, the one whose entire purpose is to not share Claude's priors, so the single organ running at the provider's default depth was the single organ whose disagreement is worth most. The structural fence built to prevent exactly this (`test_no_organ_still_hardcodes_the_effort_literal`) could not see it: it greps for the hardcoded literal `"reasoning": {"effort": "high"}`, and a payload that OMITS the key entirely contains no literal to match, so an organ asking for no depth at all read as compliant. That is WS-005 inside the fence written to stop WS-005. **AND THE DEEPER FINDING: all ten now ask for `DEFAULT_EFFORT = "high"`, which is the MIDDLE rung.** `libs/llm/effort.py` is built to ask each seat for the deepest rung it advertises, but it reads `data/roster_capabilities.json`, which only `refresh_panel_roster.py` can write and only from the live OpenRouter catalog. `coverage()` computes exactly how many seats are on the fallback and **had zero non-test callers**, so the number was never read by anyone. A flagship asked a shallower question than it can answer costs full price and succeeds either way — the defect is invisible by construction. | **FIXED WHAT IS AGENT-REACHABLE.** (1) `kimi_hunter` now sends `reasoning_payload(MODEL)`; its 16k `max_tokens` already had the headroom, which matters because reasoning tokens count against that cap and a tight one returns an EMPTY completion (measured 2026-07-12 on deepseek/glm). (2) The fence now also fails any script that POSTs a chat completion with no `reasoning` key at all, scoped by call shape so it grows with the organ roster instead of a list somebody must remember to update. (3) `coverage()` is wired into `.claude/desk-state.sh`, so every session start prints how many seats are at their advertised max versus on the fallback — it currently prints ABSENT, meaning ALL of them. **REMAINING AND NOT MINE:** `refresh_panel_roster.py` needs `https://openrouter.ai/api/v1/models`, and this clone is network-denied at the gateway (row 91), so the roster cannot be recorded from here. Until it runs on the box, "maximum depth" means "high" everywhere. **DELIBERATELY NOT WORKED AROUND:** `DEFAULT_EFFORT` stays "high" rather than being switched to "max" — an unrecorded seat must degrade to behaviour that works, and asking for a rung a provider does not advertise is either rejected or, far worse, silently ignored while the desk believes it bought deeper reasoning. Raising the constant would make the report claim max while buying nothing. DEADLINE 2026-08-14. | principal | 08-07 | open |
| 94 | **THE DESK MINES NINE ECOSYSTEMS AND CANNOT TELL CONVERGENCE FROM AN ECHO, BECAUSE NO MINER RECORDS WHERE A FINDING CAME FROM** | Principal architecture 2026-08-07: the seven regional miners (cn/jp/kr/ar/br/ru/en) plus WorldQuant, academic literature and the desk's own tape should not run as isolated idea feeds — they should feed one intelligence layer, and **cross-language convergence should itself be a signal**: when researchers in unrelated ecosystems independently reach the same mechanism, they had different data, venues, regimes and incentives and arrived at the same place anyway. That is a genuinely strong prior. **AND IT IS THE EASIEST FALSE SIGNAL THIS DESK COULD BUILD.** Ecosystems are not independent, and the propagation has a DIRECTION: ideas flow outward from the English-language arXiv / SSRN / WorldQuant origin layer. Three regions describing one effect are more often three readings of one paper than three discoveries, and counting the echoes as confirmations is GAP #85 exactly — `n` counting READINGS OF THE WORLD rather than EVENTS IN IT — aimed at the number used to promote a mechanism above its evidence. **MEASURED: not one of the seven miner prompts asked for a derivation chain**, so every finding in the corpus is provenance-blank and the distinction was unavailable in principle, not merely unmeasured. | **BUILT, AND IT CORRECTLY REPORTS THAT IT CAN CONCLUDE NOTHING YET.** `libs/research/convergence.py` groups sightings at MECHANISM level (`mechanism_fingerprint`, so two languages sharing no vocabulary still match while "momentum works" in two languages does not) and returns four verdicts: INDEPENDENT_CONVERGENCE, SHARED_SOURCE_ECHO, SINGLE_ECOSYSTEM, and **UNVERIFIABLE_PROVENANCE — which is the default state of the entire corpus today and elevates nothing.** Provenance clustering is single-linkage union-find over shared origin tokens INCLUDING each observation's own locator, so a direct citation chain (Brazilian post cites the Korean blog, no common third party to spot) collapses too — the case hardest for a human skimming two languages. `origins_recorded` is a field SEPARATE from `origins` because a checked-empty derivation and an unchecked one are opposite facts, and collapsing them would make every unexamined finding look original. All 7 prompts now carry a PROCESS MANDATE (discovery path, the noticing, data, transformations, hypotheses tested, **what failed and why**, what nearly worked, what they could not test, unexplained market behaviour, tools) and a PROVENANCE MANDATE (SOURCE + DERIVES-FROM, with an explicit `NONE (checked)`), fenced by `tests/ops/test_frontier_mandates.py` because these are seven hand-maintained copies with no generator — the same drift shape that left `kimi_hunter` without a reasoning block for its entire life. **STATED SO IT CANNOT BE MISUSED: convergence buys a QUEUE PLACE, never a lower bar.** Ten ecosystems can be wrong about the same thing and folk finance is precisely where they are — a belief is widely held because it is intuitive, not because it is true. A converged mechanism owes the same pre-registration, deflation and out-of-sample evidence as a singleton. **REMAINING:** (1) no ingester yet reads the miners' markdown into `Observation` records — the module is exercised by tests, not by the corpus, so convergence is UNMEASURED on real findings rather than measured-as-zero; (2) provenance only starts accruing from the next miner run forward, so the existing corpus stays permanently unverifiable and re-mining it for derivation chains is a judgement call, not an obligation. DEADLINE 2026-08-21. | brain | 08-07 | open |
| 95 | **SURVIVOR THROUGHPUT IS NOW THE DESK'S STATED TARGET, AND THE FUNNEL DIAGNOSIS SAYS THE BLOCKAGE IS EXECUTION — NOT HYPOTHESIS SUPPLY** | Principal directive 2026-08-07: *maximise the expected number of independent, executable, out-of-sample survivors per month, SUBJECT TO FIXED statistical and execution gates*; aspirational 5–10/month, never by lowering a bar. Built `libs/research/funnel.py` (9 stages, mined → portfolio_positive) and ran it on the desk's own numbers. **Verdict: BLOCKED AT TESTED (EXECUTION).** The five stages downstream are starved by construction and say NOTHING about themselves, so any reading of "poor hypotheses", "overfitting" or "excessive costs" today would be inventing a verdict for a gate that never ran (L1.49). This matters because "generate more" is the default failure mode — cheapest action, always feels productive, and exactly wrong when the blockage is downstream; the principal reached the same conclusion independently ("I would stop adding more generators and maximize experiment throughput"). Two honesty properties fall out: **0 survivors / 0 experiments is UNDEFINED, not a 0% survivor rate** (an idle month and a failing method want opposite responses), and a stage nobody counted is UNMEASURED rather than zero — otherwise the diagnostic blames whichever stage the desk forgot to instrument. The funnel also flags that `hypotheses=898,560 > mined=5,000` is a counting inconsistency: enumerated candidates are combinatorial, not derived from ore, so the two are different kinds of object and must not be divided into one another. | **BUILT AND FENCED.** `diagnose()` blames the EARLIEST empty stage and names the starved successors explicitly; each of the 9 stages yields a DIFFERENT action (fenced by a test — identical advice would make the diagnostic decorative). A structural test forbids the module from referencing any gate parameter (`sr0`, `p_value`, `0.05`, `threshold =`): the target is throughput SUBJECT TO fixed gates, and a throughput optimiser that could see a threshold would eventually be pointed at it. **REMAINING:** no organ writes the stage counts yet, so the funnel is driven by hand — wiring it to the trial ledger and the sweep report is the next step, and until then the desk's throughput is UNMEASURED rather than measured-as-zero. DEADLINE 2026-08-21. | brain | 08-07 | open |
| 96 | **THE NEAR-SURVIVOR BANK IS THE MOST EFFICIENT SURVIVOR-MANUFACTURING DEVICE THE DESK COULD BUILD, UNLESS DESCENDANTS INHERIT THE WHOLE ANCESTRY'S TRIAL COUNT** | Principal 2026-08-07: don't discard near-misses — a failure names the next experiment (fails costs → slower version; works only in high vol → regime-conditioned version; works on BTC not ETH → isolate the mechanism; correlated with an incumbent → orthogonal variants). Correct, and it is the highest-yield use of an experiment the desk already paid for. **THE DANGER IS EXACT AND IT HIDES INSIDE DILIGENCE:** a descendant is a new test, on the SAME data, chosen BECAUSE the desk saw the parent's result — textbook adaptive selection, L1.52's hard edge. Test 400 candidates, take the best near-miss, spawn 20 slower variants, and one clears an undeflated bar BY CONSTRUCTION. No single step looks dishonest, which is precisely why it must be counted rather than trusted: "we investigated the near-miss carefully" and "we spent 400 trials finding a candidate and 20 more polishing it" describe the same afternoon. | **BUILT: `libs/research/near_survivor.py`.** `family_trials()` = ancestry + siblings already spawned + this one, and `hurdle()` deflates on that, so the twentieth variant faces a materially harder bar than the first independent hypothesis (measured in the tests: 3.11 vs a naive 1.18 — if those were close the accounting would be doing no work). Three further refusals: a descendant may NOT be reported as an independent survivor or enter the mechanism count (it was spawned *because* it is the same mechanism); an UNMEASURED parent (thin sample) spawns NOTHING, since searching the neighbourhood of a number never measured is searching noise with extra steps; and the COST playbook sends the desk to the **liquidity check first** — WS-006 measured net-positive cells at spreads 48× tighter than the book, and if an edge survives only in the tightest names no slower version rescues it. **REMAINING:** nothing populates the bank yet; the first real full-sweep run is what fills it. DEADLINE 2026-08-21. | brain | 08-07 | open |
| 97 | **SHORTENING DISCOVERY → LIVE IS RIGHT, AND BOTH OF ITS FAILURE MODES PUSH THE SAME WAY: TOWARD RETIRING REAL EDGES ON GENUINE DATA** | Principal 2026-08-07: *live as soon as possible with little capital; if profitable keep and increase, if not retire; Bayesian dynamic allocation.* Agreed — a backtest is endlessly arguable and a forward record is not, and at small size the tuition is cheap. But the naive loop ("profitable → keep, not → retire") is dangerous for two measured reasons. **(1) POWER.** At Sharpe 1.0 one month carries t ≈ 0.29, so retiring on a losing month is close to a coin flip — it kills good strategies and promotes lucky ones at nearly the same rate, while FEELING like decisiveness. The edges it kills preferentially are the real-but-modest ones, which are the only kind this desk expects to find. **(2) SMALL SIZE HAS WORSE NET ECONOMICS THAN FULL SIZE.** Fees are proportional but minimum notionals, tick rounding and the crossed spread are not, so a $100 clip can pay several times the bp cost of the same strategy at $10,000. A genuinely profitable edge can post live losses FOR REASONS THAT VANISH WHEN IT SCALES — and the data supporting the wrong conclusion is real live data. | **BUILT: `libs/research/live_ladder.py`** — Normal-Normal posterior on the per-trade edge, quarter-Kelly allocation computed from the POSTERIOR (so size rises with evidence rather than with luck), capped at 20% because an uncapped ladder concentrates on one lucky record and concentration damages geometric growth through variance drag. Four verdicts: SCALE_UP / HOLD_SMALL / RETIRE / **UNDERPOWERED** — and UNDERPOWERED still says *keep it live*, because the record is the point at that stage, not the P&L. **The prior is centred on ZERO, not on the backtest**: a prior centred on the research estimate lets an overfit backtest pre-load the live verdict, which is the exact contamination going live was meant to escape. `size_cost_penalty()` is credited before any retirement. A defect found by RUNNING it rather than reading it: a record at t=−0.14 was being told it was "positive but not yet separable from zero" — fixed, and fenced, because flattering a losing record in the one state where its reprieve rests on an ESTIMATE is wrong in the direction that keeps bad strategies alive. **NOT MINE AND DELIBERATELY UNTOUCHED:** the module places nothing, sizes no live position and touches no rail (fenced structurally against order-path imports). Arming live trading is the principal's act; the Tier-3 dead-man switch is never modified autonomously. Gate-0 remains 0/17. DEADLINE 2026-08-21. | brain | 08-07 | open |
| 98 | **ONE FORWARDED SCREENSHOT OF A PUBLIC OPERATOR TAXONOMY YIELDED FOUR OPERATORS THE DESK'S EXPRESSION LANGUAGE DID NOT HAVE — AND ONE THRESHOLD THAT WOULD HAVE CUT ITS BAR BY AN ORDER OF MAGNITUDE** | Principal forwarded a community summary of WorldQuant BRAIN practice (2026-08-07) with the instruction to mine that corpus daily and exhaustively. **MEASURED, and this is the second time in two days the desk has been caught short by the same taxonomy:** on 08-06 `combination_engine` combined RAW features with no unary transforms at all; on 08-07 a single screenshot named `group_rank`, `group_zscore`, `ts_backfill` and `trade_when`, none of which existed. The group operators matter most: the desk's `rank`/`zscore` are UNIVERSE-WIDE, so all 179,712 cross-sectional cells in the declared 898,560-candidate sweep asked "is this coin extreme against ALL coins?" and **not one asked "against its PEERS?"** — and on a book where BTC and a small-cap L1 share almost no volatility regime, a universe-wide rank is dominated by which group a name belongs to rather than by anything about the name. `trade_when` is NOT the desk's `condition` operator: `condition` goes FLAT on a failed gate (exit and re-enter on every flicker, paying the round trip twice for a view that never changed) while `trade_when` HOLDS. That is purely a turnover difference, and turnover is what killed WS-006's order-flow momentum — Holm-cleared at t=+3.95, netting −0.656 bp/bar. **AND THE TRAP THAT ARRIVED WITH THEM:** the same source reports "in-sample Sharpe ≥ 1.25 + turnover band + fitness score". That is an IN-SAMPLE SUBMISSION FILTER for an operator that runs its own out-of-sample validation afterwards and pays per accepted alpha — their economics make a permissive in-sample screen rational because THEY bear the expensive stage. This desk bears it with its own capital against a deflated t of 5.236 plus OOS, walk-forward, leakage and independence. | **BUILT: `libs/alpha_factory/wq_operators.py`.** All four implemented with the refusals that make them safe: `group_rank`/`group_zscore` return None without a usable group map rather than silently falling back to universe-wide rank (a fallback would consume a whole new arm of the search space while re-deriving an existing transform, and every result would look like a new finding); a single group is refused because it IS `rank`; a group of one is refused because it is a constant; unlabelled members are excluded rather than pooled into an "other" bucket that is not a peer group. `ts_backfill` is BOUNDED (unbounded fill turns a delisted name into a flat line the harness reads as live signal) and FORWARD-ONLY despite the source's name — backward fill writes a future observation into a past bar. **THE BAR IS REFUSED STRUCTURALLY: there is no `passes_submission_bar()` function, and a test asserts no such path can appear**, because the function that does not exist cannot be called by an organ that read the same screenshot. `fitness()` is kept as a DIAGNOSTIC — its shape penalises churn, which is WS-006's lesson — with no pass/fail path. **NOT SILENTLY ADOPTED:** the three transforms stay OUT of `TRANSFORMS`, because adding them moves the universe 898,560 → 1,698,840 and the hurdle 5.236 → 5.356; `run_full_sweep.py` would refuse to start, which is the fence working. They need a NEW declared family first. **REMAINING:** the desk has no crypto grouping map at all (L1/L2/DeFi/meme category, liquidity tier, listing cohort or correlation cluster) — a grouping it can actually build is worth more than an operator it cannot apply, and that is now the blocking input for two of the four. All 7 miners carry a daily WorldQuant/platform-corpus mandate with the operator-extraction priority and the bar refusal. DEADLINE 2026-08-21. | brain | 08-07 | open |
| 99 | **THE VIDEO-LOCKED LOG — THE ONLY EVIDENCE GATE FOR A PAID TRANSCRIPT UNLOCK — HAS ZERO ROWS AFTER WEEKS OF DAILY DIGS ACROSS SEVEN REGIONS** | `docs/research/video_locked_log.md` exists precisely so a paid proxy/transcript purchase (GAP #26) is justified by evidence rather than by frustration, and so the desk buys for the PLATFORM the log names rather than for YouTube by default. Its own header states: "Empty log = no purchase justified (free-first protocol)." **Measured 2026-08-07: the table is empty — header only, zero rows.** The miner prompts have carried the VIDEO-LOCKED LOGGING mandate since 2026-07-20 and every region has run daily. Two readings, and they are not equally likely: either no digger has EVER hit a mechanism readable only inside video/audio — implausible on corpora that include Bilibili quant lectures, WorldQuant course video and Korean/Japanese YouTube trading channels — or the mandate is being skipped silently. **The cost of the second is that an empty log reads to a future session as "video was never a blocker",** which is the absence-reads-as-clean defect (WS-005) applied to the desk's own purchasing decision: it does not merely fail to justify a purchase, it actively argues against one. The principal has now named video transcripts as a standing daily ground, which makes the gap binding rather than theoretical. | **PARTIALLY ADDRESSED, AND THE REST IS NOT AGENT-REACHABLE.** All 7 miner prompts now carry "VIDEO IS A GROUND, NOT AN EXCUSE" naming the empty log as a measured defect and stating that a silent skip IS the defect while the row costs one line — fenced by `tests/ops/test_frontier_mandates.py` so an eighth region cannot ship without it. **WHAT I CANNOT DO:** verify whether the log is empty because video was never hit or because the mandate was skipped — that needs the miners' session logs on the collecting box, and this clone is network-denied (row 91). Nor can I fetch a transcript: the prompts record that transcript fetch is IP-blocked from the VPS, and this analysis clone reaches no external host at all. **DELIBERATELY NOT WORKED AROUND** — a paid unlock is a principal spend and the free-first protocol is the desk's own standing rule; the correct next step is that the next miner run either produces rows or explicitly records that it hit no video-locked mechanism, which converts silence into a measurement either way. DEADLINE 2026-08-14. | brain | 08-07 | open |
| 100 | **ROOT CAUSE OF THE EMPTY VIDEO LOG (row 99): EVERY DIGGER PROMPT CARRIED A REFUTED PREMISE AT LINE 11 AND ITS OWN CORRECTION AT LINE 77 — TWELVE DAYS APART, IN THAT ORDER** | Row 99 asked why the purchase-evidence log had zero rows and offered two readings: never hit, or mandate skipped. **Both were wrong.** `scripts/fetch_video_transcript.py` (committed 2026-07-26) REFUTES the 07-18 "transcript fetch is IP-BLOCKED from this VPS" finding — only the direct `youtube.com/api/timedtext` route is blocked; public Piped instances serve the same caption tracks, and the tool's own docstring records 6 subtitle tracks and 2,165 characters of real transcript on first try. **But all 7 frontier prompts, plus `prospector_dig_prompt.txt`, still opened with the refuted claim at line 11** — "when you hit a mechanism you cannot read (transcript fetch is IP-blocked from this VPS), do NOT silently skip it, append a line to the log" — with the correction ("VIDEO IS NOW READABLE", naming the working tool) sixty-six lines LATER at line 77. `prospector_coverage.md` had the identical inversion: stale bullet at line 27, refutation at line 118. A digger reading top-to-bottom acts on the first instruction, which told it video was a known dead end. So the corpus was neither FETCHED nor LOGGED — the log stayed empty not because video was never hit but because the prompt framed it as unreachable, and an unreachable ground gets no session note either. **THE GENERAL DEFECT, which is worth more than this instance: a negative result about ONE ROUTE was recorded as a fact about the CAPABILITY, and the correction was appended below the error rather than replacing it.** Append-only documents accumulate contradictions, and the reader resolves them by order, not by date. | **FIXED at the root.** The stale premise is deleted from all 7 frontier prompts and from the prospector prompt; the first mention of video in every one now says it is FIRST-CLASS dig material and names `scripts/fetch_video_transcript.py <url\|id>` (and `--bilibili <BVid>`). The log survives for its real purpose — a route genuinely TRIED AND FAILED — with the added rule that a negative result is about the ROUTE, never the whole capability. `prospector_coverage.md`'s bullet is STRUCK rather than deleted, because that file records what the desk believed and when, and the strike is the evidence. **AND THE ZERO IS NOW RECORDED:** miners must write "video: N fetched, 0 locked" in their session note, because an empty log is ambiguous between "never hit" and "never tried" and only an explicit zero separates them — without it the purchase-evidence gate silently argues against a purchase whose need was never tested. Three tests fence it: no `ops/*.txt` may contain the refuted string, every miner must name the working tool, and every miner must carry the record-the-zero rule. **NOT VERIFIABLE FROM HERE:** whether any transcript actually fetches today — the tool needs network and this clone is denied (row 91). The next miner run on the box settles it either way. DEADLINE 2026-08-14. | brain | 08-07 | closed |
| 101 | **THE LIVE LADDER HAD ZERO CONSUMERS — THE SAME DEFECT AS THE GENERATOR THAT COULD NOT RUN A CANDIDATE** | Built `libs/research/live_ladder.py` on 2026-08-07 to the principal's directive (go live fast, small capital, keep/increase what works, retire what does not, allocate dynamically). Then measured: **nothing anywhere called it.** `grep -rl live_ladder scripts/ ops/ libs/ .claude/` returned nothing. That is precisely the defect this desk found in `combination_engine` two days earlier — 898,560 structured candidates and no executor — repeated in the module written to fix the pipeline's other end. A ladder nobody calls is a document, and the directive it implements ("discovery → live should not take so long") is exactly the one a document cannot satisfy. | **WIRED: `scripts/run_live_ladder.py`.** It reads Stage-A survivors from the sweep report and forward records from `data/live_records.json`, and does the thing the directive asks: **a survivor with no forward record is owed a SHADOW START — today, at zero capital.** Shadow is the rung that actually shortens the pipeline, because the slow part was never paperwork, it was waiting for a backtest to become convincing, which waiting does not produce; the forward clock is the one input that cannot be bought later. Then MIN_LIVE → SCALE (quarter-Kelly on the posterior) → RETIRE. **AND IT COMPUTES A FLOOR NOBODY HAD NAMED: below ~86 quote units per clip the small-size cost drag exceeds 25% of a 1bp edge, so a live record there mostly measures fees.** Going live tiny feels like progress while producing a measurement whose natural reading is "retire it" — generated by costs, not by the strategy — so SHADOW is the honest rung under that size and the report flags any verdict whose clip sits below it. With neither artifact present it reports BLOCKED and says the state is UNMEASURED rather than an empty ladder. **PLACES NOTHING** — fenced by a test against order-path tokens. Arming live trading is the principal's act and the Tier-3 rail is untouched; Gate-0 is 0/17 and the sweep has not run (row 91), so the ladder is wired and idle by design rather than by omission. DEADLINE 2026-08-21. | brain | 08-07 | open |
| 102 | **THE BRAIN CORPUS COST THE DESK TWO MAJOR CAPABILITY GAPS IN TWO DAYS, DISCOVERED BY FORWARDED SCREENSHOT RATHER THAN BY ANY ORGAN** | Principal directive 2026-08-07: make the WorldQuant BRAIN mining mandate EXPLICIT rather than assuming the generic regional miners cover it. The evidence for that is the desk's own two-day record. **2026-08-06:** `combination_engine` combined RAW features and had no unary transforms at all — the entire transform axis absent. **2026-08-07:** one screenshot the principal forwarded named `group_rank`, `group_zscore`, `ts_backfill` and `trade_when`, none of which existed; the group operators mattered most because the desk's `rank`/`zscore` are UNIVERSE-WIDE, so all 179,712 cross-sectional cells in the declared 898,560-candidate sweep asked "is this coin extreme against ALL coins?" and **not one asked "against its PEERS?"**. Both findings arrived from OUTSIDE the mining layer. Seven regional miners had this corpus in scope and neither surfaced it, because a ground that is one bullet in a seven-region brief gets touched, not worked. A generic miner keeps producing findings of that size one screenshot at a time; the failure mode is not laziness, it is that the corpus is deep enough to need an organ rather than a line. | **BUILT: `ops/brain_hunter_prompt.txt` + `ops/run_brain_hunter.sh`, wired INSIDE `run_frontier_rotation.sh`** so it inherits the existing daily timer and the resumable >1500b real-log rule rather than needing its own schedule. It runs LAST — the regional grounds carry standing coverage debt, so a mid-dig credit death should cost the newest organ rather than a region. **RECURSIVE BY MANDATE, not label-bound:** WorldQuant → author → their other repos → related papers → cited papers and who-cites-them → related GitHub projects → **alternative implementations** → discussions → operators → datasets → mechanisms. The alternative-implementation node is called out as usually the highest-yield: someone who reimplemented an operator set had to UNDERSTAND it, and their README states what official docs assume (how decay interacts with turnover, what neutralization actually subtracts, where delay is applied). **MECHANISM EXTRACTION, NOT FORMULA COPYING:** every operator must return what it computes, its CRYPTO ANALOGUE (added to `evidence_tier.translate_to_crypto`), and — if it has none — what data the desk lacks, which routes to the information-frontier axis rather than being discarded. The platform is primarily an EQUITIES venue, so a factor rarely transfers while its transformation, neutralization idea, regime conditioning or methodology often does. **IT IS POINTED AT THE BLOCKING INPUT:** `group_rank`/`group_zscore` both REFUSE without a group map and the desk has NONE, so two of four newly-adopted operators are inert — a crypto grouping taxonomy (CoinGecko categories, DeFiLlama chains, liquidity tier, listing cohort, correlation cluster) is worth more right now than another operator. **THE BAR IS REFUSED AGAIN HERE**, on the organ closest to the source and therefore likeliest to import it. **AND THE BOUNDARY IS EXPLICIT (§13):** "every WorldQuant thing" means everything PUBLICLY and LEGALLY accessible — never private BRAIN data, other users' private alphas, proprietary datasets, restricted platform internals, or account-gated content behind a login the desk does not own; a credentialed account's contents are not public merely because an account exists. Naming what sits behind a wall is legitimate; going behind it is not, and a source obtained improperly poisons every result derived from it. 11 tests fence the wiring, the recursion, the translation, the bar refusal and the boundary. **REMAINING:** it cannot run from this clone (network-denied, row 91) and needs `llm_panel.json` funded; its first real output is a principal-side run. DEADLINE 2026-08-14. | brain | 08-07 | open |
| 103 | **`build_bars.py` POOLED EVERY INSTRUMENT INTO ONE OHLCV SERIES — SO 86% OF THE FIRST REAL FULL-SWEEP RUN WAS UNMEASURABLE, AND THE CROSS-SECTIONAL HALF OF THE EXPRESSION LANGUAGE COULD NEVER RUN AT ALL** | First live execution of the full sweep on the VPS, 2026-08-07: `898,560 evaluated / 128,132 measurable (85.7% UNMEASURED), 1 symbol(s), 919 common bars`. The verdict string did its job — "NOT ONE CELL cleared the deflated screen, so the kill criteria were never exercised … which is not 'no edge'" — but the CAUSE was upstream. `build_bars.build()` accumulated every trade from every file into one `px` list and resampled it into a SINGLE series: an open from one instrument and a close from another landed in the same bar, which is a price series of nothing. **Symbol was never missing — the recorders encode it in the path** (`data/moat/<venue>/<symbol>/<file>`, exactly the nesting `record_desk_metrics.py:111` already counts breadth from) and the builder discarded it. Consequences compound: every consumer saw ONE symbol, so `rank`, `zscore` and the newly-added `group_rank`/`group_zscore` had no peers to rank against and CORRECTLY refused — which is why 86% came back unmeasurable rather than wrong. **AND A SECOND DEFECT UNDERNEATH IT:** `FILE_BUDGET` took `files[-400:]` GLOBALLY, so the busiest stream ate the entire budget; the live run read 400 of 32,440 files and surfaced ONE venue (`{'spot': 537877}`, `OI: False`, 9 days of span). That is not a sampling choice anyone made — it is whichever recorder wrote most recently. | **FIXED.** `group_by_symbol()` buckets tape files by `f.parent.name`; `main()` builds and writes ONE artifact per symbol (`<SYMBOL>_15min.parquet`), which every consumer already picks up since they glob `data/bars/*.parquet` and derive the symbol from the filename. The budget is now PER SYMBOL (`FILE_BUDGET // n_symbols`), so breadth is guaranteed rather than accidental — breadth being the whole point, since no cross-sectional operator can run on fewer than two symbols. Venue is deliberately NOT part of the grouping key: the same symbol on spot and perp is one instrument for a bar series and merging deepens it; what must never merge is two different SYMBOLS. `build()`'s signature is unchanged so all nine existing tests still pass, and three new ones fence the defect — that two symbols never share a series, that the budget is per-symbol, and that the artifact name carries the symbol. **REMAINING:** the run also showed `OI: False` (no open-interest rows in the sampled files) and `moat_screen` was OOM-KILLED on the 4GB box — both are separate items. The next `build_bars` run on the box is what proves the fix; expect the sweep's measurable fraction to rise from 14% toward most of the universe. DEADLINE 2026-08-14. | brain | 08-07 | closed |
| 104 | **THREE LAWS SHIPPED TODAY (L1.53-L1.55) AND ONLY PART OF EACH HAS A FENCE — THE DESK JUST WROTE ITSELF A SET OF CLAIMS IT CANNOT YET CASH** | L1.49 says a gate that never ran is a claim the desk cannot cash, and by that standard most of 2026-08-08's constitutional work is a claim. What IS enforced: the three stranding states (`dormancy.stranding()` + `call_sites()`, wired into `run_intelligence_cycle`, feeding `run_max_push._from_stranding`), the residual mandate on four external seats (10 tests, including one that FAILS if anyone hands it to the blind researcher), and the funnel/near-survivor/evidence-tier/convergence consumer. What is NOT, item by item, each asked for explicitly by the principal in the same session: **(a) the recursive residual-gap protocol** — adaptive multi-pass with a marginal-value stop, Pass-A expansion vs Pass-B reconsideration, and the ten frontier search modes; the mandate tells a seat WHAT to hunt and never tells it to keep attacking its own answer until returns collapse. **(b) the Claude<->GPT difference engine** — semantic clustering of two corpora, overlap removal, ranked difference set; only the CLASSIFICATION LABELS exist, inside a prompt, and no organ computes the difference. **(c) a generic `Gap` input contract for max-push** — the principal warned in the same message that the queue must not become a pile of special cases, and `_from_stranding` was added as a special case. **(d) a cadence-vs-ROI audit** — the crontab was READ, not one cadence was measured against its yield. **(e) orphan detection beyond modules** — data with no downstream feature, features never used in a hypothesis, recommendations never implemented, survivors never portfolio-tested. **(f) assumption registry, contradiction engine, unknowns database, structured failure-mining fields, near-survivor bands.** **(g) miner / model / prompt ROI attribution.** **(h) the alpha state machine** that would make `DISCOVERED -> LIVE` mechanically impossible — governance asserts it and no object enforces it, which is the single largest gap between this desk's written governance and its running governance. | **THE ROW EXISTS SO THE LIST STOPS BEING CHAT PROSE.** `finding_registry` already says it: a finding that never reaches this register is invisible to the daily cycle, and the cycle only acts on what it can see. Ranked here, each item gets the 7-day staleness escalation and enters the max-push queue through `_from_register`. **ORDER IS BY EXPECTED VALUE, NOT BY EFFORT:** (a) first — it is a PROMPT change against seats that already exist, so it needs no new organ and cannot become inventory, which is the exact property (b), (e), (f) and (g) all lack. (h) is the largest but is a build, and this desk's most-repeated defect is writing organs faster than they get consumers. (c) is a refactor of working code and should ride the next real addition to the queue rather than be done for its own sake. **AND THE HONEST CAVEAT ON (a):** its value is unmeasurable until OpenRouter is funded — every external seat is 402 today, so a better protocol on an unrunnable seat is a better document. That is a principal-side input, not a hurdle to route around. DEADLINE 2026-08-15. | brain | 08-08 | open |
| 105 | **THE DESK HAD NO ECONOMIC SCOREBOARD AT ALL — SEVEN MEASUREMENT SURFACES ABOUT RESEARCH, ZERO ABOUT WEALTH** | Measured 2026-08-08 while building to the LEWIS FUSION v3 / BEAT-LEWIS mandate. Every reporting surface this desk owned answered a question about RESEARCH: kills, survivors, funnel stages, gaps, coverage, conversion, capability status. Not one answered whether the enterprise was generating and RETAINING real net wealth, which is the only question that decides whether any of the rest was worth doing. **The failure is not that the numbers were bad — it is that they did not exist and nothing noticed their absence**, which is the WS-005 shape aimed at the objective itself. **BUILT AND WIRED IN ONE CHANGE (L1.56):** `libs/portfolio/wealth_retention.py` (round-trip / gain-retention / marginal E[log W] against reserve option value), `libs/portfolio/return_engines.py` (§53 fourteen-engine attribution + §58 effective independent engine count), `libs/validation/state_conditional.py` (F3's second branch, the fix for the measured ~50% conditional ceiling, with POST_HOC_RESCUE refused), `libs/research/payoff_selection.py` (rank on E[log W], never on accuracy), `libs/research/external_benchmark.py` (PERFORMANCE_LEAD, with TARGET never silently promoted), `libs/research/conversion_velocity.py` (EVIDENCE_BOUND vs PROCESS_BOUND latency), `libs/research/decision_ledger.py` (the nine ways to decline). Consumer `scripts/run_wealth_report.py` in `ops/run_research_cycle.sh`; its board question feeds `run_max_push.py` and today ranks **#2 in the work queue**. Laws L1.57-L1.59 written and fenced. **HONEST RESIDUAL, and it is the whole finding:** six of seven sections report UNMEASURED because no NAV path, engine P&L or decision ledger exists yet — the desk has never had a real fill. The scoreboard's current answer is *NO REALISED P&L EXISTS TO RETAIN*, which is correct and is the highest-value gap on this desk. **NEXT:** emit `data/nav_path.json`, `data/engine_pnl.json` and `data/decision_ledger.jsonl` from the live/canary path. | OPEN |
| 106 | **ELEVEN RETURN ENGINES NOW EXIST AND NINE OF THEM HAVE NO INPUT — THE BOOKS ARE BUILT AND THE ARTIFACTS THAT FEED THEM ARE NOT** | Built 2026-08-09 against the survivor-yield / E[log W] expansion mandate. `scripts/run_opportunity_books.py` runs eleven books every cycle and nine report UNMEASURED, each naming the exact artifact it needs: `data/decline_events.json`, `positions.json`, `strategy_pool.json`, `signal_book_states.json`, `crowding_states.json`, `participant_cohorts.json`, `alpha_reserve_bank.json`, `strategy_paths.json`, `market_breadth.json`. **THAT IS THE HONEST STATE AND IT IS ALSO THE GAP.** L1.56 says a capability wired only when its input appears is a capability wired late, so the books were built first on purpose — but a book that has never once produced a number has never been exercised against real data, and the first run against a live artifact is where the schema mismatches surface. **The two that CAN be filled from a clone already are:** `mechanism_ontology` (1549 coherent candidates from 9282 combinations, 83% pruned) and `agent_authority` (six components declared, zero over-privileged). **NEXT:** the VPS holds live recorder output; `decline_events.json` and `crowding_states.json` are derivable there today and are the two highest-value fills, because the drawdown book and the crowding book both answer questions the desk currently answers by waiting for the P&L. | OPEN |
| 107 | **REPLACEMENT LATENCY IS THE NUMBER THIS FACTORY IS ACTUALLY OPTIMISING AND IT HAS NEVER BEEN MEASURED ONCE** | Named by the principal 2026-08-09 and correct: a factory can have enormous discovery throughput and still lose the compounding, because a live edge dies and its replacement is months behind. `libs/portfolio/alpha_reserve_bank.py` now answers *if 25/50/75% of live alpha died today, how much is replaceable WITHOUT lowering the bar* — bench deflated by effective independence (three clones are one replacement), same-mechanism cover refused against a mechanism-level death, and `switch_verdict` carrying no drawdown argument at all so the fire-it-because-it-is-down reflex cannot be expressed. **UNMEASURED HERE, AND NOT BY OVERSIGHT:** this clone has no live book, so the ratio has no denominator and the module correctly refuses to report zero. **The gap is that the desk cannot yet name a single eligible bench candidate** — `libs/research/near_survivor.py` banks near-misses and nothing has ever been promoted from it to SHADOW_CHALLENGER, which means the measured reserve ratio on the day a live book exists would be 0.00. | OPEN |
| 108 | **PER-STRATEGY MONTE CARLO UNDERSTATED PORTFOLIO DRAWDOWN BY 2.93x ON A CLONE BOOK — AND PER-STRATEGY MONTE CARLO IS WHAT THIS DESK RUNS** | Measured 2026-08-09 by `libs/portfolio/portfolio_monte_carlo.dependence_blindness` on a constructed five-clone book. `libs/discovery/monte_carlo_survival.py` reshuffles ONE strategy and `strategy_pool.sizing_drawdown` sizes each member off its own reshuffled tail; both are correct for the question they ask and neither asks what happens on the day every strategy loses at once. Independently shuffling each strategy scatters their bad days across the calendar and manufactures diversification that does not exist. The new module draws ONE block of time per draw and applies it to EVERY strategy, so co-activation, common regime, tail dependence and margin concurrency are never broken rather than modelled. **THE 2.93x IS A CONSTRUCTED-FIXTURE NUMBER, NOT A MEASUREMENT OF THIS DESK.** It proves the discriminator works in both directions (an independent book reads ~1.0). What it costs on the real book is UNMEASURED until `data/strategy_paths.json` exists, and the honest expectation for a crypto book — where basis, momentum, alt-beta and liquidation risk collapse into one factor under stress — is that it is well above 1.0. | OPEN |
| 109 | **THE DESK ASKED "HOW DO I MAKE BTC STRATEGY #384 BETTER" AND NEVER ONCE ASKED "WHERE ELSE CAN THIS MECHANISM EXPRESS ITSELF"** | Named by the principal 2026-08-09 via Parker's market count, and the framing is sharper than diversification: enormous breadth buys one simple robust rule far more INDEPENDENT CHANCES to encounter the state it needs. A parameter search adds exactly zero independent observations — it re-examines the same history and pays full multiplicity for it — while a new market adds genuinely new draws. `libs/research/market_breadth.py` prices the comparison, deflating occurrence counts by cross-expression state correlation so forty markets that fire in the same hour cannot be spent as forty times the evidence, and filtering feasibility before ranking rather than scoring it. **THE GAP IS THAT DEPTH IS STILL THE DEFAULT AND WILL STAY SO** until `data/market_breadth.json` names candidate expressions, because another parameter needs no new data, no venue access and no new operational surface — which is exactly when it is least likely to be the right call. | OPEN |
| 110 | **A KNOWN BUG WAS FIXED IN ONE FILE AND NOBODY SWEPT FOR SIBLINGS — SO ITS TWIN SAT OPEN FOR WEEKS IN THE PLACE WHERE IT WAS LOAD-BEARING** | Found by the Codex seat 2026-08-09 in `scripts/research_alpha_optimizer.py`, whose source ALREADY carried the note that keyword-counting ledger prose "counted 63 when the true count was 0, flipping the gate open". That fix was correct and it stopped at the file it was found in. `scripts/research_allocator.py` had the identical `classify()` returning `"survivor"` for any row whose prose contained **"wired"** — on a desk whose ledger is mostly about wiring modules, **82 phantom survivors against a true confirmed count of 0**. **AND THE ALLOCATOR IS THE WORSE INSTANCE, because there the phantom count was LOAD-BEARING:** `prior_dominated = total_surv < 5 or total_n < 30` read the same tally, so 82 evaluated it False and SUPPRESSED the warning that says *do not present this as data-driven* — the leak switched off the sentence that existed to catch it, then printed allocations under the banner "recomputed from evidence, not decreed". **FIXED (6386cd7):** the `survivor` reward bucket is DELETED rather than down-weighted, so restoring the 1.00 payout requires reintroducing the concept instead of editing a number; confirmed survivors come from the Stage-B shadow tracker; an unreadable tracker is UNMEASURED and fails the gate CLOSED (L1.28a); 18 tests pin it, including that the gate still OPENS on real evidence. **SWEPT:** `libs/autodiscovery/research_roi.py` and `libs/alpha_factory/research_attribution.py` read a typed `r.survived` off the candidate ledger rather than prose — clean. Only the two keyword classifiers ever had it. **THE RESIDUAL, AND IT IS THE POINT OF THIS ROW:** the desk has no mechanism that turns "defect found here" into "search for the same shape elsewhere". Every fix is filed against one file. This one survived because the sweep was nobody's job. | OPEN |
**RENUMBERED at merge time: this account's 07-27 rows 87-88 collided with the other account's independently-numbered 87-93 below (both continued from the shared row 86). Renumbered to 94-95 here -- out of number order but in date order -- because master's 87-93 cross-reference each other by number extensively and renumbering THEM by hand risked breaking those references; nothing downstream referenced this account's original 87/88, so it was the safe side to move.**

| 81 | **Capacity parity was permission, not parity -- the flat $100k floor existed in FIVE places and only one was fixed; and event-shaped edges had no promotion path at all** | Found 2026-07-26 answering 'are low-capacity edges fully equally prioritised?'. Honest answer was NO. The 07-26 survival-gate fix (capacity as a ratio) removed a categorical exclusion but left four other scorers monotone in raw size: `discovery.objective` (+1.9x rank to a $1m idea over a $50k one), `alpha_economics` (+3.2x EV), `discovery.factory` (still a flat `>= 1e5` in acceptance), `alpha_factory.capacity_intelligence` (scalability vs a $10m reference -> a niche edge scored 0.5/100). A candidate could pass the fixed gate and lose every ranking to a fund-shaped idea it beat on everything that pays. SECOND defect found in the same sweep: every promotion gate scores a continuous daily series, so a day-1 listing spike (~2 non-zero days in 30) reads as noise on every statistic -- `run_listing_watch` could collect forever and never promote. Acquired, not convertible: a §33 failure in statistics costume. THIRD, found by the new guard on its first run: `Idea.capacity_usd` defaulted to $1m, so every unestimated idea was assumed fund-scale. | FIXED 2026-07-26: `libs/research/capacity_policy.py` is now the single leaf definition (imports nothing from libs, so nothing can be 'too circular' to use it) -- capacity scores as SUFFICIENCY and goes FLAT above the headroom requirement (that flat region IS the parity), with a bounded, loosen-only-floored crowding discount keyed to an ABSOLUTE fund-scale dollar figure rather than to our own book. All five consumers rewired. Capacity is judged per SLEEVE, not against the whole book (`DEFAULT_SLEEVES`), since comparing every candidate to the full $50k re-created the exclusion at 8x. `check_capacity_hunt` raised from a 25% floor to >=50% PARITY, plus a separate `capacity-hunt-unfillable` defect (small is the advantage; too small to fill is not). NEW `check_capacity_single_source` enforces the invariant STRUCTURALLY -- every capacity consumer must import the shared policy and may not carry a fund-shaped literal on a capacity line -- so a re-tune cannot silence it and a fifth copy cannot reappear. Event path: `libs/validation/event_study.py` (Brown-Warner cross-sectional t vs a Holm bar, bootstrap CI that must ALSO exclude zero, overlap-discounted effective N per §31, hard refusal under 20 events, and a DEGENERATE refusal for zero-variance input -- `std == 0` never fires in floating point and the t-stat would otherwise hit 3e16 and clear every bar), `libs/research/listing_events.py` (hypothesis PRE-REGISTERED as module constants; a second window must raise `VARIANTS_TRIED`), `scripts/run_event_study.py` wired into the daily cycle beside the collector it consumes. Charter §39(5)-(6) + doctrine written. A PASS is EVIDENCE; Gate 0 and the ordinary promotion path still stand between it and any dollar. | CRO | 07-26 | fixed |
| 82 | **Capacity parity was still a TILT toward small, and the funnel check was a one-sided niche floor -- both violate the actual objective, which is the maximum NUMBER of simultaneous alphas** | Principal correction 2026-07-26, same day as row 49: 'deploy all edges low capacity and high... the goal is to maximise number of alphas fully for geometric growth and compounding, not be picky and pick or prioritise one over another'. Row 49 shipped (a) a bounded crowding DISCOUNT on fund-scale capacity and (b) a `>=50% niche` funnel bar. Both are the original bias pointed the other way: the discount declines sleeves for their SIZE (compounding foregone) and DOUBLE-COUNTS crowding, which `crowded_known` + DSR/PBO/persistence already price; the niche floor would have sat silent while the desk hunted nothing but tiny edges, leaving no successor inventory for the day the book outgrows them. THIRD defect found in the same pass: the whole 'capacity is a ratio to deployed equity' law was being evaluated against a HARDCODED $50k while the real book sat at $14,773 in the NAV chain -- a ratio measured against a literal is the flat-floor bug one step removed, and it meant the requirement would never rise as the desk grew. FOURTH: `validate(deployed_equity_usd=0.0)` collapsed the gate to the $2k absolute floor whenever a caller omitted the argument, so the protective ratio protected nothing. | FIXED 2026-07-26: crowding discount defaulted to NEUTRAL (`capacity_crowd_floor` 1.0, kept live and bounded so MEASURED decay-vs-capacity evidence could reintroduce it -- evidence may, preference may not); every fillable edge now scores identically. `check_capacity_hunt` made SYMMETRIC -- `capacity-hunt-fund-shaped` and the new `capacity-hunt-niche-only` fire when EITHER band drops below 25% of the fillable funnel. `live_book_usd`/`live_sleeves` read the hash-chained NAV attestation so the requirement self-scales as equity compounds; missing/stale(>7d)/corrupt falls back to the CONSTANT, never zero (an unreadable file must never be the loosest gate), and the live sleeve count is floored at the planned count so a one-sleeve day cannot hand one edge the whole book. `validate` default changed 0.0 -> None = read the live book. NEW `check_capacity_runway`: `outgrown_at` gives every edge the book size it dies at, `growth_runway` the multiple of today's equity it survives; fires on `capacity-already-outgrown` and on `capacity-no-runway` (nothing survives a doubling = no successor inventory). Net effect: both bands hunted and run concurrently, nothing declined for its size, and a sleeve retires ONLY when the arithmetic of the book passes its capacity. Charter §39(5)(a)/(c)/(c2)/(c3) rewritten, doctrine updated. | CRO | 07-26 | fixed |
| 83 | **Four capacity/label mechanisms were built, unit-tested green, and called by NOTHING -- and the orphan checker could not see it because reachability was one-hop** | Asked twice whether the work was done, checking (not asserting) found the same failure repeating: the crowding floor, the sizer governor, `allocation_usd` and `barrier_return` each had correct code and passing tests and no production caller. Unit tests are exactly what hides this -- they prove the mechanism and say nothing about whether it runs. Separately `triple_barrier_labels` (a correct Lopez de Prado port with a full test file) had sat unused since it was written, and the promised thin-tail cross-venue funding screen did not exist. `check_orphan_code` reported libs/features as idle while it was genuinely reached one hop deep, AND counted packages 'used' when their only importer was itself an orphan. | FIXED 2026-07-26: `check_capacity_knobs_are_wired` requires a PRODUCTION caller for every capacity knob (a test does not count) and caught `allocation_usd` on its first run; a test flips the knob list to a fake name so the guard is proven to fire. `check_orphan_code` is now a BFS over the import graph -- cleared 5 false positives and surfaced stage14/14_5/15 which were hiding behind them; imports needed for reachability moved to top level. `triple_barrier_labels` wired via `barrier_return` as the event study's second pre-registered exit (profit-take/stop/time), with VARIANTS_TRIED raised to 2 and BOTH verdicts printed -- running two and publishing the better one is forking paths with extra steps. NEW `libs/research/tail_funding.py` + `collect_tail_funding_divergence.py`: Binance-vs-Bybit funding spreads on the bottom half of the shared perp universe by OI, capacity = the MINIMUM leg, implausible spreads flagged rather than ranked first, wired into the daily cycle. Found and fixed a real off-by-one while testing: `int(n*q)` made the 'thin tail' include the whole universe on a 2-symbol panel. NOT verified against live venues -- both exchanges are unreachable from the build sandbox; the degradation path (exit 0, honest message) is verified and the same fapi host is already reached by run_listing_watch in production. | CRO | 07-26 | fixed |
| 84 | **The go-live path was orphaned code and the orphan checker was package-granular, so nothing saw it** | Same failure as row 83, one layer up and on the money path. `libs/execution/staging.py`, `binance_live.py` and `binance_spot_live.py` were imported by their own tests and NOTHING else. `check_orphan_code` asks 'is libs.execution reachable?', which is true forever via ea_bridge, so a dead stage machine inside a live package was invisible for 8 days. Meanwhile research_cycle's `live_connector_prebuild` detector was `(libs/execution/binance_live.py).exists()` -- the desk's progress signal for the live connector was a file-existence check, and it read DONE the whole time. Third instance of measuring the proxy instead of the thing the rule names (after the register's re-rank stamp and libs/ vs scripts/ reachability). | FIXED 2026-07-26: `scripts/run_live_guard.py` is the production caller and is on the daily cycle. `check_money_path_wired` is MODULE-granular over a transitive BFS from scripts/app/api (tests are explicitly not entry points, since being imported by your own test is the condition under audit) and blocks on a named list of 9 money-path modules; `check_orphan_modules` tracks the long tail against a budget of 66 that ratchets down only. The detector now checks wiring, not existence. Found on the way: `libs/risk/edge_gate.py` is genuinely dead -- superseded by `dynamic_leverage.py`, and `run_edge_gated_leverage.py` now delegates to `run_leverage_opt` without importing it. Left in place rather than deleted: retiring risk code is a principal call, not mine. | CRO | 07-26 | fixed |
| 85 | **`libs/risk/edge_gate.py` is dead risk code with a live-looking test file** | Surfaced by the module-granular orphan check (row 84). `gated_leverage()` -- size to forward-validated edge -- has `tests/risk/test_edge_gate.py` and no production caller: `dynamic_leverage.py` only names it in a docstring, and `run_edge_gated_leverage.py` was superseded and now delegates to `run_leverage_opt`. A passing test file on unreachable risk logic reads as coverage of a governor that cannot govern anything. | DECIDE: either `dynamic_leverage` fully subsumes the edge-gated ramp (then retire edge_gate.py to the graveyard with the mechanism of death recorded, and drop its test), or it does not (then wire it and say where). Not self-served -- confirming that one leverage model subsumes another is a principal-level call about how the desk sizes. | brain+principal | 07-26 | open | parked, re-test 2026-08-15 (post-Gate-0 activation item 8b wires ThresholdBook; edge_gate ruling 