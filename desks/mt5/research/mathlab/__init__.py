"""THE AI MATHEMATICS RESEARCH CIVILIZATION: fourteen parallel traditions on one residual.

WHAT THIS IS NOT. It is not "an LLM produced a hundred strategies". Nothing here selects from a
menu of RSI/MACD features. The pipeline the principal specified is:

    market residual / unexplained phenomenon
      -> AI mathematician populations
      -> invent mathematical objects (equations, state variables, invariants, representations,
         latent processes, interaction laws)
      -> symbolic / program search
      -> formal simplification
      -> economic interpretation
      -> falsification
      -> the desk's OWN gauntlet (never a private one)
      -> forward evidence
      -> live dE[log W]
      -> credit back to the MATHEMATICAL METHOD
      -> compute reallocation

Fourteen traditions run as parallel scientists over the same panel, each with its own real
numerics and its own named UNMEASURED list: symbolic regression / program synthesis, dynamical
systems, stochastic processes, spectral mathematics, information theory, geometry / manifolds,
optimal transport, graph theory, topology, causal mathematics, combinatorics, control theory,
game theory / ecology, and meta-mathematics (which invents transforms, objectives and search
operators from the ROI table rather than from anyone's taste).

THEY SEARCH FOR FIVE THINGS, and the object kind says which: new data RELATIONSHIPS, new
REPRESENTATIONS, new MECHANISMS, new mathematical LAWS, and new SEARCH METHODS.

EVERY DISCOVERY CARRIES ITS OWN SEARCH BURDEN. `burden.py` scores

    candidate value = predictive evidence - complexity penalty - multiple-testing burden

with complexity as MDL over the expression tree and the burden as the expected maximum of the
tradition's own EFFECTIVE TRIALS, which are charged to the desk's hash-chained trial ledger. An
object that survives goes to the same ten gates every other hypothesis meets. A beautiful formula
with a perfect in-sample fit and no held-out evidence is exactly what this module is built to
refuse, and `tests/test_mathlab_burden.py` is that refusal written down.

Modules: `objects` (the object and the panel), `grammar` (the expression language and its
canonicaliser), `scientists` (the fourteen), `burden` (the search-burden economics).
"""
from __future__ import annotations

__all__ = ["burden", "grammar", "objects", "scientists"]
