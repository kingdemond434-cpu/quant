data sanity scanner + screen-design auditor; FP-corrected; found a 175% NAV reconciliation gap

TWO NEW VALIDATORS, both credit-free, both then VALIDATED AGAINST THEMSELVES.

scripts/data_sanity.py -- systematises the two artifacts that today were caught only by EYEBALL
  (COOKIEUSDT 59.17bps flat across a 25x size range; mSOL/SOL 539bps sd on a ratio that tracks
  within ~100bps). Neither errored. Both were CONFIDENT WRONG NUMBERS that passed every
  presence-based validator and would have propagated into sizing. 7 implausibility checks encoding
  physical facts (impact must rise with size; a series that never moves is a constant; bounded
  quantities are not monotone; etc).

scripts/screen_auditor.py -- audits the TEST, not the code. Today's three worst failures were
  DESIGN failures a code auditor would have passed: structural_spreads omitted NET-OF-COST (all 4
  "candidates" die on fees), horizon_search needed the ADJACENCY rail, hl_longterm_skill was
  UNDERPOWERED -- the principal caught two of the three. Rule-based, checks for ABSENCE of the
  rails this desk EARNED, per screen kind.

CRITICAL SELF-CORRECTION -- BOTH VALIDATORS SHIPPED WITH HIGH FALSE-POSITIVE RATES, FIXED BEFORE
COMMIT. A validator with a high FP rate is WORSE than none: it trains the reader to ignore it.
  data_sanity flagged window_s=3600 and partition_s=300 (CONFIG constants), start_futures_equity
    (fixed baseline) and ts (a clock) as "implausible". Now excludes config/metadata fields. 42->37.
  screen_auditor did not credit rails INHERITED via stage_a_screen (de-contamination, lookahead,
    power are inside the harness), so correctly-gated screens read as MISSING. Now credited. 33->28.
  Residual known FP: the stale-run check fires on ANY historical run of identical values even in a
    healthy series -- venue_divergence_shadow was flagged but is fine (1044 distinct / 1290 rows).
    Should be rate-based, not presence-based. Logged, not yet fixed.

THE REAL FINDING, surfaced while verifying a false positive:
  venue_divergence_shadow shows venue_nav $5,262 vs mark_nav $14,508 -- a 175.7% divergence, and
  GROWING: 36.4% on 07-23 -> 175.7% on 07-27. venue_nav fell 10,806 -> 5,262 while mark_nav held
  ~14,500. web/portfolio.json reports equity 14,853, i.e. it tracks the MARK, not the VENUE.
  So the desk believes it holds ~$14.5k while the exchange reports ~$5.2k, and the gap has widened
  5x in four days. Either the two measure different scopes (futures wallet vs spot+futures) or
  reconciliation is broken. On testnet this is a diagnostic. AT GATE-0 WITH REAL MONEY IT IS
  CATASTROPHIC -- every sizing, leverage and risk decision would run off a NAV that does not exist.
  ADDED TO THE GATE-0 QUEUE AS A TIER-1 BLOCKER. Must be explained before any live capital.
