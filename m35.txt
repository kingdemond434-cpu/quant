UNTESTED: hypothesis_generator -- external LLMs proposing MECHANISMS, with the graveyard in hand

*** NEVER EXECUTED (OpenRouter 402). Brain must run once and check output. Assume bugs: three
validators shipped today all had high false-positive rates on first run. ***

THE GAP THE PRINCIPAL EXPOSED: the desk had an external LLM for SOURCE discovery
(breadth_expander, GPT as lead seat) but NO role for HYPOTHESIS generation. The principal supplied
a ~50-hypothesis slate from ChatGPT which was genuinely well-framed -- and three of the first four
were ALREADY REFUTED here, while the three novel escalations tested 0/3 today. That is not a model
failure, it is a CONTEXT failure: a generator that cannot see the graveyard keeps re-proposing the
dead.

DESIGN INVERTS THE BREADTH EXPANDER, deliberately:
  breadth_expander      COLD, zero desk context  -> avoids ANCHORING (find sources we cannot imagine)
  hypothesis_generator  FED the graveyard        -> avoids IGNORANCE (stop re-proposing the refuted)
Same objective (maximise new information), opposite implementation, because the failure modes are
opposite. Anchoring kills source search; ignorance kills hypothesis search.

FALSIFIABILITY FORCED BY CONTRACT: every output must give MECHANISM | DATA SOURCE | TEST | KILL
CONDITION. "Interesting area" is rejected by construction. The prompt also encodes this desk's
hard-won prior: prefer SPREADS and FORCED FLOWS over forecasts, because every forecast-style
hypothesis tested here has died and every surviving candidate has been a spread with a hard
constraint.

5 rotating MECHANISM lenses (not source lenses): transition, participant-constraint, structural
segmentation, measurement advantage, second-order. One per day, so consecutive runs cannot
converge.

ON THE PRINCIPAL'S POINT THAT GPT IS THE STRONGER IDEA GENERATOR -- accepted as the hypothesis,
not as the conclusion, and wired to be MEASURED:
  * GPT leads this role (its claimed strength: generative framing).
  * Two other labs run alongside; per-seat yield decides who keeps the slot.
  * Counter-evidence already exists: in today's breadth sweep gpt-5.6-terra-pro produced 0
    parseable rows on 5 of 6 lenses while nemotron and grok produced 18 each. Different task
    (chat-style generation vs structured extraction), so both observations can be true -- which is
    exactly why it must be measured on THIS desk rather than assumed from chat impressions.
  * NO SEAT BY REPUTATION, GPT included. A seat proposing only hypotheses that die loses its slot.

HONEST SCOPE: this raises idea SUPPLY. Today's evidence says supply is not the binding constraint
-- ~28 hypotheses tested, ONE worked, and it came from auditing the LIVE SYSTEM (funding
persistence) rather than from creative generation. Adding a generator without raising CONVERSION
lengthens a queue that already exceeds throughput. Value it accordingly.
