"""The principal's discretionary playbook, H1-H11, as detectors over one common Setup shape.

`rules` holds the OHLCV family; `tape` reads the moat's aggTrade partitions for the two
hypotheses that need signed flow. One adapter in `scripts/run_discretionary_live.py` turns any of
them into a sized, risk-checked, journalled intent -- no rule gets its own order path.
"""
