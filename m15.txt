breadth expander: external LLM as a COLLEAGUE to the miners (generative, not judicial)

Principal: a second LLM as head of research exploration -- doesn't control the miners, expands
them, recommending sources they didn't see, like a friend to all of them.

ANGLE-14 FIRST: ~70% already existed. The 13-seat / 11-lab panel with a budget envelope guard is
built, and a data/breadth FINDING CATEGORY exists. What did NOT exist is the ROLE: every current
mission (audit/premortem/verify) is JUDICIAL -- it reads finished work and critiques it. A
GENERATIVE "here is territory you have not looked at" colleague was missing. Also found: the panel
is NOT on cron and last ran 07-20 (7 days stale).

scripts/breadth_expander.py -- design decisions the principal left to me:
  COLD, NOT MEMORY. Memory anchors: a model recalling its own prior suggestions drifts into
    incremental variants of the same neighbourhood. Cold cognition + POST-HOC dedup against the
    class map gives fresh thinking AND no repeats -- the same principle as blind rediscovery,
    which is deliberately blind to current coverage maps.
  ROTATING L6 LENS (6 angles: modality gap / negative space / cross-domain / adversarial /
    non-English frontier / inversion). One per day, so "never exhausted" is mechanical -- a daily
    reshuffle of ONE prompt would converge; six orthogonal framings do not.
  2 SEATS/DAY, NOT 13. Lead = openai/gpt-5.6-terra-pro (principal's pick), plus one rotating
    non-OpenAI seat for lab diversity. Every lab contributes over a week at ~2 calls/day.

CONSTRAINTS ENFORCED:
  * Stage-A only, zero promotion authority (L4 boundary intact).
  * EVERY suggestion FEASIBILITY-PROBED. First live run VINDICATED this immediately: 6 of 11
    suggestions (55%) were dead links (HTTP-401/404, URLError). Unprobed LLM output would have
    polluted the class map with phantom sources.
  * Respects the panel's BUDGET ENVELOPE GUARD rather than bypassing it (abort, never silently
    degrade) -- caught and fixed during the build; my first version spent the same wallet with no
    guard, which would have been a safety regression.
  * s13 legitimacy gate unchanged: public + legitimately accessible only.
  * Yield MEASURED as method external_llm_expander -- 300 sources and zero survivors decays its
    budget share like anything else. No permanent seat on reputation.

FIRST LIVE RUN (lens MODALITY GAP): 11 suggestions -> 2 already known, 6 unreachable,
3 NEW+REACHABLE (SEC EDGAR Search API, Ethereum PM GitHub transcripts, Etherscan Source API) --
all in the filing/transcript/archive carriers the class map flagged as 0-coverage blind spots.

NO-REGRESSION VERIFICATION (principal instruction): every file touched this session re-parsed;
run_axis_shadows still healthy (kimchi 5/40 t=-0.58 bar=2.13, stablecoin 4/40, cny 0/40);
new scripts ruff-fixed (113 -> 100, all functional errors cleared, remainder cosmetic E501/E702).
HONEST NOTE: repo ruff baseline was ALREADY ~239 errors before this session, so CI lint was
already red -- my files did not break it, but the ~100 cosmetic remainder is logged as debt.

Also wired breadth_expander + signal_halflife into the daily cycle (daily, per principal).
