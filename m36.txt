ERV scorer: rank hypotheses BEFORE testing -- concept-level archaeology + moat weighting

PRINCIPAL'S INSIGHT, implemented: the desk does not have an idea shortage, it has a CONVERSION
problem. ~28 hypotheses tested today, ONE survived. Adding generators raises SUPPLY against a fixed
confirmation budget; what raises yield is testing the RIGHT ones first.

    ERV = archaeology x moat_advantage x mechanism_depth x cost_efficiency

THE BIGGEST FIX -- CONCEPT-LEVEL ARCHAEOLOGY, replacing token dedup:
hypothesis_generator dedups on TOKENS, which the principal correctly identified as too weak:
"Twitter sentiment predicts returns" and "social attention predicts momentum" share almost no
tokens and are the SAME dead idea. This maps text to CONCEPT FAMILIES (attention, developer,
trader_skill, premium_regional, funding_position, onchain_flow, microstructure, reflexivity) and
matches against this desk's MEASURED verdict per family. Rewording a dead idea no longer
resurrects it.

FAMILY VERDICTS (measured today, not assumed):
  attention        DEAD          level AND acceleration, 15 horizons, 5 languages
  trader_skill     DEAD          5 mechanisms, ~50k traders, gapped persistence rho -0.064
  developer        MOSTLY DEAD   commit velocity CS-IC ~0; retention n=10 UNDERPOWERED not refuted
  onchain_flow     MOSTLY DEAD   usage metrics dead; stablecoin supply weak-but-live
  premium_regional PARTLY ALIVE  kimchi/cny live; JP/BR/TR/pegs/LSD died on WIDTH
  funding_position ALIVE         funding persistence IC +0.432; ls_contrarian clock to Aug 7
  reflexivity      INCONCLUSIVE  M5 underpowered, OI capped at 500 bars
  microstructure   UNTESTED      4.4GB moat, needs book reconstruction -- THE OPEN FRONTIER

MOAT WEIGHTING (1.0 vs 0.35): public data (GitHub/TVL/Twitter/Trends) is a PROCESSING advantage
anyone can replicate -- and it died at ~27/28 today. data/moat is an INFORMATION advantage nobody
else holds. Priced accordingly.

MECHANISM PRIOR (1.0 / 0.6 / 0.35): hard-constraint and forced-flow mechanisms outrank spreads
outrank correlational forecasts. This is MEASURED, not taste: every survivor here has been a spread
with a hard constraint (kimchi=capital controls, carry=funding structure); every forecast died.

DEMONSTRATION on the principal's own slate -- 270x spread, and it independently reproduces the
principal's own priority ranking from measured history rather than judgement:
  1.000  Market maker inventory stress   (untested concept + moat + forced flow)
  1.000  Liquidity fragility score       (same)
  0.037  Developer retention momentum    (mostly-refuted concept, public data, correlational)
  0.037  Bridge flow predicts rotation   (same)
  0.006  Attention efficiency ratio      (CONCEPT ALREADY DEAD -- token dedup would have passed it)

RULE: ERV < 0.15 must not consume a confirmation slot regardless of how interesting it sounds.

NOT BUILT (principal's proposal, correctly scoped): the full GPT role-split into
Generator/Challenger, and a standalone Research Archaeologist agent. The archaeology FUNCTION now
exists here mechanically; the ADVERSARIAL half (attack the hypothesis, find the alternative
explanation) still needs the LLM code auditor pattern and remains blocked on funding.
