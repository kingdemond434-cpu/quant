# REPO_MAP.md — Repository Audit

Generated by dependency-trace from the live crypto entrypoints (executor flywheel + daily research
chain + dashboard/ops). Classification is reachability-based, not by intent.

## Headline
- **302** library modules under `libs/`. **75 LIVE/CONNECTED**, **227 ORPHANED (75%).**
- **58** scripts under `scripts/`.
- The live crypto platform uses a thin slice; a large institutional stack (built for the retired
  MT5 architecture) exists fully written but **unwired** — the integration target.

## Classification key
- **LIVE** — imported (transitively) by a running crypto entrypoint.
- **CONNECTED** — imported by a LIVE module but not itself an entrypoint.
- **ORPHANED** — present + (mostly) tested, but not reachable from any crypto entrypoint.
- **DEPRECATED** — MT5/old-stack scripts superseded by the crypto path.
- **DUPLICATE** — overlapping logic with a LIVE module.
- **MISSING** — required by the institutional target, not present anywhere.

---

## LIVE / CONNECTED (the running crypto platform, 75 modules)

| Package | Live modules | Role |
|---|---|---|
| `libs/execution` | binance_testnet (+10) | Binance testnet connector, order/exec helpers |
| `libs/data` | crypto_source, lake, timeframe, instruments, … | market data + parquet lake |
| `libs/research` | crypto_xsec, crypto_sleeves, crypto_regime, crossasset | the alpha sleeves |
| `libs/regime` | hmm, gmm, bayesian, features, engine | **new** probabilistic regime engine |
| `libs/portfolio` | construction, lifecycle, risk_parity, covariance | allocation + lifecycle (this session) |
| `libs/validation` | dsr, economic_prior (+autodiscovery.validation) | the gauntlet currently used by discovery |
| `libs/autodiscovery` | validation, models | sleeve gauntlet wrapper |
| `libs/risk` | growth_leverage, kelly, stress (partial) | leverage ladder, crisis replay |
| `libs/costs` | adv-tier costs | realistic cost model |

**Live scripts:** run_crypto_testnet (executor), watchdog, serve_dashboard, run_tunnel,
liquidation_listener, run_daily_research (chain), run_crypto_portfolio, run_discovery,
run_allocation, run_lifecycle, run_regime_engine, run_regime_allocation, run_capture_analysis,
run_derivative_backtest, run_derivative_shadow, run_crypto_shadow, run_crypto_target,
run_shadow_forward, classify_regime, collect_binance_metrics, collect_market_breadth, data_health,
build_scoreboard, run_factory_status, ingest_crypto_enriched.

---

## ORPHANED — institutional gold not wired in (227 modules)

These are the integration target. Grouped by what they deliver against the master directive.

### Risk system (Phase 6) — `libs/risk/` (14 orphaned)
`heat.py` (portfolio heat), `tail.py` (tail/ES), `stress.py`, `crisis.py`, `drawdown.py`,
`correlation.py` (correlation shock), `factor_caps.py`, `risk_budget.py`, `preservation.py`,
`scaling.py`, `sizing.py`, `vol_target.py`, `kelly.py`, `gate.py`.
→ **~all of Phase 6 exists.** Status: ORPHANED. Action: wire to the live book + executor.

### Validation engine (Phase 3) — `libs/validation/` (7 orphaned, rest CONNECTED)
`cpcv.py`, `pbo.py`, `reality_check.py`, `walk_forward.py`, `bootstrap.py`, `fdr.py` (SPA/FDR),
`gauntlet.py`, `lockbox.py`, `revalidation.py`, `stress_costs.py`, `report.py`.
→ **Phase 3 ~complete.** Currently only reached via `autodiscovery.validation`; the full
`gauntlet.py` (anchored WF + purged K-fold + MC + SPA) is ORPHANED.

### Alpha lifecycle + competition (Phase 4) — `libs/signal_engine/` (33) + `libs/alpha/` (10)
signal_engine: `alpha_competition_engine.py`, `champion_challenger.py`, `decay.py`, `crowding.py`,
`edge_estimator.py`, `expected_value.py`, `attribution.py`, `confidence_engine.py`,
`institutional_score.py`, `ranking.py`, `selection.py`, `shadow.py`, `uncertainty.py`,
`market_impact_forecaster.py`, `meta_model.py`, `factor_exposure.py`, `governance.py`, …
alpha: `registry.py` (AlphaCardStore), `manager.py` (AlphaLifecycleManager), `decay.py`,
`successor.py`, `health.py`, `ranking.py`, `card.py`, `state.py`.
→ **A near-complete alpha-management system. ORPHANED.** This is the #4/#6 spine.

### Discovery factory (Phase 2) — `libs/alpha_factory/` (21) + `libs/discovery/` (20) + `libs/autodiscovery/`
alpha_factory: `alpha_discovery_engine.py`, `hypothesis_engine.py`, `research_allocator.py`,
`capacity_intelligence.py`, `idea_ranking_engine.py`, `crowding_intelligence.py`,
`feature_drift_engine.py`, `strategy_similarity_engine.py`, `research_memory.py`, …
discovery: `factory.py`, `hypotheses.py`, `monte_carlo_survival.py`, `cagr_optimizer.py`,
`parameter_stability.py`, `fragility.py`, `tail_risk.py`, `capacity.py`, `half_life.py`,
`portfolio_geometry.py`, `regime_diversification.py`, `correlation_engine.py`.
autodiscovery: `orchestrator.py` (AutoDiscoveryLab.cycle), `generators.py` (12 generators).
→ **Massive hypothesis-generation + survival machinery. ORPHANED** (built on the MT5 `MarketSeries`
abstraction; needs a crypto data adapter).

### Meta-portfolio (Phase 5) — `libs/portfolio/` (11 orphaned)
`factor_model.py` (ShrinkageCovariance + FactorRiskModel), `optimize.py` (optimize_portfolio),
`hrp.py`, `multiperiod.py`, `analytics.py`, `exposures.py`, `diversification.py`, `constraints.py`,
`rebalance.py`, `engine.py`, `models.py`.
→ **Phase 5 optimizer set exists. ORPHANED** (uses `AlphaInput` models; thin adapter needed).

### Research OS (Phase 7) — `libs/store/` (5) + `libs/features/` (8) + `libs/monitoring/` (5)
store: `registries.py`, `trials.py`, `snapshots.py`, `hashchain.py`, `config_versions.py`,
`migrations.py`, `audit.py` (reproducibility + experiment/trial tracking).
features: `registry.py`, `pit.py` (point-in-time), `labels.py`, `definition.py`, `builtin.py`.
monitoring: `monitor.py`, `alerting.py`, `metrics_store.py`.
→ **Experiment/feature/model registries + reproducibility exist. ORPHANED.**

### Self-improvement + staged builds — `libs/self_improvement/` (15), `libs/stage14/15/14_5` (35)
Older staged research scaffolding. Mostly ORPHANED; several DUPLICATE newer crypto logic.

---

## DEPRECATED scripts (MT5/old stack — superseded by the crypto path)
run_campaign, run_carry_harvest, run_crossasset_robust, run_xsec_funding, run_xsec_funding_max,
run_funding_8h, run_portfolio_live, run_live_demo, run_demo, ingest_etfs, ingest_multiasset,
ingest_history, log_swaps, run_supervisor, run_worker, smoke_orchestration, run_prediction_markets,
run_research_lake, run_research_tick, run_autodiscovery, run_factory.
→ Candidates for archival once their useful logic is confirmed migrated.

**RETIRED 2026-08-13 (R0421):** `run_mt5_crossasset`, `run_mt5_funding_bridge`,
`run_mt5_portfolio` — deleted. Dead by three independent measures: zero `ops/crontab.manifest`
entries, no Python importer at any depth, and `run_daily_research.py` had already dropped them
("MT5 abandoned."). Their `reports/mt5_*/report.json` artifacts are kept as research record
(L1.17) and `build_scoreboard.py` now labels those rows RETIRED so a frozen artifact cannot read
as a live one.

**NOT deprecated — this list was wrong:** `run_crossasset_shadow` is LIVE, on cron at 06:40
(`ops/crontab.manifest:1834`), and its artifact was fresh when checked on 2026-08-13. It writes
`data/target_portfolio.json`, which `run_portfolio_live.py:169` depends on. Listing a live organ
as deprecated is how a working path gets deleted by a future cleanup.

## DUPLICATE logic to resolve
- `libs/research/crypto_regime.py` (heuristic) vs `libs/regime/` (probabilistic) — keep both, but the
  regime engine is now authoritative; crypto_regime is the cheap fallback.
- `libs/portfolio/construction.py` (new) vs `libs/portfolio/optimize.py` (orphaned) — converge on one
  meta-optimizer (Phase 5 integration).
- `libs/alpha/decay.py` vs `libs/signal_engine/decay.py` vs `libs/portfolio/lifecycle.decay_state`
  — RULED 2026-08-31, NOT duplicates: three layers of ONE stack, each with live consumers and
  real reuse. `alpha/decay.py` is the canonical decay SCORING; `self_improvement/decay_engine.py`
  maps score -> DecayLevel/action and is reused by `signal_engine/decay.py` (adapter);
  `portfolio/lifecycle.decay_state` is the slower governance layer above allocation (daily chain).
  Do NOT merge across layers — the two same-named `classify_decay` functions
  (`libs/data/decay.py` = data-source trend decay w/ bucketed sample sizes;
  `libs/self_improvement/decay_engine.py` = alpha PF/Sharpe levels) are DIFFERENT DOMAINS.

## Refactor opportunities
1. One **AlphaRegistry** as the single source of truth (wire `libs/alpha` + `libs/store`).
2. One **validation gauntlet** entrypoint (`libs/validation/gauntlet.py`) instead of the thin
   `autodiscovery.validation` wrapper.
3. One **risk engine** facade over `libs/risk/*` consumed by the executor.
4. A **crypto `MarketSeries` adapter** so the discovery factory + generators run on perps.

## ACTIVE 2026-08-16: desks/mt5 - MT5 research desk (XAUUSD)

- This is NOT the retired run_mt5_* scripts listed above. It is a NEW standalone
  desk executing on the local Windows host (C:\\Users\\dell\\mt5-research); this
  VPS directory is a visibility/coordination copy only - do not run it here.
- Read desks/mt5/docs/MT5_DESK.md + MT5_DESK_STATE.json: armed gateway, 4
  decorrelated daily breakout sleeves on XAUUSD, combined SR 2.34 backtest,
  ~116%/yr model at 0.01 lot/sleeve, worst DD -51%.
- Forward validation starts 2026-08-17 07:00 UTC (50 trades + 14 days gate).
- Crypto desk policy: FROZEN (CASHCARRY_KILL latched); exchange data feeds
  remain information inputs only. All new research is MT5.
